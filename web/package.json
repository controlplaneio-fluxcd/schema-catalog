{
  "name": "schema-catalog-web",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc -p tsconfig.worker.json --noEmit && tsc -p tsconfig.ui.json --noEmit",
    "test": "bun test",
    "gen-index": "bun scripts/gen-index.ts",
    "build": "bun scripts/gen-index.ts && bun scripts/build-ui.ts",
    "dev": "bun scripts/dev.ts",
    "serve": "bun scripts/serve.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "0.17.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260702.1",
    "@types/bun": "1.3.14",
    "typescript": "6.0.3",
    "wrangler": "4.107.0"
  }
}
