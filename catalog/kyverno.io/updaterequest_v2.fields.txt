# schema source: Kyverno v1.18.2 https://github.com/kyverno/kyverno
apiVersion <string> enum=kyverno.io/v2
kind <string> enum=UpdateRequest
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ResourceSpec is the information to identify the trigger resource.
spec.context <object> (required)	# Context represents admission request context. It is used upon admission review only and is shared across rules within the same UR.
spec.context.admissionRequestInfo <object>	# AdmissionRequestInfoObject stores the admission request and operation details
spec.context.admissionRequestInfo.admissionRequest <object>	# AdmissionRequest describes the admission.Attributes for the admission request.
spec.context.admissionRequestInfo.admissionRequest.dryRun <boolean>	# dryRun indicates that modifications will definitely not be persisted for this request. Defaults to false.
spec.context.admissionRequestInfo.admissionRequest.kind <object> (required)	# kind is the fully-qualified type of object being submitted (for example, v1.Pod or autoscaling.v1.Scale)
spec.context.admissionRequestInfo.admissionRequest.kind.group <string> (required)
spec.context.admissionRequestInfo.admissionRequest.kind.kind <string> (required)
spec.context.admissionRequestInfo.admissionRequest.kind.version <string> (required)
spec.context.admissionRequestInfo.admissionRequest.name <string>	# name is the name of the object as presented in the request. On a CREATE operation, the client may omit name and rely on the server to generate the name. If that is the case, this field will contain an empty string.
spec.context.admissionRequestInfo.admissionRequest.namespace <string>	# namespace is the namespace associated with the request (if any).
spec.context.admissionRequestInfo.admissionRequest.object <object (free-form)>	# object is the object from the incoming request.
spec.context.admissionRequestInfo.admissionRequest.oldObject <object (free-form)>	# oldObject is the existing object. Only populated for DELETE and UPDATE requests.
spec.context.admissionRequestInfo.admissionRequest.operation <string> (required)	# operation is the operation being performed. This may be different than the operation requested. e.g. a patch can result in either a CREATE or UPDATE Operation.
spec.context.admissionRequestInfo.admissionRequest.options <object (free-form)>	# options is the operation option structure of the operation being performed. e.g. `meta.k8s.io/v1.DeleteOptions` or `meta.k8s.io/v1.CreateOptions`. This may be different than the options the caller provided. e.g. for a patch request the performed Operation might be a CREATE, in which case the Options will a `meta.k8s.io/v1.CreateOptions` even though the caller provided `meta.k8s.io/v1.PatchOptions`.
spec.context.admissionRequestInfo.admissionRequest.requestKind <object>	# requestKind is the fully-qualified type of the original API request (for example, v1.Pod or autoscaling.v1.Scale). If this is specified and differs from the value in "kind", an equivalent match and conversion was performed. For example, if deployments can be modified via apps/v1 and apps/v1beta1, and a webhook registered a rule of `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]` and `matchPolicy: Equivalent`, an API request to apps/v1beta1 deployments would be converted and sent to the webhook with `kind: {group:"apps", version:"v1", kind:"Deployment"}` (matching the rule the webhook registered for), and `requestKind: {group:"apps", version:"v1beta1", kind:"Deployment"}` (indicating the kind of the original API request). See documentation for the "matchPolicy" field in the webhook configuration type for more details.
spec.context.admissionRequestInfo.admissionRequest.requestKind.group <string> (required)
spec.context.admissionRequestInfo.admissionRequest.requestKind.kind <string> (required)
spec.context.admissionRequestInfo.admissionRequest.requestKind.version <string> (required)
spec.context.admissionRequestInfo.admissionRequest.requestResource <object>	# requestResource is the fully-qualified resource of the original API request (for example, v1.pods). If this is specified and differs from the value in "resource", an equivalent match and conversion was performed. For example, if deployments can be modified via apps/v1 and apps/v1beta1, and a webhook registered a rule of `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]` and `matchPolicy: Equivalent`, an API request to apps/v1beta1 deployments would be converted and sent to the webhook with `resource: {group:"apps", version:"v1", resource:"deployments"}` (matching the resource the webhook registered for), and `requestResource: {group:"apps", version:"v1beta1", resource:"deployments"}` (indicating the resource of the original API request). See documentation for the "matchPolicy" field in the webhook configuration type.
spec.context.admissionRequestInfo.admissionRequest.requestResource.group <string> (required)
spec.context.admissionRequestInfo.admissionRequest.requestResource.resource <string> (required)
spec.context.admissionRequestInfo.admissionRequest.requestResource.version <string> (required)
spec.context.admissionRequestInfo.admissionRequest.requestSubResource <string>	# requestSubResource is the name of the subresource of the original API request, if any (for example, "status" or "scale") If this is specified and differs from the value in "subResource", an equivalent match and conversion was performed. See documentation for the "matchPolicy" field in the webhook configuration type.
spec.context.admissionRequestInfo.admissionRequest.resource <object> (required)	# resource is the fully-qualified resource being requested (for example, v1.pods)
spec.context.admissionRequestInfo.admissionRequest.resource.group <string> (required)
spec.context.admissionRequestInfo.admissionRequest.resource.resource <string> (required)
spec.context.admissionRequestInfo.admissionRequest.resource.version <string> (required)
spec.context.admissionRequestInfo.admissionRequest.subResource <string>	# subResource is the subresource being requested, if any (for example, "status" or "scale")
spec.context.admissionRequestInfo.admissionRequest.uid <string> (required)	# uid is an identifier for the individual request/response. It allows us to distinguish instances of requests which are otherwise identical (parallel requests, requests when earlier requests did not modify etc) The UID is meant to track the round trip (request/response) between the KAS and the WebHook, not the user request. It is suitable for correlating log entries between the webhook and apiserver, for either auditing or debugging.
spec.context.admissionRequestInfo.admissionRequest.userInfo <object> (required)	# userInfo is information about the requesting user
spec.context.admissionRequestInfo.admissionRequest.userInfo.extra <map[string]array>	# Any additional information provided by the authenticator.
spec.context.admissionRequestInfo.admissionRequest.userInfo.groups <[]string>	# The names of groups this user is a part of.
spec.context.admissionRequestInfo.admissionRequest.userInfo.uid <string>	# A unique value that identifies this user across time. If this user is deleted and another user by the same name is added, they will have different UIDs.
spec.context.admissionRequestInfo.admissionRequest.userInfo.username <string>	# The name that uniquely identifies this user among all active users.
spec.context.admissionRequestInfo.operation <string>	# Operation is the type of resource operation being checked for admission control
spec.context.userInfo <object>	# RequestInfo contains permission info carried in an admission request.
spec.context.userInfo.clusterRoles <[]string>	# ClusterRoles is a list of possible clusterRoles send the request.
spec.context.userInfo.roles <[]string>	# Roles is a list of possible role send the request.
spec.context.userInfo.synchronize <boolean>	# DryRun indicates that modifications will definitely not be persisted for this request. Defaults to false.
spec.context.userInfo.userInfo <object>	# UserInfo is the userInfo carried in the admission request.
spec.context.userInfo.userInfo.extra <map[string]array>	# Any additional information provided by the authenticator.
spec.context.userInfo.userInfo.groups <[]string>	# The names of groups this user is a part of.
spec.context.userInfo.userInfo.uid <string>	# A unique value that identifies this user across time. If this user is deleted and another user by the same name is added, they will have different UIDs.
spec.context.userInfo.userInfo.username <string>	# The name that uniquely identifies this user among all active users.
spec.deleteDownstream <boolean> (required)	# DeleteDownstream represents whether the downstream needs to be deleted. Deprecated
spec.policy <string> (required)	# Specifies the name of the policy.
spec.requestType <string> enum=mutate|generate|cel-generate|cel-mutate	# Type represents request type for background processing
spec.resource <object> (required)	# ResourceSpec is the information to identify the trigger resource.
spec.resource.apiVersion <string>	# APIVersion specifies resource apiVersion.
spec.resource.kind <string>	# Kind specifies resource kind.
spec.resource.name <string>	# Name specifies the resource name.
spec.resource.namespace <string>	# Namespace specifies resource namespace.
spec.resource.uid <string>	# UID specifies the resource uid.
spec.rule <string> (required)	# Rule is the associate rule name of the current UR.
spec.ruleContext <[]object>	# RuleContext is the associate context to apply rules. optional
spec.ruleContext[].cacheRestore <boolean>	# CacheRestore indicates whether the cache should be restored.
spec.ruleContext[].deleteDownstream <boolean> (required)	# DeleteDownstream represents whether the downstream needs to be deleted.
spec.ruleContext[].rule <string> (required)	# Rule is the associate rule name of the current UR.
spec.ruleContext[].synchronize <boolean>	# Synchronize represents the sync behavior of the corresponding rule Optional. Defaults to "false" if not specified.
spec.ruleContext[].trigger <object> (required)	# ResourceSpec is the information to identify the trigger resource.
spec.ruleContext[].trigger.apiVersion <string>	# APIVersion specifies resource apiVersion.
spec.ruleContext[].trigger.kind <string>	# Kind specifies resource kind.
spec.ruleContext[].trigger.name <string>	# Name specifies the resource name.
spec.ruleContext[].trigger.namespace <string>	# Namespace specifies resource namespace.
spec.ruleContext[].trigger.uid <string>	# UID specifies the resource uid.
spec.synchronize <boolean>	# Synchronize represents the sync behavior of the corresponding rule Optional. Defaults to "false" if not specified. Deprecated, will be removed in 1.14.
status <object>	# Status contains statistics related to update request.
status.generatedResources <[]object>	# This will track the resources that are updated by the generate Policy. Will be used during clean up resources.
status.generatedResources[].apiVersion <string>	# APIVersion specifies resource apiVersion.
status.generatedResources[].kind <string>	# Kind specifies resource kind.
status.generatedResources[].name <string>	# Name specifies the resource name.
status.generatedResources[].namespace <string>	# Namespace specifies resource namespace.
status.generatedResources[].uid <string>	# UID specifies the resource uid.
status.message <string>	# Specifies request status message.
status.retryCount <integer>
status.state <string> (required)	# State represents state of the update request.
