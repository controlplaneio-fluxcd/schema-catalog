# schema source: Kyverno v1.18.2 https://github.com/kyverno/kyverno
apiVersion <string> enum=kyverno.io/v1
kind <string> enum=Policy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Spec defines policy behaviors and contains one or more rules.
spec.admission <boolean> default=true	# Admission controls if rules are applied during admission. Optional. Default value is "true".
spec.applyRules <string> enum=All|One	# ApplyRules controls how rules in a policy are applied. Rule are processed in the order of declaration. When set to `One` processing stops after a rule has been applied i.e. the rule matches and results in a pass, fail, or error. When set to `All` all rules in the policy are processed. The default is `All`.
spec.background <boolean> default=true	# Background controls if rules are applied to existing resources during a background scan. Optional. Default value is "true". The value must be set to "false" if the policy rule uses variables that are only available in the admission review request (e.g. user name).
spec.emitWarning <boolean> default=false	# EmitWarning enables API response warnings for mutate policy rules or validate policy rules with validationFailureAction set to Audit. Enabling this option will extend admission request processing times. The default value is "false".
spec.failurePolicy <string> enum=Ignore|Fail	# Deprecated, use failurePolicy under the webhookConfiguration instead.
spec.generateExisting <boolean>	# Deprecated, use generateExisting under the generate rule instead
spec.generateExistingOnPolicyUpdate <boolean>	# Deprecated, use generateExisting instead
spec.mutateExistingOnPolicyUpdate <boolean>	# Deprecated, use mutateExistingOnPolicyUpdate under the mutate rule instead
spec.rules <[]object>	# Rules is a list of Rule instances. A Policy contains multiple rules and each rule can validate, mutate, or generate resources.
spec.rules[].celPreconditions <[]object>	# CELPreconditions are used to determine if a policy rule should be applied by evaluating a set of CEL conditions. It can only be used with the validate.cel subrule
spec.rules[].celPreconditions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables: 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject' - The existing object. The value is null for CREATE requests. 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/ Required.
spec.rules[].celPreconditions[].name <string> (required)	# Name is an identifier for this match condition, used for strategic merging of MatchConditions, as well as providing an identifier for logging purposes. A good name should be descriptive of the associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and must start and end with an alphanumeric character (e.g. 'MyName', or 'my.name', or '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName') Required.
spec.rules[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
spec.rules[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
spec.rules[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
spec.rules[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
spec.rules[].context[].apiCall.data[].value <any> (required)	# Value is the data value
spec.rules[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
spec.rules[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
spec.rules[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
spec.rules[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
spec.rules[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
spec.rules[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
spec.rules[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
spec.rules[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
spec.rules[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
spec.rules[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
spec.rules[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
spec.rules[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
spec.rules[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
spec.rules[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].context[].globalReference.name <string> (required)	# Name of the global context entry
spec.rules[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
spec.rules[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
spec.rules[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.rules[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.rules[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
spec.rules[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
spec.rules[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
spec.rules[].context[].name <string> (required)	# Name is the variable name.
spec.rules[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
spec.rules[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
spec.rules[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
spec.rules[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
spec.rules[].exclude <object>	# ExcludeResources defines when this policy rule should not be applied. The exclude criteria can include resource information (e.g. kind, name, namespace, labels) and admission review request information like the name or role.
spec.rules[].exclude.all <[]object>	# All allows specifying resources which will be ANDed
spec.rules[].exclude.all[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
spec.rules[].exclude.all[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
spec.rules[].exclude.all[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
spec.rules[].exclude.all[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].exclude.all[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
spec.rules[].exclude.all[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].exclude.all[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].exclude.all[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].exclude.all[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].exclude.all[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].exclude.all[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].exclude.all[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].exclude.all[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].exclude.all[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
spec.rules[].exclude.all[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].exclude.all[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].exclude.all[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].exclude.all[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].exclude.all[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].exclude.all[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].exclude.all[].roles <[]string>	# Roles is the list of namespaced role names for the user.
spec.rules[].exclude.all[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
spec.rules[].exclude.all[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.rules[].exclude.all[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.rules[].exclude.all[].subjects[].name <string> (required)	# Name of the object being referenced.
spec.rules[].exclude.all[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.rules[].exclude.any <[]object>	# Any allows specifying resources which will be ORed
spec.rules[].exclude.any[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
spec.rules[].exclude.any[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
spec.rules[].exclude.any[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
spec.rules[].exclude.any[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].exclude.any[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
spec.rules[].exclude.any[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].exclude.any[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].exclude.any[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].exclude.any[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].exclude.any[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].exclude.any[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].exclude.any[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].exclude.any[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].exclude.any[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
spec.rules[].exclude.any[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].exclude.any[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].exclude.any[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].exclude.any[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].exclude.any[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].exclude.any[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].exclude.any[].roles <[]string>	# Roles is the list of namespaced role names for the user.
spec.rules[].exclude.any[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
spec.rules[].exclude.any[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.rules[].exclude.any[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.rules[].exclude.any[].subjects[].name <string> (required)	# Name of the object being referenced.
spec.rules[].exclude.any[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.rules[].exclude.clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
spec.rules[].exclude.resources <object>	# ResourceDescription contains information about the resource being created or modified. Requires at least one tag to be specified when under MatchResources. Specifying ResourceDescription directly under match is being deprecated. Please specify under "any" or "all" instead.
spec.rules[].exclude.resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
spec.rules[].exclude.resources.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].exclude.resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
spec.rules[].exclude.resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].exclude.resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].exclude.resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].exclude.resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].exclude.resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].exclude.resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].exclude.resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].exclude.resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].exclude.resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
spec.rules[].exclude.resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].exclude.resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].exclude.resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].exclude.resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].exclude.resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].exclude.resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].exclude.roles <[]string>	# Roles is the list of namespaced role names for the user.
spec.rules[].exclude.subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
spec.rules[].exclude.subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.rules[].exclude.subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.rules[].exclude.subjects[].name <string> (required)	# Name of the object being referenced.
spec.rules[].exclude.subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.rules[].generate <object>	# Generation is used to create new resources.
spec.rules[].generate.apiVersion <string>	# APIVersion specifies resource apiVersion.
spec.rules[].generate.clone <object>	# Clone specifies the source resource used to populate each generated resource. At most one of Data or Clone can be specified. If neither are provided, the generated resource will be created with default data only.
spec.rules[].generate.clone.name <string>	# Name specifies name of the resource.
spec.rules[].generate.clone.namespace <string>	# Namespace specifies source resource namespace.
spec.rules[].generate.cloneList <object>	# CloneList specifies the list of source resource used to populate each generated resource.
spec.rules[].generate.cloneList.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].generate.cloneList.namespace <string>	# Namespace specifies source resource namespace.
spec.rules[].generate.cloneList.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels`. wildcard characters are not supported.
spec.rules[].generate.cloneList.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].generate.cloneList.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].generate.cloneList.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].generate.cloneList.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].generate.cloneList.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].generate.data <any>	# Data provides the resource declaration used to populate each generated resource. At most one of Data or Clone must be specified. If neither are provided, the generated resource will be created with default data only.
spec.rules[].generate.foreach <[]object>	# ForEach applies generate rules to a list of sub-elements by creating a context for each entry in the list and looping over it to apply the specified logic.
spec.rules[].generate.foreach[].apiVersion <string>	# APIVersion specifies resource apiVersion.
spec.rules[].generate.foreach[].clone <object>	# Clone specifies the source resource used to populate each generated resource. At most one of Data or Clone can be specified. If neither are provided, the generated resource will be created with default data only.
spec.rules[].generate.foreach[].clone.name <string>	# Name specifies name of the resource.
spec.rules[].generate.foreach[].clone.namespace <string>	# Namespace specifies source resource namespace.
spec.rules[].generate.foreach[].cloneList <object>	# CloneList specifies the list of source resource used to populate each generated resource.
spec.rules[].generate.foreach[].cloneList.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].generate.foreach[].cloneList.namespace <string>	# Namespace specifies source resource namespace.
spec.rules[].generate.foreach[].cloneList.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels`. wildcard characters are not supported.
spec.rules[].generate.foreach[].cloneList.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].generate.foreach[].cloneList.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].generate.foreach[].cloneList.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].generate.foreach[].cloneList.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].generate.foreach[].cloneList.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].generate.foreach[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
spec.rules[].generate.foreach[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
spec.rules[].generate.foreach[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
spec.rules[].generate.foreach[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
spec.rules[].generate.foreach[].context[].apiCall.data[].value <any> (required)	# Value is the data value
spec.rules[].generate.foreach[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
spec.rules[].generate.foreach[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].generate.foreach[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
spec.rules[].generate.foreach[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
spec.rules[].generate.foreach[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
spec.rules[].generate.foreach[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
spec.rules[].generate.foreach[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
spec.rules[].generate.foreach[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
spec.rules[].generate.foreach[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
spec.rules[].generate.foreach[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
spec.rules[].generate.foreach[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
spec.rules[].generate.foreach[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
spec.rules[].generate.foreach[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
spec.rules[].generate.foreach[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
spec.rules[].generate.foreach[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].generate.foreach[].context[].globalReference.name <string> (required)	# Name of the global context entry
spec.rules[].generate.foreach[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
spec.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
spec.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
spec.rules[].generate.foreach[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
spec.rules[].generate.foreach[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
spec.rules[].generate.foreach[].context[].name <string> (required)	# Name is the variable name.
spec.rules[].generate.foreach[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
spec.rules[].generate.foreach[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
spec.rules[].generate.foreach[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
spec.rules[].generate.foreach[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
spec.rules[].generate.foreach[].data <any>	# Data provides the resource declaration used to populate each generated resource. At most one of Data or Clone must be specified. If neither are provided, the generated resource will be created with default data only.
spec.rules[].generate.foreach[].kind <string>	# Kind specifies resource kind.
spec.rules[].generate.foreach[].list <string>	# List specifies a JMESPath expression that results in one or more elements to which the validation logic is applied.
spec.rules[].generate.foreach[].name <string>	# Name specifies the resource name.
spec.rules[].generate.foreach[].namespace <string>	# Namespace specifies resource namespace.
spec.rules[].generate.foreach[].preconditions <object>	# AnyAllConditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. See: https://kyverno.io/docs/writing-policies/preconditions/
spec.rules[].generate.foreach[].preconditions.all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
spec.rules[].generate.foreach[].preconditions.all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].generate.foreach[].preconditions.all[].message <string>	# Message is an optional display message
spec.rules[].generate.foreach[].preconditions.all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].generate.foreach[].preconditions.all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].generate.foreach[].preconditions.any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
spec.rules[].generate.foreach[].preconditions.any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].generate.foreach[].preconditions.any[].message <string>	# Message is an optional display message
spec.rules[].generate.foreach[].preconditions.any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].generate.foreach[].preconditions.any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].generate.foreach[].uid <string>	# UID specifies the resource uid.
spec.rules[].generate.generateExisting <boolean>	# GenerateExisting controls whether to trigger the rule in existing resources If is set to "true" the rule will be triggered and applied to existing matched resources.
spec.rules[].generate.kind <string>	# Kind specifies resource kind.
spec.rules[].generate.name <string>	# Name specifies the resource name.
spec.rules[].generate.namespace <string>	# Namespace specifies resource namespace.
spec.rules[].generate.orphanDownstreamOnPolicyDelete <boolean>	# OrphanDownstreamOnPolicyDelete controls whether generated resources should be deleted when the rule that generated them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type. See https://kyverno.io/docs/writing-policies/generate/#data-examples. Defaults to "false" if not specified.
spec.rules[].generate.synchronize <boolean>	# Synchronize controls if generated resources should be kept in-sync with their source resource. If Synchronize is set to "true" changes to generated resources will be overwritten with resource data from Data or the resource specified in the Clone declaration. Optional. Defaults to "false" if not specified.
spec.rules[].generate.uid <string>	# UID specifies the resource uid.
spec.rules[].imageExtractors <map[string]array>	# ImageExtractors defines a mapping from kinds to ImageExtractorConfigs. This config is only valid for verifyImages rules.
spec.rules[].match <object> (required)	# MatchResources defines when this policy rule should be applied. The match criteria can include resource information (e.g. kind, name, namespace, labels) and admission review request information like the user name or role. At least one kind is required.
spec.rules[].match.all <[]object>	# All allows specifying resources which will be ANDed
spec.rules[].match.all[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
spec.rules[].match.all[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
spec.rules[].match.all[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
spec.rules[].match.all[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].match.all[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
spec.rules[].match.all[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].match.all[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].match.all[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].match.all[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].match.all[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].match.all[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].match.all[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].match.all[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].match.all[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
spec.rules[].match.all[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].match.all[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].match.all[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].match.all[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].match.all[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].match.all[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].match.all[].roles <[]string>	# Roles is the list of namespaced role names for the user.
spec.rules[].match.all[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
spec.rules[].match.all[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.rules[].match.all[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.rules[].match.all[].subjects[].name <string> (required)	# Name of the object being referenced.
spec.rules[].match.all[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.rules[].match.any <[]object>	# Any allows specifying resources which will be ORed
spec.rules[].match.any[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
spec.rules[].match.any[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
spec.rules[].match.any[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
spec.rules[].match.any[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].match.any[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
spec.rules[].match.any[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].match.any[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].match.any[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].match.any[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].match.any[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].match.any[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].match.any[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].match.any[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].match.any[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
spec.rules[].match.any[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].match.any[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].match.any[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].match.any[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].match.any[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].match.any[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].match.any[].roles <[]string>	# Roles is the list of namespaced role names for the user.
spec.rules[].match.any[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
spec.rules[].match.any[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.rules[].match.any[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.rules[].match.any[].subjects[].name <string> (required)	# Name of the object being referenced.
spec.rules[].match.any[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.rules[].match.clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
spec.rules[].match.resources <object>	# ResourceDescription contains information about the resource being created or modified. Requires at least one tag to be specified when under MatchResources. Specifying ResourceDescription directly under match is being deprecated. Please specify under "any" or "all" instead.
spec.rules[].match.resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
spec.rules[].match.resources.kinds <[]string>	# Kinds is a list of resource kinds.
spec.rules[].match.resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
spec.rules[].match.resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].match.resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].match.resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].match.resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].match.resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].match.resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].match.resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].match.resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
spec.rules[].match.resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
spec.rules[].match.resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
spec.rules[].match.resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].match.resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].match.resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].match.resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].match.resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].match.roles <[]string>	# Roles is the list of namespaced role names for the user.
spec.rules[].match.subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
spec.rules[].match.subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.rules[].match.subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.rules[].match.subjects[].name <string> (required)	# Name of the object being referenced.
spec.rules[].match.subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.rules[].mutate <object>	# Mutation is used to modify matching resources.
spec.rules[].mutate.foreach <[]object>	# ForEach applies mutation rules to a list of sub-elements by creating a context for each entry in the list and looping over it to apply the specified logic.
spec.rules[].mutate.foreach[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
spec.rules[].mutate.foreach[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
spec.rules[].mutate.foreach[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
spec.rules[].mutate.foreach[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
spec.rules[].mutate.foreach[].context[].apiCall.data[].value <any> (required)	# Value is the data value
spec.rules[].mutate.foreach[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
spec.rules[].mutate.foreach[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].mutate.foreach[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
spec.rules[].mutate.foreach[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
spec.rules[].mutate.foreach[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
spec.rules[].mutate.foreach[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
spec.rules[].mutate.foreach[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
spec.rules[].mutate.foreach[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
spec.rules[].mutate.foreach[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
spec.rules[].mutate.foreach[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
spec.rules[].mutate.foreach[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
spec.rules[].mutate.foreach[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
spec.rules[].mutate.foreach[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
spec.rules[].mutate.foreach[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
spec.rules[].mutate.foreach[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].mutate.foreach[].context[].globalReference.name <string> (required)	# Name of the global context entry
spec.rules[].mutate.foreach[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
spec.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
spec.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
spec.rules[].mutate.foreach[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
spec.rules[].mutate.foreach[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
spec.rules[].mutate.foreach[].context[].name <string> (required)	# Name is the variable name.
spec.rules[].mutate.foreach[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
spec.rules[].mutate.foreach[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
spec.rules[].mutate.foreach[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
spec.rules[].mutate.foreach[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
spec.rules[].mutate.foreach[].foreach <any>	# Foreach declares a nested foreach iterator
spec.rules[].mutate.foreach[].list <string>	# List specifies a JMESPath expression that results in one or more elements to which the validation logic is applied.
spec.rules[].mutate.foreach[].order <string> enum=Ascending|Descending	# Order defines the iteration order on the list. Can be Ascending to iterate from first to last element or Descending to iterate in from last to first element.
spec.rules[].mutate.foreach[].patchStrategicMerge <any>	# PatchStrategicMerge is a strategic merge patch used to modify resources. See https://kubernetes.io/docs/tasks/manage-kubernetes-objects/update-api-object-kubectl-patch/ and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesstrategicmerge/.
spec.rules[].mutate.foreach[].patchesJson6902 <string>	# PatchesJSON6902 is a list of RFC 6902 JSON Patch declarations used to modify resources. See https://tools.ietf.org/html/rfc6902 and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesjson6902/.
spec.rules[].mutate.foreach[].preconditions <object>	# AnyAllConditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. See: https://kyverno.io/docs/writing-policies/preconditions/
spec.rules[].mutate.foreach[].preconditions.all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
spec.rules[].mutate.foreach[].preconditions.all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].mutate.foreach[].preconditions.all[].message <string>	# Message is an optional display message
spec.rules[].mutate.foreach[].preconditions.all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].mutate.foreach[].preconditions.all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].mutate.foreach[].preconditions.any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
spec.rules[].mutate.foreach[].preconditions.any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].mutate.foreach[].preconditions.any[].message <string>	# Message is an optional display message
spec.rules[].mutate.foreach[].preconditions.any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].mutate.foreach[].preconditions.any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].mutate.mutateExistingOnPolicyUpdate <boolean>	# MutateExistingOnPolicyUpdate controls if the mutateExisting rule will be applied on policy events.
spec.rules[].mutate.patchStrategicMerge <any>	# PatchStrategicMerge is a strategic merge patch used to modify resources. See https://kubernetes.io/docs/tasks/manage-kubernetes-objects/update-api-object-kubectl-patch/ and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesstrategicmerge/.
spec.rules[].mutate.patchesJson6902 <string>	# PatchesJSON6902 is a list of RFC 6902 JSON Patch declarations used to modify resources. See https://tools.ietf.org/html/rfc6902 and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesjson6902/.
spec.rules[].mutate.targets <[]object>	# Targets defines the target resources to be mutated.
spec.rules[].mutate.targets[].apiVersion <string>	# APIVersion specifies resource apiVersion.
spec.rules[].mutate.targets[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
spec.rules[].mutate.targets[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
spec.rules[].mutate.targets[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
spec.rules[].mutate.targets[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
spec.rules[].mutate.targets[].context[].apiCall.data[].value <any> (required)	# Value is the data value
spec.rules[].mutate.targets[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
spec.rules[].mutate.targets[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].mutate.targets[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
spec.rules[].mutate.targets[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
spec.rules[].mutate.targets[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
spec.rules[].mutate.targets[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
spec.rules[].mutate.targets[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
spec.rules[].mutate.targets[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
spec.rules[].mutate.targets[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
spec.rules[].mutate.targets[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
spec.rules[].mutate.targets[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
spec.rules[].mutate.targets[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
spec.rules[].mutate.targets[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
spec.rules[].mutate.targets[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
spec.rules[].mutate.targets[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].mutate.targets[].context[].globalReference.name <string> (required)	# Name of the global context entry
spec.rules[].mutate.targets[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
spec.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
spec.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
spec.rules[].mutate.targets[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
spec.rules[].mutate.targets[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
spec.rules[].mutate.targets[].context[].name <string> (required)	# Name is the variable name.
spec.rules[].mutate.targets[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
spec.rules[].mutate.targets[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
spec.rules[].mutate.targets[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
spec.rules[].mutate.targets[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
spec.rules[].mutate.targets[].kind <string>	# Kind specifies resource kind.
spec.rules[].mutate.targets[].name <string>	# Name specifies the resource name.
spec.rules[].mutate.targets[].namespace <string>	# Namespace specifies resource namespace.
spec.rules[].mutate.targets[].preconditions <any>	# Preconditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. A direct list of conditions (without `any` or `all` statements is supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/preconditions/
spec.rules[].mutate.targets[].selector <object>	# Selector allows you to select target resources with their labels.
spec.rules[].mutate.targets[].selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].mutate.targets[].selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].mutate.targets[].selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].mutate.targets[].selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].mutate.targets[].selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].mutate.targets[].uid <string>	# UID specifies the resource uid.
spec.rules[].name <string> (required) max=63	# Name is a label to identify the rule, It must be unique within the policy.
spec.rules[].preconditions <any>	# Preconditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. A direct list of conditions (without `any` or `all` statements is supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/preconditions/
spec.rules[].reportProperties <map[string]string>	# ReportProperties are the additional properties from the rule that will be added to the policy report result
spec.rules[].skipBackgroundRequests <boolean> default=true	# SkipBackgroundRequests bypasses admission requests that are sent by the background controller. The default value is set to "true", it must be set to "false" to apply generate and mutateExisting rules to those requests.
spec.rules[].validate <object>	# Validation is used to validate matching resources.
spec.rules[].validate.allowExistingViolations <boolean> default=true	# AllowExistingViolations allows prexisting violating resources to continue violating a policy.
spec.rules[].validate.anyPattern <any>	# AnyPattern specifies list of validation patterns. At least one of the patterns must be satisfied for the validation rule to succeed.
spec.rules[].validate.assert <object (free-form)>	# Assert defines a kyverno-json assertion tree.
spec.rules[].validate.cel <object>	# CEL allows validation checks using the Common Expression Language (https://kubernetes.io/docs/reference/using-api/cel/).
spec.rules[].validate.cel.auditAnnotations <[]object>	# AuditAnnotations contains CEL expressions which are used to produce audit annotations for the audit event of the API request.
spec.rules[].validate.cel.auditAnnotations[].key <string> (required)	# key specifies the audit annotation key. The audit annotation keys of a ValidatingAdmissionPolicy must be unique. The key must be a qualified name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length. The key is combined with the resource name of the ValidatingAdmissionPolicy to construct an audit annotation key: "{ValidatingAdmissionPolicy name}/{key}". If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy and the same audit annotation key, the annotation key will be identical. In this case, the first annotation written with the key will be included in the audit event and all subsequent annotations with the same key will be discarded. Required.
spec.rules[].validate.cel.auditAnnotations[].valueExpression <string> (required)	# valueExpression represents the expression which is evaluated by CEL to produce an audit annotation value. The expression must evaluate to either a string or null value. If the expression evaluates to a string, the audit annotation is included with the string value. If the expression evaluates to null or empty string the audit annotation will be omitted. The valueExpression may be no longer than 5kb in length. If the result of the valueExpression is more than 10kb in length, it will be truncated to 10kb. If multiple ValidatingAdmissionPolicyBinding resources match an API request, then the valueExpression will be evaluated for each binding. All unique values produced by the valueExpressions will be joined together in a comma-separated list. Required.
spec.rules[].validate.cel.expressions <[]object>	# Expressions is a list of CELExpression types.
spec.rules[].validate.cel.expressions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. ref: https://github.com/google/cel-spec CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables: - 'object' - The object from the incoming request. The value is null for DELETE requests. - 'oldObject' - The existing object. The value is null for CREATE requests. - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)). - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind. - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources. - 'variables' - Map of composited variables, from its name to its lazily evaluated value. For example, a variable named 'foo' can be accessed as 'variables.foo'. - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the object. No other metadata properties are accessible. Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible. Accessible property names are escaped according to the following rules when accessed in the expression: - '__' escapes to '__underscores__' - '.' escapes to '__dot__' - '-' escapes to '__dash__' - '/' escapes to '__slash__' - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are: "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if", "import", "let", "loop", "package", "namespace", "return". Examples: - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"} - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"} - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"} Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1]. Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type: - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and non-intersecting elements in `Y` are appended, retaining their partial order. - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with non-intersecting keys are appended, retaining their partial order. Required.
spec.rules[].validate.cel.expressions[].message <string>	# Message represents the message displayed when validation fails. The message is required if the Expression contains line breaks. The message must not contain line breaks. If unset, the message is "failed rule: {Rule}". e.g. "must be a URL with the host matching spec.host" If the Expression contains line breaks. Message is required. The message must not contain line breaks. If unset, the message is "failed Expression: {Expression}".
spec.rules[].validate.cel.expressions[].messageExpression <string>	# messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails. Since messageExpression is used as a failure message, it must evaluate to a string. If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails. If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged. messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'. Example: "object.x must be less than max ("+string(params.max)+")"
spec.rules[].validate.cel.expressions[].reason <string>	# Reason represents a machine-readable description of why this validation failed. If this is the first validation in the list to fail, this reason, as well as the corresponding HTTP response code, are used in the HTTP response to the client. The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge". If not set, StatusReasonInvalid is used in the response to the client.
spec.rules[].validate.cel.generate <boolean> default=false	# Generate specifies whether to generate a Kubernetes ValidatingAdmissionPolicy from the rule. Optional. Defaults to "false" if not specified.
spec.rules[].validate.cel.paramKind <object>	# ParamKind is a tuple of Group Kind and Version.
spec.rules[].validate.cel.paramKind.apiVersion <string>	# APIVersion is the API group version the resources belong to. In format of "group/version". Required.
spec.rules[].validate.cel.paramKind.kind <string>	# Kind is the API kind the resources belong to. Required.
spec.rules[].validate.cel.paramRef <object>	# ParamRef references a parameter resource.
spec.rules[].validate.cel.paramRef.name <string>	# name is the name of the resource being referenced. One of `name` or `selector` must be set, but `name` and `selector` are mutually exclusive properties. If one is set, the other must be unset. A single parameter used for all admission requests can be configured by setting the `name` field, leaving `selector` blank, and setting namespace if `paramKind` is namespace-scoped.
spec.rules[].validate.cel.paramRef.namespace <string>	# namespace is the namespace of the referenced resource. Allows limiting the search for params to a specific namespace. Applies to both `name` and `selector` fields. A per-namespace parameter may be used by specifying a namespace-scoped `paramKind` in the policy and leaving this field empty. - If `paramKind` is cluster-scoped, this field MUST be unset. Setting this field results in a configuration error. - If `paramKind` is namespace-scoped, the namespace of the object being evaluated for admission will be used when this field is left unset. Take care that if this is left empty the binding must not match any cluster-scoped resources, which will result in an error.
spec.rules[].validate.cel.paramRef.parameterNotFoundAction <string>	# `parameterNotFoundAction` controls the behavior of the binding when the resource exists, and name or selector is valid, but there are no parameters matched by the binding. If the value is set to `Allow`, then no matched parameters will be treated as successful validation by the binding. If set to `Deny`, then no matched parameters will be subject to the `failurePolicy` of the policy. Allowed values are `Allow` or `Deny` Required
spec.rules[].validate.cel.paramRef.selector <object>	# selector can be used to match multiple param objects based on their labels. Supply selector: {} to match all resources of the ParamKind. If multiple params are found, they are all evaluated with the policy expressions and the results are ANDed together. One of `name` or `selector` must be set, but `name` and `selector` are mutually exclusive properties. If one is set, the other must be unset.
spec.rules[].validate.cel.paramRef.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].validate.cel.paramRef.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].validate.cel.paramRef.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].validate.cel.paramRef.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].validate.cel.paramRef.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].validate.cel.variables <[]object>	# Variables contain definitions of variables that can be used in composition of other expressions. Each variable is defined as a named CEL expression. The variables defined here will be available under `variables` in other expressions of the policy.
spec.rules[].validate.cel.variables[].expression <string> (required)	# Expression is the expression that will be evaluated as the value of the variable. The CEL expression has access to the same identifiers as the CEL expressions in Validation.
spec.rules[].validate.cel.variables[].name <string> (required)	# Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables. The variable can be accessed in other expressions through `variables` For example, if name is "foo", the variable will be available as `variables.foo`
spec.rules[].validate.deny <object>	# Deny defines conditions used to pass or fail a validation rule.
spec.rules[].validate.deny.conditions <any>	# Multiple conditions can be declared under an `any` or `all` statement. A direct list of conditions (without `any` or `all` statements) is also supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/validate/#deny-rules
spec.rules[].validate.failureAction <string> enum=Audit|Enforce	# FailureAction defines if a validation policy rule violation should block the admission review request (Enforce), or allow (Audit) the admission review request and report an error in a policy report. Optional. Allowed values are Audit or Enforce.
spec.rules[].validate.failureActionOverrides <[]object>	# FailureActionOverrides is a Cluster Policy attribute that specifies FailureAction namespace-wise. It overrides FailureAction for the specified namespaces.
spec.rules[].validate.failureActionOverrides[].action <string> enum=audit|enforce|Audit|Enforce	# ValidationFailureAction defines the policy validation failure action
spec.rules[].validate.failureActionOverrides[].namespaceSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rules[].validate.failureActionOverrides[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rules[].validate.failureActionOverrides[].namespaces <[]string>
spec.rules[].validate.foreach <[]object>	# ForEach applies validate rules to a list of sub-elements by creating a context for each entry in the list and looping over it to apply the specified logic.
spec.rules[].validate.foreach[].anyPattern <any>	# AnyPattern specifies list of validation patterns. At least one of the patterns must be satisfied for the validation rule to succeed.
spec.rules[].validate.foreach[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
spec.rules[].validate.foreach[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
spec.rules[].validate.foreach[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
spec.rules[].validate.foreach[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
spec.rules[].validate.foreach[].context[].apiCall.data[].value <any> (required)	# Value is the data value
spec.rules[].validate.foreach[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
spec.rules[].validate.foreach[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].validate.foreach[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
spec.rules[].validate.foreach[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
spec.rules[].validate.foreach[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
spec.rules[].validate.foreach[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
spec.rules[].validate.foreach[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
spec.rules[].validate.foreach[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
spec.rules[].validate.foreach[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
spec.rules[].validate.foreach[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
spec.rules[].validate.foreach[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
spec.rules[].validate.foreach[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
spec.rules[].validate.foreach[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
spec.rules[].validate.foreach[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
spec.rules[].validate.foreach[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
spec.rules[].validate.foreach[].context[].globalReference.name <string> (required)	# Name of the global context entry
spec.rules[].validate.foreach[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
spec.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
spec.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
spec.rules[].validate.foreach[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
spec.rules[].validate.foreach[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
spec.rules[].validate.foreach[].context[].name <string> (required)	# Name is the variable name.
spec.rules[].validate.foreach[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
spec.rules[].validate.foreach[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
spec.rules[].validate.foreach[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
spec.rules[].validate.foreach[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
spec.rules[].validate.foreach[].deny <object>	# Deny defines conditions used to pass or fail a validation rule.
spec.rules[].validate.foreach[].deny.conditions <any>	# Multiple conditions can be declared under an `any` or `all` statement. A direct list of conditions (without `any` or `all` statements) is also supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/validate/#deny-rules
spec.rules[].validate.foreach[].elementScope <boolean>	# ElementScope specifies whether to use the current list element as the scope for validation. Defaults to "true" if not specified. When set to "false", "request.object" is used as the validation scope within the foreach block to allow referencing other elements in the subtree.
spec.rules[].validate.foreach[].foreach <any>	# Foreach declares a nested foreach iterator
spec.rules[].validate.foreach[].list <string>	# List specifies a JMESPath expression that results in one or more elements to which the validation logic is applied.
spec.rules[].validate.foreach[].pattern <any>	# Pattern specifies an overlay-style pattern used to check resources.
spec.rules[].validate.foreach[].preconditions <object>	# AnyAllConditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. See: https://kyverno.io/docs/writing-policies/preconditions/
spec.rules[].validate.foreach[].preconditions.all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
spec.rules[].validate.foreach[].preconditions.all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].validate.foreach[].preconditions.all[].message <string>	# Message is an optional display message
spec.rules[].validate.foreach[].preconditions.all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].validate.foreach[].preconditions.all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].validate.foreach[].preconditions.any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
spec.rules[].validate.foreach[].preconditions.any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].validate.foreach[].preconditions.any[].message <string>	# Message is an optional display message
spec.rules[].validate.foreach[].preconditions.any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].validate.foreach[].preconditions.any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].validate.manifests <object>	# Manifest specifies conditions for manifest verification
spec.rules[].validate.manifests.annotationDomain <string>	# AnnotationDomain is custom domain of annotation for message and signature. Default is "cosign.sigstore.dev".
spec.rules[].validate.manifests.attestors <[]object>	# Attestors specified the required attestors (i.e. authorities)
spec.rules[].validate.manifests.attestors[].count <integer> min=1	# Count specifies the required number of entries that must match. If the count is null, all entries must match (a logical AND). If the count is 1, at least one entry must match (a logical OR). If the count contains a value N, then N must be less than or equal to the size of entries, and at least N entries must match.
spec.rules[].validate.manifests.attestors[].entries <[]object>	# Entries contains the available attestors. An attestor can be a static key, attributes for keyless verification, or a nested attestor declaration.
spec.rules[].validate.manifests.attestors[].entries[].annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
spec.rules[].validate.manifests.attestors[].entries[].attestor <any>	# Attestor is a nested set of Attestor used to specify a more complex set of match authorities.
spec.rules[].validate.manifests.attestors[].entries[].certificates <object>	# Certificates specifies one or more certificates.
spec.rules[].validate.manifests.attestors[].entries[].certificates.cert <string>	# Cert is an optional PEM-encoded public certificate.
spec.rules[].validate.manifests.attestors[].entries[].certificates.certChain <string>	# CertChain is an optional PEM encoded set of certificates used to verify.
spec.rules[].validate.manifests.attestors[].entries[].certificates.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].validate.manifests.attestors[].entries[].certificates.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].validate.manifests.attestors[].entries[].certificates.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].validate.manifests.attestors[].entries[].certificates.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].validate.manifests.attestors[].entries[].certificates.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].validate.manifests.attestors[].entries[].certificates.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].validate.manifests.attestors[].entries[].certificates.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].validate.manifests.attestors[].entries[].certificates.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].validate.manifests.attestors[].entries[].keyless <object>	# Keyless is a set of attribute used to verify a Sigstore keyless attestor. See https://github.com/sigstore/cosign/blob/main/KEYLESS.md.
spec.rules[].validate.manifests.attestors[].entries[].keyless.additionalExtensions <map[string]string>	# AdditionalExtensions are certificate-extensions used for keyless signing.
spec.rules[].validate.manifests.attestors[].entries[].keyless.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].validate.manifests.attestors[].entries[].keyless.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].validate.manifests.attestors[].entries[].keyless.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].validate.manifests.attestors[].entries[].keyless.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].validate.manifests.attestors[].entries[].keyless.issuer <string>	# Issuer is the certificate issuer used for keyless signing.
spec.rules[].validate.manifests.attestors[].entries[].keyless.issuerRegExp <string>	# IssuerRegExp is the regular expression to match certificate issuer used for keyless signing.
spec.rules[].validate.manifests.attestors[].entries[].keyless.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].validate.manifests.attestors[].entries[].keyless.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].validate.manifests.attestors[].entries[].keyless.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].validate.manifests.attestors[].entries[].keyless.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].validate.manifests.attestors[].entries[].keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
spec.rules[].validate.manifests.attestors[].entries[].keyless.subject <string>	# Subject is the verified identity used for keyless signing, for example the email address.
spec.rules[].validate.manifests.attestors[].entries[].keyless.subjectRegExp <string>	# SubjectRegExp is the regular expression to match identity used for keyless signing, for example the email address.
spec.rules[].validate.manifests.attestors[].entries[].keys <object>	# Keys specifies one or more public keys.
spec.rules[].validate.manifests.attestors[].entries[].keys.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].validate.manifests.attestors[].entries[].keys.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].validate.manifests.attestors[].entries[].keys.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].validate.manifests.attestors[].entries[].keys.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].validate.manifests.attestors[].entries[].keys.kms <string>	# KMS provides the URI to the public key stored in a Key Management System. See: https://github.com/sigstore/cosign/blob/main/KMS.md
spec.rules[].validate.manifests.attestors[].entries[].keys.publicKeys <string>	# Keys is a set of X.509 public keys used to verify image signatures. The keys can be directly specified or can be a variable reference to a key specified in a ConfigMap (see https://kyverno.io/docs/writing-policies/variables/), or reference a standard Kubernetes Secret elsewhere in the cluster by specifying it in the format "k8s://<namespace>/<secret_name>". The named Secret must specify a key `cosign.pub` containing the public key used for verification, (see https://github.com/sigstore/cosign/blob/main/KMS.md#kubernetes-secret). When multiple keys are specified each key is processed as a separate staticKey entry (.attestors[*].entries.keys) within the set of attestors and the count is applied across the keys.
spec.rules[].validate.manifests.attestors[].entries[].keys.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].validate.manifests.attestors[].entries[].keys.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].validate.manifests.attestors[].entries[].keys.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].validate.manifests.attestors[].entries[].keys.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].validate.manifests.attestors[].entries[].keys.secret <object>	# Reference to a Secret resource that contains a public key
spec.rules[].validate.manifests.attestors[].entries[].keys.secret.name <string> (required)	# Name of the secret. The provided secret must contain a key named cosign.pub.
spec.rules[].validate.manifests.attestors[].entries[].keys.secret.namespace <string> (required)	# Namespace name where the Secret exists.
spec.rules[].validate.manifests.attestors[].entries[].keys.signatureAlgorithm <string> default="sha256"	# Deprecated. Use attestor.signatureAlgorithm instead.
spec.rules[].validate.manifests.attestors[].entries[].repository <string>	# Repository is an optional alternate OCI repository to use for signatures and attestations that match this rule. If specified Repository will override other OCI image repository locations for this Attestor.
spec.rules[].validate.manifests.attestors[].entries[].signatureAlgorithm <string> default="sha256"	# Specify signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512.
spec.rules[].validate.manifests.dryRun <object>	# DryRun configuration
spec.rules[].validate.manifests.dryRun.enable <boolean>
spec.rules[].validate.manifests.dryRun.namespace <string>
spec.rules[].validate.manifests.ignoreFields <[]object>	# Fields which will be ignored while comparing manifests.
spec.rules[].validate.manifests.ignoreFields[].fields <[]string>
spec.rules[].validate.manifests.ignoreFields[].objects <[]object>
spec.rules[].validate.manifests.ignoreFields[].objects[].group <string>
spec.rules[].validate.manifests.ignoreFields[].objects[].kind <string>
spec.rules[].validate.manifests.ignoreFields[].objects[].name <string>
spec.rules[].validate.manifests.ignoreFields[].objects[].namespace <string>
spec.rules[].validate.manifests.ignoreFields[].objects[].version <string>
spec.rules[].validate.manifests.repository <string>	# Repository is an optional alternate OCI repository to use for resource bundle reference. The repository can be overridden per Attestor or Attestation.
spec.rules[].validate.message <string>	# Message specifies a custom message to be displayed on failure.
spec.rules[].validate.pattern <any>	# Pattern specifies an overlay-style pattern used to check resources.
spec.rules[].validate.podSecurity <object>	# PodSecurity applies exemptions for Kubernetes Pod Security admission by specifying exclusions for Pod Security Standards controls.
spec.rules[].validate.podSecurity.exclude <[]object>	# Exclude specifies the Pod Security Standard controls to be excluded.
spec.rules[].validate.podSecurity.exclude[].controlName <string> (required) enum=HostProcess|"Host Namespaces"|"Privileged Containers"|Capabilities|"HostPath Volumes"|"Host Ports"|AppArmor|SELinux|"/proc Mount Type"|Seccomp|Sysctls|"Volume Types"|"Privilege Escalation"|"Running as Non-root"|"Running as Non-root user"	# ControlName specifies the name of the Pod Security Standard control. See: https://kubernetes.io/docs/concepts/security/pod-security-standards/
spec.rules[].validate.podSecurity.exclude[].images <[]string>	# Images selects matching containers and applies the container level PSS. Each image is the image name consisting of the registry address, repository, image, and tag. Empty list matches no containers, PSS checks are applied at the pod level only. Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
spec.rules[].validate.podSecurity.exclude[].restrictedField <string>	# RestrictedField selects the field for the given Pod Security Standard control. When not set, all restricted fields for the control are selected.
spec.rules[].validate.podSecurity.exclude[].values <[]string>	# Values defines the allowed values that can be excluded.
spec.rules[].validate.podSecurity.level <string> enum=privileged|baseline|restricted	# Level defines the Pod Security Standard level to be applied to workloads. Allowed values are privileged, baseline, and restricted.
spec.rules[].validate.podSecurity.version <string> enum=v1.19|v1.20|v1.21|v1.22|v1.23|v1.24|v1.25|v1.26|v1.27|v1.28|v1.29|v1.30|v1.31|v1.32|latest	# Version defines the Pod Security Standard versions that Kubernetes supports. Allowed values are v1.19, v1.20, v1.21, v1.22, v1.23, v1.24, v1.25, v1.26, v1.27, v1.28, v1.29, v1.30, v1.31, v1.32, latest. Defaults to latest.
spec.rules[].verifyImages <[]object>	# VerifyImages is used to verify image signatures and mutate them to add a digest
spec.rules[].verifyImages[].additionalExtensions <map[string]string>	# Deprecated.
spec.rules[].verifyImages[].annotations <map[string]string>	# Deprecated. Use annotations per Attestor instead.
spec.rules[].verifyImages[].attestations <[]object>	# Attestations are optional checks for signed in-toto Statements used to verify the image. See https://github.com/in-toto/attestation. Kyverno fetches signed attestations from the OCI registry and decodes them into a list of Statement declarations.
spec.rules[].verifyImages[].attestations[].attestors <[]object>	# Attestors specify the required attestors (i.e. authorities).
spec.rules[].verifyImages[].attestations[].attestors[].count <integer> min=1	# Count specifies the required number of entries that must match. If the count is null, all entries must match (a logical AND). If the count is 1, at least one entry must match (a logical OR). If the count contains a value N, then N must be less than or equal to the size of entries, and at least N entries must match.
spec.rules[].verifyImages[].attestations[].attestors[].entries <[]object>	# Entries contains the available attestors. An attestor can be a static key, attributes for keyless verification, or a nested attestor declaration.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].attestor <any>	# Attestor is a nested set of Attestor used to specify a more complex set of match authorities.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates <object>	# Certificates specifies one or more certificates.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.cert <string>	# Cert is an optional PEM-encoded public certificate.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.certChain <string>	# CertChain is an optional PEM encoded set of certificates used to verify.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless <object>	# Keyless is a set of attribute used to verify a Sigstore keyless attestor. See https://github.com/sigstore/cosign/blob/main/KEYLESS.md.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.additionalExtensions <map[string]string>	# AdditionalExtensions are certificate-extensions used for keyless signing.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.issuer <string>	# Issuer is the certificate issuer used for keyless signing.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.issuerRegExp <string>	# IssuerRegExp is the regular expression to match certificate issuer used for keyless signing.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.subject <string>	# Subject is the verified identity used for keyless signing, for example the email address.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.subjectRegExp <string>	# SubjectRegExp is the regular expression to match identity used for keyless signing, for example the email address.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys <object>	# Keys specifies one or more public keys.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.kms <string>	# KMS provides the URI to the public key stored in a Key Management System. See: https://github.com/sigstore/cosign/blob/main/KMS.md
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.publicKeys <string>	# Keys is a set of X.509 public keys used to verify image signatures. The keys can be directly specified or can be a variable reference to a key specified in a ConfigMap (see https://kyverno.io/docs/writing-policies/variables/), or reference a standard Kubernetes Secret elsewhere in the cluster by specifying it in the format "k8s://<namespace>/<secret_name>". The named Secret must specify a key `cosign.pub` containing the public key used for verification, (see https://github.com/sigstore/cosign/blob/main/KMS.md#kubernetes-secret). When multiple keys are specified each key is processed as a separate staticKey entry (.attestors[*].entries.keys) within the set of attestors and the count is applied across the keys.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.secret <object>	# Reference to a Secret resource that contains a public key
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.secret.name <string> (required)	# Name of the secret. The provided secret must contain a key named cosign.pub.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.secret.namespace <string> (required)	# Namespace name where the Secret exists.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].keys.signatureAlgorithm <string> default="sha256"	# Deprecated. Use attestor.signatureAlgorithm instead.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].repository <string>	# Repository is an optional alternate OCI repository to use for signatures and attestations that match this rule. If specified Repository will override other OCI image repository locations for this Attestor.
spec.rules[].verifyImages[].attestations[].attestors[].entries[].signatureAlgorithm <string> default="sha256"	# Specify signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512.
spec.rules[].verifyImages[].attestations[].conditions <[]object>	# Conditions are used to verify attributes within a Predicate. If no Conditions are specified the attestation check is satisfied as long there are predicates that match the predicate type.
spec.rules[].verifyImages[].attestations[].conditions[].all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
spec.rules[].verifyImages[].attestations[].conditions[].all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].verifyImages[].attestations[].conditions[].all[].message <string>	# Message is an optional display message
spec.rules[].verifyImages[].attestations[].conditions[].all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].verifyImages[].attestations[].conditions[].all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].verifyImages[].attestations[].conditions[].any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
spec.rules[].verifyImages[].attestations[].conditions[].any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
spec.rules[].verifyImages[].attestations[].conditions[].any[].message <string>	# Message is an optional display message
spec.rules[].verifyImages[].attestations[].conditions[].any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
spec.rules[].verifyImages[].attestations[].conditions[].any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
spec.rules[].verifyImages[].attestations[].name <string>	# Name is the variable name.
spec.rules[].verifyImages[].attestations[].predicateType <string>	# Deprecated in favour of 'Type', to be removed soon
spec.rules[].verifyImages[].attestations[].type <string>	# Type defines the type of attestation contained within the Statement.
spec.rules[].verifyImages[].attestors <[]object>	# Attestors specified the required attestors (i.e. authorities)
spec.rules[].verifyImages[].attestors[].count <integer> min=1	# Count specifies the required number of entries that must match. If the count is null, all entries must match (a logical AND). If the count is 1, at least one entry must match (a logical OR). If the count contains a value N, then N must be less than or equal to the size of entries, and at least N entries must match.
spec.rules[].verifyImages[].attestors[].entries <[]object>	# Entries contains the available attestors. An attestor can be a static key, attributes for keyless verification, or a nested attestor declaration.
spec.rules[].verifyImages[].attestors[].entries[].annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
spec.rules[].verifyImages[].attestors[].entries[].attestor <any>	# Attestor is a nested set of Attestor used to specify a more complex set of match authorities.
spec.rules[].verifyImages[].attestors[].entries[].certificates <object>	# Certificates specifies one or more certificates.
spec.rules[].verifyImages[].attestors[].entries[].certificates.cert <string>	# Cert is an optional PEM-encoded public certificate.
spec.rules[].verifyImages[].attestors[].entries[].certificates.certChain <string>	# CertChain is an optional PEM encoded set of certificates used to verify.
spec.rules[].verifyImages[].attestors[].entries[].certificates.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].verifyImages[].attestors[].entries[].certificates.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].verifyImages[].attestors[].entries[].certificates.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].verifyImages[].attestors[].entries[].certificates.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].verifyImages[].attestors[].entries[].certificates.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].verifyImages[].attestors[].entries[].certificates.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].verifyImages[].attestors[].entries[].certificates.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].verifyImages[].attestors[].entries[].certificates.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].verifyImages[].attestors[].entries[].keyless <object>	# Keyless is a set of attribute used to verify a Sigstore keyless attestor. See https://github.com/sigstore/cosign/blob/main/KEYLESS.md.
spec.rules[].verifyImages[].attestors[].entries[].keyless.additionalExtensions <map[string]string>	# AdditionalExtensions are certificate-extensions used for keyless signing.
spec.rules[].verifyImages[].attestors[].entries[].keyless.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].verifyImages[].attestors[].entries[].keyless.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].verifyImages[].attestors[].entries[].keyless.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].verifyImages[].attestors[].entries[].keyless.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].verifyImages[].attestors[].entries[].keyless.issuer <string>	# Issuer is the certificate issuer used for keyless signing.
spec.rules[].verifyImages[].attestors[].entries[].keyless.issuerRegExp <string>	# IssuerRegExp is the regular expression to match certificate issuer used for keyless signing.
spec.rules[].verifyImages[].attestors[].entries[].keyless.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].verifyImages[].attestors[].entries[].keyless.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].verifyImages[].attestors[].entries[].keyless.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].verifyImages[].attestors[].entries[].keyless.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].verifyImages[].attestors[].entries[].keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
spec.rules[].verifyImages[].attestors[].entries[].keyless.subject <string>	# Subject is the verified identity used for keyless signing, for example the email address.
spec.rules[].verifyImages[].attestors[].entries[].keyless.subjectRegExp <string>	# SubjectRegExp is the regular expression to match identity used for keyless signing, for example the email address.
spec.rules[].verifyImages[].attestors[].entries[].keys <object>	# Keys specifies one or more public keys.
spec.rules[].verifyImages[].attestors[].entries[].keys.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
spec.rules[].verifyImages[].attestors[].entries[].keys.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.rules[].verifyImages[].attestors[].entries[].keys.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
spec.rules[].verifyImages[].attestors[].entries[].keys.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.rules[].verifyImages[].attestors[].entries[].keys.kms <string>	# KMS provides the URI to the public key stored in a Key Management System. See: https://github.com/sigstore/cosign/blob/main/KMS.md
spec.rules[].verifyImages[].attestors[].entries[].keys.publicKeys <string>	# Keys is a set of X.509 public keys used to verify image signatures. The keys can be directly specified or can be a variable reference to a key specified in a ConfigMap (see https://kyverno.io/docs/writing-policies/variables/), or reference a standard Kubernetes Secret elsewhere in the cluster by specifying it in the format "k8s://<namespace>/<secret_name>". The named Secret must specify a key `cosign.pub` containing the public key used for verification, (see https://github.com/sigstore/cosign/blob/main/KMS.md#kubernetes-secret). When multiple keys are specified each key is processed as a separate staticKey entry (.attestors[*].entries.keys) within the set of attestors and the count is applied across the keys.
spec.rules[].verifyImages[].attestors[].entries[].keys.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
spec.rules[].verifyImages[].attestors[].entries[].keys.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
spec.rules[].verifyImages[].attestors[].entries[].keys.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.rules[].verifyImages[].attestors[].entries[].keys.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
spec.rules[].verifyImages[].attestors[].entries[].keys.secret <object>	# Reference to a Secret resource that contains a public key
spec.rules[].verifyImages[].attestors[].entries[].keys.secret.name <string> (required)	# Name of the secret. The provided secret must contain a key named cosign.pub.
spec.rules[].verifyImages[].attestors[].entries[].keys.secret.namespace <string> (required)	# Namespace name where the Secret exists.
spec.rules[].verifyImages[].attestors[].entries[].keys.signatureAlgorithm <string> default="sha256"	# Deprecated. Use attestor.signatureAlgorithm instead.
spec.rules[].verifyImages[].attestors[].entries[].repository <string>	# Repository is an optional alternate OCI repository to use for signatures and attestations that match this rule. If specified Repository will override other OCI image repository locations for this Attestor.
spec.rules[].verifyImages[].attestors[].entries[].signatureAlgorithm <string> default="sha256"	# Specify signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512.
spec.rules[].verifyImages[].cosignOCI11 <boolean>	# CosignOCI11 enables the experimental OCI 1.1 behaviour in cosign image verification. Defaults to false.
spec.rules[].verifyImages[].failureAction <string> enum=Audit|Enforce	# Allowed values are Audit or Enforce.
spec.rules[].verifyImages[].image <string>	# Deprecated. Use ImageReferences instead.
spec.rules[].verifyImages[].imageReferences <[]string>	# ImageReferences is a list of matching image reference patterns. At least one pattern in the list must match the image for the rule to apply. Each image reference consists of a registry address (defaults to docker.io), repository, image, and tag (defaults to latest). Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
spec.rules[].verifyImages[].imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry.
spec.rules[].verifyImages[].imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.rules[].verifyImages[].imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.rules[].verifyImages[].imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
spec.rules[].verifyImages[].issuer <string>	# Deprecated. Use KeylessAttestor instead.
spec.rules[].verifyImages[].key <string>	# Deprecated. Use StaticKeyAttestor instead.
spec.rules[].verifyImages[].mutateDigest <boolean> default=true	# MutateDigest enables replacement of image tags with digests. Defaults to true.
spec.rules[].verifyImages[].repository <string>	# Repository is an optional alternate OCI repository to use for image signatures and attestations that match this rule. If specified Repository will override the default OCI image repository configured for the installation. The repository can also be overridden per Attestor or Attestation.
spec.rules[].verifyImages[].required <boolean> default=true	# Required validates that images are verified i.e. have matched passed a signature or attestation check.
spec.rules[].verifyImages[].roots <string>	# Deprecated. Use KeylessAttestor instead.
spec.rules[].verifyImages[].skipImageReferences <[]string>	# SkipImageReferences is a list of matching image reference patterns that should be skipped. At least one pattern in the list must match the image for the rule to be skipped. Each image reference consists of a registry address (defaults to docker.io), repository, image, and tag (defaults to latest). Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
spec.rules[].verifyImages[].subject <string>	# Deprecated. Use KeylessAttestor instead.
spec.rules[].verifyImages[].type <string> enum=Cosign|SigstoreBundle|Notary	# Type specifies the method of signature validation. The allowed options are Cosign, Sigstore Bundle and Notary. By default Cosign is used if a type is not specified.
spec.rules[].verifyImages[].useCache <boolean> default=true	# UseCache enables caching of image verify responses for this rule.
spec.rules[].verifyImages[].validate <object>	# Validation checks conditions across multiple image verification attestations or context entries
spec.rules[].verifyImages[].validate.deny <object>	# Deny defines conditions used to pass or fail a validation rule.
spec.rules[].verifyImages[].validate.deny.conditions <any>	# Multiple conditions can be declared under an `any` or `all` statement. A direct list of conditions (without `any` or `all` statements) is also supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/validate/#deny-rules
spec.rules[].verifyImages[].validate.message <string>	# Message specifies a custom message to be displayed on failure.
spec.rules[].verifyImages[].verifyDigest <boolean> default=true	# VerifyDigest validates that images have a digest.
spec.schemaValidation <boolean>	# Deprecated.
spec.useServerSideApply <boolean>	# UseServerSideApply controls whether to use server-side apply for generate rules If is set to "true" create & update for generate rules will use apply instead of create/update. Defaults to "false" if not specified.
spec.validationFailureAction <string> enum=audit|enforce|Audit|Enforce default="Audit"	# Deprecated, use validationFailureAction under the validate rule instead.
spec.validationFailureActionOverrides <[]object>	# Deprecated, use validationFailureActionOverrides under the validate rule instead.
spec.validationFailureActionOverrides[].action <string> enum=audit|enforce|Audit|Enforce	# ValidationFailureAction defines the policy validation failure action
spec.validationFailureActionOverrides[].namespaceSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.validationFailureActionOverrides[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.validationFailureActionOverrides[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.validationFailureActionOverrides[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.validationFailureActionOverrides[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.validationFailureActionOverrides[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.validationFailureActionOverrides[].namespaces <[]string>
spec.webhookConfiguration <object>	# WebhookConfiguration specifies the custom configuration for Kubernetes admission webhookconfiguration.
spec.webhookConfiguration.failurePolicy <string> enum=Ignore|Fail	# FailurePolicy defines how unexpected policy errors and webhook response timeout errors are handled. Rules within the same policy share the same failure behavior. This field should not be accessed directly, instead `GetFailurePolicy()` should be used. Allowed values are Ignore or Fail. Defaults to Fail.
spec.webhookConfiguration.matchConditions <[]object>	# MatchCondition configures admission webhook matchConditions. Requires Kubernetes 1.27 or later.
spec.webhookConfiguration.matchConditions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables: 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject' - The existing object. The value is null for CREATE requests. 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/ Required.
spec.webhookConfiguration.matchConditions[].name <string> (required)	# Name is an identifier for this match condition, used for strategic merging of MatchConditions, as well as providing an identifier for logging purposes. A good name should be descriptive of the associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and must start and end with an alphanumeric character (e.g. 'MyName', or 'my.name', or '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName') Required.
spec.webhookConfiguration.timeoutSeconds <integer> format=int32	# TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy. After the configured time expires, the admission request may fail, or may simply ignore the policy results, based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
spec.webhookTimeoutSeconds <integer> format=int32	# Deprecated, use webhookTimeoutSeconds under webhookConfiguration instead.
status <object>	# Deprecated. Policy metrics are available via the metrics endpoint
status.autogen <object>	# AutogenStatus contains autogen status information.
status.autogen.rules <[]object>	# Rules is a list of Rule instances. It contains auto generated rules added for pod controllers
status.autogen.rules[].celPreconditions <[]object>	# CELPreconditions are used to determine if a policy rule should be applied by evaluating a set of CEL conditions. It can only be used with the validate.cel subrule
status.autogen.rules[].celPreconditions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables: 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject' - The existing object. The value is null for CREATE requests. 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/ Required.
status.autogen.rules[].celPreconditions[].name <string> (required)	# Name is an identifier for this match condition, used for strategic merging of MatchConditions, as well as providing an identifier for logging purposes. A good name should be descriptive of the associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and must start and end with an alphanumeric character (e.g. 'MyName', or 'my.name', or '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName') Required.
status.autogen.rules[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
status.autogen.rules[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
status.autogen.rules[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
status.autogen.rules[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
status.autogen.rules[].context[].apiCall.data[].value <any> (required)	# Value is the data value
status.autogen.rules[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
status.autogen.rules[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
status.autogen.rules[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
status.autogen.rules[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
status.autogen.rules[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
status.autogen.rules[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
status.autogen.rules[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
status.autogen.rules[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
status.autogen.rules[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
status.autogen.rules[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
status.autogen.rules[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
status.autogen.rules[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
status.autogen.rules[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
status.autogen.rules[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].context[].globalReference.name <string> (required)	# Name of the global context entry
status.autogen.rules[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
status.autogen.rules[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
status.autogen.rules[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.rules[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.rules[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
status.autogen.rules[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
status.autogen.rules[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
status.autogen.rules[].context[].name <string> (required)	# Name is the variable name.
status.autogen.rules[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
status.autogen.rules[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
status.autogen.rules[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
status.autogen.rules[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
status.autogen.rules[].exclude <object>	# ExcludeResources defines when this policy rule should not be applied. The exclude criteria can include resource information (e.g. kind, name, namespace, labels) and admission review request information like the name or role.
status.autogen.rules[].exclude.all <[]object>	# All allows specifying resources which will be ANDed
status.autogen.rules[].exclude.all[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
status.autogen.rules[].exclude.all[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
status.autogen.rules[].exclude.all[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
status.autogen.rules[].exclude.all[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].exclude.all[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
status.autogen.rules[].exclude.all[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].exclude.all[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].exclude.all[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].exclude.all[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].exclude.all[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].exclude.all[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].exclude.all[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].exclude.all[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].exclude.all[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
status.autogen.rules[].exclude.all[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].exclude.all[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].exclude.all[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].exclude.all[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].exclude.all[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].exclude.all[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].exclude.all[].roles <[]string>	# Roles is the list of namespaced role names for the user.
status.autogen.rules[].exclude.all[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
status.autogen.rules[].exclude.all[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
status.autogen.rules[].exclude.all[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
status.autogen.rules[].exclude.all[].subjects[].name <string> (required)	# Name of the object being referenced.
status.autogen.rules[].exclude.all[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
status.autogen.rules[].exclude.any <[]object>	# Any allows specifying resources which will be ORed
status.autogen.rules[].exclude.any[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
status.autogen.rules[].exclude.any[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
status.autogen.rules[].exclude.any[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
status.autogen.rules[].exclude.any[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].exclude.any[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
status.autogen.rules[].exclude.any[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].exclude.any[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].exclude.any[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].exclude.any[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].exclude.any[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].exclude.any[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].exclude.any[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].exclude.any[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].exclude.any[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
status.autogen.rules[].exclude.any[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].exclude.any[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].exclude.any[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].exclude.any[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].exclude.any[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].exclude.any[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].exclude.any[].roles <[]string>	# Roles is the list of namespaced role names for the user.
status.autogen.rules[].exclude.any[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
status.autogen.rules[].exclude.any[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
status.autogen.rules[].exclude.any[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
status.autogen.rules[].exclude.any[].subjects[].name <string> (required)	# Name of the object being referenced.
status.autogen.rules[].exclude.any[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
status.autogen.rules[].exclude.clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
status.autogen.rules[].exclude.resources <object>	# ResourceDescription contains information about the resource being created or modified. Requires at least one tag to be specified when under MatchResources. Specifying ResourceDescription directly under match is being deprecated. Please specify under "any" or "all" instead.
status.autogen.rules[].exclude.resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
status.autogen.rules[].exclude.resources.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].exclude.resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
status.autogen.rules[].exclude.resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].exclude.resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].exclude.resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].exclude.resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].exclude.resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].exclude.resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].exclude.resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].exclude.resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].exclude.resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
status.autogen.rules[].exclude.resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].exclude.resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].exclude.resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].exclude.resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].exclude.resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].exclude.resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].exclude.roles <[]string>	# Roles is the list of namespaced role names for the user.
status.autogen.rules[].exclude.subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
status.autogen.rules[].exclude.subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
status.autogen.rules[].exclude.subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
status.autogen.rules[].exclude.subjects[].name <string> (required)	# Name of the object being referenced.
status.autogen.rules[].exclude.subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
status.autogen.rules[].generate <object>	# Generation is used to create new resources.
status.autogen.rules[].generate.apiVersion <string>	# APIVersion specifies resource apiVersion.
status.autogen.rules[].generate.clone <object>	# Clone specifies the source resource used to populate each generated resource. At most one of Data or Clone can be specified. If neither are provided, the generated resource will be created with default data only.
status.autogen.rules[].generate.clone.name <string>	# Name specifies name of the resource.
status.autogen.rules[].generate.clone.namespace <string>	# Namespace specifies source resource namespace.
status.autogen.rules[].generate.cloneList <object>	# CloneList specifies the list of source resource used to populate each generated resource.
status.autogen.rules[].generate.cloneList.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].generate.cloneList.namespace <string>	# Namespace specifies source resource namespace.
status.autogen.rules[].generate.cloneList.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels`. wildcard characters are not supported.
status.autogen.rules[].generate.cloneList.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].generate.cloneList.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].generate.cloneList.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].generate.cloneList.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].generate.cloneList.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].generate.data <any>	# Data provides the resource declaration used to populate each generated resource. At most one of Data or Clone must be specified. If neither are provided, the generated resource will be created with default data only.
status.autogen.rules[].generate.foreach <[]object>	# ForEach applies generate rules to a list of sub-elements by creating a context for each entry in the list and looping over it to apply the specified logic.
status.autogen.rules[].generate.foreach[].apiVersion <string>	# APIVersion specifies resource apiVersion.
status.autogen.rules[].generate.foreach[].clone <object>	# Clone specifies the source resource used to populate each generated resource. At most one of Data or Clone can be specified. If neither are provided, the generated resource will be created with default data only.
status.autogen.rules[].generate.foreach[].clone.name <string>	# Name specifies name of the resource.
status.autogen.rules[].generate.foreach[].clone.namespace <string>	# Namespace specifies source resource namespace.
status.autogen.rules[].generate.foreach[].cloneList <object>	# CloneList specifies the list of source resource used to populate each generated resource.
status.autogen.rules[].generate.foreach[].cloneList.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].generate.foreach[].cloneList.namespace <string>	# Namespace specifies source resource namespace.
status.autogen.rules[].generate.foreach[].cloneList.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels`. wildcard characters are not supported.
status.autogen.rules[].generate.foreach[].cloneList.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].generate.foreach[].cloneList.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].generate.foreach[].cloneList.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].generate.foreach[].cloneList.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].generate.foreach[].cloneList.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].generate.foreach[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
status.autogen.rules[].generate.foreach[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
status.autogen.rules[].generate.foreach[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
status.autogen.rules[].generate.foreach[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
status.autogen.rules[].generate.foreach[].context[].apiCall.data[].value <any> (required)	# Value is the data value
status.autogen.rules[].generate.foreach[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
status.autogen.rules[].generate.foreach[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].generate.foreach[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
status.autogen.rules[].generate.foreach[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
status.autogen.rules[].generate.foreach[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
status.autogen.rules[].generate.foreach[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
status.autogen.rules[].generate.foreach[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
status.autogen.rules[].generate.foreach[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
status.autogen.rules[].generate.foreach[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
status.autogen.rules[].generate.foreach[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
status.autogen.rules[].generate.foreach[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
status.autogen.rules[].generate.foreach[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
status.autogen.rules[].generate.foreach[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
status.autogen.rules[].generate.foreach[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
status.autogen.rules[].generate.foreach[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].generate.foreach[].context[].globalReference.name <string> (required)	# Name of the global context entry
status.autogen.rules[].generate.foreach[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
status.autogen.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
status.autogen.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.rules[].generate.foreach[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
status.autogen.rules[].generate.foreach[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
status.autogen.rules[].generate.foreach[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
status.autogen.rules[].generate.foreach[].context[].name <string> (required)	# Name is the variable name.
status.autogen.rules[].generate.foreach[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
status.autogen.rules[].generate.foreach[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
status.autogen.rules[].generate.foreach[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
status.autogen.rules[].generate.foreach[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
status.autogen.rules[].generate.foreach[].data <any>	# Data provides the resource declaration used to populate each generated resource. At most one of Data or Clone must be specified. If neither are provided, the generated resource will be created with default data only.
status.autogen.rules[].generate.foreach[].kind <string>	# Kind specifies resource kind.
status.autogen.rules[].generate.foreach[].list <string>	# List specifies a JMESPath expression that results in one or more elements to which the validation logic is applied.
status.autogen.rules[].generate.foreach[].name <string>	# Name specifies the resource name.
status.autogen.rules[].generate.foreach[].namespace <string>	# Namespace specifies resource namespace.
status.autogen.rules[].generate.foreach[].preconditions <object>	# AnyAllConditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. See: https://kyverno.io/docs/writing-policies/preconditions/
status.autogen.rules[].generate.foreach[].preconditions.all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
status.autogen.rules[].generate.foreach[].preconditions.all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].generate.foreach[].preconditions.all[].message <string>	# Message is an optional display message
status.autogen.rules[].generate.foreach[].preconditions.all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].generate.foreach[].preconditions.all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].generate.foreach[].preconditions.any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
status.autogen.rules[].generate.foreach[].preconditions.any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].generate.foreach[].preconditions.any[].message <string>	# Message is an optional display message
status.autogen.rules[].generate.foreach[].preconditions.any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].generate.foreach[].preconditions.any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].generate.foreach[].uid <string>	# UID specifies the resource uid.
status.autogen.rules[].generate.generateExisting <boolean>	# GenerateExisting controls whether to trigger the rule in existing resources If is set to "true" the rule will be triggered and applied to existing matched resources.
status.autogen.rules[].generate.kind <string>	# Kind specifies resource kind.
status.autogen.rules[].generate.name <string>	# Name specifies the resource name.
status.autogen.rules[].generate.namespace <string>	# Namespace specifies resource namespace.
status.autogen.rules[].generate.orphanDownstreamOnPolicyDelete <boolean>	# OrphanDownstreamOnPolicyDelete controls whether generated resources should be deleted when the rule that generated them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type. See https://kyverno.io/docs/writing-policies/generate/#data-examples. Defaults to "false" if not specified.
status.autogen.rules[].generate.synchronize <boolean>	# Synchronize controls if generated resources should be kept in-sync with their source resource. If Synchronize is set to "true" changes to generated resources will be overwritten with resource data from Data or the resource specified in the Clone declaration. Optional. Defaults to "false" if not specified.
status.autogen.rules[].generate.uid <string>	# UID specifies the resource uid.
status.autogen.rules[].imageExtractors <map[string]array>	# ImageExtractors defines a mapping from kinds to ImageExtractorConfigs. This config is only valid for verifyImages rules.
status.autogen.rules[].match <object> (required)	# MatchResources defines when this policy rule should be applied. The match criteria can include resource information (e.g. kind, name, namespace, labels) and admission review request information like the user name or role. At least one kind is required.
status.autogen.rules[].match.all <[]object>	# All allows specifying resources which will be ANDed
status.autogen.rules[].match.all[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
status.autogen.rules[].match.all[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
status.autogen.rules[].match.all[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
status.autogen.rules[].match.all[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].match.all[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
status.autogen.rules[].match.all[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].match.all[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].match.all[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].match.all[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].match.all[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].match.all[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].match.all[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].match.all[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].match.all[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
status.autogen.rules[].match.all[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].match.all[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].match.all[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].match.all[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].match.all[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].match.all[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].match.all[].roles <[]string>	# Roles is the list of namespaced role names for the user.
status.autogen.rules[].match.all[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
status.autogen.rules[].match.all[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
status.autogen.rules[].match.all[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
status.autogen.rules[].match.all[].subjects[].name <string> (required)	# Name of the object being referenced.
status.autogen.rules[].match.all[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
status.autogen.rules[].match.any <[]object>	# Any allows specifying resources which will be ORed
status.autogen.rules[].match.any[].clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
status.autogen.rules[].match.any[].resources <object>	# ResourceDescription contains information about the resource being created or modified.
status.autogen.rules[].match.any[].resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
status.autogen.rules[].match.any[].resources.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].match.any[].resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
status.autogen.rules[].match.any[].resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].match.any[].resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].match.any[].resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].match.any[].resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].match.any[].resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].match.any[].resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].match.any[].resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].match.any[].resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].match.any[].resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
status.autogen.rules[].match.any[].resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].match.any[].resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].match.any[].resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].match.any[].resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].match.any[].resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].match.any[].resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].match.any[].roles <[]string>	# Roles is the list of namespaced role names for the user.
status.autogen.rules[].match.any[].subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
status.autogen.rules[].match.any[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
status.autogen.rules[].match.any[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
status.autogen.rules[].match.any[].subjects[].name <string> (required)	# Name of the object being referenced.
status.autogen.rules[].match.any[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
status.autogen.rules[].match.clusterRoles <[]string>	# ClusterRoles is the list of cluster-wide role names for the user.
status.autogen.rules[].match.resources <object>	# ResourceDescription contains information about the resource being created or modified. Requires at least one tag to be specified when under MatchResources. Specifying ResourceDescription directly under match is being deprecated. Please specify under "any" or "all" instead.
status.autogen.rules[].match.resources.annotations <map[string]string>	# Annotations is a map of annotations (key-value pairs of type string). Annotation keys and values support the wildcard characters "*" (matches zero or many characters) and "?" (matches at least one character).
status.autogen.rules[].match.resources.kinds <[]string>	# Kinds is a list of resource kinds.
status.autogen.rules[].match.resources.name <string>	# Name is the name of the resource. The name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character). NOTE: "Name" is being deprecated in favor of "Names".
status.autogen.rules[].match.resources.names <[]string>	# Names are the names of the resources. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].match.resources.namespaceSelector <object>	# NamespaceSelector is a label selector for the resource namespace. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character).Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].match.resources.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].match.resources.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].match.resources.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].match.resources.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].match.resources.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].match.resources.namespaces <[]string>	# Namespaces is a list of namespaces names. Each name supports wildcard characters "*" (matches zero or many characters) and "?" (at least one character).
status.autogen.rules[].match.resources.operations <[]string>	# Operations can contain values ["CREATE, "UPDATE", "CONNECT", "DELETE"], which are used to match a specific action.
status.autogen.rules[].match.resources.selector <object>	# Selector is a label selector. Label keys and values in `matchLabels` support the wildcard characters `*` (matches zero or many characters) and `?` (matches one character). Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but does not match an empty label set.
status.autogen.rules[].match.resources.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].match.resources.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].match.resources.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].match.resources.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].match.resources.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].match.roles <[]string>	# Roles is the list of namespaced role names for the user.
status.autogen.rules[].match.subjects <[]object>	# Subjects is the list of subject names like users, user groups, and service accounts.
status.autogen.rules[].match.subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
status.autogen.rules[].match.subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
status.autogen.rules[].match.subjects[].name <string> (required)	# Name of the object being referenced.
status.autogen.rules[].match.subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
status.autogen.rules[].mutate <object>	# Mutation is used to modify matching resources.
status.autogen.rules[].mutate.foreach <[]object>	# ForEach applies mutation rules to a list of sub-elements by creating a context for each entry in the list and looping over it to apply the specified logic.
status.autogen.rules[].mutate.foreach[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
status.autogen.rules[].mutate.foreach[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
status.autogen.rules[].mutate.foreach[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
status.autogen.rules[].mutate.foreach[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
status.autogen.rules[].mutate.foreach[].context[].apiCall.data[].value <any> (required)	# Value is the data value
status.autogen.rules[].mutate.foreach[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
status.autogen.rules[].mutate.foreach[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].mutate.foreach[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
status.autogen.rules[].mutate.foreach[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
status.autogen.rules[].mutate.foreach[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
status.autogen.rules[].mutate.foreach[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
status.autogen.rules[].mutate.foreach[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
status.autogen.rules[].mutate.foreach[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
status.autogen.rules[].mutate.foreach[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
status.autogen.rules[].mutate.foreach[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
status.autogen.rules[].mutate.foreach[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
status.autogen.rules[].mutate.foreach[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
status.autogen.rules[].mutate.foreach[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
status.autogen.rules[].mutate.foreach[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
status.autogen.rules[].mutate.foreach[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].mutate.foreach[].context[].globalReference.name <string> (required)	# Name of the global context entry
status.autogen.rules[].mutate.foreach[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
status.autogen.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
status.autogen.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.rules[].mutate.foreach[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
status.autogen.rules[].mutate.foreach[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
status.autogen.rules[].mutate.foreach[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
status.autogen.rules[].mutate.foreach[].context[].name <string> (required)	# Name is the variable name.
status.autogen.rules[].mutate.foreach[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
status.autogen.rules[].mutate.foreach[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
status.autogen.rules[].mutate.foreach[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
status.autogen.rules[].mutate.foreach[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
status.autogen.rules[].mutate.foreach[].foreach <any>	# Foreach declares a nested foreach iterator
status.autogen.rules[].mutate.foreach[].list <string>	# List specifies a JMESPath expression that results in one or more elements to which the validation logic is applied.
status.autogen.rules[].mutate.foreach[].order <string> enum=Ascending|Descending	# Order defines the iteration order on the list. Can be Ascending to iterate from first to last element or Descending to iterate in from last to first element.
status.autogen.rules[].mutate.foreach[].patchStrategicMerge <any>	# PatchStrategicMerge is a strategic merge patch used to modify resources. See https://kubernetes.io/docs/tasks/manage-kubernetes-objects/update-api-object-kubectl-patch/ and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesstrategicmerge/.
status.autogen.rules[].mutate.foreach[].patchesJson6902 <string>	# PatchesJSON6902 is a list of RFC 6902 JSON Patch declarations used to modify resources. See https://tools.ietf.org/html/rfc6902 and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesjson6902/.
status.autogen.rules[].mutate.foreach[].preconditions <object>	# AnyAllConditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. See: https://kyverno.io/docs/writing-policies/preconditions/
status.autogen.rules[].mutate.foreach[].preconditions.all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
status.autogen.rules[].mutate.foreach[].preconditions.all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].mutate.foreach[].preconditions.all[].message <string>	# Message is an optional display message
status.autogen.rules[].mutate.foreach[].preconditions.all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].mutate.foreach[].preconditions.all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].mutate.foreach[].preconditions.any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
status.autogen.rules[].mutate.foreach[].preconditions.any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].mutate.foreach[].preconditions.any[].message <string>	# Message is an optional display message
status.autogen.rules[].mutate.foreach[].preconditions.any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].mutate.foreach[].preconditions.any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].mutate.mutateExistingOnPolicyUpdate <boolean>	# MutateExistingOnPolicyUpdate controls if the mutateExisting rule will be applied on policy events.
status.autogen.rules[].mutate.patchStrategicMerge <any>	# PatchStrategicMerge is a strategic merge patch used to modify resources. See https://kubernetes.io/docs/tasks/manage-kubernetes-objects/update-api-object-kubectl-patch/ and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesstrategicmerge/.
status.autogen.rules[].mutate.patchesJson6902 <string>	# PatchesJSON6902 is a list of RFC 6902 JSON Patch declarations used to modify resources. See https://tools.ietf.org/html/rfc6902 and https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/patchesjson6902/.
status.autogen.rules[].mutate.targets <[]object>	# Targets defines the target resources to be mutated.
status.autogen.rules[].mutate.targets[].apiVersion <string>	# APIVersion specifies resource apiVersion.
status.autogen.rules[].mutate.targets[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
status.autogen.rules[].mutate.targets[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
status.autogen.rules[].mutate.targets[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
status.autogen.rules[].mutate.targets[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
status.autogen.rules[].mutate.targets[].context[].apiCall.data[].value <any> (required)	# Value is the data value
status.autogen.rules[].mutate.targets[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
status.autogen.rules[].mutate.targets[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].mutate.targets[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
status.autogen.rules[].mutate.targets[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
status.autogen.rules[].mutate.targets[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
status.autogen.rules[].mutate.targets[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
status.autogen.rules[].mutate.targets[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
status.autogen.rules[].mutate.targets[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
status.autogen.rules[].mutate.targets[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
status.autogen.rules[].mutate.targets[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
status.autogen.rules[].mutate.targets[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
status.autogen.rules[].mutate.targets[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
status.autogen.rules[].mutate.targets[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
status.autogen.rules[].mutate.targets[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
status.autogen.rules[].mutate.targets[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].mutate.targets[].context[].globalReference.name <string> (required)	# Name of the global context entry
status.autogen.rules[].mutate.targets[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
status.autogen.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
status.autogen.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.rules[].mutate.targets[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
status.autogen.rules[].mutate.targets[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
status.autogen.rules[].mutate.targets[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
status.autogen.rules[].mutate.targets[].context[].name <string> (required)	# Name is the variable name.
status.autogen.rules[].mutate.targets[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
status.autogen.rules[].mutate.targets[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
status.autogen.rules[].mutate.targets[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
status.autogen.rules[].mutate.targets[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
status.autogen.rules[].mutate.targets[].kind <string>	# Kind specifies resource kind.
status.autogen.rules[].mutate.targets[].name <string>	# Name specifies the resource name.
status.autogen.rules[].mutate.targets[].namespace <string>	# Namespace specifies resource namespace.
status.autogen.rules[].mutate.targets[].preconditions <any>	# Preconditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. A direct list of conditions (without `any` or `all` statements is supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/preconditions/
status.autogen.rules[].mutate.targets[].selector <object>	# Selector allows you to select target resources with their labels.
status.autogen.rules[].mutate.targets[].selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].mutate.targets[].selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].mutate.targets[].selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].mutate.targets[].selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].mutate.targets[].selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].mutate.targets[].uid <string>	# UID specifies the resource uid.
status.autogen.rules[].name <string> (required) max=63	# Name is a label to identify the rule, It must be unique within the policy.
status.autogen.rules[].preconditions <any>	# Preconditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. A direct list of conditions (without `any` or `all` statements is supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/preconditions/
status.autogen.rules[].reportProperties <map[string]string>	# ReportProperties are the additional properties from the rule that will be added to the policy report result
status.autogen.rules[].skipBackgroundRequests <boolean> default=true	# SkipBackgroundRequests bypasses admission requests that are sent by the background controller. The default value is set to "true", it must be set to "false" to apply generate and mutateExisting rules to those requests.
status.autogen.rules[].validate <object>	# Validation is used to validate matching resources.
status.autogen.rules[].validate.allowExistingViolations <boolean> default=true	# AllowExistingViolations allows prexisting violating resources to continue violating a policy.
status.autogen.rules[].validate.anyPattern <any>	# AnyPattern specifies list of validation patterns. At least one of the patterns must be satisfied for the validation rule to succeed.
status.autogen.rules[].validate.assert <object (free-form)>	# Assert defines a kyverno-json assertion tree.
status.autogen.rules[].validate.cel <object>	# CEL allows validation checks using the Common Expression Language (https://kubernetes.io/docs/reference/using-api/cel/).
status.autogen.rules[].validate.cel.auditAnnotations <[]object>	# AuditAnnotations contains CEL expressions which are used to produce audit annotations for the audit event of the API request.
status.autogen.rules[].validate.cel.auditAnnotations[].key <string> (required)	# key specifies the audit annotation key. The audit annotation keys of a ValidatingAdmissionPolicy must be unique. The key must be a qualified name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length. The key is combined with the resource name of the ValidatingAdmissionPolicy to construct an audit annotation key: "{ValidatingAdmissionPolicy name}/{key}". If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy and the same audit annotation key, the annotation key will be identical. In this case, the first annotation written with the key will be included in the audit event and all subsequent annotations with the same key will be discarded. Required.
status.autogen.rules[].validate.cel.auditAnnotations[].valueExpression <string> (required)	# valueExpression represents the expression which is evaluated by CEL to produce an audit annotation value. The expression must evaluate to either a string or null value. If the expression evaluates to a string, the audit annotation is included with the string value. If the expression evaluates to null or empty string the audit annotation will be omitted. The valueExpression may be no longer than 5kb in length. If the result of the valueExpression is more than 10kb in length, it will be truncated to 10kb. If multiple ValidatingAdmissionPolicyBinding resources match an API request, then the valueExpression will be evaluated for each binding. All unique values produced by the valueExpressions will be joined together in a comma-separated list. Required.
status.autogen.rules[].validate.cel.expressions <[]object>	# Expressions is a list of CELExpression types.
status.autogen.rules[].validate.cel.expressions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. ref: https://github.com/google/cel-spec CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables: - 'object' - The object from the incoming request. The value is null for DELETE requests. - 'oldObject' - The existing object. The value is null for CREATE requests. - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)). - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind. - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources. - 'variables' - Map of composited variables, from its name to its lazily evaluated value. For example, a variable named 'foo' can be accessed as 'variables.foo'. - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the object. No other metadata properties are accessible. Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible. Accessible property names are escaped according to the following rules when accessed in the expression: - '__' escapes to '__underscores__' - '.' escapes to '__dot__' - '-' escapes to '__dash__' - '/' escapes to '__slash__' - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are: "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if", "import", "let", "loop", "package", "namespace", "return". Examples: - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"} - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"} - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"} Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1]. Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type: - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and non-intersecting elements in `Y` are appended, retaining their partial order. - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with non-intersecting keys are appended, retaining their partial order. Required.
status.autogen.rules[].validate.cel.expressions[].message <string>	# Message represents the message displayed when validation fails. The message is required if the Expression contains line breaks. The message must not contain line breaks. If unset, the message is "failed rule: {Rule}". e.g. "must be a URL with the host matching spec.host" If the Expression contains line breaks. Message is required. The message must not contain line breaks. If unset, the message is "failed Expression: {Expression}".
status.autogen.rules[].validate.cel.expressions[].messageExpression <string>	# messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails. Since messageExpression is used as a failure message, it must evaluate to a string. If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails. If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged. messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'. Example: "object.x must be less than max ("+string(params.max)+")"
status.autogen.rules[].validate.cel.expressions[].reason <string>	# Reason represents a machine-readable description of why this validation failed. If this is the first validation in the list to fail, this reason, as well as the corresponding HTTP response code, are used in the HTTP response to the client. The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge". If not set, StatusReasonInvalid is used in the response to the client.
status.autogen.rules[].validate.cel.generate <boolean> default=false	# Generate specifies whether to generate a Kubernetes ValidatingAdmissionPolicy from the rule. Optional. Defaults to "false" if not specified.
status.autogen.rules[].validate.cel.paramKind <object>	# ParamKind is a tuple of Group Kind and Version.
status.autogen.rules[].validate.cel.paramKind.apiVersion <string>	# APIVersion is the API group version the resources belong to. In format of "group/version". Required.
status.autogen.rules[].validate.cel.paramKind.kind <string>	# Kind is the API kind the resources belong to. Required.
status.autogen.rules[].validate.cel.paramRef <object>	# ParamRef references a parameter resource.
status.autogen.rules[].validate.cel.paramRef.name <string>	# name is the name of the resource being referenced. One of `name` or `selector` must be set, but `name` and `selector` are mutually exclusive properties. If one is set, the other must be unset. A single parameter used for all admission requests can be configured by setting the `name` field, leaving `selector` blank, and setting namespace if `paramKind` is namespace-scoped.
status.autogen.rules[].validate.cel.paramRef.namespace <string>	# namespace is the namespace of the referenced resource. Allows limiting the search for params to a specific namespace. Applies to both `name` and `selector` fields. A per-namespace parameter may be used by specifying a namespace-scoped `paramKind` in the policy and leaving this field empty. - If `paramKind` is cluster-scoped, this field MUST be unset. Setting this field results in a configuration error. - If `paramKind` is namespace-scoped, the namespace of the object being evaluated for admission will be used when this field is left unset. Take care that if this is left empty the binding must not match any cluster-scoped resources, which will result in an error.
status.autogen.rules[].validate.cel.paramRef.parameterNotFoundAction <string>	# `parameterNotFoundAction` controls the behavior of the binding when the resource exists, and name or selector is valid, but there are no parameters matched by the binding. If the value is set to `Allow`, then no matched parameters will be treated as successful validation by the binding. If set to `Deny`, then no matched parameters will be subject to the `failurePolicy` of the policy. Allowed values are `Allow` or `Deny` Required
status.autogen.rules[].validate.cel.paramRef.selector <object>	# selector can be used to match multiple param objects based on their labels. Supply selector: {} to match all resources of the ParamKind. If multiple params are found, they are all evaluated with the policy expressions and the results are ANDed together. One of `name` or `selector` must be set, but `name` and `selector` are mutually exclusive properties. If one is set, the other must be unset.
status.autogen.rules[].validate.cel.paramRef.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].validate.cel.paramRef.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].validate.cel.paramRef.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].validate.cel.paramRef.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].validate.cel.paramRef.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].validate.cel.variables <[]object>	# Variables contain definitions of variables that can be used in composition of other expressions. Each variable is defined as a named CEL expression. The variables defined here will be available under `variables` in other expressions of the policy.
status.autogen.rules[].validate.cel.variables[].expression <string> (required)	# Expression is the expression that will be evaluated as the value of the variable. The CEL expression has access to the same identifiers as the CEL expressions in Validation.
status.autogen.rules[].validate.cel.variables[].name <string> (required)	# Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables. The variable can be accessed in other expressions through `variables` For example, if name is "foo", the variable will be available as `variables.foo`
status.autogen.rules[].validate.deny <object>	# Deny defines conditions used to pass or fail a validation rule.
status.autogen.rules[].validate.deny.conditions <any>	# Multiple conditions can be declared under an `any` or `all` statement. A direct list of conditions (without `any` or `all` statements) is also supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/validate/#deny-rules
status.autogen.rules[].validate.failureAction <string> enum=Audit|Enforce	# FailureAction defines if a validation policy rule violation should block the admission review request (Enforce), or allow (Audit) the admission review request and report an error in a policy report. Optional. Allowed values are Audit or Enforce.
status.autogen.rules[].validate.failureActionOverrides <[]object>	# FailureActionOverrides is a Cluster Policy attribute that specifies FailureAction namespace-wise. It overrides FailureAction for the specified namespaces.
status.autogen.rules[].validate.failureActionOverrides[].action <string> enum=audit|enforce|Audit|Enforce	# ValidationFailureAction defines the policy validation failure action
status.autogen.rules[].validate.failureActionOverrides[].namespaceSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
status.autogen.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.rules[].validate.failureActionOverrides[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.rules[].validate.failureActionOverrides[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.rules[].validate.failureActionOverrides[].namespaces <[]string>
status.autogen.rules[].validate.foreach <[]object>	# ForEach applies validate rules to a list of sub-elements by creating a context for each entry in the list and looping over it to apply the specified logic.
status.autogen.rules[].validate.foreach[].anyPattern <any>	# AnyPattern specifies list of validation patterns. At least one of the patterns must be satisfied for the validation rule to succeed.
status.autogen.rules[].validate.foreach[].context <[]object>	# Context defines variables and data sources that can be used during rule execution.
status.autogen.rules[].validate.foreach[].context[].apiCall <object>	# APICall is an HTTP request to the Kubernetes API server, or other JSON web service. The data returned is stored in the context with the name for the context entry.
status.autogen.rules[].validate.foreach[].context[].apiCall.data <[]object>	# The data object specifies the POST data sent to the server. Only applicable when the method field is set to POST.
status.autogen.rules[].validate.foreach[].context[].apiCall.data[].key <string> (required)	# Key is a unique identifier for the data value
status.autogen.rules[].validate.foreach[].context[].apiCall.data[].value <any> (required)	# Value is the data value
status.autogen.rules[].validate.foreach[].context[].apiCall.default <any>	# Default is an optional arbitrary JSON object that the context value is set to, if the apiCall returns error.
status.autogen.rules[].validate.foreach[].context[].apiCall.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].validate.foreach[].context[].apiCall.method <string> enum=GET|POST default="GET"	# Method is the HTTP request type (GET or POST). Defaults to GET.
status.autogen.rules[].validate.foreach[].context[].apiCall.service <object>	# Service is an API call to a JSON web service. This is used for non-Kubernetes API server calls. It's mutually exclusive with the URLPath field.
status.autogen.rules[].validate.foreach[].context[].apiCall.service.caBundle <string>	# CABundle is a PEM encoded CA bundle which will be used to validate the server certificate.
status.autogen.rules[].validate.foreach[].context[].apiCall.service.headers <[]object>	# Headers is a list of optional HTTP headers to be included in the request.
status.autogen.rules[].validate.foreach[].context[].apiCall.service.headers[].key <string> (required)	# Key is the header key
status.autogen.rules[].validate.foreach[].context[].apiCall.service.headers[].value <string> (required)	# Value is the header value
status.autogen.rules[].validate.foreach[].context[].apiCall.service.url <string> (required)	# URL is the JSON web service URL. A typical form is `https://{service}.{namespace}:{port}/{path}`.
status.autogen.rules[].validate.foreach[].context[].apiCall.urlPath <string>	# URLPath is the URL path to be used in the HTTP GET or POST request to the Kubernetes API server (e.g. "/api/v1/namespaces" or "/apis/apps/v1/deployments"). The format required is the same format used by the `kubectl get --raw` command. See https://kyverno.io/docs/writing-policies/external-data-sources/#variables-from-kubernetes-api-server-calls for details. It's mutually exclusive with the Service field.
status.autogen.rules[].validate.foreach[].context[].configMap <object>	# ConfigMap is the ConfigMap reference.
status.autogen.rules[].validate.foreach[].context[].configMap.name <string> (required)	# Name is the ConfigMap name.
status.autogen.rules[].validate.foreach[].context[].configMap.namespace <string>	# Namespace is the ConfigMap namespace.
status.autogen.rules[].validate.foreach[].context[].globalReference <object>	# GlobalContextEntryReference is a reference to a cached global context entry.
status.autogen.rules[].validate.foreach[].context[].globalReference.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the JSON response returned from the server. For example a JMESPath of "items | length(@)" applied to the API server response for the URLPath "/apis/apps/v1/deployments" will return the total count of deployments across all namespaces.
status.autogen.rules[].validate.foreach[].context[].globalReference.name <string> (required)	# Name of the global context entry
status.autogen.rules[].validate.foreach[].context[].imageRegistry <object>	# ImageRegistry defines requests to an OCI/Docker V2 registry to fetch image details.
status.autogen.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry
status.autogen.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.rules[].validate.foreach[].context[].imageRegistry.imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
status.autogen.rules[].validate.foreach[].context[].imageRegistry.jmesPath <string>	# JMESPath is an optional JSON Match Expression that can be used to transform the ImageData struct returned as a result of processing the image reference.
status.autogen.rules[].validate.foreach[].context[].imageRegistry.reference <string> (required)	# Reference is image reference to a container image in the registry. Example: ghcr.io/kyverno/kyverno:latest
status.autogen.rules[].validate.foreach[].context[].name <string> (required)	# Name is the variable name.
status.autogen.rules[].validate.foreach[].context[].variable <object>	# Variable defines an arbitrary JMESPath context variable that can be defined inline.
status.autogen.rules[].validate.foreach[].context[].variable.default <any>	# Default is an optional arbitrary JSON object that the variable may take if the JMESPath expression evaluates to nil
status.autogen.rules[].validate.foreach[].context[].variable.jmesPath <string>	# JMESPath is an optional JMESPath Expression that can be used to transform the variable.
status.autogen.rules[].validate.foreach[].context[].variable.value <any>	# Value is any arbitrary JSON object representable in YAML or JSON form.
status.autogen.rules[].validate.foreach[].deny <object>	# Deny defines conditions used to pass or fail a validation rule.
status.autogen.rules[].validate.foreach[].deny.conditions <any>	# Multiple conditions can be declared under an `any` or `all` statement. A direct list of conditions (without `any` or `all` statements) is also supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/validate/#deny-rules
status.autogen.rules[].validate.foreach[].elementScope <boolean>	# ElementScope specifies whether to use the current list element as the scope for validation. Defaults to "true" if not specified. When set to "false", "request.object" is used as the validation scope within the foreach block to allow referencing other elements in the subtree.
status.autogen.rules[].validate.foreach[].foreach <any>	# Foreach declares a nested foreach iterator
status.autogen.rules[].validate.foreach[].list <string>	# List specifies a JMESPath expression that results in one or more elements to which the validation logic is applied.
status.autogen.rules[].validate.foreach[].pattern <any>	# Pattern specifies an overlay-style pattern used to check resources.
status.autogen.rules[].validate.foreach[].preconditions <object>	# AnyAllConditions are used to determine if a policy rule should be applied by evaluating a set of conditions. The declaration can contain nested `any` or `all` statements. See: https://kyverno.io/docs/writing-policies/preconditions/
status.autogen.rules[].validate.foreach[].preconditions.all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
status.autogen.rules[].validate.foreach[].preconditions.all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].validate.foreach[].preconditions.all[].message <string>	# Message is an optional display message
status.autogen.rules[].validate.foreach[].preconditions.all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].validate.foreach[].preconditions.all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].validate.foreach[].preconditions.any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
status.autogen.rules[].validate.foreach[].preconditions.any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].validate.foreach[].preconditions.any[].message <string>	# Message is an optional display message
status.autogen.rules[].validate.foreach[].preconditions.any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].validate.foreach[].preconditions.any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].validate.manifests <object>	# Manifest specifies conditions for manifest verification
status.autogen.rules[].validate.manifests.annotationDomain <string>	# AnnotationDomain is custom domain of annotation for message and signature. Default is "cosign.sigstore.dev".
status.autogen.rules[].validate.manifests.attestors <[]object>	# Attestors specified the required attestors (i.e. authorities)
status.autogen.rules[].validate.manifests.attestors[].count <integer> min=1	# Count specifies the required number of entries that must match. If the count is null, all entries must match (a logical AND). If the count is 1, at least one entry must match (a logical OR). If the count contains a value N, then N must be less than or equal to the size of entries, and at least N entries must match.
status.autogen.rules[].validate.manifests.attestors[].entries <[]object>	# Entries contains the available attestors. An attestor can be a static key, attributes for keyless verification, or a nested attestor declaration.
status.autogen.rules[].validate.manifests.attestors[].entries[].annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
status.autogen.rules[].validate.manifests.attestors[].entries[].attestor <any>	# Attestor is a nested set of Attestor used to specify a more complex set of match authorities.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates <object>	# Certificates specifies one or more certificates.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.cert <string>	# Cert is an optional PEM-encoded public certificate.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.certChain <string>	# CertChain is an optional PEM encoded set of certificates used to verify.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].validate.manifests.attestors[].entries[].certificates.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless <object>	# Keyless is a set of attribute used to verify a Sigstore keyless attestor. See https://github.com/sigstore/cosign/blob/main/KEYLESS.md.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.additionalExtensions <map[string]string>	# AdditionalExtensions are certificate-extensions used for keyless signing.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.issuer <string>	# Issuer is the certificate issuer used for keyless signing.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.issuerRegExp <string>	# IssuerRegExp is the regular expression to match certificate issuer used for keyless signing.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.subject <string>	# Subject is the verified identity used for keyless signing, for example the email address.
status.autogen.rules[].validate.manifests.attestors[].entries[].keyless.subjectRegExp <string>	# SubjectRegExp is the regular expression to match identity used for keyless signing, for example the email address.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys <object>	# Keys specifies one or more public keys.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.kms <string>	# KMS provides the URI to the public key stored in a Key Management System. See: https://github.com/sigstore/cosign/blob/main/KMS.md
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.publicKeys <string>	# Keys is a set of X.509 public keys used to verify image signatures. The keys can be directly specified or can be a variable reference to a key specified in a ConfigMap (see https://kyverno.io/docs/writing-policies/variables/), or reference a standard Kubernetes Secret elsewhere in the cluster by specifying it in the format "k8s://<namespace>/<secret_name>". The named Secret must specify a key `cosign.pub` containing the public key used for verification, (see https://github.com/sigstore/cosign/blob/main/KMS.md#kubernetes-secret). When multiple keys are specified each key is processed as a separate staticKey entry (.attestors[*].entries.keys) within the set of attestors and the count is applied across the keys.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.secret <object>	# Reference to a Secret resource that contains a public key
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.secret.name <string> (required)	# Name of the secret. The provided secret must contain a key named cosign.pub.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.secret.namespace <string> (required)	# Namespace name where the Secret exists.
status.autogen.rules[].validate.manifests.attestors[].entries[].keys.signatureAlgorithm <string> default="sha256"	# Deprecated. Use attestor.signatureAlgorithm instead.
status.autogen.rules[].validate.manifests.attestors[].entries[].repository <string>	# Repository is an optional alternate OCI repository to use for signatures and attestations that match this rule. If specified Repository will override other OCI image repository locations for this Attestor.
status.autogen.rules[].validate.manifests.attestors[].entries[].signatureAlgorithm <string> default="sha256"	# Specify signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512.
status.autogen.rules[].validate.manifests.dryRun <object>	# DryRun configuration
status.autogen.rules[].validate.manifests.dryRun.enable <boolean>
status.autogen.rules[].validate.manifests.dryRun.namespace <string>
status.autogen.rules[].validate.manifests.ignoreFields <[]object>	# Fields which will be ignored while comparing manifests.
status.autogen.rules[].validate.manifests.ignoreFields[].fields <[]string>
status.autogen.rules[].validate.manifests.ignoreFields[].objects <[]object>
status.autogen.rules[].validate.manifests.ignoreFields[].objects[].group <string>
status.autogen.rules[].validate.manifests.ignoreFields[].objects[].kind <string>
status.autogen.rules[].validate.manifests.ignoreFields[].objects[].name <string>
status.autogen.rules[].validate.manifests.ignoreFields[].objects[].namespace <string>
status.autogen.rules[].validate.manifests.ignoreFields[].objects[].version <string>
status.autogen.rules[].validate.manifests.repository <string>	# Repository is an optional alternate OCI repository to use for resource bundle reference. The repository can be overridden per Attestor or Attestation.
status.autogen.rules[].validate.message <string>	# Message specifies a custom message to be displayed on failure.
status.autogen.rules[].validate.pattern <any>	# Pattern specifies an overlay-style pattern used to check resources.
status.autogen.rules[].validate.podSecurity <object>	# PodSecurity applies exemptions for Kubernetes Pod Security admission by specifying exclusions for Pod Security Standards controls.
status.autogen.rules[].validate.podSecurity.exclude <[]object>	# Exclude specifies the Pod Security Standard controls to be excluded.
status.autogen.rules[].validate.podSecurity.exclude[].controlName <string> (required) enum=HostProcess|"Host Namespaces"|"Privileged Containers"|Capabilities|"HostPath Volumes"|"Host Ports"|AppArmor|SELinux|"/proc Mount Type"|Seccomp|Sysctls|"Volume Types"|"Privilege Escalation"|"Running as Non-root"|"Running as Non-root user"	# ControlName specifies the name of the Pod Security Standard control. See: https://kubernetes.io/docs/concepts/security/pod-security-standards/
status.autogen.rules[].validate.podSecurity.exclude[].images <[]string>	# Images selects matching containers and applies the container level PSS. Each image is the image name consisting of the registry address, repository, image, and tag. Empty list matches no containers, PSS checks are applied at the pod level only. Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
status.autogen.rules[].validate.podSecurity.exclude[].restrictedField <string>	# RestrictedField selects the field for the given Pod Security Standard control. When not set, all restricted fields for the control are selected.
status.autogen.rules[].validate.podSecurity.exclude[].values <[]string>	# Values defines the allowed values that can be excluded.
status.autogen.rules[].validate.podSecurity.level <string> enum=privileged|baseline|restricted	# Level defines the Pod Security Standard level to be applied to workloads. Allowed values are privileged, baseline, and restricted.
status.autogen.rules[].validate.podSecurity.version <string> enum=v1.19|v1.20|v1.21|v1.22|v1.23|v1.24|v1.25|v1.26|v1.27|v1.28|v1.29|v1.30|v1.31|v1.32|latest	# Version defines the Pod Security Standard versions that Kubernetes supports. Allowed values are v1.19, v1.20, v1.21, v1.22, v1.23, v1.24, v1.25, v1.26, v1.27, v1.28, v1.29, v1.30, v1.31, v1.32, latest. Defaults to latest.
status.autogen.rules[].verifyImages <[]object>	# VerifyImages is used to verify image signatures and mutate them to add a digest
status.autogen.rules[].verifyImages[].additionalExtensions <map[string]string>	# Deprecated.
status.autogen.rules[].verifyImages[].annotations <map[string]string>	# Deprecated. Use annotations per Attestor instead.
status.autogen.rules[].verifyImages[].attestations <[]object>	# Attestations are optional checks for signed in-toto Statements used to verify the image. See https://github.com/in-toto/attestation. Kyverno fetches signed attestations from the OCI registry and decodes them into a list of Statement declarations.
status.autogen.rules[].verifyImages[].attestations[].attestors <[]object>	# Attestors specify the required attestors (i.e. authorities).
status.autogen.rules[].verifyImages[].attestations[].attestors[].count <integer> min=1	# Count specifies the required number of entries that must match. If the count is null, all entries must match (a logical AND). If the count is 1, at least one entry must match (a logical OR). If the count contains a value N, then N must be less than or equal to the size of entries, and at least N entries must match.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries <[]object>	# Entries contains the available attestors. An attestor can be a static key, attributes for keyless verification, or a nested attestor declaration.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].attestor <any>	# Attestor is a nested set of Attestor used to specify a more complex set of match authorities.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates <object>	# Certificates specifies one or more certificates.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.cert <string>	# Cert is an optional PEM-encoded public certificate.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.certChain <string>	# CertChain is an optional PEM encoded set of certificates used to verify.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].certificates.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless <object>	# Keyless is a set of attribute used to verify a Sigstore keyless attestor. See https://github.com/sigstore/cosign/blob/main/KEYLESS.md.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.additionalExtensions <map[string]string>	# AdditionalExtensions are certificate-extensions used for keyless signing.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.issuer <string>	# Issuer is the certificate issuer used for keyless signing.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.issuerRegExp <string>	# IssuerRegExp is the regular expression to match certificate issuer used for keyless signing.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.subject <string>	# Subject is the verified identity used for keyless signing, for example the email address.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keyless.subjectRegExp <string>	# SubjectRegExp is the regular expression to match identity used for keyless signing, for example the email address.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys <object>	# Keys specifies one or more public keys.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.kms <string>	# KMS provides the URI to the public key stored in a Key Management System. See: https://github.com/sigstore/cosign/blob/main/KMS.md
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.publicKeys <string>	# Keys is a set of X.509 public keys used to verify image signatures. The keys can be directly specified or can be a variable reference to a key specified in a ConfigMap (see https://kyverno.io/docs/writing-policies/variables/), or reference a standard Kubernetes Secret elsewhere in the cluster by specifying it in the format "k8s://<namespace>/<secret_name>". The named Secret must specify a key `cosign.pub` containing the public key used for verification, (see https://github.com/sigstore/cosign/blob/main/KMS.md#kubernetes-secret). When multiple keys are specified each key is processed as a separate staticKey entry (.attestors[*].entries.keys) within the set of attestors and the count is applied across the keys.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.secret <object>	# Reference to a Secret resource that contains a public key
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.secret.name <string> (required)	# Name of the secret. The provided secret must contain a key named cosign.pub.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.secret.namespace <string> (required)	# Namespace name where the Secret exists.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].keys.signatureAlgorithm <string> default="sha256"	# Deprecated. Use attestor.signatureAlgorithm instead.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].repository <string>	# Repository is an optional alternate OCI repository to use for signatures and attestations that match this rule. If specified Repository will override other OCI image repository locations for this Attestor.
status.autogen.rules[].verifyImages[].attestations[].attestors[].entries[].signatureAlgorithm <string> default="sha256"	# Specify signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512.
status.autogen.rules[].verifyImages[].attestations[].conditions <[]object>	# Conditions are used to verify attributes within a Predicate. If no Conditions are specified the attestation check is satisfied as long there are predicates that match the predicate type.
status.autogen.rules[].verifyImages[].attestations[].conditions[].all <[]object>	# AllConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, all of the conditions need to pass
status.autogen.rules[].verifyImages[].attestations[].conditions[].all[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].verifyImages[].attestations[].conditions[].all[].message <string>	# Message is an optional display message
status.autogen.rules[].verifyImages[].attestations[].conditions[].all[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].verifyImages[].attestations[].conditions[].all[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].verifyImages[].attestations[].conditions[].any <[]object>	# AnyConditions enable variable-based conditional rule execution. This is useful for finer control of when an rule is applied. A condition can reference object data using JMESPath notation. Here, at least one of the conditions need to pass
status.autogen.rules[].verifyImages[].attestations[].conditions[].any[].key <any>	# Key is the context entry (using JMESPath) for conditional rule evaluation.
status.autogen.rules[].verifyImages[].attestations[].conditions[].any[].message <string>	# Message is an optional display message
status.autogen.rules[].verifyImages[].attestations[].conditions[].any[].operator <string> enum=Equals|NotEquals|In|AnyIn|AllIn|NotIn|AnyNotIn|AllNotIn|GreaterThanOrEquals|GreaterThan|LessThanOrEquals|LessThan|DurationGreaterThanOrEquals|DurationGreaterThan|DurationLessThanOrEquals|DurationLessThan	# Operator is the conditional operation to perform. Valid operators are: Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals, GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan, DurationLessThanOrEquals, DurationLessThan
status.autogen.rules[].verifyImages[].attestations[].conditions[].any[].value <any>	# Value is the conditional value, or set of values. The values can be fixed set or can be variables declared using JMESPath.
status.autogen.rules[].verifyImages[].attestations[].name <string>	# Name is the variable name.
status.autogen.rules[].verifyImages[].attestations[].predicateType <string>	# Deprecated in favour of 'Type', to be removed soon
status.autogen.rules[].verifyImages[].attestations[].type <string>	# Type defines the type of attestation contained within the Statement.
status.autogen.rules[].verifyImages[].attestors <[]object>	# Attestors specified the required attestors (i.e. authorities)
status.autogen.rules[].verifyImages[].attestors[].count <integer> min=1	# Count specifies the required number of entries that must match. If the count is null, all entries must match (a logical AND). If the count is 1, at least one entry must match (a logical OR). If the count contains a value N, then N must be less than or equal to the size of entries, and at least N entries must match.
status.autogen.rules[].verifyImages[].attestors[].entries <[]object>	# Entries contains the available attestors. An attestor can be a static key, attributes for keyless verification, or a nested attestor declaration.
status.autogen.rules[].verifyImages[].attestors[].entries[].annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
status.autogen.rules[].verifyImages[].attestors[].entries[].attestor <any>	# Attestor is a nested set of Attestor used to specify a more complex set of match authorities.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates <object>	# Certificates specifies one or more certificates.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.cert <string>	# Cert is an optional PEM-encoded public certificate.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.certChain <string>	# CertChain is an optional PEM encoded set of certificates used to verify.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].verifyImages[].attestors[].entries[].certificates.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless <object>	# Keyless is a set of attribute used to verify a Sigstore keyless attestor. See https://github.com/sigstore/cosign/blob/main/KEYLESS.md.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.additionalExtensions <map[string]string>	# AdditionalExtensions are certificate-extensions used for keyless signing.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.issuer <string>	# Issuer is the certificate issuer used for keyless signing.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.issuerRegExp <string>	# IssuerRegExp is the regular expression to match certificate issuer used for keyless signing.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.subject <string>	# Subject is the verified identity used for keyless signing, for example the email address.
status.autogen.rules[].verifyImages[].attestors[].entries[].keyless.subjectRegExp <string>	# SubjectRegExp is the regular expression to match identity used for keyless signing, for example the email address.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys <object>	# Keys specifies one or more public keys.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.ctlog <object>	# CTLog (certificate timestamp log) provides a configuration for validation of Signed Certificate Timestamps (SCTs). If the value is unset, the default behavior by Cosign is used.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.ctlog.ignoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.ctlog.pubkey <string>	# PubKey, if set, is used to validate SCTs against a custom source.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.kms <string>	# KMS provides the URI to the public key stored in a Key Management System. See: https://github.com/sigstore/cosign/blob/main/KMS.md
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.publicKeys <string>	# Keys is a set of X.509 public keys used to verify image signatures. The keys can be directly specified or can be a variable reference to a key specified in a ConfigMap (see https://kyverno.io/docs/writing-policies/variables/), or reference a standard Kubernetes Secret elsewhere in the cluster by specifying it in the format "k8s://<namespace>/<secret_name>". The named Secret must specify a key `cosign.pub` containing the public key used for verification, (see https://github.com/sigstore/cosign/blob/main/KMS.md#kubernetes-secret). When multiple keys are specified each key is processed as a separate staticKey entry (.attestors[*].entries.keys) within the set of attestors and the count is applied across the keys.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.rekor <object>	# Rekor provides configuration for the Rekor transparency log service. If an empty object is provided the public instance of Rekor (https://rekor.sigstore.dev) is used.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.rekor.ignoreTlog <boolean>	# IgnoreTlog skips transparency log verification.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.rekor.pubkey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.rekor.url <string>	# URL is the address of the transparency log. Defaults to the public Rekor log instance https://rekor.sigstore.dev.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.secret <object>	# Reference to a Secret resource that contains a public key
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.secret.name <string> (required)	# Name of the secret. The provided secret must contain a key named cosign.pub.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.secret.namespace <string> (required)	# Namespace name where the Secret exists.
status.autogen.rules[].verifyImages[].attestors[].entries[].keys.signatureAlgorithm <string> default="sha256"	# Deprecated. Use attestor.signatureAlgorithm instead.
status.autogen.rules[].verifyImages[].attestors[].entries[].repository <string>	# Repository is an optional alternate OCI repository to use for signatures and attestations that match this rule. If specified Repository will override other OCI image repository locations for this Attestor.
status.autogen.rules[].verifyImages[].attestors[].entries[].signatureAlgorithm <string> default="sha256"	# Specify signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512.
status.autogen.rules[].verifyImages[].cosignOCI11 <boolean>	# CosignOCI11 enables the experimental OCI 1.1 behaviour in cosign image verification. Defaults to false.
status.autogen.rules[].verifyImages[].failureAction <string> enum=Audit|Enforce	# Allowed values are Audit or Enforce.
status.autogen.rules[].verifyImages[].image <string>	# Deprecated. Use ImageReferences instead.
status.autogen.rules[].verifyImages[].imageReferences <[]string>	# ImageReferences is a list of matching image reference patterns. At least one pattern in the list must match the image for the rule to apply. Each image reference consists of a registry address (defaults to docker.io), repository, image, and tag (defaults to latest). Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
status.autogen.rules[].verifyImages[].imageRegistryCredentials <object>	# ImageRegistryCredentials provides credentials that will be used for authentication with registry.
status.autogen.rules[].verifyImages[].imageRegistryCredentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.rules[].verifyImages[].imageRegistryCredentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.rules[].verifyImages[].imageRegistryCredentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets can be specified as a name (Kyverno namespace) or namespace/name. imagePullSecrets from the resource namespace are also used.
status.autogen.rules[].verifyImages[].issuer <string>	# Deprecated. Use KeylessAttestor instead.
status.autogen.rules[].verifyImages[].key <string>	# Deprecated. Use StaticKeyAttestor instead.
status.autogen.rules[].verifyImages[].mutateDigest <boolean> default=true	# MutateDigest enables replacement of image tags with digests. Defaults to true.
status.autogen.rules[].verifyImages[].repository <string>	# Repository is an optional alternate OCI repository to use for image signatures and attestations that match this rule. If specified Repository will override the default OCI image repository configured for the installation. The repository can also be overridden per Attestor or Attestation.
status.autogen.rules[].verifyImages[].required <boolean> default=true	# Required validates that images are verified i.e. have matched passed a signature or attestation check.
status.autogen.rules[].verifyImages[].roots <string>	# Deprecated. Use KeylessAttestor instead.
status.autogen.rules[].verifyImages[].skipImageReferences <[]string>	# SkipImageReferences is a list of matching image reference patterns that should be skipped. At least one pattern in the list must match the image for the rule to be skipped. Each image reference consists of a registry address (defaults to docker.io), repository, image, and tag (defaults to latest). Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
status.autogen.rules[].verifyImages[].subject <string>	# Deprecated. Use KeylessAttestor instead.
status.autogen.rules[].verifyImages[].type <string> enum=Cosign|SigstoreBundle|Notary	# Type specifies the method of signature validation. The allowed options are Cosign, Sigstore Bundle and Notary. By default Cosign is used if a type is not specified.
status.autogen.rules[].verifyImages[].useCache <boolean> default=true	# UseCache enables caching of image verify responses for this rule.
status.autogen.rules[].verifyImages[].validate <object>	# Validation checks conditions across multiple image verification attestations or context entries
status.autogen.rules[].verifyImages[].validate.deny <object>	# Deny defines conditions used to pass or fail a validation rule.
status.autogen.rules[].verifyImages[].validate.deny.conditions <any>	# Multiple conditions can be declared under an `any` or `all` statement. A direct list of conditions (without `any` or `all` statements) is also supported for backwards compatibility but will be deprecated in the next major release. See: https://kyverno.io/docs/writing-policies/validate/#deny-rules
status.autogen.rules[].verifyImages[].validate.message <string>	# Message specifies a custom message to be displayed on failure.
status.autogen.rules[].verifyImages[].verifyDigest <boolean> default=true	# VerifyDigest validates that images have a digest.
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.ready <boolean>	# Deprecated in favor of Conditions
status.rulecount <object>	# RuleCountStatus contains four variables which describes counts for validate, generate, mutate and verify images rules
status.rulecount.generate <integer> (required)	# Count for generate rules in policy
status.rulecount.mutate <integer> (required)	# Count for mutate rules in policy
status.rulecount.validate <integer> (required)	# Count for validate rules in policy
status.rulecount.verifyimages <integer> (required)	# Count for verify image rules in policy
status.validatingadmissionpolicy <object>	# ValidatingAdmissionPolicy contains status information
status.validatingadmissionpolicy.generated <boolean> (required)	# Generated indicates whether a validating admission policy is generated from the policy or not
status.validatingadmissionpolicy.message <string> (required)	# Message is a human readable message indicating details about the generation of validating admission policy It is an empty string when validating admission policy is successfully generated.
