# schema source: Tekton Pipeline v1.14.0 https://github.com/tektoncd/pipeline
apiVersion <string> enum=tekton.dev/v1
kind <string> enum=Pipeline
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec holds the desired state of the Pipeline from the client
spec.description <string>	# Description is a user-facing description of the pipeline that may be used to populate a UI.
spec.displayName <string>	# DisplayName is a user-facing name of the pipeline that may be used to populate a UI.
spec.finally <[]object>	# Finally declares the list of Tasks that execute just before leaving the Pipeline i.e. either after all Tasks are finished executing successfully or after a failure which would result in ending the Pipeline
spec.finally[].description <string>	# Description is the description of this task within the context of a Pipeline. This description may be used to populate a UI.
spec.finally[].displayName <string>	# DisplayName is the display name of this task within the context of a Pipeline. This display name may be used to populate a UI.
spec.finally[].matrix <object>	# Matrix declares parameters used to fan out this task.
spec.finally[].matrix.include <[]object>	# Include is a list of IncludeParams which allows passing in specific combinations of Parameters into the Matrix.
spec.finally[].matrix.include[].name <string>	# Name the specified combination
spec.finally[].matrix.include[].params <[]object>	# Params takes only `Parameters` of type `"string"` The names of the `params` must match the names of the `params` in the underlying `Task`
spec.finally[].matrix.include[].params[].name <string> (required)
spec.finally[].matrix.include[].params[].value <any> (required)
spec.finally[].matrix.params <[]object>	# Params is a list of parameters used to fan out the pipelineTask Params takes only `Parameters` of type `"array"` Each array element is supplied to the `PipelineTask` by substituting `params` of type `"string"` in the underlying `Task`. The names of the `params` in the `Matrix` must match the names of the `params` in the underlying `Task` that they will be substituting.
spec.finally[].matrix.params[].name <string> (required)
spec.finally[].matrix.params[].value <any> (required)
spec.finally[].name <string>	# Name is the name of this task within the context of a Pipeline. Name is used as a coordinate with the `from` and `runAfter` fields to establish the execution order of tasks relative to one another.
spec.finally[].onError <string>	# OnError defines the exiting behavior of a PipelineRun on error can be set to [ continue | stopAndFail ]
spec.finally[].params <[]object>	# Parameters declares parameters passed to this task.
spec.finally[].params[].name <string> (required)
spec.finally[].params[].value <any> (required)
spec.finally[].pipelineRef <object>	# PipelineRef is a reference to a pipeline definition. This is an alpha field. You must set the "enable-api-fields" feature flag to "alpha" for this field to be supported. When enabled, the referenced Pipeline is executed as a child PipelineRun owned by the parent PipelineRun.
spec.finally[].pipelineRef.apiVersion <string>	# API version of the referent
spec.finally[].pipelineRef.name <string>	# Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names
spec.finally[].pipelineRef.params <[]object>	# Params contains the parameters used to identify the referenced Tekton resource. Example entries might include "repo" or "path" but the set of params ultimately depends on the chosen resolver.
spec.finally[].pipelineRef.params[].name <string> (required)
spec.finally[].pipelineRef.params[].value <any> (required)
spec.finally[].pipelineRef.resolver <string>	# Resolver is the name of the resolver that should perform resolution of the referenced Tekton resource, such as "git".
spec.finally[].pipelineSpec <any>	# PipelineSpec is a specification of a pipeline. This is an alpha field. You must set the "enable-api-fields" feature flag to "alpha" for this field to be supported. When enabled, the embedded Pipeline is executed as a child PipelineRun owned by the parent PipelineRun. Specifying PipelineSpec can be disabled by setting `disable-inline-spec` feature flag. See Pipeline.spec (API version: tekton.dev/v1)
spec.finally[].retries <integer>	# Retries represents how many times this task should be retried in case of task failure: ConditionSucceeded set to False
spec.finally[].runAfter <[]string>	# RunAfter is the list of PipelineTask names that should be executed before this Task executes. (Used to force a specific ordering in graph execution.)
spec.finally[].taskRef <object>	# TaskRef is a reference to a task definition.
spec.finally[].taskRef.apiVersion <string>	# API version of the referent Note: A Task with non-empty APIVersion and Kind is considered a Custom Task
spec.finally[].taskRef.kind <string>	# TaskKind indicates the Kind of the Task: 1. Namespaced Task when Kind is set to "Task". If Kind is "", it defaults to "Task". 2. Custom Task when Kind is non-empty and APIVersion is non-empty
spec.finally[].taskRef.name <string>	# Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names
spec.finally[].taskRef.params <[]object>	# Params contains the parameters used to identify the referenced Tekton resource. Example entries might include "repo" or "path" but the set of params ultimately depends on the chosen resolver.
spec.finally[].taskRef.params[].name <string> (required)
spec.finally[].taskRef.params[].value <any> (required)
spec.finally[].taskRef.resolver <string>	# Resolver is the name of the resolver that should perform resolution of the referenced Tekton resource, such as "git".
spec.finally[].taskSpec <any>	# TaskSpec is a specification of a task Specifying TaskSpec can be disabled by setting `disable-inline-spec` feature flag. See Task.spec (API version: tekton.dev/v1)
spec.finally[].timeout <string>	# Duration after which the TaskRun times out. Defaults to 1 hour. Refer Go's ParseDuration documentation for expected format: https://golang.org/pkg/time/#ParseDuration
spec.finally[].when <[]object>	# When is a list of when expressions that need to be true for the task to run
spec.finally[].when[].cel <string>	# CEL is a string of Common Language Expression, which can be used to conditionally execute the task based on the result of the expression evaluation More info about CEL syntax: https://github.com/google/cel-spec/blob/master/doc/langdef.md
spec.finally[].when[].input <string>	# Input is the string for guard checking which can be a static input or an output from a parent Task
spec.finally[].when[].operator <string>	# Operator that represents an Input's relationship to the values
spec.finally[].when[].values <[]string>	# Values is an array of strings, which is compared against the input, for guard checking It must be non-empty
spec.finally[].workspaces <[]object>	# Workspaces maps workspaces from the pipeline spec to the workspaces declared in the Task.
spec.finally[].workspaces[].name <string> (required)	# Name is the name of the workspace as declared by the task
spec.finally[].workspaces[].subPath <string>	# SubPath is optionally a directory on the volume which should be used for this binding (i.e. the volume will be mounted at this sub directory).
spec.finally[].workspaces[].workspace <string>	# Workspace is the name of the workspace declared by the pipeline
spec.params <[]object>	# Params declares a list of input parameters that must be supplied when this Pipeline is run.
spec.params[].default <any>	# Default is the value a parameter takes if no input value is supplied. If default is set, a Task may be executed without a supplied value for the parameter.
spec.params[].description <string>	# Description is a user-facing description of the parameter that may be used to populate a UI.
spec.params[].enum <[]string>	# Enum declares a set of allowed param input values for tasks/pipelines that can be validated. If Enum is not set, no input validation is performed for the param.
spec.params[].name <string> (required)	# Name declares the name by which a parameter is referenced.
spec.params[].properties <map[string]object>	# Properties is the JSON Schema properties to support key-value pairs parameter.
spec.params[].properties.<key>.type <string>	# ParamType indicates the type of an input parameter; Used to distinguish between a single string and an array of strings.
spec.params[].type <string>	# Type is the user-specified type of the parameter. The possible types are currently "string", "array" and "object", and "string" is the default.
spec.results <[]object>	# Results are values that this pipeline can output once run
spec.results[].description <string>	# Description is a human-readable description of the result
spec.results[].name <string> (required)	# Name the given name
spec.results[].type <string>	# Type is the user-specified type of the result. The possible types are 'string', 'array', and 'object', with 'string' as the default. 'array' and 'object' types are alpha features.
spec.results[].value <any> (required)	# Value the expression used to retrieve the value
spec.tasks <[]object>	# Tasks declares the graph of Tasks that execute when this Pipeline is run.
spec.tasks[].description <string>	# Description is the description of this task within the context of a Pipeline. This description may be used to populate a UI.
spec.tasks[].displayName <string>	# DisplayName is the display name of this task within the context of a Pipeline. This display name may be used to populate a UI.
spec.tasks[].matrix <object>	# Matrix declares parameters used to fan out this task.
spec.tasks[].matrix.include <[]object>	# Include is a list of IncludeParams which allows passing in specific combinations of Parameters into the Matrix.
spec.tasks[].matrix.include[].name <string>	# Name the specified combination
spec.tasks[].matrix.include[].params <[]object>	# Params takes only `Parameters` of type `"string"` The names of the `params` must match the names of the `params` in the underlying `Task`
spec.tasks[].matrix.include[].params[].name <string> (required)
spec.tasks[].matrix.include[].params[].value <any> (required)
spec.tasks[].matrix.params <[]object>	# Params is a list of parameters used to fan out the pipelineTask Params takes only `Parameters` of type `"array"` Each array element is supplied to the `PipelineTask` by substituting `params` of type `"string"` in the underlying `Task`. The names of the `params` in the `Matrix` must match the names of the `params` in the underlying `Task` that they will be substituting.
spec.tasks[].matrix.params[].name <string> (required)
spec.tasks[].matrix.params[].value <any> (required)
spec.tasks[].name <string>	# Name is the name of this task within the context of a Pipeline. Name is used as a coordinate with the `from` and `runAfter` fields to establish the execution order of tasks relative to one another.
spec.tasks[].onError <string>	# OnError defines the exiting behavior of a PipelineRun on error can be set to [ continue | stopAndFail ]
spec.tasks[].params <[]object>	# Parameters declares parameters passed to this task.
spec.tasks[].params[].name <string> (required)
spec.tasks[].params[].value <any> (required)
spec.tasks[].pipelineRef <object>	# PipelineRef is a reference to a pipeline definition. This is an alpha field. You must set the "enable-api-fields" feature flag to "alpha" for this field to be supported. When enabled, the referenced Pipeline is executed as a child PipelineRun owned by the parent PipelineRun.
spec.tasks[].pipelineRef.apiVersion <string>	# API version of the referent
spec.tasks[].pipelineRef.name <string>	# Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names
spec.tasks[].pipelineRef.params <[]object>	# Params contains the parameters used to identify the referenced Tekton resource. Example entries might include "repo" or "path" but the set of params ultimately depends on the chosen resolver.
spec.tasks[].pipelineRef.params[].name <string> (required)
spec.tasks[].pipelineRef.params[].value <any> (required)
spec.tasks[].pipelineRef.resolver <string>	# Resolver is the name of the resolver that should perform resolution of the referenced Tekton resource, such as "git".
spec.tasks[].pipelineSpec <any>	# PipelineSpec is a specification of a pipeline. This is an alpha field. You must set the "enable-api-fields" feature flag to "alpha" for this field to be supported. When enabled, the embedded Pipeline is executed as a child PipelineRun owned by the parent PipelineRun. Specifying PipelineSpec can be disabled by setting `disable-inline-spec` feature flag. See Pipeline.spec (API version: tekton.dev/v1)
spec.tasks[].retries <integer>	# Retries represents how many times this task should be retried in case of task failure: ConditionSucceeded set to False
spec.tasks[].runAfter <[]string>	# RunAfter is the list of PipelineTask names that should be executed before this Task executes. (Used to force a specific ordering in graph execution.)
spec.tasks[].taskRef <object>	# TaskRef is a reference to a task definition.
spec.tasks[].taskRef.apiVersion <string>	# API version of the referent Note: A Task with non-empty APIVersion and Kind is considered a Custom Task
spec.tasks[].taskRef.kind <string>	# TaskKind indicates the Kind of the Task: 1. Namespaced Task when Kind is set to "Task". If Kind is "", it defaults to "Task". 2. Custom Task when Kind is non-empty and APIVersion is non-empty
spec.tasks[].taskRef.name <string>	# Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names
spec.tasks[].taskRef.params <[]object>	# Params contains the parameters used to identify the referenced Tekton resource. Example entries might include "repo" or "path" but the set of params ultimately depends on the chosen resolver.
spec.tasks[].taskRef.params[].name <string> (required)
spec.tasks[].taskRef.params[].value <any> (required)
spec.tasks[].taskRef.resolver <string>	# Resolver is the name of the resolver that should perform resolution of the referenced Tekton resource, such as "git".
spec.tasks[].taskSpec <any>	# TaskSpec is a specification of a task Specifying TaskSpec can be disabled by setting `disable-inline-spec` feature flag. See Task.spec (API version: tekton.dev/v1)
spec.tasks[].timeout <string>	# Duration after which the TaskRun times out. Defaults to 1 hour. Refer Go's ParseDuration documentation for expected format: https://golang.org/pkg/time/#ParseDuration
spec.tasks[].when <[]object>	# When is a list of when expressions that need to be true for the task to run
spec.tasks[].when[].cel <string>	# CEL is a string of Common Language Expression, which can be used to conditionally execute the task based on the result of the expression evaluation More info about CEL syntax: https://github.com/google/cel-spec/blob/master/doc/langdef.md
spec.tasks[].when[].input <string>	# Input is the string for guard checking which can be a static input or an output from a parent Task
spec.tasks[].when[].operator <string>	# Operator that represents an Input's relationship to the values
spec.tasks[].when[].values <[]string>	# Values is an array of strings, which is compared against the input, for guard checking It must be non-empty
spec.tasks[].workspaces <[]object>	# Workspaces maps workspaces from the pipeline spec to the workspaces declared in the Task.
spec.tasks[].workspaces[].name <string> (required)	# Name is the name of the workspace as declared by the task
spec.tasks[].workspaces[].subPath <string>	# SubPath is optionally a directory on the volume which should be used for this binding (i.e. the volume will be mounted at this sub directory).
spec.tasks[].workspaces[].workspace <string>	# Workspace is the name of the workspace declared by the pipeline
spec.workspaces <[]object>	# Workspaces declares a set of named workspaces that are expected to be provided by a PipelineRun.
spec.workspaces[].description <string>	# Description is a human readable string describing how the workspace will be used in the Pipeline. It can be useful to include a bit of detail about which tasks are intended to have access to the data on the workspace.
spec.workspaces[].name <string> (required)	# Name is the name of a workspace to be provided by a PipelineRun.
spec.workspaces[].optional <boolean>	# Optional marks a Workspace as not being required in PipelineRuns. By default this field is false and so declared workspaces are required.
