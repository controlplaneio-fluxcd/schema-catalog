# schema source: Tekton Pipeline v1.14.1 https://github.com/tektoncd/pipeline
apiVersion <string> enum=tekton.dev/v1beta1
kind <string> enum=PipelineRun
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec
spec.managedBy <string>	# ManagedBy
spec.params <[]object>	# Params
spec.params[].name <string> (required)
spec.params[].value <any> (required)	# Value
spec.pipelineRef <object>	# PipelineRef
spec.pipelineRef.apiVersion <string>	# APIVersion
spec.pipelineRef.bundle <string>	# Deprecated: Please use ResolverRef with the bundles resolver instead. Bundle
spec.pipelineRef.name <string>	# Name
spec.pipelineRef.params <[]object>	# Params
spec.pipelineRef.params[].name <string> (required)
spec.pipelineRef.params[].value <any> (required)	# Value
spec.pipelineRef.resolver <string>	# Resolver
spec.pipelineSpec <any>	# PipelineSpec
spec.podTemplate <object>	# PodTemplate
spec.podTemplate.affinity <any>	# If specified, the pod's scheduling constraints. See Pod.spec.affinity (API version: v1)
spec.podTemplate.automountServiceAccountToken <boolean>	# AutomountServiceAccountToken indicates whether pods running as this service account should have an API token automatically mounted.
spec.podTemplate.dnsConfig <object>	# Specifies the DNS parameters of a pod. Parameters specified here will be merged to the generated DNS configuration based on DNSPolicy.
spec.podTemplate.dnsConfig.nameservers <[]string>	# A list of DNS name server IP addresses. This will be appended to the base nameservers generated from DNSPolicy. Duplicated nameservers will be removed.
spec.podTemplate.dnsConfig.options <[]object>	# A list of DNS resolver options. This will be merged with the base options generated from DNSPolicy. Duplicated entries will be removed. Resolution options given in Options will override those that appear in the base DNSPolicy.
spec.podTemplate.dnsConfig.options[].name <string>	# Name is this DNS resolver option's name. Required.
spec.podTemplate.dnsConfig.options[].value <string>	# Value is this DNS resolver option's value.
spec.podTemplate.dnsConfig.searches <[]string>	# A list of DNS search domains for host-name lookup. This will be appended to the base search paths generated from DNSPolicy. Duplicated search paths will be removed.
spec.podTemplate.dnsPolicy <string>	# Set DNS policy for the pod. Defaults to "ClusterFirst". Valid values are 'ClusterFirst', 'Default' or 'None'. DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.
spec.podTemplate.enableServiceLinks <boolean>	# EnableServiceLinks indicates whether information about services should be injected into pod's environment variables, matching the syntax of Docker links. Optional: Defaults to true.
spec.podTemplate.env <[]object>	# List of environment variables that can be provided to the containers belonging to the pod.
spec.podTemplate.env[].name <string> (required)	# Name of the environment variable. May consist of any printable ASCII characters except '='.
spec.podTemplate.env[].value <string>	# Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
spec.podTemplate.env[].valueFrom <object>	# Source for the environment variable's value. Cannot be used if value is not empty.
spec.podTemplate.env[].valueFrom.configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.podTemplate.env[].valueFrom.configMapKeyRef.key <string> (required)	# The key to select.
spec.podTemplate.env[].valueFrom.configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.podTemplate.env[].valueFrom.configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.podTemplate.env[].valueFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
spec.podTemplate.env[].valueFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.podTemplate.env[].valueFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.podTemplate.env[].valueFrom.fileKeyRef <object>	# FileKeyRef selects a key of the env file. Requires the EnvFiles feature gate to be enabled.
spec.podTemplate.env[].valueFrom.fileKeyRef.key <string> (required)	# The key within the env file. An invalid key will prevent the pod from starting. The keys defined within a source may consist of any printable ASCII characters except '='. During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
spec.podTemplate.env[].valueFrom.fileKeyRef.optional <boolean> default=false	# Specify whether the file or its key must be defined. If the file or key does not exist, then the env var is not published. If optional is set to true and the specified key does not exist, the environment variable will not be set in the Pod's containers. If optional is set to false and the specified key does not exist, an error will be returned during Pod creation.
spec.podTemplate.env[].valueFrom.fileKeyRef.path <string> (required)	# The path within the volume from which to select the file. Must be relative and may not contain the '..' path or start with '..'.
spec.podTemplate.env[].valueFrom.fileKeyRef.volumeName <string> (required)	# The name of the volume mount containing the env file.
spec.podTemplate.env[].valueFrom.resourceFieldRef <object>	# Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
spec.podTemplate.env[].valueFrom.resourceFieldRef.containerName <string>	# Container name: required for volumes, optional for env vars
spec.podTemplate.env[].valueFrom.resourceFieldRef.divisor <int-or-string>	# Specifies the output format of the exposed resources, defaults to "1"
spec.podTemplate.env[].valueFrom.resourceFieldRef.resource <string> (required)	# Required: resource to select
spec.podTemplate.env[].valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.podTemplate.env[].valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.podTemplate.env[].valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.podTemplate.env[].valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.podTemplate.hostAliases <[]object>	# HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts file if specified. This is only valid for non-hostNetwork pods.
spec.podTemplate.hostAliases[].hostnames <[]string>	# Hostnames for the above IP address.
spec.podTemplate.hostAliases[].ip <string> (required)	# IP address of the host file entry.
spec.podTemplate.hostNetwork <boolean>	# HostNetwork specifies whether the pod may use the node network namespace
spec.podTemplate.hostUsers <boolean>	# HostUsers indicates whether the pod will use the host's user namespace. Optional: Default to true. If set to true or not present, the pod will be run in the host user namespace, useful for when the pod needs a feature only available to the host user namespace, such as loading a kernel module with CAP_SYS_MODULE. When set to false, a new user namespace is created for the pod. Setting false is useful to mitigating container breakout vulnerabilities such as allowing containers to run as root without their user having root privileges on the host. This field depends on the kubernetes feature gate UserNamespacesSupport being enabled.
spec.podTemplate.imagePullSecrets <[]object>	# ImagePullSecrets gives the name of the secret used by the pod to pull the image if specified
spec.podTemplate.imagePullSecrets[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.podTemplate.nodeSelector <map[string]string>	# NodeSelector is a selector which must be true for the pod to fit on a node. Selector which must match a node's labels for the pod to be scheduled on that node. More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/
spec.podTemplate.priorityClassName <string>	# If specified, indicates the pod's priority. "system-node-critical" and "system-cluster-critical" are two special keywords which indicate the highest priorities with the former being the highest priority. Any other name must be defined by creating a PriorityClass object with that name. If not specified, the pod priority will be default or zero if there is no default.
spec.podTemplate.runtimeClassName <string>	# RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used to run this pod. If no RuntimeClass resource matches the named class, the pod will not be run. If unset or empty, the "legacy" RuntimeClass will be used, which is an implicit class with an empty definition that uses the default runtime handler. More info: https://git.k8s.io/enhancements/keps/sig-node/runtime-class.md This is a beta feature as of Kubernetes v1.14.
spec.podTemplate.schedulerName <string>	# SchedulerName specifies the scheduler to be used to dispatch the Pod
spec.podTemplate.securityContext <any>	# SecurityContext holds pod-level security attributes and common container settings. Optional: Defaults to empty. See type description for default values of each field. See Pod.spec.securityContext (API version: v1)
spec.podTemplate.tolerations <[]object>	# If specified, the pod's tolerations.
spec.podTemplate.tolerations[].effect <string>	# Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
spec.podTemplate.tolerations[].key <string>	# Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.
spec.podTemplate.tolerations[].operator <string>	# Operator represents a key's relationship to the value. Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category. Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
spec.podTemplate.tolerations[].tolerationSeconds <integer> format=int64	# TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.
spec.podTemplate.tolerations[].value <string>	# Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.
spec.podTemplate.topologySpreadConstraints <[]object>	# TopologySpreadConstraints controls how Pods are spread across your cluster among failure-domains such as regions, zones, nodes, and other user-defined topology domains.
spec.podTemplate.topologySpreadConstraints[].labelSelector <object>	# LabelSelector is used to find matching pods. Pods that match this label selector are counted to determine the number of pods in their corresponding topology domain.
spec.podTemplate.topologySpreadConstraints[].labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.podTemplate.topologySpreadConstraints[].labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.podTemplate.topologySpreadConstraints[].labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.podTemplate.topologySpreadConstraints[].labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.podTemplate.topologySpreadConstraints[].labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.podTemplate.topologySpreadConstraints[].matchLabelKeys <[]string>	# MatchLabelKeys is a set of pod label keys to select the pods over which spreading will be calculated. The keys are used to lookup values from the incoming pod labels, those key-value labels are ANDed with labelSelector to select the group of existing pods over which spreading will be calculated for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector. MatchLabelKeys cannot be set when LabelSelector isn't set. Keys that don't exist in the incoming pod labels will be ignored. A null or empty list means only match against labelSelector. This is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default).
spec.podTemplate.topologySpreadConstraints[].maxSkew <integer> (required) format=int32	# MaxSkew describes the degree to which pods may be unevenly distributed. When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference between the number of matching pods in the target topology and the global minimum. The global minimum is the minimum number of matching pods in an eligible domain or zero if the number of eligible domains is less than MinDomains. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 2/2/1: In this case, the global minimum is 1. | zone1 | zone2 | zone3 | | P P | P P | P | - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2; scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2) violate MaxSkew(1). - if MaxSkew is 2, incoming pod can be scheduled onto any zone. When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence to topologies that satisfy it. It's a required field. Default value is 1 and 0 is not allowed.
spec.podTemplate.topologySpreadConstraints[].minDomains <integer> format=int32	# MinDomains indicates a minimum number of eligible domains. When the number of eligible domains with matching topology keys is less than minDomains, Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed. And when the number of eligible domains with matching topology keys equals or greater than minDomains, this value has no effect on scheduling. As a result, when the number of eligible domains is less than minDomains, scheduler won't schedule more than maxSkew Pods to those domains. If value is nil, the constraint behaves as if MinDomains is equal to 1. Valid values are integers greater than 0. When value is not nil, WhenUnsatisfiable must be DoNotSchedule. For example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same labelSelector spread as 2/2/2: | zone1 | zone2 | zone3 | | P P | P P | P P | The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0. In this situation, new pod with the same labelSelector cannot be scheduled, because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones, it will violate MaxSkew.
spec.podTemplate.topologySpreadConstraints[].nodeAffinityPolicy <string>	# NodeAffinityPolicy indicates how we will treat Pod's nodeAffinity/nodeSelector when calculating pod topology spread skew. Options are: - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations. - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations. If this value is nil, the behavior is equivalent to the Honor policy.
spec.podTemplate.topologySpreadConstraints[].nodeTaintsPolicy <string>	# NodeTaintsPolicy indicates how we will treat node taints when calculating pod topology spread skew. Options are: - Honor: nodes without taints, along with tainted nodes for which the incoming pod has a toleration, are included. - Ignore: node taints are ignored. All nodes are included. If this value is nil, the behavior is equivalent to the Ignore policy.
spec.podTemplate.topologySpreadConstraints[].topologyKey <string> (required)	# TopologyKey is the key of node labels. Nodes that have a label with this key and identical values are considered to be in the same topology. We consider each <key, value> as a "bucket", and try to put balanced number of pods into each bucket. We define a domain as a particular instance of a topology. Also, we define an eligible domain as a domain whose nodes meet the requirements of nodeAffinityPolicy and nodeTaintsPolicy. e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology. And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology. It's a required field.
spec.podTemplate.topologySpreadConstraints[].whenUnsatisfiable <string> (required)	# WhenUnsatisfiable indicates how to deal with a pod if it doesn't satisfy the spread constraint. - DoNotSchedule (default) tells the scheduler not to schedule it. - ScheduleAnyway tells the scheduler to schedule the pod in any location, but giving higher precedence to topologies that would help reduce the skew. A constraint is considered "Unsatisfiable" for an incoming pod if and only if every possible node assignment for that pod would violate "MaxSkew" on some topology. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 3/1/1: | zone1 | zone2 | zone3 | | P P P | P | P | If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler won't make it *more* imbalanced. It's a required field.
spec.podTemplate.volumes <any>	# List of volumes that can be mounted by containers belonging to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes See Pod.spec.volumes (API version: v1)
spec.resources <[]object>	# Resources Deprecated: Unused, preserved only for backwards compatibility
spec.resources[].name <string>	# Name
spec.resources[].resourceRef <object>	# ResourceRef
spec.resources[].resourceRef.apiVersion <string>	# APIVersion
spec.resources[].resourceRef.name <string>	# Name
spec.resources[].resourceSpec <object>	# ResourceSpec
spec.resources[].resourceSpec.description <string>	# Description is a user-facing description of the resource that may be used to populate a UI.
spec.resources[].resourceSpec.params <[]object> (required)
spec.resources[].resourceSpec.params[].name <string> (required)
spec.resources[].resourceSpec.params[].value <string> (required)
spec.resources[].resourceSpec.secrets <[]object>	# Secrets to fetch to populate some of resource fields
spec.resources[].resourceSpec.secrets[].fieldName <string> (required)
spec.resources[].resourceSpec.secrets[].secretKey <string> (required)
spec.resources[].resourceSpec.secrets[].secretName <string> (required)
spec.resources[].resourceSpec.type <string> (required)	# PipelineResourceType represents the type of endpoint the pipelineResource is, so that the controller will know this pipelineResource shouldx be fetched and optionally what additional metatdata should be provided for it. Deprecated: Unused, preserved only for backwards compatibility
spec.serviceAccountName <string>	# ServiceAccountName
spec.status <string>	# Status
spec.taskRunSpecs <[]object>	# TaskRunSpecs
spec.taskRunSpecs[].computeResources <object>	# ComputeResources
spec.taskRunSpecs[].computeResources.claims <[]object>	# Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container. This field depends on the DynamicResourceAllocation feature gate. This field is immutable. It can only be set for containers.
spec.taskRunSpecs[].computeResources.claims[].name <string> (required)	# Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container.
spec.taskRunSpecs[].computeResources.claims[].request <string>	# Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request.
spec.taskRunSpecs[].computeResources.limits <object (free-form)>	# Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spec.taskRunSpecs[].computeResources.requests <object (free-form)>	# Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spec.taskRunSpecs[].metadata <object>	# Metadata
spec.taskRunSpecs[].metadata.annotations <map[string]string>	# Annotations
spec.taskRunSpecs[].metadata.labels <map[string]string>	# Labels
spec.taskRunSpecs[].pipelineTaskName <string>
spec.taskRunSpecs[].sidecarOverrides <[]object>	# SidecarOverrides
spec.taskRunSpecs[].sidecarOverrides[].name <string> (required)	# Name
spec.taskRunSpecs[].sidecarOverrides[].resources <object> (required)	# Resources
spec.taskRunSpecs[].sidecarOverrides[].resources.claims <[]object>	# Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container. This field depends on the DynamicResourceAllocation feature gate. This field is immutable. It can only be set for containers.
spec.taskRunSpecs[].sidecarOverrides[].resources.claims[].name <string> (required)	# Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container.
spec.taskRunSpecs[].sidecarOverrides[].resources.claims[].request <string>	# Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request.
spec.taskRunSpecs[].sidecarOverrides[].resources.limits <object (free-form)>	# Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spec.taskRunSpecs[].sidecarOverrides[].resources.requests <object (free-form)>	# Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spec.taskRunSpecs[].stepOverrides <[]object>	# StepOverrides
spec.taskRunSpecs[].stepOverrides[].name <string> (required)	# Name
spec.taskRunSpecs[].stepOverrides[].resources <object> (required)	# Resources
spec.taskRunSpecs[].stepOverrides[].resources.claims <[]object>	# Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container. This field depends on the DynamicResourceAllocation feature gate. This field is immutable. It can only be set for containers.
spec.taskRunSpecs[].stepOverrides[].resources.claims[].name <string> (required)	# Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container.
spec.taskRunSpecs[].stepOverrides[].resources.claims[].request <string>	# Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request.
spec.taskRunSpecs[].stepOverrides[].resources.limits <object (free-form)>	# Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spec.taskRunSpecs[].stepOverrides[].resources.requests <object (free-form)>	# Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spec.taskRunSpecs[].taskPodTemplate <object>	# PodTemplate holds pod specific configuration
spec.taskRunSpecs[].taskPodTemplate.affinity <any>	# If specified, the pod's scheduling constraints. See Pod.spec.affinity (API version: v1)
spec.taskRunSpecs[].taskPodTemplate.automountServiceAccountToken <boolean>	# AutomountServiceAccountToken indicates whether pods running as this service account should have an API token automatically mounted.
spec.taskRunSpecs[].taskPodTemplate.dnsConfig <object>	# Specifies the DNS parameters of a pod. Parameters specified here will be merged to the generated DNS configuration based on DNSPolicy.
spec.taskRunSpecs[].taskPodTemplate.dnsConfig.nameservers <[]string>	# A list of DNS name server IP addresses. This will be appended to the base nameservers generated from DNSPolicy. Duplicated nameservers will be removed.
spec.taskRunSpecs[].taskPodTemplate.dnsConfig.options <[]object>	# A list of DNS resolver options. This will be merged with the base options generated from DNSPolicy. Duplicated entries will be removed. Resolution options given in Options will override those that appear in the base DNSPolicy.
spec.taskRunSpecs[].taskPodTemplate.dnsConfig.options[].name <string>	# Name is this DNS resolver option's name. Required.
spec.taskRunSpecs[].taskPodTemplate.dnsConfig.options[].value <string>	# Value is this DNS resolver option's value.
spec.taskRunSpecs[].taskPodTemplate.dnsConfig.searches <[]string>	# A list of DNS search domains for host-name lookup. This will be appended to the base search paths generated from DNSPolicy. Duplicated search paths will be removed.
spec.taskRunSpecs[].taskPodTemplate.dnsPolicy <string>	# Set DNS policy for the pod. Defaults to "ClusterFirst". Valid values are 'ClusterFirst', 'Default' or 'None'. DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.
spec.taskRunSpecs[].taskPodTemplate.enableServiceLinks <boolean>	# EnableServiceLinks indicates whether information about services should be injected into pod's environment variables, matching the syntax of Docker links. Optional: Defaults to true.
spec.taskRunSpecs[].taskPodTemplate.env <[]object>	# List of environment variables that can be provided to the containers belonging to the pod.
spec.taskRunSpecs[].taskPodTemplate.env[].name <string> (required)	# Name of the environment variable. May consist of any printable ASCII characters except '='.
spec.taskRunSpecs[].taskPodTemplate.env[].value <string>	# Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom <object>	# Source for the environment variable's value. Cannot be used if value is not empty.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.configMapKeyRef.key <string> (required)	# The key to select.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fileKeyRef <object>	# FileKeyRef selects a key of the env file. Requires the EnvFiles feature gate to be enabled.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fileKeyRef.key <string> (required)	# The key within the env file. An invalid key will prevent the pod from starting. The keys defined within a source may consist of any printable ASCII characters except '='. During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fileKeyRef.optional <boolean> default=false	# Specify whether the file or its key must be defined. If the file or key does not exist, then the env var is not published. If optional is set to true and the specified key does not exist, the environment variable will not be set in the Pod's containers. If optional is set to false and the specified key does not exist, an error will be returned during Pod creation.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fileKeyRef.path <string> (required)	# The path within the volume from which to select the file. Must be relative and may not contain the '..' path or start with '..'.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.fileKeyRef.volumeName <string> (required)	# The name of the volume mount containing the env file.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.resourceFieldRef <object>	# Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.resourceFieldRef.containerName <string>	# Container name: required for volumes, optional for env vars
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.resourceFieldRef.divisor <int-or-string>	# Specifies the output format of the exposed resources, defaults to "1"
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.resourceFieldRef.resource <string> (required)	# Required: resource to select
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.taskRunSpecs[].taskPodTemplate.env[].valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.taskRunSpecs[].taskPodTemplate.hostAliases <[]object>	# HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts file if specified. This is only valid for non-hostNetwork pods.
spec.taskRunSpecs[].taskPodTemplate.hostAliases[].hostnames <[]string>	# Hostnames for the above IP address.
spec.taskRunSpecs[].taskPodTemplate.hostAliases[].ip <string> (required)	# IP address of the host file entry.
spec.taskRunSpecs[].taskPodTemplate.hostNetwork <boolean>	# HostNetwork specifies whether the pod may use the node network namespace
spec.taskRunSpecs[].taskPodTemplate.hostUsers <boolean>	# HostUsers indicates whether the pod will use the host's user namespace. Optional: Default to true. If set to true or not present, the pod will be run in the host user namespace, useful for when the pod needs a feature only available to the host user namespace, such as loading a kernel module with CAP_SYS_MODULE. When set to false, a new user namespace is created for the pod. Setting false is useful to mitigating container breakout vulnerabilities such as allowing containers to run as root without their user having root privileges on the host. This field depends on the kubernetes feature gate UserNamespacesSupport being enabled.
spec.taskRunSpecs[].taskPodTemplate.imagePullSecrets <[]object>	# ImagePullSecrets gives the name of the secret used by the pod to pull the image if specified
spec.taskRunSpecs[].taskPodTemplate.imagePullSecrets[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.taskRunSpecs[].taskPodTemplate.nodeSelector <map[string]string>	# NodeSelector is a selector which must be true for the pod to fit on a node. Selector which must match a node's labels for the pod to be scheduled on that node. More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/
spec.taskRunSpecs[].taskPodTemplate.priorityClassName <string>	# If specified, indicates the pod's priority. "system-node-critical" and "system-cluster-critical" are two special keywords which indicate the highest priorities with the former being the highest priority. Any other name must be defined by creating a PriorityClass object with that name. If not specified, the pod priority will be default or zero if there is no default.
spec.taskRunSpecs[].taskPodTemplate.runtimeClassName <string>	# RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used to run this pod. If no RuntimeClass resource matches the named class, the pod will not be run. If unset or empty, the "legacy" RuntimeClass will be used, which is an implicit class with an empty definition that uses the default runtime handler. More info: https://git.k8s.io/enhancements/keps/sig-node/runtime-class.md This is a beta feature as of Kubernetes v1.14.
spec.taskRunSpecs[].taskPodTemplate.schedulerName <string>	# SchedulerName specifies the scheduler to be used to dispatch the Pod
spec.taskRunSpecs[].taskPodTemplate.securityContext <any>	# SecurityContext holds pod-level security attributes and common container settings. Optional: Defaults to empty. See type description for default values of each field. See Pod.spec.securityContext (API version: v1)
spec.taskRunSpecs[].taskPodTemplate.tolerations <[]object>	# If specified, the pod's tolerations.
spec.taskRunSpecs[].taskPodTemplate.tolerations[].effect <string>	# Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
spec.taskRunSpecs[].taskPodTemplate.tolerations[].key <string>	# Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.
spec.taskRunSpecs[].taskPodTemplate.tolerations[].operator <string>	# Operator represents a key's relationship to the value. Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category. Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
spec.taskRunSpecs[].taskPodTemplate.tolerations[].tolerationSeconds <integer> format=int64	# TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.
spec.taskRunSpecs[].taskPodTemplate.tolerations[].value <string>	# Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints <[]object>	# TopologySpreadConstraints controls how Pods are spread across your cluster among failure-domains such as regions, zones, nodes, and other user-defined topology domains.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].labelSelector <object>	# LabelSelector is used to find matching pods. Pods that match this label selector are counted to determine the number of pods in their corresponding topology domain.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].matchLabelKeys <[]string>	# MatchLabelKeys is a set of pod label keys to select the pods over which spreading will be calculated. The keys are used to lookup values from the incoming pod labels, those key-value labels are ANDed with labelSelector to select the group of existing pods over which spreading will be calculated for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector. MatchLabelKeys cannot be set when LabelSelector isn't set. Keys that don't exist in the incoming pod labels will be ignored. A null or empty list means only match against labelSelector. This is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default).
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].maxSkew <integer> (required) format=int32	# MaxSkew describes the degree to which pods may be unevenly distributed. When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference between the number of matching pods in the target topology and the global minimum. The global minimum is the minimum number of matching pods in an eligible domain or zero if the number of eligible domains is less than MinDomains. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 2/2/1: In this case, the global minimum is 1. | zone1 | zone2 | zone3 | | P P | P P | P | - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2; scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2) violate MaxSkew(1). - if MaxSkew is 2, incoming pod can be scheduled onto any zone. When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence to topologies that satisfy it. It's a required field. Default value is 1 and 0 is not allowed.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].minDomains <integer> format=int32	# MinDomains indicates a minimum number of eligible domains. When the number of eligible domains with matching topology keys is less than minDomains, Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed. And when the number of eligible domains with matching topology keys equals or greater than minDomains, this value has no effect on scheduling. As a result, when the number of eligible domains is less than minDomains, scheduler won't schedule more than maxSkew Pods to those domains. If value is nil, the constraint behaves as if MinDomains is equal to 1. Valid values are integers greater than 0. When value is not nil, WhenUnsatisfiable must be DoNotSchedule. For example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same labelSelector spread as 2/2/2: | zone1 | zone2 | zone3 | | P P | P P | P P | The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0. In this situation, new pod with the same labelSelector cannot be scheduled, because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones, it will violate MaxSkew.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].nodeAffinityPolicy <string>	# NodeAffinityPolicy indicates how we will treat Pod's nodeAffinity/nodeSelector when calculating pod topology spread skew. Options are: - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations. - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations. If this value is nil, the behavior is equivalent to the Honor policy.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].nodeTaintsPolicy <string>	# NodeTaintsPolicy indicates how we will treat node taints when calculating pod topology spread skew. Options are: - Honor: nodes without taints, along with tainted nodes for which the incoming pod has a toleration, are included. - Ignore: node taints are ignored. All nodes are included. If this value is nil, the behavior is equivalent to the Ignore policy.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].topologyKey <string> (required)	# TopologyKey is the key of node labels. Nodes that have a label with this key and identical values are considered to be in the same topology. We consider each <key, value> as a "bucket", and try to put balanced number of pods into each bucket. We define a domain as a particular instance of a topology. Also, we define an eligible domain as a domain whose nodes meet the requirements of nodeAffinityPolicy and nodeTaintsPolicy. e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology. And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology. It's a required field.
spec.taskRunSpecs[].taskPodTemplate.topologySpreadConstraints[].whenUnsatisfiable <string> (required)	# WhenUnsatisfiable indicates how to deal with a pod if it doesn't satisfy the spread constraint. - DoNotSchedule (default) tells the scheduler not to schedule it. - ScheduleAnyway tells the scheduler to schedule the pod in any location, but giving higher precedence to topologies that would help reduce the skew. A constraint is considered "Unsatisfiable" for an incoming pod if and only if every possible node assignment for that pod would violate "MaxSkew" on some topology. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 3/1/1: | zone1 | zone2 | zone3 | | P P P | P | P | If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler won't make it *more* imbalanced. It's a required field.
spec.taskRunSpecs[].taskPodTemplate.volumes <any>	# List of volumes that can be mounted by containers belonging to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes See Pod.spec.volumes (API version: v1)
spec.taskRunSpecs[].taskServiceAccountName <string>
spec.taskRunSpecs[].timeout <string>	# Timeout
spec.timeout <string>	# Deprecated: use pipelineRunSpec.Timeouts.Pipeline instead Timeout
spec.timeouts <object>	# Timeouts
spec.timeouts.finally <string>	# Finally
spec.timeouts.pipeline <string>	# Pipeline
spec.timeouts.tasks <string>	# Tasks
spec.workspaces <[]object>	# Workspaces
spec.workspaces[].configMap <object>	# ConfigMap
spec.workspaces[].configMap.defaultMode <integer> format=int32	# defaultMode is optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].configMap.items <[]object>	# items if unspecified, each key-value pair in the Data field of the referenced ConfigMap will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the ConfigMap, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the '..' path or start with '..'.
spec.workspaces[].configMap.items[].key <string> (required)	# key is the key to project.
spec.workspaces[].configMap.items[].mode <integer> format=int32	# mode is Optional: mode bits used to set permissions on this file. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].configMap.items[].path <string> (required)	# path is the relative path of the file to map the key to. May not be an absolute path. May not contain the path element '..'. May not start with the string '..'.
spec.workspaces[].configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.workspaces[].configMap.optional <boolean>	# optional specify whether the ConfigMap or its keys must be defined
spec.workspaces[].csi <object>	# CSI
spec.workspaces[].csi.driver <string> (required)	# driver is the name of the CSI driver that handles this volume. Consult with your admin for the correct name as registered in the cluster.
spec.workspaces[].csi.fsType <string>	# fsType to mount. Ex. "ext4", "xfs", "ntfs". If not provided, the empty value is passed to the associated CSI driver which will determine the default filesystem to apply.
spec.workspaces[].csi.nodePublishSecretRef <object>	# nodePublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodePublishVolume and NodeUnpublishVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secret references are passed.
spec.workspaces[].csi.nodePublishSecretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.workspaces[].csi.readOnly <boolean>	# readOnly specifies a read-only configuration for the volume. Defaults to false (read/write).
spec.workspaces[].csi.volumeAttributes <map[string]string>	# volumeAttributes stores driver-specific properties that are passed to the CSI driver. Consult your driver's documentation for supported values.
spec.workspaces[].emptyDir <object>	# EmptyDir
spec.workspaces[].emptyDir.medium <string>	# medium represents what type of storage medium should back this directory. The default is "" which means to use the node's default medium. Must be an empty string (default) or Memory. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
spec.workspaces[].emptyDir.sizeLimit <int-or-string>	# sizeLimit is the total amount of local storage required for this EmptyDir volume. The size limit is also applicable for memory medium. The maximum usage on memory medium EmptyDir would be the minimum value between the SizeLimit specified here and the sum of memory limits of all containers in a pod. The default is nil which means that the limit is undefined. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
spec.workspaces[].name <string> (required)	# Name
spec.workspaces[].persistentVolumeClaim <object>	# PersistentVolumeClaim
spec.workspaces[].persistentVolumeClaim.claimName <string> (required)	# claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
spec.workspaces[].persistentVolumeClaim.readOnly <boolean>	# readOnly Will force the ReadOnly setting in VolumeMounts. Default false.
spec.workspaces[].projected <object>	# Projected
spec.workspaces[].projected.defaultMode <integer> format=int32	# defaultMode are the mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].projected.sources <[]object>	# sources is the list of volume projections. Each entry in this list handles one source.
spec.workspaces[].projected.sources[].clusterTrustBundle <object>	# ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file. Alpha, gated by the ClusterTrustBundleProjection feature gate. ClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector. Kubelet performs aggressive normalization of the PEM contents written into the pod filesystem. Esoteric PEM features such as inter-block comments and block headers are stripped. Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time.
spec.workspaces[].projected.sources[].clusterTrustBundle.labelSelector <object>	# Select all ClusterTrustBundles that match this label selector. Only has effect if signerName is set. Mutually-exclusive with name. If unset, interpreted as "match nothing". If set but empty, interpreted as "match everything".
spec.workspaces[].projected.sources[].clusterTrustBundle.labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.workspaces[].projected.sources[].clusterTrustBundle.labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.workspaces[].projected.sources[].clusterTrustBundle.labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.workspaces[].projected.sources[].clusterTrustBundle.labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.workspaces[].projected.sources[].clusterTrustBundle.labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.workspaces[].projected.sources[].clusterTrustBundle.name <string>	# Select a single ClusterTrustBundle by object name. Mutually-exclusive with signerName and labelSelector.
spec.workspaces[].projected.sources[].clusterTrustBundle.optional <boolean>	# If true, don't block pod startup if the referenced ClusterTrustBundle(s) aren't available. If using name, then the named ClusterTrustBundle is allowed not to exist. If using signerName, then the combination of signerName and labelSelector is allowed to match zero ClusterTrustBundles.
spec.workspaces[].projected.sources[].clusterTrustBundle.path <string> (required)	# Relative path from the volume root to write the bundle.
spec.workspaces[].projected.sources[].clusterTrustBundle.signerName <string>	# Select all ClusterTrustBundles that match this signer name. Mutually-exclusive with name. The contents of all selected ClusterTrustBundles will be unified and deduplicated.
spec.workspaces[].projected.sources[].configMap <object>	# configMap information about the configMap data to project
spec.workspaces[].projected.sources[].configMap.items <[]object>	# items if unspecified, each key-value pair in the Data field of the referenced ConfigMap will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the ConfigMap, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the '..' path or start with '..'.
spec.workspaces[].projected.sources[].configMap.items[].key <string> (required)	# key is the key to project.
spec.workspaces[].projected.sources[].configMap.items[].mode <integer> format=int32	# mode is Optional: mode bits used to set permissions on this file. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].projected.sources[].configMap.items[].path <string> (required)	# path is the relative path of the file to map the key to. May not be an absolute path. May not contain the path element '..'. May not start with the string '..'.
spec.workspaces[].projected.sources[].configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.workspaces[].projected.sources[].configMap.optional <boolean>	# optional specify whether the ConfigMap or its keys must be defined
spec.workspaces[].projected.sources[].downwardAPI <object>	# downwardAPI information about the downwardAPI data to project
spec.workspaces[].projected.sources[].downwardAPI.items <[]object>	# Items is a list of DownwardAPIVolume file
spec.workspaces[].projected.sources[].downwardAPI.items[].fieldRef <object>	# Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.
spec.workspaces[].projected.sources[].downwardAPI.items[].fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.workspaces[].projected.sources[].downwardAPI.items[].fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.workspaces[].projected.sources[].downwardAPI.items[].mode <integer> format=int32	# Optional: mode bits used to set permissions on this file, must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].projected.sources[].downwardAPI.items[].path <string> (required)	# Required: Path is the relative path name of the file to be created. Must not be absolute or contain the '..' path. Must be utf-8 encoded. The first item of the relative path must not start with '..'
spec.workspaces[].projected.sources[].downwardAPI.items[].resourceFieldRef <object>	# Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.
spec.workspaces[].projected.sources[].downwardAPI.items[].resourceFieldRef.containerName <string>	# Container name: required for volumes, optional for env vars
spec.workspaces[].projected.sources[].downwardAPI.items[].resourceFieldRef.divisor <int-or-string>	# Specifies the output format of the exposed resources, defaults to "1"
spec.workspaces[].projected.sources[].downwardAPI.items[].resourceFieldRef.resource <string> (required)	# Required: resource to select
spec.workspaces[].projected.sources[].podCertificate <object>	# Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server. Kubelet generates a private key and uses it to send a PodCertificateRequest to the named signer. Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem. The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec. Kubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp. Kubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields. The credential bundle is a single file in PEM format. The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order). Prefer using the credential bundle format, since your application code can read it atomically. If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other. Your application will need to check for this condition, and re-read until they are consistent. The named signer controls chooses the format of the certificate it issues; consult the signer implementation's documentation to learn how to use the certificates it issues.
spec.workspaces[].projected.sources[].podCertificate.certificateChainPath <string>	# Write the certificate chain at this path in the projected volume. Most applications should use credentialBundlePath. When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation.
spec.workspaces[].projected.sources[].podCertificate.credentialBundlePath <string>	# Write the credential bundle at this path in the projected volume. The credential bundle is a single file that contains multiple PEM blocks. The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private key. The remaining blocks are CERTIFICATE blocks, containing the issued certificate chain from the signer (leaf and any intermediates). Using credentialBundlePath lets your Pod's application code make a single atomic read that retrieves a consistent key and certificate chain. If you project them to separate files, your application code will need to additionally check that the leaf certificate was issued to the key.
spec.workspaces[].projected.sources[].podCertificate.keyPath <string>	# Write the key at this path in the projected volume. Most applications should use credentialBundlePath. When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation.
spec.workspaces[].projected.sources[].podCertificate.keyType <string> (required)	# The type of keypair Kubelet will generate for the pod. Valid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384", "ECDSAP521", and "ED25519".
spec.workspaces[].projected.sources[].podCertificate.maxExpirationSeconds <integer> format=int32	# maxExpirationSeconds is the maximum lifetime permitted for the certificate. Kubelet copies this value verbatim into the PodCertificateRequests it generates for this projection. If omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver will reject values shorter than 3600 (1 hour). The maximum allowable value is 7862400 (91 days). The signer implementation is then free to issue a certificate with any lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600 seconds (1 hour). This constraint is enforced by kube-apiserver. `kubernetes.io` signers will never issue certificates with a lifetime longer than 24 hours.
spec.workspaces[].projected.sources[].podCertificate.signerName <string> (required)	# Kubelet's generated CSRs will be addressed to this signer.
spec.workspaces[].projected.sources[].podCertificate.userAnnotations <map[string]string>	# userAnnotations allow pod authors to pass additional information to the signer implementation. Kubernetes does not restrict or validate this metadata in any way. These values are copied verbatim into the `spec.unverifiedUserAnnotations` field of the PodCertificateRequest objects that Kubelet creates. Entries are subject to the same validation as object metadata annotations, with the addition that all keys must be domain-prefixed. No restrictions are placed on values, except an overall size limitation on the entire field. Signers should document the keys and values they support. Signers should deny requests that contain keys they do not recognize.
spec.workspaces[].projected.sources[].secret <object>	# secret information about the secret data to project
spec.workspaces[].projected.sources[].secret.items <[]object>	# items if unspecified, each key-value pair in the Data field of the referenced Secret will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the Secret, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the '..' path or start with '..'.
spec.workspaces[].projected.sources[].secret.items[].key <string> (required)	# key is the key to project.
spec.workspaces[].projected.sources[].secret.items[].mode <integer> format=int32	# mode is Optional: mode bits used to set permissions on this file. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].projected.sources[].secret.items[].path <string> (required)	# path is the relative path of the file to map the key to. May not be an absolute path. May not contain the path element '..'. May not start with the string '..'.
spec.workspaces[].projected.sources[].secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.workspaces[].projected.sources[].secret.optional <boolean>	# optional field specify whether the Secret or its key must be defined
spec.workspaces[].projected.sources[].serviceAccountToken <object>	# serviceAccountToken is information about the serviceAccountToken data to project
spec.workspaces[].projected.sources[].serviceAccountToken.audience <string>	# audience is the intended audience of the token. A recipient of a token must identify itself with an identifier specified in the audience of the token, and otherwise should reject the token. The audience defaults to the identifier of the apiserver.
spec.workspaces[].projected.sources[].serviceAccountToken.expirationSeconds <integer> format=int64	# expirationSeconds is the requested duration of validity of the service account token. As the token approaches expiration, the kubelet volume plugin will proactively rotate the service account token. The kubelet will start trying to rotate the token if the token is older than 80 percent of its time to live or if the token is older than 24 hours.Defaults to 1 hour and must be at least 10 minutes.
spec.workspaces[].projected.sources[].serviceAccountToken.path <string> (required)	# path is the path relative to the mount point of the file to project the token into.
spec.workspaces[].secret <object>	# Secret
spec.workspaces[].secret.defaultMode <integer> format=int32	# defaultMode is Optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].secret.items <[]object>	# items If unspecified, each key-value pair in the Data field of the referenced Secret will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the Secret, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the '..' path or start with '..'.
spec.workspaces[].secret.items[].key <string> (required)	# key is the key to project.
spec.workspaces[].secret.items[].mode <integer> format=int32	# mode is Optional: mode bits used to set permissions on this file. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set.
spec.workspaces[].secret.items[].path <string> (required)	# path is the relative path of the file to map the key to. May not be an absolute path. May not contain the path element '..'. May not start with the string '..'.
spec.workspaces[].secret.optional <boolean>	# optional field specify whether the Secret or its keys must be defined
spec.workspaces[].secret.secretName <string>	# secretName is the name of the secret in the pod's namespace to use. More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
spec.workspaces[].subPath <string>	# SubPath
spec.workspaces[].volumeClaimTemplate <any>	# VolumeClaimTemplate
status <object>	# Status
status.annotations <map[string]string>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.childReferences <[]object>	# ChildReferences
status.childReferences[].apiVersion <string>
status.childReferences[].displayName <string>	# DisplayName
status.childReferences[].kind <string>
status.childReferences[].name <string>	# Name
status.childReferences[].pipelineTaskName <string>	# PipelineTaskName
status.childReferences[].whenExpressions <[]object>	# WhenExpressions
status.childReferences[].whenExpressions[].cel <string>	# CEL
status.childReferences[].whenExpressions[].input <string>	# Input
status.childReferences[].whenExpressions[].operator <string>	# Operator
status.childReferences[].whenExpressions[].values <[]string>	# Values
status.completionTime <string> format=date-time	# CompletionTime
status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.finallyStartTime <string> format=date-time	# FinallyStartTime
status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
status.pipelineResults <[]object>	# PipelineResults
status.pipelineResults[].name <string> (required)	# Name
status.pipelineResults[].value <any> (required)	# Value
status.pipelineSpec <any>	# PipelineSpec
status.provenance <object>	# Provenance
status.provenance.configSource <object>	# ConfigSource Deprecated: Use RefSource instead
status.provenance.configSource.digest <map[string]string>	# Digest
status.provenance.configSource.entryPoint <string>	# EntryPoint
status.provenance.configSource.uri <string>	# URI
status.provenance.featureFlags <object>	# FeatureFlags
status.provenance.featureFlags.awaitSidecarReadiness <boolean>
status.provenance.featureFlags.coschedule <string>
status.provenance.featureFlags.disableCredsInit <boolean>
status.provenance.featureFlags.disableInlineSpec <string>
status.provenance.featureFlags.enableAPIFields <string>
status.provenance.featureFlags.enableArtifacts <boolean>
status.provenance.featureFlags.enableCELInWhenExpression <boolean>
status.provenance.featureFlags.enableConciseResolverSyntax <boolean>
status.provenance.featureFlags.enableKeepPodOnCancel <boolean>
status.provenance.featureFlags.enableKubernetesSidecar <boolean>
status.provenance.featureFlags.enableParamEnum <boolean>
status.provenance.featureFlags.enableProvenanceInStatus <boolean>
status.provenance.featureFlags.enableStepActions <boolean>	# EnableStepActions is a no-op flag since StepActions are stable
status.provenance.featureFlags.enableTektonOCIBundles <boolean>	# DeprecatedEnableTektonOCIBundles is maintained for backward compatibility to allow deletion of PipelineRuns created before v0.62.x. This field is not used and can be removed in a future release once we're confident old PipelineRuns have been cleaned up. See issue #8359 for context.
status.provenance.featureFlags.enableTerminationMessageCompression <boolean>
status.provenance.featureFlags.enableWaitExponentialBackoff <boolean>
status.provenance.featureFlags.enforceNonfalsifiability <string>
status.provenance.featureFlags.maxResultSize <integer>
status.provenance.featureFlags.requireGitSSHSecretKnownHosts <boolean>
status.provenance.featureFlags.resultExtractionMethod <string>
status.provenance.featureFlags.runningInEnvWithInjectedSidecars <boolean>
status.provenance.featureFlags.sendCloudEventsForRuns <boolean>
status.provenance.featureFlags.setSecurityContext <boolean>
status.provenance.featureFlags.setSecurityContextReadOnlyRootFilesystem <boolean>
status.provenance.featureFlags.verificationNoMatchPolicy <string>	# VerificationNoMatchPolicy is the feature flag for "trusted-resources-verification-no-match-policy" VerificationNoMatchPolicy can be set to "ignore", "warn" and "fail" values. ignore: skip trusted resources verification when no matching verification policies found warn: skip trusted resources verification when no matching verification policies found and log a warning fail: fail the taskrun or pipelines run if no matching verification policies found
status.provenance.refSource <object>	# RefSource
status.provenance.refSource.digest <map[string]string>	# Digest
status.provenance.refSource.entryPoint <string>	# EntryPoint
status.provenance.refSource.uri <string>	# URI
status.runs <map[string]object>	# Runs
status.runs.<key>.pipelineTaskName <string>	# PipelineTaskName
status.runs.<key>.status <object>	# Status
status.runs.<key>.status.annotations <map[string]string>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.runs.<key>.status.completionTime <string> format=date-time	# CompletionTime is the time the build completed.
status.runs.<key>.status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.runs.<key>.status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.runs.<key>.status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.runs.<key>.status.conditions[].reason <string>	# The reason for the condition's last transition.
status.runs.<key>.status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.runs.<key>.status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.runs.<key>.status.conditions[].type <string> (required)	# Type of condition.
status.runs.<key>.status.extraFields <any>	# ExtraFields holds arbitrary fields provided by the custom task controller.
status.runs.<key>.status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
status.runs.<key>.status.results <[]object>	# Results reports any output result values to be consumed by later tasks in a pipeline.
status.runs.<key>.status.results[].name <string> (required)	# Name the given name
status.runs.<key>.status.results[].value <string> (required)	# Value the given value of the result
status.runs.<key>.status.retriesStatus <any>	# RetriesStatus contains the history of CustomRunStatus, in case of a retry. See CustomRun.status (API version: tekton.dev/v1beta1)
status.runs.<key>.status.startTime <string> format=date-time	# StartTime is the time the build is actually started.
status.runs.<key>.whenExpressions <[]object>	# WhenExpressions
status.runs.<key>.whenExpressions[].cel <string>	# CEL
status.runs.<key>.whenExpressions[].input <string>	# Input
status.runs.<key>.whenExpressions[].operator <string>	# Operator
status.runs.<key>.whenExpressions[].values <[]string>	# Values
status.skippedTasks <[]object>	# SkippedTasks
status.skippedTasks[].name <string> (required)	# Name
status.skippedTasks[].reason <string> (required)	# Reason
status.skippedTasks[].whenExpressions <[]object>	# WhenExpressions
status.skippedTasks[].whenExpressions[].cel <string>	# CEL
status.skippedTasks[].whenExpressions[].input <string>	# Input
status.skippedTasks[].whenExpressions[].operator <string>	# Operator
status.skippedTasks[].whenExpressions[].values <[]string>	# Values
status.spanContext <map[string]string>	# SpanContext
status.startTime <string> format=date-time	# StartTime
status.taskRuns <map[string]object>	# TaskRuns
status.taskRuns.<key>.pipelineTaskName <string>	# PipelineTaskName
status.taskRuns.<key>.status <object>	# Status
status.taskRuns.<key>.status.annotations <map[string]string>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.taskRuns.<key>.status.cloudEvents <[]object>	# CloudEvents
status.taskRuns.<key>.status.cloudEvents[].status <object>	# CloudEventDeliveryState
status.taskRuns.<key>.status.cloudEvents[].status.condition <string>	# Condition
status.taskRuns.<key>.status.cloudEvents[].status.message <string> (required)	# Error
status.taskRuns.<key>.status.cloudEvents[].status.retryCount <integer> (required) format=int32	# RetryCount
status.taskRuns.<key>.status.cloudEvents[].status.sentAt <string> format=date-time	# SentAt
status.taskRuns.<key>.status.cloudEvents[].target <string>	# Target
status.taskRuns.<key>.status.completionTime <string> format=date-time	# CompletionTime
status.taskRuns.<key>.status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.taskRuns.<key>.status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.taskRuns.<key>.status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.taskRuns.<key>.status.conditions[].reason <string>	# The reason for the condition's last transition.
status.taskRuns.<key>.status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.taskRuns.<key>.status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.taskRuns.<key>.status.conditions[].type <string> (required)	# Type of condition.
status.taskRuns.<key>.status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
status.taskRuns.<key>.status.podName <string> (required)	# PodName
status.taskRuns.<key>.status.provenance <object>	# Provenance
status.taskRuns.<key>.status.provenance.configSource <object>	# ConfigSource Deprecated: Use RefSource instead
status.taskRuns.<key>.status.provenance.configSource.digest <map[string]string>	# Digest
status.taskRuns.<key>.status.provenance.configSource.entryPoint <string>	# EntryPoint
status.taskRuns.<key>.status.provenance.configSource.uri <string>	# URI
status.taskRuns.<key>.status.provenance.featureFlags <object>	# FeatureFlags
status.taskRuns.<key>.status.provenance.featureFlags.awaitSidecarReadiness <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.coschedule <string>
status.taskRuns.<key>.status.provenance.featureFlags.disableCredsInit <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.disableInlineSpec <string>
status.taskRuns.<key>.status.provenance.featureFlags.enableAPIFields <string>
status.taskRuns.<key>.status.provenance.featureFlags.enableArtifacts <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableCELInWhenExpression <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableConciseResolverSyntax <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableKeepPodOnCancel <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableKubernetesSidecar <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableParamEnum <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableProvenanceInStatus <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableStepActions <boolean>	# EnableStepActions is a no-op flag since StepActions are stable
status.taskRuns.<key>.status.provenance.featureFlags.enableTektonOCIBundles <boolean>	# DeprecatedEnableTektonOCIBundles is maintained for backward compatibility to allow deletion of PipelineRuns created before v0.62.x. This field is not used and can be removed in a future release once we're confident old PipelineRuns have been cleaned up. See issue #8359 for context.
status.taskRuns.<key>.status.provenance.featureFlags.enableTerminationMessageCompression <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enableWaitExponentialBackoff <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.enforceNonfalsifiability <string>
status.taskRuns.<key>.status.provenance.featureFlags.maxResultSize <integer>
status.taskRuns.<key>.status.provenance.featureFlags.requireGitSSHSecretKnownHosts <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.resultExtractionMethod <string>
status.taskRuns.<key>.status.provenance.featureFlags.runningInEnvWithInjectedSidecars <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.sendCloudEventsForRuns <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.setSecurityContext <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.setSecurityContextReadOnlyRootFilesystem <boolean>
status.taskRuns.<key>.status.provenance.featureFlags.verificationNoMatchPolicy <string>	# VerificationNoMatchPolicy is the feature flag for "trusted-resources-verification-no-match-policy" VerificationNoMatchPolicy can be set to "ignore", "warn" and "fail" values. ignore: skip trusted resources verification when no matching verification policies found warn: skip trusted resources verification when no matching verification policies found and log a warning fail: fail the taskrun or pipelines run if no matching verification policies found
status.taskRuns.<key>.status.provenance.refSource <object>	# RefSource
status.taskRuns.<key>.status.provenance.refSource.digest <map[string]string>	# Digest
status.taskRuns.<key>.status.provenance.refSource.entryPoint <string>	# EntryPoint
status.taskRuns.<key>.status.provenance.refSource.uri <string>	# URI
status.taskRuns.<key>.status.resourcesResult <[]object>	# ResourcesResult Deprecated: this field is not populated and is preserved only for backwards compatibility
status.taskRuns.<key>.status.resourcesResult[].key <string> (required)
status.taskRuns.<key>.status.resourcesResult[].resourceName <string>	# ResourceName may be used in tests, but it is not populated in termination messages. It is preserved here for backwards compatibility and will not be ported to v1.
status.taskRuns.<key>.status.resourcesResult[].type <integer>	# ResultType used to find out whether a RunResult is from a task result or not Note that ResultsType is another type which is used to define the data type (e.g. string, array, etc) we used for Results
status.taskRuns.<key>.status.resourcesResult[].value <string> (required)
status.taskRuns.<key>.status.retriesStatus <any>	# RetriesStatus
status.taskRuns.<key>.status.sidecars <[]object>	# Sidecars
status.taskRuns.<key>.status.sidecars[].container <string>
status.taskRuns.<key>.status.sidecars[].imageID <string>
status.taskRuns.<key>.status.sidecars[].name <string>
status.taskRuns.<key>.status.sidecars[].running <object>	# Details about a running container
status.taskRuns.<key>.status.sidecars[].running.startedAt <string> format=date-time	# Time at which the container was last (re-)started
status.taskRuns.<key>.status.sidecars[].terminated <object>	# Details about a terminated container
status.taskRuns.<key>.status.sidecars[].terminated.containerID <string>	# Container's ID in the format '<type>://<container_id>'
status.taskRuns.<key>.status.sidecars[].terminated.exitCode <integer> (required) format=int32	# Exit status from the last termination of the container
status.taskRuns.<key>.status.sidecars[].terminated.finishedAt <string> format=date-time	# Time at which the container last terminated
status.taskRuns.<key>.status.sidecars[].terminated.message <string>	# Message regarding the last termination of the container
status.taskRuns.<key>.status.sidecars[].terminated.reason <string>	# (brief) reason from the last termination of the container
status.taskRuns.<key>.status.sidecars[].terminated.signal <integer> format=int32	# Signal from the last termination of the container
status.taskRuns.<key>.status.sidecars[].terminated.startedAt <string> format=date-time	# Time at which previous execution of the container started
status.taskRuns.<key>.status.sidecars[].waiting <object>	# Details about a waiting container
status.taskRuns.<key>.status.sidecars[].waiting.message <string>	# Message regarding why the container is not yet running.
status.taskRuns.<key>.status.sidecars[].waiting.reason <string>	# (brief) reason the container is not yet running.
status.taskRuns.<key>.status.spanContext <map[string]string>	# SpanContext
status.taskRuns.<key>.status.startTime <string> format=date-time	# StartTime
status.taskRuns.<key>.status.steps <[]object>	# Steps
status.taskRuns.<key>.status.steps[].container <string>
status.taskRuns.<key>.status.steps[].imageID <string>
status.taskRuns.<key>.status.steps[].inputs <[]object>
status.taskRuns.<key>.status.steps[].inputs[].buildOutput <boolean>	# BuildOutput
status.taskRuns.<key>.status.steps[].inputs[].name <string>	# Name
status.taskRuns.<key>.status.steps[].inputs[].values <[]object>	# Values
status.taskRuns.<key>.status.steps[].inputs[].values[].digest <map[string]string>
status.taskRuns.<key>.status.steps[].inputs[].values[].uri <string>
status.taskRuns.<key>.status.steps[].name <string>
status.taskRuns.<key>.status.steps[].outputs <[]object>
status.taskRuns.<key>.status.steps[].outputs[].buildOutput <boolean>	# BuildOutput
status.taskRuns.<key>.status.steps[].outputs[].name <string>	# Name
status.taskRuns.<key>.status.steps[].outputs[].values <[]object>	# Values
status.taskRuns.<key>.status.steps[].outputs[].values[].digest <map[string]string>
status.taskRuns.<key>.status.steps[].outputs[].values[].uri <string>
status.taskRuns.<key>.status.steps[].provenance <object>	# Provenance
status.taskRuns.<key>.status.steps[].provenance.configSource <object>	# ConfigSource Deprecated: Use RefSource instead
status.taskRuns.<key>.status.steps[].provenance.configSource.digest <map[string]string>	# Digest
status.taskRuns.<key>.status.steps[].provenance.configSource.entryPoint <string>	# EntryPoint
status.taskRuns.<key>.status.steps[].provenance.configSource.uri <string>	# URI
status.taskRuns.<key>.status.steps[].provenance.featureFlags <object>	# FeatureFlags
status.taskRuns.<key>.status.steps[].provenance.featureFlags.awaitSidecarReadiness <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.coschedule <string>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.disableCredsInit <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.disableInlineSpec <string>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableAPIFields <string>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableArtifacts <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableCELInWhenExpression <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableConciseResolverSyntax <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableKeepPodOnCancel <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableKubernetesSidecar <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableParamEnum <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableProvenanceInStatus <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableStepActions <boolean>	# EnableStepActions is a no-op flag since StepActions are stable
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableTektonOCIBundles <boolean>	# DeprecatedEnableTektonOCIBundles is maintained for backward compatibility to allow deletion of PipelineRuns created before v0.62.x. This field is not used and can be removed in a future release once we're confident old PipelineRuns have been cleaned up. See issue #8359 for context.
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableTerminationMessageCompression <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enableWaitExponentialBackoff <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.enforceNonfalsifiability <string>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.maxResultSize <integer>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.requireGitSSHSecretKnownHosts <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.resultExtractionMethod <string>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.runningInEnvWithInjectedSidecars <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.sendCloudEventsForRuns <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.setSecurityContext <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.setSecurityContextReadOnlyRootFilesystem <boolean>
status.taskRuns.<key>.status.steps[].provenance.featureFlags.verificationNoMatchPolicy <string>	# VerificationNoMatchPolicy is the feature flag for "trusted-resources-verification-no-match-policy" VerificationNoMatchPolicy can be set to "ignore", "warn" and "fail" values. ignore: skip trusted resources verification when no matching verification policies found warn: skip trusted resources verification when no matching verification policies found and log a warning fail: fail the taskrun or pipelines run if no matching verification policies found
status.taskRuns.<key>.status.steps[].provenance.refSource <object>	# RefSource
status.taskRuns.<key>.status.steps[].provenance.refSource.digest <map[string]string>	# Digest
status.taskRuns.<key>.status.steps[].provenance.refSource.entryPoint <string>	# EntryPoint
status.taskRuns.<key>.status.steps[].provenance.refSource.uri <string>	# URI
status.taskRuns.<key>.status.steps[].results <[]object>
status.taskRuns.<key>.status.steps[].results[].name <string> (required)	# Name
status.taskRuns.<key>.status.steps[].results[].type <string>	# Type
status.taskRuns.<key>.status.steps[].results[].value <any> (required)	# Value
status.taskRuns.<key>.status.steps[].running <object>	# Details about a running container
status.taskRuns.<key>.status.steps[].running.startedAt <string> format=date-time	# Time at which the container was last (re-)started
status.taskRuns.<key>.status.steps[].terminated <object>	# Details about a terminated container
status.taskRuns.<key>.status.steps[].terminated.containerID <string>	# Container's ID in the format '<type>://<container_id>'
status.taskRuns.<key>.status.steps[].terminated.exitCode <integer> (required) format=int32	# Exit status from the last termination of the container
status.taskRuns.<key>.status.steps[].terminated.finishedAt <string> format=date-time	# Time at which the container last terminated
status.taskRuns.<key>.status.steps[].terminated.message <string>	# Message regarding the last termination of the container
status.taskRuns.<key>.status.steps[].terminated.reason <string>	# (brief) reason from the last termination of the container
status.taskRuns.<key>.status.steps[].terminated.signal <integer> format=int32	# Signal from the last termination of the container
status.taskRuns.<key>.status.steps[].terminated.startedAt <string> format=date-time	# Time at which previous execution of the container started
status.taskRuns.<key>.status.steps[].waiting <object>	# Details about a waiting container
status.taskRuns.<key>.status.steps[].waiting.message <string>	# Message regarding why the container is not yet running.
status.taskRuns.<key>.status.steps[].waiting.reason <string>	# (brief) reason the container is not yet running.
status.taskRuns.<key>.status.taskResults <[]object>	# TaskRunResults
status.taskRuns.<key>.status.taskResults[].name <string> (required)	# Name
status.taskRuns.<key>.status.taskResults[].type <string>	# Type
status.taskRuns.<key>.status.taskResults[].value <any> (required)	# Value
status.taskRuns.<key>.status.taskSpec <any>	# TaskSpec
status.taskRuns.<key>.whenExpressions <[]object>	# WhenExpressions
status.taskRuns.<key>.whenExpressions[].cel <string>	# CEL
status.taskRuns.<key>.whenExpressions[].input <string>	# Input
status.taskRuns.<key>.whenExpressions[].operator <string>	# Operator
status.taskRuns.<key>.whenExpressions[].values <[]string>	# Values
