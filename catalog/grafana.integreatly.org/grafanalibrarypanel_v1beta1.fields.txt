# schema source: Grafana Operator v5.24.0 https://github.com/grafana/grafana-operator
apiVersion <string> enum=grafana.integreatly.org/v1beta1
kind <string> enum=GrafanaLibraryPanel
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# GrafanaLibraryPanelSpec defines the desired state of GrafanaLibraryPanel
spec.allowCrossNamespaceImport <boolean> default=false	# Allow the Operator to match this resource with Grafanas outside the current namespace
spec.configMapRef <object>	# model from configmap
spec.configMapRef.key <string> (required)	# The key to select.
spec.configMapRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.configMapRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.contentCacheDuration <string>	# Cache duration for models fetched from URLs
spec.datasources <[]object>	# maps required data sources to existing ones
spec.datasources[].datasourceName <string> (required)
spec.datasources[].inputName <string> (required)
spec.envFrom <[]object>	# environments variables from secrets or config maps
spec.envFrom[].configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.envFrom[].configMapKeyRef.key <string> (required)	# The key to select.
spec.envFrom[].configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.envFrom[].configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.envFrom[].secretKeyRef <object>	# Selects a key of a Secret.
spec.envFrom[].secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.envFrom[].secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.envFrom[].secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.envs <[]object>	# environments variables as a map
spec.envs[].name <string> (required)
spec.envs[].value <string>	# Inline env value
spec.envs[].valueFrom <object>	# Reference on value source, might be the reference on a secret or config map
spec.envs[].valueFrom.configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.envs[].valueFrom.configMapKeyRef.key <string> (required)	# The key to select.
spec.envs[].valueFrom.configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.envs[].valueFrom.configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.envs[].valueFrom.secretKeyRef <object>	# Selects a key of a Secret.
spec.envs[].valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.envs[].valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.envs[].valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.folderRef <string>	# Name of a `GrafanaFolder` resource in the same namespace
spec.folderUID <string>	# UID of the target folder for this dashboard
spec.grafanaCom <object>	# grafana.com/dashboards
spec.grafanaCom.id <integer> (required)
spec.grafanaCom.revision <integer>
spec.gzipJson <string> format=byte	# GzipJson the model's JSON compressed with Gzip. Base64-encoded when in YAML.
spec.instanceSelector <object> (required) (immutable)	# Selects Grafana instances for import
spec.instanceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.instanceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.instanceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.instanceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.instanceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.json <string>	# model json
spec.jsonnet <string>	# Jsonnet
spec.jsonnetLib <object>	# Jsonnet project build
spec.jsonnetLib.fileName <string> (required)
spec.jsonnetLib.gzipJsonnetProject <string> (required) format=byte
spec.jsonnetLib.jPath <[]string>
spec.oci <object>	# model from an OCI artifact (e.g. ghcr.io/team/dashboards:v1)
spec.oci.insecurePlainHTTP <boolean>	# InsecurePlainHTTP switches the registry connection to plain HTTP (non-TLS) instead of HTTPS. Intended for in-cluster or test registries; HTTPS registries with self-signed certificates are not supported. Default false.
spec.oci.path <string> (required) min=1 max=512	# Path is the path of the file to extract from the artifact.
spec.oci.pullSecretRef <object>	# PullSecretRef references a kubernetes.io/dockerconfigjson Secret in the same namespace as the CR. If omitted, anonymous pull is attempted.
spec.oci.pullSecretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.oci.reference <string> (required) pattern="^[^:@]+(:[^:@/]+|@sha256:[a-fA-F0-9]{64})$" min=3 max=512	# Reference is the full OCI artifact reference including a tag or digest, e.g. "ghcr.io/team/dashboards:v1.4.7" or "ghcr.io/team/dashboards@sha256:abc123...". Prefer a digest for reproducible deployments.
spec.plugins <[]object>	# plugins
spec.plugins[].name <string> (required) min=1
spec.plugins[].version <string> (required) pattern="^((0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?|latest)$"
spec.resyncPeriod <string> pattern="^([0-9]+(\\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$"	# How often the resource is synced, defaults to 10m0s if not set
spec.suspend <boolean>	# Suspend pauses synchronizing attempts and tells the operator to ignore changes
spec.uid <string> pattern="^[a-zA-Z0-9-_]+$" max=40 (immutable)	# Manually specify the uid, overwrites uids already present in the json model. Can be any string consisting of alphanumeric characters, - and _ with a maximum length of 40.
spec.url <string> pattern="^https?://.+$"	# model url
spec.urlAuthorization <object>	# authorization options for model from url
spec.urlAuthorization.basicAuth <object>
spec.urlAuthorization.basicAuth.password <object>	# SecretKeySelector selects a key of a Secret.
spec.urlAuthorization.basicAuth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.urlAuthorization.basicAuth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.urlAuthorization.basicAuth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.urlAuthorization.basicAuth.username <object>	# SecretKeySelector selects a key of a Secret.
spec.urlAuthorization.basicAuth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.urlAuthorization.basicAuth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.urlAuthorization.basicAuth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
status <object>	# GrafanaLibraryPanelStatus defines the observed state of GrafanaLibraryPanel
status.conditions <[]object>	# Results when synchronizing resource with Grafana instances
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.contentCache <string> format=byte
status.contentTimestamp <string> format=date-time
status.contentUrl <string>
status.hash <string>
status.lastResync <string> format=date-time	# Last time the resource was synchronized with Grafana instances
status.uid <string>
