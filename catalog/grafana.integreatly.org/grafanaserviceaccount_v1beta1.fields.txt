# schema source: Grafana Operator v5.24.0 https://github.com/grafana/grafana-operator
apiVersion <string> enum=grafana.integreatly.org/v1beta1
kind <string> enum=GrafanaServiceAccount
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# GrafanaServiceAccountSpec defines the desired state of a GrafanaServiceAccount.
spec.instanceName <string> (required) min=1 (immutable)	# Name of the Grafana instance to create the service account for
spec.isDisabled <boolean> default=false	# Whether the service account is disabled
spec.name <string> min=1 (immutable)	# Name of the service account in Grafana
spec.resyncPeriod <string> pattern="^([0-9]+(\\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$"	# How often the resource is synced, defaults to 10m0s if not set
spec.role <string> (required) enum=Viewer|Editor|Admin	# Role of the service account (Viewer, Editor, Admin)
spec.suspend <boolean> default=false	# Suspend pauses reconciliation of the service account
spec.tokens <[]object>	# Tokens to create for the service account
spec.tokens[].expires <string> format=date-time	# Expiration date of the token. If not set, the token never expires
spec.tokens[].name <string> (required) min=1	# Name of the token
spec.tokens[].secretName <string> min=1	# Name of the secret to store the token. If not set, a name will be generated
status <object>	# GrafanaServiceAccountStatus defines the observed state of a GrafanaServiceAccount
status.account <object>	# Info contains the Grafana service account information
status.account.id <integer> (required) format=int64	# ID of the service account in Grafana
status.account.isDisabled <boolean> (required)	# IsDisabled indicates if the service account is disabled
status.account.login <string> (required)
status.account.name <string> (required)
status.account.role <string> (required)	# Role is the Grafana role for the service account (Viewer, Editor, Admin)
status.account.tokens <[]object>	# Information about tokens
status.account.tokens[].expires <string> format=date-time	# Expiration time of the token N.B. There's possible discrepancy with the expiration time in spec It happens because Grafana API accepts TTL in seconds then calculates the expiration time against the current time
status.account.tokens[].id <integer> (required) format=int64	# ID of the token in Grafana
status.account.tokens[].name <string> (required)
status.account.tokens[].secret <object>	# Name of the secret containing the token
status.account.tokens[].secret.name <string>
status.account.tokens[].secret.namespace <string>
status.conditions <[]object>	# Results when synchronizing resource with Grafana instances
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.lastResync <string> format=date-time	# Last time the resource was synchronized with Grafana instances
