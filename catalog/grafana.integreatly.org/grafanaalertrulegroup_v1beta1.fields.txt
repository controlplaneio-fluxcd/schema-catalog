# schema source: Grafana Operator v5.24.0 https://github.com/grafana/grafana-operator
apiVersion <string> enum=grafana.integreatly.org/v1beta1
kind <string> enum=GrafanaAlertRuleGroup
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# GrafanaAlertRuleGroupSpec defines the desired state of GrafanaAlertRuleGroup
spec.allowCrossNamespaceImport <boolean> default=false	# Allow the Operator to match this resource with Grafanas outside the current namespace
spec.editable <boolean> (immutable)	# Whether to enable or disable editing of the alert rule group in Grafana UI
spec.folderRef <string> (immutable)	# Match GrafanaFolders CRs to infer the uid
spec.folderUID <string> (immutable)	# UID of the folder containing this rule group Overrides the FolderSelector
spec.instanceSelector <object> (required) (immutable)	# Selects Grafana instances for import
spec.instanceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.instanceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.instanceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.instanceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.instanceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.interval <string> (required) format=duration pattern="^([0-9]+(\\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$"
spec.name <string>	# Name of the alert rule group. If not specified, the resource name will be used.
spec.resyncPeriod <string> pattern="^([0-9]+(\\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$"	# How often the resource is synced, defaults to 10m0s if not set
spec.rules <[]object> (required) min=1
spec.rules[].annotations <map[string]string>
spec.rules[].condition <string> (required)
spec.rules[].dashboardUid <string>	# Deprecated: The field is not used, use rules[].annotations.__dashboardUid__
spec.rules[].data <[]object> (required)
spec.rules[].data[].datasourceUid <string>	# Grafana data source unique identifier; it should be '__expr__' for a Server Side Expression operation.
spec.rules[].data[].model <any>	# JSON is the raw JSON query and includes the above properties as well as custom properties.
spec.rules[].data[].queryType <string>	# QueryType is an optional identifier for the type of query. It can be used to distinguish different types of queries.
spec.rules[].data[].refId <string>	# RefID is the unique identifier of the query, set by the frontend call.
spec.rules[].data[].relativeTimeRange <object>	# relative time range
spec.rules[].data[].relativeTimeRange.from <integer> format=int64	# from
spec.rules[].data[].relativeTimeRange.to <integer> format=int64	# to
spec.rules[].execErrState <string> (required) enum=OK|Alerting|Error|KeepLast
spec.rules[].for <string> (required) default="0s" pattern="^([0-9]+(\\.[0-9]+)?(s|m|h|d|w))+$"
spec.rules[].isPaused <boolean>
spec.rules[].keepFiringFor <string> format=duration pattern="^([0-9]+(\\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$"
spec.rules[].labels <map[string]string>
spec.rules[].missingSeriesEvalsToResolve <integer> format=int64	# The number of missing series evaluations that must occur before the rule is considered to be resolved.
spec.rules[].noDataState <string> (required) enum=Alerting|NoData|OK|KeepLast
spec.rules[].notificationSettings <object>
spec.rules[].notificationSettings.active_time_intervals <[]string>	# ActiveTimeIntervals defines the time intervals during which notifications should NOT be muted.
spec.rules[].notificationSettings.group_by <[]string>	# GroupBy defines the labels by which incoming alerts are grouped together.
spec.rules[].notificationSettings.group_interval <string>	# GroupInterval defines how long to wait before sending a notification about new alerts added to a group for which an initial notification has already been sent. (e.g. 5m)
spec.rules[].notificationSettings.group_wait <string>	# GroupWait defines how long to initially wait to send a notification for a group of alerts. (e.g. 30s)
spec.rules[].notificationSettings.mute_time_intervals <[]string>	# MuteTimeIntervals defines the time intervals during which notifications should be muted. These must match the name of a mute time interval defined in the Alertmanager configuration.
spec.rules[].notificationSettings.receiver <string> (required) min=1	# Receiver is the name of the receiver to send notifications to.
spec.rules[].notificationSettings.repeat_interval <string>	# RepeatInterval defines how long to wait before sending a notification again if it has already been sent successfully for an alert. (e.g. 4h) Should not be less than GroupInterval.
spec.rules[].panelId <integer>	# Deprecated: The field is not used, use rules[].annotations.__panelId__
spec.rules[].record <object>
spec.rules[].record.from <string> (required)
spec.rules[].record.metric <string> (required)
spec.rules[].record.targetDatasourceUid <string>
spec.rules[].title <string> (required) min=1 max=190
spec.rules[].uid <string> (required) pattern="^[a-zA-Z0-9-_]+$" max=40	# UID of the alert rule. Can be any string consisting of alphanumeric characters, - and _ with a maximum length of 40
spec.suspend <boolean>	# Suspend pauses synchronizing attempts and tells the operator to ignore changes
status <object>	# The most recent observed state of a Grafana resource
status.conditions <[]object>	# Results when synchronizing resource with Grafana instances
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.lastResync <string> format=date-time	# Last time the resource was synchronized with Grafana instances
