# schema source: Antrea v2.6.2 https://github.com/antrea-io/antrea
apiVersion <string> enum=crd.antrea.io/v1alpha1
kind <string> enum=NodeLatencyMonitor (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)
spec.pingIntervalSeconds <integer> (required) default=60 format=int32 min=1	# Ping interval in seconds, must be at least 1.
