{
  "properties": {
    "metadata": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "pattern": "^default$",
          "type": "string"
        }
      },
      "type": "object"
    },
    "spec": {
      "additionalProperties": false,
      "properties": {
        "pingIntervalSeconds": {
          "default": 60,
          "description": "Ping interval in seconds, must be at least 1.",
          "format": "int32",
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "pingIntervalSeconds"
      ],
      "type": "object"
    }
  },
  "required": [
    "spec"
  ],
  "type": "object",
  "x-flux-schema-type": "NodeLatencyMonitor"
}
