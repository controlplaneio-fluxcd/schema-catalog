# schema source: Antrea v2.7.0 https://github.com/antrea-io/antrea
apiVersion <string> enum=crd.antrea.io/v1alpha1
kind <string> enum=AntreaNodeConfig (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)
spec.nodeSelector <object> (required)	# Selects Nodes to which this AntreaNodeConfig applies.
spec.nodeSelector.matchExpressions <[]object>
spec.nodeSelector.matchExpressions[].key <string>
spec.nodeSelector.matchExpressions[].operator <string> enum=In|NotIn|Exists|DoesNotExist
spec.nodeSelector.matchExpressions[].values <[]string>
spec.nodeSelector.matchLabels <any>
spec.secondaryNetwork <object>	# Secondary network bridge configuration for the selected Nodes.
spec.secondaryNetwork.ovsBridges <[]object> max=1	# OVS bridge configurations for secondary networks. At the moment, only a single OVS bridge is supported.
spec.secondaryNetwork.ovsBridges[].bridgeName <string> (required) pattern="^[a-zA-Z0-9._-]+$" min=1 max=15	# Name of the OVS bridge.
spec.secondaryNetwork.ovsBridges[].enableMulticastSnooping <boolean> default=false	# Enable multicast snooping on the bridge so that multicast traffic is forwarded only to ports with interested receivers. Defaults to false.
spec.secondaryNetwork.ovsBridges[].physicalInterfaces <[]object> max=8	# Physical interfaces to be connected to this bridge.
spec.secondaryNetwork.ovsBridges[].physicalInterfaces[].allowedVLANs <[]string>	# VLAN IDs or VLAN ID ranges (e.g. "100", "200-300") allowed on this interface. If not specified, all VLANs are allowed.
spec.secondaryNetwork.ovsBridges[].physicalInterfaces[].name <string> (required) pattern="^[a-zA-Z0-9._-]+$" min=1 max=15	# Name of the physical interface.
