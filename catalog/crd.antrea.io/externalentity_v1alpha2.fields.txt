# schema source: Antrea v2.6.2 https://github.com/antrea-io/antrea
apiVersion <string> enum=crd.antrea.io/v1alpha2
kind <string> enum=ExternalEntity
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.endpoints <[]object>
spec.endpoints[].ip <string>
spec.endpoints[].name <string>
spec.externalNode <string>
spec.ports <[]object>
spec.ports[].name <string>
spec.ports[].port <int-or-string>
spec.ports[].protocol <string> enum=TCP|UDP|SCTP
