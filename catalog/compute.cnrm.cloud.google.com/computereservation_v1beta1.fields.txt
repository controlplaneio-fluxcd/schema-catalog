# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=compute.cnrm.cloud.google.com/v1beta1
kind <string> enum=ComputeReservation
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# ComputeReservationSpec defines the desired state of ComputeReservation
spec.description <string>	# Immutable. An optional description of this resource.
spec.resourceID <string>	# Immutable. Optional. The name of the resource. Used for creation and acquisition. When unset, the value of `metadata.name` is used as the default.
spec.shareSettings <object>	# The share setting for reservations and sole tenancy node groups.
spec.shareSettings.projectMap <[]object>	# A map of key(i.e. project or other shared resources) and associated project config. This is only valid when shareType's value is SPECIFIC_PROJECTS.
spec.shareSettings.projectMap[].keyRef <object> (required)	# The resource reference that defaults to Project if Kind is not specified.
spec.shareSettings.projectMap[].keyRef.apiVersion <string>	# APIVersion of the referenced resource
spec.shareSettings.projectMap[].keyRef.external <string>	# The external name of the referenced resource
spec.shareSettings.projectMap[].keyRef.kind <string>	# Kind of the referenced resource
spec.shareSettings.projectMap[].keyRef.name <string>
spec.shareSettings.projectMap[].keyRef.namespace <string>
spec.shareSettings.projectMap[].value <object>
spec.shareSettings.projectMap[].value.projectIDRef <object>	# The project ID, should be same as the key of this project config in the parent map.
spec.shareSettings.projectMap[].value.projectIDRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.shareSettings.projectMap[].value.projectIDRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.shareSettings.projectMap[].value.projectIDRef.name <string>	# The `name` field of a `Project` resource.
spec.shareSettings.projectMap[].value.projectIDRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.shareSettings.shareType <string>	# Immutable. Type of sharing for this shared-reservation Check the ShareType enum for the list of possible values.
spec.specificReservation <object> (required)	# Reservation for instances with specific machine shapes.
spec.specificReservation.count <integer> (required) format=int32	# The number of resources that are allocated.
spec.specificReservation.inUseCount <integer> format=int32	# How many instances are in use.
spec.specificReservation.instanceProperties <object> (required)	# Immutable. The instance properties for the reservation.
spec.specificReservation.instanceProperties.guestAccelerators <[]object>	# Immutable. Guest accelerator type and count.
spec.specificReservation.instanceProperties.guestAccelerators[].acceleratorCount <integer> (required) format=int32	# Immutable. The number of the guest accelerator cards exposed to this instance.
spec.specificReservation.instanceProperties.guestAccelerators[].acceleratorType <string> (required)	# Immutable. The full or partial URL of the accelerator type to attach to this instance. For example: 'projects/my-project/zones/us-central1-c/acceleratorTypes/nvidia-tesla-p100' If you are creating an instance template, specify only the accelerator name.
spec.specificReservation.instanceProperties.localSsds <[]object>	# Immutable. The amount of local ssd to reserve with each instance. This reserves disks of type 'local-ssd'.
spec.specificReservation.instanceProperties.localSsds[].diskSizeGb <integer> (required) format=int32	# Immutable. The size of the disk in base-2 GB.
spec.specificReservation.instanceProperties.localSsds[].interface <string>	# Immutable. The disk interface to use for attaching this disk. Default value: "SCSI" Possible values: ["SCSI", "NVME"].
spec.specificReservation.instanceProperties.machineType <string> (required)	# Immutable. The name of the machine type to reserve.
spec.specificReservation.instanceProperties.minCpuPlatform <string>	# Immutable. The minimum CPU platform for the reservation. For example, '"Intel Skylake"'. See the CPU platform availability reference](https://cloud.google.com/compute/docs/instances/specify-min-cpu-platform#availablezones) for information on available CPU platforms.
spec.specificReservationRequired <boolean>	# Immutable. When set to true, only VMs that target this reservation by name can consume this reservation. Otherwise, it can be consumed by VMs with affinity for any reservation. Defaults to false.
spec.zone <string> (required)	# Immutable. The zone where the reservation is made.
status <object>	# ComputeReservationStatus defines the config connector machine state of ComputeReservation
status.commitment <string>	# Full or partial URL to a parent commitment. This field displays for reservations that are tied to a commitment.
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.creationTimestamp <string>	# Creation timestamp in RFC3339 text format.
status.externalRef <string>	# A unique specifier for the ComputeReservation resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.selfLink <string>	# Server-defined URL for the resource.
status.status <string>	# The status of the reservation.
