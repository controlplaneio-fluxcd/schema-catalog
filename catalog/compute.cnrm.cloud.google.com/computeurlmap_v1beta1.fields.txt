# schema source: GCP Config Connector v1.154.1 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=compute.cnrm.cloud.google.com/v1beta1
kind <string> enum=ComputeURLMap
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)
spec.defaultCustomErrorResponsePolicy <object>
spec.defaultCustomErrorResponsePolicy.errorResponseRule <[]object>
spec.defaultCustomErrorResponsePolicy.errorResponseRule[].matchResponseCodes <[]string>
spec.defaultCustomErrorResponsePolicy.errorResponseRule[].overrideResponseCode <integer> format=int64
spec.defaultCustomErrorResponsePolicy.errorResponseRule[].path <string>
spec.defaultCustomErrorResponsePolicy.errorServiceRef <object>	# The full or partial URL to the BackendBucket resource that contains the custom error content.
spec.defaultCustomErrorResponsePolicy.errorServiceRef.external <string>
spec.defaultCustomErrorResponsePolicy.errorServiceRef.name <string>
spec.defaultCustomErrorResponsePolicy.errorServiceRef.namespace <string>
spec.defaultRouteAction <object>	# defaultRouteAction takes effect when none of the hostRules match. The load balancer performs advanced routing actions, such as URL rewrites and header transformations, before forwarding the request to the selected backend. If defaultRouteAction specifies any weightedBackendServices, defaultService must not be set. Conversely if defaultService is set, defaultRouteAction cannot contain any weightedBackendServices. Only one of defaultRouteAction or defaultUrlRedirect must be set. URL maps for Classic external HTTP(S) load balancers only support the urlRewrite action within defaultRouteAction. defaultRouteAction has no effect when the URL map is bound to a target gRPC proxy that has the validateForProxyless field set to true.
spec.defaultRouteAction.corsPolicy <object>	# The specification for allowing client side cross-origin requests. Please see [W3C Recommendation for Cross Origin Resource Sharing](https://www.w3.org/TR/cors/).
spec.defaultRouteAction.corsPolicy.allowCredentials <boolean>	# In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This translates to the Access- Control-Allow-Credentials header. Defaults to false.
spec.defaultRouteAction.corsPolicy.allowHeaders <[]string>	# Specifies the content for the Access-Control-Allow-Headers header.
spec.defaultRouteAction.corsPolicy.allowMethods <[]string>	# Specifies the content for the Access-Control-Allow-Methods header.
spec.defaultRouteAction.corsPolicy.allowOriginRegexes <[]string>	# Specifies the regular expression patterns that match allowed origins. For regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript An origin is allowed if it matches either allow_origins or allow_origin_regex.
spec.defaultRouteAction.corsPolicy.allowOrigins <[]string>	# Specifies the list of origins that will be allowed to do CORS requests. An origin is allowed if it matches either allow_origins or allow_origin_regex.
spec.defaultRouteAction.corsPolicy.disabled <boolean>	# If true, specifies the CORS policy is disabled. which indicates that the CORS policy is in effect. Defaults to false.
spec.defaultRouteAction.corsPolicy.exposeHeaders <[]string>	# Specifies the content for the Access-Control-Expose-Headers header.
spec.defaultRouteAction.corsPolicy.maxAge <integer> format=int64	# Specifies how long the results of a preflight request can be cached. This translates to the content for the Access-Control-Max-Age header.
spec.defaultRouteAction.faultInjectionPolicy <object>	# The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by Loadbalancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the Loadbalancer for a percentage of requests. timeout and retryPolicy will be ignored by clients that are configured with a faultInjectionPolicy.
spec.defaultRouteAction.faultInjectionPolicy.abort <object>	# The specification for how client requests are aborted as part of fault injection.
spec.defaultRouteAction.faultInjectionPolicy.abort.httpStatus <integer> format=int64	# The HTTP status code used to abort the request. The value must be between 200 and 599 inclusive.
spec.defaultRouteAction.faultInjectionPolicy.abort.percentage <number>	# The percentage of traffic (connections/operations/requests) which will be aborted as part of fault injection. The value must be between 0.0 and 100.0 inclusive.
spec.defaultRouteAction.faultInjectionPolicy.delay <object>	# The specification for how client requests are delayed as part of fault injection, before being sent to a backend service.
spec.defaultRouteAction.faultInjectionPolicy.delay.fixedDelay <object>	# Specifies the value of the fixed delay interval.
spec.defaultRouteAction.faultInjectionPolicy.delay.fixedDelay.nanos <integer> format=int64	# Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 'seconds' field and a positive 'nanos' field. Must be from 0 to 999,999,999 inclusive.
spec.defaultRouteAction.faultInjectionPolicy.delay.fixedDelay.seconds <string>	# Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
spec.defaultRouteAction.faultInjectionPolicy.delay.percentage <number>	# The percentage of traffic (connections/operations/requests) on which delay will be introduced as part of fault injection. The value must be between 0.0 and 100.0 inclusive.
spec.defaultRouteAction.requestMirrorPolicy <object>	# Specifies the policy on how requests intended for the route's backends are shadowed to a separate mirrored backend service. Loadbalancer does not wait for responses from the shadow service. Prior to sending traffic to the shadow service, the host / authority header is suffixed with -shadow.
spec.defaultRouteAction.requestMirrorPolicy.backendServiceRef <object>	# Required. The backend service resource being mirrored to.
spec.defaultRouteAction.requestMirrorPolicy.backendServiceRef.external <string>
spec.defaultRouteAction.requestMirrorPolicy.backendServiceRef.name <string>
spec.defaultRouteAction.requestMirrorPolicy.backendServiceRef.namespace <string>
spec.defaultRouteAction.retryPolicy <object>	# Specifies the retry policy associated with this route.
spec.defaultRouteAction.retryPolicy.numRetries <integer> format=int64	# Specifies the allowed number retries. This number must be > 0.
spec.defaultRouteAction.retryPolicy.perTryTimeout <object>	# Specifies a non-zero timeout per retry attempt.
spec.defaultRouteAction.retryPolicy.perTryTimeout.nanos <integer> format=int64	# Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 'seconds' field and a positive 'nanos' field. Must be from 0 to 999,999,999 inclusive.
spec.defaultRouteAction.retryPolicy.perTryTimeout.seconds <string>	# Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
spec.defaultRouteAction.retryPolicy.retryConditions <[]string>	# Specifies one or more conditions when this retry rule applies. Valid values are: * 5xx: Loadbalancer will attempt a retry if the backend service responds with any 5xx response code, or if the backend service does not respond at all, example: disconnects, reset, read timeout, connection failure, and refused streams. * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or 504. * connect-failure: Loadbalancer will retry on failures connecting to backend services, for example due to connection timeouts. * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes. Currently the only retriable error supported is 409. * refused-stream: Loadbalancer will retry if the backend service resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry. * cancelled: Loadbalancer will retry if the gRPC status code in the response header is set to cancelled * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the response header is set to deadline-exceeded * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response header is set to resource-exhausted * unavailable: Loadbalancer will retry if the gRPC status code in the response header is set to unavailable.
spec.defaultRouteAction.timeout <object>	# Specifies the timeout for the selected route. Timeout is computed from the time the request has been fully processed (i.e. end-of-stream) up until the response has been completely processed. Timeout includes all retries. If not specified, will use the largest timeout among all backend services associated with the route.
spec.defaultRouteAction.timeout.nanos <integer> format=int64	# Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 'seconds' field and a positive 'nanos' field. Must be from 0 to 999,999,999 inclusive.
spec.defaultRouteAction.timeout.seconds <string>	# Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
spec.defaultRouteAction.urlRewrite <object>	# The spec to modify the URL of the request, prior to forwarding the request to the matched service.
spec.defaultRouteAction.urlRewrite.hostRewrite <string>	# Prior to forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be between 1 and 255 characters.
spec.defaultRouteAction.urlRewrite.pathPrefixRewrite <string>	# Prior to forwarding the request to the selected backend service, the matching portion of the request's path is replaced by pathPrefixRewrite. The value must be between 1 and 1024 characters.
spec.defaultRouteAction.weightedBackendServices <[]object>	# A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be one weightedBackendService with weight set to a non 0 number. Once a backendService is identified and before forwarding the request to the backend service, advanced routing actions like Url rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction.
spec.defaultRouteAction.weightedBackendServices[].backendServiceRef <object>	# Required. The default backend service resource. Before forwarding the request to backendService, the loadbalancer applies any relevant headerActions specified as part of this backendServiceWeight.
spec.defaultRouteAction.weightedBackendServices[].backendServiceRef.external <string>
spec.defaultRouteAction.weightedBackendServices[].backendServiceRef.name <string>
spec.defaultRouteAction.weightedBackendServices[].backendServiceRef.namespace <string>
spec.defaultRouteAction.weightedBackendServices[].headerAction <object>	# Specifies changes to request and response headers that need to take effect for the selected backendService. headerAction specified here take effect before headerAction in the enclosing HttpRouteRule, PathMatcher and UrlMap.
spec.defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd <[]object>
spec.defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerName <string>	# The name of the header.
spec.defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerValue <string>	# The value of the header to add.
spec.defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].replace <boolean>	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToRemove <[]string>
spec.defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd <[]object>
spec.defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerName <string>	# The name of the header.
spec.defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerValue <string>	# The value of the header to add.
spec.defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].replace <boolean>	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToRemove <[]string>
spec.defaultRouteAction.weightedBackendServices[].weight <integer> format=int64	# Specifies the fraction of traffic sent to backendService, computed as weight / (sum of all weightedBackendService weights in routeAction) . The selection of a backend service is determined only for new traffic. Once a user's request has been directed to a backendService, subsequent requests will be sent to the same backendService as determined by the BackendService's session affinity policy. The value must be between 0 and 1000.
spec.defaultService <object>	# The defaultService resource to which traffic is directed if none of the hostRules match. For the Global URL Map, it should be a reference to the backend service or backend bucket. For the Regional URL Map, it should be a reference to the backend service. If defaultRouteAction is additionally specified, advanced routing actions like URL Rewrites, etc. take effect prior to sending the request to the backend. However, if defaultService is specified, defaultRouteAction cannot contain any weightedBackendServices. Conversely, if routeAction specifies any weightedBackendServices, service must not be specified. Only one of defaultService, defaultUrlRedirect or defaultRouteAction.weightedBackendService must be set.
spec.defaultService.backendBucketRef <object>
spec.defaultService.backendBucketRef.external <string>
spec.defaultService.backendBucketRef.name <string>
spec.defaultService.backendBucketRef.namespace <string>
spec.defaultService.backendServiceRef <object>
spec.defaultService.backendServiceRef.external <string>
spec.defaultService.backendServiceRef.name <string>
spec.defaultService.backendServiceRef.namespace <string>
spec.defaultUrlRedirect <object>	# When none of the specified hostRules match, the request is redirected to a URL specified by defaultUrlRedirect. If defaultUrlRedirect is specified, defaultService or defaultRouteAction must not be set.
spec.defaultUrlRedirect.hostRedirect <string>	# The host that will be used in the redirect response instead of the one that was supplied in the request. The value must be between 1 and 255 characters.
spec.defaultUrlRedirect.httpsRedirect <boolean>	# If set to true, the URL scheme in the redirected request is set to https. If set to false, the URL scheme of the redirected request will remain the same as that of the request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this true for TargetHttpsProxy is not permitted. The default is set to false.
spec.defaultUrlRedirect.pathRedirect <string>	# The path that will be used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together with prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.defaultUrlRedirect.prefixRedirect <string>	# The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.defaultUrlRedirect.redirectResponseCode <string>	# The HTTP Status code to use for this RedirectAction. Supported values are: * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. * FOUND, which corresponds to 302. * SEE_OTHER which corresponds to 303. * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method will be retained. * PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"].
spec.defaultUrlRedirect.stripQuery <boolean> (required)	# If set to true, any accompanying query portion of the original URL is removed prior to redirecting the request. If set to false, the query portion of the original URL is retained. This field is required to ensure an empty block is not set. The normal default value is false.
spec.description <string>	# An optional description of this resource. Provide this property when you create the resource.
spec.headerAction <object>	# Specifies changes to request and response headers that need to take effect for the selected backendService. The headerAction specified here take effect after headerAction specified under pathMatcher.
spec.headerAction.requestHeadersToAdd <[]object>	# Headers to add to a matching request prior to forwarding the request to the backendService.
spec.headerAction.requestHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.headerAction.requestHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.headerAction.requestHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.headerAction.requestHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the request prior to forwarding the request to the backendService.
spec.headerAction.responseHeadersToAdd <[]object>	# Headers to add the response prior to sending the response back to the client.
spec.headerAction.responseHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.headerAction.responseHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.headerAction.responseHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.headerAction.responseHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the response prior to sending the response back to the client.
spec.hostRule <[]object>	# The list of HostRules to use against the URL.
spec.hostRule[].description <string>	# An optional description of this HostRule. Provide this property when you create the resource.
spec.hostRule[].hosts <[]string> (required)	# The list of host patterns to match. They must be valid hostnames, except * will match any string of ([a-z0-9-.]*). In that case, * must be the first character and must be followed in the pattern by either - or ..
spec.hostRule[].pathMatcher <string> (required)	# The name of the PathMatcher to use to match the path portion of the URL if the hostRule matches the URL's host portion.
spec.location <string> (required)	# Location represents the geographical location of the ComputeURLMap. Specify a region name or "global" for global resources. Reference: GCP definition of regions/zones (https://cloud.google.com/compute/docs/regions-zones/)
spec.pathMatcher <[]object>	# The list of named PathMatchers to use against the URL.
spec.pathMatcher[].defaultCustomErrorResponsePolicy <object>
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorResponseRule <[]object>
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorResponseRule[].matchResponseCodes <[]string>
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorResponseRule[].overrideResponseCode <integer> format=int64
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorResponseRule[].path <string>
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorServiceRef <object>	# The full or partial URL to the BackendBucket resource that contains the custom error content.
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorServiceRef.external <string>
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorServiceRef.name <string>
spec.pathMatcher[].defaultCustomErrorResponsePolicy.errorServiceRef.namespace <string>
spec.pathMatcher[].defaultRouteAction <object>	# defaultRouteAction takes effect when none of the pathRules or routeRules match. The load balancer performs advanced routing actions like URL rewrites, header transformations, etc. prior to forwarding the request to the selected backend. If defaultRouteAction specifies any weightedBackendServices, defaultService must not be set. Conversely if defaultService is set, defaultRouteAction cannot contain any weightedBackendServices. Only one of defaultRouteAction or defaultUrlRedirect must be set.
spec.pathMatcher[].defaultRouteAction.corsPolicy <object>	# The specification for allowing client side cross-origin requests. Please see W3C Recommendation for Cross Origin Resource Sharing.
spec.pathMatcher[].defaultRouteAction.corsPolicy.allowCredentials <boolean>	# In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This translates to the Access- Control-Allow-Credentials header. Defaults to false.
spec.pathMatcher[].defaultRouteAction.corsPolicy.allowHeaders <[]string>	# Specifies the content for the Access-Control-Allow-Headers header.
spec.pathMatcher[].defaultRouteAction.corsPolicy.allowMethods <[]string>	# Specifies the content for the Access-Control-Allow-Methods header.
spec.pathMatcher[].defaultRouteAction.corsPolicy.allowOriginRegexes <[]string>	# Specifies the regular expression patterns that match allowed origins. For regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript An origin is allowed if it matches either allow_origins or allow_origin_regex.
spec.pathMatcher[].defaultRouteAction.corsPolicy.allowOrigins <[]string>	# Specifies the list of origins that will be allowed to do CORS requests. An origin is allowed if it matches either allow_origins or allow_origin_regex.
spec.pathMatcher[].defaultRouteAction.corsPolicy.disabled <boolean>	# If true, specifies the CORS policy is disabled. which indicates that the CORS policy is in effect. Defaults to false.
spec.pathMatcher[].defaultRouteAction.corsPolicy.exposeHeaders <[]string>	# Specifies the content for the Access-Control-Expose-Headers header.
spec.pathMatcher[].defaultRouteAction.corsPolicy.maxAge <integer> format=int64	# Specifies how long the results of a preflight request can be cached. This translates to the content for the Access-Control-Max-Age header.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy <object>	# The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by Loadbalancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the Loadbalancer for a percentage of requests. timeout and retry_policy will be ignored by clients that are configured with a fault_injection_policy.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.abort <object>	# The specification for how client requests are aborted as part of fault injection.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.abort.httpStatus <integer> format=int64	# The HTTP status code used to abort the request. The value must be between 200 and 599 inclusive.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.abort.percentage <number>	# The percentage of traffic (connections/operations/requests) which will be aborted as part of fault injection. The value must be between 0.0 and 100.0 inclusive.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.delay <object>	# The specification for how client requests are delayed as part of fault injection, before being sent to a backend service.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.delay.fixedDelay <object>	# Specifies the value of the fixed delay interval.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.delay.fixedDelay.nanos <integer> format=int64	# Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 'seconds' field and a positive 'nanos' field. Must be from 0 to 999,999,999 inclusive.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.delay.fixedDelay.seconds <string>	# Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
spec.pathMatcher[].defaultRouteAction.faultInjectionPolicy.delay.percentage <number>	# The percentage of traffic (connections/operations/requests) on which delay will be introduced as part of fault injection. The value must be between 0.0 and 100.0 inclusive.
spec.pathMatcher[].defaultRouteAction.requestMirrorPolicy <object>	# Specifies the policy on how requests intended for the route's backends are shadowed to a separate mirrored backend service. Loadbalancer does not wait for responses from the shadow service. Prior to sending traffic to the shadow service, the host / authority header is suffixed with -shadow.
spec.pathMatcher[].defaultRouteAction.requestMirrorPolicy.backendServiceRef <object> (required)
spec.pathMatcher[].defaultRouteAction.requestMirrorPolicy.backendServiceRef.external <string>
spec.pathMatcher[].defaultRouteAction.requestMirrorPolicy.backendServiceRef.name <string>
spec.pathMatcher[].defaultRouteAction.requestMirrorPolicy.backendServiceRef.namespace <string>
spec.pathMatcher[].defaultRouteAction.retryPolicy <object>	# Specifies the retry policy associated with this route.
spec.pathMatcher[].defaultRouteAction.retryPolicy.numRetries <integer> format=int64	# Specifies the allowed number retries. This number must be > 0.
spec.pathMatcher[].defaultRouteAction.retryPolicy.perTryTimeout <object>	# Specifies a non-zero timeout per retry attempt.
spec.pathMatcher[].defaultRouteAction.retryPolicy.perTryTimeout.nanos <integer> format=int64	# Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 'seconds' field and a positive 'nanos' field. Must be from 0 to 999,999,999 inclusive.
spec.pathMatcher[].defaultRouteAction.retryPolicy.perTryTimeout.seconds <string>	# Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
spec.pathMatcher[].defaultRouteAction.retryPolicy.retryConditions <[]string>	# Specifies one or more conditions when this retry rule applies. Valid values are: * 5xx: Loadbalancer will attempt a retry if the backend service responds with any 5xx response code, or if the backend service does not respond at all, example: disconnects, reset, read timeout, connection failure, and refused streams. * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or 504. * connect-failure: Loadbalancer will retry on failures connecting to backend services, for example due to connection timeouts. * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes. Currently the only retriable error supported is 409. * refused-stream: Loadbalancer will retry if the backend service resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry. * cancelled: Loadbalancer will retry if the gRPC status code in the response header is set to cancelled * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the response header is set to deadline-exceeded * resource-exhausted: Loadbalancer will retry if the gRPC status code in the response header is set to resource-exhausted * unavailable: Loadbalancer will retry if the gRPC status code in the response header is set to unavailable.
spec.pathMatcher[].defaultRouteAction.timeout <object>	# Specifies the timeout for the selected route. Timeout is computed from the time the request is has been fully processed (i.e. end-of-stream) up until the response has been completely processed. Timeout includes all retries. If not specified, the default value is 15 seconds.
spec.pathMatcher[].defaultRouteAction.timeout.nanos <integer> format=int64	# Span of time that's a fraction of a second at nanosecond resolution. Durations less than one second are represented with a 0 'seconds' field and a positive 'nanos' field. Must be from 0 to 999,999,999 inclusive.
spec.pathMatcher[].defaultRouteAction.timeout.seconds <string>	# Span of time at a resolution of a second. Must be from 0 to 315,576,000,000 inclusive.
spec.pathMatcher[].defaultRouteAction.urlRewrite <object>	# The spec to modify the URL of the request, prior to forwarding the request to the matched service.
spec.pathMatcher[].defaultRouteAction.urlRewrite.hostRewrite <string>	# Prior to forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be between 1 and 255 characters.
spec.pathMatcher[].defaultRouteAction.urlRewrite.pathPrefixRewrite <string>	# Prior to forwarding the request to the selected backend service, the matching portion of the request's path is replaced by pathPrefixRewrite. The value must be between 1 and 1024 characters.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices <[]object>	# A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be one weightedBackendService with weight set to a non 0 number. Once a backendService is identified and before forwarding the request to the backend service, advanced routing actions like Url rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].backendServiceRef <object>	# Required. The default backend service resource. Before forwarding the request to backendService, the loadbalancer applies any relevant headerActions specified as part of this backendServiceWeight.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].backendServiceRef.external <string>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].backendServiceRef.name <string>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].backendServiceRef.namespace <string>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction <object>	# Specifies changes to request and response headers that need to take effect for the selected backendService. headerAction specified here take effect before headerAction in the enclosing HttpRouteRule, PathMatcher and UrlMap.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd <[]object>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerName <string>	# The name of the header.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerValue <string>	# The value of the header to add.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].replace <boolean>	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.requestHeadersToRemove <[]string>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd <[]object>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerName <string>	# The name of the header.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerValue <string>	# The value of the header to add.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].replace <boolean>	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].headerAction.responseHeadersToRemove <[]string>
spec.pathMatcher[].defaultRouteAction.weightedBackendServices[].weight <integer> format=int64	# Specifies the fraction of traffic sent to backendService, computed as weight / (sum of all weightedBackendService weights in routeAction) . The selection of a backend service is determined only for new traffic. Once a user's request has been directed to a backendService, subsequent requests will be sent to the same backendService as determined by the BackendService's session affinity policy. The value must be between 0 and 1000.
spec.pathMatcher[].defaultService <object>	# The default service to use if none of the pathRules defined by this PathMatcher is matched by the URL's path portion. For the Global URL Map, it should be a reference to the backend service or backend bucket. For the Regional URL Map, it should be a reference to the backend service.
spec.pathMatcher[].defaultService.backendBucketRef <object>
spec.pathMatcher[].defaultService.backendBucketRef.external <string>
spec.pathMatcher[].defaultService.backendBucketRef.name <string>
spec.pathMatcher[].defaultService.backendBucketRef.namespace <string>
spec.pathMatcher[].defaultService.backendServiceRef <object>
spec.pathMatcher[].defaultService.backendServiceRef.external <string>
spec.pathMatcher[].defaultService.backendServiceRef.name <string>
spec.pathMatcher[].defaultService.backendServiceRef.namespace <string>
spec.pathMatcher[].defaultUrlRedirect <object>	# When none of the specified hostRules match, the request is redirected to a URL specified by defaultUrlRedirect. If defaultUrlRedirect is specified, defaultService or defaultRouteAction must not be set.
spec.pathMatcher[].defaultUrlRedirect.hostRedirect <string>	# The host that will be used in the redirect response instead of the one that was supplied in the request. The value must be between 1 and 255 characters.
spec.pathMatcher[].defaultUrlRedirect.httpsRedirect <boolean>	# If set to true, the URL scheme in the redirected request is set to https. If set to false, the URL scheme of the redirected request will remain the same as that of the request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this true for TargetHttpsProxy is not permitted. The default is set to false.
spec.pathMatcher[].defaultUrlRedirect.pathRedirect <string>	# The path that will be used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together with prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.pathMatcher[].defaultUrlRedirect.prefixRedirect <string>	# The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.pathMatcher[].defaultUrlRedirect.redirectResponseCode <string>	# The HTTP Status code to use for this RedirectAction. Supported values are: * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. * FOUND, which corresponds to 302. * SEE_OTHER which corresponds to 303. * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method will be retained. * PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"].
spec.pathMatcher[].defaultUrlRedirect.stripQuery <boolean> (required)	# If set to true, any accompanying query portion of the original URL is removed prior to redirecting the request. If set to false, the query portion of the original URL is retained. This field is required to ensure an empty block is not set. The normal default value is false.
spec.pathMatcher[].description <string>	# An optional description of this resource.
spec.pathMatcher[].headerAction <object>	# Specifies changes to request and response headers that need to take effect for the selected backendService. HeaderAction specified here are applied after the matching HttpRouteRule HeaderAction and before the HeaderAction in the UrlMap.
spec.pathMatcher[].headerAction.requestHeadersToAdd <[]object>	# Headers to add to a matching request prior to forwarding the request to the backendService.
spec.pathMatcher[].headerAction.requestHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].headerAction.requestHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].headerAction.requestHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].headerAction.requestHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the request prior to forwarding the request to the backendService.
spec.pathMatcher[].headerAction.responseHeadersToAdd <[]object>	# Headers to add the response prior to sending the response back to the client.
spec.pathMatcher[].headerAction.responseHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].headerAction.responseHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].headerAction.responseHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].headerAction.responseHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the response prior to sending the response back to the client.
spec.pathMatcher[].name <string> (required)	# The name to which this PathMatcher is referred by the HostRule.
spec.pathMatcher[].pathRule <[]object>	# The list of path rules. Use this list instead of routeRules when routing based on simple path matching is all that's required. The order by which path rules are specified does not matter. Matches are always done on the longest-path-first basis. For example: a pathRule with a path /a/b/c/* will match before /a/b/* irrespective of the order in which those paths appear in this list. Within a given pathMatcher, only one of pathRules or routeRules must be set.
spec.pathMatcher[].pathRule[].paths <[]string> (required)	# The list of path patterns to match. Each must start with / and the only place a \* is allowed is at the end following a /. The string fed to the path matcher does not include any text after the first ? or #, and those chars are not allowed here.
spec.pathMatcher[].pathRule[].routeAction <object>	# In response to a matching path, the load balancer performs advanced routing actions like URL rewrites, header transformations, etc. prior to forwarding the request to the selected backend. If routeAction specifies any weightedBackendServices, service must not be set. Conversely if service is set, routeAction cannot contain any weightedBackendServices. Only one of routeAction or urlRedirect must be set.
spec.pathMatcher[].pathRule[].routeAction.corsPolicy <object>	# The specification for allowing client side cross-origin requests. Please see W3C Recommendation for Cross Origin Resource Sharing.
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.allowCredentials <boolean>
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.allowHeaders <[]string>
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.allowMethods <[]string>
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.allowOriginRegexes <[]string>
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.allowOrigins <[]string>
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.disabled <boolean> (required)
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.exposeHeaders <[]string>
spec.pathMatcher[].pathRule[].routeAction.corsPolicy.maxAge <integer> format=int64
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy <object>	# The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by Loadbalancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the Loadbalancer for a percentage of requests. timeout and retry_policy will be ignored by clients that are configured with a fault_injection_policy.
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.abort <object>
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.abort.httpStatus <integer> (required) format=int64
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.abort.percentage <number> (required)
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.delay <object>
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.delay.fixedDelay <object> (required)
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.delay.fixedDelay.nanos <integer> format=int64
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.delay.fixedDelay.seconds <string> (required)
spec.pathMatcher[].pathRule[].routeAction.faultInjectionPolicy.delay.percentage <number> (required)
spec.pathMatcher[].pathRule[].routeAction.requestMirrorPolicy <object>	# Specifies the policy on how requests intended for the route's backends are shadowed to a separate mirrored backend service. Loadbalancer does not wait for responses from the shadow service. Prior to sending traffic to the shadow service, the host / authority header is suffixed with -shadow.
spec.pathMatcher[].pathRule[].routeAction.requestMirrorPolicy.backendServiceRef <object> (required)
spec.pathMatcher[].pathRule[].routeAction.requestMirrorPolicy.backendServiceRef.external <string>
spec.pathMatcher[].pathRule[].routeAction.requestMirrorPolicy.backendServiceRef.name <string>
spec.pathMatcher[].pathRule[].routeAction.requestMirrorPolicy.backendServiceRef.namespace <string>
spec.pathMatcher[].pathRule[].routeAction.retryPolicy <object>	# Specifies the retry policy associated with this route.
spec.pathMatcher[].pathRule[].routeAction.retryPolicy.numRetries <integer> format=int64
spec.pathMatcher[].pathRule[].routeAction.retryPolicy.perTryTimeout <object>
spec.pathMatcher[].pathRule[].routeAction.retryPolicy.perTryTimeout.nanos <integer> format=int64
spec.pathMatcher[].pathRule[].routeAction.retryPolicy.perTryTimeout.seconds <string> (required)
spec.pathMatcher[].pathRule[].routeAction.retryPolicy.retryConditions <[]string>
spec.pathMatcher[].pathRule[].routeAction.timeout <object>	# Specifies the timeout for the selected route. Timeout is computed from the time the request is has been fully processed (i.e. end-of-stream) up until the response has been completely processed. Timeout includes all retries. If not specified, the default value is 15 seconds.
spec.pathMatcher[].pathRule[].routeAction.timeout.nanos <integer> format=int64
spec.pathMatcher[].pathRule[].routeAction.timeout.seconds <string> (required)
spec.pathMatcher[].pathRule[].routeAction.urlRewrite <object>	# The spec to modify the URL of the request, prior to forwarding the request to the matched service.
spec.pathMatcher[].pathRule[].routeAction.urlRewrite.hostRewrite <string>	# Prior to forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be between 1 and 255 characters.
spec.pathMatcher[].pathRule[].routeAction.urlRewrite.pathPrefixRewrite <string>	# Prior to forwarding the request to the selected backend service, the matching portion of the request's path is replaced by pathPrefixRewrite. The value must be between 1 and 1024 characters.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices <[]object>	# A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be one weightedBackendService with weight set to a non 0 number. Once a backendService is identified and before forwarding the request to the backend service, advanced routing actions like Url rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].backendServiceRef <object> (required)
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].backendServiceRef.external <string>
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].backendServiceRef.name <string>
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].backendServiceRef.namespace <string>
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction <object>
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd <[]object>	# Headers to add to a matching request prior to forwarding the request to the backendService.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.requestHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the request prior to forwarding the request to the backendService.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd <[]object>	# Headers to add the response prior to sending the response back to the client.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].headerAction.responseHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the response prior to sending the response back to the client.
spec.pathMatcher[].pathRule[].routeAction.weightedBackendServices[].weight <integer> (required) format=int64
spec.pathMatcher[].pathRule[].service <object>	# The backend service to which traffic is directed if this rule is matched. For the Global URL Map, it should be a reference to the backend service or backend bucket. For the Regional URL Map, it should be a reference to the backend service. If routeAction is additionally specified, advanced routing actions like URL Rewrites, etc. take effect prior to sending the request to the backend. However, if service is specified, routeAction cannot contain any weightedBackendServices. Conversely, if routeAction specifies any weightedBackendServices, service must not be specified. Only one of urlRedirect, service or routeAction.weightedBackendService must be set.
spec.pathMatcher[].pathRule[].service.backendBucketRef <object>
spec.pathMatcher[].pathRule[].service.backendBucketRef.external <string>
spec.pathMatcher[].pathRule[].service.backendBucketRef.name <string>
spec.pathMatcher[].pathRule[].service.backendBucketRef.namespace <string>
spec.pathMatcher[].pathRule[].service.backendServiceRef <object>
spec.pathMatcher[].pathRule[].service.backendServiceRef.external <string>
spec.pathMatcher[].pathRule[].service.backendServiceRef.name <string>
spec.pathMatcher[].pathRule[].service.backendServiceRef.namespace <string>
spec.pathMatcher[].pathRule[].urlRedirect <object>	# When a path pattern is matched, the request is redirected to a URL specified by urlRedirect. If urlRedirect is specified, service or routeAction must not be set.
spec.pathMatcher[].pathRule[].urlRedirect.hostRedirect <string>	# The host that will be used in the redirect response instead of the one that was supplied in the request. The value must be between 1 and 255 characters.
spec.pathMatcher[].pathRule[].urlRedirect.httpsRedirect <boolean>	# If set to true, the URL scheme in the redirected request is set to https. If set to false, the URL scheme of the redirected request will remain the same as that of the request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this true for TargetHttpsProxy is not permitted. The default is set to false.
spec.pathMatcher[].pathRule[].urlRedirect.pathRedirect <string>	# The path that will be used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together with prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.pathMatcher[].pathRule[].urlRedirect.prefixRedirect <string>	# The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.pathMatcher[].pathRule[].urlRedirect.redirectResponseCode <string>	# The HTTP Status code to use for this RedirectAction. Supported values are: * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. * FOUND, which corresponds to 302. * SEE_OTHER which corresponds to 303. * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method will be retained. * PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"].
spec.pathMatcher[].pathRule[].urlRedirect.stripQuery <boolean> (required)	# If set to true, any accompanying query portion of the original URL is removed prior to redirecting the request. If set to false, the query portion of the original URL is retained. This field is required to ensure an empty block is not set. The normal default value is false.
spec.pathMatcher[].routeRules <[]object>	# The list of ordered HTTP route rules. Use this list instead of pathRules when advanced route matching and routing actions are desired. The order of specifying routeRules matters: the first rule that matches will cause its specified routing action to take effect. Within a given pathMatcher, only one of pathRules or routeRules must be set. routeRules are not supported in UrlMaps intended for External load balancers.
spec.pathMatcher[].routeRules[].headerAction <object>	# Specifies changes to request and response headers that need to take effect for the selected backendService. The headerAction specified here are applied before the matching pathMatchers[].headerAction and after pathMatchers[].routeRules[].r outeAction.weightedBackendService.backendServiceWeightAction[].headerAction.
spec.pathMatcher[].routeRules[].headerAction.requestHeadersToAdd <[]object>	# Headers to add to a matching request prior to forwarding the request to the backendService.
spec.pathMatcher[].routeRules[].headerAction.requestHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].routeRules[].headerAction.requestHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].routeRules[].headerAction.requestHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].routeRules[].headerAction.requestHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the request prior to forwarding the request to the backendService.
spec.pathMatcher[].routeRules[].headerAction.responseHeadersToAdd <[]object>	# Headers to add the response prior to sending the response back to the client.
spec.pathMatcher[].routeRules[].headerAction.responseHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].routeRules[].headerAction.responseHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].routeRules[].headerAction.responseHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].routeRules[].headerAction.responseHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the response prior to sending the response back to the client.
spec.pathMatcher[].routeRules[].matchRules <[]object>	# The rules for determining a match.
spec.pathMatcher[].routeRules[].matchRules[].fullPathMatch <string>	# For satisfying the matchRule condition, the path of the request must exactly match the value specified in fullPathMatch after removing any query parameters and anchor that may be part of the original URL. FullPathMatch must be between 1 and 1024 characters. Only one of prefixMatch, fullPathMatch or regexMatch must be specified.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches <[]object>	# Specifies a list of header match criteria, all of which must match corresponding headers in the request.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].exactMatch <string>	# The value should exactly match contents of exactMatch. Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].headerName <string> (required)	# The name of the HTTP header to match. For matching against the HTTP request's authority, use a headerMatch with the header name ":authority". For matching a request's method, use the headerName ":method".
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].invertMatch <boolean>	# If set to false, the headerMatch is considered a match if the match criteria above are met. If set to true, the headerMatch is considered a match if the match criteria above are NOT met. Defaults to false.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].prefixMatch <string>	# The value of the header must start with the contents of prefixMatch. Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].presentMatch <boolean>	# A header with the contents of headerName must exist. The match takes place whether or not the request's header has a value or not. Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].rangeMatch <object>	# The header value must be an integer and its value must be in the range specified in rangeMatch. If the header does not contain an integer, number or is empty, the match fails. For example for a range [-5, 0] * -3 will match * 0 will not match * 0.25 will not match * -3someString will not match. Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].rangeMatch.rangeEnd <integer> (required) format=int64	# The end of the range (exclusive).
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].rangeMatch.rangeStart <integer> (required) format=int64	# The start of the range (inclusive).
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].regexMatch <string>	# The value of the header must match the regular expression specified in regexMatch. For regular expression grammar, please see: en.cppreference.com/w/cpp/regex/ecmascript For matching against a port specified in the HTTP request, use a headerMatch with headerName set to PORT and a regular expression that satisfies the RFC2616 Host header's port specifier. Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].headerMatches[].suffixMatch <string>	# The value of the header must end with the contents of suffixMatch. Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].ignoreCase <boolean>	# Specifies that prefixMatch and fullPathMatch matches are case sensitive. Defaults to false.
spec.pathMatcher[].routeRules[].matchRules[].metadataFilters <[]object>	# Opaque filter criteria used by Loadbalancer to restrict routing configuration to a limited set xDS compliant clients. In their xDS requests to Loadbalancer, xDS clients present node metadata. If a match takes place, the relevant routing configuration is made available to those proxies. For each metadataFilter in this list, if its filterMatchCriteria is set to MATCH_ANY, at least one of the filterLabels must match the corresponding label provided in the metadata. If its filterMatchCriteria is set to MATCH_ALL, then all of its filterLabels must match with corresponding labels in the provided metadata. metadataFilters specified here can be overrides those specified in ForwardingRule that refers to this UrlMap. metadataFilters only applies to Loadbalancers that have their loadBalancingScheme set to INTERNAL_SELF_MANAGED.
spec.pathMatcher[].routeRules[].matchRules[].metadataFilters[].filterLabels <[]object> (required)	# The list of label value pairs that must match labels in the provided metadata based on filterMatchCriteria This list must not be empty and can have at the most 64 entries.
spec.pathMatcher[].routeRules[].matchRules[].metadataFilters[].filterLabels[].name <string> (required)	# Name of metadata label. The name can have a maximum length of 1024 characters and must be at least 1 character long.
spec.pathMatcher[].routeRules[].matchRules[].metadataFilters[].filterLabels[].value <string> (required)	# The value of the label must match the specified value. value can have a maximum length of 1024 characters.
spec.pathMatcher[].routeRules[].matchRules[].metadataFilters[].filterMatchCriteria <string> (required)	# Specifies how individual filterLabel matches within the list of filterLabels contribute towards the overall metadataFilter match. Supported values are: * MATCH_ANY: At least one of the filterLabels must have a matching label in the provided metadata. * MATCH_ALL: All filterLabels must have matching labels in the provided metadata. Possible values: ["MATCH_ALL", "MATCH_ANY"].
spec.pathMatcher[].routeRules[].matchRules[].pathTemplateMatch <string>	# For satisfying the matchRule condition, the path of the request must match the wildcard pattern specified in pathTemplateMatch after removing any query parameters and anchor that may be part of the original URL. pathTemplateMatch must be between 1 and 255 characters (inclusive). The pattern specified by pathTemplateMatch may have at most 5 wildcard operators and at most 5 variable captures in total.
spec.pathMatcher[].routeRules[].matchRules[].prefixMatch <string>	# For satisfying the matchRule condition, the request's path must begin with the specified prefixMatch. prefixMatch must begin with a /. The value must be between 1 and 1024 characters. Only one of prefixMatch, fullPathMatch or regexMatch must be specified.
spec.pathMatcher[].routeRules[].matchRules[].queryParameterMatches <[]object>	# Specifies a list of query parameter match criteria, all of which must match corresponding query parameters in the request.
spec.pathMatcher[].routeRules[].matchRules[].queryParameterMatches[].exactMatch <string>	# The queryParameterMatch matches if the value of the parameter exactly matches the contents of exactMatch. Only one of presentMatch, exactMatch and regexMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].queryParameterMatches[].name <string> (required)	# The name of the query parameter to match. The query parameter must exist in the request, in the absence of which the request match fails.
spec.pathMatcher[].routeRules[].matchRules[].queryParameterMatches[].presentMatch <boolean>	# Specifies that the queryParameterMatch matches if the request contains the query parameter, irrespective of whether the parameter has a value or not. Only one of presentMatch, exactMatch and regexMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].queryParameterMatches[].regexMatch <string>	# The queryParameterMatch matches if the value of the parameter matches the regular expression specified by regexMatch. For the regular expression grammar, please see en.cppreference.com/w/cpp/regex/ecmascript Only one of presentMatch, exactMatch and regexMatch must be set.
spec.pathMatcher[].routeRules[].matchRules[].regexMatch <string>	# For satisfying the matchRule condition, the path of the request must satisfy the regular expression specified in regexMatch after removing any query parameters and anchor supplied with the original URL. For regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript Only one of prefixMatch, fullPathMatch or regexMatch must be specified.
spec.pathMatcher[].routeRules[].priority <integer> (required) format=int64	# For routeRules within a given pathMatcher, priority determines the order in which load balancer will interpret routeRules. RouteRules are evaluated in order of priority, from the lowest to highest number. The priority of a rule decreases as its number increases (1, 2, 3, N+1). The first rule that matches the request is applied. You cannot configure two or more routeRules with the same priority. Priority for each rule must be set to a number between 0 and 2147483647 inclusive. Priority numbers can have gaps, which enable you to add or remove rules in the future without affecting the rest of the rules. For example, 1, 2, 3, 4, 5, 9, 12, 16 is a valid series of priority numbers to which you could add rules numbered from 6 to 8, 10 to 11, and 13 to 15 in the future without any impact on existing rules.
spec.pathMatcher[].routeRules[].routeAction <object>	# In response to a matching matchRule, the load balancer performs advanced routing actions like URL rewrites, header transformations, etc. prior to forwarding the request to the selected backend. If routeAction specifies any weightedBackendServices, service must not be set. Conversely if service is set, routeAction cannot contain any weightedBackendServices. Only one of routeAction or urlRedirect must be set.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy <object>	# The specification for allowing client side cross-origin requests. Please see W3C Recommendation for Cross Origin Resource Sharing.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.allowCredentials <boolean>	# In response to a preflight request, setting this to true indicates that the actual request can include user credentials. This translates to the Access- Control-Allow-Credentials header. Defaults to false.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.allowHeaders <[]string>	# Specifies the content for the Access-Control-Allow-Headers header.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.allowMethods <[]string>	# Specifies the content for the Access-Control-Allow-Methods header.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.allowOriginRegexes <[]string>	# Specifies the regular expression patterns that match allowed origins. For regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript An origin is allowed if it matches either allow_origins or allow_origin_regex.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.allowOrigins <[]string>	# Specifies the list of origins that will be allowed to do CORS requests. An origin is allowed if it matches either allow_origins or allow_origin_regex.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.disabled <boolean>	# If true, specifies the CORS policy is disabled. which indicates that the CORS policy is in effect. Defaults to false.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.exposeHeaders <[]string>	# Specifies the content for the Access-Control-Expose-Headers header.
spec.pathMatcher[].routeRules[].routeAction.corsPolicy.maxAge <integer> format=int64	# Specifies how long the results of a preflight request can be cached. This translates to the content for the Access-Control-Max-Age header.
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy <object>	# The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure. As part of fault injection, when clients send requests to a backend service, delays can be introduced by Loadbalancer on a percentage of requests before sending those request to the backend service. Similarly requests from clients can be aborted by the Loadbalancer for a percentage of requests. timeout and retry_policy will be ignored by clients that are configured with a fault_injection_policy.
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.abort <object>
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.abort.httpStatus <integer> format=int64	# The HTTP status code used to abort the request. The value must be between 200 and 599 inclusive.
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.abort.percentage <number>	# The percentage of traffic (connections/operations/requests) which will be aborted as part of fault injection. The value must be between 0.0 and 100.0 inclusive.
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.delay <object>
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.delay.fixedDelay <object>
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.delay.fixedDelay.nanos <integer> format=int64
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.delay.fixedDelay.seconds <string> (required)
spec.pathMatcher[].routeRules[].routeAction.faultInjectionPolicy.delay.percentage <number>
spec.pathMatcher[].routeRules[].routeAction.requestMirrorPolicy <object>	# Specifies the policy on how requests intended for the route's backends are shadowed to a separate mirrored backend service. Loadbalancer does not wait for responses from the shadow service. Prior to sending traffic to the shadow service, the host / authority header is suffixed with -shadow.
spec.pathMatcher[].routeRules[].routeAction.requestMirrorPolicy.backendServiceRef <object> (required)
spec.pathMatcher[].routeRules[].routeAction.requestMirrorPolicy.backendServiceRef.external <string>
spec.pathMatcher[].routeRules[].routeAction.requestMirrorPolicy.backendServiceRef.name <string>
spec.pathMatcher[].routeRules[].routeAction.requestMirrorPolicy.backendServiceRef.namespace <string>
spec.pathMatcher[].routeRules[].routeAction.retryPolicy <object>	# Specifies the retry policy associated with this route.
spec.pathMatcher[].routeRules[].routeAction.retryPolicy.numRetries <integer> (required) format=int64
spec.pathMatcher[].routeRules[].routeAction.retryPolicy.perTryTimeout <object>
spec.pathMatcher[].routeRules[].routeAction.retryPolicy.perTryTimeout.nanos <integer> format=int64
spec.pathMatcher[].routeRules[].routeAction.retryPolicy.perTryTimeout.seconds <string> (required)
spec.pathMatcher[].routeRules[].routeAction.retryPolicy.retryConditions <[]string>
spec.pathMatcher[].routeRules[].routeAction.timeout <object>	# Specifies the timeout for the selected route. Timeout is computed from the time the request is has been fully processed (i.e. end-of-stream) up until the response has been completely processed. Timeout includes all retries. If not specified, the default value is 15 seconds.
spec.pathMatcher[].routeRules[].routeAction.timeout.nanos <integer> format=int64
spec.pathMatcher[].routeRules[].routeAction.timeout.seconds <string> (required)
spec.pathMatcher[].routeRules[].routeAction.urlRewrite <object>	# The spec to modify the URL of the request, prior to forwarding the request to the matched service.
spec.pathMatcher[].routeRules[].routeAction.urlRewrite.hostRewrite <string>	# Prior to forwarding the request to the selected service, the request's host header is replaced with contents of hostRewrite. The value must be between 1 and 255 characters.
spec.pathMatcher[].routeRules[].routeAction.urlRewrite.pathPrefixRewrite <string>	# Prior to forwarding the request to the selected backend service, the matching portion of the request's path is replaced by pathPrefixRewrite. The value must be between 1 and 1024 characters.
spec.pathMatcher[].routeRules[].routeAction.urlRewrite.pathTemplateRewrite <string>	# Prior to forwarding the request to the selected origin, if the request matched a pathTemplateMatch, the matching portion of the request's path is replaced re-written using the pattern specified by pathTemplateRewrite. pathTemplateRewrite must be between 1 and 255 characters (inclusive), must start with a '/', and must only use variables captured by the route's pathTemplate matchers. pathTemplateRewrite may only be used when all of a route's MatchRules specify pathTemplate. Only one of pathPrefixRewrite and pathTemplateRewrite may be specified.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices <[]object>	# A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be one weightedBackendService with weight set to a non 0 number. Once a backendService is identified and before forwarding the request to the backend service, advanced routing actions like Url rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].backendServiceRef <object> (required)
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].backendServiceRef.external <string>
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].backendServiceRef.name <string>
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].backendServiceRef.namespace <string>
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction <object>
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd <[]object>	# Headers to add to a matching request prior to forwarding the request to the backendService.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.requestHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.requestHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the request prior to forwarding the request to the backendService.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd <[]object>	# Headers to add the response prior to sending the response back to the client.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerName <string> (required)	# The name of the header.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].headerValue <string> (required)	# The value of the header to add.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.responseHeadersToAdd[].replace <boolean> (required)	# If false, headerValue is appended to any values that already exist for the header. If true, headerValue is set for the header, discarding any values that were set for that header.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].headerAction.responseHeadersToRemove <[]string>	# A list of header names for headers that need to be removed from the response prior to sending the response back to the client.
spec.pathMatcher[].routeRules[].routeAction.weightedBackendServices[].weight <integer> (required) format=int64
spec.pathMatcher[].routeRules[].service <string>	# The region backend service resource to which traffic is directed if this rule is matched. If routeAction is additionally specified, advanced routing actions like URL Rewrites, etc. take effect prior to sending the request to the backend. However, if service is specified, routeAction cannot contain any weightedBackendService s. Conversely, if routeAction specifies any weightedBackendServices, service must not be specified. Only one of urlRedirect, service or routeAction.weightedBackendService must be set.
spec.pathMatcher[].routeRules[].urlRedirect <object>	# When this rule is matched, the request is redirected to a URL specified by urlRedirect. If urlRedirect is specified, service or routeAction must not be set.
spec.pathMatcher[].routeRules[].urlRedirect.hostRedirect <string>	# The host that will be used in the redirect response instead of the one that was supplied in the request. The value must be between 1 and 255 characters.
spec.pathMatcher[].routeRules[].urlRedirect.httpsRedirect <boolean>	# If set to true, the URL scheme in the redirected request is set to https. If set to false, the URL scheme of the redirected request will remain the same as that of the request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this true for TargetHttpsProxy is not permitted. The default is set to false.
spec.pathMatcher[].routeRules[].urlRedirect.pathRedirect <string>	# The path that will be used in the redirect response instead of the one that was supplied in the request. pathRedirect cannot be supplied together with prefixRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.pathMatcher[].routeRules[].urlRedirect.prefixRedirect <string>	# The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch, retaining the remaining portion of the URL before redirecting the request. prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or neither. If neither is supplied, the path of the original request will be used for the redirect. The value must be between 1 and 1024 characters.
spec.pathMatcher[].routeRules[].urlRedirect.redirectResponseCode <string>	# The HTTP Status code to use for this RedirectAction. Supported values are: * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301. * FOUND, which corresponds to 302. * SEE_OTHER which corresponds to 303. * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method will be retained. * PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"].
spec.pathMatcher[].routeRules[].urlRedirect.stripQuery <boolean>	# If set to true, any accompanying query portion of the original URL is removed prior to redirecting the request. If set to false, the query portion of the original URL is retained. The default value is false.
spec.resourceID <string>	# Immutable. Optional. The name of the resource. Used for creation and acquisition. When unset, the value of `metadata.name` is used as the default.
spec.test <[]object>	# The list of expected URL mappings. Requests to update this UrlMap will succeed only if all of the test cases pass.
spec.test[].description <string>	# Description of this test case.
spec.test[].host <string> (required)	# Host portion of the URL.
spec.test[].path <string> (required)	# Path portion of the URL.
spec.test[].service <object> (required)	# The backend service resource that should be matched by this test. For the Global URL Map, it should be a reference to the backend service or backend bucket. For the Regional URL Map, it should be a reference to the backend service.
spec.test[].service.backendBucketRef <object>
spec.test[].service.backendBucketRef.external <string>
spec.test[].service.backendBucketRef.name <string>
spec.test[].service.backendBucketRef.namespace <string>
spec.test[].service.backendServiceRef <object>
spec.test[].service.backendServiceRef.external <string>
spec.test[].service.backendServiceRef.name <string>
spec.test[].service.backendServiceRef.namespace <string>
status <object>
status.conditions <[]object>	# Conditions represent the latest available observations of the ComputeURLMap's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.creationTimestamp <string>	# Creation timestamp in RFC3339 text format.
status.fingerprint <string>	# Fingerprint of this resource. This field is used internally during updates of this resource.
status.mapId <integer> format=int64	# The unique identifier for the resource.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.selfLink <string>
