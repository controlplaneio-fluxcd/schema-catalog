# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=compute.cnrm.cloud.google.com/v1beta1
kind <string> enum=ComputeResourcePolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# ComputeResourcePolicySpec defines the desired state of ComputeResourcePolicy
spec.description <string>	# Immutable. An optional description of this resource. Provide this property when you create the resource.
spec.diskConsistencyGroupPolicy <object>	# Immutable. Replication consistency group for asynchronous disk replication.
spec.diskConsistencyGroupPolicy.enabled <boolean> (required)	# Immutable. Enable disk consistency on the resource policy.
spec.groupPlacementPolicy <object>	# Immutable. Resource policy for instances used for placement configuration.
spec.groupPlacementPolicy.availabilityDomainCount <integer>	# Immutable. The number of availability domains instances will be spread across. If two instances are in different availability domain, they will not be put in the same low latency network.
spec.groupPlacementPolicy.collocation <string>	# Immutable. Collocation specifies whether to place VMs inside the same availability domain on the same low-latency network. Specify 'COLLOCATED' to enable collocation. Can only be specified with 'vm_count'. If compute instances are created with a COLLOCATED policy, then exactly 'vm_count' instances must be created at the same time with the resource policy attached. Possible values: ["COLLOCATED"].
spec.groupPlacementPolicy.maxDistance <integer>	# Immutable. Specifies the number of max logical switches.
spec.groupPlacementPolicy.vmCount <integer>	# Immutable. Number of VMs in this placement group. Google does not recommend that you use this field unless you use a compact policy and you want your policy to work only if it contains this exact number of VMs.
spec.instanceSchedulePolicy <object>	# Immutable. Resource policy for scheduling instance operations.
spec.instanceSchedulePolicy.expirationTime <string>	# Immutable. The expiration time of the schedule. The timestamp is an RFC3339 string.
spec.instanceSchedulePolicy.startTime <string>	# Immutable. The start time of the schedule. The timestamp is an RFC3339 string.
spec.instanceSchedulePolicy.timeZone <string> (required)	# Immutable. Specifies the time zone to be used in interpreting the schedule. The value of this field must be a time zone name from the tz database: http://en.wikipedia.org/wiki/Tz_database.
spec.instanceSchedulePolicy.vmStartSchedule <object>	# Immutable. Specifies the schedule for starting instances.
spec.instanceSchedulePolicy.vmStartSchedule.schedule <string> (required)	# Immutable. Specifies the frequency for the operation, using the unix-cron format.
spec.instanceSchedulePolicy.vmStopSchedule <object>	# Immutable. Specifies the schedule for stopping instances.
spec.instanceSchedulePolicy.vmStopSchedule.schedule <string> (required)	# Immutable. Specifies the frequency for the operation, using the unix-cron format.
spec.region <string> (required)	# Immutable. Region where resource policy resides.
spec.resourceID <string>	# Immutable. Optional. The name of the resource. Used for creation and acquisition. When unset, the value of `metadata.name` is used as the default.
spec.snapshotSchedulePolicy <object>	# Immutable. Policy for creating snapshots of persistent disks.
spec.snapshotSchedulePolicy.retentionPolicy <object>	# Immutable. Retention policy applied to snapshots created by this resource policy.
spec.snapshotSchedulePolicy.retentionPolicy.maxRetentionDays <integer> (required)	# Immutable. Maximum age of the snapshot that is allowed to be kept.
spec.snapshotSchedulePolicy.retentionPolicy.onSourceDiskDelete <string>	# Immutable. Specifies the behavior to apply to scheduled snapshots when the source disk is deleted. Default value: "KEEP_AUTO_SNAPSHOTS" Possible values: ["KEEP_AUTO_SNAPSHOTS", "APPLY_RETENTION_POLICY"].
spec.snapshotSchedulePolicy.schedule <object> (required)	# Immutable. Contains one of an 'hourlySchedule', 'dailySchedule', or 'weeklySchedule'.
spec.snapshotSchedulePolicy.schedule.dailySchedule <object>	# Immutable. The policy will execute every nth day at the specified time.
spec.snapshotSchedulePolicy.schedule.dailySchedule.daysInCycle <integer> (required)	# Immutable. Defines a schedule with units measured in days. The value determines how many days pass between the start of each cycle. Days in cycle for snapshot schedule policy must be 1.
spec.snapshotSchedulePolicy.schedule.dailySchedule.startTime <string> (required)	# Immutable. This must be in UTC format that resolves to one of 00:00, 04:00, 08:00, 12:00, 16:00, or 20:00. For example, both 13:00-5 and 08:00 are valid.
spec.snapshotSchedulePolicy.schedule.hourlySchedule <object>	# Immutable. The policy will execute every nth hour starting at the specified time.
spec.snapshotSchedulePolicy.schedule.hourlySchedule.hoursInCycle <integer> (required)	# Immutable. The number of hours between snapshots.
spec.snapshotSchedulePolicy.schedule.hourlySchedule.startTime <string> (required)	# Immutable. Time within the window to start the operations. It must be in an hourly format "HH:MM", where HH : [00-23] and MM : [00] GMT. eg: 21:00.
spec.snapshotSchedulePolicy.schedule.weeklySchedule <object>	# Immutable. Allows specifying a snapshot time for each day of the week.
spec.snapshotSchedulePolicy.schedule.weeklySchedule.dayOfWeeks <[]object> (required)	# Immutable. May contain up to seven (one for each day of the week) snapshot times.
spec.snapshotSchedulePolicy.schedule.weeklySchedule.dayOfWeeks[].day <string> (required)	# Immutable. The day of the week to create the snapshot. e.g. MONDAY Possible values: ["MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY", "SATURDAY", "SUNDAY"].
spec.snapshotSchedulePolicy.schedule.weeklySchedule.dayOfWeeks[].startTime <string> (required)	# Immutable. Time within the window to start the operations. It must be in format "HH:MM", where HH : [00-23] and MM : [00-00] GMT.
spec.snapshotSchedulePolicy.snapshotProperties <object>	# Immutable. Properties with which the snapshots are created, such as labels.
spec.snapshotSchedulePolicy.snapshotProperties.chainName <string>	# Immutable. Chain name that the snapshot is created in.
spec.snapshotSchedulePolicy.snapshotProperties.guestFlush <boolean>	# Immutable. Whether to perform a 'guest aware' snapshot.
spec.snapshotSchedulePolicy.snapshotProperties.labels <map[string]string>	# Immutable. A set of key-value pairs.
spec.snapshotSchedulePolicy.snapshotProperties.storageLocations <[]string>	# Immutable. Cloud Storage bucket location to store the auto snapshot (regional or multi-regional).
status <object>	# ComputeResourcePolicyStatus defines the config connector machine state of ComputeResourcePolicy
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the ComputeResourcePolicy resource in GCP.
status.observedGeneration <integer>	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.creationTimestamp <string>	# [Output Only] Creation timestamp in RFC3339 text format.
status.observedState.id <integer> format=int64	# [Output Only] The unique identifier for the resource. This identifier is defined by the server.
status.observedState.status <string>	# [Output Only] The status of resource policy creation.
status.selfLink <string>	# The server-defined URL of this resource.
