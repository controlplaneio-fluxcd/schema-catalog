# schema source: OpenShift v4.21 https://github.com/openshift/api
apiVersion <string> enum=project.openshift.io/v1
kind <string> enum=Project
metadata.name <string> (required)
metadata.namespace <string>
spec <object> default={}	# spec defines the behavior of the Namespace.
spec.finalizers <[]string>	# finalizers is an opaque list of values that must be empty to permanently remove object from storage
status <object> default={}	# status describes the current status of a Namespace
status.conditions <[]object>	# Represents the latest available observations of the project current state.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string> (required) default=""	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) default=""	# Type of namespace controller condition.
status.phase <string> enum=Active|Terminating	# phase is the current lifecycle phase of the project Possible enum values: - `"Active"` means the namespace is available for use in the system - `"Terminating"` means the namespace is undergoing graceful termination
