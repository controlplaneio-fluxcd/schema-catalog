# schema source: OpenShift v4.20
apiVersion <string> enum=security.internal.openshift.io/v1
kind <string> enum=RangeAllocation
metadata.name <string> (required)
metadata.namespace <string>
data <string> (required) format=byte	# data is a byte array representing the serialized state of a range allocation. It is a bitmap with each bit set to one to represent a range is taken.
range <string> (required) default=""	# range is a string representing a unique label for a range of uids, "1000000000-2000000000/10000".
