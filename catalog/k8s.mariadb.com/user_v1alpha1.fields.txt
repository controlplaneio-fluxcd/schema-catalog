# schema source: MariaDB Operator 26.6.0 https://github.com/mariadb-operator/mariadb-operator
apiVersion <string> enum=k8s.mariadb.com/v1alpha1
kind <string> enum=User
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# UserSpec defines the desired state of User
spec.cleanupPolicy <string> enum=Skip|Delete	# CleanupPolicy defines the behavior for cleaning up a SQL resource.
spec.host <string> max=255	# Host related to the User.
spec.mariaDbRef <object> (required)	# MariaDBRef is a reference to a MariaDB object.
spec.mariaDbRef.kind <string>	# Kind of the referent.
spec.mariaDbRef.name <string>
spec.mariaDbRef.namespace <string>
spec.mariaDbRef.waitForIt <boolean> default=true	# WaitForIt indicates whether the controller using this reference should wait for MariaDB to be ready.
spec.maxUserConnections <integer> default=10 format=int32	# MaxUserConnections defines the maximum number of simultaneous connections that the User can establish.
spec.name <string> max=80	# Name overrides the default name provided by metadata.name.
spec.passwordHashSecretKeyRef <object>	# PasswordHashSecretKeyRef is a reference to the password hash to be used by the User. If the referred Secret is labeled with "k8s.mariadb.com/watch", updates may be performed to the Secret in order to update the password hash.
spec.passwordHashSecretKeyRef.key <string> (required)
spec.passwordHashSecretKeyRef.name <string> default=""
spec.passwordPlugin <object>	# PasswordPlugin is a reference to the password plugin and arguments to be used by the User.
spec.passwordPlugin.pluginArgSecretKeyRef <object>	# PluginArgSecretKeyRef is a reference to the arguments to be provided to the authentication plugin for the User. If the referred Secret is labeled with "k8s.mariadb.com/watch", updates may be performed to the Secret in order to update the authentication plugin arguments.
spec.passwordPlugin.pluginArgSecretKeyRef.key <string> (required)
spec.passwordPlugin.pluginArgSecretKeyRef.name <string> default=""
spec.passwordPlugin.pluginNameSecretKeyRef <object>	# PluginNameSecretKeyRef is a reference to the authentication plugin to be used by the User. If the referred Secret is labeled with "k8s.mariadb.com/watch", updates may be performed to the Secret in order to update the authentication plugin.
spec.passwordPlugin.pluginNameSecretKeyRef.key <string> (required)
spec.passwordPlugin.pluginNameSecretKeyRef.name <string> default=""
spec.passwordSecretKeyRef <object>	# PasswordSecretKeyRef is a reference to the password to be used by the User. If not provided, the account will be locked and the password will expire. If the referred Secret is labeled with "k8s.mariadb.com/watch", updates may be performed to the Secret in order to update the password.
spec.passwordSecretKeyRef.key <string> (required)
spec.passwordSecretKeyRef.name <string> default=""
spec.requeueInterval <string>	# RequeueInterval is used to perform requeue reconciliations.
spec.require <object>	# Require specifies TLS requirements for the user to connect. See: https://mariadb.com/kb/en/securing-connections-for-client-and-server/#requiring-tls.
spec.require.issuer <string>	# Issuer indicates that the TLS certificate provided by the user must be issued by a specific issuer.
spec.require.ssl <boolean>	# SSL indicates that the user must connect via TLS.
spec.require.subject <string>	# Subject indicates that the TLS certificate provided by the user must have a specific subject.
spec.require.x509 <boolean>	# X509 indicates that the user must provide a valid x509 certificate to connect.
spec.retryInterval <string>	# RetryInterval is the interval used to perform retries.
status <object>	# UserStatus defines the observed state of User
status.conditions <[]object>	# Conditions for the User object.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
