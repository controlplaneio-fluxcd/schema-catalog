# schema source: OPA Gatekeeper v3.23.0 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=status.gatekeeper.sh/v1beta1
kind <string> enum=ConstraintPodStatus
metadata.name <string> (required)
metadata.namespace <string> (required)
status <object>	# ConstraintPodStatusStatus defines the observed state of ConstraintPodStatus.
status.constraintUID <string>	# Storing the constraint UID allows us to detect drift, such as when a constraint has been recreated after its CRD was deleted out from under it, interrupting the watch
status.enforced <boolean>
status.enforcementPointsStatus <[]object>
status.enforcementPointsStatus[].enforcementPoint <string> (required)
status.enforcementPointsStatus[].message <string>
status.enforcementPointsStatus[].observedGeneration <integer> format=int64
status.enforcementPointsStatus[].state <string> (required)
status.errors <[]object>
status.errors[].code <string> (required)
status.errors[].location <string>
status.errors[].message <string> (required)
status.id <string>
status.observedGeneration <integer> format=int64
status.operations <[]string>
