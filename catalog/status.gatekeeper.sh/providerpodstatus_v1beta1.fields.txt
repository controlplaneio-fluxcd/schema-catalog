# schema source: OPA Gatekeeper v3.22.2 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=status.gatekeeper.sh/v1beta1
kind <string> enum=ProviderPodStatus
metadata.name <string> (required)
metadata.namespace <string> (required)
status <object>	# No spec field is defined here, as this is a status-only resource.
status.active <boolean>
status.errors <[]object>
status.errors[].errorTimestamp <string> (required) format=date-time
status.errors[].message <string> (required)
status.errors[].retryable <boolean> (required)
status.errors[].type <string> (required)	# Type indicates a specific class of error for use by controller code. If not present, the error should be treated as not matching any known type.
status.id <string>
status.lastCacheUpdateTime <string> format=date-time
status.lastTransitionTime <string> format=date-time
status.observedGeneration <integer> format=int64
status.operations <[]string>
status.providerUID <string>	# Storing the provider UID allows us to detect drift, such as when a provider has been recreated after its CRD was deleted out from under it, interrupting the watch
