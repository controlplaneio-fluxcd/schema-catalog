# schema source: OPA Gatekeeper v3.22.2 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=status.gatekeeper.sh/v1beta1
kind <string> enum=ConfigPodStatus
metadata.name <string> (required)
metadata.namespace <string> (required)
status <object>
status.configUID <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.errors <[]object>
status.errors[].message <string> (required)
status.errors[].type <string>
status.id <string>
status.observedGeneration <integer> format=int64
status.operations <[]string>
