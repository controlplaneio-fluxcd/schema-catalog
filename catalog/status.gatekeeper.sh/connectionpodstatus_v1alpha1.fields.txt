# schema source: OPA Gatekeeper v3.23.0 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=status.gatekeeper.sh/v1alpha1
kind <string> enum=ConnectionPodStatus
metadata.name <string> (required)
metadata.namespace <string> (required)
status <object>	# No spec field is defined here, as this is a status-only resource.
status.active <boolean>	# Indicator for alive connection with at least one successful publish
status.connectionUID <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.errors <[]object>
status.errors[].message <string> (required)
status.errors[].type <string> (required)
status.id <string>	# ID is the unique identifier for the pod that wrote the status
status.observedGeneration <integer> format=int64
status.operations <[]string>
