# schema source: OPA Gatekeeper v3.22.2 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=status.gatekeeper.sh/v1beta1
kind <string> enum=MutatorPodStatus
metadata.name <string> (required)
metadata.namespace <string> (required)
status <object>	# MutatorPodStatusStatus defines the observed state of MutatorPodStatus.
status.enforced <boolean>
status.errors <[]object>
status.errors[].message <string> (required)
status.errors[].type <string>	# Type indicates a specific class of error for use by controller code. If not present, the error should be treated as not matching any known type.
status.id <string>
status.mutatorUID <string>	# Storing the mutator UID allows us to detect drift, such as when a mutator has been recreated after its CRD was deleted out from under it, interrupting the watch
status.observedGeneration <integer> format=int64
status.operations <[]string>
