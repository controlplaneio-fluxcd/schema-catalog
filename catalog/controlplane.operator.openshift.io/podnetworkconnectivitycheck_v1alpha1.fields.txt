# schema source: OpenShift v4.20 https://github.com/openshift/api
apiVersion <string> enum=controlplane.operator.openshift.io/v1alpha1
kind <string> enum=PodNetworkConnectivityCheck
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}	# spec defines the source and target of the connectivity check
spec.sourcePod <string> (required) default=""	# sourcePod names the pod from which the condition will be checked
spec.targetEndpoint <string> (required) default=""	# EndpointAddress to check. A TCP address of the form host:port. Note that if host is a DNS name, then the check would fail if the DNS name cannot be resolved. Specify an IP address for host to bypass DNS name lookup.
spec.tlsClientCert <object> default={}	# TLSClientCert, if specified, references a kubernetes.io/tls type secret with 'tls.crt' and 'tls.key' entries containing an optional TLS client certificate and key to be used when checking endpoints that require a client certificate in order to gracefully preform the scan without causing excessive logging in the endpoint process. The secret must exist in the same namespace as this resource.
spec.tlsClientCert.name <string> (required) default=""	# name is the metadata.name of the referenced secret
status <object> default={}	# status contains the observed status of the connectivity check
status.conditions <[]object>	# conditions summarize the status of the check
status.conditions[].lastTransitionTime <string> (required) format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# message indicating details about last transition in a human readable format.
status.conditions[].reason <string>	# reason for the condition's last status transition in a machine readable format.
status.conditions[].status <string> (required) default=""	# status of the condition
status.conditions[].type <string> (required) default=""	# type of the condition
status.failures <[]object>	# failures contains logs of unsuccessful check actions
status.failures[].latency <string>	# latency records how long the action mentioned in the entry took.
status.failures[].message <string>	# message explaining status in a human readable format.
status.failures[].reason <string>	# reason for status in a machine readable format.
status.failures[].success <boolean> (required) default=false	# success indicates if the log entry indicates a success or failure.
status.failures[].time <string> (required) format=date-time	# Start time of check action.
status.outages <[]object>	# outages contains logs of time periods of outages
status.outages[].end <string> format=date-time	# end of outage detected
status.outages[].endLogs <[]object>	# endLogs contains log entries related to the end of this outage. Should contain the success entry that resolved the outage and possibly a few of the failure log entries that preceded it.
status.outages[].endLogs[].latency <string>	# latency records how long the action mentioned in the entry took.
status.outages[].endLogs[].message <string>	# message explaining status in a human readable format.
status.outages[].endLogs[].reason <string>	# reason for status in a machine readable format.
status.outages[].endLogs[].success <boolean> (required) default=false	# success indicates if the log entry indicates a success or failure.
status.outages[].endLogs[].time <string> (required) format=date-time	# Start time of check action.
status.outages[].message <string>	# message summarizes outage details in a human readable format.
status.outages[].start <string> (required) format=date-time	# start of outage detected
status.outages[].startLogs <[]object>	# startLogs contains log entries related to the start of this outage. Should contain the original failure, any entries where the failure mode changed.
status.outages[].startLogs[].latency <string>	# latency records how long the action mentioned in the entry took.
status.outages[].startLogs[].message <string>	# message explaining status in a human readable format.
status.outages[].startLogs[].reason <string>	# reason for status in a machine readable format.
status.outages[].startLogs[].success <boolean> (required) default=false	# success indicates if the log entry indicates a success or failure.
status.outages[].startLogs[].time <string> (required) format=date-time	# Start time of check action.
status.successes <[]object>	# successes contains logs successful check actions
status.successes[].latency <string>	# latency records how long the action mentioned in the entry took.
status.successes[].message <string>	# message explaining status in a human readable format.
status.successes[].reason <string>	# reason for status in a machine readable format.
status.successes[].success <boolean> (required) default=false	# success indicates if the log entry indicates a success or failure.
status.successes[].time <string> (required) format=date-time	# Start time of check action.
