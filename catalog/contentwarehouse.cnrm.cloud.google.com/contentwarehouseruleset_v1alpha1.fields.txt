# schema source: GCP Config Connector v1.154.1 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=contentwarehouse.cnrm.cloud.google.com/v1alpha1
kind <string> enum=ContentWarehouseRuleSet
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# ContentWarehouseRuleSetSpec defines the desired state of ContentWarehouseRuleSet
spec.description <string>	# Short description of the rule-set.
spec.location <string> (required)	# The location of this resource.
spec.projectRef <object> (required)	# The project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The ContentWarehouseRuleSet name. If not given, the metadata.name will be used.
spec.rules <[]object>	# List of rules given by the customer.
spec.rules[].actions <[]object>	# List of actions that are executed when the rule is satisfied.
spec.rules[].actions[].accessControl <object>	# Action triggering access control operations.
spec.rules[].actions[].accessControl.operationType <string>	# Identifies the type of operation.
spec.rules[].actions[].accessControl.policy <object>	# Represents the new policy from which bindings are added, removed or replaced based on the type of the operation. the policy is limited to a few 10s of KB.
spec.rules[].actions[].accessControl.policy.auditConfigs <[]object>	# Specifies cloud audit logging configuration for this policy.
spec.rules[].actions[].accessControl.policy.auditConfigs[].auditLogConfigs <[]object>	# The configuration for logging of each type of permission.
spec.rules[].actions[].accessControl.policy.auditConfigs[].auditLogConfigs[].exemptedMembers <[]string>	# Specifies the identities that do not cause logging for this type of permission.
spec.rules[].actions[].accessControl.policy.auditConfigs[].auditLogConfigs[].logType <string>	# The log type that this config enables.
spec.rules[].actions[].accessControl.policy.auditConfigs[].service <string>	# Specifies a service that will be enabled for audit logging.
spec.rules[].actions[].accessControl.policy.bindings <[]object>	# Associates a list of `members`, or principals, with a `role`.
spec.rules[].actions[].accessControl.policy.bindings[].condition <object>	# The condition that is associated with this binding.
spec.rules[].actions[].accessControl.policy.bindings[].condition.description <string>	# Optional. Description of the expression.
spec.rules[].actions[].accessControl.policy.bindings[].condition.expression <string>	# Textual representation of an expression in Common Expression Language syntax.
spec.rules[].actions[].accessControl.policy.bindings[].condition.location <string>	# Optional. String indicating the location of the expression for error reporting.
spec.rules[].actions[].accessControl.policy.bindings[].condition.title <string>	# Optional. Title for the expression.
spec.rules[].actions[].accessControl.policy.bindings[].members <[]string>	# Specifies the principals requesting access for a Google Cloud resource.
spec.rules[].actions[].accessControl.policy.bindings[].role <string>	# Role that is assigned to the list of `members`, or principals.
spec.rules[].actions[].accessControl.policy.version <integer> format=int32	# Specifies the format of the policy.
spec.rules[].actions[].actionID <string>	# ID of the action. Managed internally.
spec.rules[].actions[].addToFolder <object>	# Action triggering create document link operation.
spec.rules[].actions[].addToFolder.folders <[]string>	# Names of the folder under which new document is to be added.
spec.rules[].actions[].dataUpdate <object>	# Action triggering data update operations.
spec.rules[].actions[].dataUpdate.entries <map[string]string>	# Map of (K, V) -> (valid name of the field, new value of the field)
spec.rules[].actions[].dataValidation <object>	# Action triggering data validation operations.
spec.rules[].actions[].dataValidation.conditions <map[string]string>	# Map of (K, V) -> (field, string condition to be evaluated on the field)
spec.rules[].actions[].deleteDocumentAction <object>	# Action deleting the document.
spec.rules[].actions[].deleteDocumentAction.enableHardDelete <boolean>	# Boolean field to select between hard vs soft delete options.
spec.rules[].actions[].publishToPubSub <object>	# Action publish to Pub/Sub operation.
spec.rules[].actions[].publishToPubSub.messages <[]string>	# Messages to be published.
spec.rules[].actions[].publishToPubSub.topicID <string>	# The topic id in the Pub/Sub service for which messages will be published to.
spec.rules[].actions[].removeFromFolderAction <object>	# Action removing a document from a folder.
spec.rules[].actions[].removeFromFolderAction.condition <string>	# Condition of the action to be executed.
spec.rules[].actions[].removeFromFolderAction.folder <string>	# Name of the folder under which new document is to be added.
spec.rules[].condition <string>	# Represents the conditional expression to be evaluated. Expression should evaluate to a boolean result. When the condition is true actions are executed. Example: user_role = "hsbc_role_1" AND doc.salary > 20000
spec.rules[].description <string>	# Short description of the rule and its context.
spec.rules[].ruleID <string>	# ID of the rule. It has to be unique across all the examples. This is managed internally.
spec.rules[].triggerType <string>	# Identifies the trigger type for running the policy.
spec.source <string>	# Source of the rules i.e., customer name.
status <object>	# ContentWarehouseRuleSetStatus defines the config connector machine state of ContentWarehouseRuleSet
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the ContentWarehouseRuleSet resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object (free-form)>	# ObservedState is the state of the resource as most recently observed in GCP.
