# schema source: OpenSearch Operator opensearch-operator-3.0.2 https://github.com/opensearch-project/opensearch-k8s-operator
apiVersion <string> enum=opensearch.org/v1
kind <string> enum=OpensearchIndexTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec._meta <any>	# Optional user metadata about the index template
spec.composedOf <[]string>	# An ordered list of component template names. Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence
spec.dataStream <object>	# The dataStream config that should be applied
spec.dataStream.timestamp_field <object>	# TimestampField for dataStream
spec.dataStream.timestamp_field.name <string> (required)	# Name of the field that are used for the DataStream
spec.indexPatterns <[]string> (required)	# Array of wildcard expressions used to match the names of indices during creation
spec.name <string>	# The name of the index template. Defaults to metadata.name
spec.opensearchCluster <object> (required)	# LocalObjectReference contains enough information to let you locate the referenced object inside the same namespace.
spec.opensearchCluster.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.priority <integer>	# Priority to determine index template precedence when a new data stream or index is created. The index template with the highest priority is chosen
spec.template <object>	# The template that should be applied
spec.template.aliases <map[string]object>	# Aliases to add
spec.template.aliases.<key>.alias <string>	# The name of the alias.
spec.template.aliases.<key>.filter <any>	# Query used to limit documents the alias can access.
spec.template.aliases.<key>.index <string>	# The name of the index that the alias points to.
spec.template.aliases.<key>.isWriteIndex <boolean>	# If true, the index is the write index for the alias
spec.template.aliases.<key>.routing <string>	# Value used to route indexing and search operations to a specific shard.
spec.template.mappings <any>	# Mapping for fields in the index
spec.template.settings <any>	# Configuration options for the index
spec.version <integer>	# Version number used to manage the component template externally
status <object>
status.existingIndexTemplate <boolean>
status.indexTemplateName <string>	# Name of the currently managed index template
status.managedCluster <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.reason <string>
status.state <string>
