# schema source: KubeEdge v1.23.0 https://github.com/kubeedge/kubeedge
apiVersion <string> enum=devices.kubeedge.io/v1beta1
kind <string> enum=DeviceModel
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# DeviceModelSpec defines the model for a device.It is a blueprint which describes the device capabilities and access mechanism via property visitors.
spec.additionalProperties <any>
spec.properties <[]object>	# Required: List of device properties.
spec.properties[].accessMode <string> enum=ReadWrite|ReadOnly	# Required: Access mode of property, ReadWrite or ReadOnly.
spec.properties[].description <string>	# The device property description.
spec.properties[].maximum <string>
spec.properties[].minimum <string>
spec.properties[].name <string>	# Required: The device property name. Note: If you need to use the built-in stream data processing function, you need to define Name as saveFrame or saveVideo
spec.properties[].type <string> enum=INT|FLOAT|DOUBLE|STRING|BOOLEAN|BYTES|STREAM	# Required: Type of device property, ENUM: INT,FLOAT,DOUBLE,STRING,BOOLEAN,BYTES,STREAM
spec.properties[].unit <string>	# The unit of the property
spec.properties[].visitors <object>	# Note: It can be overridden in the device instance
spec.properties[].visitors.configData <object (free-form)>	# Required: The configData of customized protocol
spec.properties[].visitors.protocolName <string>	# Required: name of customized protocol
spec.protocol <string>	# Required: Protocol name used by the device.
spec.protocolConfigData <object (free-form)>	# Optional: Any config data.It can be overridden in the device instance.
