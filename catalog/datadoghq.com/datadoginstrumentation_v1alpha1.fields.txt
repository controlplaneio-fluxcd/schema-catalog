# schema source: Datadog Operator v1.30.0 https://github.com/DataDog/datadog-operator
apiVersion <string> enum=datadoghq.com/v1alpha1
kind <string> enum=DatadogInstrumentation
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# DatadogInstrumentationSpec defines the desired state of DatadogInstrumentation.
spec.config <object> (required)	# Config defines the Datadog instrumentation configuration to apply to the target workload.
spec.config.checks <[]object>	# Checks configures Datadog Agent Autodiscovery checks for the target workload.
spec.config.checks[].containerName <string>	# ContainerName identifies the container name this check applies to.
spec.config.checks[].initConfig <object (free-form)>	# InitConfig is the integration-specific Autodiscovery init_config payload.
spec.config.checks[].instances <[]object>	# Instances contains integration-specific Autodiscovery instances payloads.
spec.config.checks[].integration <string> (required)	# Integration is the Datadog integration name, for example redisdb.
spec.config.logs <[]object>	# Logs configures Datadog Agent log collection for the target workload.
spec.config.logs[].allowed_ips <[]string>	# AllowedIPs lists IP addresses or CIDR ranges allowed to send TCP or UDP logs.
spec.config.logs[].attribute_parsing <boolean>	# AttributeParsing controls whether the full syslog parser is enabled for this source.
spec.config.logs[].auto_multi_line <object>	# AutoMultiLine defines detailed automatic multi-line detection options for this source.
spec.config.logs[].auto_multi_line.enable_datetime_detection <boolean>	# EnableDatetimeDetection controls detection of multi-line logs based on leading timestamps.
spec.config.logs[].auto_multi_line.enable_json_aggregation <boolean>	# EnableJSONAggregation controls aggregation of multi-line JSON logs.
spec.config.logs[].auto_multi_line.enable_json_detection <boolean>	# EnableJSONDetection controls detection of JSON logs so they are not aggregated as unstructured multi-line logs.
spec.config.logs[].auto_multi_line.pattern_table_max_size <integer> format=int32 min=1	# PatternTableMaxSize sets the maximum number of patterns used by automatic multi-line detection.
spec.config.logs[].auto_multi_line.stack_trace_parsers <[]string>	# StackTraceParsers overrides the enabled stack trace parsers. An empty list disables stack trace aggregation.
spec.config.logs[].auto_multi_line.tag_aggregated_json <boolean>	# TagAggregatedJSON controls whether aggregated JSON logs are tagged.
spec.config.logs[].auto_multi_line.tokenizer_max_input_bytes <integer> format=int32	# TokenizerMaxInputBytes sets the maximum number of bytes tokenized from each log line.
spec.config.logs[].auto_multi_line_detection <boolean>	# AutoMultiLineDetection enables automatic multi-line detection for this source.
spec.config.logs[].auto_multi_line_detection_custom_samples <[]object>	# AutoMultiLineDetectionCustomSamples defines samples and regular expressions used by automatic multi-line detection.
spec.config.logs[].auto_multi_line_detection_custom_samples[].label <string>	# Label is applied when the sample or regular expression matches.
spec.config.logs[].auto_multi_line_detection_custom_samples[].regex <string>	# Regex is a regular expression used to identify the start of a multi-line log.
spec.config.logs[].auto_multi_line_detection_custom_samples[].sample <string>	# Sample is a representative raw log message used to identify the start of a multi-line log.
spec.config.logs[].bind_host <string>	# BindHost is the host or IP address on which a TCP or UDP log source listens.
spec.config.logs[].channel_path <string>	# ChannelPath is the Windows event channel path when type is windows_event.
spec.config.logs[].containerName <string> (required)	# ContainerName identifies the container name this log configuration applies to.
spec.config.logs[].denied_ips <[]string>	# DeniedIPs lists IP addresses or CIDR ranges denied from sending TCP or UDP logs.
spec.config.logs[].encoding <string>	# Encoding sets the file encoding when type is file. Common values include utf-16-le, utf-16-be, and shift-jis.
spec.config.logs[].exclude_paths <[]string>	# ExcludePaths lists matching files to exclude when type is file and path contains a wildcard.
spec.config.logs[].exclude_units <[]string>	# ExcludeUnits lists journald units to exclude when type is journald.
spec.config.logs[].fingerprint_config <object>	# FingerprintConfig defines per-source file fingerprinting options.
spec.config.logs[].fingerprint_config.count <integer> format=int32 min=1	# Count is the number of lines or bytes used to compute the fingerprint.
spec.config.logs[].fingerprint_config.count_to_skip <integer> format=int32 min=0	# CountToSkip is the number of lines or bytes skipped before computing the fingerprint.
spec.config.logs[].fingerprint_config.fingerprint_strategy <string> (required) enum=line_checksum|byte_checksum|disabled	# FingerprintStrategy sets the fingerprinting strategy.
spec.config.logs[].fingerprint_config.max_bytes <integer> format=int32 min=1	# MaxBytes limits the number of bytes used for line-based fingerprinting.
spec.config.logs[].format <string>	# Format sets the parsing format for this log source. The supported structured format is syslog.
spec.config.logs[].idle_timeout <string>	# IdleTimeout is the duration before an idle TCP connection is closed.
spec.config.logs[].include_units <[]string>	# IncludeUnits lists journald units to include when type is journald.
spec.config.logs[].log_processing_rules <[]object>	# LogProcessingRules contains Agent log processing rules for this log source.
spec.config.logs[].max_connections <integer> format=int32	# MaxConnections is the maximum number of concurrent TCP connections.
spec.config.logs[].max_message_size_bytes <integer> format=int32 min=1	# MaxMessageSizeBytes overrides the global maximum message size for this source.
spec.config.logs[].path <string>	# Path is the file path for gathering logs when type is file or journald.
spec.config.logs[].port <integer> format=int32	# Port is the port for listening to logs when type is tcp or udp.
spec.config.logs[].service <string>	# Service sets the log service name.
spec.config.logs[].source <string>	# Source sets the log source name.
spec.config.logs[].sourcecategory <string>	# SourceCategory sets the source category attribute.
spec.config.logs[].start_position <string>	# StartPosition sets where the Agent starts reading for file and journald tailers. Common values include beginning, end, forceBeginning, and forceEnd.
spec.config.logs[].tags <[]string>	# Tags sets additional tags on collected logs.
spec.config.logs[].type <string>	# Type is the type of log input source. Common values include tcp, udp, file, windows_event, docker, and journald.
spec.targetRef <object> (required)	# TargetRef is the reference to the workload resource to instrument.
spec.targetRef.apiVersion <string>	# apiVersion is the API version of the referent
spec.targetRef.kind <string> (required)	# kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.targetRef.name <string> (required)	# name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status <object>	# DatadogInstrumentationStatus defines the observed state of DatadogInstrumentation.
status.conditions <[]object>	# Conditions represent the latest available observations of the instrumentation handlers.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
