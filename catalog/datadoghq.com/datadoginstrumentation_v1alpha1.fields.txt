# schema source: Datadog Operator v1.28.0 https://github.com/DataDog/datadog-operator
apiVersion <string> enum=datadoghq.com/v1alpha1
kind <string> enum=DatadogInstrumentation
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# DatadogInstrumentationSpec defines the desired state of DatadogInstrumentation.
spec.config <object> (required)	# Config defines the Datadog instrumentation configuration to apply to the target workload.
spec.config.checks <[]object>	# Checks configures Datadog Agent Autodiscovery checks for the target workload.
spec.config.checks[].containerImage <[]string>	# ContainerImage identifies container image names this check applies to.
spec.config.checks[].initConfig <object (free-form)>	# InitConfig is the integration-specific Autodiscovery init_config payload.
spec.config.checks[].instances <[]object>	# Instances contains integration-specific Autodiscovery instances payloads.
spec.config.checks[].integration <string> (required)	# Integration is the Datadog integration name, for example redisdb.
spec.config.checks[].logs <[]object>	# Logs contains log collection configuration payloads for this integration.
spec.config.checks[].logs[].channel_path <string>	# ChannelPath is the Windows event channel path when type is windows_event.
spec.config.checks[].logs[].encoding <string>	# Encoding sets the file encoding when type is file. Common values include utf-16-le, utf-16-be, and shift-jis.
spec.config.checks[].logs[].exclude_paths <[]string>	# ExcludePaths lists matching files to exclude when type is file and path contains a wildcard.
spec.config.checks[].logs[].exclude_units <[]string>	# ExcludeUnits lists journald units to exclude when type is journald.
spec.config.checks[].logs[].include_units <[]string>	# IncludeUnits lists journald units to include when type is journald.
spec.config.checks[].logs[].log_processing_rules <[]object>	# LogProcessingRules contains Agent log processing rules for this log source.
spec.config.checks[].logs[].path <string>	# Path is the file path for gathering logs when type is file or journald.
spec.config.checks[].logs[].port <integer> format=int32	# Port is the port for listening to logs when type is tcp or udp.
spec.config.checks[].logs[].service <string>	# Service sets the log service name.
spec.config.checks[].logs[].source <string>	# Source sets the log source name.
spec.config.checks[].logs[].sourcecategory <string>	# SourceCategory sets the source category attribute.
spec.config.checks[].logs[].start_position <string>	# StartPosition sets where the Agent starts reading for file and journald tailers. Common values include beginning, end, forceBeginning, and forceEnd.
spec.config.checks[].logs[].tags <[]string>	# Tags sets additional tags on collected logs.
spec.config.checks[].logs[].type <string>	# Type is the type of log input source. Common values include tcp, udp, file, windows_event, docker, and journald.
spec.targetRef <object> (required)	# TargetRef is the reference to the workload resource to instrument.
spec.targetRef.apiVersion <string>	# apiVersion is the API version of the referent
spec.targetRef.kind <string> (required)	# kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.targetRef.name <string> (required)	# name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status <object>	# DatadogInstrumentationStatus defines the observed state of DatadogInstrumentation.
status.conditions <[]object>	# Conditions represent the latest available observations of the instrumentation handlers.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
