# schema source: Datadog Operator v1.28.0 https://github.com/DataDog/datadog-operator
apiVersion <string> enum=datadoghq.com/v1alpha2
kind <string> enum=DatadogPodAutoscaler
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# DatadogPodAutoscalerSpec defines the desired state of DatadogPodAutoscaler
spec.applyPolicy <object> default={}	# ApplyPolicy defines how recommendations should be applied.
spec.applyPolicy.mode <string> enum=Apply|Preview default="Apply"	# Mode determines recommendations that should be applied by the controller: - Apply: Apply all recommendations. - Preview: Recommendations are received and visible through .Status, but the controller does not apply them. It's also possible to selectively deactivate upscale, downscale or update actions thanks to the `ScaleUp`, `ScaleDown` and `Update` fields.
spec.applyPolicy.scaleDown <object>	# ScaleDown defines the policy to scale down the target resource.
spec.applyPolicy.scaleDown.rules <[]object>	# Rules is a list of potential scaling polices which can be used during scaling. At least one policy must be specified, otherwise the DatadogPodAutoscalerScalingPolicy will be discarded as invalid
spec.applyPolicy.scaleDown.rules[].periodSeconds <integer> (required) format=int32 min=1 max=3600	# PeriodSeconds specifies the window of time for which the policy should hold true. PeriodSeconds must be greater than zero and less than or equal to 3600 (1 hour).
spec.applyPolicy.scaleDown.rules[].type <string> (required) enum=Pods|Percent	# Type is used to specify the scaling policy.
spec.applyPolicy.scaleDown.rules[].value <integer> (required) format=int32 min=0	# Value contains the amount of change which is permitted by the policy. Setting it to 0 will prevent any scaling in this direction.
spec.applyPolicy.scaleDown.stabilizationWindowSeconds <integer> format=int32 min=0 max=3600	# StabilizationWindowSeconds is the number of seconds the controller should lookback at previous recommendations before deciding to apply a new one. Defaults to 0.
spec.applyPolicy.scaleDown.strategy <string> enum=Max|Min|Disabled	# Strategy is used to specify which policy should be used. If not set, the default value Max is used.
spec.applyPolicy.scaleUp <object>	# ScaleUp defines the policy to scale up the target resource.
spec.applyPolicy.scaleUp.rules <[]object>	# Rules is a list of potential scaling polices which can be used during scaling. At least one policy must be specified, otherwise the DatadogPodAutoscalerScalingPolicy will be discarded as invalid
spec.applyPolicy.scaleUp.rules[].periodSeconds <integer> (required) format=int32 min=1 max=3600	# PeriodSeconds specifies the window of time for which the policy should hold true. PeriodSeconds must be greater than zero and less than or equal to 3600 (1 hour).
spec.applyPolicy.scaleUp.rules[].type <string> (required) enum=Pods|Percent	# Type is used to specify the scaling policy.
spec.applyPolicy.scaleUp.rules[].value <integer> (required) format=int32 min=0	# Value contains the amount of change which is permitted by the policy. Setting it to 0 will prevent any scaling in this direction.
spec.applyPolicy.scaleUp.stabilizationWindowSeconds <integer> format=int32 min=0 max=3600	# StabilizationWindowSeconds is the number of seconds the controller should lookback at previous recommendations before deciding to apply a new one. Defaults to 0.
spec.applyPolicy.scaleUp.strategy <string> enum=Max|Min|Disabled	# Strategy is used to specify which policy should be used. If not set, the default value Max is used.
spec.applyPolicy.update <object>	# Update defines the policy for updating the target resource.
spec.applyPolicy.update.resizePendingPeriod <integer> format=int32 min=1 max=3600	# Controls how long we wait before forcing an eviction when the kubelet reports a resize as pending. Must be greater than 0 and less than or equal to 3600 (1 hour).
spec.applyPolicy.update.rolloutFallbackDelay <integer> format=int32 min=1 max=3600	# Controls how long we wait before falling back to a full rollout when evictions are blocked. Must be greater than 0 and less than or equal to 3600 (1 hour).
spec.applyPolicy.update.strategy <string> enum=Auto|Disabled|TriggerRollout	# Strategy defines the mode of the update policy.
spec.constraints <object>	# Constraints defines constraints that should always be respected.
spec.constraints.containers <[]object>	# Containers defines constraints for the containers.
spec.constraints.containers[].controlledResources <[]string>	# Specifies the resources for which recommendations will be computed. If not specified, it defaults to CPU and Memory. If an empty list is provided, no resource will be controlled (equivalent to Enabled=false).
spec.constraints.containers[].controlledValues <string> enum=RequestsAndLimits|RequestsOnly|CPURequestsRemoveLimitsMemoryRequestsAndLimits	# Specifies whether recommendations are made to Requests and Limits (RequestsAndLimits) or Requests only (RequestsOnly). The default is "RequestsAndLimits".
spec.constraints.containers[].enabled <boolean>	# Enabled, if false, allows one to disable resource autoscaling for the container. Defaults to true.
spec.constraints.containers[].maxAllowed <object (free-form)>	# MaxAllowed is the upper limit for the requests of the container.
spec.constraints.containers[].minAllowed <object (free-form)>	# MinAllowed is the lower limit for the requests of the container.
spec.constraints.containers[].name <string> (required)	# Name is the name of the container. Can be "*" to apply to all containers.
spec.constraints.containers[].requests <object>	# Requests defines the constraints for the requests of the container. WARNING: Deprecated
spec.constraints.containers[].requests.maxAllowed <object (free-form)>	# MaxAllowed is the upper limit for the requests of the container.
spec.constraints.containers[].requests.minAllowed <object (free-form)>	# MinAllowed is the lower limit for the requests of the container.
spec.constraints.maxReplicas <integer> format=int32 min=1	# MaxReplicas is the upper limit for the number of POD replicas. Needs to be >= minReplicas.
spec.constraints.minReplicas <integer> format=int32 min=1	# MinReplicas is the lower limit for the number of pod replicas. Needs to be >= 1. Defaults to 1.
spec.fallback <object> default={}	# Fallback defines how recommendations should be applied when in fallback mode.
spec.fallback.horizontal <object> default={}	# Horizontal configures the behavior during horizontal fallback mode.
spec.fallback.horizontal.direction <string> enum=ScaleUp|ScaleDown|All default="ScaleUp"	# Direction determines the direction that recommendations should be applied.
spec.fallback.horizontal.enabled <boolean> default=true	# Enabled determines whether recommendations should be applied by the controller:
spec.fallback.horizontal.objectives <[]object>	# Objectives are the objectives to reach and maintain for the target resource in fallback mode. If not set, the regular objectives will be used.
spec.fallback.horizontal.objectives[].containerResource <object>	# ContainerResource allows to set a container-level resource objective.
spec.fallback.horizontal.objectives[].containerResource.container <string> (required)	# Container is the name of the container.
spec.fallback.horizontal.objectives[].containerResource.name <string> (required) enum=cpu|memory	# Name is the name of the resource.
spec.fallback.horizontal.objectives[].containerResource.value <object> (required)	# Value is the value of the objective
spec.fallback.horizontal.objectives[].containerResource.value.absoluteValue <int-or-string>	# AbsoluteValue defines a target as an absolute value divided by the number of running pods. Use a plain number (e.g., "11" or "11.5"). Represented as a resource.Quantity to avoid floating point in CRDs.
spec.fallback.horizontal.objectives[].containerResource.value.type <string> (required) enum=Utilization|AbsoluteValue	# Type specifies how the value is expressed (possible values: Utilization, AbsoluteValue).
spec.fallback.horizontal.objectives[].containerResource.value.utilization <integer> format=int32 min=0 max=100	# Utilization defines a percentage of the target compared to requested workload
spec.fallback.horizontal.objectives[].customQuery <object>	# CustomQuery allows to set a controller-level objective.
spec.fallback.horizontal.objectives[].customQuery.request <object> (required)	# Request is the timeseries query to use for the objective.
spec.fallback.horizontal.objectives[].customQuery.request.formula <string>	# Formula to compute (optional).
spec.fallback.horizontal.objectives[].customQuery.request.queries <[]object> (required) min=1	# Queries is a list of timeseries queries to use for the objective. At least one query must be specified
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics <object>	# ApmMetrics is allows to query APM metrics.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.groupBy <[]string>	# GroupBy is the list of tags to group by.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.operationName <string>	# OperationName is the name of the operation to query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.queryFilter <string>	# QueryFilter is the filter to apply to the query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.resourceHash <string>	# ResourceHash is a fingerprint of the resource name that can be used to identify the resource instead of the resource name.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.resourceName <string>	# ResourceName is the name of the resource to query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.service <string>	# Service is the name of the service to query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.spanKind <string>	# SpanKind is the kind of span to query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].apmMetrics.stat <string> (required) enum=error_rate|errors|errors_per_second|hits|hits_per_second|apdex|latency_avg|latency_max|latency_p50|latency_p75|latency_p90|latency_p95|latency_p99|latency_p999|latency_distribution|total_time	# Stat defines the statistic to compute for the APM metrics query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].metrics <object>	# Metrics is a standard Datadog metrics query.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].metrics.query <string> (required) min=1	# Classic Datadog metrics query, e.g. "avg:system.cpu.user{*} by {env}".
spec.fallback.horizontal.objectives[].customQuery.request.queries[].name <string>	# Optional variable name ("a", "b", etc.) to reference in formulas.
spec.fallback.horizontal.objectives[].customQuery.request.queries[].source <string> (required) enum=Metrics|ApmMetrics	# Source defines the source of the timeseries query.
spec.fallback.horizontal.objectives[].customQuery.value <object> (required)	# Value is the value of the objective
spec.fallback.horizontal.objectives[].customQuery.value.absoluteValue <int-or-string>	# AbsoluteValue defines a target as an absolute value divided by the number of running pods. Use a plain number (e.g., "11" or "11.5"). Represented as a resource.Quantity to avoid floating point in CRDs.
spec.fallback.horizontal.objectives[].customQuery.value.type <string> (required) enum=Utilization|AbsoluteValue	# Type specifies how the value is expressed (possible values: Utilization, AbsoluteValue).
spec.fallback.horizontal.objectives[].customQuery.value.utilization <integer> format=int32 min=0 max=100	# Utilization defines a percentage of the target compared to requested workload
spec.fallback.horizontal.objectives[].customQuery.window <string> (required)	# Window is the time duration over which the query is computed. It should contain at least one full sample.
spec.fallback.horizontal.objectives[].podResource <object>	# PodResource allows to set a pod-level resource objective.
spec.fallback.horizontal.objectives[].podResource.name <string> (required) enum=cpu|memory	# Name is the name of the resource.
spec.fallback.horizontal.objectives[].podResource.value <object> (required)	# Value is the value of the objective.
spec.fallback.horizontal.objectives[].podResource.value.absoluteValue <int-or-string>	# AbsoluteValue defines a target as an absolute value divided by the number of running pods. Use a plain number (e.g., "11" or "11.5"). Represented as a resource.Quantity to avoid floating point in CRDs.
spec.fallback.horizontal.objectives[].podResource.value.type <string> (required) enum=Utilization|AbsoluteValue	# Type specifies how the value is expressed (possible values: Utilization, AbsoluteValue).
spec.fallback.horizontal.objectives[].podResource.value.utilization <integer> format=int32 min=0 max=100	# Utilization defines a percentage of the target compared to requested workload
spec.fallback.horizontal.objectives[].type <string> (required) enum=PodResource|ContainerResource|CustomQuery	# Type sets the type of the objective.
spec.fallback.horizontal.triggers <object> default={}	# Triggers defines the triggers that will generate recommendations.
spec.fallback.horizontal.triggers.staleRecommendationThresholdSeconds <integer> default=600 format=int32 min=100 max=3600	# StaleRecommendationThresholdSeconds defines the time window the controller will wait after detecting an error before applying recommendations.
spec.objectives <[]object> min=1	# Objectives are the objectives to reach and maintain for the target resource. Default to a single objective to maintain 80% POD CPU utilization.
spec.objectives[].containerResource <object>	# ContainerResource allows to set a container-level resource objective.
spec.objectives[].containerResource.container <string> (required)	# Container is the name of the container.
spec.objectives[].containerResource.name <string> (required) enum=cpu|memory	# Name is the name of the resource.
spec.objectives[].containerResource.value <object> (required)	# Value is the value of the objective
spec.objectives[].containerResource.value.absoluteValue <int-or-string>	# AbsoluteValue defines a target as an absolute value divided by the number of running pods. Use a plain number (e.g., "11" or "11.5"). Represented as a resource.Quantity to avoid floating point in CRDs.
spec.objectives[].containerResource.value.type <string> (required) enum=Utilization|AbsoluteValue	# Type specifies how the value is expressed (possible values: Utilization, AbsoluteValue).
spec.objectives[].containerResource.value.utilization <integer> format=int32 min=0 max=100	# Utilization defines a percentage of the target compared to requested workload
spec.objectives[].customQuery <object>	# CustomQuery allows to set a controller-level objective.
spec.objectives[].customQuery.request <object> (required)	# Request is the timeseries query to use for the objective.
spec.objectives[].customQuery.request.formula <string>	# Formula to compute (optional).
spec.objectives[].customQuery.request.queries <[]object> (required) min=1	# Queries is a list of timeseries queries to use for the objective. At least one query must be specified
spec.objectives[].customQuery.request.queries[].apmMetrics <object>	# ApmMetrics is allows to query APM metrics.
spec.objectives[].customQuery.request.queries[].apmMetrics.groupBy <[]string>	# GroupBy is the list of tags to group by.
spec.objectives[].customQuery.request.queries[].apmMetrics.operationName <string>	# OperationName is the name of the operation to query.
spec.objectives[].customQuery.request.queries[].apmMetrics.queryFilter <string>	# QueryFilter is the filter to apply to the query.
spec.objectives[].customQuery.request.queries[].apmMetrics.resourceHash <string>	# ResourceHash is a fingerprint of the resource name that can be used to identify the resource instead of the resource name.
spec.objectives[].customQuery.request.queries[].apmMetrics.resourceName <string>	# ResourceName is the name of the resource to query.
spec.objectives[].customQuery.request.queries[].apmMetrics.service <string>	# Service is the name of the service to query.
spec.objectives[].customQuery.request.queries[].apmMetrics.spanKind <string>	# SpanKind is the kind of span to query.
spec.objectives[].customQuery.request.queries[].apmMetrics.stat <string> (required) enum=error_rate|errors|errors_per_second|hits|hits_per_second|apdex|latency_avg|latency_max|latency_p50|latency_p75|latency_p90|latency_p95|latency_p99|latency_p999|latency_distribution|total_time	# Stat defines the statistic to compute for the APM metrics query.
spec.objectives[].customQuery.request.queries[].metrics <object>	# Metrics is a standard Datadog metrics query.
spec.objectives[].customQuery.request.queries[].metrics.query <string> (required) min=1	# Classic Datadog metrics query, e.g. "avg:system.cpu.user{*} by {env}".
spec.objectives[].customQuery.request.queries[].name <string>	# Optional variable name ("a", "b", etc.) to reference in formulas.
spec.objectives[].customQuery.request.queries[].source <string> (required) enum=Metrics|ApmMetrics	# Source defines the source of the timeseries query.
spec.objectives[].customQuery.value <object> (required)	# Value is the value of the objective
spec.objectives[].customQuery.value.absoluteValue <int-or-string>	# AbsoluteValue defines a target as an absolute value divided by the number of running pods. Use a plain number (e.g., "11" or "11.5"). Represented as a resource.Quantity to avoid floating point in CRDs.
spec.objectives[].customQuery.value.type <string> (required) enum=Utilization|AbsoluteValue	# Type specifies how the value is expressed (possible values: Utilization, AbsoluteValue).
spec.objectives[].customQuery.value.utilization <integer> format=int32 min=0 max=100	# Utilization defines a percentage of the target compared to requested workload
spec.objectives[].customQuery.window <string> (required)	# Window is the time duration over which the query is computed. It should contain at least one full sample.
spec.objectives[].podResource <object>	# PodResource allows to set a pod-level resource objective.
spec.objectives[].podResource.name <string> (required) enum=cpu|memory	# Name is the name of the resource.
spec.objectives[].podResource.value <object> (required)	# Value is the value of the objective.
spec.objectives[].podResource.value.absoluteValue <int-or-string>	# AbsoluteValue defines a target as an absolute value divided by the number of running pods. Use a plain number (e.g., "11" or "11.5"). Represented as a resource.Quantity to avoid floating point in CRDs.
spec.objectives[].podResource.value.type <string> (required) enum=Utilization|AbsoluteValue	# Type specifies how the value is expressed (possible values: Utilization, AbsoluteValue).
spec.objectives[].podResource.value.utilization <integer> format=int32 min=0 max=100	# Utilization defines a percentage of the target compared to requested workload
spec.objectives[].type <string> (required) enum=PodResource|ContainerResource|CustomQuery	# Type sets the type of the objective.
spec.options <object>	# Options defines optional behavior modifications for the autoscaler.
spec.options.burstable <boolean>	# Burstable, if true, removes CPU limits from containers while keeping CPU request recommendations, granting the pod a Burstable QoS class and allowing it to consume idle node CPU capacity beyond its requests. If not set, the default value is determined by the Cluster Agent setting autoscaling.workload.options.burstable.
spec.options.outOfMemory <object>	# OutOfMemory configures behavior when OOM events are detected.
spec.options.outOfMemory.bumpUpRatio <int-or-string>	# BumpUpRatio defines the ratio to multiply memory by when OOM is detected. For example, "1.2" means increase memory by 20%. Represented as a resource.Quantity to avoid floating point in CRDs.
spec.owner <string> (required) enum=Local|Remote	# Owner defines the source of truth for this object (local or remote). Value must be set when a DatadogPodAutoscaler object is created.
spec.remoteVersion <integer> format=int64	# RemoteVersion is the version of the .Spec currently stored in this object. This is only set if the owner is Remote.
spec.targetRef <object> (required)	# TargetRef is the reference to the resource to scale.
spec.targetRef.apiVersion <string>	# apiVersion is the API version of the referent
spec.targetRef.kind <string> (required)	# kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.targetRef.name <string> (required)	# name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status <object>	# DatadogPodAutoscalerStatus defines the observed state of DatadogPodAutoscaler
status.conditions <[]object>	# Conditions describe the current state of the DatadogPodAutoscaler operations.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# DatadogPodAutoscalerConditionType is the type of DatadogPodAutoscaler condition.
status.currentReplicas <integer> format=int32	# CurrentReplicas is the current number of pods for the targetRef observed by the controller.
status.horizontal <object>	# Horizontal is the status of the horizontal scaling, if activated.
status.horizontal.lastActions <[]object>	# LastActions are the last successful actions done by the controller
status.horizontal.lastActions[].limitedReason <string>	# LimitedReason is the reason why the action was limited (that is ToReplicas != RecommendedReplicas)
status.horizontal.lastActions[].recommendedReplicas <integer> format=int32	# RecommendedReplicas is the original number of replicas recommended by Datadog
status.horizontal.lastActions[].replicas <integer> (required) format=int32	# FromReplicas is the number of replicas before the action
status.horizontal.lastActions[].time <string> (required) format=date-time	# Time is the timestamp of the action
status.horizontal.lastActions[].toReplicas <integer> (required) format=int32	# ToReplicas is the effective number of replicas after the action
status.horizontal.lastRecommendations <[]object>	# LastRecommendations stores the most recent recommendations
status.horizontal.lastRecommendations[].desiredReplicas <integer> (required) format=int32	# Replicas is the recommended number of replicas for the workload
status.horizontal.lastRecommendations[].generatedAt <string> format=date-time	# GeneratedAt is the timestamp at which the recommendation was generated
status.horizontal.lastRecommendations[].source <string>	# Source is the source of the value used to scale the target workload
status.horizontal.target <object>	# Target is the current target of the horizontal scaling
status.horizontal.target.desiredReplicas <integer> (required) format=int32	# Replicas is the recommended number of replicas for the workload
status.horizontal.target.generatedAt <string> format=date-time	# GeneratedAt is the timestamp at which the recommendation was generated
status.horizontal.target.source <string>	# Source is the source of the value used to scale the target workload
status.options <object>	# Options reflects the effective options applied by the autoscaler.
status.options.burstable <boolean>	# Burstable is the effective value of the burstable setting applied by the autoscaler. When not set in the spec, this reflects the default determined by the Cluster Agent setting autoscaling.workload.options.burstable.
status.vertical <object>	# Vertical is the status of the vertical scaling, if activated.
status.vertical.lastAction <object>	# LastAction is the last successful action done by the controller
status.vertical.lastAction.time <string> (required) format=date-time	# Time is the timestamp of the action
status.vertical.lastAction.type <string> (required)	# Type is the type of action
status.vertical.lastAction.version <string> (required)	# Version is the version of the recommendation used for the action
status.vertical.target <object>	# Target is the current target of the vertical scaling
status.vertical.target.desiredResources <[]object> (required)	# DesiredResources is the desired resources for containers
status.vertical.target.desiredResources[].limits <object (free-form)>	# Limits describes the maximum amount of compute resources allowed.
status.vertical.target.desiredResources[].name <string> (required)	# Name is the name of the container
status.vertical.target.desiredResources[].requests <object (free-form)>	# Requests describes the requested amount of compute resources.
status.vertical.target.evicted <integer> format=int32	# Evicted is the number of pods evicted as an in-place resize fallback during the current recommendation cycle. Resets when the recommendation changes.
status.vertical.target.generatedAt <string> format=date-time	# GeneratedAt is the timestamp at which the recommendation was generated
status.vertical.target.podCPURequest <int-or-string> (required)	# PodCPURequest is the sum of CPU requests for all containers (used for display)
status.vertical.target.podMemoryRequest <int-or-string> (required)	# PodMemoryRequest is the sum of memory requests for all containers (used for display)
status.vertical.target.scaled <integer> format=int32	# Scaled is the current number of pods having desired resources
status.vertical.target.source <string> (required)	# Source is the source of the value used to scale the target resource
status.vertical.target.version <string> (required)	# Version is the current version of the received recommendation
