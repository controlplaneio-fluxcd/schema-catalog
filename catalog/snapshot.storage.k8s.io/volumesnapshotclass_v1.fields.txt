# schema source: CSI External Snapshotter v8.6.0 https://github.com/kubernetes-csi/external-snapshotter
apiVersion <string> enum=snapshot.storage.k8s.io/v1
kind <string> enum=VolumeSnapshotClass (cluster-scoped)
metadata.name <string> (required)
deletionPolicy <string> (required) enum=Delete|Retain	# deletionPolicy determines whether a VolumeSnapshotContent created through the VolumeSnapshotClass should be deleted when its bound VolumeSnapshot is deleted. Supported values are "Retain" and "Delete". "Retain" means that the VolumeSnapshotContent and its physical snapshot on underlying storage system are kept. "Delete" means that the VolumeSnapshotContent and its physical snapshot on underlying storage system are deleted. Required.
driver <string> (required)	# driver is the name of the storage driver that handles this VolumeSnapshotClass. Required.
parameters <map[string]string>	# parameters is a key-value map with storage driver specific parameters for creating snapshots. These values are opaque to Kubernetes.
