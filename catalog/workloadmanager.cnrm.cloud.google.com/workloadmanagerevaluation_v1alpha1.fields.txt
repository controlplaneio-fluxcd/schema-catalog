# schema source: GCP Config Connector v1.155.1 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=workloadmanager.cnrm.cloud.google.com/v1alpha1
kind <string> enum=WorkloadManagerEvaluation
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# WorkloadManagerEvaluationSpec defines the desired state of WorkloadManagerEvaluation
spec.bigQueryDestination <object>	# Optional. The BigQuery destination for detailed evaluation results. If this field is specified, the results of each evaluation execution are exported to BigQuery.
spec.bigQueryDestination.createNewResultsTable <boolean>	# Optional. Determines if a new results table will be created when an Execution is created.
spec.bigQueryDestination.destinationDataset <string>	# Optional. Destination dataset to save evaluation results.
spec.customRulesBucket <string>	# The Cloud Storage bucket name for custom rules.
spec.description <string>	# Description of the Evaluation.
spec.evaluationType <string>	# Evaluation type.
spec.kmsKeyRef <object>	# Optional. Immutable. Customer-managed encryption key name.
spec.kmsKeyRef.external <string>	# A reference to an externally managed KMSCryptoKey. Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
spec.kmsKeyRef.name <string>	# The `name` of a `KMSCryptoKey` resource.
spec.kmsKeyRef.namespace <string>	# The `namespace` of a `KMSCryptoKey` resource.
spec.labels <map[string]string>	# Labels as key value pairs.
spec.location <string> (required)	# The location of this resource.
spec.projectRef <object> (required)	# The project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceFilter <object>	# Resource filter for an evaluation defining the scope of resources to be evaluated.
spec.resourceFilter.gceInstanceFilter <object>	# Filter compute engine resources.
spec.resourceFilter.gceInstanceFilter.serviceAccounts <[]string>	# If non-empty, only Compute Engine instances associated with at least one of the provided service accounts will be included in the evaluation.
spec.resourceFilter.inclusionLabels <map[string]string>	# Labels to filter resources by. Each key-value pair in the map must exist on the resource for it to be included.
spec.resourceFilter.resourceIDPatterns <[]string>	# Pattern to filter resources by their id.
spec.resourceFilter.scopeRefs <[]object>	# The scopes of evaluation resource.
spec.resourceFilter.scopeRefs[].folderRef <object>	# A folder to scope the evaluation to.
spec.resourceFilter.scopeRefs[].folderRef.external <string>	# The 'name' field of a folder, when not managed by Config Connector. This field must be set when 'name' field is not set.
spec.resourceFilter.scopeRefs[].folderRef.name <string>	# The 'name' field of a 'Folder' resource. This field must be set when 'external' field is not set.
spec.resourceFilter.scopeRefs[].folderRef.namespace <string>	# The 'namespace' field of a 'Folder' resource. If unset, the namespace is defaulted to the namespace of the referencer resource.
spec.resourceFilter.scopeRefs[].organizationRef <object>	# An organization to scope the evaluation to.
spec.resourceFilter.scopeRefs[].organizationRef.external <string> (required)	# The 'name' field of an organization, when not managed by Config Connector.
spec.resourceFilter.scopeRefs[].projectRef <object>	# A project to scope the evaluation to.
spec.resourceFilter.scopeRefs[].projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.resourceFilter.scopeRefs[].projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.resourceFilter.scopeRefs[].projectRef.name <string>	# The `name` field of a `Project` resource.
spec.resourceFilter.scopeRefs[].projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The WorkloadManagerEvaluation name. If not given, the metadata.name will be used.
spec.ruleNames <[]string>	# The names of the rules used for this evaluation.
spec.schedule <string>	# Crontab format schedule for scheduled evaluation, currently only supports the following fixed schedules: * `0 */1 * * *` # Hourly * `0 */6 * * *` # Every 6 hours * `0 */12 * * *` # Every 12 hours * `0 0 */1 * *` # Daily * `0 0 */7 * *` # Weekly * `0 0 */14 * *` # Every 14 days * `0 0 1 */1 *` # Monthly
status <object>	# WorkloadManagerEvaluationStatus defines the config connector machine state of WorkloadManagerEvaluation
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the WorkloadManagerEvaluation resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.createTime <string>	# Output only. [Output only] Create time stamp.
status.observedState.resourceStatus <object>	# Output only. [Output only] The current lifecycle state of the evaluation resource.
status.observedState.resourceStatus.state <string>	# State of the Evaluation resource.
status.observedState.updateTime <string>	# Output only. [Output only] Update time stamp.
