# schema source: Kyverno v1.18.1 https://github.com/kyverno/kyverno
apiVersion <string> enum=policies.kyverno.io/v1beta1
kind <string> enum=NamespacedImageValidatingPolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
spec.attestations <[]object>	# Attestations provides a list of image metadata to verify
spec.attestations[].intoto <object>	# InToto defines the details of attestation attached using intoto format
spec.attestations[].intoto.type <string> (required)	# Type defines the type of attestation contained within the statement.
spec.attestations[].name <string> (required)	# Name is the name for this attestation. It is used to refer to the attestation in verification
spec.attestations[].referrer <object>	# Referrer defines the details of attestation attached using OCI 1.1 format
spec.attestations[].referrer.type <string> (required)	# Type defines the type of attestation attached to the image.
spec.attestors <[]object> (required)	# Attestors provides a list of trusted authorities.
spec.attestors[].cosign <object>	# Cosign defines attestor configuration for Cosign based signatures
spec.attestors[].cosign.annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
spec.attestors[].cosign.certificate <object>	# Certificate defines the configuration for local signature verification
spec.attestors[].cosign.certificate.cert <object>	# Certificate is the to the public certificate for local signature verification.
spec.attestors[].cosign.certificate.cert.expression <string>	# Expression defines the a CEL expression input.
spec.attestors[].cosign.certificate.cert.value <string>	# Value defines the raw string input.
spec.attestors[].cosign.certificate.certChain <object>	# CertificateChain is the list of CA certificates in PEM format which will be needed when building the certificate chain for the signing certificate. Must start with the parent intermediate CA certificate of the signing certificate and end with the root certificate
spec.attestors[].cosign.certificate.certChain.expression <string>	# Expression defines the a CEL expression input.
spec.attestors[].cosign.certificate.certChain.value <string>	# Value defines the raw string input.
spec.attestors[].cosign.ctlog <object>	# CTLog sets the configuration to verify the authority against a Rekor instance.
spec.attestors[].cosign.ctlog.ctLogPubKey <string>	# CTLogPubKey, if set, is used to validate SCTs against a custom source.
spec.attestors[].cosign.ctlog.insecureIgnoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
spec.attestors[].cosign.ctlog.insecureIgnoreTlog <boolean>	# InsecureIgnoreTlog skips transparency log verification.
spec.attestors[].cosign.ctlog.rekorPubKey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
spec.attestors[].cosign.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
spec.attestors[].cosign.ctlog.url <string>	# URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
spec.attestors[].cosign.key <object>	# Key defines the type of key to validate the image.
spec.attestors[].cosign.key.data <string>	# Data contains the inline public key
spec.attestors[].cosign.key.expression <string>	# Expression is a Expression expression that returns the public key.
spec.attestors[].cosign.key.hashAlgorithm <string>	# HashAlgorithm specifues signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512. Defaults to sha256.
spec.attestors[].cosign.key.kms <string>	# KMS contains the KMS url of the public key Supported formats differ based on the KMS system used.
spec.attestors[].cosign.keyless <object>	# Keyless sets the configuration to verify the authority against a Fulcio instance.
spec.attestors[].cosign.keyless.identities <[]object> (required)	# Identities sets a list of identities.
spec.attestors[].cosign.keyless.identities[].issuer <string>	# Issuer defines the issuer for this identity.
spec.attestors[].cosign.keyless.identities[].issuerRegExp <string>	# IssuerRegExp specifies a regular expression to match the issuer for this identity.
spec.attestors[].cosign.keyless.identities[].subject <string>	# Subject defines the subject for this identity.
spec.attestors[].cosign.keyless.identities[].subjectRegExp <string>	# SubjectRegExp specifies a regular expression to match the subject for this identity.
spec.attestors[].cosign.keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
spec.attestors[].cosign.source <object>	# Sources sets the configuration to specify the sources from where to consume the signature and attestations.
spec.attestors[].cosign.source.PullSecrets <[]object>	# SignaturePullSecrets is an optional list of references to secrets in the same namespace as the deploying resource for pulling any of the signatures used by this Source.
spec.attestors[].cosign.source.PullSecrets[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.attestors[].cosign.source.repository <string>	# Repository defines the location from where to pull the signature / attestations.
spec.attestors[].cosign.source.tagPrefix <string>	# TagPrefix is an optional prefix that signature and attestations have. This is the 'tag based discovery' and in the future once references are fully supported that should likely be the preferred way to handle these.
spec.attestors[].cosign.tuf <object>	# TUF defines the configuration to fetch sigstore root
spec.attestors[].cosign.tuf.mirror <string>	# Mirror is the base URL of Sigstore TUF repository
spec.attestors[].cosign.tuf.root <object>	# Root defines the path or data of the trusted root
spec.attestors[].cosign.tuf.root.data <string>	# Data is the base64 encoded TUF root
spec.attestors[].cosign.tuf.root.path <string>	# Path is the URL or File location of the TUF root
spec.attestors[].name <string> (required)	# Name is the name for this attestor. It is used to refer to the attestor in verification
spec.attestors[].notary <object>	# Notary defines attestor configuration for Notary based signatures
spec.attestors[].notary.certs <object>	# Certs define the cert chain for Notary signature verification
spec.attestors[].notary.certs.expression <string>	# Expression defines the a CEL expression input.
spec.attestors[].notary.certs.value <string>	# Value defines the raw string input.
spec.attestors[].notary.tsaCerts <object>	# TSACerts define the cert chain for verifying timestamps of notary signature
spec.attestors[].notary.tsaCerts.expression <string>	# Expression defines the a CEL expression input.
spec.attestors[].notary.tsaCerts.value <string>	# Value defines the raw string input.
spec.auditAnnotations <[]object>	# auditAnnotations contains CEL expressions which are used to produce audit annotations for the audit event of the API request. validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is required.
spec.auditAnnotations[].key <string> (required)	# key specifies the audit annotation key. The audit annotation keys of a ValidatingAdmissionPolicy must be unique. The key must be a qualified name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length. The key is combined with the resource name of the ValidatingAdmissionPolicy to construct an audit annotation key: "{ValidatingAdmissionPolicy name}/{key}". If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy and the same audit annotation key, the annotation key will be identical. In this case, the first annotation written with the key will be included in the audit event and all subsequent annotations with the same key will be discarded. Required.
spec.auditAnnotations[].valueExpression <string> (required)	# valueExpression represents the expression which is evaluated by CEL to produce an audit annotation value. The expression must evaluate to either a string or null value. If the expression evaluates to a string, the audit annotation is included with the string value. If the expression evaluates to null or empty string the audit annotation will be omitted. The valueExpression may be no longer than 5kb in length. If the result of the valueExpression is more than 10kb in length, it will be truncated to 10kb. If multiple ValidatingAdmissionPolicyBinding resources match an API request, then the valueExpression will be evaluated for each binding. All unique values produced by the valueExpressions will be joined together in a comma-separated list. Required.
spec.autogen <object>	# AutogenConfiguration defines the configuration for the generation controller.
spec.autogen.podControllers <object>	# PodControllers specifies whether to generate a pod controllers rules.
spec.autogen.podControllers.controllers <[]string>
spec.credentials <object>	# Credentials provides credentials that will be used for authentication with registry.
spec.credentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
spec.credentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
spec.credentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets must live in the Kyverno namespace.
spec.evaluation <object>	# EvaluationConfiguration defines the configuration for the policy evaluation.
spec.evaluation.admission <object>	# Admission controls policy evaluation during admission.
spec.evaluation.admission.enabled <boolean> default=true	# Enabled controls if rules are applied during admission. Optional. Default value is "true".
spec.evaluation.background <object>	# Background controls policy evaluation during background scan.
spec.evaluation.background.enabled <boolean> default=true	# Enabled controls if rules are applied to existing resources during a background scan. Optional. Default value is "true". The value must be set to "false" if the policy rule uses variables that are only available in the admission review request (e.g. user name).
spec.evaluation.mode <string>	# Mode is the mode of policy evaluation. Allowed values are "Kubernetes" or "JSON". Optional. Default value is "Kubernetes".
spec.failurePolicy <string> enum=Ignore|Fail	# FailurePolicy defines how to handle failures for the admission policy. Failures can occur from CEL expression parse errors, type check errors, runtime errors and invalid or mis-configured policy definitions or bindings.
spec.images <[]object>	# ImageExtractors is a list of CEL expression to extract images from the resource
spec.images[].expression <string> (required)	# Expression defines CEL expression to extract images from the resource.
spec.images[].name <string> (required)	# Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
spec.matchConditions <[]object>	# MatchConditions is a list of conditions that must be met for a request to be validated. Match conditions filter requests that have already been matched by the rules, namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests. There are a maximum of 64 match conditions allowed.
spec.matchConditions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables: 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject' - The existing object. The value is null for CREATE requests. 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/ Required.
spec.matchConditions[].name <string> (required)	# Name is an identifier for this match condition, used for strategic merging of MatchConditions, as well as providing an identifier for logging purposes. A good name should be descriptive of the associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and must start and end with an alphanumeric character (e.g. 'MyName', or 'my.name', or '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName') Required.
spec.matchConstraints <object>	# MatchConstraints specifies what resources this policy is designed to validate.
spec.matchConstraints.excludeResourceRules <[]object>	# ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about. The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
spec.matchConstraints.excludeResourceRules[].apiGroups <[]string>	# APIGroups is the API groups the resources belong to. '*' is all groups. If '*' is present, the length of the slice must be one. Required.
spec.matchConstraints.excludeResourceRules[].apiVersions <[]string>	# APIVersions is the API versions the resources belong to. '*' is all versions. If '*' is present, the length of the slice must be one. Required.
spec.matchConstraints.excludeResourceRules[].operations <[]string>	# Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or * for all of those operations and any future admission operations that are added. If '*' is present, the length of the slice must be one. Required.
spec.matchConstraints.excludeResourceRules[].resourceNames <[]string>	# ResourceNames is an optional white list of names that the rule applies to. An empty set means that everything is allowed.
spec.matchConstraints.excludeResourceRules[].resources <[]string>	# Resources is a list of resources this rule applies to. For example: 'pods' means pods. 'pods/log' means the log subresource of pods. '*' means all resources, but not subresources. 'pods/*' means all subresources of pods. '*/scale' means all scale subresources. '*/*' means all resources and their subresources. If wildcard is present, the validation rule will ensure resources do not overlap with each other. Depending on the enclosing object, subresources might not be allowed. Required.
spec.matchConstraints.excludeResourceRules[].scope <string>	# scope specifies the scope of this rule. Valid values are "Cluster", "Namespaced", and "*" "Cluster" means that only cluster-scoped resources will match this rule. Namespace API objects are cluster-scoped. "Namespaced" means that only namespaced resources will match this rule. "*" means that there are no scope restrictions. Subresources match the scope of their parent resource. Default is "*".
spec.matchConstraints.matchPolicy <string>	# matchPolicy defines how the "MatchResources" list is used to match incoming requests. Allowed values are "Exact" or "Equivalent". - Exact: match a request only if it exactly matches a specified rule. For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1, but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`, a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy. - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version. For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1, and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`, a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy. Defaults to "Equivalent"
spec.matchConstraints.namespaceSelector <object>	# NamespaceSelector decides whether to run the admission control policy on an object based on whether the namespace for that object matches the selector. If the object itself is a namespace, the matching is performed on object.metadata.labels. If the object is another cluster scoped resource, it never skips the policy. For example, to run the webhook on any objects whose namespace is not associated with "runlevel" of "0" or "1"; you will set the selector as follows: "namespaceSelector": { "matchExpressions": [ { "key": "runlevel", "operator": "NotIn", "values": [ "0", "1" ] } ] } If instead you want to only run the policy on any objects whose namespace is associated with the "environment" of "prod" or "staging"; you will set the selector as follows: "namespaceSelector": { "matchExpressions": [ { "key": "environment", "operator": "In", "values": [ "prod", "staging" ] } ] } See https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/ for more examples of label selectors. Default to the empty LabelSelector, which matches everything.
spec.matchConstraints.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.matchConstraints.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.matchConstraints.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.matchConstraints.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.matchConstraints.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.matchConstraints.objectSelector <object>	# ObjectSelector decides whether to run the validation based on if the object has matching labels. objectSelector is evaluated against both the oldObject and newObject that would be sent to the cel validation, and is considered to match if either object matches the selector. A null object (oldObject in the case of create, or newObject in the case of delete) or an object that cannot have labels (like a DeploymentRollback or a PodProxyOptions object) is not considered to match. Use the object selector only if the webhook is opt-in, because end users may skip the admission webhook by setting the labels. Default to the empty LabelSelector, which matches everything.
spec.matchConstraints.objectSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.matchConstraints.objectSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.matchConstraints.objectSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.matchConstraints.objectSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.matchConstraints.objectSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.matchConstraints.resourceRules <[]object>	# ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches. The policy cares about an operation if it matches _any_ Rule.
spec.matchConstraints.resourceRules[].apiGroups <[]string>	# APIGroups is the API groups the resources belong to. '*' is all groups. If '*' is present, the length of the slice must be one. Required.
spec.matchConstraints.resourceRules[].apiVersions <[]string>	# APIVersions is the API versions the resources belong to. '*' is all versions. If '*' is present, the length of the slice must be one. Required.
spec.matchConstraints.resourceRules[].operations <[]string>	# Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or * for all of those operations and any future admission operations that are added. If '*' is present, the length of the slice must be one. Required.
spec.matchConstraints.resourceRules[].resourceNames <[]string>	# ResourceNames is an optional white list of names that the rule applies to. An empty set means that everything is allowed.
spec.matchConstraints.resourceRules[].resources <[]string>	# Resources is a list of resources this rule applies to. For example: 'pods' means pods. 'pods/log' means the log subresource of pods. '*' means all resources, but not subresources. 'pods/*' means all subresources of pods. '*/scale' means all scale subresources. '*/*' means all resources and their subresources. If wildcard is present, the validation rule will ensure resources do not overlap with each other. Depending on the enclosing object, subresources might not be allowed. Required.
spec.matchConstraints.resourceRules[].scope <string>	# scope specifies the scope of this rule. Valid values are "Cluster", "Namespaced", and "*" "Cluster" means that only cluster-scoped resources will match this rule. Namespace API objects are cluster-scoped. "Namespaced" means that only namespaced resources will match this rule. "*" means that there are no scope restrictions. Subresources match the scope of their parent resource. Default is "*".
spec.matchImageReferences <[]object>	# MatchImageReferences is a list of Glob and CELExpressions to match images. Any image that matches one of the rules is considered for validation Any image that does not match a rule is skipped, even when they are passed as arguments to image verification functions
spec.matchImageReferences[].expression <string>	# Expression defines CEL Expressions for matching images
spec.matchImageReferences[].glob <string>	# Glob defines a globbing pattern for matching images
spec.validationActions <[]string>	# ValidationAction specifies the action to be taken when the matched resource violates the policy. If a validation evaluates to false it is always enforced according to these actions. Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according to these actions only if the FailurePolicy is set to Fail, otherwise the failures are ignored. This includes compilation errors, runtime errors and misconfigurations of the policy. validationActions is declared as a set of action values. Order does not matter. validationActions may not contain duplicates of the same action. The supported actions values are: "Deny" specifies that a validation failure results in a denied request. "Warn" specifies that a validation failure is reported to the request client in HTTP Warning headers, with a warning code of 299. Warnings can be sent both for allowed or denied admission responses. "Audit" specifies that a validation failure is recorded in the created reports. Clients should expect to handle additional values by ignoring any values not recognized. "Deny" and "Warn" may not be used together since this combination needlessly duplicates the validation failure both in the API response body and the HTTP warning headers. Required.
spec.validationConfigurations <object> default={}	# ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
spec.validationConfigurations.mutateDigest <boolean> default=true	# MutateDigest enables replacement of image tags with digests. Defaults to true.
spec.validationConfigurations.required <boolean> default=true	# Required validates that images are verified, i.e., have passed a signature or attestation check.
spec.validationConfigurations.verifyDigest <boolean> default=true	# VerifyDigest validates that images have a digest.
spec.validations <[]object> (required)	# Validations contain CEL expressions which is used to apply the image validation checks.
spec.validations[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. ref: https://github.com/google/cel-spec CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables: - 'object' - The object from the incoming request. The value is null for DELETE requests. - 'oldObject' - The existing object. The value is null for CREATE requests. - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)). - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind. - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources. - 'variables' - Map of composited variables, from its name to its lazily evaluated value. For example, a variable named 'foo' can be accessed as 'variables.foo'. - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the object. No other metadata properties are accessible. Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible. Accessible property names are escaped according to the following rules when accessed in the expression: - '__' escapes to '__underscores__' - '.' escapes to '__dot__' - '-' escapes to '__dash__' - '/' escapes to '__slash__' - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are: "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if", "import", "let", "loop", "package", "namespace", "return". Examples: - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"} - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"} - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"} Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1]. Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type: - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and non-intersecting elements in `Y` are appended, retaining their partial order. - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with non-intersecting keys are appended, retaining their partial order. Required.
spec.validations[].message <string>	# Message represents the message displayed when validation fails. The message is required if the Expression contains line breaks. The message must not contain line breaks. If unset, the message is "failed rule: {Rule}". e.g. "must be a URL with the host matching spec.host" If the Expression contains line breaks. Message is required. The message must not contain line breaks. If unset, the message is "failed Expression: {Expression}".
spec.validations[].messageExpression <string>	# messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails. Since messageExpression is used as a failure message, it must evaluate to a string. If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails. If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged. messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'. Example: "object.x must be less than max ("+string(params.max)+")"
spec.validations[].reason <string>	# Reason represents a machine-readable description of why this validation failed. If this is the first validation in the list to fail, this reason, as well as the corresponding HTTP response code, are used in the HTTP response to the client. The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge". If not set, StatusReasonInvalid is used in the response to the client.
spec.variables <[]object>	# Variables contain definitions of variables that can be used in composition of other expressions. Each variable is defined as a named CEL expression.
spec.variables[].expression <string> (required)	# Expression is the expression that will be evaluated as the value of the variable. The CEL expression has access to the same identifiers as the CEL expressions in Validation.
spec.variables[].name <string> (required)	# Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables. The variable can be accessed in other expressions through `variables` For example, if name is "foo", the variable will be available as `variables.foo`
spec.webhookConfiguration <object>	# WebhookConfiguration defines the configuration for the webhook.
spec.webhookConfiguration.timeoutSeconds <integer> format=int32	# TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy. After the configured time expires, the admission request may fail, or may simply ignore the policy results, based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
status <object>	# Status contains policy runtime data.
status.autogen <object>
status.autogen.configs <map[string]object>
status.autogen.configs.<key>.spec <object> (required)	# ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
status.autogen.configs.<key>.spec.attestations <[]object>	# Attestations provides a list of image metadata to verify
status.autogen.configs.<key>.spec.attestations[].intoto <object>	# InToto defines the details of attestation attached using intoto format
status.autogen.configs.<key>.spec.attestations[].intoto.type <string> (required)	# Type defines the type of attestation contained within the statement.
status.autogen.configs.<key>.spec.attestations[].name <string> (required)	# Name is the name for this attestation. It is used to refer to the attestation in verification
status.autogen.configs.<key>.spec.attestations[].referrer <object>	# Referrer defines the details of attestation attached using OCI 1.1 format
status.autogen.configs.<key>.spec.attestations[].referrer.type <string> (required)	# Type defines the type of attestation attached to the image.
status.autogen.configs.<key>.spec.attestors <[]object> (required)	# Attestors provides a list of trusted authorities.
status.autogen.configs.<key>.spec.attestors[].cosign <object>	# Cosign defines attestor configuration for Cosign based signatures
status.autogen.configs.<key>.spec.attestors[].cosign.annotations <map[string]string>	# Annotations are used for image verification. Every specified key-value pair must exist and match in the verified payload. The payload may contain other key-value pairs.
status.autogen.configs.<key>.spec.attestors[].cosign.certificate <object>	# Certificate defines the configuration for local signature verification
status.autogen.configs.<key>.spec.attestors[].cosign.certificate.cert <object>	# Certificate is the to the public certificate for local signature verification.
status.autogen.configs.<key>.spec.attestors[].cosign.certificate.cert.expression <string>	# Expression defines the a CEL expression input.
status.autogen.configs.<key>.spec.attestors[].cosign.certificate.cert.value <string>	# Value defines the raw string input.
status.autogen.configs.<key>.spec.attestors[].cosign.certificate.certChain <object>	# CertificateChain is the list of CA certificates in PEM format which will be needed when building the certificate chain for the signing certificate. Must start with the parent intermediate CA certificate of the signing certificate and end with the root certificate
status.autogen.configs.<key>.spec.attestors[].cosign.certificate.certChain.expression <string>	# Expression defines the a CEL expression input.
status.autogen.configs.<key>.spec.attestors[].cosign.certificate.certChain.value <string>	# Value defines the raw string input.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog <object>	# CTLog sets the configuration to verify the authority against a Rekor instance.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog.ctLogPubKey <string>	# CTLogPubKey, if set, is used to validate SCTs against a custom source.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog.insecureIgnoreSCT <boolean>	# IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate timestamp. Default is false. Set to true if this was opted out during signing.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog.insecureIgnoreTlog <boolean>	# InsecureIgnoreTlog skips transparency log verification.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog.rekorPubKey <string>	# RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor. If set, this will be used to validate transparency log signatures from a custom Rekor.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog.tsaCertChain <string>	# TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must contain the root CA certificate. Optionally may contain intermediate CA certificates, and may contain the leaf TSA certificate if not present in the timestamurce.
status.autogen.configs.<key>.spec.attestors[].cosign.ctlog.url <string>	# URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
status.autogen.configs.<key>.spec.attestors[].cosign.key <object>	# Key defines the type of key to validate the image.
status.autogen.configs.<key>.spec.attestors[].cosign.key.data <string>	# Data contains the inline public key
status.autogen.configs.<key>.spec.attestors[].cosign.key.expression <string>	# Expression is a Expression expression that returns the public key.
status.autogen.configs.<key>.spec.attestors[].cosign.key.hashAlgorithm <string>	# HashAlgorithm specifues signature algorithm for public keys. Supported values are sha224, sha256, sha384 and sha512. Defaults to sha256.
status.autogen.configs.<key>.spec.attestors[].cosign.key.kms <string>	# KMS contains the KMS url of the public key Supported formats differ based on the KMS system used.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless <object>	# Keyless sets the configuration to verify the authority against a Fulcio instance.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless.identities <[]object> (required)	# Identities sets a list of identities.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless.identities[].issuer <string>	# Issuer defines the issuer for this identity.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless.identities[].issuerRegExp <string>	# IssuerRegExp specifies a regular expression to match the issuer for this identity.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless.identities[].subject <string>	# Subject defines the subject for this identity.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless.identities[].subjectRegExp <string>	# SubjectRegExp specifies a regular expression to match the subject for this identity.
status.autogen.configs.<key>.spec.attestors[].cosign.keyless.roots <string>	# Roots is an optional set of PEM encoded trusted root certificates. If not provided, the system roots are used.
status.autogen.configs.<key>.spec.attestors[].cosign.source <object>	# Sources sets the configuration to specify the sources from where to consume the signature and attestations.
status.autogen.configs.<key>.spec.attestors[].cosign.source.PullSecrets <[]object>	# SignaturePullSecrets is an optional list of references to secrets in the same namespace as the deploying resource for pulling any of the signatures used by this Source.
status.autogen.configs.<key>.spec.attestors[].cosign.source.PullSecrets[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status.autogen.configs.<key>.spec.attestors[].cosign.source.repository <string>	# Repository defines the location from where to pull the signature / attestations.
status.autogen.configs.<key>.spec.attestors[].cosign.source.tagPrefix <string>	# TagPrefix is an optional prefix that signature and attestations have. This is the 'tag based discovery' and in the future once references are fully supported that should likely be the preferred way to handle these.
status.autogen.configs.<key>.spec.attestors[].cosign.tuf <object>	# TUF defines the configuration to fetch sigstore root
status.autogen.configs.<key>.spec.attestors[].cosign.tuf.mirror <string>	# Mirror is the base URL of Sigstore TUF repository
status.autogen.configs.<key>.spec.attestors[].cosign.tuf.root <object>	# Root defines the path or data of the trusted root
status.autogen.configs.<key>.spec.attestors[].cosign.tuf.root.data <string>	# Data is the base64 encoded TUF root
status.autogen.configs.<key>.spec.attestors[].cosign.tuf.root.path <string>	# Path is the URL or File location of the TUF root
status.autogen.configs.<key>.spec.attestors[].name <string> (required)	# Name is the name for this attestor. It is used to refer to the attestor in verification
status.autogen.configs.<key>.spec.attestors[].notary <object>	# Notary defines attestor configuration for Notary based signatures
status.autogen.configs.<key>.spec.attestors[].notary.certs <object>	# Certs define the cert chain for Notary signature verification
status.autogen.configs.<key>.spec.attestors[].notary.certs.expression <string>	# Expression defines the a CEL expression input.
status.autogen.configs.<key>.spec.attestors[].notary.certs.value <string>	# Value defines the raw string input.
status.autogen.configs.<key>.spec.attestors[].notary.tsaCerts <object>	# TSACerts define the cert chain for verifying timestamps of notary signature
status.autogen.configs.<key>.spec.attestors[].notary.tsaCerts.expression <string>	# Expression defines the a CEL expression input.
status.autogen.configs.<key>.spec.attestors[].notary.tsaCerts.value <string>	# Value defines the raw string input.
status.autogen.configs.<key>.spec.auditAnnotations <[]object>	# auditAnnotations contains CEL expressions which are used to produce audit annotations for the audit event of the API request. validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is required.
status.autogen.configs.<key>.spec.auditAnnotations[].key <string> (required)	# key specifies the audit annotation key. The audit annotation keys of a ValidatingAdmissionPolicy must be unique. The key must be a qualified name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length. The key is combined with the resource name of the ValidatingAdmissionPolicy to construct an audit annotation key: "{ValidatingAdmissionPolicy name}/{key}". If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy and the same audit annotation key, the annotation key will be identical. In this case, the first annotation written with the key will be included in the audit event and all subsequent annotations with the same key will be discarded. Required.
status.autogen.configs.<key>.spec.auditAnnotations[].valueExpression <string> (required)	# valueExpression represents the expression which is evaluated by CEL to produce an audit annotation value. The expression must evaluate to either a string or null value. If the expression evaluates to a string, the audit annotation is included with the string value. If the expression evaluates to null or empty string the audit annotation will be omitted. The valueExpression may be no longer than 5kb in length. If the result of the valueExpression is more than 10kb in length, it will be truncated to 10kb. If multiple ValidatingAdmissionPolicyBinding resources match an API request, then the valueExpression will be evaluated for each binding. All unique values produced by the valueExpressions will be joined together in a comma-separated list. Required.
status.autogen.configs.<key>.spec.autogen <object>	# AutogenConfiguration defines the configuration for the generation controller.
status.autogen.configs.<key>.spec.autogen.podControllers <object>	# PodControllers specifies whether to generate a pod controllers rules.
status.autogen.configs.<key>.spec.autogen.podControllers.controllers <[]string>
status.autogen.configs.<key>.spec.credentials <object>	# Credentials provides credentials that will be used for authentication with registry.
status.autogen.configs.<key>.spec.credentials.allowInsecureRegistry <boolean>	# AllowInsecureRegistry allows insecure access to a registry.
status.autogen.configs.<key>.spec.credentials.providers <[]string>	# Providers specifies a list of OCI Registry names, whose authentication providers are provided. It can be of one of these values: default,google,azure,amazon,github.
status.autogen.configs.<key>.spec.credentials.secrets <[]string>	# Secrets specifies a list of secrets that are provided for credentials. Secrets must live in the Kyverno namespace.
status.autogen.configs.<key>.spec.evaluation <object>	# EvaluationConfiguration defines the configuration for the policy evaluation.
status.autogen.configs.<key>.spec.evaluation.admission <object>	# Admission controls policy evaluation during admission.
status.autogen.configs.<key>.spec.evaluation.admission.enabled <boolean> default=true	# Enabled controls if rules are applied during admission. Optional. Default value is "true".
status.autogen.configs.<key>.spec.evaluation.background <object>	# Background controls policy evaluation during background scan.
status.autogen.configs.<key>.spec.evaluation.background.enabled <boolean> default=true	# Enabled controls if rules are applied to existing resources during a background scan. Optional. Default value is "true". The value must be set to "false" if the policy rule uses variables that are only available in the admission review request (e.g. user name).
status.autogen.configs.<key>.spec.evaluation.mode <string>	# Mode is the mode of policy evaluation. Allowed values are "Kubernetes" or "JSON". Optional. Default value is "Kubernetes".
status.autogen.configs.<key>.spec.failurePolicy <string> enum=Ignore|Fail	# FailurePolicy defines how to handle failures for the admission policy. Failures can occur from CEL expression parse errors, type check errors, runtime errors and invalid or mis-configured policy definitions or bindings.
status.autogen.configs.<key>.spec.images <[]object>	# ImageExtractors is a list of CEL expression to extract images from the resource
status.autogen.configs.<key>.spec.images[].expression <string> (required)	# Expression defines CEL expression to extract images from the resource.
status.autogen.configs.<key>.spec.images[].name <string> (required)	# Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
status.autogen.configs.<key>.spec.matchConditions <[]object>	# MatchConditions is a list of conditions that must be met for a request to be validated. Match conditions filter requests that have already been matched by the rules, namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests. There are a maximum of 64 match conditions allowed.
status.autogen.configs.<key>.spec.matchConditions[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables: 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject' - The existing object. The value is null for CREATE requests. 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/ Required.
status.autogen.configs.<key>.spec.matchConditions[].name <string> (required)	# Name is an identifier for this match condition, used for strategic merging of MatchConditions, as well as providing an identifier for logging purposes. A good name should be descriptive of the associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and must start and end with an alphanumeric character (e.g. 'MyName', or 'my.name', or '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName') Required.
status.autogen.configs.<key>.spec.matchConstraints <object>	# MatchConstraints specifies what resources this policy is designed to validate.
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules <[]object>	# ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about. The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules[].apiGroups <[]string>	# APIGroups is the API groups the resources belong to. '*' is all groups. If '*' is present, the length of the slice must be one. Required.
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules[].apiVersions <[]string>	# APIVersions is the API versions the resources belong to. '*' is all versions. If '*' is present, the length of the slice must be one. Required.
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules[].operations <[]string>	# Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or * for all of those operations and any future admission operations that are added. If '*' is present, the length of the slice must be one. Required.
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules[].resourceNames <[]string>	# ResourceNames is an optional white list of names that the rule applies to. An empty set means that everything is allowed.
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules[].resources <[]string>	# Resources is a list of resources this rule applies to. For example: 'pods' means pods. 'pods/log' means the log subresource of pods. '*' means all resources, but not subresources. 'pods/*' means all subresources of pods. '*/scale' means all scale subresources. '*/*' means all resources and their subresources. If wildcard is present, the validation rule will ensure resources do not overlap with each other. Depending on the enclosing object, subresources might not be allowed. Required.
status.autogen.configs.<key>.spec.matchConstraints.excludeResourceRules[].scope <string>	# scope specifies the scope of this rule. Valid values are "Cluster", "Namespaced", and "*" "Cluster" means that only cluster-scoped resources will match this rule. Namespace API objects are cluster-scoped. "Namespaced" means that only namespaced resources will match this rule. "*" means that there are no scope restrictions. Subresources match the scope of their parent resource. Default is "*".
status.autogen.configs.<key>.spec.matchConstraints.matchPolicy <string>	# matchPolicy defines how the "MatchResources" list is used to match incoming requests. Allowed values are "Exact" or "Equivalent". - Exact: match a request only if it exactly matches a specified rule. For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1, but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`, a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy. - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version. For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1, and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`, a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy. Defaults to "Equivalent"
status.autogen.configs.<key>.spec.matchConstraints.namespaceSelector <object>	# NamespaceSelector decides whether to run the admission control policy on an object based on whether the namespace for that object matches the selector. If the object itself is a namespace, the matching is performed on object.metadata.labels. If the object is another cluster scoped resource, it never skips the policy. For example, to run the webhook on any objects whose namespace is not associated with "runlevel" of "0" or "1"; you will set the selector as follows: "namespaceSelector": { "matchExpressions": [ { "key": "runlevel", "operator": "NotIn", "values": [ "0", "1" ] } ] } If instead you want to only run the policy on any objects whose namespace is associated with the "environment" of "prod" or "staging"; you will set the selector as follows: "namespaceSelector": { "matchExpressions": [ { "key": "environment", "operator": "In", "values": [ "prod", "staging" ] } ] } See https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/ for more examples of label selectors. Default to the empty LabelSelector, which matches everything.
status.autogen.configs.<key>.spec.matchConstraints.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.configs.<key>.spec.matchConstraints.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.configs.<key>.spec.matchConstraints.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.configs.<key>.spec.matchConstraints.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.configs.<key>.spec.matchConstraints.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.configs.<key>.spec.matchConstraints.objectSelector <object>	# ObjectSelector decides whether to run the validation based on if the object has matching labels. objectSelector is evaluated against both the oldObject and newObject that would be sent to the cel validation, and is considered to match if either object matches the selector. A null object (oldObject in the case of create, or newObject in the case of delete) or an object that cannot have labels (like a DeploymentRollback or a PodProxyOptions object) is not considered to match. Use the object selector only if the webhook is opt-in, because end users may skip the admission webhook by setting the labels. Default to the empty LabelSelector, which matches everything.
status.autogen.configs.<key>.spec.matchConstraints.objectSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
status.autogen.configs.<key>.spec.matchConstraints.objectSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
status.autogen.configs.<key>.spec.matchConstraints.objectSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
status.autogen.configs.<key>.spec.matchConstraints.objectSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
status.autogen.configs.<key>.spec.matchConstraints.objectSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules <[]object>	# ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches. The policy cares about an operation if it matches _any_ Rule.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules[].apiGroups <[]string>	# APIGroups is the API groups the resources belong to. '*' is all groups. If '*' is present, the length of the slice must be one. Required.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules[].apiVersions <[]string>	# APIVersions is the API versions the resources belong to. '*' is all versions. If '*' is present, the length of the slice must be one. Required.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules[].operations <[]string>	# Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or * for all of those operations and any future admission operations that are added. If '*' is present, the length of the slice must be one. Required.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules[].resourceNames <[]string>	# ResourceNames is an optional white list of names that the rule applies to. An empty set means that everything is allowed.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules[].resources <[]string>	# Resources is a list of resources this rule applies to. For example: 'pods' means pods. 'pods/log' means the log subresource of pods. '*' means all resources, but not subresources. 'pods/*' means all subresources of pods. '*/scale' means all scale subresources. '*/*' means all resources and their subresources. If wildcard is present, the validation rule will ensure resources do not overlap with each other. Depending on the enclosing object, subresources might not be allowed. Required.
status.autogen.configs.<key>.spec.matchConstraints.resourceRules[].scope <string>	# scope specifies the scope of this rule. Valid values are "Cluster", "Namespaced", and "*" "Cluster" means that only cluster-scoped resources will match this rule. Namespace API objects are cluster-scoped. "Namespaced" means that only namespaced resources will match this rule. "*" means that there are no scope restrictions. Subresources match the scope of their parent resource. Default is "*".
status.autogen.configs.<key>.spec.matchImageReferences <[]object>	# MatchImageReferences is a list of Glob and CELExpressions to match images. Any image that matches one of the rules is considered for validation Any image that does not match a rule is skipped, even when they are passed as arguments to image verification functions
status.autogen.configs.<key>.spec.matchImageReferences[].expression <string>	# Expression defines CEL Expressions for matching images
status.autogen.configs.<key>.spec.matchImageReferences[].glob <string>	# Glob defines a globbing pattern for matching images
status.autogen.configs.<key>.spec.validationActions <[]string>	# ValidationAction specifies the action to be taken when the matched resource violates the policy. If a validation evaluates to false it is always enforced according to these actions. Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according to these actions only if the FailurePolicy is set to Fail, otherwise the failures are ignored. This includes compilation errors, runtime errors and misconfigurations of the policy. validationActions is declared as a set of action values. Order does not matter. validationActions may not contain duplicates of the same action. The supported actions values are: "Deny" specifies that a validation failure results in a denied request. "Warn" specifies that a validation failure is reported to the request client in HTTP Warning headers, with a warning code of 299. Warnings can be sent both for allowed or denied admission responses. "Audit" specifies that a validation failure is recorded in the created reports. Clients should expect to handle additional values by ignoring any values not recognized. "Deny" and "Warn" may not be used together since this combination needlessly duplicates the validation failure both in the API response body and the HTTP warning headers. Required.
status.autogen.configs.<key>.spec.validationConfigurations <object> default={}	# ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
status.autogen.configs.<key>.spec.validationConfigurations.mutateDigest <boolean> default=true	# MutateDigest enables replacement of image tags with digests. Defaults to true.
status.autogen.configs.<key>.spec.validationConfigurations.required <boolean> default=true	# Required validates that images are verified, i.e., have passed a signature or attestation check.
status.autogen.configs.<key>.spec.validationConfigurations.verifyDigest <boolean> default=true	# VerifyDigest validates that images have a digest.
status.autogen.configs.<key>.spec.validations <[]object> (required)	# Validations contain CEL expressions which is used to apply the image validation checks.
status.autogen.configs.<key>.spec.validations[].expression <string> (required)	# Expression represents the expression which will be evaluated by CEL. ref: https://github.com/google/cel-spec CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables: - 'object' - The object from the incoming request. The value is null for DELETE requests. - 'oldObject' - The existing object. The value is null for CREATE requests. - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)). - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind. - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources. - 'variables' - Map of composited variables, from its name to its lazily evaluated value. For example, a variable named 'foo' can be accessed as 'variables.foo'. - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request. See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the request resource. The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the object. No other metadata properties are accessible. Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible. Accessible property names are escaped according to the following rules when accessed in the expression: - '__' escapes to '__underscores__' - '.' escapes to '__dot__' - '-' escapes to '__dash__' - '/' escapes to '__slash__' - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are: "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if", "import", "let", "loop", "package", "namespace", "return". Examples: - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"} - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"} - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"} Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1]. Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type: - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and non-intersecting elements in `Y` are appended, retaining their partial order. - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with non-intersecting keys are appended, retaining their partial order. Required.
status.autogen.configs.<key>.spec.validations[].message <string>	# Message represents the message displayed when validation fails. The message is required if the Expression contains line breaks. The message must not contain line breaks. If unset, the message is "failed rule: {Rule}". e.g. "must be a URL with the host matching spec.host" If the Expression contains line breaks. Message is required. The message must not contain line breaks. If unset, the message is "failed Expression: {Expression}".
status.autogen.configs.<key>.spec.validations[].messageExpression <string>	# messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails. Since messageExpression is used as a failure message, it must evaluate to a string. If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails. If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged. messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'. Example: "object.x must be less than max ("+string(params.max)+")"
status.autogen.configs.<key>.spec.validations[].reason <string>	# Reason represents a machine-readable description of why this validation failed. If this is the first validation in the list to fail, this reason, as well as the corresponding HTTP response code, are used in the HTTP response to the client. The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge". If not set, StatusReasonInvalid is used in the response to the client.
status.autogen.configs.<key>.spec.variables <[]object>	# Variables contain definitions of variables that can be used in composition of other expressions. Each variable is defined as a named CEL expression.
status.autogen.configs.<key>.spec.variables[].expression <string> (required)	# Expression is the expression that will be evaluated as the value of the variable. The CEL expression has access to the same identifiers as the CEL expressions in Validation.
status.autogen.configs.<key>.spec.variables[].name <string> (required)	# Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables. The variable can be accessed in other expressions through `variables` For example, if name is "foo", the variable will be available as `variables.foo`
status.autogen.configs.<key>.spec.webhookConfiguration <object>	# WebhookConfiguration defines the configuration for the webhook.
status.autogen.configs.<key>.spec.webhookConfiguration.timeoutSeconds <integer> format=int32	# TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy. After the configured time expires, the admission request may fail, or may simply ignore the policy results, based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
status.autogen.configs.<key>.targets <[]object> (required)
status.autogen.configs.<key>.targets[].group <string>
status.autogen.configs.<key>.targets[].kind <string> (required)
status.autogen.configs.<key>.targets[].resource <string> (required)
status.autogen.configs.<key>.targets[].version <string> (required)
status.conditionStatus <object>	# ConditionStatus is the shared status across all policy types
status.conditionStatus.conditions <[]object>
status.conditionStatus.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditionStatus.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditionStatus.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditionStatus.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditionStatus.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditionStatus.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.conditionStatus.message <string>	# Message is a human readable message indicating details about the generation of ValidatingAdmissionPolicy/MutatingAdmissionPolicy It is an empty string when ValidatingAdmissionPolicy/MutatingAdmissionPolicy is successfully generated.
status.conditionStatus.ready <boolean>	# The ready of a policy is a high-level summary of where the policy is in its lifecycle. The conditions array, the reason and message fields contain more detail about the policy's status.
