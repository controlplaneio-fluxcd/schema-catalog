# schema source: AWS RDS Controller v1.10.1 https://github.com/aws-controllers-k8s/rds-controller
apiVersion <string> enum=rds.services.k8s.aws/v1alpha1
kind <string> enum=DBSnapshot
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# DBSnapshotSpec defines the desired state of DBSnapshot. Contains the details of an Amazon RDS DB snapshot. This data type is used as a response element in the DescribeDBSnapshots action.
spec.dbInstanceIdentifier <string>	# The identifier of the DB instance that you want to create the snapshot of. Constraints: * Must match the identifier of an existing DBInstance.
spec.dbInstanceIdentifierRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.dbInstanceIdentifierRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.dbInstanceIdentifierRef.from.name <string>
spec.dbInstanceIdentifierRef.from.namespace <string>
spec.dbSnapshotIdentifier <string> (required)	# The identifier for the DB snapshot. Constraints: * Can't be null, empty, or blank * Must contain from 1 to 255 letters, numbers, or hyphens * First character must be a letter * Can't end with a hyphen or contain two consecutive hyphens Example: my-snapshot-id
spec.tags <[]object>
spec.tags[].key <string>
spec.tags[].value <string>
status <object>	# DBSnapshotStatus defines the observed state of DBSnapshot
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.allocatedStorage <integer> format=int64	# Specifies the allocated storage size in gibibytes (GiB).
status.availabilityZone <string>	# Specifies the name of the Availability Zone the DB instance was located in at the time of the DB snapshot.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.dbiResourceID <string>	# The identifier for the source DB instance, which can't be changed and which is unique to an Amazon Web Services Region.
status.encrypted <boolean>	# Indicates whether the DB snapshot is encrypted.
status.engine <string>	# Specifies the name of the database engine.
status.iamDatabaseAuthenticationEnabled <boolean>	# Indicates whether mapping of Amazon Web Services Identity and Access Management (IAM) accounts to database accounts is enabled.
status.instanceCreateTime <string> format=date-time	# Specifies the time in Coordinated Universal Time (UTC) when the DB instance, from which the snapshot was taken, was created.
status.iops <integer> format=int64	# Specifies the Provisioned IOPS (I/O operations per second) value of the DB instance at the time of the snapshot.
status.kmsKeyID <string>	# If Encrypted is true, the Amazon Web Services KMS key identifier for the encrypted DB snapshot. The Amazon Web Services KMS key identifier is the key ARN, key ID, alias ARN, or alias name for the KMS key.
status.licenseModel <string>	# License model information for the restored DB instance.
status.masterUsername <string>	# Provides the master username for the DB snapshot.
status.originalSnapshotCreateTime <string> format=date-time	# Specifies the time of the CreateDBSnapshot operation in Coordinated Universal Time (UTC). Doesn't change when the snapshot is copied.
status.percentProgress <integer> format=int64	# The percentage of the estimated data that has been transferred.
status.port <integer> format=int64	# Specifies the port that the database engine was listening on at the time of the snapshot.
status.processorFeatures <[]object>	# The number of CPU cores and the number of threads per core for the DB instance class of the DB instance when the DB snapshot was created.
status.processorFeatures[].name <string>
status.processorFeatures[].value <string>
status.snapshotCreateTime <string> format=date-time	# Specifies when the snapshot was taken in Coordinated Universal Time (UTC). Changes for the copy when the snapshot is copied.
status.snapshotDatabaseTime <string> format=date-time	# The timestamp of the most recent transaction applied to the database that you're backing up. Thus, if you restore a snapshot, SnapshotDatabaseTime is the most recent transaction in the restored DB instance. In contrast, originalSnapshotCreateTime specifies the system time that the snapshot completed. If you back up a read replica, you can determine the replica lag by comparing SnapshotDatabaseTime with originalSnapshotCreateTime. For example, if originalSnapshotCreateTime is two hours later than SnapshotDatabaseTime, then the replica lag is two hours.
status.snapshotTarget <string>	# Specifies where manual snapshots are stored: Amazon Web Services Outposts or the Amazon Web Services Region.
status.snapshotType <string>	# Provides the type of the DB snapshot.
status.sourceDBSnapshotIdentifier <string>	# The DB snapshot Amazon Resource Name (ARN) that the DB snapshot was copied from. It only has a value in the case of a cross-account or cross-Region copy.
status.sourceRegion <string>	# The Amazon Web Services Region that the DB snapshot was created in or copied from.
status.status <string>	# Specifies the status of this DB snapshot.
status.storageThroughput <integer> format=int64	# Specifies the storage throughput for the DB snapshot.
status.storageType <string>	# Specifies the storage type associated with DB snapshot.
status.tagList <[]object>
status.tagList[].key <string>
status.tagList[].value <string>
status.tdeCredentialARN <string>	# The ARN from the key store with which to associate the instance for TDE encryption.
status.timezone <string>	# The time zone of the DB snapshot. In most cases, the Timezone element is empty. Timezone content appears only for snapshots taken from Microsoft SQL Server DB instances that were created with a time zone specified.
status.vpcID <string>	# Provides the VPC ID associated with the DB snapshot.
