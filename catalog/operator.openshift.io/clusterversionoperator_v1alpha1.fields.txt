# schema source: OpenShift v4.21 https://github.com/openshift/api
apiVersion <string> enum=operator.openshift.io/v1alpha1
kind <string> enum=ClusterVersionOperator
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}	# spec is the specification of the desired behavior of the Cluster Version Operator.
spec.operatorLogLevel <string>	# operatorLogLevel is an intent based logging for the operator itself. It does not give fine grained control, but it is a simple way to manage coarse grained logging choices that operators have to interpret for themselves. Valid values are: "Normal", "Debug", "Trace", "TraceAll". Defaults to "Normal".
status <object> default={}	# status is the most recently observed status of the Cluster Version Operator.
status.observedGeneration <integer> format=int64	# observedGeneration represents the most recent generation observed by the operator and specifies the version of the spec field currently being synced.
