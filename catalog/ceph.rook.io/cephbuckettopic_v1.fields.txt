# schema source: Rook v1.20.2 https://github.com/rook/rook
apiVersion <string> enum=ceph.rook.io/v1
kind <string> enum=CephBucketTopic
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# BucketTopicSpec represent the spec of a Bucket Topic
spec.endpoint <object> (required)	# Contains the endpoint spec of the topic
spec.endpoint.amqp <object>	# Spec of AMQP endpoint
spec.endpoint.amqp.ackLevel <string> enum=none|broker|routeable default="broker"	# The ack level required for this topic (none/broker/routeable)
spec.endpoint.amqp.disableVerifySSL <boolean>	# Indicate whether the server certificate is validated by the client or not
spec.endpoint.amqp.exchange <string> (required) min=1	# Name of the exchange that is used to route messages based on topics
spec.endpoint.amqp.uri <string> (required) min=1	# The URI of the AMQP endpoint to push notification to
spec.endpoint.http <object>	# Spec of HTTP endpoint
spec.endpoint.http.disableVerifySSL <boolean>	# Indicate whether the server certificate is validated by the client or not
spec.endpoint.http.sendCloudEvents <boolean>	# Send the notifications with the CloudEvents header: https://github.com/cloudevents/spec/blob/main/cloudevents/adapters/aws-s3.md
spec.endpoint.http.uri <string> (required) min=1	# The URI of the HTTP endpoint to push notification to
spec.endpoint.kafka <object>	# Spec of Kafka endpoint
spec.endpoint.kafka.ackLevel <string> enum=none|broker default="broker"	# The ack level required for this topic (none/broker)
spec.endpoint.kafka.disableVerifySSL <boolean>	# Indicate whether the server certificate is validated by the client or not
spec.endpoint.kafka.mechanism <string> enum=PLAIN|SCRAM-SHA-512|SCRAM-SHA-256|GSSAPI|OAUTHBEARER default="PLAIN"	# The authentication mechanism for this topic (PLAIN/SCRAM-SHA-512/SCRAM-SHA-256/GSSAPI/OAUTHBEARER)
spec.endpoint.kafka.passwordSecretRef <object>	# The kafka password to use for authentication
spec.endpoint.kafka.passwordSecretRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.endpoint.kafka.passwordSecretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.endpoint.kafka.passwordSecretRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.endpoint.kafka.uri <string> (required) min=1	# The URI of the Kafka endpoint to push notification to
spec.endpoint.kafka.useSSL <boolean>	# Indicate whether to use SSL when communicating with the broker
spec.endpoint.kafka.userSecretRef <object>	# The kafka user name to use for authentication
spec.endpoint.kafka.userSecretRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.endpoint.kafka.userSecretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.endpoint.kafka.userSecretRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.objectStoreName <string> (required) min=1	# The name of the object store on which to define the topic
spec.objectStoreNamespace <string> (required) min=1	# The namespace of the object store on which to define the topic
spec.opaqueData <string>	# Data which is sent in each event
spec.persistent <boolean>	# Indication whether notifications to this endpoint are persistent or not
status <object>	# BucketTopicStatus represents the Status of a CephBucketTopic
status.ARN <string>	# The ARN of the topic generated by the RGW
status.observedGeneration <integer> format=int64	# ObservedGeneration is the latest generation observed by the controller.
status.phase <string>
status.secrets <[]object>
status.secrets[].name <string>	# name is unique within a namespace to reference a secret resource.
status.secrets[].namespace <string>	# namespace defines the space within which the secret name must be unique.
status.secrets[].resourceVersion <string>
status.secrets[].uid <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
