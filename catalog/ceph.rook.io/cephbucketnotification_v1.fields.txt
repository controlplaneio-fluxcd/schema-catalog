# schema source: Rook v1.20.1 https://github.com/rook/rook
apiVersion <string> enum=ceph.rook.io/v1
kind <string> enum=CephBucketNotification
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# BucketNotificationSpec represent the spec of a Bucket Notification
spec.events <[]string>	# List of events that should trigger the notification
spec.filter <object>	# Spec of notification filter
spec.filter.keyFilters <[]object>	# Filters based on the object's key
spec.filter.keyFilters[].name <string> (required) enum=prefix|suffix|regex	# Name of the filter - prefix/suffix/regex
spec.filter.keyFilters[].value <string> (required)	# Value to filter on
spec.filter.metadataFilters <[]object>	# Filters based on the object's metadata
spec.filter.metadataFilters[].name <string> (required) min=1	# Name of the metadata or tag
spec.filter.metadataFilters[].value <string> (required)	# Value to filter on
spec.filter.tagFilters <[]object>	# Filters based on the object's tags
spec.filter.tagFilters[].name <string> (required) min=1	# Name of the metadata or tag
spec.filter.tagFilters[].value <string> (required)	# Value to filter on
spec.topic <string> (required) min=1	# The name of the topic associated with this notification
status <object>	# Status represents the status of an object
status.conditions <[]object>
status.conditions[].lastHeartbeatTime <string> format=date-time
status.conditions[].lastTransitionTime <string> format=date-time
status.conditions[].message <string>
status.conditions[].reason <string>	# ConditionReason is a reason for a condition
status.conditions[].status <string>
status.conditions[].type <string>	# ConditionType represent a resource's status
status.observedGeneration <integer> format=int64	# ObservedGeneration is the latest generation observed by the controller.
status.phase <string>
