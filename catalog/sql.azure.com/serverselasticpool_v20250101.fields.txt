# schema source: Azure Service Operator v2.21.0 https://github.com/Azure/azure-service-operator
apiVersion <string> enum=sql.azure.com/v20250101
kind <string> enum=ServersElasticPool
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.autoPauseDelay <integer>	# AutoPauseDelay: Time in minutes after which elastic pool is automatically paused. A value of -1 means that automatic pause is disabled
spec.availabilityZone <string> enum=1|2|3|NoPreference	# AvailabilityZone: Specifies the availability zone the pool's primary replica is pinned to.
spec.azureName <string>	# AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it doesn't have to be.
spec.highAvailabilityReplicaCount <integer>	# HighAvailabilityReplicaCount: The number of secondary replicas associated with the Business Critical, Premium, or Hyperscale edition elastic pool that are used to provide high availability. Applicable only to Hyperscale elastic pools.
spec.licenseType <string> enum=BasePrice|LicenseIncluded	# LicenseType: The license type to apply for this elastic pool.
spec.location <string> (required)	# Location: The geo-location where the resource lives
spec.maintenanceConfigurationId <string>	# MaintenanceConfigurationId: Maintenance configuration id assigned to the elastic pool. This configuration defines the period when the maintenance updates will will occur.
spec.maxSizeBytes <integer>	# MaxSizeBytes: The storage limit for the database elastic pool in bytes.
spec.minCapacity <number>	# MinCapacity: Minimal capacity that serverless pool will not shrink below, if not paused
spec.operatorSpec <object>	# OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not passed directly to Azure
spec.operatorSpec.configMapExpressions <[]object>	# ConfigMapExpressions: configures where to place operator written dynamic ConfigMaps (created with CEL expressions).
spec.operatorSpec.configMapExpressions[].annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the destination resource.
spec.operatorSpec.configMapExpressions[].key <string>	# Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting map[string]string.
spec.operatorSpec.configMapExpressions[].labels <map[string]string>	# Labels is an optional set of labels to apply to the destination resource.
spec.operatorSpec.configMapExpressions[].name <string> (required)	# Name is the name of the Kubernetes configmap or secret to write to. The configmap or secret will be created in the same namespace as the resource.
spec.operatorSpec.configMapExpressions[].value <string> (required)	# Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
spec.operatorSpec.secretExpressions <[]object>	# SecretExpressions: configures where to place operator written dynamic secrets (created with CEL expressions).
spec.operatorSpec.secretExpressions[].annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the destination resource.
spec.operatorSpec.secretExpressions[].key <string>	# Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting map[string]string.
spec.operatorSpec.secretExpressions[].labels <map[string]string>	# Labels is an optional set of labels to apply to the destination resource.
spec.operatorSpec.secretExpressions[].name <string> (required)	# Name is the name of the Kubernetes configmap or secret to write to. The configmap or secret will be created in the same namespace as the resource.
spec.operatorSpec.secretExpressions[].value <string> (required)	# Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
spec.owner <object> (required)	# Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a reference to a sql.azure.com/Server resource
spec.owner.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"
spec.owner.name <string>	# This is the name of the Kubernetes resource to reference.
spec.perDatabaseSettings <object>	# PerDatabaseSettings: The per database settings for the elastic pool.
spec.perDatabaseSettings.autoPauseDelay <integer>	# AutoPauseDelay: Auto Pause Delay for per database within pool
spec.perDatabaseSettings.maxCapacity <number>	# MaxCapacity: The maximum capacity any one database can consume.
spec.perDatabaseSettings.minCapacity <number>	# MinCapacity: The minimum capacity all databases are guaranteed.
spec.preferredEnclaveType <string> enum=Default|VBS	# PreferredEnclaveType: Type of enclave requested on the elastic pool.
spec.sku <object>	# Sku: The elastic pool SKU. The list of SKUs may vary by region and support offer. To determine the SKUs (including the SKU name, tier/edition, family, and capacity) that are available to your subscription in an Azure region, use the `Capabilities_ListByLocation` REST API or the following command: ```azurecli az sql elastic-pool list-editions -l <location> -o table ```
spec.sku.capacity <integer>	# Capacity: Capacity of the particular SKU.
spec.sku.family <string>	# Family: If the service has different generations of hardware, for the same SKU, then that can be captured here.
spec.sku.name <string> (required)	# Name: The name of the SKU, typically, a letter + Number code, e.g. P3.
spec.sku.size <string>	# Size: Size of the particular SKU
spec.sku.tier <string>	# Tier: The tier or edition of the particular SKU, e.g. Basic, Premium.
spec.tags <map[string]string>	# Tags: Resource tags.
spec.zoneRedundant <boolean>	# ZoneRedundant: Whether or not this elastic pool is zone redundant, which means the replicas of this elastic pool will be spread across multiple availability zones.
status <object>
status.autoPauseDelay <integer>	# AutoPauseDelay: Time in minutes after which elastic pool is automatically paused. A value of -1 means that automatic pause is disabled
status.availabilityZone <string>	# AvailabilityZone: Specifies the availability zone the pool's primary replica is pinned to.
status.conditions <[]object>	# Conditions: The observed state of the resource
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Message is a human readable message indicating details about the transition. This field may be empty.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# Reason for the condition's last transition. Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False. This is omitted in all cases when Status == Unknown
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, or Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.creationDate <string>	# CreationDate: The creation date of the elastic pool (ISO8601 format).
status.highAvailabilityReplicaCount <integer>	# HighAvailabilityReplicaCount: The number of secondary replicas associated with the Business Critical, Premium, or Hyperscale edition elastic pool that are used to provide high availability. Applicable only to Hyperscale elastic pools.
status.id <string>	# Id: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
status.kind <string>	# Kind: Kind of elastic pool. This is metadata used for the Azure portal experience.
status.licenseType <string>	# LicenseType: The license type to apply for this elastic pool.
status.location <string>	# Location: The geo-location where the resource lives
status.maintenanceConfigurationId <string>	# MaintenanceConfigurationId: Maintenance configuration id assigned to the elastic pool. This configuration defines the period when the maintenance updates will will occur.
status.maxSizeBytes <integer>	# MaxSizeBytes: The storage limit for the database elastic pool in bytes.
status.minCapacity <number>	# MinCapacity: Minimal capacity that serverless pool will not shrink below, if not paused
status.name <string>	# Name: The name of the resource
status.perDatabaseSettings <object>	# PerDatabaseSettings: The per database settings for the elastic pool.
status.perDatabaseSettings.autoPauseDelay <integer>	# AutoPauseDelay: Auto Pause Delay for per database within pool
status.perDatabaseSettings.maxCapacity <number>	# MaxCapacity: The maximum capacity any one database can consume.
status.perDatabaseSettings.minCapacity <number>	# MinCapacity: The minimum capacity all databases are guaranteed.
status.preferredEnclaveType <string>	# PreferredEnclaveType: Type of enclave requested on the elastic pool.
status.sku <object>	# Sku: The elastic pool SKU. The list of SKUs may vary by region and support offer. To determine the SKUs (including the SKU name, tier/edition, family, and capacity) that are available to your subscription in an Azure region, use the `Capabilities_ListByLocation` REST API or the following command: ```azurecli az sql elastic-pool list-editions -l <location> -o table ```
status.sku.capacity <integer>	# Capacity: Capacity of the particular SKU.
status.sku.family <string>	# Family: If the service has different generations of hardware, for the same SKU, then that can be captured here.
status.sku.name <string>	# Name: The name of the SKU, typically, a letter + Number code, e.g. P3.
status.sku.size <string>	# Size: Size of the particular SKU
status.sku.tier <string>	# Tier: The tier or edition of the particular SKU, e.g. Basic, Premium.
status.state <string>	# State: The state of the elastic pool.
status.systemData <object>	# SystemData: Azure Resource Manager metadata containing createdBy and modifiedBy information.
status.systemData.createdAt <string>	# CreatedAt: The timestamp of resource creation (UTC).
status.systemData.createdBy <string>	# CreatedBy: The identity that created the resource.
status.systemData.createdByType <string>	# CreatedByType: The type of identity that created the resource.
status.systemData.lastModifiedAt <string>	# LastModifiedAt: The timestamp of resource last modification (UTC)
status.systemData.lastModifiedBy <string>	# LastModifiedBy: The identity that last modified the resource.
status.systemData.lastModifiedByType <string>	# LastModifiedByType: The type of identity that last modified the resource.
status.tags <map[string]string>	# Tags: Resource tags.
status.type <string>	# Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
status.zoneRedundant <boolean>	# ZoneRedundant: Whether or not this elastic pool is zone redundant, which means the replicas of this elastic pool will be spread across multiple availability zones.
