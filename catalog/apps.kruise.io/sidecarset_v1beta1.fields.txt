# schema source: OpenKruise v1.9.1 https://github.com/openkruise/kruise
apiVersion <string> enum=apps.kruise.io/v1beta1
kind <string> enum=SidecarSet (cluster-scoped)
metadata.name <string> (required)
spec <object>	# SidecarSetSpec defines the desired state of SidecarSet
spec.containers <[]object>	# Containers is the list of sidecar containers to be injected into the selected pod
spec.containers[].podInjectPolicy <string>	# The rules that injected SidecarContainer into Pod.spec.containers, not takes effect in initContainers If BeforeAppContainer, the SidecarContainer will be injected in front of the pod.spec.containers otherwise it will be injected into the back. default BeforeAppContainerType
spec.containers[].resourcesPolicy <object>	# ResourcesPolicy defines the policy for dynamically configuring container resources based on Pod specification during pod creation. Validation webhook will reject pod creation request if both resourcesPolicy and resources are configured.
spec.containers[].resourcesPolicy.resourcesExpr <object> (required)	# ResourcesExpr defines the expressions for calculating resource values
spec.containers[].resourcesPolicy.resourcesExpr.limits <object>	# Limits defines expressions for calculating resource limits
spec.containers[].resourcesPolicy.resourcesExpr.limits.cpu <string>	# CPU expression for calculating CPU limits Support +, -, *, /, max(), min() and variable 'cpu' Variable 'cpu' represents the sum or max of resources.limits.cpu of all matched containers If matched containers don't have resources.limits configured, this field will be treated as unlimited If the expression result is unlimited, sidecar container resources.limits won't be configured
spec.containers[].resourcesPolicy.resourcesExpr.limits.memory <string>	# Memory expression for calculating memory limits Support +, -, *, /, max(), min() and variable 'memory' Variable 'memory' represents the sum or max of resources.limits.memory of all matched containers If matched containers don't have resources.limits configured, this field will be treated as unlimited If the expression result is unlimited, sidecar container resources.limits won't be configured
spec.containers[].resourcesPolicy.resourcesExpr.requests <object>	# Requests defines expressions for calculating resource requests
spec.containers[].resourcesPolicy.resourcesExpr.requests.cpu <string>	# CPU expression for calculating CPU requests Support +, -, *, /, max(), min() and variable 'cpu' Variable 'cpu' represents the sum or max of resources.limits.cpu of all matched containers If matched containers don't have resources.requests configured, the corresponding resource value will be treated as 0
spec.containers[].resourcesPolicy.resourcesExpr.requests.memory <string>	# Memory expression for calculating memory requests Support +, -, *, /, max(), min() and variable 'memory' Variable 'memory' represents the sum or max of resources.limits.memory of all matched containers If matched containers don't have resources.requests configured, the corresponding resource value will be treated as 0
spec.containers[].resourcesPolicy.targetContainersMode <string> (required) enum=sum|max	# TargetContainersMode defines how to aggregate resources from target containers
spec.containers[].resourcesPolicy.targetContainersNameRegex <string> default=".*"	# TargetContainersNameRegex is the regex pattern to match target container names If no container names match this regex, the pod creation request will be rejected by the webhook Target containers include native sidecar containers and plain containers, excluding Kruise sidecar containers
spec.containers[].shareVolumeDevicePolicy <object>	# If ShareVolumeDevicePolicy is enabled, the sidecar container will share the other container's VolumeDevices in the pod(don't contain the injected sidecar container). This is a pointer to ensure that the sidecarset-hash does not change if the user does not configure this field, mainly for compatibility with older versions.
spec.containers[].shareVolumeDevicePolicy.type <string>
spec.containers[].shareVolumePolicy <object>	# If ShareVolumePolicy is enabled, the sidecar container will share the other container's VolumeMounts in the pod(not including the injected sidecar container).
spec.containers[].shareVolumePolicy.type <string>
spec.containers[].transferEnv <[]object>	# TransferEnv will transfer env info from other container SourceContainerName is pod.spec.container[x].name; EnvName is pod.spec.container[x].Env.name
spec.containers[].transferEnv[].envName <string>
spec.containers[].transferEnv[].envNames <[]string>
spec.containers[].transferEnv[].sourceContainerName <string>
spec.containers[].transferEnv[].sourceContainerNameFrom <object>
spec.containers[].transferEnv[].sourceContainerNameFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
spec.containers[].transferEnv[].sourceContainerNameFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.containers[].transferEnv[].sourceContainerNameFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.containers[].upgradeStrategy <object>	# sidecarContainer upgrade strategy, include: ColdUpgrade, HotUpgrade
spec.containers[].upgradeStrategy.hotUpgradeEmptyImage <string>	# when HotUpgrade, HotUpgradeEmptyImage is used to complete the hot upgrading process HotUpgradeEmptyImage is consistent of sidecar container in Command, Args, Liveness probe, etc. but it does no actual work.
spec.containers[].upgradeStrategy.upgradeType <string>	# when sidecar container is stateless, use ColdUpgrade otherwise HotUpgrade are more HotUpgrade. examples for istio envoy container is suitable for HotUpgrade default is ColdUpgrade
spec.customVersion <string>	# CustomVersion is designed to record and label the controllerRevision of sidecarSet. This field will be passed from SidecarSet to its corresponding ControllerRevision, users can use this field to selector the ControllerRevision they want. For example, users can update the customVersion from "version-1" to "version-2" when they upgrade the sidecarSet to "version-2", and they write the "version-2" to InjectionStrategy.Revision.CustomVersion when they decided to promote the "version-2", to avoid some risks about gray deployment of SidecarSet.
spec.imagePullSecrets <[]object>	# List of the names of secrets required by pulling sidecar container images
spec.imagePullSecrets[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.initContainers <[]object>	# InitContainers is the list of init containers to be injected into the selected pod We will inject those containers by their name in ascending order We only inject init containers when a new pod is created, it does not apply to any existing pod
spec.initContainers[].podInjectPolicy <string>	# The rules that injected SidecarContainer into Pod.spec.containers, not takes effect in initContainers If BeforeAppContainer, the SidecarContainer will be injected in front of the pod.spec.containers otherwise it will be injected into the back. default BeforeAppContainerType
spec.initContainers[].resourcesPolicy <object>	# ResourcesPolicy defines the policy for dynamically configuring container resources based on Pod specification during pod creation. Validation webhook will reject pod creation request if both resourcesPolicy and resources are configured.
spec.initContainers[].resourcesPolicy.resourcesExpr <object> (required)	# ResourcesExpr defines the expressions for calculating resource values
spec.initContainers[].resourcesPolicy.resourcesExpr.limits <object>	# Limits defines expressions for calculating resource limits
spec.initContainers[].resourcesPolicy.resourcesExpr.limits.cpu <string>	# CPU expression for calculating CPU limits Support +, -, *, /, max(), min() and variable 'cpu' Variable 'cpu' represents the sum or max of resources.limits.cpu of all matched containers If matched containers don't have resources.limits configured, this field will be treated as unlimited If the expression result is unlimited, sidecar container resources.limits won't be configured
spec.initContainers[].resourcesPolicy.resourcesExpr.limits.memory <string>	# Memory expression for calculating memory limits Support +, -, *, /, max(), min() and variable 'memory' Variable 'memory' represents the sum or max of resources.limits.memory of all matched containers If matched containers don't have resources.limits configured, this field will be treated as unlimited If the expression result is unlimited, sidecar container resources.limits won't be configured
spec.initContainers[].resourcesPolicy.resourcesExpr.requests <object>	# Requests defines expressions for calculating resource requests
spec.initContainers[].resourcesPolicy.resourcesExpr.requests.cpu <string>	# CPU expression for calculating CPU requests Support +, -, *, /, max(), min() and variable 'cpu' Variable 'cpu' represents the sum or max of resources.limits.cpu of all matched containers If matched containers don't have resources.requests configured, the corresponding resource value will be treated as 0
spec.initContainers[].resourcesPolicy.resourcesExpr.requests.memory <string>	# Memory expression for calculating memory requests Support +, -, *, /, max(), min() and variable 'memory' Variable 'memory' represents the sum or max of resources.limits.memory of all matched containers If matched containers don't have resources.requests configured, the corresponding resource value will be treated as 0
spec.initContainers[].resourcesPolicy.targetContainersMode <string> (required) enum=sum|max	# TargetContainersMode defines how to aggregate resources from target containers
spec.initContainers[].resourcesPolicy.targetContainersNameRegex <string> default=".*"	# TargetContainersNameRegex is the regex pattern to match target container names If no container names match this regex, the pod creation request will be rejected by the webhook Target containers include native sidecar containers and plain containers, excluding Kruise sidecar containers
spec.initContainers[].shareVolumeDevicePolicy <object>	# If ShareVolumeDevicePolicy is enabled, the sidecar container will share the other container's VolumeDevices in the pod(don't contain the injected sidecar container). This is a pointer to ensure that the sidecarset-hash does not change if the user does not configure this field, mainly for compatibility with older versions.
spec.initContainers[].shareVolumeDevicePolicy.type <string>
spec.initContainers[].shareVolumePolicy <object>	# If ShareVolumePolicy is enabled, the sidecar container will share the other container's VolumeMounts in the pod(not including the injected sidecar container).
spec.initContainers[].shareVolumePolicy.type <string>
spec.initContainers[].transferEnv <[]object>	# TransferEnv will transfer env info from other container SourceContainerName is pod.spec.container[x].name; EnvName is pod.spec.container[x].Env.name
spec.initContainers[].transferEnv[].envName <string>
spec.initContainers[].transferEnv[].envNames <[]string>
spec.initContainers[].transferEnv[].sourceContainerName <string>
spec.initContainers[].transferEnv[].sourceContainerNameFrom <object>
spec.initContainers[].transferEnv[].sourceContainerNameFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
spec.initContainers[].transferEnv[].sourceContainerNameFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.initContainers[].transferEnv[].sourceContainerNameFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.initContainers[].upgradeStrategy <object>	# sidecarContainer upgrade strategy, include: ColdUpgrade, HotUpgrade
spec.initContainers[].upgradeStrategy.hotUpgradeEmptyImage <string>	# when HotUpgrade, HotUpgradeEmptyImage is used to complete the hot upgrading process HotUpgradeEmptyImage is consistent of sidecar container in Command, Args, Liveness probe, etc. but it does no actual work.
spec.initContainers[].upgradeStrategy.upgradeType <string>	# when sidecar container is stateless, use ColdUpgrade otherwise HotUpgrade are more HotUpgrade. examples for istio envoy container is suitable for HotUpgrade default is ColdUpgrade
spec.injectionStrategy <object>	# InjectionStrategy describe the strategy when sidecarset is injected into pods
spec.injectionStrategy.paused <boolean>	# Paused indicates that SidecarSet will suspend injection into Pods If Paused is true, the sidecarSet will not be injected to newly created Pods, but the injected sidecar container remains updating and running. default is false
spec.injectionStrategy.revision <object>	# Revision can help users rolling update SidecarSet safely. If users set this filed, SidecarSet will try to inject specific revision according to different policies.
spec.injectionStrategy.revision.customVersion <string>	# CustomVersion corresponds to the customVersion field of (History) SidecarSet. SidecarSet will select the specific ControllerRevision via this CustomVersion, and then restore the history SidecarSet to inject specific version of the sidecar to pods.
spec.injectionStrategy.revision.policy <string> enum=Always|Partial default="Always"	# Policy describes the behavior of revision injection.
spec.injectionStrategy.revision.revisionName <string>	# RevisionName corresponds to a specific ControllerRevision name of SidecarSet that you want to inject to Pods.
spec.namespaceSelector <object>	# NamespaceSelector select which namespaces to inject sidecar containers. If not specified, match pods in all namespaces(in cluster) Default to the empty LabelSelector, which matches everything.
spec.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.patchPodMetadata <[]object>	# SidecarSet support to inject & in-place update metadata in pod.
spec.patchPodMetadata[].annotations <map[string]string>	# annotations
spec.patchPodMetadata[].patchPolicy <string>	# labels map[string]string `json:"labels,omitempty"` patch pod metadata policy, Default is "Retain"
spec.revisionHistoryLimit <integer> format=int32	# RevisionHistoryLimit indicates the maximum quantity of stored revisions about the SidecarSet. default value is 10
spec.selector <object>	# selector is a label query over pods that should be injected
spec.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.updateStrategy <object>	# The sidecarset updateStrategy to use to replace existing pods with new ones.
spec.updateStrategy.maxUnavailable <int-or-string>	# The maximum number of SidecarSet pods that can be unavailable during the update. Value can be an absolute number (ex: 5) or a percentage of total number of SidecarSet pods at the start of the update (ex: 10%). Absolute number is calculated from percentage by rounding up. This cannot be 0. Default value is 1.
spec.updateStrategy.partition <int-or-string>	# Partition is the desired number of pods in old revisions. It means when partition is set during pods updating, (replicas - partition) number of pods will be updated. Default value is 0.
spec.updateStrategy.paused <boolean>	# Paused indicates that the SidecarSet is paused to update the injected pods, For the impact on the injection behavior for newly created Pods, please refer to the comments of Selector.
spec.updateStrategy.priorityStrategy <object>	# Priorities are the rules for calculating the priority of updating pods. Each pod to be updated, will pass through these terms and get a sum of weights.
spec.updateStrategy.priorityStrategy.orderPriority <[]object>	# Order priority terms, pods will be sorted by the value of orderedKey. For example: ``` orderPriority: - orderedKey: key1 - orderedKey: key2 ``` First, all pods which have key1 in labels will be sorted by the value of key1. Then, the left pods which have no key1 but have key2 in labels will be sorted by the value of key2 and put behind those pods have key1.
spec.updateStrategy.priorityStrategy.orderPriority[].orderedKey <string> (required)	# Calculate priority by value of this key. Values of this key, will be sorted by GetInt(val). GetInt method will find the last int in value, such as getting 5 in value '5', getting 10 in value 'sts-10'.
spec.updateStrategy.priorityStrategy.weightPriority <[]object>	# Weight priority terms, pods will be sorted by the sum of all terms weight.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector <object> (required)	# MatchSelector is used to select by pod's labels.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.updateStrategy.priorityStrategy.weightPriority[].weight <integer> (required) format=int32	# Weight associated with matching the corresponding matchExpressions, in the range 1-100.
spec.updateStrategy.scatterStrategy <[]object>	# ScatterStrategy defines the scatter rules to make pods been scattered when update. This will avoid pods with the same key-value to be updated in one batch. - Note that pods will be scattered after priority sort. So, although priority strategy and scatter strategy can be applied together, we suggest to use either one of them. - If scatterStrategy is used, we suggest to just use one term. Otherwise, the update order can be hard to understand.
spec.updateStrategy.scatterStrategy[].key <string> (required)
spec.updateStrategy.scatterStrategy[].value <string> (required)
spec.updateStrategy.selector <object>	# If selector is not nil, this upgrade will only update the selected pods. Starting from Kruise 1.8.0, the updateStrategy.Selector affects the version of the Sidecar container injected into newly created Pods by a SidecarSet configured with an injectionStrategy. In most cases, all newly created Pods are injected with the specified Sidecar version as configured in injectionStrategy.revision, which is consistent with previous versions.
spec.updateStrategy.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.updateStrategy.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.updateStrategy.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.updateStrategy.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.updateStrategy.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.updateStrategy.type <string>	# Type is NotUpdate, the SidecarSet don't update the injected pods, it will only inject sidecar container into the newly created pods. Type is RollingUpdate, the SidecarSet will update the injected pods to the latest version on RollingUpdate Strategy. default is RollingUpdate
spec.volumes <any>	# List of volumes that can be mounted by sidecar containers
status <object>	# SidecarSetStatus defines the observed state of SidecarSet
status.collisionCount <integer> format=int32	# CollisionCount is the count of hash collisions for the SidecarSet. The SidecarSet controller uses this field as a collision avoidance mechanism when it needs to create the name for the newest ControllerRevision.
status.latestRevision <string>	# LatestRevision, if not empty, indicates the latest controllerRevision name of the SidecarSet.
status.matchedPods <integer> (required) format=int32	# matchedPods is the number of Pods whose labels are matched with this SidecarSet's selector and are created after sidecarset creates
status.observedGeneration <integer> format=int64	# observedGeneration is the most recent generation observed for this SidecarSet. It corresponds to the SidecarSet's generation, which is updated on mutation by the API Server.
status.readyPods <integer> (required) format=int32	# readyPods is the number of matched Pods that have a ready condition
status.updatedPods <integer> (required) format=int32	# updatedPods is the number of matched Pods that are injected with the latest SidecarSet's containers
status.updatedReadyPods <integer> format=int32	# updatedReadyPods is the number of matched pods that updated and ready
