# schema source: OpenKruise v1.9.1 https://github.com/openkruise/kruise
apiVersion <string> enum=apps.kruise.io/v1alpha1
kind <string> enum=CloneSet
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# CloneSetSpec defines the desired state of CloneSet
spec.lifecycle <object>	# Lifecycle defines the lifecycle hooks for Pods pre-available(pre-normal), pre-delete, in-place update.
spec.lifecycle.inPlaceUpdate <object>	# InPlaceUpdate is the hook before Pod to update and after Pod has been updated.
spec.lifecycle.inPlaceUpdate.finalizersHandler <[]string>
spec.lifecycle.inPlaceUpdate.labelsHandler <map[string]string>
spec.lifecycle.inPlaceUpdate.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.lifecycle.preDelete <object>	# PreDelete is the hook before Pod to be deleted.
spec.lifecycle.preDelete.finalizersHandler <[]string>
spec.lifecycle.preDelete.labelsHandler <map[string]string>
spec.lifecycle.preDelete.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.lifecycle.preNormal <object>	# PreNormal is the hook after Pod to be created and ready to be Normal.
spec.lifecycle.preNormal.finalizersHandler <[]string>
spec.lifecycle.preNormal.labelsHandler <map[string]string>
spec.lifecycle.preNormal.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.minReadySeconds <integer> format=int32	# Minimum number of seconds for which a newly created pod should be ready without any of its container crashing, for it to be considered available. Defaults to 0 (pod will be considered available as soon as it is ready)
spec.progressDeadlineSeconds <integer> format=int32	# ProgressDeadlineSeconds specifies the maximum time for the CloneSet to reach available or paused state due to partitioning. The controller will set a ProgressDeadlineExceeded condition when timeout occurs, while excluding paused state duration from the deadline calculation. This field is optional. If not set, the controller will not track progress deadlines or add the condition.
spec.replicas <integer> format=int32	# Replicas is the desired number of replicas of the given Template. These are replicas in the sense that they are instantiations of the same Template. If unspecified, defaults to 1.
spec.revisionHistoryLimit <integer> format=int32	# RevisionHistoryLimit is the maximum number of revisions that will be maintained in the CloneSet's revision history. The revision history consists of all revisions not represented by a currently applied CloneSetSpec version. The default value is 10.
spec.scaleStrategy <object>	# ScaleStrategy indicates the ScaleStrategy that will be employed to create and delete Pods in the CloneSet.
spec.scaleStrategy.disablePVCReuse <boolean>	# Indicate if cloneSet will reuse already existed pvc to rebuild a new pod
spec.scaleStrategy.maxUnavailable <int-or-string>	# The maximum number of pods that can be unavailable for scaled pods. This field can control the changes rate of replicas for CloneSet so as to minimize the impact for users' service. The scale will fail if the number of unavailable pods were greater than this MaxUnavailable at scaling up. MaxUnavailable works only when scaling up.
spec.scaleStrategy.podsToDelete <[]string>	# PodsToDelete is the names of Pod should be deleted. Note that this list will be truncated for non-existing pod names.
spec.selector <object> (required)	# Selector is a label query over pods that should match the replica count. It must match the pod template's labels. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
spec.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template <any> (required)	# Template describes the pods that will be created.
spec.updateStrategy <object>	# UpdateStrategy indicates the UpdateStrategy that will be employed to update Pods in the CloneSet when a revision is made to Template.
spec.updateStrategy.inPlaceUpdateStrategy <object>	# InPlaceUpdateStrategy contains strategies for in-place update.
spec.updateStrategy.inPlaceUpdateStrategy.gracePeriodSeconds <integer> format=int32	# GracePeriodSeconds is the timespan between set Pod status to not-ready and update images in Pod spec when in-place update a Pod.
spec.updateStrategy.maxSurge <int-or-string>	# The maximum number of pods that can be scheduled above the desired replicas during update or specified delete. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding up. Defaults to 0.
spec.updateStrategy.maxUnavailable <int-or-string>	# The maximum number of pods that can be unavailable during update or scale. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding up by default. When maxSurge > 0, absolute number is calculated from percentage by rounding down. Defaults to 20%.
spec.updateStrategy.partition <int-or-string>	# Partition is the desired number of pods in old revisions. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding up by default. It means when partition is set during pods updating, (replicas - partition value) number of pods will be updated. Default value is 0.
spec.updateStrategy.paused <boolean>	# Paused indicates that the CloneSet is paused. Default value is false
spec.updateStrategy.priorityStrategy <object>	# Priorities are the rules for calculating the priority of updating pods. Each pod to be updated, will pass through these terms and get a sum of weights.
spec.updateStrategy.priorityStrategy.orderPriority <[]object>	# Order priority terms, pods will be sorted by the value of orderedKey. For example: ``` orderPriority: - orderedKey: key1 - orderedKey: key2 ``` First, all pods which have key1 in labels will be sorted by the value of key1. Then, the left pods which have no key1 but have key2 in labels will be sorted by the value of key2 and put behind those pods have key1.
spec.updateStrategy.priorityStrategy.orderPriority[].orderedKey <string> (required)	# Calculate priority by value of this key. Values of this key, will be sorted by GetInt(val). GetInt method will find the last int in value, such as getting 5 in value '5', getting 10 in value 'sts-10'.
spec.updateStrategy.priorityStrategy.weightPriority <[]object>	# Weight priority terms, pods will be sorted by the sum of all terms weight.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector <object> (required)	# MatchSelector is used to select by pod's labels.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.updateStrategy.priorityStrategy.weightPriority[].weight <integer> (required) format=int32	# Weight associated with matching the corresponding matchExpressions, in the range 1-100.
spec.updateStrategy.scatterStrategy <[]object>	# ScatterStrategy defines the scatter rules to make pods been scattered when update. This will avoid pods with the same key-value to be updated in one batch. - Note that pods will be scattered after priority sort. So, although priority strategy and scatter strategy can be applied together, we suggest to use either one of them. - If scatterStrategy is used, we suggest to just use one term. Otherwise, the update order can be hard to understand.
spec.updateStrategy.scatterStrategy[].key <string> (required)
spec.updateStrategy.scatterStrategy[].value <string> (required)
spec.updateStrategy.type <string>	# Type indicates the type of the CloneSetUpdateStrategy. Default is ReCreate.
spec.volumeClaimTemplates <any>	# VolumeClaimTemplates is a list of claims that pods are allowed to reference. Note that PVC will be deleted when its pod has been deleted.
status <object>	# CloneSetStatus defines the observed state of CloneSet
status.availableReplicas <integer> (required) format=int32	# AvailableReplicas is the number of Pods created by the CloneSet controller that have a Ready Condition for at least minReadySeconds.
status.collisionCount <integer> format=int32	# CollisionCount is the count of hash collisions for the CloneSet. The CloneSet controller uses this field as a collision avoidance mechanism when it needs to create the name for the newest ControllerRevision.
status.conditions <[]object>	# Conditions represents the latest available observations of a CloneSet's current state.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string> format=date-time	# Last time the condition is updated.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of CloneSet condition.
status.currentRevision <string>	# currentRevision, if not empty, indicates the current revision version of the CloneSet.
status.expectedUpdatedReplicas <integer> format=int32	# ExpectedUpdatedReplicas is the number of Pods that should be updated by CloneSet controller. This field is calculated via Replicas - Partition.
status.labelSelector <string>	# LabelSelector is label selectors for query over pods that should match the replica count used by HPA.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the most recent generation observed for this CloneSet. It corresponds to the CloneSet's generation, which is updated on mutation by the API Server.
status.readyReplicas <integer> (required) format=int32	# ReadyReplicas is the number of Pods created by the CloneSet controller that have a Ready Condition.
status.replicas <integer> (required) format=int32	# Replicas is the number of Pods created by the CloneSet controller.
status.updateRevision <string>	# UpdateRevision, if not empty, indicates the latest revision of the CloneSet.
status.updatedAvailableReplicas <integer> format=int32	# UpdatedAvailableReplicas is the number of Pods created by the CloneSet controller from the CloneSet version indicated by updateRevision and have a Ready Condition for at least minReadySeconds. Notice: when enable InPlaceWorkloadVerticalScaling, pod during resource resizing will also be unavailable. This means these pod will be counted in maxUnavailable.
status.updatedReadyReplicas <integer> (required) format=int32	# UpdatedReadyReplicas is the number of Pods created by the CloneSet controller from the CloneSet version indicated by updateRevision and have a Ready Condition.
status.updatedReplicas <integer> (required) format=int32	# UpdatedReplicas is the number of Pods created by the CloneSet controller from the CloneSet version indicated by updateRevision.
