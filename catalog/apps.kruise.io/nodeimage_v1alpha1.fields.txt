# schema source: OpenKruise v1.9.1 https://github.com/openkruise/kruise
apiVersion <string> enum=apps.kruise.io/v1alpha1
kind <string> enum=NodeImage (cluster-scoped)
metadata.name <string> (required)
spec <object>	# NodeImageSpec defines the desired state of NodeImage
spec.images <map[string]object>	# Specifies images to be pulled on this node It can not be more than 256 for each NodeImage
spec.images.<key>.pullSecrets <[]object>	# PullSecrets is an optional list of references to secrets in the same namespace to use for pulling the image. If specified, these secrets will be passed to individual puller implementations for them to use. For example, in the case of docker, only DockerConfig type secrets are honored.
spec.images.<key>.pullSecrets[].name <string>
spec.images.<key>.pullSecrets[].namespace <string>
spec.images.<key>.sandboxConfig <object>	# SandboxConfig support attach metadata in PullImage CRI interface during ImagePulljobs
spec.images.<key>.sandboxConfig.annotations <map[string]string>
spec.images.<key>.sandboxConfig.labels <map[string]string>
spec.images.<key>.tags <[]object> (required)	# Tags is a list of versions of this image
spec.images.<key>.tags[].createdAt <string> format=date-time	# Specifies the create time of this tag
spec.images.<key>.tags[].imagePullPolicy <string>	# Image pull policy. One of Always, IfNotPresent. Defaults to IfNotPresent.
spec.images.<key>.tags[].ownerReferences <[]object>	# List of objects depended by this object. If this image is managed by a controller, then an entry in this list will point to this controller.
spec.images.<key>.tags[].ownerReferences[].apiVersion <string>	# API version of the referent.
spec.images.<key>.tags[].ownerReferences[].fieldPath <string>	# If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object.
spec.images.<key>.tags[].ownerReferences[].kind <string>	# Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.images.<key>.tags[].ownerReferences[].name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.images.<key>.tags[].ownerReferences[].namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.images.<key>.tags[].ownerReferences[].resourceVersion <string>	# Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
spec.images.<key>.tags[].ownerReferences[].uid <string>	# UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
spec.images.<key>.tags[].pullPolicy <object>	# PullPolicy is an optional field to set parameters of the pulling task. If not specified, the system will use the default values.
spec.images.<key>.tags[].pullPolicy.activeDeadlineSeconds <integer> format=int64	# ActiveDeadlineSeconds specifies the duration in seconds relative to the startTime that the task may be active before the system tries to terminate it; value must be positive integer. if not specified, the system will never terminate it.
spec.images.<key>.tags[].pullPolicy.backoffLimit <integer> format=int32	# Specifies the number of retries before marking the pulling task failed. Defaults to 3
spec.images.<key>.tags[].pullPolicy.timeoutSeconds <integer> format=int32	# Specifies the timeout of the pulling task. Defaults to 600
spec.images.<key>.tags[].pullPolicy.ttlSecondsAfterFinished <integer> format=int32	# TTLSecondsAfterFinished limits the lifetime of a pulling task that has finished execution (either Complete or Failed). If this field is set, ttlSecondsAfterFinished after the task finishes, it is eligible to be automatically deleted. If this field is unset, the task won't be automatically deleted. If this field is set to zero, the task becomes eligible to be deleted immediately after it finishes.
spec.images.<key>.tags[].tag <string> (required)	# Specifies the image tag
spec.images.<key>.tags[].version <integer> format=int64	# An opaque value that represents the internal version of this tag that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. Populated by the system. Read-only. Value must be treated as opaque by clients and .
status <object>	# NodeImageStatus defines the observed state of NodeImage
status.desired <integer> (required) format=int32	# The desired number of pulling tasks, this is typically equal to the number of images in spec.
status.failed <integer> format=int32	# The number of pulling tasks which reached phase Failed.
status.firstSyncStatus <object>	# The first of all job has finished on this node. When a node is added to the cluster, we want to know the time when the node's image pulling is completed, and use it to trigger the operation of the upper system.
status.firstSyncStatus.message <string>
status.firstSyncStatus.status <string>	# SyncStatusPhase defines the node status
status.firstSyncStatus.syncAt <string> format=date-time
status.imageStatuses <map[string]object>	# all statuses of active image pulling tasks
status.imageStatuses.<key>.tags <[]object> (required)	# Represents statuses of pulling tasks on this node
status.imageStatuses.<key>.tags[].completionTime <string> format=date-time	# Represents time when the pulling task was completed. It is not guaranteed to be set in happens-before order across separate operations. It is represented in RFC3339 form and is in UTC.
status.imageStatuses.<key>.tags[].imageID <string>	# Represents the ID of this image.
status.imageStatuses.<key>.tags[].message <string>	# Represents the summary information of this node
status.imageStatuses.<key>.tags[].phase <string> (required)	# Represents the image pulling task phase.
status.imageStatuses.<key>.tags[].progress <integer> format=int32	# Represents the pulling progress of this tag, which is between 0-100. There is no guarantee of monotonic consistency, and it may be a rollback due to retry during pulling.
status.imageStatuses.<key>.tags[].startTime <string> format=date-time	# Represents time when the pulling task was acknowledged by the image puller. It is not guaranteed to be set in happens-before order across separate operations. It is represented in RFC3339 form and is in UTC.
status.imageStatuses.<key>.tags[].tag <string> (required)	# Represents the image tag.
status.imageStatuses.<key>.tags[].version <integer> format=int64	# Represents the internal version of this tag that the daemon handled.
status.pulling <integer> format=int32	# The number of pulling tasks which are not finished.
status.succeeded <integer> format=int32	# The number of pulling tasks which reached phase Succeeded.
status.waiting <integer> format=int32	# The number of pulling tasks which are waiting.
