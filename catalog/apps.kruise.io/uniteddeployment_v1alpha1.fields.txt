# schema source: OpenKruise v1.9.1 https://github.com/openkruise/kruise
apiVersion <string> enum=apps.kruise.io/v1alpha1
kind <string> enum=UnitedDeployment
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# UnitedDeploymentSpec defines the desired state of UnitedDeployment.
spec.replicas <integer> format=int32	# Replicas is the total desired replicas of all the subsets. If unspecified, defaults to 1.
spec.revisionHistoryLimit <integer> format=int32	# Indicates the number of histories to be conserved. If unspecified, defaults to 10.
spec.selector <object> (required)	# Selector is a label query over pods that should match the replica count. It must match the pod template's labels.
spec.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template <object>	# Template describes the subset that will be created.
spec.template.advancedStatefulSetTemplate <object>	# AdvancedStatefulSet template
spec.template.advancedStatefulSetTemplate.metadata <any>
spec.template.advancedStatefulSetTemplate.spec <object> (required)	# StatefulSetSpec defines the desired state of StatefulSet
spec.template.advancedStatefulSetTemplate.spec.lifecycle <object>	# Lifecycle defines the lifecycle hooks for Pods pre-delete, in-place update.
spec.template.advancedStatefulSetTemplate.spec.lifecycle.inPlaceUpdate <object>	# InPlaceUpdate is the hook before Pod to update and after Pod has been updated.
spec.template.advancedStatefulSetTemplate.spec.lifecycle.inPlaceUpdate.finalizersHandler <[]string>
spec.template.advancedStatefulSetTemplate.spec.lifecycle.inPlaceUpdate.labelsHandler <map[string]string>
spec.template.advancedStatefulSetTemplate.spec.lifecycle.inPlaceUpdate.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preDelete <object>	# PreDelete is the hook before Pod to be deleted.
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preDelete.finalizersHandler <[]string>
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preDelete.labelsHandler <map[string]string>
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preDelete.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preNormal <object>	# PreNormal is the hook after Pod to be created and ready to be Normal.
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preNormal.finalizersHandler <[]string>
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preNormal.labelsHandler <map[string]string>
spec.template.advancedStatefulSetTemplate.spec.lifecycle.preNormal.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.template.advancedStatefulSetTemplate.spec.ordinals <object>	# ordinals controls the numbering of replica indices in a StatefulSet. The default ordinals behavior assigns a "0" index to the first replica and increments the index by one for each additional replica requested. Using the ordinals field requires the StatefulSetStartOrdinal feature gate to be enabled, which is beta.
spec.template.advancedStatefulSetTemplate.spec.ordinals.start <integer> format=int32	# start is the number representing the first replica's index. It may be used to number replicas from an alternate index (eg: 1-indexed) over the default 0-indexed names, or to orchestrate progressive movement of replicas from one StatefulSet to another. If set, replica indices will be in the range: [.spec.ordinals.start, .spec.ordinals.start + .spec.replicas). If unset, defaults to 0. Replica indices will be in the range: [0, .spec.replicas).
spec.template.advancedStatefulSetTemplate.spec.persistentVolumeClaimRetentionPolicy <object>	# PersistentVolumeClaimRetentionPolicy describes the policy used for PVCs created from the StatefulSet VolumeClaimTemplates. This requires the StatefulSetAutoDeletePVC feature gate to be enabled, which is alpha.
spec.template.advancedStatefulSetTemplate.spec.persistentVolumeClaimRetentionPolicy.whenDeleted <string>	# WhenDeleted specifies what happens to PVCs created from StatefulSet VolumeClaimTemplates when the StatefulSet is deleted. The default policy of `Retain` causes PVCs to not be affected by StatefulSet deletion. The `Delete` policy causes those PVCs to be deleted.
spec.template.advancedStatefulSetTemplate.spec.persistentVolumeClaimRetentionPolicy.whenScaled <string>	# WhenScaled specifies what happens to PVCs created from StatefulSet VolumeClaimTemplates when the StatefulSet is scaled down. The default policy of `Retain` causes PVCs to not be affected by a scaledown. The `Delete` policy causes the associated PVCs for any excess pods above the replica count to be deleted.
spec.template.advancedStatefulSetTemplate.spec.podManagementPolicy <string>	# podManagementPolicy controls how pods are created during initial scale up, when replacing pods on nodes, or when scaling down. The default policy is `OrderedReady`, where pods are created in increasing order (pod-0, then pod-1, etc) and the controller will wait until each pod is ready before continuing. When scaling down, the pods are removed in the opposite order. The alternative policy is `Parallel` which will create pods in parallel to match the desired scale without waiting, and on scale down will delete all pods at once.
spec.template.advancedStatefulSetTemplate.spec.replicas <integer> format=int32	# replicas is the desired number of replicas of the given Template. These are replicas in the sense that they are instantiations of the same Template, but individual replicas also have a consistent identity. If unspecified, defaults to 1.
spec.template.advancedStatefulSetTemplate.spec.reserveOrdinals <[]object>	# reserveOrdinals controls the ordinal numbers that should be reserved, and the replicas will always be the expectation number of running Pods. For a sts with replicas=3 and its Pods in [0, 1, 2]: - If you want to migrate Pod-1 and reserve this ordinal, just set spec.reserveOrdinal to [1]. Then controller will delete Pod-1 and create Pod-3 (existing Pods will be [0, 2, 3]) - If you just want to delete Pod-1, you should set spec.reserveOrdinal to [1] and spec.replicas to 2. Then controller will delete Pod-1 (existing Pods will be [0, 2]) You can also use ranges along with numbers, such as [1, 3-5], which is a shortcut for [1, 3, 4, 5].
spec.template.advancedStatefulSetTemplate.spec.revisionHistoryLimit <integer> format=int32	# revisionHistoryLimit is the maximum number of revisions that will be maintained in the StatefulSet's revision history. The revision history consists of all revisions not represented by a currently applied StatefulSetSpec version. The default value is 10.
spec.template.advancedStatefulSetTemplate.spec.scaleStrategy <object>	# scaleStrategy indicates the StatefulSetScaleStrategy that will be employed to scale Pods in the StatefulSet.
spec.template.advancedStatefulSetTemplate.spec.scaleStrategy.maxUnavailable <int-or-string>	# The maximum number of pods that can be unavailable during scaling. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding down. It can just be allowed to work with Parallel podManagementPolicy.
spec.template.advancedStatefulSetTemplate.spec.selector <object> (required)	# selector is a label query over pods that should match the replica count. It must match the pod template's labels. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
spec.template.advancedStatefulSetTemplate.spec.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.template.advancedStatefulSetTemplate.spec.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.template.advancedStatefulSetTemplate.spec.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.template.advancedStatefulSetTemplate.spec.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.template.advancedStatefulSetTemplate.spec.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template.advancedStatefulSetTemplate.spec.serviceName <string>	# serviceName is the name of the service that governs this StatefulSet. This service must exist before the StatefulSet, and is responsible for the network identity of the set. Pods get DNS/hostnames that follow the pattern: pod-specific-string.serviceName.default.svc.cluster.local where "pod-specific-string" is managed by the StatefulSet controller.
spec.template.advancedStatefulSetTemplate.spec.template <any> (required)	# template is the object that describes the pod that will be created if insufficient replicas are detected. Each pod stamped out by the StatefulSet will fulfill this Template, but have a unique identity from the rest of the StatefulSet.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy <object>	# updateStrategy indicates the StatefulSetUpdateStrategy that will be employed to update Pods in the StatefulSet when a revision is made to Template.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate <object>	# RollingUpdate is used to communicate parameters when Type is RollingUpdateStatefulSetStrategyType.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.inPlaceUpdateStrategy <object>	# InPlaceUpdateStrategy contains strategies for in-place update.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.inPlaceUpdateStrategy.gracePeriodSeconds <integer> format=int32	# GracePeriodSeconds is the timespan between set Pod status to not-ready and update images in Pod spec when in-place update a Pod.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.maxUnavailable <int-or-string>	# The maximum number of pods that can be unavailable during the update. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding down. Also, maxUnavailable can just be allowed to work with Parallel podManagementPolicy. Defaults to 1.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.minReadySeconds <integer> format=int32	# MinReadySeconds indicates how long will the pod be considered ready after it's updated. MinReadySeconds works with both OrderedReady and Parallel podManagementPolicy. It affects the pod scale up speed when the podManagementPolicy is set to be OrderedReady. Combined with MaxUnavailable, it affects the pod update speed regardless of podManagementPolicy. Default value is 0, max is 300.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.partition <integer> format=int32	# Partition indicates the number of pods the StatefulSet should be partitioned by default. - It means controller will update $(replicas - partition) number of pod. Default value is 0.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.paused <boolean>	# Paused indicates that the StatefulSet is paused. Default value is false
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.podUpdatePolicy <string>	# PodUpdatePolicy indicates how pods should be updated Default value is "ReCreate"
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate <object>	# UnorderedUpdate contains strategies for non-ordered update. If it is not nil, pods will be updated with non-ordered sequence. Noted that UnorderedUpdate can only be allowed to work with Parallel podManagementPolicy
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy <object>	# Priorities are the rules for calculating the priority of updating pods. Each pod to be updated, will pass through these terms and get a sum of weights.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.orderPriority <[]object>	# Order priority terms, pods will be sorted by the value of orderedKey. For example: ``` orderPriority: - orderedKey: key1 - orderedKey: key2 ``` First, all pods which have key1 in labels will be sorted by the value of key1. Then, the left pods which have no key1 but have key2 in labels will be sorted by the value of key2 and put behind those pods have key1.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.orderPriority[].orderedKey <string> (required)	# Calculate priority by value of this key. Values of this key, will be sorted by GetInt(val). GetInt method will find the last int in value, such as getting 5 in value '5', getting 10 in value 'sts-10'.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority <[]object>	# Weight priority terms, pods will be sorted by the sum of all terms weight.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].matchSelector <object> (required)	# MatchSelector is used to select by pod's labels.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].matchSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].matchSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.rollingUpdate.unorderedUpdate.priorityStrategy.weightPriority[].weight <integer> (required) format=int32	# Weight associated with matching the corresponding matchExpressions, in the range 1-100.
spec.template.advancedStatefulSetTemplate.spec.updateStrategy.type <string>	# Type indicates the type of the StatefulSetUpdateStrategy. Default is RollingUpdate.
spec.template.advancedStatefulSetTemplate.spec.volumeClaimTemplates <any>	# volumeClaimTemplates is a list of claims that pods are allowed to reference. The StatefulSet controller is responsible for mapping network identities to claims in a way that maintains the identity of a pod. Every claim in this list must have at least one matching (by name) volumeMount in one container in the template. A claim in this list takes precedence over any volumes in the template, with the same name.
spec.template.advancedStatefulSetTemplate.spec.volumeClaimUpdateStrategy <object>	# VolumeClaimUpdateStrategy specifies the strategy for updating VolumeClaimTemplates within a StatefulSet. This field is currently only effective if the StatefulSetAutoResizePVCGate is enabled.
spec.template.advancedStatefulSetTemplate.spec.volumeClaimUpdateStrategy.type <string>	# Type specifies the type of update strategy, possible values include: OnPodRollingUpdateVolumeClaimUpdateStrategyType: Apply the update strategy during pod rolling updates. OnPVCDeleteVolumeClaimUpdateStrategyType: Apply the update strategy when a PersistentVolumeClaim is deleted.
spec.template.cloneSetTemplate <object>	# CloneSet template
spec.template.cloneSetTemplate.metadata <any>
spec.template.cloneSetTemplate.spec <object> (required)	# CloneSetSpec defines the desired state of CloneSet
spec.template.cloneSetTemplate.spec.lifecycle <object>	# Lifecycle defines the lifecycle hooks for Pods pre-available(pre-normal), pre-delete, in-place update.
spec.template.cloneSetTemplate.spec.lifecycle.inPlaceUpdate <object>	# InPlaceUpdate is the hook before Pod to update and after Pod has been updated.
spec.template.cloneSetTemplate.spec.lifecycle.inPlaceUpdate.finalizersHandler <[]string>
spec.template.cloneSetTemplate.spec.lifecycle.inPlaceUpdate.labelsHandler <map[string]string>
spec.template.cloneSetTemplate.spec.lifecycle.inPlaceUpdate.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.template.cloneSetTemplate.spec.lifecycle.preDelete <object>	# PreDelete is the hook before Pod to be deleted.
spec.template.cloneSetTemplate.spec.lifecycle.preDelete.finalizersHandler <[]string>
spec.template.cloneSetTemplate.spec.lifecycle.preDelete.labelsHandler <map[string]string>
spec.template.cloneSetTemplate.spec.lifecycle.preDelete.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.template.cloneSetTemplate.spec.lifecycle.preNormal <object>	# PreNormal is the hook after Pod to be created and ready to be Normal.
spec.template.cloneSetTemplate.spec.lifecycle.preNormal.finalizersHandler <[]string>
spec.template.cloneSetTemplate.spec.lifecycle.preNormal.labelsHandler <map[string]string>
spec.template.cloneSetTemplate.spec.lifecycle.preNormal.markPodNotReady <boolean>	# MarkPodNotReady = true means: - Pod will be set to 'NotReady' at preparingDelete/preparingUpdate state. - Pod will be restored to 'Ready' at Updated state if it was set to 'NotReady' at preparingUpdate state. Currently, MarkPodNotReady only takes effect on InPlaceUpdate & PreDelete hook. Default to false.
spec.template.cloneSetTemplate.spec.minReadySeconds <integer> format=int32	# Minimum number of seconds for which a newly created pod should be ready without any of its container crashing, for it to be considered available. Defaults to 0 (pod will be considered available as soon as it is ready)
spec.template.cloneSetTemplate.spec.progressDeadlineSeconds <integer> format=int32	# ProgressDeadlineSeconds specifies the maximum time for the CloneSet to reach available or paused state due to partitioning. The controller will set a ProgressDeadlineExceeded condition when timeout occurs, while excluding paused state duration from the deadline calculation. This field is optional. If not set, the controller will not track progress deadlines or add the condition.
spec.template.cloneSetTemplate.spec.replicas <integer> format=int32	# Replicas is the desired number of replicas of the given Template. These are replicas in the sense that they are instantiations of the same Template. If unspecified, defaults to 1.
spec.template.cloneSetTemplate.spec.revisionHistoryLimit <integer> format=int32	# RevisionHistoryLimit is the maximum number of revisions that will be maintained in the CloneSet's revision history. The revision history consists of all revisions not represented by a currently applied CloneSetSpec version. The default value is 10.
spec.template.cloneSetTemplate.spec.scaleStrategy <object>	# ScaleStrategy indicates the ScaleStrategy that will be employed to create and delete Pods in the CloneSet.
spec.template.cloneSetTemplate.spec.scaleStrategy.disablePVCReuse <boolean>	# Indicate if cloneSet will reuse already existed pvc to rebuild a new pod
spec.template.cloneSetTemplate.spec.scaleStrategy.maxUnavailable <int-or-string>	# The maximum number of pods that can be unavailable for scaled pods. This field can control the changes rate of replicas for CloneSet so as to minimize the impact for users' service. The scale will fail if the number of unavailable pods were greater than this MaxUnavailable at scaling up. MaxUnavailable works only when scaling up.
spec.template.cloneSetTemplate.spec.scaleStrategy.podsToDelete <[]string>	# PodsToDelete is the names of Pod should be deleted. Note that this list will be truncated for non-existing pod names.
spec.template.cloneSetTemplate.spec.selector <object> (required)	# Selector is a label query over pods that should match the replica count. It must match the pod template's labels. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
spec.template.cloneSetTemplate.spec.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.template.cloneSetTemplate.spec.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.template.cloneSetTemplate.spec.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.template.cloneSetTemplate.spec.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.template.cloneSetTemplate.spec.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template.cloneSetTemplate.spec.template <any> (required)	# Template describes the pods that will be created.
spec.template.cloneSetTemplate.spec.updateStrategy <object>	# UpdateStrategy indicates the UpdateStrategy that will be employed to update Pods in the CloneSet when a revision is made to Template.
spec.template.cloneSetTemplate.spec.updateStrategy.inPlaceUpdateStrategy <object>	# InPlaceUpdateStrategy contains strategies for in-place update.
spec.template.cloneSetTemplate.spec.updateStrategy.inPlaceUpdateStrategy.gracePeriodSeconds <integer> format=int32	# GracePeriodSeconds is the timespan between set Pod status to not-ready and update images in Pod spec when in-place update a Pod.
spec.template.cloneSetTemplate.spec.updateStrategy.maxSurge <int-or-string>	# The maximum number of pods that can be scheduled above the desired replicas during update or specified delete. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding up. Defaults to 0.
spec.template.cloneSetTemplate.spec.updateStrategy.maxUnavailable <int-or-string>	# The maximum number of pods that can be unavailable during update or scale. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding up by default. When maxSurge > 0, absolute number is calculated from percentage by rounding down. Defaults to 20%.
spec.template.cloneSetTemplate.spec.updateStrategy.partition <int-or-string>	# Partition is the desired number of pods in old revisions. Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%). Absolute number is calculated from percentage by rounding up by default. It means when partition is set during pods updating, (replicas - partition value) number of pods will be updated. Default value is 0.
spec.template.cloneSetTemplate.spec.updateStrategy.paused <boolean>	# Paused indicates that the CloneSet is paused. Default value is false
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy <object>	# Priorities are the rules for calculating the priority of updating pods. Each pod to be updated, will pass through these terms and get a sum of weights.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.orderPriority <[]object>	# Order priority terms, pods will be sorted by the value of orderedKey. For example: ``` orderPriority: - orderedKey: key1 - orderedKey: key2 ``` First, all pods which have key1 in labels will be sorted by the value of key1. Then, the left pods which have no key1 but have key2 in labels will be sorted by the value of key2 and put behind those pods have key1.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.orderPriority[].orderedKey <string> (required)	# Calculate priority by value of this key. Values of this key, will be sorted by GetInt(val). GetInt method will find the last int in value, such as getting 5 in value '5', getting 10 in value 'sts-10'.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority <[]object>	# Weight priority terms, pods will be sorted by the sum of all terms weight.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector <object> (required)	# MatchSelector is used to select by pod's labels.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].matchSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template.cloneSetTemplate.spec.updateStrategy.priorityStrategy.weightPriority[].weight <integer> (required) format=int32	# Weight associated with matching the corresponding matchExpressions, in the range 1-100.
spec.template.cloneSetTemplate.spec.updateStrategy.scatterStrategy <[]object>	# ScatterStrategy defines the scatter rules to make pods been scattered when update. This will avoid pods with the same key-value to be updated in one batch. - Note that pods will be scattered after priority sort. So, although priority strategy and scatter strategy can be applied together, we suggest to use either one of them. - If scatterStrategy is used, we suggest to just use one term. Otherwise, the update order can be hard to understand.
spec.template.cloneSetTemplate.spec.updateStrategy.scatterStrategy[].key <string> (required)
spec.template.cloneSetTemplate.spec.updateStrategy.scatterStrategy[].value <string> (required)
spec.template.cloneSetTemplate.spec.updateStrategy.type <string>	# Type indicates the type of the CloneSetUpdateStrategy. Default is ReCreate.
spec.template.cloneSetTemplate.spec.volumeClaimTemplates <any>	# VolumeClaimTemplates is a list of claims that pods are allowed to reference. Note that PVC will be deleted when its pod has been deleted.
spec.template.deploymentTemplate <object>	# Deployment template
spec.template.deploymentTemplate.metadata <any>
spec.template.deploymentTemplate.spec <any> (required)
spec.template.statefulSetTemplate <object>	# StatefulSet template
spec.template.statefulSetTemplate.metadata <any>
spec.template.statefulSetTemplate.spec <any> (required)
spec.topology <object>	# Topology describes the pods distribution detail between each of subsets.
spec.topology.scheduleStrategy <object>	# ScheduleStrategy indicates the strategy the UnitedDeployment used to preform the schedule between each of subsets.
spec.topology.scheduleStrategy.adaptive <object>	# Adaptive is used to communicate parameters when Type is AdaptiveUnitedDeploymentScheduleStrategyType.
spec.topology.scheduleStrategy.adaptive.rescheduleCriticalSeconds <integer> format=int32	# RescheduleCriticalSeconds indicates how long controller will reschedule a schedule failed Pod to the subset that has redundant capacity after the subset where the Pod lives. If a Pod was scheduled failed and still in an unschedulabe status over RescheduleCriticalSeconds duration, the controller will reschedule it to a suitable subset. Default is 30 seconds.
spec.topology.scheduleStrategy.adaptive.reserveUnschedulablePods <boolean>	# ReserveUnschedulablePods indicates whether to enable reservation rescheduling mode, which is disabled by default. If this feature is enabled, those pending pods that would otherwise be permanently transferred to other subsets due to scheduling failure will be retained, and a temporary substitute Pod will be created in another subset to take over its work. When the retained pod is successfully scheduled and ready, its temporary substitute will be deleted.
spec.topology.scheduleStrategy.adaptive.unschedulableDuration <integer> format=int32	# UnschedulableDuration is used to set the number of seconds for a Subset to recover from an unschedulable state, with a default value of 300 seconds.
spec.topology.scheduleStrategy.type <string> enum=Adaptive|Fixed|	# Type indicates the type of the UnitedDeploymentScheduleStrategy. Default is Fixed
spec.topology.subsets <[]object>	# Contains the details of each subset. Each element in this array represents one subset which will be provisioned and managed by UnitedDeployment.
spec.topology.subsets[].maxReplicas <int-or-string>	# Indicates the upper bounded replicas of the subset. MaxReplicas must be more than or equal to MinReplicas. MaxReplicas == nil means no limitation. Please ensure that at least one subset has empty MaxReplicas(no limitation) to avoid stuck scaling. Defaults to nil.
spec.topology.subsets[].minReplicas <int-or-string>	# Indicates the lower bounded replicas of the subset. MinReplicas must be more than or equal to 0 if it is set. Controller will prioritize satisfy minReplicas for each subset according to the order of Topology.Subsets. Defaults to 0.
spec.topology.subsets[].name <string> (required)	# Indicates subset name as a DNS_LABEL, which will be used to generate subset workload name prefix in the format '<deployment-name>-<subset-name>-'. Name should be unique between all of the subsets under one UnitedDeployment.
spec.topology.subsets[].nodeSelectorTerm <object>	# Indicates the node selector to form the subset. Depending on the node selector, pods provisioned could be distributed across multiple groups of nodes. A subset's nodeSelectorTerm is not allowed to be updated.
spec.topology.subsets[].nodeSelectorTerm.matchExpressions <[]object>	# A list of node selector requirements by node's labels.
spec.topology.subsets[].nodeSelectorTerm.matchExpressions[].key <string> (required)	# The label key that the selector applies to.
spec.topology.subsets[].nodeSelectorTerm.matchExpressions[].operator <string> (required)	# Represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
spec.topology.subsets[].nodeSelectorTerm.matchExpressions[].values <[]string>	# An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch.
spec.topology.subsets[].nodeSelectorTerm.matchFields <[]object>	# A list of node selector requirements by node's fields.
spec.topology.subsets[].nodeSelectorTerm.matchFields[].key <string> (required)	# The label key that the selector applies to.
spec.topology.subsets[].nodeSelectorTerm.matchFields[].operator <string> (required)	# Represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
spec.topology.subsets[].nodeSelectorTerm.matchFields[].values <[]string>	# An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch.
spec.topology.subsets[].patch <any>	# Patch indicates patching to the templateSpec. Patch takes precedence over other fields If the Patch also modifies the Replicas, NodeSelectorTerm or Tolerations, use value in the Patch
spec.topology.subsets[].replicas <int-or-string>	# Indicates the number of the pod to be created under this subset. Replicas could also be percentage like '10%', which means 10% of UnitedDeployment replicas of pods will be distributed under this subset. If nil, the number of replicas in this subset is determined by controller. Controller will try to keep all the subsets with nil replicas have average pods. Replicas and MinReplicas/MaxReplicas are mutually exclusive in a UnitedDeployment.
spec.topology.subsets[].tolerations <[]object>	# Indicates the tolerations the pods under this subset have. A subset's tolerations is not allowed to be updated.
spec.topology.subsets[].tolerations[].effect <string>	# Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
spec.topology.subsets[].tolerations[].key <string>	# Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.
spec.topology.subsets[].tolerations[].operator <string>	# Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.
spec.topology.subsets[].tolerations[].tolerationSeconds <integer> format=int64	# TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.
spec.topology.subsets[].tolerations[].value <string>	# Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.
spec.updateStrategy <object>	# UpdateStrategy indicates the strategy the UnitedDeployment use to preform the update, when template is changed.
spec.updateStrategy.manualUpdate <object>	# Includes all of the parameters a Manual update strategy needs.
spec.updateStrategy.manualUpdate.partitions <map[string]integer>	# Indicates number of subset partition.
spec.updateStrategy.type <string>	# Type of UnitedDeployment update strategy. Default is Manual.
status <object>	# UnitedDeploymentStatus defines the observed state of UnitedDeployment.
status.collisionCount <integer> format=int32	# Count of hash collisions for the UnitedDeployment. The UnitedDeployment controller uses this field as a collision avoidance mechanism when it needs to create the name for the newest ControllerRevision.
status.conditions <[]object>	# Represents the latest available observations of a UnitedDeployment's current state.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human-readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string>	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string>	# Type of in place set condition.
status.currentRevision <string> (required)	# CurrentRevision, if not empty, indicates the current version of the UnitedDeployment.
status.labelSelector <string>	# LabelSelector is label selectors for query over pods that should match the replica count used by HPA.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the most recent generation observed for this UnitedDeployment. It corresponds to the UnitedDeployment's generation, which is updated on mutation by the API Server.
status.readyReplicas <integer> format=int32	# The number of ready replicas.
status.replicas <integer> (required) format=int32	# Replicas is the most recently observed number of replicas.
status.reservedPods <integer> format=int32	# The number of reserved pods in temporary adaptive strategy.
status.subsetReplicas <map[string]integer>	# Records the topology detail information of the replicas of each subset.
status.subsetStatuses <[]object>	# Record the conditions of each subset.
status.subsetStatuses[].conditions <[]object>	# Conditions is an array of current observed subset conditions.
status.subsetStatuses[].conditions[].lastTransitionTime <string> format=date-time
status.subsetStatuses[].conditions[].message <string>
status.subsetStatuses[].conditions[].reason <string>
status.subsetStatuses[].conditions[].status <string> (required)
status.subsetStatuses[].conditions[].type <string> (required)
status.subsetStatuses[].name <string>	# Subset name specified in Topology.Subsets
status.subsetStatuses[].partition <integer> format=int32	# Records the current partition. Currently unused.
status.subsetStatuses[].readyReplicas <integer> format=int32	# Records the current ready replicas. Currently unused.
status.subsetStatuses[].replicas <integer> format=int32	# Records the current replicas. Currently unused.
status.subsetStatuses[].reservedPods <integer> format=int32	# Records the reserved pods in the subset.
status.updateStatus <object>	# Records the information of update progress.
status.updateStatus.currentPartitions <map[string]integer>	# Records the current partition.
status.updateStatus.updatedRevision <string>	# Records the latest revision.
status.updatedReadyReplicas <integer> format=int32	# The number of ready current revision replicas for this UnitedDeployment.
status.updatedReplicas <integer> (required) format=int32	# The number of pods in current version.
