# schema source: NATS v0.23.0 https://github.com/nats-io/nack
apiVersion <string> enum=jetstream.nats.io/v1beta2
kind <string> enum=Account
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.creds <object>	# The creds to be used to connect to the NATS Service.
spec.creds.file <string>	# Credentials file, generated with github.com/nats-io/nsc tool.
spec.creds.secret <object>
spec.creds.secret.name <string>	# Name of the secret with the creds.
spec.name <string> pattern="^[^.*>]*$" min=1	# A unique name for the Account.
spec.nkey <object>	# The NKey seed to be used to connect to the NATS Service.
spec.nkey.secret <object>
spec.nkey.secret.name <string>	# Name of the secret containing the NKey seed.
spec.nkey.seed <string>	# Key in the secret that contains the NKey seed.
spec.servers <[]string>	# A list of servers to connect.
spec.tls <object>	# The TLS certs to be used to connect to the NATS Service.
spec.tls.ca <string>	# Filename of the Root CA of the TLS cert.
spec.tls.cert <string>	# Filename of the TLS cert.
spec.tls.key <string>	# Filename of the TLS cert key.
spec.tls.secret <object>
spec.tls.secret.name <string>	# Name of the TLS secret with the certs.
spec.tlsFirst <boolean> default=false	# When true, the KV Store will initiate TLS before server INFO.
spec.token <object>	# The token to be used to connect to the NATS Service.
spec.token.secret <object>
spec.token.secret.name <string>	# Name of the secret with the token.
spec.token.token <string>	# Key in the secret that contains the token.
spec.user <object>	# The user and password to be used to connect to the NATS Service.
spec.user.password <string>	# Key in the secret that contains the password.
spec.user.secret <object>
spec.user.secret.name <string>	# Name of the secret with the user and password.
spec.user.user <string>	# Key in the secret that contains the user.
status <object>
status.conditions <[]object>
status.conditions[].lastTransitionTime <string>
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string>
status.conditions[].type <string>
status.observedGeneration <integer>
