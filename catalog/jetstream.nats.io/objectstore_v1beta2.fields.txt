# schema source: NATS v0.23.0 https://github.com/nats-io/nack
apiVersion <string> enum=jetstream.nats.io/v1beta2
kind <string> enum=ObjectStore
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.account <string> pattern="^[^.*>]*$"	# Name of the account to which the Object Store belongs.
spec.bucket <string>	# A unique name for the Object Store.
spec.compression <boolean>	# Object Store compression.
spec.creds <string> default=""	# NATS user credentials for connecting to servers. Please make sure your controller has mounted the creds on its path.
spec.description <string>	# The description of the Object Store.
spec.jsDomain <string>	# The JetStream domain to use for the Object Store.
spec.maxBytes <integer>	# The maximum size of the Store in bytes.
spec.metadata <map[string]string>	# Additional Object Store metadata.
spec.nkey <string> default=""	# NATS user NKey for connecting to servers.
spec.placement <object>	# The Object Store placement via tags or cluster name.
spec.placement.cluster <string>
spec.placement.tags <[]string>
spec.preventDelete <boolean> default=false	# When true, the managed Object Store will not be deleted when the resource is deleted.
spec.preventUpdate <boolean> default=false	# When true, the managed Object Store will not be updated when the resource is updated.
spec.replicas <integer> default=1 min=1 max=5	# The number of replicas to keep for the Object Store in clustered JetStream.
spec.servers <[]string> default=[]	# A list of servers for creating the Object Store.
spec.storage <string> enum=file|memory	# The storage backend to use for the Object Store.
spec.tls <object>	# A client's TLS certs and keys.
spec.tls.clientCert <string>	# A client's cert filepath. Should be mounted.
spec.tls.clientKey <string>	# A client's key filepath. Should be mounted.
spec.tls.rootCas <[]string>	# A list of filepaths to CAs. Should be mounted.
spec.tlsFirst <boolean> default=false	# When true, the Object Store will initiate TLS before server INFO.
spec.ttl <string>	# The time expiry for keys.
status <object>
status.conditions <[]object>
status.conditions[].lastTransitionTime <string>
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string>
status.conditions[].type <string>
status.observedGeneration <integer>
