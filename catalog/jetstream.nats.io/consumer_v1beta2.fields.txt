# schema source: NATS v0.23.0 https://github.com/nats-io/nack
apiVersion <string> enum=jetstream.nats.io/v1beta2
kind <string> enum=Consumer
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.account <string> pattern="^[^.*>]*$"	# Name of the account to which the Consumer belongs.
spec.ackPolicy <string> enum=none|all|explicit|flow_control default="none"	# How messages should be acknowledged.
spec.ackWait <string> default="1ns"	# How long to allow messages to remain un-acknowledged before attempting redelivery.
spec.backoff <[]string>	# List of durations representing a retry time scale for NaK'd or retried messages.
spec.creds <string> default=""	# NATS user credentials for connecting to servers. Please make sure your controller has mounted the creds on its path.
spec.deliverGroup <string>	# The name of a queue group.
spec.deliverPolicy <string> enum=all|last|new|byStartSequence|byStartTime|lastPerSubject default="all"
spec.deliverSubject <string>	# The subject to deliver observed messages, when not set, a pull-based Consumer is created.
spec.description <string>	# The description of the consumer.
spec.durableName <string> pattern="^[^.*>]+$" min=1	# The name of the Consumer.
spec.filterSubject <string>	# Select only a specific incoming subjects, supports wildcards.
spec.filterSubjects <[]string>	# List of incoming subjects, supports wildcards. Available since 2.10.
spec.flowControl <boolean> default=false	# Enables flow control.
spec.headersOnly <boolean> default=false	# When set, only the headers of messages in the stream are delivered, and not the bodies. Additionally, Nats-Msg-Size header is added to indicate the size of the removed payload.
spec.heartbeatInterval <string>	# The interval used to deliver idle heartbeats for push-based consumers, in Go's time.Duration format.
spec.inactiveThreshold <string>	# The idle time an Ephemeral Consumer allows before it is removed.
spec.jsDomain <string>	# The JetStream domain to use for the consumer.
spec.maxAckPending <integer>	# Maximum pending Acks before consumers are paused.
spec.maxDeliver <integer> min=-1
spec.maxRequestBatch <integer>	# The largest batch property that may be specified when doing a pull on a Pull Consumer.
spec.maxRequestExpires <string>	# The maximum expires duration that may be set when doing a pull on a Pull Consumer.
spec.maxRequestMaxBytes <integer>	# The maximum max_bytes value that maybe set when dong a pull on a Pull Consumer.
spec.maxWaiting <integer>	# The number of pulls that can be outstanding on a pull consumer, pulls received after this is reached are ignored.
spec.memStorage <boolean> default=false	# Force the consumer state to be kept in memory rather than inherit the setting from the stream.
spec.metadata <map[string]string>	# Additional Consumer metadata.
spec.nkey <string> default=""	# NATS user NKey for connecting to servers.
spec.optStartSeq <integer> min=0
spec.optStartTime <string>	# Time format must be RFC3339.
spec.pauseUntil <string> default=""	# RFC3339 timestamp until which the consumer should be paused.
spec.pinnedTtl <string> default=""	# TTL for pinned client when using pinned_client priority policy.
spec.preventDelete <boolean> default=false	# When true, the managed Consumer will not be deleted when the resource is deleted.
spec.preventUpdate <boolean> default=false	# When true, the managed Consumer will not be updated when the resource is updated.
spec.priorityGroups <[]string>	# List of priority groups for the consumer. For now, only one group is supported.
spec.priorityPolicy <string> default=""	# Priority policy for consumer (pinned_client, overflow, prioritized, or none).
spec.rateLimitBps <integer>	# Rate at which messages will be delivered to clients, expressed in bit per second.
spec.replayPolicy <string> enum=instant|original default="instant"	# How messages are sent.
spec.replicas <integer>	# When set do not inherit the replica count from the stream but specifically set it to this amount.
spec.sampleFreq <string>	# What percentage of acknowledgements should be samples for observability.
spec.servers <[]string> default=[]	# A list of servers for creating consumer.
spec.streamName <string>	# The name of the Stream to create the Consumer in.
spec.tls <object>	# A client's TLS certs and keys.
spec.tls.clientCert <string>	# A client's cert filepath. Should be mounted.
spec.tls.clientKey <string>	# A client's key filepath. Should be mounted.
spec.tls.rootCas <[]string>	# A list of filepaths to CAs. Should be mounted.
spec.tlsFirst <boolean> default=false	# When true, the KV Store will initiate TLS before server INFO.
status <object>
status.conditions <[]object>
status.conditions[].lastTransitionTime <string>
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string>
status.conditions[].type <string>
status.observedGeneration <integer>
