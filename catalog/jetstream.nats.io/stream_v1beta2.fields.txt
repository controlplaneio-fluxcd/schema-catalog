# schema source: NATS v0.23.0 https://github.com/nats-io/nack
apiVersion <string> enum=jetstream.nats.io/v1beta2
kind <string> enum=Stream
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.account <string> pattern="^[^.*>]*$"	# Name of the account to which the Stream belongs.
spec.allowAtomicPublish <boolean> default=false	# When true, enables atomic batch publishing.
spec.allowBatched <boolean> default=false	# When true, enables fast-ingest batch publishing (nats-server 2.14+).
spec.allowDirect <boolean> default=false	# When true, allow higher performance, direct access to get individual messages.
spec.allowMsgCounter <boolean> default=false	# When true, enables message counters for the stream.
spec.allowMsgSchedules <boolean> default=false	# When true, enables message scheduling.
spec.allowMsgTtl <boolean> default=false	# When true, allows header initiated per-message TTLs. If disabled, then the `NATS-TTL` header will be ignored.
spec.allowRollup <boolean> default=false	# When true, allows the use of the Nats-Rollup header to replace all contents of a stream, or subject in a stream, with a single new message.
spec.compression <string> enum=s2|none| default=""	# Stream specific compression.
spec.consumerLimits <object>
spec.consumerLimits.inactiveThreshold <string>	# The duration of inactivity after which a consumer is considered inactive.
spec.consumerLimits.maxAckPending <integer>	# Maximum number of outstanding unacknowledged messages.
spec.creds <string> default=""	# NATS user credentials for connecting to servers. Please make sure your controller has mounted the creds on this path.
spec.denyDelete <boolean> default=false	# When true, restricts the ability to delete messages from a stream via the API. Cannot be changed once set to true.
spec.denyPurge <boolean> default=false	# When true, restricts the ability to purge a stream via the API. Cannot be changed once set to true.
spec.description <string>	# The description of the stream.
spec.discard <string> enum=old|new default="old"	# When a Stream reach it's limits either old messages are deleted or new ones are denied.
spec.discardPerSubject <boolean> default=false	# Applies discard policy on a per-subject basis. Requires discard policy 'new' and 'maxMsgs' to be set.
spec.duplicateWindow <string>	# The duration window to track duplicate messages for.
spec.firstSequence <number> default=0	# Sequence number from which the Stream will start.
spec.jsDomain <string>	# The JetStream domain to use for the stream.
spec.maxAge <string> default=""	# Maximum age of any message in the stream, expressed in Go's time.Duration format. Empty for unlimited.
spec.maxBytes <integer> default=-1 min=-1	# How big the Stream may be, when the combined stream size exceeds this old messages are removed. -1 for unlimited.
spec.maxConsumers <integer> default=-1 min=-1	# How many Consumers can be defined for a given Stream. -1 for unlimited.
spec.maxMsgSize <integer> default=-1 min=-1	# The largest message that will be accepted by the Stream. -1 for unlimited.
spec.maxMsgs <integer> default=-1 min=-1	# How many messages may be in a Stream, oldest messages will be removed if the Stream exceeds this size. -1 for unlimited.
spec.maxMsgsPerSubject <integer> default=0	# The maximum number of messages per subject.
spec.metadata <map[string]string>	# Additional Stream metadata.
spec.mirror <object>	# A stream mirror.
spec.mirror.consumer <object>	# Reference a durable consumer when sourcing/mirroring (nats-server 2.14+).
spec.mirror.consumer.deliverSubject <string>	# Deliver subject for the durable push consumer.
spec.mirror.consumer.name <string> (required)	# Durable consumer name on the source stream.
spec.mirror.externalApiPrefix <string>
spec.mirror.externalDeliverPrefix <string>
spec.mirror.filterSubject <string>
spec.mirror.name <string>
spec.mirror.optStartSeq <integer>
spec.mirror.optStartTime <string>	# Time format must be RFC3339.
spec.mirror.subjectTransforms <[]object>	# List of subject transforms for this mirror.
spec.mirror.subjectTransforms[].dest <string>	# Destination subject.
spec.mirror.subjectTransforms[].source <string>	# Source subject.
spec.mirrorDirect <boolean> default=false	# When true, enables direct access to messages from the origin stream.
spec.name <string> pattern="^[^.*>]*$" min=1	# A unique name for the Stream.
spec.nkey <string> default=""	# NATS user NKey for connecting to servers.
spec.noAck <boolean> default=false	# Disables acknowledging messages that are received by the Stream.
spec.persistMode <string> default=""	# Configures stream persistence settings (async or default).
spec.placement <object>	# A stream's placement.
spec.placement.cluster <string>
spec.placement.tags <[]string>
spec.preventDelete <boolean> default=false	# When true, the managed Stream will not be deleted when the resource is deleted.
spec.preventUpdate <boolean> default=false	# When true, the managed Stream will not be updated when the resource is updated.
spec.replicas <integer> default=1 min=1	# How many replicas to keep for each message.
spec.republish <object>	# Republish configuration of the stream.
spec.republish.destination <string>	# Messages will be additionally published to this subject.
spec.republish.source <string>	# Messages will be published from this subject to the destination subject.
spec.retention <string> enum=limits|interest|workqueue default="limits"	# How messages are retained in the Stream, once this is exceeded old messages are removed.
spec.sealed <boolean> default=false	# Seal an existing stream so no new messages may be added.
spec.servers <[]string> default=[]	# A list of servers for creating stream.
spec.sources <[]object>	# A stream's sources.
spec.sources[].consumer <object>	# Reference a durable consumer when sourcing/mirroring (nats-server 2.14+).
spec.sources[].consumer.deliverSubject <string>	# Deliver subject for the durable push consumer.
spec.sources[].consumer.name <string> (required)	# Durable consumer name on the source stream.
spec.sources[].externalApiPrefix <string>
spec.sources[].externalDeliverPrefix <string>
spec.sources[].filterSubject <string>
spec.sources[].name <string>
spec.sources[].optStartSeq <integer>
spec.sources[].optStartTime <string>	# Time format must be RFC3339.
spec.sources[].subjectTransforms <[]object>	# List of subject transforms for this mirror.
spec.sources[].subjectTransforms[].dest <string>	# Destination subject.
spec.sources[].subjectTransforms[].source <string>	# Source subject.
spec.storage <string> enum=file|memory default="memory"	# The storage backend to use for the Stream.
spec.subjectDeleteMarkerTtl <string> default=""	# Enables and sets a duration for adding server markers for delete, purge and max age limits.
spec.subjectTransform <object>	# SubjectTransform is for applying a subject transform (to matching messages) when a new message is received.
spec.subjectTransform.dest <string>	# Destination subject to transform into.
spec.subjectTransform.source <string>	# Source subject.
spec.subjects <[]string>	# A list of subjects to consume, supports wildcards.
spec.tls <object>	# A client's TLS certs and keys.
spec.tls.clientCert <string>	# A client's cert filepath. Should be mounted.
spec.tls.clientKey <string>	# A client's key filepath. Should be mounted.
spec.tls.rootCas <[]string>	# A list of filepaths to CAs. Should be mounted.
spec.tlsFirst <boolean> default=false	# When true, the KV Store will initiate TLS before server INFO.
status <object>
status.conditions <[]object>
status.conditions[].lastTransitionTime <string>
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string>
status.conditions[].type <string>
status.observedGeneration <integer>
