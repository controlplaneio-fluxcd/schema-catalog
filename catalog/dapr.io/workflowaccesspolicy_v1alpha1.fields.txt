# schema source: Dapr v1.18.2 https://github.com/dapr/dapr
apiVersion <string> enum=dapr.io/v1alpha1
kind <string> enum=WorkflowAccessPolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
scopes <[]string>
spec <object>	# WorkflowAccessPolicySpec defines the desired state of WorkflowAccessPolicy.
spec.rules <[]object>	# Rules defines the allow-list of which callers can perform which operations.
spec.rules[].activities <[]object>	# Activities are the activity rules that the matched callers are allowed.
spec.rules[].activities[].name <string> (required) min=1	# Name is the exact name or glob pattern for the activity.
spec.rules[].callers <[]object> (required) min=1	# Callers that this rule applies to.
spec.rules[].callers[].appID <string> (required) min=1	# AppID is the Dapr app ID of the caller.
spec.rules[].workflows <[]object>	# Workflows are the workflow rules that the matched callers are allowed.
spec.rules[].workflows[].name <string> (required) min=1	# Name is the exact name or glob pattern for the workflow.
spec.rules[].workflows[].operations <[]string> (required) min=1	# Operations is the set of operations this rule applies to.
