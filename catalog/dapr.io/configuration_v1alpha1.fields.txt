# schema source: Dapr v1.18.2 https://github.com/dapr/dapr
apiVersion <string> enum=dapr.io/v1alpha1
kind <string> enum=Configuration
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ConfigurationSpec is the spec for a configuration.
spec.accessControl <object>	# AccessControlSpec is the spec object in ConfigurationSpec.
spec.accessControl.defaultAction <string>
spec.accessControl.policies <[]object>
spec.accessControl.policies[].appId <string> (required)
spec.accessControl.policies[].defaultAction <string>
spec.accessControl.policies[].namespace <string>
spec.accessControl.policies[].operations <[]object>
spec.accessControl.policies[].operations[].action <string> (required)
spec.accessControl.policies[].operations[].httpVerb <[]string>
spec.accessControl.policies[].operations[].name <string> (required)
spec.accessControl.policies[].trustDomain <string>
spec.accessControl.trustDomain <string>
spec.api <object>	# APISpec describes the configuration for Dapr APIs.
spec.api.allowed <[]object>	# List of allowed APIs. Can be used in conjunction with denied.
spec.api.allowed[].name <string> (required)
spec.api.allowed[].protocol <string>
spec.api.allowed[].version <string> (required)
spec.api.denied <[]object>	# List of denied APIs. Can be used in conjunction with allowed.
spec.api.denied[].name <string> (required)
spec.api.denied[].protocol <string>
spec.api.denied[].version <string> (required)
spec.appHttpPipeline <object>	# PipelineSpec defines the middleware pipeline.
spec.appHttpPipeline.handlers <[]object> (required)
spec.appHttpPipeline.handlers[].name <string> (required)
spec.appHttpPipeline.handlers[].selector <object>	# SelectorSpec selects target services to which the handler is to be applied.
spec.appHttpPipeline.handlers[].selector.fields <[]object> (required)
spec.appHttpPipeline.handlers[].selector.fields[].field <string> (required)
spec.appHttpPipeline.handlers[].selector.fields[].value <string> (required)
spec.appHttpPipeline.handlers[].type <string> (required)
spec.components <object>	# ComponentsSpec describes the configuration for Dapr components
spec.components.deny <[]string>	# Denylist of component types that cannot be instantiated
spec.features <[]object>
spec.features[].enabled <boolean> (required)
spec.features[].name <string> (required)
spec.httpPipeline <object>	# PipelineSpec defines the middleware pipeline.
spec.httpPipeline.handlers <[]object> (required)
spec.httpPipeline.handlers[].name <string> (required)
spec.httpPipeline.handlers[].selector <object>	# SelectorSpec selects target services to which the handler is to be applied.
spec.httpPipeline.handlers[].selector.fields <[]object> (required)
spec.httpPipeline.handlers[].selector.fields[].field <string> (required)
spec.httpPipeline.handlers[].selector.fields[].value <string> (required)
spec.httpPipeline.handlers[].type <string> (required)
spec.logging <object>	# LoggingSpec defines the configuration for logging.
spec.logging.apiLogging <object>	# Configure API logging.
spec.logging.apiLogging.enabled <boolean>	# Default value for enabling API logging. Sidecars can always override this by setting `--enable-api-logging` to true or false explicitly. The default value is false.
spec.logging.apiLogging.obfuscateURLs <boolean>	# When enabled, obfuscates the values of URLs in HTTP API logs, logging the route name rather than the full path being invoked, which could contain PII. Default: false. This option has no effect if API logging is disabled.
spec.logging.apiLogging.omitHealthChecks <boolean>	# If true, health checks are not reported in API logs. Default: false. This option has no effect if API logging is disabled.
spec.metric <object> default={"enabled":true}	# MetricSpec defines metrics configuration.
spec.metric.enabled <boolean> (required)
spec.metric.http <object>	# MetricHTTP defines configuration for metrics for the HTTP server
spec.metric.http.excludeVerbs <boolean>	# If true (default is false) HTTP verbs (e.g., GET, POST) are excluded from the metrics.
spec.metric.http.increasedCardinality <boolean>	# If false, metrics for the HTTP server are collected with increased cardinality. The default is true in Dapr 1.13, but will be changed to false in 1.15+
spec.metric.http.pathMatching <[]string>
spec.metric.latencyDistributionBuckets <[]integer>	# The LatencyDistributionBuckets variable specifies the latency distribution buckets (in milliseconds) used for histograms in the application. If this variable is not set or left empty, the application will default to using the standard histogram buckets. The default histogram latency buckets (in milliseconds) are as follows: 1, 2, 3, 4, 5, 6, 8, 10, 13, 16, 20, 25, 30, 40, 50, 65, 80, 100, 130, 160, 200, 250, 300, 400, 500, 650, 800, 1,000, 2,000, 5,000, 10,000, 20,000, 50,000, 100,000.
spec.metric.recordErrorCodes <boolean>
spec.metric.rules <[]object>
spec.metric.rules[].labels <[]object> (required)
spec.metric.rules[].labels[].name <string> (required)
spec.metric.rules[].labels[].regex <map[string]string> (required)
spec.metric.rules[].name <string> (required)
spec.metrics <object> default={"enabled":true}	# MetricSpec defines metrics configuration.
spec.metrics.enabled <boolean> (required)
spec.metrics.http <object>	# MetricHTTP defines configuration for metrics for the HTTP server
spec.metrics.http.excludeVerbs <boolean>	# If true (default is false) HTTP verbs (e.g., GET, POST) are excluded from the metrics.
spec.metrics.http.increasedCardinality <boolean>	# If false, metrics for the HTTP server are collected with increased cardinality. The default is true in Dapr 1.13, but will be changed to false in 1.15+
spec.metrics.http.pathMatching <[]string>
spec.metrics.latencyDistributionBuckets <[]integer>	# The LatencyDistributionBuckets variable specifies the latency distribution buckets (in milliseconds) used for histograms in the application. If this variable is not set or left empty, the application will default to using the standard histogram buckets. The default histogram latency buckets (in milliseconds) are as follows: 1, 2, 3, 4, 5, 6, 8, 10, 13, 16, 20, 25, 30, 40, 50, 65, 80, 100, 130, 160, 200, 250, 300, 400, 500, 650, 800, 1,000, 2,000, 5,000, 10,000, 20,000, 50,000, 100,000.
spec.metrics.recordErrorCodes <boolean>
spec.metrics.rules <[]object>
spec.metrics.rules[].labels <[]object> (required)
spec.metrics.rules[].labels[].name <string> (required)
spec.metrics.rules[].labels[].regex <map[string]string> (required)
spec.metrics.rules[].name <string> (required)
spec.mtls <object>	# MTLSSpec defines mTLS configuration.
spec.mtls.allowedClockSkew <string>
spec.mtls.controlPlaneTrustDomain <string> (required)
spec.mtls.enabled <boolean> (required)
spec.mtls.sentryAddress <string> (required)
spec.mtls.tokenValidators <[]object>	# Additional token validators to use. When Dapr is running in Kubernetes mode, this is in addition to the built-in "kubernetes" validator. In self-hosted mode, enabling a custom validator will disable the built-in "insecure" validator.
spec.mtls.tokenValidators[].name <string> (required) enum=jwks	# Name of the validator
spec.mtls.tokenValidators[].options <object (free-form)>	# Options for the validator, if any
spec.mtls.workloadCertTTL <string>
spec.nameResolution <object>	# NameResolutionSpec is the spec for name resolution configuration.
spec.nameResolution.component <string> (required)
spec.nameResolution.configuration <object (free-form)> (required)	# DynamicValue is a dynamic value struct for the component.metadata pair value.
spec.nameResolution.version <string> (required)
spec.secrets <object>	# SecretsSpec is the spec for secrets configuration.
spec.secrets.scopes <[]object> (required)
spec.secrets.scopes[].allowedSecrets <[]string>
spec.secrets.scopes[].defaultAccess <string>
spec.secrets.scopes[].deniedSecrets <[]string>
spec.secrets.scopes[].storeName <string> (required)
spec.tracing <object>	# TracingSpec defines distributed tracing configuration.
spec.tracing.otel <object>	# OtelSpec defines Otel exporter configurations.
spec.tracing.otel.endpointAddress <string> (required)
spec.tracing.otel.headers <[]object>	# Headers to add to the OTLP trace exporter request. Each header can contain plaintext values, reference secrets, or reference environment variables.
spec.tracing.otel.headers[].envRef <string>	# EnvRef is the name of an environmental variable to read the value from.
spec.tracing.otel.headers[].name <string> (required)	# Name of the property.
spec.tracing.otel.headers[].secretKeyRef <object>	# SecretKeyRef is the reference of a value in a secret store component.
spec.tracing.otel.headers[].secretKeyRef.key <string>	# Field in the secret.
spec.tracing.otel.headers[].secretKeyRef.name <string> (required)	# Secret name.
spec.tracing.otel.headers[].value <any>	# Value of the property, in plaintext.
spec.tracing.otel.isSecure <boolean> (required)
spec.tracing.otel.protocol <string> (required)
spec.tracing.otel.timeout <string>	# Timeout for the OTLP trace exporter request.
spec.tracing.samplingRate <string> (required)
spec.tracing.stdout <boolean>
spec.tracing.zipkin <object>	# ZipkinSpec defines Zipkin trace configurations.
spec.tracing.zipkin.endpointAddress <string> (required)
spec.wasm <object>	# WasmSpec describes the security profile for all Dapr Wasm components.
spec.wasm.strictSandbox <boolean>	# Force enabling strict sandbox mode for all WASM components. When this is enabled, WASM components always run in strict mode regardless of their configuration. Strict mode enhances security of the WASM sandbox by limiting access to certain capabilities such as real-time clocks and random number generators.
spec.workflow <object>	# WorkflowSpec defines the configuration for Dapr workflows.
spec.workflow.activityConcurrencyLimits <[]object>	# activityConcurrencyLimits defines per-activity-name concurrency limits enforced globally across all replicas by the scheduler.
spec.workflow.activityConcurrencyLimits[].maxConcurrent <integer> format=int32	# MaxConcurrent is the maximum number of concurrent invocations across all replicas.
spec.workflow.activityConcurrencyLimits[].name <string>	# Name is the workflow or activity name to limit.
spec.workflow.globalMaxConcurrentActivityInvocations <integer> format=int32	# globalMaxConcurrentActivityInvocations is the maximum number of concurrent activity invocations across all replicas, enforced by the scheduler. If omitted, no global maximum will be enforced.
spec.workflow.globalMaxConcurrentWorkflowInvocations <integer> format=int32	# globalMaxConcurrentWorkflowInvocations is the maximum number of concurrent workflow invocations across all replicas, enforced by the scheduler. If omitted, no global maximum will be enforced.
spec.workflow.maxConcurrentActivityInvocations <integer> format=int32	# maxConcurrentActivityInvocations is the maximum number of concurrent activities that can be processed by a single Dapr instance. Attempted invocations beyond this will be queued until the number of concurrent invocations drops below this value. If omitted, no maximum will be enforced.
spec.workflow.maxConcurrentWorkflowInvocations <integer> format=int32	# maxConcurrentWorkflowInvocations is the maximum number of concurrent workflow invocations that can be scheduled by a single Dapr instance. Attempted invocations beyond this will be queued until the number of concurrent invocations drops below this value. If omitted, no maximum will be enforced.
spec.workflow.stateRetentionPolicy <object>	# StateRetentionPolicy defines the retention configuration for workflow state once a workflow reaches a terminal state. If not set, workflow instances will not be automatically purged.
spec.workflow.stateRetentionPolicy.anyTerminal <string>	# AnyTerminal is the TTL for purging workflow instances that reach any terminal state.
spec.workflow.stateRetentionPolicy.completed <string>	# Completed is the TTL for purging workflow instances that reach the Completed terminal state.
spec.workflow.stateRetentionPolicy.failed <string>	# Failed is the TTL for purging workflow instances that reach the Failed terminal state.
spec.workflow.stateRetentionPolicy.terminated <string>	# Terminated is the TTL for purging workflow instances that reach the Terminated terminal state.
spec.workflow.workflowConcurrencyLimits <[]object>	# workflowConcurrencyLimits defines per-workflow-name concurrency limits enforced globally across all replicas by the scheduler.
spec.workflow.workflowConcurrencyLimits[].maxConcurrent <integer> format=int32	# MaxConcurrent is the maximum number of concurrent invocations across all replicas.
spec.workflow.workflowConcurrencyLimits[].name <string>	# Name is the workflow or activity name to limit.
