# schema source: Dapr v1.18.2 https://github.com/dapr/dapr
apiVersion <string> enum=dapr.io/v1alpha1
kind <string> enum=MCPServer
metadata.name <string> (required)
metadata.namespace <string> (required)
scopes <[]string>
spec <object>	# MCPServerSpec is the full configuration for an MCP server connection.
spec.catalog <object>	# Catalog holds user-facing governance metadata (display name, description, owner, tags, links). It is purely informational and not used at runtime.
spec.catalog.description <string>
spec.catalog.displayName <string>
spec.catalog.links <map[string]string>	# Links is a free-form map of named URLs (e.g. "docs", "runbook", "dashboard").
spec.catalog.owner <object>	# MCPCatalogOwner identifies the team responsible for the MCP server.
spec.catalog.owner.contact <string>
spec.catalog.owner.team <string>
spec.catalog.tags <[]string>
spec.endpoint <object> (required)	# Endpoint describes the transport and target of the MCP server.
spec.endpoint.sse <object>	# SSE holds configuration for the legacy SSE transport.
spec.endpoint.sse.auth <object>	# Auth configures authentication for the MCP server connection.
spec.endpoint.sse.auth.oauth2 <object>	# OAuth2 configures OAuth2 client credentials flow for the MCP server.
spec.endpoint.sse.auth.oauth2.audience <string>
spec.endpoint.sse.auth.oauth2.clientID <string>	# ClientID is the OAuth2 client identifier sent to the token endpoint. Required by RFC 6749 for the standard client_credentials flow (form parameter or HTTP Basic username). May be left empty for non-standard flows (e.g. JWT-bearer assertions or token endpoints that key solely on the client_secret). Client IDs are typically public identifiers; use spec.headers with a secretKeyRef if your environment requires sourcing the value from a secret store.
spec.endpoint.sse.auth.oauth2.issuer <string> (required) min=1	# Issuer is the token endpoint of the authorization server.
spec.endpoint.sse.auth.oauth2.scopes <[]string>
spec.endpoint.sse.auth.oauth2.secretKeyRef <object>	# SecretKeyRef references the client secret in the configured secret store.
spec.endpoint.sse.auth.oauth2.secretKeyRef.key <string>	# Field in the secret.
spec.endpoint.sse.auth.oauth2.secretKeyRef.name <string> (required)	# Secret name.
spec.endpoint.sse.auth.secretStore <string>	# SecretStore names the Dapr secret store used to resolve secretKeyRef entries in spec.endpoint.http.headers. auth.oauth2.secretKeyRef is resolved separately at call time by the MCP worker. Defaults to "kubernetes".
spec.endpoint.sse.auth.spiffe <object>	# SPIFFE configures workload identity JWT injection via Dapr's Sentry CA. No secret is required; Sentry issues the SVID automatically.
spec.endpoint.sse.auth.spiffe.jwt <object>	# JWT configures how the SPIFFE SVID JWT is attached to outbound requests.
spec.endpoint.sse.auth.spiffe.jwt.audience <string> (required) min=1	# Audience is the intended audience for the JWT (e.g. "mcp://payments").
spec.endpoint.sse.auth.spiffe.jwt.header <string> (required) min=1	# Header is the HTTP header name to inject the JWT into (e.g. "Authorization").
spec.endpoint.sse.auth.spiffe.jwt.headerValuePrefix <string>	# HeaderValuePrefix is an optional string prepended to the JWT value (e.g. "Bearer ").
spec.endpoint.sse.headers <[]object>	# Headers are injected on all outbound HTTP requests to the MCP server.
spec.endpoint.sse.headers[].envRef <string>	# EnvRef is the name of an environmental variable to read the value from.
spec.endpoint.sse.headers[].name <string> (required)	# Name of the property.
spec.endpoint.sse.headers[].secretKeyRef <object>	# SecretKeyRef is the reference of a value in a secret store component.
spec.endpoint.sse.headers[].secretKeyRef.key <string>	# Field in the secret.
spec.endpoint.sse.headers[].secretKeyRef.name <string> (required)	# Secret name.
spec.endpoint.sse.headers[].value <any>	# Value of the property, in plaintext.
spec.endpoint.sse.protocolVersion <string>	# ProtocolVersion pins the MCP spec version the server implements, using the date-based format defined by the MCP specification (e.g. "2025-06-18"). When unset, the go-sdk negotiates the latest version supported by both sides.
spec.endpoint.sse.timeout <string>	# Timeout is the per-call deadline for MCP requests.
spec.endpoint.sse.url <string> (required) min=1	# URL is the endpoint URL of the MCP server.
spec.endpoint.stdio <object>	# Stdio holds configuration for the stdio subprocess transport.
spec.endpoint.stdio.args <[]string>
spec.endpoint.stdio.command <string> (required) min=1	# Command is the executable to run.
spec.endpoint.stdio.env <[]object>	# Env are environment variables injected into the subprocess. Supports plain value, secretKeyRef, and envRef.
spec.endpoint.stdio.env[].envRef <string>	# EnvRef is the name of an environmental variable to read the value from.
spec.endpoint.stdio.env[].name <string> (required)	# Name of the property.
spec.endpoint.stdio.env[].secretKeyRef <object>	# SecretKeyRef is the reference of a value in a secret store component.
spec.endpoint.stdio.env[].secretKeyRef.key <string>	# Field in the secret.
spec.endpoint.stdio.env[].secretKeyRef.name <string> (required)	# Secret name.
spec.endpoint.stdio.env[].value <any>	# Value of the property, in plaintext.
spec.endpoint.streamableHTTP <object>	# StreamableHTTP holds configuration for the streamable_http transport.
spec.endpoint.streamableHTTP.auth <object>	# Auth configures authentication for the MCP server connection.
spec.endpoint.streamableHTTP.auth.oauth2 <object>	# OAuth2 configures OAuth2 client credentials flow for the MCP server.
spec.endpoint.streamableHTTP.auth.oauth2.audience <string>
spec.endpoint.streamableHTTP.auth.oauth2.clientID <string>	# ClientID is the OAuth2 client identifier sent to the token endpoint. Required by RFC 6749 for the standard client_credentials flow (form parameter or HTTP Basic username). May be left empty for non-standard flows (e.g. JWT-bearer assertions or token endpoints that key solely on the client_secret). Client IDs are typically public identifiers; use spec.headers with a secretKeyRef if your environment requires sourcing the value from a secret store.
spec.endpoint.streamableHTTP.auth.oauth2.issuer <string> (required) min=1	# Issuer is the token endpoint of the authorization server.
spec.endpoint.streamableHTTP.auth.oauth2.scopes <[]string>
spec.endpoint.streamableHTTP.auth.oauth2.secretKeyRef <object>	# SecretKeyRef references the client secret in the configured secret store.
spec.endpoint.streamableHTTP.auth.oauth2.secretKeyRef.key <string>	# Field in the secret.
spec.endpoint.streamableHTTP.auth.oauth2.secretKeyRef.name <string> (required)	# Secret name.
spec.endpoint.streamableHTTP.auth.secretStore <string>	# SecretStore names the Dapr secret store used to resolve secretKeyRef entries in spec.endpoint.http.headers. auth.oauth2.secretKeyRef is resolved separately at call time by the MCP worker. Defaults to "kubernetes".
spec.endpoint.streamableHTTP.auth.spiffe <object>	# SPIFFE configures workload identity JWT injection via Dapr's Sentry CA. No secret is required; Sentry issues the SVID automatically.
spec.endpoint.streamableHTTP.auth.spiffe.jwt <object>	# JWT configures how the SPIFFE SVID JWT is attached to outbound requests.
spec.endpoint.streamableHTTP.auth.spiffe.jwt.audience <string> (required) min=1	# Audience is the intended audience for the JWT (e.g. "mcp://payments").
spec.endpoint.streamableHTTP.auth.spiffe.jwt.header <string> (required) min=1	# Header is the HTTP header name to inject the JWT into (e.g. "Authorization").
spec.endpoint.streamableHTTP.auth.spiffe.jwt.headerValuePrefix <string>	# HeaderValuePrefix is an optional string prepended to the JWT value (e.g. "Bearer ").
spec.endpoint.streamableHTTP.headers <[]object>	# Headers are injected on all outbound HTTP requests to the MCP server. Each entry follows the same NameValuePair contract used by HTTPEndpoint: plain value, secretKeyRef, or envRef. Secret resolution uses auth.secretStore.
spec.endpoint.streamableHTTP.headers[].envRef <string>	# EnvRef is the name of an environmental variable to read the value from.
spec.endpoint.streamableHTTP.headers[].name <string> (required)	# Name of the property.
spec.endpoint.streamableHTTP.headers[].secretKeyRef <object>	# SecretKeyRef is the reference of a value in a secret store component.
spec.endpoint.streamableHTTP.headers[].secretKeyRef.key <string>	# Field in the secret.
spec.endpoint.streamableHTTP.headers[].secretKeyRef.name <string> (required)	# Secret name.
spec.endpoint.streamableHTTP.headers[].value <any>	# Value of the property, in plaintext.
spec.endpoint.streamableHTTP.protocolVersion <string>	# ProtocolVersion pins the MCP spec version the server implements, using the date-based format defined by the MCP specification (e.g. "2025-06-18"). When unset, the go-sdk negotiates the latest version supported by both sides.
spec.endpoint.streamableHTTP.timeout <string>	# Timeout is the per-call deadline for MCP requests.
spec.endpoint.streamableHTTP.url <string> (required) min=1	# URL is the endpoint URL of the MCP server.
spec.ignoreErrors <boolean>	# IgnoreErrors, when true, allows daprd to continue running if validation or secret resolution fails for this MCPServer. When false (default), such failures cause daprd to exit gracefully.
spec.middleware <object>	# Middleware defines optional workflow hooks invoked around each tool call.
spec.middleware.afterCallTool <[]object>	# AfterCallTool hooks are invoked in order after each CallTool. Receives {mcpServer, toolName, arguments, result} as input. Errors fail the workflow — after-hooks act as authorization gates.
spec.middleware.afterCallTool[].mutate <boolean>	# Mutate, when true, causes the hook's return value to replace the data flowing through the pipeline: - beforeCallTool: replaces the arguments sent to the tool call (e.g. redact PII, inject defaults). - afterCallTool / afterListTools: replaces the result returned to the caller. When false (default), the hook validates/observes only — its output is discarded.
spec.middleware.afterCallTool[].workflow <object> (required)	# Workflow is the Dapr workflow to invoke as the hook.
spec.middleware.afterCallTool[].workflow.appID <string>	# AppID targets the workflow on a remote Dapr app via service invocation. When unset, the workflow is invoked locally.
spec.middleware.afterCallTool[].workflow.workflowName <string> (required) min=1	# WorkflowName is the name of the workflow to invoke.
spec.middleware.afterListTools <[]object>	# AfterListTools hooks are invoked in order after each ListTools. Receives {mcpServer, result} as input. Errors are logged but do not affect the result.
spec.middleware.afterListTools[].mutate <boolean>	# Mutate, when true, causes the hook's return value to replace the data flowing through the pipeline: - beforeCallTool: replaces the arguments sent to the tool call (e.g. redact PII, inject defaults). - afterCallTool / afterListTools: replaces the result returned to the caller. When false (default), the hook validates/observes only — its output is discarded.
spec.middleware.afterListTools[].workflow <object> (required)	# Workflow is the Dapr workflow to invoke as the hook.
spec.middleware.afterListTools[].workflow.appID <string>	# AppID targets the workflow on a remote Dapr app via service invocation. When unset, the workflow is invoked locally.
spec.middleware.afterListTools[].workflow.workflowName <string> (required) min=1	# WorkflowName is the name of the workflow to invoke.
spec.middleware.beforeCallTool <[]object>	# BeforeCallTool hooks are invoked in order before each CallTool. Receives {mcpServer, toolName, arguments} as input. If any hook returns an error, the chain stops and the workflow completes with CallToolResult{isError: true} so the agent/LLM can self-correct.
spec.middleware.beforeCallTool[].mutate <boolean>	# Mutate, when true, causes the hook's return value to replace the data flowing through the pipeline: - beforeCallTool: replaces the arguments sent to the tool call (e.g. redact PII, inject defaults). - afterCallTool / afterListTools: replaces the result returned to the caller. When false (default), the hook validates/observes only — its output is discarded.
spec.middleware.beforeCallTool[].workflow <object> (required)	# Workflow is the Dapr workflow to invoke as the hook.
spec.middleware.beforeCallTool[].workflow.appID <string>	# AppID targets the workflow on a remote Dapr app via service invocation. When unset, the workflow is invoked locally.
spec.middleware.beforeCallTool[].workflow.workflowName <string> (required) min=1	# WorkflowName is the name of the workflow to invoke.
spec.middleware.beforeListTools <[]object>	# BeforeListTools hooks are invoked in order before each ListTools. Receives {mcpServer} as input. If any hook returns an error, the chain stops and the error is returned.
spec.middleware.beforeListTools[].workflow <object> (required)	# Workflow is the Dapr workflow to invoke as the hook.
spec.middleware.beforeListTools[].workflow.appID <string>	# AppID targets the workflow on a remote Dapr app via service invocation. When unset, the workflow is invoked locally.
spec.middleware.beforeListTools[].workflow.workflowName <string> (required) min=1	# WorkflowName is the name of the workflow to invoke.
