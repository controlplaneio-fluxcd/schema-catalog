# schema source: Logging Operator 6.7.0 https://github.com/kube-logging/logging-operator
apiVersion <string> enum=logging.banzaicloud.io/v1beta1
kind <string> enum=LoggingRoute (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.source <string> (required)
spec.targets <object> (required)
spec.targets.matchExpressions <[]object>
spec.targets.matchExpressions[].key <string> (required)
spec.targets.matchExpressions[].operator <string> (required)
spec.targets.matchExpressions[].values <[]string>
spec.targets.matchLabels <map[string]string>
status <object>
status.notices <[]string>
status.noticesCount <integer>
status.problems <[]string>
status.problemsCount <integer>
status.tenants <[]object>
status.tenants[].name <string> (required)
status.tenants[].namespaces <[]string>
