# schema source: Skupper 2.2.1 https://github.com/skupperproject/skupper
apiVersion <string> enum=skupper.io/v2alpha1
kind <string> enum=AttachedConnector
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.includeNotReadyPods <boolean>	# Advanced. If true, include server pods in the NotReady state. By default it is false.
spec.port <integer> (required)	# The port on the target server to connect to.
spec.selector <string> (required)	# A Kubernetes label selector for specifying target server pods. It uses <label-name>=<label-value> syntax. On Kubernetes, either selector or host is required.
spec.settings <map[string]string>	# Advanced. A map containing additional settings. Each map entry has a string name and a string value. Note: In general, we recommend not changing settings from their default values.
spec.siteNamespace <string> (required)	# The name of the namespace in which the site this connector should be attached to is defined.
spec.tlsCredentials <string>	# Advanced. The name of a bundle of TLS certificates used for secure router-to-server communication. The bundle contains the trusted server certificate (usually a CA). It optionally includes a client certificate and key for mutual TLS. On Kubernetes, the value is the name of a Secret in the current namespace. On Docker, Podman, and Linux, the value is the name of a directory under input/certs/ in the current namespace.
spec.type <string>	# Selected protocol for service networking. By default, its value is TCP, the only option available.
spec.useClientCert <boolean>	# Advanced. Send the client certificate when connecting in order to enable mutual TLS. Default value is false.
status <object>
status.conditions <[]object>	# A set of named conditions describing the current state of the resource.
status.conditions[].lastTransitionTime <string> (required) format=date-time
status.conditions[].message <string> (required) max=32768
status.conditions[].observedGeneration <integer> format=int64 min=0
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024
status.conditions[].status <string> (required) enum=True|False|Unknown
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316
status.message <string>	# A human-readable status message. Error messages are reported here.
status.selectedPods <[]object>
status.selectedPods[].ip <string>
status.selectedPods[].name <string>
status.status <string>	# The current state of the resource. - `Pending`: The resource is being processed. - `Error`: There was an error processing the resource. See message for more information. - `Ready`: The resource is ready to use.
