# schema source: Skupper 2.2.1 https://github.com/skupperproject/skupper
apiVersion <string> enum=skupper.io/v2alpha1
kind <string> enum=Site
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.defaultIssuer <string>	# Advanced. The name of a Kubernetes secret containing the signing CA used to generate a certificate from a token. A secret is generated if none is specified. This issuer is used by AccessGrant and RouterAccess if a specific issuer is not set. Defaults to `skupper-site-ca`
spec.edge <boolean>	# Advanced. Configure the site to operate in edge mode. Edge sites cannot accept links from remote sites. Edge mode can help you scale your network to large numbers of sites. However, for networks with 16 or fewer sites, there is little benefit. Currently, edge sites cannot also have HA enabled.
spec.ha <boolean>	# Configure the site for high availability (HA). HA sites have two active routers. Note that Skupper routers are stateless, and they restart after failure. This already provides a high level of availability. Enabling HA goes further and reduces the window of downtime caused by restarts. By default, Pod anti-affinity will be configured on the router Deployments when HA is enabled. To overwrite this behavior see the `disable-anti-affinity` Site setting.
spec.linkAccess <string>	# Configure external access for links from remote sites. When set, implies a RouterAccess resource with accessType set according to the linkAccess value. Sites and links are the basis for creating application networks. In a simple two-site network, at least one of the sites must have link access enabled. Choices include: - `none`: No linking to this site is enabled. - `default`: Use the default link access for the current platform. For OpenShift, the default is `route`. For other Kubernetes flavors, the default is `loadbalancer`. - `route`: Use an OpenShift route. - `loadbalancer`: Use a Kubernetes load balancer. - `ingress` / `ingress-nginx`: When enabled on the controller, use a Kubernetes Ingress. Use `ingress` for generic controllers; `ingress-nginx` adds NGINX-specific TLS annotations. Set `ingressClassName` via RouterAccess `spec.settings` or controller `SKUPPER_INGRESS_CLASS_NAME`.
spec.serviceAccount <string>	# Advanced. The name of the Kubernetes service account under which to run the Skupper router. A service account is generated if none is specified.
spec.settings <map[string]string>	# Advanced. A map containing additional settings. Each map entry has a string name and a string value. **Note:** In general, we recommend not changing `settings` from their default values. - `routerDataConnections`: Set the number of router worker threads. Minimum 2. - `routerLogging`: Set the number of router logging level. Options are "info", "warning", "error". - `disable-anti-affinity`: Set to "true" in order to prevent skupper from specifying router pod affinity. - `size`: The desired site sizing profile to use for constraining pod resources. Corresponds to a ConfigMap with matching `skupper.io/site-sizing` label. - `tls-prior-valid-revisions`: Set the number of revisions to TLS Secrets backing Site Link connections that are permissible to hold open to preserve established service connections. An unsigned integer defaults to 1. Set to 0 to immediately disrupt connections secured with old TLS configurations. - `ingressClassName`: When using `ingress` or `ingress-nginx` link access, sets the Kubernetes IngressClass for Skupper-managed Ingress resources (also copied to RouterAccess). Overrides controller `SKUPPER_INGRESS_CLASS_NAME`. Use empty string to clear a previously set class.
status <object>
status.conditions <[]object>	# A set of named conditions describing the current state of the resource. - `Configured`: The output resources for this resource have been created. - `Running`: There is at least one router pod running. - `Resolved`: The hostname or IP address for link access is available. - `Ready`: The site is ready for use. All other conditions are true.
status.conditions[].lastTransitionTime <string> (required) format=date-time
status.conditions[].message <string> (required) max=32768
status.conditions[].observedGeneration <integer> format=int64 min=0
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024
status.conditions[].status <string> (required) enum=True|False|Unknown
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316
status.controller <object>
status.controller.name <string>
status.controller.namespace <string>
status.controller.version <string>
status.defaultIssuer <string>	# The name of the Kubernetes secret containing the active default signing CA.
status.endpoints <[]object>	# An array of connection endpoints. Each item has a name, host, port, and group. These include connection endpoints for link access.
status.endpoints[].group <string>
status.endpoints[].host <string>
status.endpoints[].name <string>
status.endpoints[].port <string>
status.message <string>	# A human-readable status message. Error messages are reported here.
status.network <[]object>
status.network[].id <string>
status.network[].links <[]object>
status.network[].links[].name <string>
status.network[].links[].operational <boolean>
status.network[].links[].remoteSiteId <string>
status.network[].links[].remoteSiteName <string>
status.network[].name <string>
status.network[].namespace <string>
status.network[].platform <string>
status.network[].services <[]object>
status.network[].services[].connectors <[]string>
status.network[].services[].listeners <[]string>
status.network[].services[].routingKey <string>
status.network[].version <string>
status.sitesInNetwork <integer>
status.status <string>	# The current state of the resource. - `Pending`: The resource is being processed. - `Error`: There was an error processing the resource. See `message` for more information. - `Ready`: The resource is ready to use.
