# schema source: Skupper 2.2.1 https://github.com/skupperproject/skupper
apiVersion <string> enum=skupper.io/v2alpha1
kind <string> enum=RouterAccess
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.accessType <string>	# Configures the access type for the router endpoints. Available access types and the default selection is configured on the Skupper controller for Kubernetes. Additional options when enabled on the Skupper controller include: - `local`: No external ingress. Implies a Kubernetes Service with type ClusterIP. - `route`: Exposed via an OpenShift Route. - `loadbalancer`: Exposed via a Kubernetes Service with type LoadBalancer. - `ingress`: Exposed via a Kubernetes Ingress (no nginx-specific annotations). - `ingress-nginx`: Exposed via Ingress with the NGINX Ingress Controller annotations. To select which IngressClass handles Skupper-managed Ingress resources, set controller flag/env `SKUPPER_INGRESS_CLASS_NAME` or per-site `spec.settings.ingressClassName` on this resource.
spec.bindHost <string>	# The hostname or IP address of the network interface to bind to. By default, Skupper binds all the interfaces on the host.
spec.generateTlsCredentials <boolean>	# When set, Skupper generates the TLS credentials to be stored in the Secret specified by `tlsCredentials`. See also `issuer`.
spec.issuer <string>	# The name of the Kubernetes Secret containing the signing CA used to generate TLS certificates for the RouterAccess when `generateTlsCredentials` is set.
spec.roles <[]object> (required)	# The named interfaces by which a router can be accessed. These include "inter-router" for links between interior routers and "edge" for links from edge routers to interior routers.
spec.roles[].name <string> (required)	# The role name. Either "inter-router" or "edge".
spec.roles[].port <integer>	# The port for the router to bind. Must not conflict with another role.
spec.settings <map[string]string>	# Advanced. A map containing additional settings. Each map entry has a string name and a string value. For access types `ingress` and `ingress-nginx`, the key `ingressClassName` sets `spec.ingressClassName` on the managed Ingress (overrides the controller-wide default). **Note:** In general, we recommend not changing `settings` from their default values except when required.
spec.subjectAlternativeNames <[]string>	# The hostnames and IPs secured by the router TLS certificate.
spec.tlsCredentials <string> (required)	# The name of a bundle of TLS certificates used for mutual TLS router-to-router communication. The bundle contains the server certificate and key and the trusted client certificate (usually a CA). On Kubernetes, the value is the name of a Secret in the current namespace. On Docker, Podman, and Linux, the value is the name of a directory under `input/certs/` in the current namespace.
status <object>
status.conditions <[]object>	# A set of named conditions describing the current state of the resource. - `Configured`: The output resources for this resource have been created. - `Resolved`: The connection endpoints are available. - `Ready`: The router access is ready for use. All other conditions are true.
status.conditions[].lastTransitionTime <string> (required) format=date-time
status.conditions[].message <string> (required) max=32768
status.conditions[].observedGeneration <integer> format=int64 min=0
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024
status.conditions[].status <string> (required) enum=True|False|Unknown
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316
status.endpoints <[]object>	# An array of connection endpoints. Each item has a name, host, port, and group.
status.endpoints[].group <string>
status.endpoints[].host <string>
status.endpoints[].name <string>
status.endpoints[].port <string>
status.message <string>	# A human-readable status message. Error messages are reported here.
status.status <string>	# The current state of the resource. - `Pending`: The resource is being processed. - `Error`: There was an error processing the resource. See `message` for more information. - `Ready`: The resource is ready to use.
