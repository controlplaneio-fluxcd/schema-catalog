# schema source: Skupper 2.2.1 https://github.com/skupperproject/skupper
apiVersion <string> enum=skupper.io/v2alpha1
kind <string> enum=Connector
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.exposePodsByName <boolean>	# If true, expose each pod as an individual service.
spec.host <string>	# The hostname or IP address of the server. This is an alternative to selector for specifying the target server. On Kubernetes, either selector or host is required. On Docker, Podman, or Linux, host is required.
spec.includeNotReadyPods <boolean>	# If true, include server pods in the NotReady state.
spec.port <integer> (required)	# The port on the target server to connect to.
spec.routingKey <string> (required)	# The identifier used to route traffic from listeners to connectors. To expose a local workload to a remote site, the remote listener and the local connector must have matching routing keys.
spec.selector <string>	# A Kubernetes label selector for specifying target server pods. It uses <label-name>=<label-value> syntax. On Kubernetes, either selector or host is required.
spec.settings <map[string]string>	# A map containing additional settings. Each map entry has a string name and a string value. Note: In general, we recommend not changing settings from their default values.
spec.tlsCredentials <string>	# The name of a bundle of TLS certificates used for secure router-to-server communication. The bundle contains the trusted server certificate (usually a CA). It optionally includes a client certificate and key for mutual TLS. On Kubernetes, the value is the name of a Secret in the current namespace. On Docker, Podman, and Linux, the value is the name of a directory under input/certs/ in the current namespace.
spec.type <string>	# Currently only supports tcp.
spec.useClientCert <boolean>	# Send the client certificate when connecting in order to enable mutual TLS.
spec.verifyHostname <boolean>	# If true, require that the hostname of the server connected to matches the hostname in the server's certificate.
status <object>
status.conditions <[]object>	# A set of named conditions describing the current state of the resource. - `Configured`: The connector configuration has been applied to the router. - `Matched`: There is at least one listener corresponding to this connector. - `Ready`: The connector is ready to use. All other conditions are true.
status.conditions[].lastTransitionTime <string> (required) format=date-time
status.conditions[].message <string> (required) max=32768
status.conditions[].observedGeneration <integer> format=int64 min=0
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024
status.conditions[].status <string> (required) enum=True|False|Unknown
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316
status.hasMatchingListener <boolean>	# True if there is at least one listener with a matching routing key (usually in a remote site).
status.message <string>	# A human-readable status message. Error messages are reported here.
status.selectedPods <[]object>
status.selectedPods[].ip <string>
status.selectedPods[].name <string>
status.status <string>	# The current state of the resource. - `Pending`: The resource is being processed. - `Error`: There was an error processing the resource. See `message` for more information. - `Ready`: The resource is ready to use.
