# schema source: OpenShift v4.20
apiVersion <string> enum=security.openshift.io/v1
kind <string> enum=SecurityContextConstraints
metadata.name <string> (required)
metadata.namespace <string>
allowHostDirVolumePlugin <boolean> (required) default=false	# allowHostDirVolumePlugin determines if the policy allow containers to use the HostDir volume plugin
allowHostIPC <boolean> (required) default=false	# allowHostIPC determines if the policy allows host ipc in the containers.
allowHostNetwork <boolean> (required) default=false	# allowHostNetwork determines if the policy allows the use of HostNetwork in the pod spec.
allowHostPID <boolean> (required) default=false	# allowHostPID determines if the policy allows host pid in the containers.
allowHostPorts <boolean> (required) default=false	# allowHostPorts determines if the policy allows host ports in the containers.
allowPrivilegeEscalation <boolean>	# allowPrivilegeEscalation determines if a pod can request to allow privilege escalation. If unspecified, defaults to true.
allowPrivilegedContainer <boolean> (required) default=false	# allowPrivilegedContainer determines if a container can request to be run as privileged.
allowedCapabilities <[]string> (required)	# allowedCapabilities is a list of capabilities that can be requested to add to the container. Capabilities in this field maybe added at the pod author's discretion. You must not list a capability in both AllowedCapabilities and RequiredDropCapabilities. To allow all capabilities you may use '*'.
allowedFlexVolumes <[]object>	# allowedFlexVolumes is a whitelist of allowed Flexvolumes. Empty or nil indicates that all Flexvolumes may be used. This parameter is effective only when the usage of the Flexvolumes is allowed in the "Volumes" field.
allowedFlexVolumes[].driver <string> (required) default=""	# driver is the name of the Flexvolume driver.
allowedUnsafeSysctls <[]string>	# allowedUnsafeSysctls is a list of explicitly allowed unsafe sysctls, defaults to none. Each entry is either a plain sysctl name or ends in "*" in which case it is considered as a prefix of allowed sysctls. Single * means all unsafe sysctls are allowed. Kubelet has to whitelist all allowed unsafe sysctls explicitly to avoid rejection. Examples: e.g. "foo/*" allows "foo/bar", "foo/baz", etc. e.g. "foo.*" allows "foo.bar", "foo.baz", etc.
defaultAddCapabilities <[]string> (required)	# defaultAddCapabilities is the default set of capabilities that will be added to the container unless the pod spec specifically drops the capability. You may not list a capabiility in both DefaultAddCapabilities and RequiredDropCapabilities.
defaultAllowPrivilegeEscalation <boolean>	# defaultAllowPrivilegeEscalation controls the default setting for whether a process can gain more privileges than its parent process.
forbiddenSysctls <[]string>	# forbiddenSysctls is a list of explicitly forbidden sysctls, defaults to none. Each entry is either a plain sysctl name or ends in "*" in which case it is considered as a prefix of forbidden sysctls. Single * means all sysctls are forbidden. Examples: e.g. "foo/*" forbids "foo/bar", "foo/baz", etc. e.g. "foo.*" forbids "foo.bar", "foo.baz", etc.
fsGroup <object> default={}	# fsGroup is the strategy that will dictate what fs group is used by the SecurityContext.
fsGroup.ranges <[]object>	# ranges are the allowed ranges of fs groups. If you would like to force a single fs group then supply a single range with the same start and end.
fsGroup.ranges[].max <integer> format=int64	# max is the end of the range, inclusive.
fsGroup.ranges[].min <integer> format=int64	# min is the start of the range, inclusive.
fsGroup.type <string>	# type is the strategy that will dictate what FSGroup is used in the SecurityContext.
groups <[]string>	# The groups that have permission to use this security context constraints
priority <integer> (required) format=int32	# priority influences the sort order of SCCs when evaluating which SCCs to try first for a given pod request based on access in the Users and Groups fields. The higher the int, the higher priority. An unset value is considered a 0 priority. If scores for multiple SCCs are equal they will be sorted from most restrictive to least restrictive. If both priorities and restrictions are equal the SCCs will be sorted by name.
readOnlyRootFilesystem <boolean> (required) default=false	# readOnlyRootFilesystem when set to true will force containers to run with a read only root file system. If the container specifically requests to run with a non-read only root file system the SCC should deny the pod. If set to false the container may run with a read only root file system if it wishes but it will not be forced to.
requiredDropCapabilities <[]string> (required)	# requiredDropCapabilities are the capabilities that will be dropped from the container. These are required to be dropped and cannot be added.
runAsUser <object> default={}	# runAsUser is the strategy that will dictate what RunAsUser is used in the SecurityContext.
runAsUser.type <string>	# type is the strategy that will dictate what RunAsUser is used in the SecurityContext.
runAsUser.uid <integer> format=int64	# uid is the user id that containers must run as. Required for the MustRunAs strategy if not using namespace/service account allocated uids.
runAsUser.uidRangeMax <integer> format=int64	# uidRangeMax defines the max value for a strategy that allocates by range.
runAsUser.uidRangeMin <integer> format=int64	# uidRangeMin defines the min value for a strategy that allocates by range.
seLinuxContext <object> default={}	# seLinuxContext is the strategy that will dictate what labels will be set in the SecurityContext.
seLinuxContext.seLinuxOptions <object>	# seLinuxOptions required to run as; required for MustRunAs
seLinuxContext.seLinuxOptions.level <string>	# Level is SELinux level label that applies to the container.
seLinuxContext.seLinuxOptions.role <string>	# Role is a SELinux role label that applies to the container.
seLinuxContext.seLinuxOptions.type <string>	# Type is a SELinux type label that applies to the container.
seLinuxContext.seLinuxOptions.user <string>	# User is a SELinux user label that applies to the container.
seLinuxContext.type <string>	# type is the strategy that will dictate what SELinux context is used in the SecurityContext.
seccompProfiles <[]string>	# seccompProfiles lists the allowed profiles that may be set for the pod or container's seccomp annotations. An unset (nil) or empty value means that no profiles may be specifid by the pod or container. The wildcard '*' may be used to allow all profiles. When used to generate a value for a pod the first non-wildcard profile will be used as the default.
supplementalGroups <object> default={}	# supplementalGroups is the strategy that will dictate what supplemental groups are used by the SecurityContext.
supplementalGroups.ranges <[]object>	# ranges are the allowed ranges of supplemental groups. If you would like to force a single supplemental group then supply a single range with the same start and end.
supplementalGroups.ranges[].max <integer> format=int64	# max is the end of the range, inclusive.
supplementalGroups.ranges[].min <integer> format=int64	# min is the start of the range, inclusive.
supplementalGroups.type <string>	# type is the strategy that will dictate what supplemental groups is used in the SecurityContext.
userNamespaceLevel <string> default="AllowHostLevel"	# userNamespaceLevel determines if the policy allows host users in containers. Valid values are "AllowHostLevel", "RequirePodLevel", and omitted. When "AllowHostLevel" is set, a pod author may set `hostUsers` to either `true` or `false`. When "RequirePodLevel" is set, a pod author must set `hostUsers` to `false`. When omitted, the default value is "AllowHostLevel".
users <[]string>	# The users who have permissions to use this security context constraints
volumes <[]string> (required)	# volumes is a white list of allowed volume plugins. FSType corresponds directly with the field names of a VolumeSource (azureFile, configMap, emptyDir). To allow all volumes you may use "*". To allow no volumes, set to ["none"].
