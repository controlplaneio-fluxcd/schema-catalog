# schema source: VictoriaMetrics Operator v0.73.1 https://github.com/VictoriaMetrics/operator
apiVersion <string> enum=operator.victoriametrics.com/v1beta1
kind <string> enum=VMAlertmanagerConfig
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# VMAlertmanagerConfigSpec defines configuration for VMAlertmanagerConfig it must reference only locally defined objects
spec.inhibit_rules <[]object>	# InhibitRules will only apply for alerts matching the resource's namespace.
spec.inhibit_rules[].equal <[]string>	# Labels that must have an equal value in the source and target alert for the inhibition to take effect.
spec.inhibit_rules[].source_matchers <[]string>	# SourceMatchers defines a list of matchers for which one or more alerts have to exist for the inhibition to take effect.
spec.inhibit_rules[].target_matchers <[]string>	# TargetMatchers defines a list of matchers that have to be fulfilled by the target alerts to be muted.
spec.receivers <[]object>	# Receivers defines alert receivers
spec.receivers[].discord_configs <[]object>
spec.receivers[].discord_configs[].avatar_url <string>	# AvatarURL defines message avatar URL Available from alertmanager v0.28.0
spec.receivers[].discord_configs[].content <string> max=2000	# Content defines message content template Available from alertmanager v0.28.0
spec.receivers[].discord_configs[].http_config <object>	# HTTP client configuration.
spec.receivers[].discord_configs[].http_config.authorization <object>	# Authorization header configuration for the client. This is mutually exclusive with BasicAuth and is only available starting from Alertmanager v0.22+.
spec.receivers[].discord_configs[].http_config.authorization.credentials <object>	# Reference to the secret with value for authorization
spec.receivers[].discord_configs[].http_config.authorization.credentials.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.authorization.credentials.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.authorization.credentials.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.authorization.credentialsFile <string>	# File with value for authorization
spec.receivers[].discord_configs[].http_config.authorization.type <string>	# Type of authorization, default to bearer
spec.receivers[].discord_configs[].http_config.basic_auth <object>	# BasicAuth for the client.
spec.receivers[].discord_configs[].http_config.basic_auth.password <object>	# Password defines reference for secret with password value The secret needs to be in the same namespace as scrape object
spec.receivers[].discord_configs[].http_config.basic_auth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.basic_auth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.basic_auth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.basic_auth.password_file <string>	# PasswordFile defines path to password file at disk must be pre-mounted
spec.receivers[].discord_configs[].http_config.basic_auth.username <object>	# Username defines reference for secret with username value The secret needs to be in the same namespace as scrape object
spec.receivers[].discord_configs[].http_config.basic_auth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.basic_auth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.basic_auth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.bearer_token_file <string>	# BearerTokenFile defines filename for bearer token, it must be mounted to pod.
spec.receivers[].discord_configs[].http_config.bearer_token_secret <object>	# The secret's key that contains the bearer token It must be at them same namespace as CRD
spec.receivers[].discord_configs[].http_config.bearer_token_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.bearer_token_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.bearer_token_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.follow_redirects <boolean>	# FollowRedirects controls redirects for scraping.
spec.receivers[].discord_configs[].http_config.noProxy <string>	# NoProxy defines a comma-separated string that can contain IPs, CIDR notation, domain names that should be excluded from proxying. IP and domain names can contain port numbers.
spec.receivers[].discord_configs[].http_config.oauth2 <object>	# OAuth2 client credentials used to fetch a token for the targets.
spec.receivers[].discord_configs[].http_config.oauth2.client_id <object> (required)	# The secret or configmap containing the OAuth2 client id
spec.receivers[].discord_configs[].http_config.oauth2.client_id.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].discord_configs[].http_config.oauth2.client_id.configMap.key <string> (required)	# The key to select.
spec.receivers[].discord_configs[].http_config.oauth2.client_id.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.oauth2.client_id.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].discord_configs[].http_config.oauth2.client_id.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].discord_configs[].http_config.oauth2.client_id.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.oauth2.client_id.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.oauth2.client_id.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.oauth2.client_secret <object>	# The secret containing the OAuth2 client secret
spec.receivers[].discord_configs[].http_config.oauth2.client_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.oauth2.client_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.oauth2.client_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.oauth2.client_secret_file <string>	# ClientSecretFile defines path for client secret file.
spec.receivers[].discord_configs[].http_config.oauth2.endpoint_params <map[string]string>	# Parameters to append to the token URL
spec.receivers[].discord_configs[].http_config.oauth2.proxy_url <string>	# The proxy URL for token_url connection Is only supported by Scrape objects family
spec.receivers[].discord_configs[].http_config.oauth2.scopes <[]string>	# OAuth2 scopes used for the token request
spec.receivers[].discord_configs[].http_config.oauth2.tls_config <any>	# TLSConfig for token_url connection Is only supported by Scrape objects family
spec.receivers[].discord_configs[].http_config.oauth2.token_url <string> (required) min=1	# The URL to fetch the token from
spec.receivers[].discord_configs[].http_config.proxyConnectHeader <map[string]array>	# ProxyConnectHeader optionally specifies headers to send to proxies during CONNECT requests.
spec.receivers[].discord_configs[].http_config.proxyFromEnvironment <boolean>	# ProxyFromEnvironment defines whether to use the proxy configuration defined by environment variables (HTTP_PROXY, HTTPS_PROXY, and NO_PROXY).
spec.receivers[].discord_configs[].http_config.proxyURL <string> pattern="^(http|https|socks5)://.+$"	# ProxyUrl defines the HTTP proxy server to use.
spec.receivers[].discord_configs[].http_config.tls_config <object>	# TLS configuration for the client.
spec.receivers[].discord_configs[].http_config.tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].discord_configs[].http_config.tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].discord_configs[].http_config.tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].discord_configs[].http_config.tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].discord_configs[].http_config.tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].discord_configs[].http_config.tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].discord_configs[].http_config.tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].http_config.tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].http_config.tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].discord_configs[].http_config.tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].discord_configs[].message <string>	# The message body template
spec.receivers[].discord_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].discord_configs[].title <string>	# The message title template
spec.receivers[].discord_configs[].username <string>	# Username defines message username Available from alertmanager v0.28.0
spec.receivers[].discord_configs[].webhook_url <string>	# The discord webhook URL one of `urlSecret` and `url` must be defined.
spec.receivers[].discord_configs[].webhook_url_secret <object>	# URLSecret defines secret name and key at the CRD namespace. It must contain the webhook URL. one of `urlSecret` and `url` must be defined.
spec.receivers[].discord_configs[].webhook_url_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].discord_configs[].webhook_url_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].discord_configs[].webhook_url_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].email_configs <[]object>	# EmailConfigs defines email notification configurations.
spec.receivers[].email_configs[].auth_identity <string>	# The identity to use for authentication.
spec.receivers[].email_configs[].auth_password <object>	# AuthPassword defines secret name and key at CRD namespace.
spec.receivers[].email_configs[].auth_password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].email_configs[].auth_password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].auth_password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].email_configs[].auth_secret <object>	# AuthSecret defines secret name and key at CRD namespace. It must contain the CRAM-MD5 secret.
spec.receivers[].email_configs[].auth_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].email_configs[].auth_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].auth_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].email_configs[].auth_username <string>	# The username to use for authentication.
spec.receivers[].email_configs[].from <string>	# The sender address. fallback to global setting if empty
spec.receivers[].email_configs[].headers <map[string]string>	# Further headers email header key/value pairs. Overrides any headers previously set by the notification implementation.
spec.receivers[].email_configs[].hello <string>	# The hostname to identify to the SMTP server.
spec.receivers[].email_configs[].html <string>	# The HTML body of the email notification.
spec.receivers[].email_configs[].require_tls <boolean>	# The SMTP TLS requirement. Note that Go does not support unencrypted connections to remote SMTP endpoints.
spec.receivers[].email_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].email_configs[].smarthost <string>	# The SMTP host through which emails are sent. fallback to global setting if empty
spec.receivers[].email_configs[].text <string>	# The text body of the email notification.
spec.receivers[].email_configs[].tls_config <object>	# TLS configuration
spec.receivers[].email_configs[].tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].email_configs[].tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].email_configs[].tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].email_configs[].tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].email_configs[].tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].email_configs[].tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].email_configs[].tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].email_configs[].tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].email_configs[].tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].email_configs[].tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].email_configs[].tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].email_configs[].tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].email_configs[].tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].email_configs[].tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].email_configs[].tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].email_configs[].tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].email_configs[].tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].email_configs[].tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].email_configs[].tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].email_configs[].tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].email_configs[].tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].email_configs[].tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].email_configs[].tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].email_configs[].to <string>	# The email address to send notifications to.
spec.receivers[].incidentio_configs <[]object>
spec.receivers[].incidentio_configs[].alert_source_token <object>	# AlertSourceToken is used to authenticate with incident.io. Mutually exclusive with AlertSourceTokenFile.
spec.receivers[].incidentio_configs[].alert_source_token.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].incidentio_configs[].alert_source_token.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].incidentio_configs[].alert_source_token.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].incidentio_configs[].alert_source_token_file <string>	# AlertSourceTokenFile defines the path to a file that contains the alert source token. Mutually exclusive with AlertSourceToken.
spec.receivers[].incidentio_configs[].http_config <any>
spec.receivers[].incidentio_configs[].max_alerts <integer>	# MaxAlerts defines maximum number of alerts to be sent per incident.io message.
spec.receivers[].incidentio_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].incidentio_configs[].timeout <string>	# Timeout is the maximum time allowed to invoke incident.io
spec.receivers[].incidentio_configs[].url <string>	# The URL to send the incident.io alert. This would typically be provided by the incident.io team when setting up an alert source. Mutually exclusive with URLFile.
spec.receivers[].incidentio_configs[].url_file <string>	# URLFile defines the path to a file that holds the incident.io alert URL. Mutually exclusive with URL.
spec.receivers[].jira_configs <[]object>
spec.receivers[].jira_configs[].api_url <string>	# The URL to send API requests to. The full API path must be included. Example: https://company.atlassian.net/rest/api/2/
spec.receivers[].jira_configs[].custom_fields <object (free-form)>	# Other issue and custom fields. Jira issue field can have multiple types. Depends on the field type, the values must be provided differently. See https://developer.atlassian.com/server/jira/platform/jira-rest-api-examples/#setting-custom-field-data-for-other-field-types for further examples.
spec.receivers[].jira_configs[].description <string>	# Issue description template.
spec.receivers[].jira_configs[].http_config <any>	# The HTTP client's configuration. You must use this configuration to supply the personal access token (PAT) as part of the HTTP `Authorization` header. For Jira Cloud, use basic_auth with the email address as the username and the PAT as the password. For Jira Data Center, use the 'authorization' field with 'credentials: <PAT value>'.
spec.receivers[].jira_configs[].issue_type <string> (required)	# Type of the issue (e.g. Bug)
spec.receivers[].jira_configs[].labels <[]string>	# Labels to be added to the issue
spec.receivers[].jira_configs[].priority <string>	# Priority of the issue
spec.receivers[].jira_configs[].project <string> (required)	# The project key where issues are created
spec.receivers[].jira_configs[].reopen_duration <string> pattern="^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$"	# If reopen_transition is defined, reopen the issue when it is not older than this value (rounded down to the nearest minute). The resolutiondate field is used to determine the age of the issue.
spec.receivers[].jira_configs[].reopen_transition <string>	# Name of the workflow transition to resolve an issue. The target status must have the category "done".
spec.receivers[].jira_configs[].resolve_transition <string>	# Name of the workflow transition to reopen an issue. The target status should not have the category "done".
spec.receivers[].jira_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].jira_configs[].summary <string>	# Issue summary template
spec.receivers[].jira_configs[].wont_fix_resolution <string>	# If reopen_transition is defined, ignore issues with that resolution.
spec.receivers[].mattermost_configs <[]object>	# MattermostConfigs defines Mattermost notification configurations.
spec.receivers[].mattermost_configs[].attachments <[]object>	# Attachments defines richer formatting options
spec.receivers[].mattermost_configs[].attachments[].author_icon <string>
spec.receivers[].mattermost_configs[].attachments[].author_link <string>
spec.receivers[].mattermost_configs[].attachments[].author_name <string>
spec.receivers[].mattermost_configs[].attachments[].color <string>
spec.receivers[].mattermost_configs[].attachments[].fallback <string>
spec.receivers[].mattermost_configs[].attachments[].fields <[]object>
spec.receivers[].mattermost_configs[].attachments[].fields[].short <boolean>
spec.receivers[].mattermost_configs[].attachments[].fields[].title <string>
spec.receivers[].mattermost_configs[].attachments[].fields[].value <string>
spec.receivers[].mattermost_configs[].attachments[].footer <string>
spec.receivers[].mattermost_configs[].attachments[].footer_icon <string>
spec.receivers[].mattermost_configs[].attachments[].image_url <string>
spec.receivers[].mattermost_configs[].attachments[].pretext <string>
spec.receivers[].mattermost_configs[].attachments[].text <string>
spec.receivers[].mattermost_configs[].attachments[].thumb_url <string>
spec.receivers[].mattermost_configs[].attachments[].title <string>
spec.receivers[].mattermost_configs[].attachments[].title_link <string>
spec.receivers[].mattermost_configs[].channel <string>	# Channel overrides the channel the message posts in.
spec.receivers[].mattermost_configs[].http_config <any>
spec.receivers[].mattermost_configs[].icon_emoji <string>	# IconEmoji overrides the profile picture and icon_url parameter.
spec.receivers[].mattermost_configs[].icon_url <string>	# IconURL overrides the profile picture the message posts with.
spec.receivers[].mattermost_configs[].priority <object>
spec.receivers[].mattermost_configs[].priority.persistent_notifications <boolean>
spec.receivers[].mattermost_configs[].priority.priority <string>
spec.receivers[].mattermost_configs[].priority.requested_ack <boolean>
spec.receivers[].mattermost_configs[].props <object>
spec.receivers[].mattermost_configs[].props.card <string>
spec.receivers[].mattermost_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].mattermost_configs[].text <string> (required)	# Text defines markdown-formatted message to display in the post.
spec.receivers[].mattermost_configs[].url <string>	# URL to send requests to, one of `urlSecret` and `url` must be defined.
spec.receivers[].mattermost_configs[].url_secret <object>	# URLSecret defines secret name and key at the CRD namespace. It must contain the Mattermost URL. one of `urlSecret` and `url` must be defined.
spec.receivers[].mattermost_configs[].url_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].mattermost_configs[].url_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].mattermost_configs[].url_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].mattermost_configs[].username <string>	# Username overrides the username the message posts as
spec.receivers[].msteams_configs <[]object>
spec.receivers[].msteams_configs[].http_config <object>	# HTTP client configuration.
spec.receivers[].msteams_configs[].http_config.authorization <object>	# Authorization header configuration for the client. This is mutually exclusive with BasicAuth and is only available starting from Alertmanager v0.22+.
spec.receivers[].msteams_configs[].http_config.authorization.credentials <object>	# Reference to the secret with value for authorization
spec.receivers[].msteams_configs[].http_config.authorization.credentials.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.authorization.credentials.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.authorization.credentials.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.authorization.credentialsFile <string>	# File with value for authorization
spec.receivers[].msteams_configs[].http_config.authorization.type <string>	# Type of authorization, default to bearer
spec.receivers[].msteams_configs[].http_config.basic_auth <object>	# BasicAuth for the client.
spec.receivers[].msteams_configs[].http_config.basic_auth.password <object>	# Password defines reference for secret with password value The secret needs to be in the same namespace as scrape object
spec.receivers[].msteams_configs[].http_config.basic_auth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.basic_auth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.basic_auth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.basic_auth.password_file <string>	# PasswordFile defines path to password file at disk must be pre-mounted
spec.receivers[].msteams_configs[].http_config.basic_auth.username <object>	# Username defines reference for secret with username value The secret needs to be in the same namespace as scrape object
spec.receivers[].msteams_configs[].http_config.basic_auth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.basic_auth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.basic_auth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.bearer_token_file <string>	# BearerTokenFile defines filename for bearer token, it must be mounted to pod.
spec.receivers[].msteams_configs[].http_config.bearer_token_secret <object>	# The secret's key that contains the bearer token It must be at them same namespace as CRD
spec.receivers[].msteams_configs[].http_config.bearer_token_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.bearer_token_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.bearer_token_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.follow_redirects <boolean>	# FollowRedirects controls redirects for scraping.
spec.receivers[].msteams_configs[].http_config.noProxy <string>	# NoProxy defines a comma-separated string that can contain IPs, CIDR notation, domain names that should be excluded from proxying. IP and domain names can contain port numbers.
spec.receivers[].msteams_configs[].http_config.oauth2 <object>	# OAuth2 client credentials used to fetch a token for the targets.
spec.receivers[].msteams_configs[].http_config.oauth2.client_id <object> (required)	# The secret or configmap containing the OAuth2 client id
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.configMap.key <string> (required)	# The key to select.
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.oauth2.client_id.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.oauth2.client_secret <object>	# The secret containing the OAuth2 client secret
spec.receivers[].msteams_configs[].http_config.oauth2.client_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.oauth2.client_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.oauth2.client_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.oauth2.client_secret_file <string>	# ClientSecretFile defines path for client secret file.
spec.receivers[].msteams_configs[].http_config.oauth2.endpoint_params <map[string]string>	# Parameters to append to the token URL
spec.receivers[].msteams_configs[].http_config.oauth2.proxy_url <string>	# The proxy URL for token_url connection Is only supported by Scrape objects family
spec.receivers[].msteams_configs[].http_config.oauth2.scopes <[]string>	# OAuth2 scopes used for the token request
spec.receivers[].msteams_configs[].http_config.oauth2.tls_config <any>	# TLSConfig for token_url connection Is only supported by Scrape objects family
spec.receivers[].msteams_configs[].http_config.oauth2.token_url <string> (required) min=1	# The URL to fetch the token from
spec.receivers[].msteams_configs[].http_config.proxyConnectHeader <map[string]array>	# ProxyConnectHeader optionally specifies headers to send to proxies during CONNECT requests.
spec.receivers[].msteams_configs[].http_config.proxyFromEnvironment <boolean>	# ProxyFromEnvironment defines whether to use the proxy configuration defined by environment variables (HTTP_PROXY, HTTPS_PROXY, and NO_PROXY).
spec.receivers[].msteams_configs[].http_config.proxyURL <string> pattern="^(http|https|socks5)://.+$"	# ProxyUrl defines the HTTP proxy server to use.
spec.receivers[].msteams_configs[].http_config.tls_config <object>	# TLS configuration for the client.
spec.receivers[].msteams_configs[].http_config.tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].msteams_configs[].http_config.tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].msteams_configs[].http_config.tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].msteams_configs[].http_config.tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].msteams_configs[].http_config.tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].msteams_configs[].http_config.tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].msteams_configs[].http_config.tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].http_config.tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].http_config.tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteams_configs[].http_config.tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].msteams_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].msteams_configs[].text <string>	# The text body of the teams notification.
spec.receivers[].msteams_configs[].title <string>	# The title of the teams notification.
spec.receivers[].msteams_configs[].webhook_url <string>	# The incoming webhook URL one of `urlSecret` and `url` must be defined.
spec.receivers[].msteams_configs[].webhook_url_secret <object>	# URLSecret defines secret name and key at the CRD namespace. It must contain the webhook URL. one of `urlSecret` and `url` must be defined.
spec.receivers[].msteams_configs[].webhook_url_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteams_configs[].webhook_url_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteams_configs[].webhook_url_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].msteamsv2_configs <[]object>
spec.receivers[].msteamsv2_configs[].http_config <any>
spec.receivers[].msteamsv2_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].msteamsv2_configs[].text <string>	# Message body template.
spec.receivers[].msteamsv2_configs[].title <string>	# Message title template.
spec.receivers[].msteamsv2_configs[].webhook_url <string>	# The incoming webhook URL one of `urlSecret` and `url` must be defined.
spec.receivers[].msteamsv2_configs[].webhook_url_secret <object>	# URLSecret defines secret name and key at the CRD namespace. It must contain the webhook URL. one of `webhook_url` or `webhook_url_secret` must be defined.
spec.receivers[].msteamsv2_configs[].webhook_url_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].msteamsv2_configs[].webhook_url_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].msteamsv2_configs[].webhook_url_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].name <string> (required) min=1	# Name of the receiver. Must be unique across all items from the list.
spec.receivers[].opsgenie_configs <[]object>	# OpsGenieConfigs defines ops genie notification configurations.
spec.receivers[].opsgenie_configs[].actions <string>	# Comma separated list of actions that will be available for the alert.
spec.receivers[].opsgenie_configs[].apiURL <string>	# The URL to send OpsGenie API requests to.
spec.receivers[].opsgenie_configs[].api_key <object>	# The secret's key that contains the OpsGenie API key. It must be at them same namespace as CRD fallback to global setting if empty
spec.receivers[].opsgenie_configs[].api_key.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].opsgenie_configs[].api_key.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].opsgenie_configs[].api_key.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].opsgenie_configs[].description <string>	# Description of the incident.
spec.receivers[].opsgenie_configs[].details <map[string]string>	# A set of arbitrary key/value pairs that provide further detail about the incident.
spec.receivers[].opsgenie_configs[].entity <string>	# Optional field that can be used to specify which domain alert is related to.
spec.receivers[].opsgenie_configs[].http_config <object (free-form)>	# HTTP client configuration.
spec.receivers[].opsgenie_configs[].message <string>	# Alert text limited to 130 characters.
spec.receivers[].opsgenie_configs[].note <string>	# Additional alert note.
spec.receivers[].opsgenie_configs[].priority <string>	# Priority level of alert. Possible values are P1, P2, P3, P4, and P5.
spec.receivers[].opsgenie_configs[].responders <[]object>	# List of responders responsible for notifications.
spec.receivers[].opsgenie_configs[].responders[].id <string>	# ID of the responder.
spec.receivers[].opsgenie_configs[].responders[].name <string>	# Name of the responder.
spec.receivers[].opsgenie_configs[].responders[].type <string> (required) min=1	# Type of responder.
spec.receivers[].opsgenie_configs[].responders[].username <string>	# Username of the responder.
spec.receivers[].opsgenie_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].opsgenie_configs[].source <string>	# Backlink to the sender of the notification.
spec.receivers[].opsgenie_configs[].tags <string>	# Comma separated list of tags attached to the notifications.
spec.receivers[].opsgenie_configs[].update_alerts <boolean>	# Whether to update message and description of the alert in OpsGenie if it already exists By default, the alert is never updated in OpsGenie, the new message only appears in activity log.
spec.receivers[].pagerduty_configs <[]object>	# PagerDutyConfigs defines pager duty notification configurations.
spec.receivers[].pagerduty_configs[].class <string>	# The class/type of the event.
spec.receivers[].pagerduty_configs[].client <string>	# Client identification.
spec.receivers[].pagerduty_configs[].client_url <string>	# Backlink to the sender of notification.
spec.receivers[].pagerduty_configs[].component <string>	# The part or component of the affected system that is broken.
spec.receivers[].pagerduty_configs[].description <string>	# Description of the incident.
spec.receivers[].pagerduty_configs[].details <map[string]string>	# Arbitrary key/value pairs that provide further detail about the incident.
spec.receivers[].pagerduty_configs[].group <string>	# A cluster or grouping of sources.
spec.receivers[].pagerduty_configs[].http_config <object (free-form)>	# HTTP client configuration.
spec.receivers[].pagerduty_configs[].images <[]object>	# Images to attach to the incident.
spec.receivers[].pagerduty_configs[].images[].alt <string>
spec.receivers[].pagerduty_configs[].images[].href <string>
spec.receivers[].pagerduty_configs[].images[].source <string> (required)
spec.receivers[].pagerduty_configs[].links <[]object>	# Links to attach to the incident.
spec.receivers[].pagerduty_configs[].links[].href <string> (required)
spec.receivers[].pagerduty_configs[].links[].text <string>
spec.receivers[].pagerduty_configs[].routing_key <object>	# The secret's key that contains the PagerDuty integration key (when using Events API v2). Either this field or `serviceKey` needs to be defined. It must be at them same namespace as CRD
spec.receivers[].pagerduty_configs[].routing_key.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].pagerduty_configs[].routing_key.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].pagerduty_configs[].routing_key.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].pagerduty_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].pagerduty_configs[].service_key <object>	# The secret's key that contains the PagerDuty service key (when using integration type "Prometheus"). Either this field or `routingKey` needs to be defined. It must be at them same namespace as CRD
spec.receivers[].pagerduty_configs[].service_key.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].pagerduty_configs[].service_key.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].pagerduty_configs[].service_key.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].pagerduty_configs[].severity <string>	# Severity of the incident.
spec.receivers[].pagerduty_configs[].url <string>	# The URL to send requests to.
spec.receivers[].pushover_configs <[]object>	# PushoverConfigs defines push over notification configurations.
spec.receivers[].pushover_configs[].expire <string>	# How long your notification will continue to be retried for, unless the user acknowledges the notification.
spec.receivers[].pushover_configs[].html <boolean>	# Whether notification message is HTML or plain text.
spec.receivers[].pushover_configs[].http_config <object (free-form)>	# HTTP client configuration.
spec.receivers[].pushover_configs[].message <string>	# Notification message.
spec.receivers[].pushover_configs[].priority <string>	# Priority, see https://pushover.net/api#priority
spec.receivers[].pushover_configs[].retry <string>	# How often the Pushover servers will send the same notification to the user. Must be at least 30 seconds.
spec.receivers[].pushover_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].pushover_configs[].sound <string>	# The name of one of the sounds supported by device clients to override the user's default sound choice
spec.receivers[].pushover_configs[].title <string>	# Notification title.
spec.receivers[].pushover_configs[].token <object>	# The secret's key that contains the registered application’s API token, see https://pushover.net/apps. It must be at them same namespace as CRD
spec.receivers[].pushover_configs[].token.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].pushover_configs[].token.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].pushover_configs[].token.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].pushover_configs[].url <string>	# A supplementary URL shown alongside the message.
spec.receivers[].pushover_configs[].url_title <string>	# A title for supplementary URL, otherwise just the URL is shown
spec.receivers[].pushover_configs[].user_key <object>	# The secret's key that contains the recipient user’s user key. It must be at them same namespace as CRD
spec.receivers[].pushover_configs[].user_key.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].pushover_configs[].user_key.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].pushover_configs[].user_key.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].rocketchat_configs <[]object>
spec.receivers[].rocketchat_configs[].actions <[]object>
spec.receivers[].rocketchat_configs[].actions[].msg <string>
spec.receivers[].rocketchat_configs[].actions[].text <string>
spec.receivers[].rocketchat_configs[].actions[].type <string>
spec.receivers[].rocketchat_configs[].actions[].url <string>
spec.receivers[].rocketchat_configs[].api_url <string>
spec.receivers[].rocketchat_configs[].channel <string>	# RocketChat channel override, (like #other-channel or @username).
spec.receivers[].rocketchat_configs[].color <string>
spec.receivers[].rocketchat_configs[].emoji <string>
spec.receivers[].rocketchat_configs[].fields <[]object>
spec.receivers[].rocketchat_configs[].fields[].short <boolean>
spec.receivers[].rocketchat_configs[].fields[].title <string>
spec.receivers[].rocketchat_configs[].fields[].value <string>
spec.receivers[].rocketchat_configs[].http_config <any>
spec.receivers[].rocketchat_configs[].icon_url <string>
spec.receivers[].rocketchat_configs[].image_url <string>
spec.receivers[].rocketchat_configs[].link_names <boolean>
spec.receivers[].rocketchat_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].rocketchat_configs[].short_fields <boolean>
spec.receivers[].rocketchat_configs[].text <string>
spec.receivers[].rocketchat_configs[].thumb_url <string>
spec.receivers[].rocketchat_configs[].title <string>
spec.receivers[].rocketchat_configs[].title_link <string>
spec.receivers[].rocketchat_configs[].token <object>	# SecretKeySelector selects a key of a Secret.
spec.receivers[].rocketchat_configs[].token.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].rocketchat_configs[].token.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].rocketchat_configs[].token.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].rocketchat_configs[].token_id <object>	# The sender token and token_id See https://docs.rocket.chat/docs/manage-personal-access-tokens
spec.receivers[].rocketchat_configs[].token_id.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].rocketchat_configs[].token_id.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].rocketchat_configs[].token_id.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].slack_configs <[]object>	# SlackConfigs defines slack notification configurations.
spec.receivers[].slack_configs[].actions <[]object>	# A list of Slack actions that are sent with each notification.
spec.receivers[].slack_configs[].actions[].confirm <object>	# SlackConfirmationField protect users from destructive actions or particularly distinguished decisions by asking them to confirm their button click one more time. See https://api.slack.com/docs/interactive-message-field-guide#confirmation_fields for more information.
spec.receivers[].slack_configs[].actions[].confirm.dismiss_text <string>
spec.receivers[].slack_configs[].actions[].confirm.ok_text <string>
spec.receivers[].slack_configs[].actions[].confirm.text <string> (required) min=1
spec.receivers[].slack_configs[].actions[].confirm.title <string>
spec.receivers[].slack_configs[].actions[].name <string>
spec.receivers[].slack_configs[].actions[].style <string>
spec.receivers[].slack_configs[].actions[].text <string> (required) min=1
spec.receivers[].slack_configs[].actions[].type <string> (required) min=1
spec.receivers[].slack_configs[].actions[].url <string>
spec.receivers[].slack_configs[].actions[].value <string>
spec.receivers[].slack_configs[].api_url <object>	# The secret's key that contains the Slack webhook URL. It must be at them same namespace as CRD fallback to global setting if empty
spec.receivers[].slack_configs[].api_url.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].slack_configs[].api_url.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].slack_configs[].api_url.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].slack_configs[].callback_id <string>
spec.receivers[].slack_configs[].channel <string>	# The channel or user to send notifications to.
spec.receivers[].slack_configs[].color <string>
spec.receivers[].slack_configs[].fallback <string>
spec.receivers[].slack_configs[].fields <[]object>	# A list of Slack fields that are sent with each notification.
spec.receivers[].slack_configs[].fields[].short <boolean>
spec.receivers[].slack_configs[].fields[].title <string> (required) min=1
spec.receivers[].slack_configs[].fields[].value <string> (required) min=1
spec.receivers[].slack_configs[].footer <string>
spec.receivers[].slack_configs[].http_config <object (free-form)>	# HTTP client configuration.
spec.receivers[].slack_configs[].icon_emoji <string>
spec.receivers[].slack_configs[].icon_url <string>
spec.receivers[].slack_configs[].image_url <string>
spec.receivers[].slack_configs[].link_names <boolean>
spec.receivers[].slack_configs[].mrkdwn_in <[]string>
spec.receivers[].slack_configs[].pretext <string>
spec.receivers[].slack_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].slack_configs[].short_fields <boolean>
spec.receivers[].slack_configs[].text <string>
spec.receivers[].slack_configs[].thumb_url <string>
spec.receivers[].slack_configs[].title <string>
spec.receivers[].slack_configs[].title_link <string>
spec.receivers[].slack_configs[].update_message <boolean>	# Whether to update the original message in-place instead of sending a new one. Requires Slack Bot API and chat:write scope. Available since alertmanager v0.32.0.
spec.receivers[].slack_configs[].username <string>
spec.receivers[].sns_configs <[]object>
spec.receivers[].sns_configs[].api_url <string>	# The api URL
spec.receivers[].sns_configs[].attributes <map[string]string>	# SNS message attributes
spec.receivers[].sns_configs[].http_config <object>	# HTTP client configuration.
spec.receivers[].sns_configs[].http_config.authorization <object>	# Authorization header configuration for the client. This is mutually exclusive with BasicAuth and is only available starting from Alertmanager v0.22+.
spec.receivers[].sns_configs[].http_config.authorization.credentials <object>	# Reference to the secret with value for authorization
spec.receivers[].sns_configs[].http_config.authorization.credentials.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.authorization.credentials.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.authorization.credentials.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.authorization.credentialsFile <string>	# File with value for authorization
spec.receivers[].sns_configs[].http_config.authorization.type <string>	# Type of authorization, default to bearer
spec.receivers[].sns_configs[].http_config.basic_auth <object>	# BasicAuth for the client.
spec.receivers[].sns_configs[].http_config.basic_auth.password <object>	# Password defines reference for secret with password value The secret needs to be in the same namespace as scrape object
spec.receivers[].sns_configs[].http_config.basic_auth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.basic_auth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.basic_auth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.basic_auth.password_file <string>	# PasswordFile defines path to password file at disk must be pre-mounted
spec.receivers[].sns_configs[].http_config.basic_auth.username <object>	# Username defines reference for secret with username value The secret needs to be in the same namespace as scrape object
spec.receivers[].sns_configs[].http_config.basic_auth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.basic_auth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.basic_auth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.bearer_token_file <string>	# BearerTokenFile defines filename for bearer token, it must be mounted to pod.
spec.receivers[].sns_configs[].http_config.bearer_token_secret <object>	# The secret's key that contains the bearer token It must be at them same namespace as CRD
spec.receivers[].sns_configs[].http_config.bearer_token_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.bearer_token_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.bearer_token_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.follow_redirects <boolean>	# FollowRedirects controls redirects for scraping.
spec.receivers[].sns_configs[].http_config.noProxy <string>	# NoProxy defines a comma-separated string that can contain IPs, CIDR notation, domain names that should be excluded from proxying. IP and domain names can contain port numbers.
spec.receivers[].sns_configs[].http_config.oauth2 <object>	# OAuth2 client credentials used to fetch a token for the targets.
spec.receivers[].sns_configs[].http_config.oauth2.client_id <object> (required)	# The secret or configmap containing the OAuth2 client id
spec.receivers[].sns_configs[].http_config.oauth2.client_id.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].sns_configs[].http_config.oauth2.client_id.configMap.key <string> (required)	# The key to select.
spec.receivers[].sns_configs[].http_config.oauth2.client_id.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.oauth2.client_id.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].sns_configs[].http_config.oauth2.client_id.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].sns_configs[].http_config.oauth2.client_id.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.oauth2.client_id.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.oauth2.client_id.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.oauth2.client_secret <object>	# The secret containing the OAuth2 client secret
spec.receivers[].sns_configs[].http_config.oauth2.client_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.oauth2.client_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.oauth2.client_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.oauth2.client_secret_file <string>	# ClientSecretFile defines path for client secret file.
spec.receivers[].sns_configs[].http_config.oauth2.endpoint_params <map[string]string>	# Parameters to append to the token URL
spec.receivers[].sns_configs[].http_config.oauth2.proxy_url <string>	# The proxy URL for token_url connection Is only supported by Scrape objects family
spec.receivers[].sns_configs[].http_config.oauth2.scopes <[]string>	# OAuth2 scopes used for the token request
spec.receivers[].sns_configs[].http_config.oauth2.tls_config <any>	# TLSConfig for token_url connection Is only supported by Scrape objects family
spec.receivers[].sns_configs[].http_config.oauth2.token_url <string> (required) min=1	# The URL to fetch the token from
spec.receivers[].sns_configs[].http_config.proxyConnectHeader <map[string]array>	# ProxyConnectHeader optionally specifies headers to send to proxies during CONNECT requests.
spec.receivers[].sns_configs[].http_config.proxyFromEnvironment <boolean>	# ProxyFromEnvironment defines whether to use the proxy configuration defined by environment variables (HTTP_PROXY, HTTPS_PROXY, and NO_PROXY).
spec.receivers[].sns_configs[].http_config.proxyURL <string> pattern="^(http|https|socks5)://.+$"	# ProxyUrl defines the HTTP proxy server to use.
spec.receivers[].sns_configs[].http_config.tls_config <object>	# TLS configuration for the client.
spec.receivers[].sns_configs[].http_config.tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].sns_configs[].http_config.tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].sns_configs[].http_config.tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].sns_configs[].http_config.tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].sns_configs[].http_config.tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].sns_configs[].http_config.tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].sns_configs[].http_config.tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].http_config.tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].http_config.tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].http_config.tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].sns_configs[].message <string>	# The message content of the SNS notification.
spec.receivers[].sns_configs[].phone_number <string>	# Phone number if message is delivered via SMS Specify this, topic_arn or target_arn
spec.receivers[].sns_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].sns_configs[].sigv4 <object>	# Configure the AWS Signature Verification 4 signing process
spec.receivers[].sns_configs[].sigv4.access_key <string>	# The AWS API keys. Both access_key and secret_key must be supplied or both must be blank. If blank the environment variables `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` are used.
spec.receivers[].sns_configs[].sigv4.access_key_selector <object>	# secret key selector to get the keys from a Kubernetes Secret
spec.receivers[].sns_configs[].sigv4.access_key_selector.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].sigv4.access_key_selector.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].sigv4.access_key_selector.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].sigv4.profile <string>	# Named AWS profile used to authenticate
spec.receivers[].sns_configs[].sigv4.region <string>	# AWS region, if blank the region from the default credentials chain is used
spec.receivers[].sns_configs[].sigv4.role_arn <string>	# AWS Role ARN, an alternative to using AWS API keys
spec.receivers[].sns_configs[].sigv4.secret_key_selector <object>	# secret key selector to get the keys from a Kubernetes Secret
spec.receivers[].sns_configs[].sigv4.secret_key_selector.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].sns_configs[].sigv4.secret_key_selector.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].sns_configs[].sigv4.secret_key_selector.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].sns_configs[].subject <string>	# The subject line if message is delivered to an email endpoint.
spec.receivers[].sns_configs[].target_arn <string>	# Mobile platform endpoint ARN if message is delivered via mobile notifications Specify this, topic_arn or phone_number
spec.receivers[].sns_configs[].topic_arn <string>	# SNS topic ARN, either specify this, phone_number or target_arn
spec.receivers[].telegram_configs <[]object>
spec.receivers[].telegram_configs[].api_url <string>	# APIUrl the Telegram API URL i.e. https://api.telegram.org.
spec.receivers[].telegram_configs[].bot_token <object> (required)	# BotToken token for the bot https://core.telegram.org/bots/api
spec.receivers[].telegram_configs[].bot_token.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].telegram_configs[].bot_token.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].telegram_configs[].bot_token.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].telegram_configs[].chat_id <integer> (required)	# ChatID is ID of the chat where to send the messages.
spec.receivers[].telegram_configs[].disable_notifications <boolean>	# DisableNotifications
spec.receivers[].telegram_configs[].http_config <object (free-form)>	# HTTP client configuration.
spec.receivers[].telegram_configs[].message <string>	# Message is templated message
spec.receivers[].telegram_configs[].message_thread_id <integer>	# MessageThreadID defines ID of the message thread where to send the messages.
spec.receivers[].telegram_configs[].parse_mode <string>	# ParseMode for telegram message, supported values are MarkdownV2, Markdown, Markdown and empty string for plain text.
spec.receivers[].telegram_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].victorops_configs <[]object>	# VictorOpsConfigs defines victor ops notification configurations.
spec.receivers[].victorops_configs[].api_key <object>	# The secret's key that contains the API key to use when talking to the VictorOps API. It must be at them same namespace as CRD fallback to global setting if empty
spec.receivers[].victorops_configs[].api_key.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].api_key.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].api_key.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].api_url <string>	# The VictorOps API URL.
spec.receivers[].victorops_configs[].custom_fields <map[string]string>	# Adds optional custom fields https://github.com/prometheus/alertmanager/blob/v0.24.0/config/notifiers.go#L537
spec.receivers[].victorops_configs[].entity_display_name <string>	# Contains summary of the alerted problem.
spec.receivers[].victorops_configs[].http_config <object>	# The HTTP client's configuration.
spec.receivers[].victorops_configs[].http_config.authorization <object>	# Authorization header configuration for the client. This is mutually exclusive with BasicAuth and is only available starting from Alertmanager v0.22+.
spec.receivers[].victorops_configs[].http_config.authorization.credentials <object>	# Reference to the secret with value for authorization
spec.receivers[].victorops_configs[].http_config.authorization.credentials.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.authorization.credentials.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.authorization.credentials.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.authorization.credentialsFile <string>	# File with value for authorization
spec.receivers[].victorops_configs[].http_config.authorization.type <string>	# Type of authorization, default to bearer
spec.receivers[].victorops_configs[].http_config.basic_auth <object>	# BasicAuth for the client.
spec.receivers[].victorops_configs[].http_config.basic_auth.password <object>	# Password defines reference for secret with password value The secret needs to be in the same namespace as scrape object
spec.receivers[].victorops_configs[].http_config.basic_auth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.basic_auth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.basic_auth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.basic_auth.password_file <string>	# PasswordFile defines path to password file at disk must be pre-mounted
spec.receivers[].victorops_configs[].http_config.basic_auth.username <object>	# Username defines reference for secret with username value The secret needs to be in the same namespace as scrape object
spec.receivers[].victorops_configs[].http_config.basic_auth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.basic_auth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.basic_auth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.bearer_token_file <string>	# BearerTokenFile defines filename for bearer token, it must be mounted to pod.
spec.receivers[].victorops_configs[].http_config.bearer_token_secret <object>	# The secret's key that contains the bearer token It must be at them same namespace as CRD
spec.receivers[].victorops_configs[].http_config.bearer_token_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.bearer_token_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.bearer_token_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.follow_redirects <boolean>	# FollowRedirects controls redirects for scraping.
spec.receivers[].victorops_configs[].http_config.noProxy <string>	# NoProxy defines a comma-separated string that can contain IPs, CIDR notation, domain names that should be excluded from proxying. IP and domain names can contain port numbers.
spec.receivers[].victorops_configs[].http_config.oauth2 <object>	# OAuth2 client credentials used to fetch a token for the targets.
spec.receivers[].victorops_configs[].http_config.oauth2.client_id <object> (required)	# The secret or configmap containing the OAuth2 client id
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.configMap.key <string> (required)	# The key to select.
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.oauth2.client_id.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.oauth2.client_secret <object>	# The secret containing the OAuth2 client secret
spec.receivers[].victorops_configs[].http_config.oauth2.client_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.oauth2.client_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.oauth2.client_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.oauth2.client_secret_file <string>	# ClientSecretFile defines path for client secret file.
spec.receivers[].victorops_configs[].http_config.oauth2.endpoint_params <map[string]string>	# Parameters to append to the token URL
spec.receivers[].victorops_configs[].http_config.oauth2.proxy_url <string>	# The proxy URL for token_url connection Is only supported by Scrape objects family
spec.receivers[].victorops_configs[].http_config.oauth2.scopes <[]string>	# OAuth2 scopes used for the token request
spec.receivers[].victorops_configs[].http_config.oauth2.tls_config <any>	# TLSConfig for token_url connection Is only supported by Scrape objects family
spec.receivers[].victorops_configs[].http_config.oauth2.token_url <string> (required) min=1	# The URL to fetch the token from
spec.receivers[].victorops_configs[].http_config.proxyConnectHeader <map[string]array>	# ProxyConnectHeader optionally specifies headers to send to proxies during CONNECT requests.
spec.receivers[].victorops_configs[].http_config.proxyFromEnvironment <boolean>	# ProxyFromEnvironment defines whether to use the proxy configuration defined by environment variables (HTTP_PROXY, HTTPS_PROXY, and NO_PROXY).
spec.receivers[].victorops_configs[].http_config.proxyURL <string> pattern="^(http|https|socks5)://.+$"	# ProxyUrl defines the HTTP proxy server to use.
spec.receivers[].victorops_configs[].http_config.tls_config <object>	# TLS configuration for the client.
spec.receivers[].victorops_configs[].http_config.tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].victorops_configs[].http_config.tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].victorops_configs[].http_config.tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].victorops_configs[].http_config.tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].victorops_configs[].http_config.tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].victorops_configs[].http_config.tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].victorops_configs[].http_config.tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].victorops_configs[].http_config.tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].victorops_configs[].http_config.tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].victorops_configs[].http_config.tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].victorops_configs[].message_type <string>	# Describes the behavior of the alert (CRITICAL, WARNING, INFO).
spec.receivers[].victorops_configs[].monitoring_tool <string>	# The monitoring tool the state message is from.
spec.receivers[].victorops_configs[].routing_key <string> (required)	# A key used to map the alert to a team.
spec.receivers[].victorops_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].victorops_configs[].state_message <string>	# Contains long explanation of the alerted problem.
spec.receivers[].webex_configs <[]object>
spec.receivers[].webex_configs[].api_url <string>	# The Webex Teams API URL, i.e. https://webexapis.com/v1/messages
spec.receivers[].webex_configs[].http_config <object>	# HTTP client configuration. You must use this configuration to supply the bot token as part of the HTTP `Authorization` header.
spec.receivers[].webex_configs[].http_config.authorization <object>	# Authorization header configuration for the client. This is mutually exclusive with BasicAuth and is only available starting from Alertmanager v0.22+.
spec.receivers[].webex_configs[].http_config.authorization.credentials <object>	# Reference to the secret with value for authorization
spec.receivers[].webex_configs[].http_config.authorization.credentials.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.authorization.credentials.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.authorization.credentials.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.authorization.credentialsFile <string>	# File with value for authorization
spec.receivers[].webex_configs[].http_config.authorization.type <string>	# Type of authorization, default to bearer
spec.receivers[].webex_configs[].http_config.basic_auth <object>	# BasicAuth for the client.
spec.receivers[].webex_configs[].http_config.basic_auth.password <object>	# Password defines reference for secret with password value The secret needs to be in the same namespace as scrape object
spec.receivers[].webex_configs[].http_config.basic_auth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.basic_auth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.basic_auth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.basic_auth.password_file <string>	# PasswordFile defines path to password file at disk must be pre-mounted
spec.receivers[].webex_configs[].http_config.basic_auth.username <object>	# Username defines reference for secret with username value The secret needs to be in the same namespace as scrape object
spec.receivers[].webex_configs[].http_config.basic_auth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.basic_auth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.basic_auth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.bearer_token_file <string>	# BearerTokenFile defines filename for bearer token, it must be mounted to pod.
spec.receivers[].webex_configs[].http_config.bearer_token_secret <object>	# The secret's key that contains the bearer token It must be at them same namespace as CRD
spec.receivers[].webex_configs[].http_config.bearer_token_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.bearer_token_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.bearer_token_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.follow_redirects <boolean>	# FollowRedirects controls redirects for scraping.
spec.receivers[].webex_configs[].http_config.noProxy <string>	# NoProxy defines a comma-separated string that can contain IPs, CIDR notation, domain names that should be excluded from proxying. IP and domain names can contain port numbers.
spec.receivers[].webex_configs[].http_config.oauth2 <object>	# OAuth2 client credentials used to fetch a token for the targets.
spec.receivers[].webex_configs[].http_config.oauth2.client_id <object> (required)	# The secret or configmap containing the OAuth2 client id
spec.receivers[].webex_configs[].http_config.oauth2.client_id.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].webex_configs[].http_config.oauth2.client_id.configMap.key <string> (required)	# The key to select.
spec.receivers[].webex_configs[].http_config.oauth2.client_id.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.oauth2.client_id.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].webex_configs[].http_config.oauth2.client_id.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].webex_configs[].http_config.oauth2.client_id.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.oauth2.client_id.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.oauth2.client_id.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.oauth2.client_secret <object>	# The secret containing the OAuth2 client secret
spec.receivers[].webex_configs[].http_config.oauth2.client_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.oauth2.client_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.oauth2.client_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.oauth2.client_secret_file <string>	# ClientSecretFile defines path for client secret file.
spec.receivers[].webex_configs[].http_config.oauth2.endpoint_params <map[string]string>	# Parameters to append to the token URL
spec.receivers[].webex_configs[].http_config.oauth2.proxy_url <string>	# The proxy URL for token_url connection Is only supported by Scrape objects family
spec.receivers[].webex_configs[].http_config.oauth2.scopes <[]string>	# OAuth2 scopes used for the token request
spec.receivers[].webex_configs[].http_config.oauth2.tls_config <any>	# TLSConfig for token_url connection Is only supported by Scrape objects family
spec.receivers[].webex_configs[].http_config.oauth2.token_url <string> (required) min=1	# The URL to fetch the token from
spec.receivers[].webex_configs[].http_config.proxyConnectHeader <map[string]array>	# ProxyConnectHeader optionally specifies headers to send to proxies during CONNECT requests.
spec.receivers[].webex_configs[].http_config.proxyFromEnvironment <boolean>	# ProxyFromEnvironment defines whether to use the proxy configuration defined by environment variables (HTTP_PROXY, HTTPS_PROXY, and NO_PROXY).
spec.receivers[].webex_configs[].http_config.proxyURL <string> pattern="^(http|https|socks5)://.+$"	# ProxyUrl defines the HTTP proxy server to use.
spec.receivers[].webex_configs[].http_config.tls_config <object>	# TLS configuration for the client.
spec.receivers[].webex_configs[].http_config.tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].webex_configs[].http_config.tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].webex_configs[].http_config.tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].webex_configs[].http_config.tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].webex_configs[].http_config.tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].webex_configs[].http_config.tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].webex_configs[].http_config.tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webex_configs[].http_config.tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webex_configs[].http_config.tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].webex_configs[].http_config.tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].webex_configs[].message <string>	# The message body template
spec.receivers[].webex_configs[].room_id <string> (required)	# The ID of the Webex Teams room where to send the messages
spec.receivers[].webex_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].webhook_configs <[]object>	# WebhookConfigs defines webhook notification configurations.
spec.receivers[].webhook_configs[].http_config <object (free-form)>	# HTTP client configuration.
spec.receivers[].webhook_configs[].max_alerts <integer> format=int32 min=0	# Maximum number of alerts to be sent per webhook message. When 0, all alerts are included.
spec.receivers[].webhook_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].webhook_configs[].timeout <string> pattern="^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$"	# Timeout is the maximum time allowed to invoke the webhook available since v0.28.0 alertmanager version
spec.receivers[].webhook_configs[].url <string>	# URL to send requests to, one of `urlSecret` and `url` must be defined.
spec.receivers[].webhook_configs[].url_secret <object>	# URLSecret defines secret name and key at the CRD namespace. It must contain the webhook URL. one of `urlSecret` and `url` must be defined.
spec.receivers[].webhook_configs[].url_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].webhook_configs[].url_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].webhook_configs[].url_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs <[]object>	# WechatConfigs defines wechat notification configurations.
spec.receivers[].wechat_configs[].agent_id <string>
spec.receivers[].wechat_configs[].api_secret <object>	# The secret's key that contains the WeChat API key. The secret needs to be in the same namespace as the AlertmanagerConfig fallback to global alertmanager setting if empty
spec.receivers[].wechat_configs[].api_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].api_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].api_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].api_url <string>	# The WeChat API URL. fallback to global alertmanager setting if empty
spec.receivers[].wechat_configs[].corp_id <string>	# The corp id for authentication. fallback to global alertmanager setting if empty
spec.receivers[].wechat_configs[].http_config <object>	# HTTP client configuration.
spec.receivers[].wechat_configs[].http_config.authorization <object>	# Authorization header configuration for the client. This is mutually exclusive with BasicAuth and is only available starting from Alertmanager v0.22+.
spec.receivers[].wechat_configs[].http_config.authorization.credentials <object>	# Reference to the secret with value for authorization
spec.receivers[].wechat_configs[].http_config.authorization.credentials.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.authorization.credentials.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.authorization.credentials.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.authorization.credentialsFile <string>	# File with value for authorization
spec.receivers[].wechat_configs[].http_config.authorization.type <string>	# Type of authorization, default to bearer
spec.receivers[].wechat_configs[].http_config.basic_auth <object>	# BasicAuth for the client.
spec.receivers[].wechat_configs[].http_config.basic_auth.password <object>	# Password defines reference for secret with password value The secret needs to be in the same namespace as scrape object
spec.receivers[].wechat_configs[].http_config.basic_auth.password.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.basic_auth.password.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.basic_auth.password.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.basic_auth.password_file <string>	# PasswordFile defines path to password file at disk must be pre-mounted
spec.receivers[].wechat_configs[].http_config.basic_auth.username <object>	# Username defines reference for secret with username value The secret needs to be in the same namespace as scrape object
spec.receivers[].wechat_configs[].http_config.basic_auth.username.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.basic_auth.username.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.basic_auth.username.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.bearer_token_file <string>	# BearerTokenFile defines filename for bearer token, it must be mounted to pod.
spec.receivers[].wechat_configs[].http_config.bearer_token_secret <object>	# The secret's key that contains the bearer token It must be at them same namespace as CRD
spec.receivers[].wechat_configs[].http_config.bearer_token_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.bearer_token_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.bearer_token_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.follow_redirects <boolean>	# FollowRedirects controls redirects for scraping.
spec.receivers[].wechat_configs[].http_config.noProxy <string>	# NoProxy defines a comma-separated string that can contain IPs, CIDR notation, domain names that should be excluded from proxying. IP and domain names can contain port numbers.
spec.receivers[].wechat_configs[].http_config.oauth2 <object>	# OAuth2 client credentials used to fetch a token for the targets.
spec.receivers[].wechat_configs[].http_config.oauth2.client_id <object> (required)	# The secret or configmap containing the OAuth2 client id
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.configMap.key <string> (required)	# The key to select.
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.oauth2.client_id.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.oauth2.client_secret <object>	# The secret containing the OAuth2 client secret
spec.receivers[].wechat_configs[].http_config.oauth2.client_secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.oauth2.client_secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.oauth2.client_secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.oauth2.client_secret_file <string>	# ClientSecretFile defines path for client secret file.
spec.receivers[].wechat_configs[].http_config.oauth2.endpoint_params <map[string]string>	# Parameters to append to the token URL
spec.receivers[].wechat_configs[].http_config.oauth2.proxy_url <string>	# The proxy URL for token_url connection Is only supported by Scrape objects family
spec.receivers[].wechat_configs[].http_config.oauth2.scopes <[]string>	# OAuth2 scopes used for the token request
spec.receivers[].wechat_configs[].http_config.oauth2.tls_config <any>	# TLSConfig for token_url connection Is only supported by Scrape objects family
spec.receivers[].wechat_configs[].http_config.oauth2.token_url <string> (required) min=1	# The URL to fetch the token from
spec.receivers[].wechat_configs[].http_config.proxyConnectHeader <map[string]array>	# ProxyConnectHeader optionally specifies headers to send to proxies during CONNECT requests.
spec.receivers[].wechat_configs[].http_config.proxyFromEnvironment <boolean>	# ProxyFromEnvironment defines whether to use the proxy configuration defined by environment variables (HTTP_PROXY, HTTPS_PROXY, and NO_PROXY).
spec.receivers[].wechat_configs[].http_config.proxyURL <string> pattern="^(http|https|socks5)://.+$"	# ProxyUrl defines the HTTP proxy server to use.
spec.receivers[].wechat_configs[].http_config.tls_config <object>	# TLS configuration for the client.
spec.receivers[].wechat_configs[].http_config.tls_config.ca <object>	# Struct containing the CA cert to use for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.ca.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.ca.configMap.key <string> (required)	# The key to select.
spec.receivers[].wechat_configs[].http_config.tls_config.ca.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.tls_config.ca.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].wechat_configs[].http_config.tls_config.ca.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.ca.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.tls_config.ca.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.tls_config.ca.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.tls_config.caFile <string>	# Path to the CA cert in the container to use for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.cert <object>	# Struct containing the client cert file for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.cert.configMap <object>	# ConfigMap containing data to use for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.cert.configMap.key <string> (required)	# The key to select.
spec.receivers[].wechat_configs[].http_config.tls_config.cert.configMap.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.tls_config.cert.configMap.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.receivers[].wechat_configs[].http_config.tls_config.cert.secret <object>	# Secret containing data to use for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.cert.secret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.tls_config.cert.secret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.tls_config.cert.secret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.tls_config.certFile <string>	# Path to the client cert file in the container for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.insecureSkipVerify <boolean>	# Disable target certificate validation.
spec.receivers[].wechat_configs[].http_config.tls_config.keyFile <string>	# Path to the client key file in the container for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.keySecret <object>	# Secret containing the client key file for the targets.
spec.receivers[].wechat_configs[].http_config.tls_config.keySecret.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.receivers[].wechat_configs[].http_config.tls_config.keySecret.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.receivers[].wechat_configs[].http_config.tls_config.keySecret.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.receivers[].wechat_configs[].http_config.tls_config.serverName <string>	# Used to verify the hostname for the targets.
spec.receivers[].wechat_configs[].message <string>	# API request data as defined by the WeChat API.
spec.receivers[].wechat_configs[].message_type <string>
spec.receivers[].wechat_configs[].send_resolved <boolean>	# SendResolved controls notify about resolved alerts.
spec.receivers[].wechat_configs[].to_party <string>
spec.receivers[].wechat_configs[].to_tag <string>
spec.receivers[].wechat_configs[].to_user <string>
spec.route <object>	# Route definition for alertmanager, may include nested routes.
spec.route.active_time_intervals <[]string>	# ActiveTimeIntervals Times when the route should be active These must match the name at time_intervals
spec.route.continue <boolean>	# Continue indicating whether an alert should continue matching subsequent sibling nodes. It will always be true for the first-level route if disableRouteContinueEnforce for vmalertmanager not set.
spec.route.group_by <[]string>	# List of labels to group by.
spec.route.group_interval <string> pattern="[0-9]+(ms|s|m|h)"	# How long to wait before sending an updated notification.
spec.route.group_wait <string> pattern="[0-9]+(ms|s|m|h)"	# How long to wait before sending the initial notification.
spec.route.matchers <[]string>	# List of matchers that the alert’s labels should match. For the first level route, the operator adds a namespace: "CRD_NS" matcher. https://prometheus.io/docs/alerting/latest/configuration/#matcher
spec.route.mute_time_intervals <[]string>	# MuteTimeIntervals is a list of interval names that will mute matched alert
spec.route.receiver <string> (required)	# Name of the receiver for this route.
spec.route.repeat_interval <string> pattern="[0-9]+(ms|s|m|h)"	# How long to wait before repeating the last notification.
spec.route.routes <[]object>	# Child routes. https://prometheus.io/docs/alerting/latest/configuration/#route
spec.time_intervals <[]object>	# TimeIntervals defines named interval for active/mute notifications interval See https://prometheus.io/docs/alerting/latest/configuration/#time_interval
spec.time_intervals[].name <string> (required)	# Name of interval
spec.time_intervals[].time_intervals <[]object> (required)	# TimeIntervals interval configuration
spec.time_intervals[].time_intervals[].days_of_month <[]string>	# DayOfMonth defines list of numerical days in the month. Days begin at 1. Negative values are also accepted. for example, ['1:5', '-3:-1']
spec.time_intervals[].time_intervals[].location <string>	# Location in golang time location form, e.g. UTC
spec.time_intervals[].time_intervals[].months <[]string>	# Months defines list of calendar months identified by a case-insensitive name (e.g. ‘January’) or numeric 1. For example, ['1:3', 'may:august', 'december']
spec.time_intervals[].time_intervals[].times <[]object>	# Times defines time range for mute
spec.time_intervals[].time_intervals[].times[].end_time <string> (required)	# EndTime for example HH:MM
spec.time_intervals[].time_intervals[].times[].start_time <string> (required)	# StartTime for example HH:MM
spec.time_intervals[].time_intervals[].weekdays <[]string>	# Weekdays defines list of days of the week, where the week begins on Sunday and ends on Saturday.
spec.time_intervals[].time_intervals[].years <[]string>	# Years defines numerical list of years, ranges are accepted. For example, ['2020:2022', '2030']
status <object>	# VMAlertmanagerConfigStatus defines the observed state of VMAlertmanagerConfig
status.conditions <[]object>	# Known .status.conditions.type are: "Available", "Progressing", and "Degraded"
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string> (required) format=date-time	# LastUpdateTime is the last time of given type update. This value is used for status TTL update and removal
status.conditions[].message <string> max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) max=316	# Type of condition in CamelCase or in name.namespace.resource.victoriametrics.com/CamelCase.
status.lastErrorParentAlertmanagerName <string>
status.observedGeneration <integer> format=int64	# ObservedGeneration defines current generation picked by operator for the reconcile
status.reason <string>	# Reason defines human readable error reason
status.updateStatus <string>	# UpdateStatus defines a status for update rollout
