# schema source: Elastic Cloud v3.4.1 https://github.com/elastic/cloud-on-k8s
apiVersion <string> enum=stackconfigpolicy.k8s.elastic.co/v1alpha1
kind <string> enum=StackConfigPolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.elasticsearch <object>
spec.elasticsearch.clusterSettings <object (free-form)>	# ClusterSettings holds the Elasticsearch cluster settings (/_cluster/settings)
spec.elasticsearch.config <object (free-form)>	# Config holds the settings that go into elasticsearch.yml.
spec.elasticsearch.indexLifecyclePolicies <object (free-form)>	# IndexLifecyclePolicies holds the Index Lifecycle policies settings (/_ilm/policy)
spec.elasticsearch.indexTemplates <object>	# IndexTemplates holds the Index and Component Templates settings
spec.elasticsearch.indexTemplates.componentTemplates <object (free-form)>	# ComponentTemplates holds the Component Templates settings (/_component_template)
spec.elasticsearch.indexTemplates.composableIndexTemplates <object (free-form)>	# ComposableIndexTemplates holds the Index Templates settings (/_index_template)
spec.elasticsearch.ingestPipelines <object (free-form)>	# IngestPipelines holds the Ingest Pipelines settings (/_ingest/pipeline)
spec.elasticsearch.secretMounts <[]object>	# SecretMounts are additional Secrets that need to be mounted into the Elasticsearch pods.
spec.elasticsearch.secretMounts[].mountPath <string>	# MountPath denotes the path to which the secret should be mounted to inside the elasticsearch pod
spec.elasticsearch.secretMounts[].secretName <string>	# SecretName denotes the name of the secret that needs to be mounted to the elasticsearch pod
spec.elasticsearch.secureSettings <[]object>	# SecureSettings are additional Secrets that contain data to be configured to Elasticsearch's keystore.
spec.elasticsearch.secureSettings[].entries <[]object>	# Entries define how to project each key-value pair in the secret to filesystem paths. If not defined, all keys will be projected to similarly named paths in the filesystem. If defined, only the specified keys will be projected to the corresponding paths.
spec.elasticsearch.secureSettings[].entries[].key <string> (required)	# Key is the key contained in the secret.
spec.elasticsearch.secureSettings[].entries[].path <string>	# Path is the relative file path to map the key to. Path must not be an absolute file path and must not contain any ".." components.
spec.elasticsearch.secureSettings[].secretName <string> (required)	# SecretName is the name of the secret.
spec.elasticsearch.securityRoleMappings <object (free-form)>	# SecurityRoleMappings holds the Role Mappings settings (/_security/role_mapping)
spec.elasticsearch.snapshotLifecyclePolicies <object (free-form)>	# SnapshotLifecyclePolicies holds the Snapshot Lifecycle Policies settings (/_slm/policy)
spec.elasticsearch.snapshotRepositories <object (free-form)>	# SnapshotRepositories holds the Snapshot Repositories settings (/_snapshot)
spec.kibana <object>
spec.kibana.config <object (free-form)>	# Config holds the settings that go into kibana.yml.
spec.kibana.secureSettings <[]object>	# SecureSettings are additional Secrets that contain data to be configured to Kibana's keystore.
spec.kibana.secureSettings[].entries <[]object>	# Entries define how to project each key-value pair in the secret to filesystem paths. If not defined, all keys will be projected to similarly named paths in the filesystem. If defined, only the specified keys will be projected to the corresponding paths.
spec.kibana.secureSettings[].entries[].key <string> (required)	# Key is the key contained in the secret.
spec.kibana.secureSettings[].entries[].path <string>	# Path is the relative file path to map the key to. Path must not be an absolute file path and must not contain any ".." components.
spec.kibana.secureSettings[].secretName <string> (required)	# SecretName is the name of the secret.
spec.resourceSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.resourceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.resourceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.resourceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.resourceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.resourceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.secureSettings <[]object>	# Deprecated: SecureSettings only applies to Elasticsearch and is deprecated. It must be set per application instead.
spec.secureSettings[].entries <[]object>	# Entries define how to project each key-value pair in the secret to filesystem paths. If not defined, all keys will be projected to similarly named paths in the filesystem. If defined, only the specified keys will be projected to the corresponding paths.
spec.secureSettings[].entries[].key <string> (required)	# Key is the key contained in the secret.
spec.secureSettings[].entries[].path <string>	# Path is the relative file path to map the key to. Path must not be an absolute file path and must not contain any ".." components.
spec.secureSettings[].secretName <string> (required)	# SecretName is the name of the secret.
spec.weight <integer> default=0 format=int32	# Weight determines the priority of this policy when multiple policies target the same resource. Higher weight values take precedence. Defaults to 0.
status <object>
status.details <map[string]object>	# Details holds the status details for each resource to be configured.
status.errors <integer>	# Errors is the number of resources which have an incorrect configuration
status.observedGeneration <integer> format=int64	# ObservedGeneration is the most recent generation observed for this StackConfigPolicy.
status.phase <string>	# Phase is the phase of the StackConfigPolicy.
status.ready <integer>	# Ready is the number of resources successfully configured.
status.readyCount <string>	# ReadyCount is a human representation of the number of resources successfully configured.
status.resources <integer>	# Resources is the number of resources to be configured.
status.resourcesStatuses <map[string]object>	# ResourcesStatuses holds the status for each resource to be configured. Deprecated: Details is used to store the status of resources from ECK 2.11
status.resourcesStatuses.<key>.currentVersion <integer> format=int64	# CurrentVersion denotes the current version of filesettings applied to the Elasticsearch cluster This field does not apply to Kibana resources
status.resourcesStatuses.<key>.error <object>
status.resourcesStatuses.<key>.error.message <string>
status.resourcesStatuses.<key>.error.version <integer> format=int64
status.resourcesStatuses.<key>.expectedVersion <integer> format=int64	# ExpectedVersion denotes the expected version of filesettings that should be applied to the Elasticsearch cluster This field does not apply to Kibana resources
status.resourcesStatuses.<key>.phase <string>
