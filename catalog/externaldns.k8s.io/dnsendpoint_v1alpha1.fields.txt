# schema source: ExternalDNS v0.21.0 https://github.com/kubernetes-sigs/external-dns
apiVersion <string> enum=externaldns.k8s.io/v1alpha1
kind <string> enum=DNSEndpoint
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# DNSEndpointSpec defines the desired state of DNSEndpoint
spec.endpoints <[]object>
spec.endpoints[].dnsName <string>	# The hostname of the DNS record
spec.endpoints[].labels <map[string]string>	# Labels stores labels defined for the Endpoint
spec.endpoints[].providerSpecific <[]object>	# ProviderSpecific stores provider specific config
spec.endpoints[].providerSpecific[].name <string>
spec.endpoints[].providerSpecific[].value <string>
spec.endpoints[].recordTTL <integer> format=int64	# TTL for the record
spec.endpoints[].recordType <string>	# RecordType type of record, e.g. CNAME, A, AAAA, SRV, TXT etc
spec.endpoints[].setIdentifier <string>	# Identifier to distinguish multiple records with the same name and type (e.g. Route53 records with routing policies other than 'simple')
spec.endpoints[].targets <[]string>	# The targets the DNS record points to
status <object>	# DNSEndpointStatus defines the observed state of DNSEndpoint
status.observedGeneration <integer> format=int64	# The generation observed by the external-dns controller.
