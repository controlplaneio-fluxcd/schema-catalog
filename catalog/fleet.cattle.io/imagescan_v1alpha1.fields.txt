# schema source: Rancher Fleet v0.15.4 https://github.com/rancher/fleet
apiVersion <string> enum=fleet.cattle.io/v1alpha1
kind <string> enum=ImageScan
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# API is taken from https://github.com/fluxcd/image-reflector-controller
spec.gitrepoName <string>	# GitRepo reference name
spec.image <string> (required)	# Image is the name of the image repository
spec.interval <string> (required)	# Interval is the length of time to wait between scans of the image repository.
spec.policy <object>	# Policy gives the particulars of the policy to be followed in selecting the most recent image
spec.policy.alphabetical <object>	# Alphabetical set of rules to use for alphabetical ordering of the tags.
spec.policy.alphabetical.order <string>	# Order specifies the sorting order of the tags. Given the letters of the alphabet as tags, ascending order would select Z, and descending order would select A.
spec.policy.semver <object>	# SemVer gives a semantic version range to check against the tags available.
spec.policy.semver.range <string>	# Range gives a semver range for the image tag; the highest version within the range that's a tag yields the latest image.
spec.secretRef <object>	# SecretRef can be given the name of a secret containing credentials to use for the image registry. The secret should be created with `kubectl create secret docker-registry`, or the equivalent.
spec.secretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.suspend <boolean>	# This flag tells the controller to suspend subsequent image scans. It does not apply to already started scans. Defaults to false.
spec.tagName <string>	# TagName is the tag ref that needs to be put in manifest to replace fields
status <object>
status.canonicalImageName <string>	# CanonicalName is the name of the image repository with all the implied bits made explicit; e.g., `docker.io/library/alpine` rather than `alpine`.
status.conditions <[]object>
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string>	# The last time this condition was updated.
status.conditions[].message <string>	# Human-readable message indicating details about last transition
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of cluster condition.
status.lastScanTime <string> format=date-time	# LastScanTime is the last time image was scanned
status.latestDigest <string>	# LatestDigest is the digest of latest tag
status.latestImage <string>	# LatestImage gives the first in the list of images scanned by the image repository, when filtered and ordered according to the policy.
status.latestTag <string>	# Latest tag is the latest tag filtered by the policy
status.observedGeneration <integer> format=int64
