# schema source: Rancher Fleet v0.15.4 https://github.com/rancher/fleet
apiVersion <string> enum=fleet.cattle.io/v1alpha1
kind <string> enum=Bundle
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.allowedTargetNamespaceSelector <object>	# AllowedTargetNamespaceSelector restricts deployments to namespaces matching this selector. Propagated from GitRepoRestriction and validated by the agent on the downstream cluster.
spec.allowedTargetNamespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.allowedTargetNamespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.allowedTargetNamespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.allowedTargetNamespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.allowedTargetNamespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.contentsId <string>	# ContentsID stores the contents id when deploying contents using an OCI registry.
spec.correctDrift <object>	# CorrectDrift specifies how drift correction should work.
spec.correctDrift.enabled <boolean>	# Enabled correct drift if true.
spec.correctDrift.force <boolean>	# Force helm rollback with --force option will be used if true. This will try to recreate all resources in the release.
spec.correctDrift.keepFailHistory <boolean>	# KeepFailHistory keeps track of failed rollbacks in the helm history.
spec.defaultNamespace <string>	# DefaultNamespace is the namespace to use for resources that do not specify a namespace. This field is not used to enforce or lock down the deployment to a specific namespace.
spec.deleteCRDResources <boolean>	# DeleteCRDResources deletes CRDs. Warning! this will also delete all your Custom Resources.
spec.deleteNamespace <boolean>	# DeleteNamespace can be used to delete the deployed namespace when removing the bundle
spec.dependsOn <[]object>	# DependsOn refers to the bundles which must be ready before this bundle can be deployed.
spec.dependsOn[].acceptedStates <[]string>	# AcceptedStates is a list of BundleDeployment state that are considered acceptable for this dependency. If the dependency is in one of these states, it will not block the deployment of the dependent bundle. Valid Values should match the StateRank keys. If not specified, default to ["Ready"]: only fully ready dependencies are accepted Example: ["Ready", "Modified"] will accept dependencies that are either ready or have drifted from their desired state.
spec.dependsOn[].name <string>	# Name of the bundle.
spec.dependsOn[].selector <object>	# Selector matching bundle's labels.
spec.dependsOn[].selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.dependsOn[].selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.dependsOn[].selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.dependsOn[].selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.dependsOn[].selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.diff <object>	# Diff can be used to ignore the modified state of objects which are amended at runtime.
spec.diff.comparePatches <[]object>	# ComparePatches match a resource and remove fields, or the resource itself from the check for modifications.
spec.diff.comparePatches[].apiVersion <string>	# APIVersion is the apiVersion of the resource to match.
spec.diff.comparePatches[].jsonPointers <[]string>	# JSONPointers ignore diffs at a certain JSON path.
spec.diff.comparePatches[].kind <string>	# Kind is the kind of the resource to match.
spec.diff.comparePatches[].name <string>	# Name is the name of the resource to match.
spec.diff.comparePatches[].namespace <string>	# Namespace is the namespace of the resource to match.
spec.diff.comparePatches[].operations <[]object>	# Operations remove a JSON path from the resource.
spec.diff.comparePatches[].operations[].op <string>	# Op is usually "remove" or "ignore"
spec.diff.comparePatches[].operations[].path <string>	# Path is the JSON path to remove. Not needed if Op is "ignore".
spec.diff.comparePatches[].operations[].value <string>	# Value is usually empty.
spec.downstreamResources <[]object>	# DownstreamResources points to resources to be copied into downstream clusters, from the bundle's namespace.
spec.downstreamResources[].kind <string>
spec.downstreamResources[].name <string>
spec.forceSyncGeneration <integer> format=int64	# ForceSyncGeneration is used to force a redeployment
spec.helm <object>	# Helm options for the deployment, like the chart name, repo and values.
spec.helm.atomic <boolean>	# Atomic sets the --atomic flag when Helm is performing an upgrade
spec.helm.chart <string>	# Chart can refer to any go-getter URL or OCI registry based helm chart URL. The chart will be downloaded.
spec.helm.disableDNS <boolean>	# DisableDNS can be used to customize Helm's EnableDNS option, which Fleet sets to `true` by default.
spec.helm.disableDependencyUpdate <boolean>	# DisableDependencyUpdate allows skipping chart dependencies update
spec.helm.disablePreProcess <boolean>	# DisablePreProcess disables template processing in values
spec.helm.force <boolean>	# Force allows to override immutable resources. This could be dangerous.
spec.helm.maxHistory <integer>	# MaxHistory limits the maximum number of revisions saved per release by Helm.
spec.helm.releaseName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=53	# ReleaseName sets a custom release name to deploy the chart as. If not specified a release name will be generated by combining the invoking GitRepo.name + GitRepo.path.
spec.helm.repo <string>	# Repo is the name of the HTTPS helm repo to download the chart from.
spec.helm.skipSchemaValidation <boolean>	# SkipSchemaValidation allows skipping schema validation against the chart values
spec.helm.takeOwnership <boolean>	# TakeOwnership makes helm skip the check for its own annotations
spec.helm.templateValues <map[string]string>	# Template Values passed to Helm. It is possible to specify the keys and values as go template strings. Unlike .values, content of each key will be templated first, before serializing to yaml. This allows to template complex values, like ranges and maps. templateValues keys have precedence over values keys in case of conflict.
spec.helm.timeoutSeconds <integer>	# TimeoutSeconds is the time to wait for Helm operations.
spec.helm.values <object (free-form)>	# Values passed to Helm. It is possible to specify the keys and values as go template strings.
spec.helm.valuesFiles <[]string>	# ValuesFiles is a list of files to load values from.
spec.helm.valuesFrom <[]object>	# ValuesFrom loads the values from configmaps and secrets.
spec.helm.valuesFrom[].configMapKeyRef <object>	# The reference to a config map with release values.
spec.helm.valuesFrom[].configMapKeyRef.key <string>
spec.helm.valuesFrom[].configMapKeyRef.name <string>	# Name of a resource in the same namespace as the referent.
spec.helm.valuesFrom[].configMapKeyRef.namespace <string>
spec.helm.valuesFrom[].secretKeyRef <object>	# The reference to a secret with release values.
spec.helm.valuesFrom[].secretKeyRef.key <string>
spec.helm.valuesFrom[].secretKeyRef.name <string>	# Name of a resource in the same namespace as the referent.
spec.helm.valuesFrom[].secretKeyRef.namespace <string>
spec.helm.version <string>	# Version of the chart to download
spec.helm.waitForJobs <boolean>	# WaitForJobs if set and timeoutSeconds provided, will wait until all Jobs have been completed before marking the GitRepo as ready. It will wait for as long as timeoutSeconds
spec.helmOpOptions <object>	# HelmOpOptions stores the options relative to HelmOp resources Non-nil HelmOpOptions indicate that the source of resources is a Helm chart, not a git repository.
spec.helmOpOptions.helmOpInsecureSkipTLSVerify <boolean>	# InsecureSkipTLSverify will use insecure HTTPS to clone the helm app resource.
spec.helmOpOptions.helmOpSecretName <string>	# SecretName stores the secret name for storing credentials when accessing a remote helm repository defined in a HelmOp resource
spec.ignore <object>	# IgnoreOptions can be used to ignore fields when monitoring the bundle.
spec.ignore.conditions <[]object>	# Conditions is a list of conditions to be ignored when monitoring the Bundle.
spec.keepResources <boolean>	# KeepResources can be used to keep the deployed resources when removing the bundle
spec.kustomize <object>	# Kustomize options for the deployment, like the dir containing the kustomization.yaml file.
spec.kustomize.dir <string>	# Dir points to a custom folder for kustomize resources. This folder must contain a kustomization.yaml file.
spec.namespace <string>	# TargetNamespace if present will assign all resource to this namespace and if any cluster scoped resource exists the deployment will fail.
spec.namespaceAnnotations <map[string]string>	# NamespaceAnnotations are annotations that will be appended to the namespace created by Fleet.
spec.namespaceLabels <map[string]string>	# NamespaceLabels are labels that will be appended to the namespace created by Fleet.
spec.overwrites <[]object>	# Overwrites indicates which resources, if any, come from this bundle and overwrite another existing bundle. This flag is set internally by Fleet, and should not be altered by users.
spec.overwrites[].kind <string>
spec.overwrites[].name <string>
spec.overwrites[].namespace <string>
spec.paused <boolean>	# Paused if set to true, will stop any BundleDeployments from being updated. It will be marked as out of sync.
spec.resources <[]object>	# Resources contains the resources that were read from the bundle's path. This includes the content of downloaded helm charts.
spec.resources[].content <string>	# The content of the resource, can be compressed.
spec.resources[].encoding <string>	# Encoding is either empty or "base64+gz".
spec.resources[].name <string>	# Name of the resource, can include the bundle's internal path.
spec.rolloutStrategy <object>	# RolloutStrategy controls the rollout of bundles, by defining partitions, canaries and percentages for cluster availability.
spec.rolloutStrategy.autoPartitionSize <int-or-string>	# A number or percentage of how to automatically partition clusters if no specific partitioning strategy is configured. default: 25%
spec.rolloutStrategy.autoPartitionThreshold <integer>	# AutoPartitionThreshold is the minimum number of clusters that need to be present before auto-partitioning is enabled. If the number of target clusters is less than this value, all clusters will be placed in a single partition. default: 200
spec.rolloutStrategy.maxUnavailable <int-or-string>	# A number or percentage of clusters that can be unavailable during an update of a bundle. This follows the same basic approach as a deployment rollout strategy. Once the number of clusters meets unavailable state update will be paused. Default value is 100% which doesn't take effect on update. default: 100%
spec.rolloutStrategy.maxUnavailablePartitions <int-or-string>	# A number or percentage of cluster partitions that can be unavailable during an update of a bundle. default: 0
spec.rolloutStrategy.partitions <[]object>	# A list of definitions of partitions. If any target clusters do not match the configuration they are added to partitions at the end following the autoPartitionSize.
spec.rolloutStrategy.partitions[].clusterGroup <string>	# A cluster group name to include in this partition
spec.rolloutStrategy.partitions[].clusterGroupSelector <object>	# Selector matching cluster group labels to include in this partition
spec.rolloutStrategy.partitions[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rolloutStrategy.partitions[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rolloutStrategy.partitions[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rolloutStrategy.partitions[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rolloutStrategy.partitions[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rolloutStrategy.partitions[].clusterName <string>	# ClusterName is the name of a cluster to include in this partition
spec.rolloutStrategy.partitions[].clusterSelector <object>	# Selector matching cluster labels to include in this partition
spec.rolloutStrategy.partitions[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.rolloutStrategy.partitions[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.rolloutStrategy.partitions[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.rolloutStrategy.partitions[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.rolloutStrategy.partitions[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.rolloutStrategy.partitions[].maxUnavailable <int-or-string>	# A number or percentage of clusters that can be unavailable in this partition before this partition is treated as done. default: 10%
spec.rolloutStrategy.partitions[].name <string>	# A user-friendly name given to the partition used for Display (optional).
spec.serviceAccount <string>	# ServiceAccount which will be used to perform this deployment.
spec.targetRestrictions <[]object>	# TargetRestrictions is an allow list, which controls if a bundledeployment is created for a target.
spec.targetRestrictions[].clusterGroup <string>
spec.targetRestrictions[].clusterGroupSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.targetRestrictions[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targetRestrictions[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targetRestrictions[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targetRestrictions[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targetRestrictions[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targetRestrictions[].clusterName <string>
spec.targetRestrictions[].clusterSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.targetRestrictions[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targetRestrictions[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targetRestrictions[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targetRestrictions[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targetRestrictions[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targetRestrictions[].name <string>
spec.targets <[]object>	# Targets refer to the clusters which will be deployed to. Targets are evaluated in order and the first one to match is used.
spec.targets[].allowedTargetNamespaceSelector <object>	# AllowedTargetNamespaceSelector restricts deployments to namespaces matching this selector. Propagated from GitRepoRestriction and validated by the agent on the downstream cluster.
spec.targets[].allowedTargetNamespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets[].allowedTargetNamespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets[].allowedTargetNamespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets[].allowedTargetNamespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets[].allowedTargetNamespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets[].clusterGroup <string>	# ClusterGroup to match a specific cluster group by name.
spec.targets[].clusterGroupSelector <object>	# ClusterGroupSelector is a selector to match cluster groups.
spec.targets[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets[].clusterName <string>	# ClusterName to match a specific cluster by name that will be selected
spec.targets[].clusterSelector <object>	# ClusterSelector is a selector to match clusters. The structure is the standard metav1.LabelSelector format. If clusterGroupSelector or clusterGroup is specified, clusterSelector will be used only to further refine the selection after clusterGroupSelector and clusterGroup is evaluated.
spec.targets[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets[].correctDrift <object>	# CorrectDrift specifies how drift correction should work.
spec.targets[].correctDrift.enabled <boolean>	# Enabled correct drift if true.
spec.targets[].correctDrift.force <boolean>	# Force helm rollback with --force option will be used if true. This will try to recreate all resources in the release.
spec.targets[].correctDrift.keepFailHistory <boolean>	# KeepFailHistory keeps track of failed rollbacks in the helm history.
spec.targets[].defaultNamespace <string>	# DefaultNamespace is the namespace to use for resources that do not specify a namespace. This field is not used to enforce or lock down the deployment to a specific namespace.
spec.targets[].deleteCRDResources <boolean>	# DeleteCRDResources deletes CRDs. Warning! this will also delete all your Custom Resources.
spec.targets[].deleteNamespace <boolean>	# DeleteNamespace can be used to delete the deployed namespace when removing the bundle
spec.targets[].diff <object>	# Diff can be used to ignore the modified state of objects which are amended at runtime.
spec.targets[].diff.comparePatches <[]object>	# ComparePatches match a resource and remove fields, or the resource itself from the check for modifications.
spec.targets[].diff.comparePatches[].apiVersion <string>	# APIVersion is the apiVersion of the resource to match.
spec.targets[].diff.comparePatches[].jsonPointers <[]string>	# JSONPointers ignore diffs at a certain JSON path.
spec.targets[].diff.comparePatches[].kind <string>	# Kind is the kind of the resource to match.
spec.targets[].diff.comparePatches[].name <string>	# Name is the name of the resource to match.
spec.targets[].diff.comparePatches[].namespace <string>	# Namespace is the namespace of the resource to match.
spec.targets[].diff.comparePatches[].operations <[]object>	# Operations remove a JSON path from the resource.
spec.targets[].diff.comparePatches[].operations[].op <string>	# Op is usually "remove" or "ignore"
spec.targets[].diff.comparePatches[].operations[].path <string>	# Path is the JSON path to remove. Not needed if Op is "ignore".
spec.targets[].diff.comparePatches[].operations[].value <string>	# Value is usually empty.
spec.targets[].doNotDeploy <boolean>	# DoNotDeploy if set to true, will not deploy to this target.
spec.targets[].downstreamResources <[]object>	# DownstreamResources points to resources to be copied into downstream clusters, from the bundle's namespace.
spec.targets[].downstreamResources[].kind <string>
spec.targets[].downstreamResources[].name <string>
spec.targets[].forceSyncGeneration <integer> format=int64	# ForceSyncGeneration is used to force a redeployment
spec.targets[].helm <object>	# Helm options for the deployment, like the chart name, repo and values.
spec.targets[].helm.atomic <boolean>	# Atomic sets the --atomic flag when Helm is performing an upgrade
spec.targets[].helm.chart <string>	# Chart can refer to any go-getter URL or OCI registry based helm chart URL. The chart will be downloaded.
spec.targets[].helm.disableDNS <boolean>	# DisableDNS can be used to customize Helm's EnableDNS option, which Fleet sets to `true` by default.
spec.targets[].helm.disableDependencyUpdate <boolean>	# DisableDependencyUpdate allows skipping chart dependencies update
spec.targets[].helm.disablePreProcess <boolean>	# DisablePreProcess disables template processing in values
spec.targets[].helm.force <boolean>	# Force allows to override immutable resources. This could be dangerous.
spec.targets[].helm.maxHistory <integer>	# MaxHistory limits the maximum number of revisions saved per release by Helm.
spec.targets[].helm.releaseName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=53	# ReleaseName sets a custom release name to deploy the chart as. If not specified a release name will be generated by combining the invoking GitRepo.name + GitRepo.path.
spec.targets[].helm.repo <string>	# Repo is the name of the HTTPS helm repo to download the chart from.
spec.targets[].helm.skipSchemaValidation <boolean>	# SkipSchemaValidation allows skipping schema validation against the chart values
spec.targets[].helm.takeOwnership <boolean>	# TakeOwnership makes helm skip the check for its own annotations
spec.targets[].helm.templateValues <map[string]string>	# Template Values passed to Helm. It is possible to specify the keys and values as go template strings. Unlike .values, content of each key will be templated first, before serializing to yaml. This allows to template complex values, like ranges and maps. templateValues keys have precedence over values keys in case of conflict.
spec.targets[].helm.timeoutSeconds <integer>	# TimeoutSeconds is the time to wait for Helm operations.
spec.targets[].helm.values <object (free-form)>	# Values passed to Helm. It is possible to specify the keys and values as go template strings.
spec.targets[].helm.valuesFiles <[]string>	# ValuesFiles is a list of files to load values from.
spec.targets[].helm.valuesFrom <[]object>	# ValuesFrom loads the values from configmaps and secrets.
spec.targets[].helm.valuesFrom[].configMapKeyRef <object>	# The reference to a config map with release values.
spec.targets[].helm.valuesFrom[].configMapKeyRef.key <string>
spec.targets[].helm.valuesFrom[].configMapKeyRef.name <string>	# Name of a resource in the same namespace as the referent.
spec.targets[].helm.valuesFrom[].configMapKeyRef.namespace <string>
spec.targets[].helm.valuesFrom[].secretKeyRef <object>	# The reference to a secret with release values.
spec.targets[].helm.valuesFrom[].secretKeyRef.key <string>
spec.targets[].helm.valuesFrom[].secretKeyRef.name <string>	# Name of a resource in the same namespace as the referent.
spec.targets[].helm.valuesFrom[].secretKeyRef.namespace <string>
spec.targets[].helm.version <string>	# Version of the chart to download
spec.targets[].helm.waitForJobs <boolean>	# WaitForJobs if set and timeoutSeconds provided, will wait until all Jobs have been completed before marking the GitRepo as ready. It will wait for as long as timeoutSeconds
spec.targets[].ignore <object>	# IgnoreOptions can be used to ignore fields when monitoring the bundle.
spec.targets[].ignore.conditions <[]object>	# Conditions is a list of conditions to be ignored when monitoring the Bundle.
spec.targets[].keepResources <boolean>	# KeepResources can be used to keep the deployed resources when removing the bundle
spec.targets[].kustomize <object>	# Kustomize options for the deployment, like the dir containing the kustomization.yaml file.
spec.targets[].kustomize.dir <string>	# Dir points to a custom folder for kustomize resources. This folder must contain a kustomization.yaml file.
spec.targets[].name <string>	# Name of target. This value is largely for display and logging. If not specified a default name of the format "target000" will be used
spec.targets[].namespace <string>	# TargetNamespace if present will assign all resource to this namespace and if any cluster scoped resource exists the deployment will fail.
spec.targets[].namespaceAnnotations <map[string]string>	# NamespaceAnnotations are annotations that will be appended to the namespace created by Fleet.
spec.targets[].namespaceLabels <map[string]string>	# NamespaceLabels are labels that will be appended to the namespace created by Fleet.
spec.targets[].overwrites <[]object>	# Overwrites indicates which resources, if any, come from this bundle and overwrite another existing bundle. This flag is set internally by Fleet, and should not be altered by users.
spec.targets[].overwrites[].kind <string>
spec.targets[].overwrites[].name <string>
spec.targets[].overwrites[].namespace <string>
spec.targets[].serviceAccount <string>	# ServiceAccount which will be used to perform this deployment.
spec.targets[].yaml <object>	# YAML options, if using raw YAML these are names that map to overlays/{name} files that will be used to replace or patch a resource.
spec.targets[].yaml.overlays <[]string>	# Overlays is a list of names that maps to folders in "overlays/". If you wish to customize the file ./subdir/resource.yaml then a file ./overlays/myoverlay/subdir/resource.yaml will replace the base file. A file named ./overlays/myoverlay/subdir/resource_patch.yaml will patch the base file.
spec.valuesHash <string>	# ValuesHash is the hash of the values used to render the Helm chart. It changes when any values from fleet.yaml, values from ValuesFiles or values from target customization changes.
spec.yaml <object>	# YAML options, if using raw YAML these are names that map to overlays/{name} files that will be used to replace or patch a resource.
spec.yaml.overlays <[]string>	# Overlays is a list of names that maps to folders in "overlays/". If you wish to customize the file ./subdir/resource.yaml then a file ./overlays/myoverlay/subdir/resource.yaml will replace the base file. A file named ./overlays/myoverlay/subdir/resource_patch.yaml will patch the base file.
status <object>
status.conditions <[]object>	# Conditions is a list of Wrangler conditions that describe the state of the bundle.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string>	# The last time this condition was updated.
status.conditions[].message <string>	# Human-readable message indicating details about last transition
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of cluster condition.
status.display <object>	# Display contains the number of ready, desiredready clusters and a summary state for the bundle's resources.
status.display.readyClusters <string>	# ReadyClusters is a string in the form "%d/%d", that describes the number of clusters that are ready vs. the number of clusters desired to be ready.
status.display.state <string>	# State is a summary state for the bundle, calculated over the non-ready resources.
status.maxNew <integer>	# MaxNew is always 50. A bundle change can only stage 50 bundledeployments at a time.
status.maxUnavailable <integer>	# MaxUnavailable is the maximum number of unavailable deployments. See rollout configuration.
status.maxUnavailablePartitions <integer>	# MaxUnavailablePartitions is the maximum number of unavailable partitions. The rollout configuration defines a maximum number or percentage of unavailable partitions.
status.newlyCreated <integer>	# NewlyCreated is the number of bundle deployments that have been created, not updated.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the current generation of the bundle.
status.ociReference <string>	# OCIReference is the OCI reference used to store contents, this is only for informational purposes.
status.partitions <[]object>	# PartitionStatus lists the status of each partition.
status.partitions[].count <integer>	# Count is the number of clusters in the partition.
status.partitions[].maxUnavailable <integer>	# MaxUnavailable is the maximum number of unavailable clusters in the partition.
status.partitions[].name <string>	# Name is the name of the partition.
status.partitions[].summary <object>	# Summary is a summary state for the partition, calculated over its non-ready resources.
status.partitions[].summary.desiredReady <integer>	# DesiredReady is the number of bundle deployments that should be ready.
status.partitions[].summary.errApplied <integer>	# ErrApplied is the number of bundle deployments that have been synced from the Fleet controller and the downstream cluster, but with some errors when deploying the bundle.
status.partitions[].summary.modified <integer>	# Modified is the number of bundle deployments that have been deployed and for which all resources are ready, but where some changes from the Git repository have not yet been synced.
status.partitions[].summary.nonReadyResources <[]object>	# NonReadyClusters is a list of states, which is filled for a bundle that is not ready.
status.partitions[].summary.nonReadyResources[].bundleState <string>	# State is the state of the resource, like e.g. "NotReady" or "ErrApplied".
status.partitions[].summary.nonReadyResources[].message <string>	# Message contains information why the bundle is not ready.
status.partitions[].summary.nonReadyResources[].modifiedStatus <[]object>	# ModifiedStatus lists the state for each modified resource.
status.partitions[].summary.nonReadyResources[].modifiedStatus[].apiVersion <string>
status.partitions[].summary.nonReadyResources[].modifiedStatus[].delete <boolean>
status.partitions[].summary.nonReadyResources[].modifiedStatus[].exist <boolean>	# Exist is true if the resource exists but is not owned by us. This can happen if a resource was adopted by another bundle whereas the first bundle still exists and due to that reports that it does not own it.
status.partitions[].summary.nonReadyResources[].modifiedStatus[].kind <string>
status.partitions[].summary.nonReadyResources[].modifiedStatus[].missing <boolean>
status.partitions[].summary.nonReadyResources[].modifiedStatus[].name <string>
status.partitions[].summary.nonReadyResources[].modifiedStatus[].namespace <string>
status.partitions[].summary.nonReadyResources[].modifiedStatus[].patch <string>
status.partitions[].summary.nonReadyResources[].name <string>	# Name is the name of the resource.
status.partitions[].summary.nonReadyResources[].nonReadyStatus <[]object>	# NonReadyStatus lists the state for each non-ready resource.
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].apiVersion <string>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].kind <string>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].name <string>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].namespace <string>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].summary <object>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].summary.error <boolean>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].summary.message <[]string>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].summary.state <string>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].summary.transitioning <boolean>
status.partitions[].summary.nonReadyResources[].nonReadyStatus[].uid <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.partitions[].summary.notReady <integer>	# NotReady is the number of bundle deployments that have been deployed where some resources are not ready.
status.partitions[].summary.outOfSync <integer>	# OutOfSync is the number of bundle deployments that have been synced from Fleet controller, but not yet by the downstream agent.
status.partitions[].summary.pending <integer>	# Pending is the number of bundle deployments that are being processed by Fleet controller.
status.partitions[].summary.ready <integer>	# Ready is the number of bundle deployments that have been deployed where all resources are ready.
status.partitions[].summary.waitApplied <integer>	# WaitApplied is the number of bundle deployments that have been synced from Fleet controller and downstream cluster, but are waiting to be deployed.
status.partitions[].unavailable <integer>	# Unavailable is the number of unavailable clusters in the partition.
status.resourceKey <[]object>	# ResourceKey lists resources, which will likely be deployed. The actual list of resources on a cluster might differ, depending on the helm chart, value templating, etc.. (deprecated to reduce bundle size)
status.resourceKey[].apiVersion <string>	# APIVersion is the k8s api version of the resource.
status.resourceKey[].kind <string>	# Kind is the k8s api kind of the resource.
status.resourceKey[].name <string>	# Name is the name of the resource.
status.resourceKey[].namespace <string>	# Namespace is the namespace of the resource.
status.resourcesSha256Sum <string>	# ResourcesSHA256Sum corresponds to the JSON serialization of the .Spec.Resources field
status.summary <object>	# Summary contains the number of bundle deployments in each state and a list of non-ready resources.
status.summary.desiredReady <integer>	# DesiredReady is the number of bundle deployments that should be ready.
status.summary.errApplied <integer>	# ErrApplied is the number of bundle deployments that have been synced from the Fleet controller and the downstream cluster, but with some errors when deploying the bundle.
status.summary.modified <integer>	# Modified is the number of bundle deployments that have been deployed and for which all resources are ready, but where some changes from the Git repository have not yet been synced.
status.summary.nonReadyResources <[]object>	# NonReadyClusters is a list of states, which is filled for a bundle that is not ready.
status.summary.nonReadyResources[].bundleState <string>	# State is the state of the resource, like e.g. "NotReady" or "ErrApplied".
status.summary.nonReadyResources[].message <string>	# Message contains information why the bundle is not ready.
status.summary.nonReadyResources[].modifiedStatus <[]object>	# ModifiedStatus lists the state for each modified resource.
status.summary.nonReadyResources[].modifiedStatus[].apiVersion <string>
status.summary.nonReadyResources[].modifiedStatus[].delete <boolean>
status.summary.nonReadyResources[].modifiedStatus[].exist <boolean>	# Exist is true if the resource exists but is not owned by us. This can happen if a resource was adopted by another bundle whereas the first bundle still exists and due to that reports that it does not own it.
status.summary.nonReadyResources[].modifiedStatus[].kind <string>
status.summary.nonReadyResources[].modifiedStatus[].missing <boolean>
status.summary.nonReadyResources[].modifiedStatus[].name <string>
status.summary.nonReadyResources[].modifiedStatus[].namespace <string>
status.summary.nonReadyResources[].modifiedStatus[].patch <string>
status.summary.nonReadyResources[].name <string>	# Name is the name of the resource.
status.summary.nonReadyResources[].nonReadyStatus <[]object>	# NonReadyStatus lists the state for each non-ready resource.
status.summary.nonReadyResources[].nonReadyStatus[].apiVersion <string>
status.summary.nonReadyResources[].nonReadyStatus[].kind <string>
status.summary.nonReadyResources[].nonReadyStatus[].name <string>
status.summary.nonReadyResources[].nonReadyStatus[].namespace <string>
status.summary.nonReadyResources[].nonReadyStatus[].summary <object>
status.summary.nonReadyResources[].nonReadyStatus[].summary.error <boolean>
status.summary.nonReadyResources[].nonReadyStatus[].summary.message <[]string>
status.summary.nonReadyResources[].nonReadyStatus[].summary.state <string>
status.summary.nonReadyResources[].nonReadyStatus[].summary.transitioning <boolean>
status.summary.nonReadyResources[].nonReadyStatus[].uid <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.summary.notReady <integer>	# NotReady is the number of bundle deployments that have been deployed where some resources are not ready.
status.summary.outOfSync <integer>	# OutOfSync is the number of bundle deployments that have been synced from Fleet controller, but not yet by the downstream agent.
status.summary.pending <integer>	# Pending is the number of bundle deployments that are being processed by Fleet controller.
status.summary.ready <integer>	# Ready is the number of bundle deployments that have been deployed where all resources are ready.
status.summary.waitApplied <integer>	# WaitApplied is the number of bundle deployments that have been synced from Fleet controller and downstream cluster, but are waiting to be deployed.
status.unavailable <integer>	# Unavailable is the number of bundle deployments that are not ready or where the AppliedDeploymentID in the status does not match the DeploymentID from the spec.
status.unavailablePartitions <integer>	# UnavailablePartitions is the number of unavailable partitions.
