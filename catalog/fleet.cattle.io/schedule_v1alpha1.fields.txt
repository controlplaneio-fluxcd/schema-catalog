# schema source: Rancher Fleet v0.15.4 https://github.com/rancher/fleet
apiVersion <string> enum=fleet.cattle.io/v1alpha1
kind <string> enum=Schedule
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.duration <string>
spec.location <string>
spec.schedule <string>
spec.targets <object>	# Targets is a list of resources affected by this schedule
spec.targets.clusters <[]object>
spec.targets.clusters[].clusterGroup <string>	# ClusterGroup is the name of a cluster group in the same namespace as the clusters.
spec.targets.clusters[].clusterGroupSelector <object>	# ClusterGroupSelector is a label selector to select cluster groups.
spec.targets.clusters[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets.clusters[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets.clusters[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets.clusters[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets.clusters[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets.clusters[].clusterName <string>	# ClusterName is the name of a cluster.
spec.targets.clusters[].clusterSelector <object>	# ClusterSelector is a label selector to select clusters.
spec.targets.clusters[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets.clusters[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets.clusters[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets.clusters[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets.clusters[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets.clusters[].name <string>	# Name is the name of this target.
status <object>
status.active <boolean>	# Active is set to true when the Schedule is actively running
status.conditions <[]object>	# Conditions is a list of Wrangler conditions that describe the state of the resource.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string>	# The last time this condition was updated.
status.conditions[].message <string>	# Human-readable message indicating details about last transition
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of cluster condition.
status.matchingClusters <[]string>	# MatchingClusters is the list of clusters targeted by the Schedule
status.nextStartTime <string> format=date-time	# NextStartTime stores the next time this Schedule will start
