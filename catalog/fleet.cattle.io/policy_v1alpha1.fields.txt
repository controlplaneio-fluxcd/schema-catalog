# schema source: Fleet v0.15.4 https://github.com/rancher/fleet
apiVersion <string> enum=fleet.cattle.io/v1alpha1
kind <string> enum=Policy
metadata.name <string> (required)
metadata.namespace <string> (required)
allowedServiceAccounts <[]string>	# AllowedServiceAccounts lists service accounts that may be used. If non-empty, the ServiceAccount must appear in this list. When RequireServiceAccount is also true, an empty ServiceAccount is rejected regardless of this list.
gitRepo <object>	# GitRepo contains restrictions and defaults applied only by the GitRepo reconciler.
gitRepo.allowedClientSecretNames <[]string>	# AllowedClientSecretNames lists client secret names that GitRepo objects may reference.
gitRepo.allowedRepoPatterns <[]string>	# AllowedRepoPatterns is a list of regex patterns restricting the Repo field of GitRepo objects.
gitRepo.defaultClientSecretName <string>	# DefaultClientSecretName is applied to GitRepo objects whose ClientSecretName is empty.
gitRepo.defaultServiceAccount <string>	# DefaultServiceAccount is applied to GitRepo objects whose ServiceAccount is empty, before the top-level RequireServiceAccount check runs.
helmOp <object>	# HelmOp contains restrictions and defaults applied only by the HelmOp reconciler.
helmOp.allowedChartPatterns <[]string>	# AllowedChartPatterns is a list of regex patterns restricting the spec.helm.chart field of HelmOp objects.
helmOp.allowedHelmRepoPatterns <[]string>	# AllowedHelmRepoPatterns is a list of regex patterns restricting the spec.helm.repo field of HelmOp objects.
helmOp.allowedHelmSecretNames <[]string>	# AllowedHelmSecretNames lists credential secret names that HelmOp objects may reference.
helmOp.defaultHelmSecretName <string>	# DefaultHelmSecretName is applied to HelmOp objects whose HelmSecretName is empty.
helmOp.defaultServiceAccount <string>	# DefaultServiceAccount is applied to HelmOp objects whose ServiceAccount is empty, before the top-level RequireServiceAccount check runs.
requireServiceAccount <boolean>	# RequireServiceAccount, when true, rejects any GitRepo, HelmOp, or Bundle whose ServiceAccount is empty after any defaulting has been applied. Combine with AllowedServiceAccounts to also restrict which account is used.
