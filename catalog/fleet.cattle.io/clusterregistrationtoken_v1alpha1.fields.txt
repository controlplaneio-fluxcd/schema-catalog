# schema source: Rancher Fleet v0.16.0 https://github.com/rancher/fleet
apiVersion <string> enum=fleet.cattle.io/v1alpha1
kind <string> enum=ClusterRegistrationToken
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.ttl <string> min=1 max=32	# TTL is the time to live for the token. It is used to calculate the expiration time. If the token expires, it will be deleted.
status <object>
status.expires <string> format=date-time	# Expires is the time when the token expires.
status.secretName <string>	# SecretName is the name of the secret containing the token.
