# schema source: Fleet v0.15.4 https://github.com/rancher/fleet
apiVersion <string> enum=fleet.cattle.io/v1alpha1
kind <string> enum=GitRepo
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.branch <string>	# Branch The git branch to follow.
spec.bundles <[]object>	# Bundles defines the paths of bundles to be read. This drives the fleet resource scanner that simply loads the specified folders
spec.bundles[].base <string>	# Base is the base path for the bundle resources
spec.bundles[].options <string>	# Options is the path (relative to path above) that defines a fleet.yaml file to configure the bundle
spec.caBundle <string> format=byte	# CABundle is a PEM encoded CA bundle which will be used to validate the repo's certificate.
spec.clientSecretName <string>	# ClientSecretName is the name of the client secret to be used to connect to the repo It is expected the secret be of type "kubernetes.io/basic-auth" or "kubernetes.io/ssh-auth".
spec.correctDrift <object>	# CorrectDrift specifies how drift correction should work.
spec.correctDrift.enabled <boolean>	# Enabled correct drift if true.
spec.correctDrift.force <boolean>	# Force helm rollback with --force option will be used if true. This will try to recreate all resources in the release.
spec.correctDrift.keepFailHistory <boolean>	# KeepFailHistory keeps track of failed rollbacks in the helm history.
spec.deleteNamespace <boolean>	# DeleteNamespace specifies if the namespace created must be deleted after deleting the GitRepo.
spec.disablePolling <boolean>	# Disables git polling. When enabled only webhooks will be used.
spec.forceSyncGeneration <integer> format=int64	# Increment this number to force a redeployment of contents from Git.
spec.helmRepoURLRegex <string>	# HelmRepoURLRegex Helm credentials will be used if the helm repo matches this regex. Credentials will not be used if this is empty or not provided.
spec.helmSecretName <string>	# HelmSecretName contains the auth secret for a private Helm repository.
spec.helmSecretNameForPaths <string>	# HelmSecretNameForPaths contains the auth secret for private Helm repository for each path.
spec.imageScanCommit <object>	# Commit specifies how to commit to the git repo when a new image is scanned and written back to git repo.
spec.imageScanCommit.authorEmail <string>	# AuthorEmail gives the email to provide when making a commit
spec.imageScanCommit.authorName <string>	# AuthorName gives the name to provide when making a commit
spec.imageScanCommit.messageTemplate <string>	# MessageTemplate provides a template for the commit message, into which will be interpolated the details of the change made.
spec.imageScanInterval <string>	# ImageScanInterval is the interval of syncing scanned images and writing back to git repo.
spec.insecureSkipTLSVerify <boolean>	# InsecureSkipTLSverify will use insecure HTTPS to clone the repo.
spec.keepResources <boolean>	# KeepResources specifies if the resources created must be kept after deleting the GitRepo.
spec.ociRegistrySecret <string>	# OCIRegistrySecret contains the name of the secret to be used for retrieving the OCI registry connection details.
spec.paths <[]string>	# Paths is the directories relative to the git repo root that contain resources to be applied. Path globbing is supported, for example ["charts/*"] will match all folders as a subdirectory of charts/ If empty, "/" is the default.
spec.paused <boolean>	# Paused, when true, causes changes in Git not to be propagated down to the clusters but instead to mark resources as OutOfSync.
spec.pollingInterval <string>	# PollingInterval is how often to check git for new updates.
spec.repo <string> (required) min=1	# Repo is a URL to a git repo to clone and index.
spec.revision <string>	# Revision A specific commit or tag to operate on.
spec.serviceAccount <string>	# ServiceAccount used in the downstream cluster for deployment.
spec.targetNamespace <string>	# Ensure that all resources are created in this namespace Any cluster scoped resource will be rejected if this is set Additionally this namespace will be created on demand.
spec.targets <[]object>	# Targets is a list of targets this repo will deploy to.
spec.targets[].clusterGroup <string>	# ClusterGroup is the name of a cluster group in the same namespace as the clusters.
spec.targets[].clusterGroupSelector <object>	# ClusterGroupSelector is a label selector to select cluster groups.
spec.targets[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets[].clusterName <string>	# ClusterName is the name of a cluster.
spec.targets[].clusterSelector <object>	# ClusterSelector is a label selector to select clusters.
spec.targets[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.targets[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.targets[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.targets[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.targets[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.targets[].name <string>	# Name is the name of this target.
spec.webhookSecret <string>	# WebhookSecret contains the name of the secret to use for webhook parsing
status <object>
status.commit <string>	# Commit is the Git commit hash from the last git job run.
status.conditions <[]object>	# Conditions is a list of Wrangler conditions that describe the state of the resource.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string>	# The last time this condition was updated.
status.conditions[].message <string>	# Human-readable message indicating details about last transition
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of cluster condition.
status.desiredReadyClusters <integer>	# DesiredReadyClusters is the number of clusters that should be ready for bundles of this resource.
status.display <object>	# Display contains a human readable summary of the status.
status.display.error <boolean>	# Error is true if a message is present.
status.display.message <string>	# Message contains the relevant message from the deployment conditions.
status.display.readyBundleDeployments <string>	# ReadyBundleDeployments is a string in the form "%d/%d", that describes the number of ready bundledeployments over the total number of bundledeployments.
status.display.state <string>	# State is the state of the resource, e.g. "GitUpdating" or the maximal BundleState according to StateRank.
status.gitJobStatus <string>	# GitJobStatus is the status of the last Git job run, e.g. "Current" if there was no error.
status.lastPollingTriggered <string> format=date-time	# LastPollingTime is the last time the polling check was triggered
status.lastSyncedImageScanTime <string> format=date-time	# LastSyncedImageScanTime is the time of the last image scan.
status.lastWebhookTime <string> format=date-time	# LastWebhookTime is the time of the last received webhook notification
status.observedGeneration <integer> format=int64	# ObservedGeneration is the current generation of the resource in the cluster. It is copied from k8s metadata.Generation. The value is incremented for all changes, except for changes to .metadata or .status.
status.perClusterResourceCounts <map[string]object>	# PerClusterResourceCounts contains the number of resources in each state over all bundles, per cluster.
status.perClusterResourceCounts.<key>.desiredReady <integer>	# DesiredReady is the number of resources that should be ready.
status.perClusterResourceCounts.<key>.missing <integer>	# Missing is the number of missing resources.
status.perClusterResourceCounts.<key>.modified <integer>	# Modified is the number of resources that have been modified.
status.perClusterResourceCounts.<key>.notReady <integer>	# NotReady is the number of not ready resources. Resources are not ready if they do not match any other state.
status.perClusterResourceCounts.<key>.orphaned <integer>	# Orphaned is the number of orphaned resources.
status.perClusterResourceCounts.<key>.ready <integer>	# Ready is the number of ready resources.
status.perClusterResourceCounts.<key>.unknown <integer>	# Unknown is the number of resources in an unknown state.
status.perClusterResourceCounts.<key>.waitApplied <integer>	# WaitApplied is the number of resources that are waiting to be applied.
status.pollingCommit <string>	# PollingCommit is the latest Git commit hash received from polling
status.readyClusters <integer>	# ReadyClusters is the lowest number of clusters that are ready over all the bundles of this resource.
status.resourceCounts <object>	# ResourceCounts contains the number of resources in each state over all bundles.
status.resourceCounts.desiredReady <integer>	# DesiredReady is the number of resources that should be ready.
status.resourceCounts.missing <integer>	# Missing is the number of missing resources.
status.resourceCounts.modified <integer>	# Modified is the number of resources that have been modified.
status.resourceCounts.notReady <integer>	# NotReady is the number of not ready resources. Resources are not ready if they do not match any other state.
status.resourceCounts.orphaned <integer>	# Orphaned is the number of orphaned resources.
status.resourceCounts.ready <integer>	# Ready is the number of ready resources.
status.resourceCounts.unknown <integer>	# Unknown is the number of resources in an unknown state.
status.resourceCounts.waitApplied <integer>	# WaitApplied is the number of resources that are waiting to be applied.
status.resources <[]object>	# Resources contains metadata about the resources of each bundle.
status.resources[].apiVersion <string>	# APIVersion is the API version of the resource.
status.resources[].error <boolean>	# Error is true if any Error in the PerClusterState is true.
status.resources[].id <string>	# ID is the name of the resource, e.g. "namespace1/my-config" or "backingimagemanagers.storage.io".
status.resources[].incompleteState <boolean>	# IncompleteState is true if a bundle summary has 10 or more non-ready resources or a non-ready resource has more 10 or more non-ready or modified states.
status.resources[].kind <string>	# Kind is the k8s kind of the resource.
status.resources[].message <string>	# Message is the first message from the PerClusterStates.
status.resources[].name <string>	# Name of the resource.
status.resources[].namespace <string>	# Namespace of the resource.
status.resources[].perClusterState <object> (required)	# PerClusterState contains lists of cluster IDs for every State for this resource
status.resources[].perClusterState.missing <[]string>	# Missing is a list of cluster IDs for which this a resource is in Missing state
status.resources[].perClusterState.modified <[]string>	# Modified is a list of cluster IDs for which this a resource is in Modified state
status.resources[].perClusterState.notReady <[]string>	# NotReady is a list of cluster IDs for which this a resource is in NotReady state
status.resources[].perClusterState.orphaned <[]string>	# Orphaned is a list of cluster IDs for which this a resource is in Orphaned state
status.resources[].perClusterState.pending <[]string>	# Pending is a list of cluster IDs for which this a resource is in Pending state
status.resources[].perClusterState.ready <[]string>	# Ready is a list of cluster IDs for which this a resource is in Ready state
status.resources[].perClusterState.unknown <[]string>	# Unknown is a list of cluster IDs for which this a resource is in Unknown state
status.resources[].perClusterState.waitApplied <[]string>	# WaitApplied is a list of cluster IDs for which this a resource is in WaitApplied state
status.resources[].state <string>	# State is the state of the resource, e.g. "Unknown", "WaitApplied", "ErrApplied" or "Ready".
status.resources[].transitioning <boolean>	# Transitioning is true if any Transitioning in the PerClusterState is true.
status.resources[].type <string>	# Type is the type of the resource, e.g. "apiextensions.k8s.io.customresourcedefinition" or "configmap".
status.summary <object>	# Summary contains the number of bundle deployments in each state and a list of non-ready resources.
status.summary.desiredReady <integer>	# DesiredReady is the number of bundle deployments that should be ready.
status.summary.errApplied <integer>	# ErrApplied is the number of bundle deployments that have been synced from the Fleet controller and the downstream cluster, but with some errors when deploying the bundle.
status.summary.modified <integer>	# Modified is the number of bundle deployments that have been deployed and for which all resources are ready, but where some changes from the Git repository have not yet been synced.
status.summary.nonReadyResources <[]object>	# NonReadyClusters is a list of states, which is filled for a bundle that is not ready.
status.summary.nonReadyResources[].bundleState <string>	# State is the state of the resource, like e.g. "NotReady" or "ErrApplied".
status.summary.nonReadyResources[].message <string>	# Message contains information why the bundle is not ready.
status.summary.nonReadyResources[].modifiedStatus <[]object>	# ModifiedStatus lists the state for each modified resource.
status.summary.nonReadyResources[].modifiedStatus[].apiVersion <string>
status.summary.nonReadyResources[].modifiedStatus[].delete <boolean>
status.summary.nonReadyResources[].modifiedStatus[].exist <boolean>	# Exist is true if the resource exists but is not owned by us. This can happen if a resource was adopted by another bundle whereas the first bundle still exists and due to that reports that it does not own it.
status.summary.nonReadyResources[].modifiedStatus[].kind <string>
status.summary.nonReadyResources[].modifiedStatus[].missing <boolean>
status.summary.nonReadyResources[].modifiedStatus[].name <string>
status.summary.nonReadyResources[].modifiedStatus[].namespace <string>
status.summary.nonReadyResources[].modifiedStatus[].patch <string>
status.summary.nonReadyResources[].name <string>	# Name is the name of the resource.
status.summary.nonReadyResources[].nonReadyStatus <[]object>	# NonReadyStatus lists the state for each non-ready resource.
status.summary.nonReadyResources[].nonReadyStatus[].apiVersion <string>
status.summary.nonReadyResources[].nonReadyStatus[].kind <string>
status.summary.nonReadyResources[].nonReadyStatus[].name <string>
status.summary.nonReadyResources[].nonReadyStatus[].namespace <string>
status.summary.nonReadyResources[].nonReadyStatus[].summary <object>
status.summary.nonReadyResources[].nonReadyStatus[].summary.error <boolean>
status.summary.nonReadyResources[].nonReadyStatus[].summary.message <[]string>
status.summary.nonReadyResources[].nonReadyStatus[].summary.state <string>
status.summary.nonReadyResources[].nonReadyStatus[].summary.transitioning <boolean>
status.summary.nonReadyResources[].nonReadyStatus[].uid <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.summary.notReady <integer>	# NotReady is the number of bundle deployments that have been deployed where some resources are not ready.
status.summary.outOfSync <integer>	# OutOfSync is the number of bundle deployments that have been synced from Fleet controller, but not yet by the downstream agent.
status.summary.pending <integer>	# Pending is the number of bundle deployments that are being processed by Fleet controller.
status.summary.ready <integer>	# Ready is the number of bundle deployments that have been deployed where all resources are ready.
status.summary.waitApplied <integer>	# WaitApplied is the number of bundle deployments that have been synced from Fleet controller and downstream cluster, but are waiting to be deployed.
status.updateGeneration <integer> format=int64	# Update generation is the force update generation if spec.forceSyncGeneration is set
status.webhookCommit <string>	# WebhookCommit is the latest Git commit hash received from a webhook
