# schema source: Kubescape Operator 1.40.3 https://github.com/kubescape/helm-charts
apiVersion <string> enum=kubescape.io/v1
kind <string> enum=Rules
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.rules <[]object> (required)
spec.rules[].agentVersionRequirement <string>	# Agent version requirement to evaluate this rule (supports semver ranges like ~1.0, >=1.2.0, etc.)
spec.rules[].description <string> (required)	# Description of the rule
spec.rules[].enabled <boolean> (required)	# Whether the rule is enabled
spec.rules[].expressions <object> (required)
spec.rules[].expressions.message <string> (required)	# Message expression
spec.rules[].expressions.ruleExpression <[]object> (required)
spec.rules[].expressions.ruleExpression[].eventType <string> (required)	# Type of event this expression handles
spec.rules[].expressions.ruleExpression[].expression <string> (required)	# The rule expression string
spec.rules[].expressions.uniqueId <string> (required)	# Unique identifier expression
spec.rules[].id <string> (required)	# Unique identifier for the rule
spec.rules[].isTriggerAlert <boolean> (required) default=true	# Whether the rule is a trigger alert
spec.rules[].mitreTactic <string> (required)	# MITRE tactic associated with the rule
spec.rules[].mitreTechnique <string> (required)	# MITRE technique associated with the rule
spec.rules[].name <string> (required)	# Name of the rule
spec.rules[].profileDataRequired <object (free-form)>	# Per-rule profile fields required for rule-aware projection.
spec.rules[].profileDependency <integer> (required) enum=0|1|2	# Profile dependency level (0=Required, 1=Optional, 2=NotRequired)
spec.rules[].severity <integer> (required)	# Severity level of the rule
spec.rules[].state <object (free-form)>	# State information for the rule
spec.rules[].supportPolicy <boolean> (required) default=false	# Whether the rule supports rule policy enforcement
spec.rules[].tags <[]string>	# Tags associated with the rule
