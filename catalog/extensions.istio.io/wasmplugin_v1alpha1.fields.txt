# schema source: Istio 1.30.2 https://github.com/istio/istio
apiVersion <string> enum=extensions.istio.io/v1alpha1
kind <string> enum=WasmPlugin
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Extend the functionality provided by the Istio proxy through WebAssembly filters. See more details at: https://istio.io/docs/reference/config/proxy_extensions/wasm-plugin.html
spec.failStrategy <string> enum=FAIL_CLOSE|FAIL_OPEN|FAIL_RELOAD	# Specifies the failure behavior for the plugin due to fatal errors. Valid Options: FAIL_CLOSE, FAIL_OPEN, FAIL_RELOAD
spec.imagePullPolicy <string> enum=UNSPECIFIED_POLICY|IfNotPresent|Always	# The pull behaviour to be applied when fetching Wasm module by either OCI image or `http/https`. Valid Options: IfNotPresent, Always
spec.imagePullSecret <string> min=1 max=253	# Credentials to use for OCI image pulling.
spec.match <[]object>	# Specifies the criteria to determine which traffic is passed to WasmPlugin.
spec.match[].mode <string> enum=UNDEFINED|CLIENT|SERVER|CLIENT_AND_SERVER	# Criteria for selecting traffic by their direction. Valid Options: CLIENT, SERVER, CLIENT_AND_SERVER
spec.match[].ports <[]object>	# Criteria for selecting traffic by their destination port.
spec.match[].ports[].number <integer> (required) min=1 max=65535
spec.phase <string> enum=UNSPECIFIED_PHASE|AUTHN|AUTHZ|STATS	# Determines where in the filter chain this `WasmPlugin` is to be injected. Valid Options: AUTHN, AUTHZ, STATS
spec.pluginConfig <object (free-form)>	# The configuration that will be passed on to the plugin.
spec.pluginName <string> min=1 max=256	# The plugin name to be used in the Envoy configuration (used to be called `rootID`).
spec.priority <integer> format=int32	# Determines ordering of `WasmPlugins` in the same `phase`.
spec.selector <object>	# Criteria used to select the specific set of pods/VMs on which this plugin configuration should be applied.
spec.selector.matchLabels <map[string]string>	# One or more labels that indicate a specific set of pods/VMs on which a policy should be applied.
spec.sha256 <string> pattern="(^$|^[a-f0-9]{64}$)"	# SHA256 checksum that will be used to verify Wasm module or OCI container.
spec.targetRef <object>
spec.targetRef.group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRef.kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRef.name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRef.namespace <string>	# namespace is the namespace of the referent.
spec.targetRefs <[]object> max=16	# Optional.
spec.targetRefs[].group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRefs[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRefs[].name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRefs[].namespace <string>	# namespace is the namespace of the referent.
spec.type <string> enum=UNSPECIFIED_PLUGIN_TYPE|HTTP|NETWORK	# Specifies the type of Wasm Extension to be used. Valid Options: HTTP, NETWORK
spec.url <string> (required) min=1	# URL of a Wasm module or OCI container.
spec.verificationKey <string>
spec.vmConfig <object>	# Configuration for a Wasm VM.
spec.vmConfig.env <[]object> max=256	# Specifies environment variables to be injected to this VM.
spec.vmConfig.env[].name <string> (required) min=1 max=256	# Name of the environment variable.
spec.vmConfig.env[].value <string> max=2048	# Value for the environment variable.
spec.vmConfig.env[].valueFrom <string> enum=INLINE|HOST	# Source for the environment variable's value. Valid Options: INLINE, HOST
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
