# schema source: OPA Gatekeeper v3.23.0 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=config.gatekeeper.sh/v1alpha1
kind <string> enum=Config
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ConfigSpec defines the desired state of Config.
spec.match <[]object>	# Configuration for namespace exclusion
spec.match[].excludedNamespaces <[]string>
spec.match[].processes <[]string>
spec.readiness <object>	# Configuration for readiness tracker
spec.readiness.statsEnabled <boolean>
spec.sync <object>	# Configuration for syncing k8s objects
spec.sync.syncOnly <[]object>	# If non-empty, only entries on this list will be replicated into OPA
spec.sync.syncOnly[].group <string>
spec.sync.syncOnly[].kind <string>
spec.sync.syncOnly[].version <string>
spec.validation <object>	# Configuration for validation
spec.validation.traces <[]object>	# List of requests to trace. Both "user" and "kinds" must be specified
spec.validation.traces[].dump <string>	# Also dump the state of OPA with the trace. Set to `All` to dump everything.
spec.validation.traces[].kind <object>	# Only trace requests of the following GroupVersionKind
spec.validation.traces[].kind.group <string>
spec.validation.traces[].kind.kind <string>
spec.validation.traces[].kind.version <string>
spec.validation.traces[].user <string>	# Only trace requests from the specified user
status <object>	# ConfigStatus defines the observed state of Config.
status.byPod <[]object>
status.byPod[].configUID <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.byPod[].errors <[]object>
status.byPod[].errors[].message <string> (required)
status.byPod[].errors[].type <string>
status.byPod[].id <string>
status.byPod[].observedGeneration <integer> format=int64
status.byPod[].operations <[]string>
