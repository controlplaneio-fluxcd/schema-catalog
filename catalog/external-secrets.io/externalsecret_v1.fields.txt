# schema source: External Secrets v2.8.0 https://github.com/external-secrets/external-secrets
apiVersion <string> enum=external-secrets.io/v1
kind <string> enum=ExternalSecret
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ExternalSecretSpec defines the desired state of ExternalSecret.
spec.data <[]object>	# Data defines the connection between the Kubernetes Secret keys and the Provider data
spec.data[].remoteRef <object> (required)	# RemoteRef points to the remote secret and defines which secret (version/property/..) to fetch.
spec.data[].remoteRef.conversionStrategy <string> enum=Default|Unicode default="Default"	# Used to define a conversion Strategy
spec.data[].remoteRef.decodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy
spec.data[].remoteRef.key <string> (required)	# Key is the key used in the Provider, mandatory
spec.data[].remoteRef.metadataPolicy <string> enum=None|Fetch default="None"	# Policy for fetching tags/labels from provider secrets, possible options are Fetch, None. Defaults to None
spec.data[].remoteRef.nullBytePolicy <string> enum=Ignore|Fail	# Controls how ESO handles fetched secret data containing NUL bytes for this source.
spec.data[].remoteRef.property <string>	# Used to select a specific property of the Provider value (if a map), if supported
spec.data[].remoteRef.version <string>	# Used to select a specific version of the Provider value, if supported
spec.data[].secretKey <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key in the Kubernetes Secret to store the value.
spec.data[].sourceRef <object>	# SourceRef allows you to override the source from which the value will be pulled.
spec.data[].sourceRef.generatorRef <object>	# GeneratorRef points to a generator custom resource. Deprecated: The generatorRef is not implemented in .data[]. this will be removed with v1.
spec.data[].sourceRef.generatorRef.apiVersion <string> default="generators.external-secrets.io/v1alpha1"	# Specify the apiVersion of the generator resource
spec.data[].sourceRef.generatorRef.kind <string> (required) enum=ACRAccessToken|BeyondtrustWorkloadCredentialsDynamicSecret|ClusterGenerator|CloudsmithAccessToken|ECRAuthorizationToken|Fake|GCRAccessToken|GithubAccessToken|GitlabDeployToken|QuayAccessToken|Password|SSHKey|STSSessionToken|UUID|VaultDynamicSecret|Webhook|Grafana|MFA	# Specify the Kind of the generator resource
spec.data[].sourceRef.generatorRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Specify the name of the generator resource
spec.data[].sourceRef.storeRef <object>	# SecretStoreRef defines which SecretStore to fetch the ExternalSecret data.
spec.data[].sourceRef.storeRef.kind <string> enum=SecretStore|ClusterSecretStore	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore) Defaults to `SecretStore`
spec.data[].sourceRef.storeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the SecretStore resource
spec.dataFrom <[]object>	# DataFrom is used to fetch all properties from a specific Provider data If multiple entries are specified, the Secret keys are merged in the specified order
spec.dataFrom[].extract <object>	# Used to extract multiple key/value pairs from one secret Note: Extract does not support sourceRef.Generator or sourceRef.GeneratorRef.
spec.dataFrom[].extract.conversionStrategy <string> enum=Default|Unicode default="Default"	# Used to define a conversion Strategy
spec.dataFrom[].extract.decodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy
spec.dataFrom[].extract.key <string> (required)	# Key is the key used in the Provider, mandatory
spec.dataFrom[].extract.metadataPolicy <string> enum=None|Fetch default="None"	# Policy for fetching tags/labels from provider secrets, possible options are Fetch, None. Defaults to None
spec.dataFrom[].extract.nullBytePolicy <string> enum=Ignore|Fail	# Controls how ESO handles fetched secret data containing NUL bytes for this source.
spec.dataFrom[].extract.property <string>	# Used to select a specific property of the Provider value (if a map), if supported
spec.dataFrom[].extract.version <string>	# Used to select a specific version of the Provider value, if supported
spec.dataFrom[].find <object>	# Used to find secrets based on tags or regular expressions Note: Find does not support sourceRef.Generator or sourceRef.GeneratorRef.
spec.dataFrom[].find.conversionStrategy <string> enum=Default|Unicode default="Default"	# Used to define a conversion Strategy
spec.dataFrom[].find.decodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy
spec.dataFrom[].find.name <object>	# Finds secrets based on the name.
spec.dataFrom[].find.name.regexp <string>	# Finds secrets base
spec.dataFrom[].find.nullBytePolicy <string> enum=Ignore|Fail	# Controls how ESO handles fetched secret data containing NUL bytes for this find source.
spec.dataFrom[].find.path <string>	# A root path to start the find operations.
spec.dataFrom[].find.tags <map[string]string>	# Find secrets based on tags.
spec.dataFrom[].rewrite <[]object>	# Used to rewrite secret Keys after getting them from the secret Provider Multiple Rewrite operations can be provided. They are applied in a layered order (first to last)
spec.dataFrom[].rewrite[].merge <object>	# Used to merge key/values in one single Secret The resulting key will contain all values from the specified secrets
spec.dataFrom[].rewrite[].merge.conflictPolicy <string> enum=Ignore|Error default="Error"	# Used to define the policy to use in conflict resolution.
spec.dataFrom[].rewrite[].merge.into <string> default=""	# Used to define the target key of the merge operation. Required if strategy is JSON. Ignored otherwise.
spec.dataFrom[].rewrite[].merge.priority <[]string>	# Used to define key priority in conflict resolution.
spec.dataFrom[].rewrite[].merge.priorityPolicy <string> enum=IgnoreNotFound|Strict default="Strict"	# Used to define the policy when a key in the priority list does not exist in the input.
spec.dataFrom[].rewrite[].merge.strategy <string> enum=Extract|JSON default="Extract"	# Used to define the strategy to use in the merge operation.
spec.dataFrom[].rewrite[].regexp <object>	# Used to rewrite with regular expressions. The resulting key will be the output of a regexp.ReplaceAll operation.
spec.dataFrom[].rewrite[].regexp.source <string> (required)	# Used to define the regular expression of a re.Compiler.
spec.dataFrom[].rewrite[].regexp.target <string> (required)	# Used to define the target pattern of a ReplaceAll operation.
spec.dataFrom[].rewrite[].transform <object>	# Used to apply string transformation on the secrets. The resulting key will be the output of the template applied by the operation.
spec.dataFrom[].rewrite[].transform.template <string> (required)	# Used to define the template to apply on the secret name. `.value ` will specify the secret name in the template.
spec.dataFrom[].sourceRef <object>	# SourceRef points to a store or generator which contains secret values ready to use. Use this in combination with Extract or Find pull values out of a specific SecretStore. When sourceRef points to a generator Extract or Find is not supported. The generator returns a static map of values
spec.dataFrom[].sourceRef.generatorRef <object>	# GeneratorRef points to a generator custom resource.
spec.dataFrom[].sourceRef.generatorRef.apiVersion <string> default="generators.external-secrets.io/v1alpha1"	# Specify the apiVersion of the generator resource
spec.dataFrom[].sourceRef.generatorRef.kind <string> (required) enum=ACRAccessToken|BeyondtrustWorkloadCredentialsDynamicSecret|ClusterGenerator|CloudsmithAccessToken|ECRAuthorizationToken|Fake|GCRAccessToken|GithubAccessToken|GitlabDeployToken|QuayAccessToken|Password|SSHKey|STSSessionToken|UUID|VaultDynamicSecret|Webhook|Grafana|MFA	# Specify the Kind of the generator resource
spec.dataFrom[].sourceRef.generatorRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Specify the name of the generator resource
spec.dataFrom[].sourceRef.storeRef <object>	# SecretStoreRef defines which SecretStore to fetch the ExternalSecret data.
spec.dataFrom[].sourceRef.storeRef.kind <string> enum=SecretStore|ClusterSecretStore	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore) Defaults to `SecretStore`
spec.dataFrom[].sourceRef.storeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the SecretStore resource
spec.refreshInterval <string> default="1h0m0s"	# RefreshInterval is the amount of time before the values are read again from the SecretStore provider, specified as Golang Duration strings. Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h" Example values: "1h0m0s", "2h30m0s", "10m0s" May be set to "0s" to fetch and create it once. Defaults to 1h0m0s.
spec.refreshPolicy <string> enum=CreatedOnce|Periodic|OnChange	# RefreshPolicy determines how the ExternalSecret should be refreshed: - CreatedOnce: Creates the Secret only if it does not exist and does not update it thereafter - Periodic: Synchronizes the Secret from the external source at regular intervals specified by refreshInterval. No periodic updates occur if refreshInterval is 0. - OnChange: Only synchronizes the Secret when the ExternalSecret's metadata or specification changes
spec.secretStoreRef <object>	# SecretStoreRef defines which SecretStore to fetch the ExternalSecret data.
spec.secretStoreRef.kind <string> enum=SecretStore|ClusterSecretStore	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore) Defaults to `SecretStore`
spec.secretStoreRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the SecretStore resource
spec.syncWindows <object>	# SyncWindows optionally restricts when periodic refreshes may occur. Evaluated in UTC, only for Periodic refresh policy (or when refreshPolicy is unset).
spec.syncWindows.kind <string> (required) enum=allow|deny	# Kind applies to every window in the list. "allow" -- syncs are permitted only while at least one window is active; all other times are blocked. "deny" -- syncs are blocked while any window is active; all other times are permitted.
spec.syncWindows.windows <[]object> (required) min=1	# Windows is the list of schedule+duration pairs.
spec.syncWindows.windows[].duration <string> (required)	# Duration specifies how long the window stays open after each Schedule firing. Example: "8h".
spec.syncWindows.windows[].schedule <string> (required) pattern="^(@(annually|yearly|monthly|weekly|daily|midnight|hourly)|@every [^\\s]+.*|[^\\s]+( [^\\s]+){4})$" min=1	# Schedule is a standard 5-field cron expression evaluated in UTC, or a named shorthand such as @daily or @every 1h. It marks the start time of each window occurrence. Example: "0 22 * * 1-5" opens a window every weekday at 22:00 UTC.
spec.target <object> default={"creationPolicy":"Owner","deletionPolicy":"Retain"}	# ExternalSecretTarget defines the Kubernetes Secret to be created, there can be only one target per ExternalSecret.
spec.target.creationPolicy <string> enum=Owner|Orphan|Merge|None|CreateOrMerge default="Owner"	# CreationPolicy defines rules on how to create the resulting Secret. Defaults to "Owner"
spec.target.deletionPolicy <string> enum=Delete|Merge|Retain default="Retain"	# DeletionPolicy defines rules on how to delete the resulting Secret. Defaults to "Retain"
spec.target.immutable <boolean>	# Immutable defines if the final secret will be immutable
spec.target.manifest <object>	# Manifest defines a custom Kubernetes resource to create instead of a Secret. When specified, ExternalSecret will create the resource type defined here (e.g., ConfigMap, Custom Resource) instead of a Secret. Warning: Using Generic target. Make sure access policies and encryption are properly configured.
spec.target.manifest.apiVersion <string> (required) min=1	# APIVersion of the target resource (e.g., "v1" for ConfigMap, "argoproj.io/v1alpha1" for ArgoCD Application)
spec.target.manifest.kind <string> (required) min=1	# Kind of the target resource (e.g., "ConfigMap", "Application")
spec.target.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource to be managed. Defaults to the .metadata.name of the ExternalSecret resource
spec.target.template <object>	# Template defines a blueprint for the created Secret resource.
spec.target.template.data <map[string]string>
spec.target.template.engineVersion <string> enum=v2 default="v2"	# EngineVersion specifies the template engine version that should be used to compile/execute the template specified in .data and .templateFrom[].
spec.target.template.mergePolicy <string> enum=Replace|Merge default="Replace"	# TemplateMergePolicy defines how the rendered template should be merged with the existing Secret data.
spec.target.template.metadata <object>	# ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
spec.target.template.metadata.annotations <map[string]string>
spec.target.template.metadata.finalizers <[]string>
spec.target.template.metadata.labels <map[string]string>
spec.target.template.templateFrom <[]object>
spec.target.template.templateFrom[].configMap <object>	# TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
spec.target.template.templateFrom[].configMap.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.target.template.templateFrom[].configMap.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.target.template.templateFrom[].configMap.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope specifies how the template keys should be interpreted.
spec.target.template.templateFrom[].configMap.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.target.template.templateFrom[].literal <string>
spec.target.template.templateFrom[].secret <object>	# TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
spec.target.template.templateFrom[].secret.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.target.template.templateFrom[].secret.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.target.template.templateFrom[].secret.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope specifies how the template keys should be interpreted.
spec.target.template.templateFrom[].secret.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.target.template.templateFrom[].target <string> default="Data"	# Target specifies where to place the template result. For Secret resources, common values are: "Data", "Annotations", "Labels". For custom resources (when spec.target.manifest is set), this supports nested paths like "spec.database.config" or "data".
spec.target.template.templateFrom[].valuesDecodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy for the rendered template values.
spec.target.template.type <string>
status <object>	# ExternalSecretStatus defines the observed state of ExternalSecret.
status.binding <object>	# Binding represents a servicebinding.io Provisioned Service reference to the secret
status.binding.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> format=date-time
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string> (required)
status.conditions[].type <string> (required) enum=Ready|Deleted	# ExternalSecretConditionType defines a value type for ExternalSecret conditions.
status.refreshTime <string> format=date-time	# refreshTime is the time and date the external secret was fetched and the target secret updated
status.syncedResourceVersion <string>	# SyncedResourceVersion keeps track of the last synced version
