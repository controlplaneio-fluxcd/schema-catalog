# schema source: External Secrets v2.7.0
apiVersion <string> enum=external-secrets.io/v1beta1 (deprecated)
kind <string> enum=ClusterExternalSecret (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ClusterExternalSecretSpec defines the desired state of ClusterExternalSecret.
spec.externalSecretMetadata <object>	# The metadata of the external secrets to be created
spec.externalSecretMetadata.annotations <map[string]string>
spec.externalSecretMetadata.labels <map[string]string>
spec.externalSecretName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the external secrets to be created. Defaults to the name of the ClusterExternalSecret
spec.externalSecretSpec <object> (required)	# The spec for the ExternalSecrets to be created
spec.externalSecretSpec.data <[]object>	# Data defines the connection between the Kubernetes Secret keys and the Provider data
spec.externalSecretSpec.data[].remoteRef <object> (required)	# RemoteRef points to the remote secret and defines which secret (version/property/..) to fetch.
spec.externalSecretSpec.data[].remoteRef.conversionStrategy <string> enum=Default|Unicode default="Default"	# Used to define a conversion Strategy
spec.externalSecretSpec.data[].remoteRef.decodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy
spec.externalSecretSpec.data[].remoteRef.key <string> (required)	# Key is the key used in the Provider, mandatory
spec.externalSecretSpec.data[].remoteRef.metadataPolicy <string> enum=None|Fetch default="None"	# Policy for fetching tags/labels from provider secrets, possible options are Fetch, None. Defaults to None
spec.externalSecretSpec.data[].remoteRef.property <string>	# Used to select a specific property of the Provider value (if a map), if supported
spec.externalSecretSpec.data[].remoteRef.version <string>	# Used to select a specific version of the Provider value, if supported
spec.externalSecretSpec.data[].secretKey <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key in the Kubernetes Secret to store the value.
spec.externalSecretSpec.data[].sourceRef <object>	# SourceRef allows you to override the source from which the value will be pulled.
spec.externalSecretSpec.data[].sourceRef.generatorRef <object>	# GeneratorRef points to a generator custom resource. Deprecated: The generatorRef is not implemented in .data[]. this will be removed with v1.
spec.externalSecretSpec.data[].sourceRef.generatorRef.apiVersion <string> default="generators.external-secrets.io/v1alpha1"	# Specify the apiVersion of the generator resource
spec.externalSecretSpec.data[].sourceRef.generatorRef.kind <string> (required) enum=ACRAccessToken|ClusterGenerator|ECRAuthorizationToken|Fake|GCRAccessToken|GithubAccessToken|QuayAccessToken|Password|SSHKey|STSSessionToken|UUID|VaultDynamicSecret|Webhook|Grafana	# Specify the Kind of the generator resource
spec.externalSecretSpec.data[].sourceRef.generatorRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Specify the name of the generator resource
spec.externalSecretSpec.data[].sourceRef.storeRef <object>	# SecretStoreRef defines which SecretStore to fetch the ExternalSecret data.
spec.externalSecretSpec.data[].sourceRef.storeRef.kind <string> enum=SecretStore|ClusterSecretStore	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore) Defaults to `SecretStore`
spec.externalSecretSpec.data[].sourceRef.storeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the SecretStore resource
spec.externalSecretSpec.dataFrom <[]object>	# DataFrom is used to fetch all properties from a specific Provider data If multiple entries are specified, the Secret keys are merged in the specified order
spec.externalSecretSpec.dataFrom[].extract <object>	# Used to extract multiple key/value pairs from one secret Note: Extract does not support sourceRef.Generator or sourceRef.GeneratorRef.
spec.externalSecretSpec.dataFrom[].extract.conversionStrategy <string> enum=Default|Unicode default="Default"	# Used to define a conversion Strategy
spec.externalSecretSpec.dataFrom[].extract.decodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy
spec.externalSecretSpec.dataFrom[].extract.key <string> (required)	# Key is the key used in the Provider, mandatory
spec.externalSecretSpec.dataFrom[].extract.metadataPolicy <string> enum=None|Fetch default="None"	# Policy for fetching tags/labels from provider secrets, possible options are Fetch, None. Defaults to None
spec.externalSecretSpec.dataFrom[].extract.property <string>	# Used to select a specific property of the Provider value (if a map), if supported
spec.externalSecretSpec.dataFrom[].extract.version <string>	# Used to select a specific version of the Provider value, if supported
spec.externalSecretSpec.dataFrom[].find <object>	# Used to find secrets based on tags or regular expressions Note: Find does not support sourceRef.Generator or sourceRef.GeneratorRef.
spec.externalSecretSpec.dataFrom[].find.conversionStrategy <string> enum=Default|Unicode default="Default"	# Used to define a conversion Strategy
spec.externalSecretSpec.dataFrom[].find.decodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy
spec.externalSecretSpec.dataFrom[].find.name <object>	# Finds secrets based on the name.
spec.externalSecretSpec.dataFrom[].find.name.regexp <string>	# Finds secrets base
spec.externalSecretSpec.dataFrom[].find.path <string>	# A root path to start the find operations.
spec.externalSecretSpec.dataFrom[].find.tags <map[string]string>	# Find secrets based on tags.
spec.externalSecretSpec.dataFrom[].rewrite <[]object>	# Used to rewrite secret Keys after getting them from the secret Provider Multiple Rewrite operations can be provided. They are applied in a layered order (first to last)
spec.externalSecretSpec.dataFrom[].rewrite[].regexp <object>	# Used to rewrite with regular expressions. The resulting key will be the output of a regexp.ReplaceAll operation.
spec.externalSecretSpec.dataFrom[].rewrite[].regexp.source <string> (required)	# Used to define the regular expression of a re.Compiler.
spec.externalSecretSpec.dataFrom[].rewrite[].regexp.target <string> (required)	# Used to define the target pattern of a ReplaceAll operation.
spec.externalSecretSpec.dataFrom[].rewrite[].transform <object>	# Used to apply string transformation on the secrets. The resulting key will be the output of the template applied by the operation.
spec.externalSecretSpec.dataFrom[].rewrite[].transform.template <string> (required)	# Used to define the template to apply on the secret name. `.value ` will specify the secret name in the template.
spec.externalSecretSpec.dataFrom[].sourceRef <object>	# SourceRef points to a store or generator which contains secret values ready to use. Use this in combination with Extract or Find pull values out of a specific SecretStore. When sourceRef points to a generator Extract or Find is not supported. The generator returns a static map of values
spec.externalSecretSpec.dataFrom[].sourceRef.generatorRef <object>	# GeneratorRef points to a generator custom resource.
spec.externalSecretSpec.dataFrom[].sourceRef.generatorRef.apiVersion <string> default="generators.external-secrets.io/v1alpha1"	# Specify the apiVersion of the generator resource
spec.externalSecretSpec.dataFrom[].sourceRef.generatorRef.kind <string> (required) enum=ACRAccessToken|ClusterGenerator|ECRAuthorizationToken|Fake|GCRAccessToken|GithubAccessToken|QuayAccessToken|Password|SSHKey|STSSessionToken|UUID|VaultDynamicSecret|Webhook|Grafana	# Specify the Kind of the generator resource
spec.externalSecretSpec.dataFrom[].sourceRef.generatorRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Specify the name of the generator resource
spec.externalSecretSpec.dataFrom[].sourceRef.storeRef <object>	# SecretStoreRef defines which SecretStore to fetch the ExternalSecret data.
spec.externalSecretSpec.dataFrom[].sourceRef.storeRef.kind <string> enum=SecretStore|ClusterSecretStore	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore) Defaults to `SecretStore`
spec.externalSecretSpec.dataFrom[].sourceRef.storeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the SecretStore resource
spec.externalSecretSpec.refreshInterval <string> default="1h0m0s"	# RefreshInterval is the amount of time before the values are read again from the SecretStore provider, specified as Golang Duration strings. Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h" Example values: "1h0m0s", "2h30m0s", "10m0s" May be set to "0s" to fetch and create it once. Defaults to 1h0m0s.
spec.externalSecretSpec.refreshPolicy <string> enum=CreatedOnce|Periodic|OnChange	# RefreshPolicy determines how the ExternalSecret should be refreshed: - CreatedOnce: Creates the Secret only if it does not exist and does not update it thereafter - Periodic: Synchronizes the Secret from the external source at regular intervals specified by refreshInterval. No periodic updates occur if refreshInterval is 0. - OnChange: Only synchronizes the Secret when the ExternalSecret's metadata or specification changes
spec.externalSecretSpec.secretStoreRef <object>	# SecretStoreRef defines which SecretStore to fetch the ExternalSecret data.
spec.externalSecretSpec.secretStoreRef.kind <string> enum=SecretStore|ClusterSecretStore	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore) Defaults to `SecretStore`
spec.externalSecretSpec.secretStoreRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the SecretStore resource
spec.externalSecretSpec.target <object> default={"creationPolicy":"Owner","deletionPolicy":"Retain"}	# ExternalSecretTarget defines the Kubernetes Secret to be created There can be only one target per ExternalSecret.
spec.externalSecretSpec.target.creationPolicy <string> enum=Owner|Orphan|Merge|None default="Owner"	# CreationPolicy defines rules on how to create the resulting Secret. Defaults to "Owner"
spec.externalSecretSpec.target.deletionPolicy <string> enum=Delete|Merge|Retain default="Retain"	# DeletionPolicy defines rules on how to delete the resulting Secret. Defaults to "Retain"
spec.externalSecretSpec.target.immutable <boolean>	# Immutable defines if the final secret will be immutable
spec.externalSecretSpec.target.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource to be managed. Defaults to the .metadata.name of the ExternalSecret resource
spec.externalSecretSpec.target.template <object>	# Template defines a blueprint for the created Secret resource.
spec.externalSecretSpec.target.template.data <map[string]string>
spec.externalSecretSpec.target.template.engineVersion <string> enum=v2 default="v2"	# EngineVersion specifies the template engine version that should be used to compile/execute the template specified in .data and .templateFrom[].
spec.externalSecretSpec.target.template.mergePolicy <string> enum=Replace|Merge default="Replace"	# TemplateMergePolicy defines how template values should be merged when generating a secret.
spec.externalSecretSpec.target.template.metadata <object>	# ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
spec.externalSecretSpec.target.template.metadata.annotations <map[string]string>
spec.externalSecretSpec.target.template.metadata.labels <map[string]string>
spec.externalSecretSpec.target.template.templateFrom <[]object>
spec.externalSecretSpec.target.template.templateFrom[].configMap <object>	# TemplateRef defines a reference to a template source in a ConfigMap or Secret.
spec.externalSecretSpec.target.template.templateFrom[].configMap.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.externalSecretSpec.target.template.templateFrom[].configMap.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.externalSecretSpec.target.template.templateFrom[].configMap.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope defines the scope of the template when processing template data.
spec.externalSecretSpec.target.template.templateFrom[].configMap.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.externalSecretSpec.target.template.templateFrom[].literal <string>
spec.externalSecretSpec.target.template.templateFrom[].secret <object>	# TemplateRef defines a reference to a template source in a ConfigMap or Secret.
spec.externalSecretSpec.target.template.templateFrom[].secret.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.externalSecretSpec.target.template.templateFrom[].secret.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.externalSecretSpec.target.template.templateFrom[].secret.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope defines the scope of the template when processing template data.
spec.externalSecretSpec.target.template.templateFrom[].secret.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.externalSecretSpec.target.template.templateFrom[].target <string> enum=Data|Annotations|Labels default="Data"	# TemplateTarget defines the target field where the template result will be stored.
spec.externalSecretSpec.target.template.type <string>
spec.namespaceSelector <object>	# The labels to select by to find the Namespaces to create the ExternalSecrets in
spec.namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.namespaceSelectors <[]object>	# A list of labels to select by to find the Namespaces to create the ExternalSecrets in. The selectors are ORed.
spec.namespaceSelectors[].matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.namespaceSelectors[].matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.namespaceSelectors[].matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.namespaceSelectors[].matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.namespaceSelectors[].matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.namespaces <[]string>	# Choose namespaces by name. This field is ORed with anything that NamespaceSelectors ends up choosing. Deprecated: Use NamespaceSelectors instead.
spec.refreshTime <string>	# The time in which the controller should reconcile its objects and recheck namespaces for labels.
status <object>	# ClusterExternalSecretStatus defines the observed state of ClusterExternalSecret.
status.conditions <[]object>
status.conditions[].message <string>
status.conditions[].status <string> (required)
status.conditions[].type <string> (required)	# ClusterExternalSecretConditionType indicates the condition of the ClusterExternalSecret.
status.externalSecretName <string>	# ExternalSecretName is the name of the ExternalSecrets created by the ClusterExternalSecret
status.failedNamespaces <[]object>	# Failed namespaces are the namespaces that failed to apply an ExternalSecret
status.failedNamespaces[].namespace <string> (required)	# Namespace is the namespace that failed when trying to apply an ExternalSecret
status.failedNamespaces[].reason <string>	# Reason is why the ExternalSecret failed to apply to the namespace
status.provisionedNamespaces <[]string>	# ProvisionedNamespaces are the namespaces where the ClusterExternalSecret has secrets
