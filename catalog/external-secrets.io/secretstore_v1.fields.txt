# schema source: External Secrets v2.7.0 https://github.com/external-secrets/external-secrets
apiVersion <string> enum=external-secrets.io/v1
kind <string> enum=SecretStore
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# SecretStoreSpec defines the desired state of SecretStore.
spec.conditions <[]object>	# Used to constrain a ClusterSecretStore to specific namespaces. Relevant only to ClusterSecretStore.
spec.conditions[].namespaceRegexes <[]string>	# Choose namespaces by using regex matching
spec.conditions[].namespaceSelector <object>	# Choose namespace using a labelSelector
spec.conditions[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.conditions[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.conditions[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.conditions[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.conditions[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.conditions[].namespaces <[]string>	# Choose namespaces by name
spec.controller <string>	# Used to select the correct ESO controller (think: ingress.ingressClassName) The ESO controller is instantiated with a specific controller name and filters ES based on this property
spec.provider <object> (required)	# Used to configure the provider. Only one provider may be set
spec.provider.akeyless <object>	# Akeyless configures this store to sync secrets using Akeyless Vault provider
spec.provider.akeyless.akeylessGWApiURL <string> (required)	# Akeyless GW API Url from which the secrets to be fetched from.
spec.provider.akeyless.authSecretRef <object> (required)	# Auth configures how the operator authenticates with Akeyless.
spec.provider.akeyless.authSecretRef.kubernetesAuth <object>	# Kubernetes authenticates with Akeyless by passing the ServiceAccount token stored in the named Secret resource.
spec.provider.akeyless.authSecretRef.kubernetesAuth.accessID <string> (required)	# the Akeyless Kubernetes auth-method access-id
spec.provider.akeyless.authSecretRef.kubernetesAuth.k8sConfName <string> (required)	# Kubernetes-auth configuration name in Akeyless-Gateway
spec.provider.akeyless.authSecretRef.kubernetesAuth.secretRef <object>	# Optional secret field containing a Kubernetes ServiceAccount JWT used for authenticating with Akeyless. If a name is specified without a key, `token` is the default. If one is not specified, the one bound to the controller will be used.
spec.provider.akeyless.authSecretRef.kubernetesAuth.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.akeyless.authSecretRef.kubernetesAuth.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.akeyless.authSecretRef.kubernetesAuth.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.akeyless.authSecretRef.kubernetesAuth.serviceAccountRef <object>	# Optional service account field containing the name of a kubernetes ServiceAccount. If the service account is specified, the service account secret token JWT will be used for authenticating with Akeyless. If the service account selector is not supplied, the secretRef will be used instead.
spec.provider.akeyless.authSecretRef.kubernetesAuth.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.akeyless.authSecretRef.kubernetesAuth.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.akeyless.authSecretRef.kubernetesAuth.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.akeyless.authSecretRef.secretRef <object>	# Reference to a Secret that contains the details to authenticate with Akeyless.
spec.provider.akeyless.authSecretRef.secretRef.accessID <object>	# The SecretAccessID is used for authentication
spec.provider.akeyless.authSecretRef.secretRef.accessID.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.akeyless.authSecretRef.secretRef.accessID.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.akeyless.authSecretRef.secretRef.accessID.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.akeyless.authSecretRef.secretRef.accessType <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.akeyless.authSecretRef.secretRef.accessType.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.akeyless.authSecretRef.secretRef.accessType.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.akeyless.authSecretRef.secretRef.accessType.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.akeyless.authSecretRef.secretRef.accessTypeParam <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.akeyless.authSecretRef.secretRef.accessTypeParam.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.akeyless.authSecretRef.secretRef.accessTypeParam.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.akeyless.authSecretRef.secretRef.accessTypeParam.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.akeyless.caBundle <string> format=byte	# PEM/base64 encoded CA bundle used to validate Akeyless Gateway certificate. Only used if the AkeylessGWApiURL URL is using HTTPS protocol. If not set the system root certificates are used to validate the TLS connection.
spec.provider.akeyless.caProvider <object>	# The provider for the CA bundle to use to validate Akeyless Gateway certificate.
spec.provider.akeyless.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.akeyless.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.akeyless.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.akeyless.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.aws <object>	# AWS configures this store to sync secrets using AWS Secret Manager provider
spec.provider.aws.additionalRoles <[]string>	# AdditionalRoles is a chained list of Role ARNs which the provider will sequentially assume before assuming the Role
spec.provider.aws.auth <object>	# Auth defines the information necessary to authenticate against AWS if not set aws sdk will infer credentials from your environment see: https://docs.aws.amazon.com/sdk-for-go/v1/developer-guide/configuring-sdk.html#specifying-credentials
spec.provider.aws.auth.jwt <object>	# AWSJWTAuth stores reference to Authenticate against AWS using service account tokens.
spec.provider.aws.auth.jwt.serviceAccountRef <object>	# ServiceAccountSelector is a reference to a ServiceAccount resource.
spec.provider.aws.auth.jwt.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.aws.auth.jwt.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.aws.auth.jwt.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.aws.auth.secretRef <object>	# AWSAuthSecretRef holds secret references for AWS credentials both AccessKeyID and SecretAccessKey must be defined in order to properly authenticate.
spec.provider.aws.auth.secretRef.accessKeyIDSecretRef <object>	# The AccessKeyID is used for authentication
spec.provider.aws.auth.secretRef.accessKeyIDSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.aws.auth.secretRef.accessKeyIDSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.aws.auth.secretRef.accessKeyIDSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.aws.auth.secretRef.secretAccessKeySecretRef <object>	# The SecretAccessKey is used for authentication
spec.provider.aws.auth.secretRef.secretAccessKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.aws.auth.secretRef.secretAccessKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.aws.auth.secretRef.secretAccessKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.aws.auth.secretRef.sessionTokenSecretRef <object>	# The SessionToken used for authentication This must be defined if AccessKeyID and SecretAccessKey are temporary credentials see: https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_use-resources.html
spec.provider.aws.auth.secretRef.sessionTokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.aws.auth.secretRef.sessionTokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.aws.auth.secretRef.sessionTokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.aws.customSessionTags <map[string]string>	# CustomSessionTags defines additional STS session tags to include when SessionTagsPolicy is Custom. These are merged with the automatically injected esoNamespace, esoStoreName, and esoStoreKind tags.
spec.provider.aws.externalID <string>	# AWS External ID set on assumed IAM roles
spec.provider.aws.prefix <string>	# Prefix adds a prefix to all retrieved values.
spec.provider.aws.region <string> (required)	# AWS Region to be used for the provider
spec.provider.aws.role <string>	# Role is a Role ARN which the provider will assume
spec.provider.aws.secretsManager <object>	# SecretsManager defines how the provider behaves when interacting with AWS SecretsManager
spec.provider.aws.secretsManager.forceDeleteWithoutRecovery <boolean>	# Specifies whether to delete the secret without any recovery window. You can't use both this parameter and RecoveryWindowInDays in the same call. If you don't use either, then by default Secrets Manager uses a 30 day recovery window. see: https://docs.aws.amazon.com/secretsmanager/latest/apireference/API_DeleteSecret.html#SecretsManager-DeleteSecret-request-ForceDeleteWithoutRecovery
spec.provider.aws.secretsManager.recoveryWindowInDays <integer> format=int64	# The number of days from 7 to 30 that Secrets Manager waits before permanently deleting the secret. You can't use both this parameter and ForceDeleteWithoutRecovery in the same call. If you don't use either, then by default Secrets Manager uses a 30-day recovery window. see: https://docs.aws.amazon.com/secretsmanager/latest/apireference/API_DeleteSecret.html#SecretsManager-DeleteSecret-request-RecoveryWindowInDays
spec.provider.aws.service <string> (required) enum=SecretsManager|ParameterStore	# Service defines which service should be used to fetch the secrets
spec.provider.aws.sessionTags <[]object>	# AWS STS assume role session tags
spec.provider.aws.sessionTags[].key <string> (required)
spec.provider.aws.sessionTags[].value <string> (required)
spec.provider.aws.sessionTagsPolicy <string> enum=None|Simple|Custom default="None"	# SessionTagsPolicy controls whether and how STS session tags are added when assuming roles. None (default): no tags are added. Simple: automatically adds esoNamespace (from the ExternalSecret), esoStoreName, and esoStoreKind tags. Custom: adds esoNamespace, esoStoreName, and esoStoreKind plus any tags defined in CustomSessionTags. Note: the IAM role must have sts:TagSession permission when using Simple or Custom.
spec.provider.aws.transitiveTagKeys <[]string>	# AWS STS assume role transitive session tags. Required when multiple rules are used with the provider
spec.provider.azurekv <object>	# AzureKV configures this store to sync secrets using Azure Key Vault provider
spec.provider.azurekv.authSecretRef <object>	# Auth configures how the operator authenticates with Azure. Required for ServicePrincipal auth type. Optional for WorkloadIdentity.
spec.provider.azurekv.authSecretRef.clientCertificate <object>	# The Azure ClientCertificate of the service principle used for authentication.
spec.provider.azurekv.authSecretRef.clientCertificate.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.azurekv.authSecretRef.clientCertificate.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.azurekv.authSecretRef.clientCertificate.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.azurekv.authSecretRef.clientId <object>	# The Azure clientId of the service principle or managed identity used for authentication.
spec.provider.azurekv.authSecretRef.clientId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.azurekv.authSecretRef.clientId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.azurekv.authSecretRef.clientId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.azurekv.authSecretRef.clientSecret <object>	# The Azure ClientSecret of the service principle used for authentication.
spec.provider.azurekv.authSecretRef.clientSecret.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.azurekv.authSecretRef.clientSecret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.azurekv.authSecretRef.clientSecret.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.azurekv.authSecretRef.tenantId <object>	# The Azure tenantId of the managed identity used for authentication.
spec.provider.azurekv.authSecretRef.tenantId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.azurekv.authSecretRef.tenantId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.azurekv.authSecretRef.tenantId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.azurekv.authType <string> enum=ServicePrincipal|ManagedIdentity|WorkloadIdentity default="ServicePrincipal"	# Auth type defines how to authenticate to the keyvault service. Valid values are: - "ServicePrincipal" (default): Using a service principal (tenantId, clientId, clientSecret) - "ManagedIdentity": Using Managed Identity assigned to the pod (see aad-pod-identity) - "WorkloadIdentity": Using a Kubernetes ServiceAccount federated with Entra ID
spec.provider.azurekv.customCloudConfig <object>	# CustomCloudConfig defines custom Azure endpoints for non-standard clouds. Required when EnvironmentType is AzureStackCloud. Optional for other environment types - useful for Azure China when using Workload Identity with AKS, where the OIDC issuer (login.partner.microsoftonline.cn) differs from the standard China Cloud endpoint (login.chinacloudapi.cn). IMPORTANT: This feature REQUIRES UseAzureSDK to be set to true. Custom cloud configuration is not supported with the legacy go-autorest SDK.
spec.provider.azurekv.customCloudConfig.activeDirectoryEndpoint <string> (required)	# ActiveDirectoryEndpoint is the AAD endpoint for authentication Required when using custom cloud configuration
spec.provider.azurekv.customCloudConfig.keyVaultDNSSuffix <string>	# KeyVaultDNSSuffix is the DNS suffix for Key Vault URLs
spec.provider.azurekv.customCloudConfig.keyVaultEndpoint <string>	# KeyVaultEndpoint is the Key Vault service endpoint
spec.provider.azurekv.customCloudConfig.resourceManagerEndpoint <string>	# ResourceManagerEndpoint is the Azure Resource Manager endpoint
spec.provider.azurekv.environmentType <string> enum=PublicCloud|USGovernmentCloud|ChinaCloud|GermanCloud|AzureStackCloud default="PublicCloud"	# EnvironmentType specifies the Azure cloud environment endpoints to use for connecting and authenticating with Azure. By default it points to the public cloud AAD endpoint. The following endpoints are available, also see here: https://github.com/Azure/go-autorest/blob/main/autorest/azure/environments.go#L152 PublicCloud, USGovernmentCloud, ChinaCloud, GermanCloud, AzureStackCloud Use AzureStackCloud when you need to configure custom Azure Stack Hub or Azure Stack Edge endpoints.
spec.provider.azurekv.identityId <string>	# If multiple Managed Identity is assigned to the pod, you can select the one to be used
spec.provider.azurekv.serviceAccountRef <object>	# ServiceAccountRef specified the service account that should be used when authenticating with WorkloadIdentity.
spec.provider.azurekv.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.azurekv.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.azurekv.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.azurekv.tenantId <string>	# TenantID configures the Azure Tenant to send requests to. Required for ServicePrincipal auth type. Optional for WorkloadIdentity.
spec.provider.azurekv.useAzureSDK <boolean> default=false	# UseAzureSDK enables the use of the new Azure SDK for Go (azcore-based) instead of the legacy go-autorest SDK. This is experimental and may have behavioral differences. Defaults to false (legacy SDK).
spec.provider.azurekv.vaultUrl <string> (required)	# Vault Url from which the secrets to be fetched from.
spec.provider.barbican <object>	# Barbican configures this store to sync secrets using the OpenStack Barbican provider
spec.provider.barbican.auth <object> (required)	# BarbicanAuth contains the authentication information for Barbican.
spec.provider.barbican.auth.password <object> (required)	# BarbicanProviderPasswordRef defines a reference to a secret containing password for the Barbican provider.
spec.provider.barbican.auth.password.secretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.barbican.auth.password.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.barbican.auth.password.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.barbican.auth.password.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.barbican.auth.username <object> (required)	# BarbicanProviderUsernameRef defines a reference to a secret containing username for the Barbican provider.
spec.provider.barbican.auth.username.secretRef <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.barbican.auth.username.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.barbican.auth.username.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.barbican.auth.username.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.barbican.auth.username.value <string>
spec.provider.barbican.authURL <string>
spec.provider.barbican.domainName <string>
spec.provider.barbican.region <string>
spec.provider.barbican.tenantName <string>
spec.provider.beyondtrust <object>	# Beyondtrust configures this store to sync secrets using Password Safe provider.
spec.provider.beyondtrust.auth <object> (required)	# Auth configures how the operator authenticates with Beyondtrust.
spec.provider.beyondtrust.auth.apiKey <object>	# APIKey If not provided then ClientID/ClientSecret become required.
spec.provider.beyondtrust.auth.apiKey.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.beyondtrust.auth.apiKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.beyondtrust.auth.apiKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.beyondtrust.auth.apiKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.beyondtrust.auth.apiKey.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.beyondtrust.auth.certificate <object>	# Certificate (cert.pem) for use when authenticating with an OAuth client Id using a Client Certificate.
spec.provider.beyondtrust.auth.certificate.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.beyondtrust.auth.certificate.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.beyondtrust.auth.certificate.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.beyondtrust.auth.certificate.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.beyondtrust.auth.certificate.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.beyondtrust.auth.certificateKey <object>	# Certificate private key (key.pem). For use when authenticating with an OAuth client Id
spec.provider.beyondtrust.auth.certificateKey.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.beyondtrust.auth.certificateKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.beyondtrust.auth.certificateKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.beyondtrust.auth.certificateKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.beyondtrust.auth.certificateKey.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.beyondtrust.auth.clientId <object>	# ClientID is the API OAuth Client ID.
spec.provider.beyondtrust.auth.clientId.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.beyondtrust.auth.clientId.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.beyondtrust.auth.clientId.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.beyondtrust.auth.clientId.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.beyondtrust.auth.clientId.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.beyondtrust.auth.clientSecret <object>	# ClientSecret is the API OAuth Client Secret.
spec.provider.beyondtrust.auth.clientSecret.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.beyondtrust.auth.clientSecret.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.beyondtrust.auth.clientSecret.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.beyondtrust.auth.clientSecret.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.beyondtrust.auth.clientSecret.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.beyondtrust.server <object> (required)	# Auth configures how API server works.
spec.provider.beyondtrust.server.apiUrl <string> (required)
spec.provider.beyondtrust.server.apiVersion <string>
spec.provider.beyondtrust.server.clientTimeOutSeconds <integer>	# Timeout specifies a time limit for requests made by this Client. The timeout includes connection time, any redirects, and reading the response body. Defaults to 45 seconds.
spec.provider.beyondtrust.server.decrypt <boolean> default=true	# When true, the response includes the decrypted password. When false, the password field is omitted. This option only applies to the SECRET retrieval type. Default: true.
spec.provider.beyondtrust.server.retrievalType <string>	# The secret retrieval type. SECRET = Secrets Safe (credential, text, file). MANAGED_ACCOUNT = Password Safe account associated with a system.
spec.provider.beyondtrust.server.separator <string>	# A character that separates the folder names.
spec.provider.beyondtrust.server.verifyCA <boolean> (required)
spec.provider.beyondtrustworkloadcredentials <object>	# BeyondtrustWorkloadCredentials configures this store to sync secrets using the BeyondTrust Workload Credentials provider.
spec.provider.beyondtrustworkloadcredentials.auth <object> (required)	# Auth configures how the Operator authenticates with the BeyondTrust Workload Credentials API. Currently supports API key authentication via Kubernetes secret reference. For authentication setup, see: https://docs.beyondtrust.com/bt-docs/docs/secrets-api#authentication
spec.provider.beyondtrustworkloadcredentials.auth.apikey <object> (required)	# APIKey configures API token authentication for BeyondTrust Workload Credentials. The token is retrieved from a Kubernetes secret and used as a Bearer token for API requests.
spec.provider.beyondtrustworkloadcredentials.auth.apikey.token <object> (required)	# Token references the Kubernetes secret containing the BeyondTrust Workload Credentials API token. The secret should contain the API key used to authenticate with BeyondTrust Workload Credentials. Create an API token in your BeyondTrust Workload Credentials console and store it in a Kubernetes secret. For details on creating API tokens, see: https://docs.beyondtrust.com/bt-docs/docs/secrets-api#authentication
spec.provider.beyondtrustworkloadcredentials.auth.apikey.token.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.beyondtrustworkloadcredentials.auth.apikey.token.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.beyondtrustworkloadcredentials.auth.apikey.token.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.beyondtrustworkloadcredentials.caBundle <string> format=byte	# CABundle is a base64-encoded CA certificate used to validate the BeyondTrust Workload Credentials API TLS certificate. Use this when your BeyondTrust instance uses a self-signed certificate or internal CA. If not set, the system's trusted root certificates are used.
spec.provider.beyondtrustworkloadcredentials.caProvider <object>	# CAProvider points to a Secret or ConfigMap containing a PEM-encoded CA certificate. This is used to validate the BeyondTrust Workload Credentials API TLS certificate. Use this as an alternative to CABundle when you want to reference an existing Kubernetes resource.
spec.provider.beyondtrustworkloadcredentials.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.beyondtrustworkloadcredentials.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.beyondtrustworkloadcredentials.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.beyondtrustworkloadcredentials.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.beyondtrustworkloadcredentials.folderPath <string>	# FolderPath specifies the default folder path for secret retrieval. Secrets will be fetched from this folder unless overridden in the ExternalSecret spec. Example: "production/database" or "dev/api-keys" Leave empty to retrieve secrets from the root folder. For folder organization, see: https://docs.beyondtrust.com/bt-docs/docs/secrets-api#folders
spec.provider.beyondtrustworkloadcredentials.server <object> (required)	# Server configures the BeyondTrust Workload Credentials server connection details. Includes the API URL and Site ID for your BeyondTrust instance. For API reference, see: https://docs.beyondtrust.com/bt-docs/docs/secrets-api
spec.provider.beyondtrustworkloadcredentials.server.apiUrl <string> (required)	# APIURL is the base URL of your BeyondTrust Workload Credentials API server. This should be the full URL to your BeyondTrust instance. Example: https://api.beyondtrust.io/siie For more information, see: https://docs.beyondtrust.com/bt-docs/docs/secrets-api#base-url
spec.provider.beyondtrustworkloadcredentials.server.siteId <string> (required)	# SiteID is your BeyondTrust Workload Credentials site identifier (UUID format). This identifier is unique to your BeyondTrust Workload Credentials instance. You can find your Site ID in the BeyondTrust Workload Credentials admin console. Example: a1b2c3d4-e5f6-4890-abcd-ef1234567890 For more information, see: https://docs.beyondtrust.com/bt-docs/docs/secrets-api
spec.provider.bitwardensecretsmanager <object>	# BitwardenSecretsManager configures this store to sync secrets using BitwardenSecretsManager provider
spec.provider.bitwardensecretsmanager.apiURL <string>
spec.provider.bitwardensecretsmanager.auth <object> (required)	# Auth configures how secret-manager authenticates with a bitwarden machine account instance. Make sure that the token being used has permissions on the given secret.
spec.provider.bitwardensecretsmanager.auth.secretRef <object> (required)	# BitwardenSecretsManagerSecretRef contains the credential ref to the bitwarden instance.
spec.provider.bitwardensecretsmanager.auth.secretRef.credentials <object> (required)	# AccessToken used for the bitwarden instance.
spec.provider.bitwardensecretsmanager.auth.secretRef.credentials.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.bitwardensecretsmanager.auth.secretRef.credentials.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.bitwardensecretsmanager.auth.secretRef.credentials.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.bitwardensecretsmanager.bitwardenServerSDKURL <string>
spec.provider.bitwardensecretsmanager.caBundle <string>	# Base64 encoded certificate for the bitwarden server sdk. The sdk MUST run with HTTPS to make sure no MITM attack can be performed.
spec.provider.bitwardensecretsmanager.caProvider <object>	# see: https://external-secrets.io/latest/spec/#external-secrets.io/v1alpha1.CAProvider
spec.provider.bitwardensecretsmanager.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.bitwardensecretsmanager.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.bitwardensecretsmanager.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.bitwardensecretsmanager.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.bitwardensecretsmanager.identityURL <string>
spec.provider.bitwardensecretsmanager.organizationID <string> (required)	# OrganizationID determines which organization this secret store manages.
spec.provider.bitwardensecretsmanager.projectID <string> (required)	# ProjectID determines which project this secret store manages.
spec.provider.chef <object>	# Chef configures this store to sync secrets with chef server
spec.provider.chef.auth <object> (required)	# Auth defines the information necessary to authenticate against chef Server
spec.provider.chef.auth.secretRef <object> (required)	# ChefAuthSecretRef holds secret references for chef server login credentials.
spec.provider.chef.auth.secretRef.privateKeySecretRef <object> (required)	# SecretKey is the Signing Key in PEM format, used for authentication.
spec.provider.chef.auth.secretRef.privateKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.chef.auth.secretRef.privateKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.chef.auth.secretRef.privateKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.chef.serverUrl <string> (required)	# ServerURL is the chef server URL used to connect to. If using orgs you should include your org in the url and terminate the url with a "/"
spec.provider.chef.username <string> (required)	# UserName should be the user ID on the chef server
spec.provider.cloudrusm <object>	# CloudruSM configures this store to sync secrets using the Cloud.ru Secret Manager provider
spec.provider.cloudrusm.auth <object> (required)	# CSMAuth contains a secretRef for credentials.
spec.provider.cloudrusm.auth.secretRef <object>	# CSMAuthSecretRef holds secret references for Cloud.ru credentials.
spec.provider.cloudrusm.auth.secretRef.accessKeyIDSecretRef <object> (required)	# The AccessKeyID is used for authentication
spec.provider.cloudrusm.auth.secretRef.accessKeyIDSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.cloudrusm.auth.secretRef.accessKeyIDSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.cloudrusm.auth.secretRef.accessKeyIDSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.cloudrusm.auth.secretRef.accessKeySecretSecretRef <object> (required)	# The AccessKeySecret is used for authentication
spec.provider.cloudrusm.auth.secretRef.accessKeySecretSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.cloudrusm.auth.secretRef.accessKeySecretSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.cloudrusm.auth.secretRef.accessKeySecretSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.cloudrusm.projectID <string>	# ProjectID is the project, which the secrets are stored in.
spec.provider.conjur <object>	# Conjur configures this store to sync secrets using conjur provider
spec.provider.conjur.auth <object> (required)	# Defines authentication settings for connecting to Conjur.
spec.provider.conjur.auth.apikey <object>	# Authenticates with Conjur using an API key.
spec.provider.conjur.auth.apikey.account <string> (required)	# Account is the Conjur organization account name.
spec.provider.conjur.auth.apikey.apiKeyRef <object> (required)	# A reference to a specific 'key' containing the Conjur API key within a Secret resource. In some instances, `key` is a required field.
spec.provider.conjur.auth.apikey.apiKeyRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.conjur.auth.apikey.apiKeyRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.conjur.auth.apikey.apiKeyRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.conjur.auth.apikey.userRef <object> (required)	# A reference to a specific 'key' containing the Conjur username within a Secret resource. In some instances, `key` is a required field.
spec.provider.conjur.auth.apikey.userRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.conjur.auth.apikey.userRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.conjur.auth.apikey.userRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.conjur.auth.jwt <object>	# Jwt enables JWT authentication using Kubernetes service account tokens.
spec.provider.conjur.auth.jwt.account <string> (required)	# Account is the Conjur organization account name.
spec.provider.conjur.auth.jwt.hostId <string>	# Optional HostID for JWT authentication. This may be used depending on how the Conjur JWT authenticator policy is configured.
spec.provider.conjur.auth.jwt.secretRef <object>	# Optional SecretRef that refers to a key in a Secret resource containing JWT token to authenticate with Conjur using the JWT authentication method.
spec.provider.conjur.auth.jwt.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.conjur.auth.jwt.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.conjur.auth.jwt.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.conjur.auth.jwt.serviceAccountRef <object>	# Optional ServiceAccountRef specifies the Kubernetes service account for which to request a token for with the `TokenRequest` API.
spec.provider.conjur.auth.jwt.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.conjur.auth.jwt.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.conjur.auth.jwt.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.conjur.auth.jwt.serviceID <string> (required)	# The conjur authn jwt webservice id
spec.provider.conjur.caBundle <string>	# CABundle is a PEM encoded CA bundle that will be used to validate the Conjur server certificate.
spec.provider.conjur.caProvider <object>	# Used to provide custom certificate authority (CA) certificates for a secret store. The CAProvider points to a Secret or ConfigMap resource that contains a PEM-encoded certificate.
spec.provider.conjur.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.conjur.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.conjur.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.conjur.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.conjur.url <string> (required)	# URL is the endpoint of the Conjur instance.
spec.provider.delinea <object>	# Delinea DevOps Secrets Vault https://docs.delinea.com/online-help/products/devops-secrets-vault/current
spec.provider.delinea.clientId <object> (required)	# ClientID is the non-secret part of the credential.
spec.provider.delinea.clientId.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.delinea.clientId.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.delinea.clientId.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.delinea.clientId.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.delinea.clientId.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.delinea.clientSecret <object> (required)	# ClientSecret is the secret part of the credential.
spec.provider.delinea.clientSecret.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.delinea.clientSecret.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.delinea.clientSecret.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.delinea.clientSecret.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.delinea.clientSecret.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.delinea.tenant <string> (required)	# Tenant is the chosen hostname / site name.
spec.provider.delinea.tld <string>	# TLD is based on the server location that was chosen during provisioning. If unset, defaults to "com".
spec.provider.delinea.urlTemplate <string>	# URLTemplate If unset, defaults to "https://%s.secretsvaultcloud.%s/v1/%s%s".
spec.provider.doppler <object>	# Doppler configures this store to sync secrets using the Doppler provider
spec.provider.doppler.auth <object> (required)	# Auth configures how the Operator authenticates with the Doppler API
spec.provider.doppler.auth.oidcConfig <object>	# OIDCConfig authenticates using Kubernetes ServiceAccount tokens via OIDC.
spec.provider.doppler.auth.oidcConfig.expirationSeconds <integer> default=600 format=int64	# ExpirationSeconds sets the ServiceAccount token validity duration. Defaults to 10 minutes.
spec.provider.doppler.auth.oidcConfig.identity <string> (required)	# Identity is the Doppler Service Account Identity ID configured for OIDC authentication.
spec.provider.doppler.auth.oidcConfig.serviceAccountRef <object> (required)	# ServiceAccountRef specifies the Kubernetes ServiceAccount to use for authentication.
spec.provider.doppler.auth.oidcConfig.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.doppler.auth.oidcConfig.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.doppler.auth.oidcConfig.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.doppler.auth.secretRef <object>	# SecretRef authenticates using a Doppler service token stored in a Kubernetes Secret.
spec.provider.doppler.auth.secretRef.dopplerToken <object> (required)	# The DopplerToken is used for authentication. See https://docs.doppler.com/reference/api#authentication for auth token types. The Key attribute defaults to dopplerToken if not specified.
spec.provider.doppler.auth.secretRef.dopplerToken.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.doppler.auth.secretRef.dopplerToken.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.doppler.auth.secretRef.dopplerToken.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.doppler.config <string>	# Doppler config (required if not using a Service Token)
spec.provider.doppler.format <string> enum=json|dotnet-json|env|yaml|docker	# Format enables the downloading of secrets as a file (string)
spec.provider.doppler.nameTransformer <string> enum=upper-camel|camel|lower-snake|tf-var|dotnet-env|lower-kebab	# Environment variable compatible name transforms that change secret names to a different format
spec.provider.doppler.project <string>	# Doppler project (required if not using a Service Token)
spec.provider.dvls <object>	# DVLS configures this store to sync secrets using Devolutions Server provider
spec.provider.dvls.auth <object> (required)	# Auth defines the authentication method to use.
spec.provider.dvls.auth.secretRef <object> (required)	# SecretRef contains the Application ID and Application Secret for authentication.
spec.provider.dvls.auth.secretRef.appId <object> (required)	# AppID is the reference to the secret containing the Application ID.
spec.provider.dvls.auth.secretRef.appId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.dvls.auth.secretRef.appId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.dvls.auth.secretRef.appId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.dvls.auth.secretRef.appSecret <object> (required)	# AppSecret is the reference to the secret containing the Application Secret.
spec.provider.dvls.auth.secretRef.appSecret.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.dvls.auth.secretRef.appSecret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.dvls.auth.secretRef.appSecret.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.dvls.insecure <boolean>	# Insecure allows connecting to DVLS over plain HTTP. This is NOT RECOMMENDED for production use. Set to true only if you understand the security implications.
spec.provider.dvls.serverUrl <string> (required)	# ServerURL is the DVLS instance URL (e.g., https://dvls.example.com).
spec.provider.dvls.vault <string>	# Vault is the name or UUID of the vault to fetch secrets from. When omitted, the vault must be specified in the secret key using the legacy format "<vault-id>/<entry-id>".
spec.provider.fake <object>	# Fake configures a store with static key/value pairs
spec.provider.fake.data <[]object> (required)
spec.provider.fake.data[].key <string> (required)
spec.provider.fake.data[].value <string> (required)
spec.provider.fake.data[].version <string>
spec.provider.fake.validationResult <integer>	# ValidationResult is defined type for the number of validation results.
spec.provider.fortanix <object>	# Fortanix configures this store to sync secrets using the Fortanix provider
spec.provider.fortanix.apiKey <object>	# APIKey is the API token to access SDKMS Applications.
spec.provider.fortanix.apiKey.secretRef <object>	# SecretRef is a reference to a secret containing the SDKMS API Key.
spec.provider.fortanix.apiKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.fortanix.apiKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.fortanix.apiKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.fortanix.apiUrl <string>	# APIURL is the URL of SDKMS API. Defaults to `sdkms.fortanix.com`.
spec.provider.gcpsm <object>	# GCPSM configures this store to sync secrets using Google Cloud Platform Secret Manager provider
spec.provider.gcpsm.auth <object>	# Auth defines the information necessary to authenticate against GCP
spec.provider.gcpsm.auth.secretRef <object>	# GCPSMAuthSecretRef contains the secret references for GCP Secret Manager authentication.
spec.provider.gcpsm.auth.secretRef.secretAccessKeySecretRef <object>	# The SecretAccessKey is used for authentication
spec.provider.gcpsm.auth.secretRef.secretAccessKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.gcpsm.auth.secretRef.secretAccessKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.gcpsm.auth.secretRef.secretAccessKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.gcpsm.auth.workloadIdentity <object>	# GCPWorkloadIdentity defines configuration for workload identity authentication to GCP.
spec.provider.gcpsm.auth.workloadIdentity.clusterLocation <string>	# ClusterLocation is the location of the cluster If not specified, it fetches information from the metadata server
spec.provider.gcpsm.auth.workloadIdentity.clusterName <string>	# ClusterName is the name of the cluster If not specified, it fetches information from the metadata server
spec.provider.gcpsm.auth.workloadIdentity.clusterProjectID <string>	# ClusterProjectID is the project ID of the cluster If not specified, it fetches information from the metadata server
spec.provider.gcpsm.auth.workloadIdentity.serviceAccountRef <object> (required)	# ServiceAccountSelector is a reference to a ServiceAccount resource.
spec.provider.gcpsm.auth.workloadIdentity.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.gcpsm.auth.workloadIdentity.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.gcpsm.auth.workloadIdentity.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.gcpsm.auth.workloadIdentityFederation <object>	# GCPWorkloadIdentityFederation holds the configurations required for generating federated access tokens.
spec.provider.gcpsm.auth.workloadIdentityFederation.audience <string>	# audience is the Secure Token Service (STS) audience which contains the resource name for the workload identity pool and the provider identifier in that pool. If specified, Audience found in the external account credential config will be overridden with the configured value. audience must be provided when serviceAccountRef or awsSecurityCredentials is configured.
spec.provider.gcpsm.auth.workloadIdentityFederation.awsSecurityCredentials <object>	# awsSecurityCredentials is for configuring AWS region and credentials to use for obtaining the access token, when using the AWS metadata server is not an option.
spec.provider.gcpsm.auth.workloadIdentityFederation.awsSecurityCredentials.awsCredentialsSecretRef <object> (required)	# awsCredentialsSecretRef is the reference to the secret which holds the AWS credentials. Secret should be created with below names for keys - aws_access_key_id: Access Key ID, which is the unique identifier for the AWS account or the IAM user. - aws_secret_access_key: Secret Access Key, which is used to authenticate requests made to AWS services. - aws_session_token: Session Token, is the short-lived token to authenticate requests made to AWS services.
spec.provider.gcpsm.auth.workloadIdentityFederation.awsSecurityCredentials.awsCredentialsSecretRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the secret.
spec.provider.gcpsm.auth.workloadIdentityFederation.awsSecurityCredentials.awsCredentialsSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# namespace in which the secret exists. If empty, secret will looked up in local namespace.
spec.provider.gcpsm.auth.workloadIdentityFederation.awsSecurityCredentials.region <string> (required) pattern="^[a-z0-9-]+$" min=1 max=50	# region is for configuring the AWS region to be used.
spec.provider.gcpsm.auth.workloadIdentityFederation.credConfig <object>	# credConfig holds the configmap reference containing the GCP external account credential configuration in JSON format and the key name containing the json data. For using Kubernetes cluster as the identity provider, use serviceAccountRef instead. Operators mounted serviceaccount token cannot be used as the token source, instead serviceAccountRef must be used by providing operators service account details.
spec.provider.gcpsm.auth.workloadIdentityFederation.credConfig.key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# key name holding the external account credential config.
spec.provider.gcpsm.auth.workloadIdentityFederation.credConfig.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the configmap.
spec.provider.gcpsm.auth.workloadIdentityFederation.credConfig.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# namespace in which the configmap exists. If empty, configmap will looked up in local namespace.
spec.provider.gcpsm.auth.workloadIdentityFederation.externalTokenEndpoint <string>	# externalTokenEndpoint is the endpoint explicitly set up to provide tokens, which will be matched against the credential_source.url in the provided credConfig. This field is merely to double-check the external token source URL is having the expected value.
spec.provider.gcpsm.auth.workloadIdentityFederation.gcpServiceAccountEmail <string> pattern="^.*@.*\\.iam\\.gserviceaccount\\.com$" min=1	# GCPServiceAccountEmail is the email of the Google Cloud service account to impersonate after Workload Identity Federation. Use this to grant access through the service account's IAM bindings (for example roles/secretmanager.secretAccessor). When set, it overrides service_account_impersonation_url in the external account JSON from credConfig; when serviceAccountRef is set, it also overrides the "iam.gke.io/gcp-service-account" annotation on that ServiceAccount.
spec.provider.gcpsm.auth.workloadIdentityFederation.serviceAccountRef <object>	# serviceAccountRef is the reference to the kubernetes ServiceAccount to be used for obtaining the tokens, when Kubernetes is configured as provider in workload identity pool.
spec.provider.gcpsm.auth.workloadIdentityFederation.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.gcpsm.auth.workloadIdentityFederation.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.gcpsm.auth.workloadIdentityFederation.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.gcpsm.location <string>	# Location optionally defines a location for a secret
spec.provider.gcpsm.projectID <string>	# ProjectID project where secret is located
spec.provider.gcpsm.secretVersionSelectionPolicy <string> default="LatestOrFail"	# SecretVersionSelectionPolicy specifies how the provider selects a secret version when "latest" is disabled or destroyed. Possible values are: - LatestOrFail: the provider always uses "latest", or fails if that version is disabled/destroyed. - LatestOrFetch: the provider falls back to fetching the latest version if the version is DESTROYED or DISABLED
spec.provider.github <object>	# Github configures this store to push GitHub Actions secrets using the GitHub API provider. Note: This provider only supports write operations (PushSecret) and cannot fetch secrets from GitHub
spec.provider.github.appID <integer> (required) format=int64	# appID specifies the Github APP that will be used to authenticate the client
spec.provider.github.auth <object> (required)	# auth configures how secret-manager authenticates with a Github instance.
spec.provider.github.auth.privateKey <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.github.auth.privateKey.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.github.auth.privateKey.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.github.auth.privateKey.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.github.environment <string>	# environment will be used to fetch secrets from a particular environment within a github repository
spec.provider.github.installationID <integer> (required) format=int64	# installationID specifies the Github APP installation that will be used to authenticate the client
spec.provider.github.orgSecretVisibility <string> enum=all|private	# orgSecretVisibility controls the visibility of organization secrets pushed via PushSecret. Valid values are "all" or "private". When unset, new secrets are created with visibility "all" and existing secrets preserve whatever visibility they already have in GitHub.
spec.provider.github.organization <string> (required)	# organization will be used to fetch secrets from the Github organization
spec.provider.github.repository <string>	# repository will be used to fetch secrets from the Github repository within an organization
spec.provider.github.uploadURL <string>	# Upload URL for enterprise instances. Default to URL.
spec.provider.github.url <string> default="https://github.com/"	# URL configures the Github instance URL. Defaults to https://github.com/.
spec.provider.gitlab <object>	# GitLab configures this store to sync secrets using GitLab Variables provider
spec.provider.gitlab.auth <object> (required)	# Auth configures how secret-manager authenticates with a GitLab instance.
spec.provider.gitlab.auth.SecretRef <object> (required)	# GitlabSecretRef contains the secret reference for GitLab authentication credentials.
spec.provider.gitlab.auth.SecretRef.accessToken <object>	# AccessToken is used for authentication.
spec.provider.gitlab.auth.SecretRef.accessToken.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.gitlab.auth.SecretRef.accessToken.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.gitlab.auth.SecretRef.accessToken.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.gitlab.caBundle <string> format=byte	# Base64 encoded certificate for the GitLab server sdk. The sdk MUST run with HTTPS to make sure no MITM attack can be performed.
spec.provider.gitlab.caProvider <object>	# see: https://external-secrets.io/latest/spec/#external-secrets.io/v1alpha1.CAProvider
spec.provider.gitlab.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.gitlab.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.gitlab.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.gitlab.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.gitlab.environment <string>	# Environment environment_scope of gitlab CI/CD variables (Please see https://docs.gitlab.com/ee/ci/environments/#create-a-static-environment on how to create environments)
spec.provider.gitlab.groupIDs <[]string>	# GroupIDs specify, which gitlab groups to pull secrets from. Group secrets are read from left to right followed by the project variables.
spec.provider.gitlab.inheritFromGroups <boolean>	# InheritFromGroups specifies whether parent groups should be discovered and checked for secrets.
spec.provider.gitlab.projectID <string>	# ProjectID specifies a project where secrets are located.
spec.provider.gitlab.url <string>	# URL configures the GitLab instance URL. Defaults to https://gitlab.com/.
spec.provider.ibm <object>	# IBM configures this store to sync secrets using IBM Cloud provider
spec.provider.ibm.auth <object> (required)	# Auth configures how secret-manager authenticates with the IBM secrets manager.
spec.provider.ibm.auth.containerAuth <object>	# IBMAuthContainerAuth defines container-based authentication with IAM Trusted Profile.
spec.provider.ibm.auth.containerAuth.iamEndpoint <string>
spec.provider.ibm.auth.containerAuth.profile <string> (required)	# the IBM Trusted Profile
spec.provider.ibm.auth.containerAuth.tokenLocation <string>	# Location the token is mounted on the pod
spec.provider.ibm.auth.secretRef <object>	# IBMAuthSecretRef contains the secret reference for IBM Cloud API key authentication.
spec.provider.ibm.auth.secretRef.iamEndpoint <string>	# The IAM endpoint used to obain a token
spec.provider.ibm.auth.secretRef.secretApiKeySecretRef <object>	# The SecretAccessKey is used for authentication
spec.provider.ibm.auth.secretRef.secretApiKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.ibm.auth.secretRef.secretApiKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.ibm.auth.secretRef.secretApiKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.ibm.serviceUrl <string>	# ServiceURL is the Endpoint URL that is specific to the Secrets Manager service instance
spec.provider.infisical <object>	# Infisical configures this store to sync secrets using the Infisical provider
spec.provider.infisical.auth <object> (required)	# Auth configures how the Operator authenticates with the Infisical API
spec.provider.infisical.auth.awsAuthCredentials <object>	# AwsAuthCredentials represents the credentials for AWS authentication.
spec.provider.infisical.auth.awsAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.awsAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.awsAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.awsAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.azureAuthCredentials <object>	# AzureAuthCredentials represents the credentials for Azure authentication.
spec.provider.infisical.auth.azureAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.azureAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.azureAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.azureAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.azureAuthCredentials.resource <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.azureAuthCredentials.resource.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.azureAuthCredentials.resource.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.azureAuthCredentials.resource.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.gcpIamAuthCredentials <object>	# GcpIamAuthCredentials represents the credentials for GCP IAM authentication.
spec.provider.infisical.auth.gcpIamAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.gcpIamAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.gcpIamAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.gcpIamAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.gcpIamAuthCredentials.serviceAccountKeyFilePath <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.gcpIamAuthCredentials.serviceAccountKeyFilePath.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.gcpIamAuthCredentials.serviceAccountKeyFilePath.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.gcpIamAuthCredentials.serviceAccountKeyFilePath.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.gcpIdTokenAuthCredentials <object>	# GcpIDTokenAuthCredentials represents the credentials for GCP ID token authentication.
spec.provider.infisical.auth.gcpIdTokenAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.gcpIdTokenAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.gcpIdTokenAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.gcpIdTokenAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.jwtAuthCredentials <object>	# JwtAuthCredentials represents the credentials for JWT authentication.
spec.provider.infisical.auth.jwtAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.jwtAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.jwtAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.jwtAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.jwtAuthCredentials.jwt <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.jwtAuthCredentials.jwt.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.jwtAuthCredentials.jwt.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.jwtAuthCredentials.jwt.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.kubernetesAuthCredentials <object>	# KubernetesAuthCredentials represents the credentials for Kubernetes authentication.
spec.provider.infisical.auth.kubernetesAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.kubernetesAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.kubernetesAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.kubernetesAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.kubernetesAuthCredentials.serviceAccountTokenPath <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.kubernetesAuthCredentials.serviceAccountTokenPath.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.kubernetesAuthCredentials.serviceAccountTokenPath.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.kubernetesAuthCredentials.serviceAccountTokenPath.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ldapAuthCredentials <object>	# LdapAuthCredentials represents the credentials for LDAP authentication.
spec.provider.infisical.auth.ldapAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ldapAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ldapAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ldapAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ldapAuthCredentials.ldapPassword <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ldapAuthCredentials.ldapPassword.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ldapAuthCredentials.ldapPassword.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ldapAuthCredentials.ldapPassword.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ldapAuthCredentials.ldapUsername <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ldapAuthCredentials.ldapUsername.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ldapAuthCredentials.ldapUsername.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ldapAuthCredentials.ldapUsername.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials <object>	# OciAuthCredentials represents the credentials for OCI authentication.
spec.provider.infisical.auth.ociAuthCredentials.fingerprint <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.fingerprint.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.fingerprint.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.fingerprint.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials.identityId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.identityId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.identityId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.identityId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials.privateKey <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.privateKey.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.privateKey.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.privateKey.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials.privateKeyPassphrase <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.privateKeyPassphrase.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.privateKeyPassphrase.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.privateKeyPassphrase.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials.region <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.region.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.region.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.region.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials.tenancyId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.tenancyId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.tenancyId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.tenancyId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.ociAuthCredentials.userId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.ociAuthCredentials.userId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.ociAuthCredentials.userId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.ociAuthCredentials.userId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.tokenAuthCredentials <object>	# TokenAuthCredentials represents the credentials for access token-based authentication.
spec.provider.infisical.auth.tokenAuthCredentials.accessToken <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.tokenAuthCredentials.accessToken.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.tokenAuthCredentials.accessToken.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.tokenAuthCredentials.accessToken.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.universalAuthCredentials <object>	# UniversalAuthCredentials represents the client credentials for universal authentication.
spec.provider.infisical.auth.universalAuthCredentials.clientId <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.universalAuthCredentials.clientId.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.universalAuthCredentials.clientId.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.universalAuthCredentials.clientId.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.auth.universalAuthCredentials.clientSecret <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.infisical.auth.universalAuthCredentials.clientSecret.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.infisical.auth.universalAuthCredentials.clientSecret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.infisical.auth.universalAuthCredentials.clientSecret.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.infisical.caBundle <string> format=byte	# CABundle is a PEM-encoded CA certificate bundle used to validate the Infisical server's TLS certificate. Mutually exclusive with CAProvider.
spec.provider.infisical.caProvider <object>	# CAProvider is a reference to a Secret or ConfigMap that contains a CA certificate. The certificate is used to validate the Infisical server's TLS certificate. Mutually exclusive with CABundle.
spec.provider.infisical.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.infisical.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.infisical.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.infisical.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.infisical.hostAPI <string> default="https://app.infisical.com/api"	# HostAPI specifies the base URL of the Infisical API. If not provided, it defaults to "https://app.infisical.com/api".
spec.provider.infisical.secretsScope <object> (required)	# SecretsScope defines the scope of the secrets within the workspace
spec.provider.infisical.secretsScope.environmentSlug <string> (required)	# EnvironmentSlug is the required slug identifier for the environment.
spec.provider.infisical.secretsScope.expandSecretReferences <boolean> default=true	# ExpandSecretReferences indicates whether secret references should be expanded. Defaults to true if not provided.
spec.provider.infisical.secretsScope.organizationSlug <string>	# OrganizationSlug is the optional slug that identifies the organization that will be used during authentication. Useful for sub-organization setups
spec.provider.infisical.secretsScope.projectSlug <string> (required)	# ProjectSlug is the required slug identifier for the project.
spec.provider.infisical.secretsScope.recursive <boolean> default=false	# Recursive indicates whether the secrets should be fetched recursively. Defaults to false if not provided.
spec.provider.infisical.secretsScope.secretsPath <string> default="/"	# SecretsPath specifies the path to the secrets within the workspace. Defaults to "/" if not provided.
spec.provider.keepersecurity <object>	# KeeperSecurity configures this store to sync secrets using the KeeperSecurity provider
spec.provider.keepersecurity.authRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.keepersecurity.authRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.keepersecurity.authRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.keepersecurity.authRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.keepersecurity.folderID <string> (required)
spec.provider.keepersecurity.getByTitleFallback <boolean>
spec.provider.kubernetes <object>	# Kubernetes configures this store to sync secrets using a Kubernetes cluster provider
spec.provider.kubernetes.auth <object>	# Auth configures how secret-manager authenticates with a Kubernetes instance.
spec.provider.kubernetes.auth.cert <object>	# has both clientCert and clientKey as secretKeySelector
spec.provider.kubernetes.auth.cert.clientCert <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.kubernetes.auth.cert.clientCert.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.kubernetes.auth.cert.clientCert.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.kubernetes.auth.cert.clientCert.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.kubernetes.auth.cert.clientKey <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.kubernetes.auth.cert.clientKey.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.kubernetes.auth.cert.clientKey.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.kubernetes.auth.cert.clientKey.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.kubernetes.auth.serviceAccount <object>	# points to a service account that should be used for authentication
spec.provider.kubernetes.auth.serviceAccount.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.kubernetes.auth.serviceAccount.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.kubernetes.auth.serviceAccount.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.kubernetes.auth.token <object>	# use static token to authenticate with
spec.provider.kubernetes.auth.token.bearerToken <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.kubernetes.auth.token.bearerToken.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.kubernetes.auth.token.bearerToken.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.kubernetes.auth.token.bearerToken.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.kubernetes.authRef <object>	# A reference to a secret that contains the auth information.
spec.provider.kubernetes.authRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.kubernetes.authRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.kubernetes.authRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.kubernetes.remoteNamespace <string> default="default" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Remote namespace to fetch the secrets from
spec.provider.kubernetes.server <object>	# configures the Kubernetes server Address.
spec.provider.kubernetes.server.caBundle <string> format=byte	# CABundle is a base64-encoded CA certificate
spec.provider.kubernetes.server.caProvider <object>	# see: https://external-secrets.io/v0.4.1/spec/#external-secrets.io/v1alpha1.CAProvider
spec.provider.kubernetes.server.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.kubernetes.server.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.kubernetes.server.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.kubernetes.server.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.kubernetes.server.url <string> default="kubernetes.default"	# configures the Kubernetes server Address.
spec.provider.nebiusmysterybox <object>	# NebiusMysterybox configures this store to sync secrets using NebiusMysterybox provider
spec.provider.nebiusmysterybox.apiDomain <string> (required)	# NebiusMysterybox API endpoint
spec.provider.nebiusmysterybox.auth <object> (required)	# Auth defines parameters to authenticate in MysteryBox
spec.provider.nebiusmysterybox.auth.serviceAccountCredsSecretRef <object>	# ServiceAccountCreds references a Kubernetes Secret key that contains a JSON document with service account credentials used to get an IAM token. Expected JSON structure: { "subject-credentials": { "alg": "RS256", "private-key": "-----BEGIN PRIVATE KEY-----\n<private-key>\n-----END PRIVATE KEY-----\n", "kid": "<public-key-id>", "iss": "<issuer-service-account-id>", "sub": "<subject-service-account-id>" } }
spec.provider.nebiusmysterybox.auth.serviceAccountCredsSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.nebiusmysterybox.auth.serviceAccountCredsSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.nebiusmysterybox.auth.serviceAccountCredsSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.nebiusmysterybox.auth.tokenSecretRef <object>	# Token authenticates with Nebius Mysterybox by presenting a token.
spec.provider.nebiusmysterybox.auth.tokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.nebiusmysterybox.auth.tokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.nebiusmysterybox.auth.tokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.nebiusmysterybox.caProvider <object>	# The provider for the CA bundle to use to validate NebiusMysterybox server certificate.
spec.provider.nebiusmysterybox.caProvider.certSecretRef <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.nebiusmysterybox.caProvider.certSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.nebiusmysterybox.caProvider.certSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.nebiusmysterybox.caProvider.certSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.ngrok <object>	# Ngrok configures this store to sync secrets using the ngrok provider.
spec.provider.ngrok.apiUrl <string> default="https://api.ngrok.com"	# APIURL is the URL of the ngrok API.
spec.provider.ngrok.auth <object> (required)	# Auth configures how the ngrok provider authenticates with the ngrok API.
spec.provider.ngrok.auth.apiKey <object>	# APIKey is the API Key used to authenticate with ngrok. See https://ngrok.com/docs/api/#authentication
spec.provider.ngrok.auth.apiKey.secretRef <object>	# SecretRef is a reference to a secret containing the ngrok API key.
spec.provider.ngrok.auth.apiKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.ngrok.auth.apiKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.ngrok.auth.apiKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.ngrok.vault <object> (required)	# Vault configures the ngrok vault to sync secrets with.
spec.provider.ngrok.vault.name <string> (required)	# Name is the name of the ngrok vault to sync secrets with.
spec.provider.onboardbase <object>	# Onboardbase configures this store to sync secrets using the Onboardbase provider
spec.provider.onboardbase.apiHost <string> (required) default="https://public.onboardbase.com/api/v1/"	# APIHost use this to configure the host url for the API for selfhosted installation, default is https://public.onboardbase.com/api/v1/
spec.provider.onboardbase.auth <object> (required)	# Auth configures how the Operator authenticates with the Onboardbase API
spec.provider.onboardbase.auth.apiKeyRef <object> (required)	# OnboardbaseAPIKey is the APIKey generated by an admin account. It is used to recognize and authorize access to a project and environment within onboardbase
spec.provider.onboardbase.auth.apiKeyRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.onboardbase.auth.apiKeyRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.onboardbase.auth.apiKeyRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.onboardbase.auth.passcodeRef <object> (required)	# OnboardbasePasscode is the passcode attached to the API Key
spec.provider.onboardbase.auth.passcodeRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.onboardbase.auth.passcodeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.onboardbase.auth.passcodeRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.onboardbase.environment <string> (required) default="development"	# Environment is the name of an environmnent within a project to pull the secrets from
spec.provider.onboardbase.project <string> (required) default="development"	# Project is an onboardbase project that the secrets should be pulled from
spec.provider.onepassword <object>	# OnePassword configures this store to sync secrets using the 1Password Cloud provider
spec.provider.onepassword.auth <object> (required)	# Auth defines the information necessary to authenticate against OnePassword Connect Server
spec.provider.onepassword.auth.secretRef <object> (required)	# OnePasswordAuthSecretRef holds secret references for 1Password credentials.
spec.provider.onepassword.auth.secretRef.connectTokenSecretRef <object> (required)	# The ConnectToken is used for authentication to a 1Password Connect Server.
spec.provider.onepassword.auth.secretRef.connectTokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.onepassword.auth.secretRef.connectTokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.onepassword.auth.secretRef.connectTokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.onepassword.connectHost <string> (required)	# ConnectHost defines the OnePassword Connect Server to connect to
spec.provider.onepassword.vaults <map[string]integer> (required)	# Vaults defines which OnePassword vaults to search in which order
spec.provider.onepasswordSDK <object>	# OnePasswordSDK configures this store to use 1Password's new Go SDK to sync secrets.
spec.provider.onepasswordSDK.auth <object> (required)	# Auth defines the information necessary to authenticate against OnePassword API.
spec.provider.onepasswordSDK.auth.serviceAccountSecretRef <object> (required)	# ServiceAccountSecretRef points to the secret containing the token to access 1Password vault.
spec.provider.onepasswordSDK.auth.serviceAccountSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.onepasswordSDK.auth.serviceAccountSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.onepasswordSDK.auth.serviceAccountSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.onepasswordSDK.cache <object>	# Cache configures client-side caching for read operations (GetSecret, GetSecretMap). When enabled, secrets are cached with the specified TTL. Write operations (PushSecret, DeleteSecret) automatically invalidate relevant cache entries. If omitted, caching is disabled (default). cache: {} is a valid option to set.
spec.provider.onepasswordSDK.cache.maxSize <integer> default=100 min=1	# MaxSize is the maximum number of secrets to cache. When the cache is full, least-recently-used entries are evicted.
spec.provider.onepasswordSDK.cache.ttl <string> default="5m"	# TTL is the time-to-live for cached secrets. Format: duration string (e.g., "5m", "1h", "30s")
spec.provider.onepasswordSDK.integrationInfo <object>	# IntegrationInfo specifies the name and version of the integration built using the 1Password Go SDK. If you don't know which name and version to use, use `DefaultIntegrationName` and `DefaultIntegrationVersion`, respectively.
spec.provider.onepasswordSDK.integrationInfo.name <string> default="1Password SDK"	# Name defaults to "1Password SDK".
spec.provider.onepasswordSDK.integrationInfo.version <string> default="v1.0.0"	# Version defaults to "v1.0.0".
spec.provider.onepasswordSDK.vault <string> (required)	# Vault defines the vault's name or uuid to access. Do NOT add op:// prefix. This will be done automatically.
spec.provider.openBao <object>	# OpenBao configures this store to sync secrets using the OpenBao provider.
spec.provider.openBao.auth <object>	# Auth configures how secret-manager authenticates with the OpenBao server.
spec.provider.openBao.auth.appRole <object>	# AppRole authenticates with OpenBao using the [App Role auth mechanism], with the role and secret stored in a Kubernetes Secret resource. [App Role auth mechanism]: https://openbao.org/docs/auth/approle/
spec.provider.openBao.auth.appRole.path <string> (required) default="approle"	# Path where the App Role authentication backend is mounted in OpenBao, e.g: "approle"
spec.provider.openBao.auth.appRole.roleId <string> min=1	# RoleID configured in the App Role authentication backend when setting up the authentication backend in OpenBao.
spec.provider.openBao.auth.appRole.roleRef <object>	# Reference to a key in a Secret that contains the App Role ID used to authenticate with OpenBao. The `key` field must be specified and denotes which entry within the Secret resource is used as the app role id.
spec.provider.openBao.auth.appRole.roleRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.openBao.auth.appRole.roleRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.openBao.auth.appRole.roleRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.openBao.auth.appRole.secretRef <object> (required)	# Reference to a key in a Secret that contains the App Role secret used to authenticate with OpenBao. The `key` field must be specified and denotes which entry within the Secret resource is used as the app role secret.
spec.provider.openBao.auth.appRole.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.openBao.auth.appRole.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.openBao.auth.appRole.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.openBao.auth.namespace <string>	# Name of the [OpenBao Namespace] to authenticate to. This can be different than the namespace your secret is in. Namespaces is a set of features within OpenBao that allows OpenBao environments to support secure multi-tenancy. e.g: "ns1". This will default to OpenBao.Namespace field if set, or empty otherwise [OpenBao Namespace]: https://openbao.org/docs/concepts/namespaces/
spec.provider.openBao.auth.tokenSecretRef <object>	# TokenSecretRef authenticates with OpenBao by presenting a token.
spec.provider.openBao.auth.tokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.openBao.auth.tokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.openBao.auth.tokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.openBao.auth.userPass <object>	# UserPass authenticates with OpenBao by passing a username/password pair
spec.provider.openBao.auth.userPass.path <string> (required) default="userpass"	# Path where the UserPassword authentication backend is mounted in OpenBao, e.g: "userpass"
spec.provider.openBao.auth.userPass.secretRef <object>	# SecretRef to a key in a Secret resource containing password for the user used to authenticate with OpenBao using the [UserPass authentication method] [UserPass authentication method]: https://openbao.org/docs/auth/userpass/
spec.provider.openBao.auth.userPass.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.openBao.auth.userPass.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.openBao.auth.userPass.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.openBao.auth.userPass.username <string> (required)	# Username is a username used to authenticate using the [UserPass authentication method] [UserPass authentication method]: https://openbao.org/docs/auth/userpass/
spec.provider.openBao.caBundle <string> format=byte	# PEM encoded CA bundle used to validate the OpenBao server certificate. If this and `caProvider` are not set the system root certificates are used to validate the TLS connection.
spec.provider.openBao.caProvider <object>	# The provider for the CA bundle to use to validate OpenBao server certificate. If this and `caBundle` are not set the system root certificates are used to validate the TLS connection.
spec.provider.openBao.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.openBao.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.openBao.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.openBao.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.openBao.namespace <string>	# Name of the [OpenBao Namespace]. Namespaces is a set of features within OpenBao that allows OpenBao environments to support secure multi-tenancy. e.g: "ns1". [OpenBao Namespace]: https://openbao.org/docs/concepts/namespaces/
spec.provider.openBao.path <string>	# Path is the mount path of the OpenBao KV backend endpoint, e.g: "secret". The v2 KV secret engine version specific "/data" path suffix for fetching secrets from OpenBao is optional and will be appended if not present in specified path.
spec.provider.openBao.server <string> (required)	# Server is the connection address for the OpenBao server, e.g: `https://openbao.example.com:8200`.
spec.provider.openBao.version <string> enum=v1|v2 default="v2"	# Version is the OpenBao KV secret engine version. This can be either "v1" or "v2". Version defaults to "v2".
spec.provider.oracle <object>	# Oracle configures this store to sync secrets using Oracle Vault provider
spec.provider.oracle.auth <object>	# Auth configures how secret-manager authenticates with the Oracle Vault. If empty, use the instance principal, otherwise the user credentials specified in Auth.
spec.provider.oracle.auth.secretRef <object> (required)	# SecretRef to pass through sensitive information.
spec.provider.oracle.auth.secretRef.fingerprint <object> (required)	# Fingerprint is the fingerprint of the API private key.
spec.provider.oracle.auth.secretRef.fingerprint.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.oracle.auth.secretRef.fingerprint.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.oracle.auth.secretRef.fingerprint.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.oracle.auth.secretRef.privatekey <object> (required)	# PrivateKey is the user's API Signing Key in PEM format, used for authentication.
spec.provider.oracle.auth.secretRef.privatekey.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.oracle.auth.secretRef.privatekey.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.oracle.auth.secretRef.privatekey.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.oracle.auth.tenancy <string> (required)	# Tenancy is the tenancy OCID where user is located.
spec.provider.oracle.auth.user <string> (required)	# User is an access OCID specific to the account.
spec.provider.oracle.compartment <string>	# Compartment is the vault compartment OCID. Required for PushSecret
spec.provider.oracle.encryptionKey <string>	# EncryptionKey is the OCID of the encryption key within the vault. Required for PushSecret
spec.provider.oracle.principalType <string> enum=|UserPrincipal|InstancePrincipal|Workload	# The type of principal to use for authentication. If left blank, the Auth struct will determine the principal type. This optional field must be specified if using workload identity.
spec.provider.oracle.region <string> (required)	# Region is the region where vault is located.
spec.provider.oracle.serviceAccountRef <object>	# ServiceAccountRef specified the service account that should be used when authenticating with WorkloadIdentity.
spec.provider.oracle.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.oracle.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.oracle.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.oracle.vault <string> (required)	# Vault is the vault's OCID of the specific vault where secret is located.
spec.provider.ovh <object>	# OVHcloud configures this store to sync secrets using the OVHcloud provider.
spec.provider.ovh.auth <object> (required)	# Authentication method (mtls or token).
spec.provider.ovh.auth.mtls <object>	# OvhClientMTLS defines the configuration required to authenticate to OVHcloud's Secret Manager using mTLS.
spec.provider.ovh.auth.mtls.caBundle <string> format=byte
spec.provider.ovh.auth.mtls.caProvider <object>	# CAProvider provides a custom certificate authority for accessing the provider's store. The CAProvider points to a Secret or ConfigMap resource that contains a PEM-encoded certificate.
spec.provider.ovh.auth.mtls.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.ovh.auth.mtls.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.ovh.auth.mtls.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.ovh.auth.mtls.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.ovh.auth.mtls.certSecretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.ovh.auth.mtls.certSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.ovh.auth.mtls.certSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.ovh.auth.mtls.certSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.ovh.auth.mtls.keySecretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.ovh.auth.mtls.keySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.ovh.auth.mtls.keySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.ovh.auth.mtls.keySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.ovh.auth.token <object>	# OvhClientToken defines the configuration required to authenticate to OVHcloud's Secret Manager using a token.
spec.provider.ovh.auth.token.tokenSecretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.ovh.auth.token.tokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.ovh.auth.token.tokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.ovh.auth.token.tokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.ovh.casRequired <boolean>	# Enables or disables check-and-set (CAS) (default: false).
spec.provider.ovh.okmsTimeout <integer> default=30 format=int32 min=1	# Setup a timeout in seconds when requests to the KMS are made (default: 30).
spec.provider.ovh.okmsid <string> (required)	# specifies the OKMS ID.
spec.provider.ovh.server <string> (required)	# specifies the OKMS server endpoint.
spec.provider.passbolt <object>	# PassboltProvider provides access to Passbolt secrets manager. See: https://www.passbolt.com.
spec.provider.passbolt.auth <object> (required)	# Auth defines the information necessary to authenticate against Passbolt Server
spec.provider.passbolt.auth.passwordSecretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.passbolt.auth.passwordSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.passbolt.auth.passwordSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.passbolt.auth.passwordSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.passbolt.auth.privateKeySecretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.passbolt.auth.privateKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.passbolt.auth.privateKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.passbolt.auth.privateKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.passbolt.caBundle <string> format=byte	# PEM encoded CA bundle used to validate Passbolt server certificate. Only used if the Host URL is using HTTPS protocol. If not set the system root certificates are used to validate the TLS connection.
spec.provider.passbolt.caProvider <object>	# The provider for the CA bundle to use to validate Passbolt server certificate.
spec.provider.passbolt.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.passbolt.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.passbolt.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.passbolt.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.passbolt.host <string> (required)	# Host defines the Passbolt Server to connect to
spec.provider.passworddepot <object>	# PasswordDepotProvider configures a store to sync secrets with a Password Depot instance.
spec.provider.passworddepot.auth <object> (required)	# Auth configures how secret-manager authenticates with a Password Depot instance.
spec.provider.passworddepot.auth.secretRef <object> (required)	# PasswordDepotSecretRef contains the secret reference for Password Depot authentication.
spec.provider.passworddepot.auth.secretRef.credentials <object>	# Username / Password is used for authentication.
spec.provider.passworddepot.auth.secretRef.credentials.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.passworddepot.auth.secretRef.credentials.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.passworddepot.auth.secretRef.credentials.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.passworddepot.database <string> (required)	# Database to use as source
spec.provider.passworddepot.host <string> (required)	# URL configures the Password Depot instance URL.
spec.provider.previder <object>	# Previder configures this store to sync secrets using the Previder provider
spec.provider.previder.auth <object> (required)	# PreviderAuth contains a secretRef for credentials.
spec.provider.previder.auth.secretRef <object>	# PreviderAuthSecretRef holds secret references for Previder Vault credentials.
spec.provider.previder.auth.secretRef.accessToken <object> (required)	# The AccessToken is used for authentication
spec.provider.previder.auth.secretRef.accessToken.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.previder.auth.secretRef.accessToken.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.previder.auth.secretRef.accessToken.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.previder.baseUri <string>
spec.provider.pulumi <object>	# Pulumi configures this store to sync secrets using the Pulumi provider
spec.provider.pulumi.accessToken <object>	# AccessToken is the access tokens to sign in to the Pulumi Cloud Console. Deprecated: Use auth.accessToken instead.
spec.provider.pulumi.accessToken.secretRef <object>	# SecretRef is a reference to a secret containing the Pulumi API token.
spec.provider.pulumi.accessToken.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.pulumi.accessToken.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.pulumi.accessToken.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.pulumi.apiUrl <string> default="https://api.pulumi.com/api/esc"	# APIURL is the URL of the Pulumi API.
spec.provider.pulumi.auth <object>	# Auth configures how the Operator authenticates with the Pulumi API. Either auth or the deprecated accessToken field must be specified.
spec.provider.pulumi.auth.accessToken <object>	# AccessToken authenticates using a Pulumi access token stored in a Kubernetes Secret.
spec.provider.pulumi.auth.accessToken.secretRef <object>	# SecretRef is a reference to a secret containing the Pulumi API token.
spec.provider.pulumi.auth.accessToken.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.pulumi.auth.accessToken.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.pulumi.auth.accessToken.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.pulumi.auth.oidcConfig <object>	# OIDCConfig authenticates using Kubernetes ServiceAccount tokens via OIDC.
spec.provider.pulumi.auth.oidcConfig.expirationSeconds <integer> default=600 format=int64 min=600	# ExpirationSeconds sets the token validity duration for service account and OIDC token. Defaults to 10 minutes.
spec.provider.pulumi.auth.oidcConfig.organization <string> (required)	# Organization is the name of the Pulumi organization configured for OIDC authentication.
spec.provider.pulumi.auth.oidcConfig.serviceAccountRef <object> (required)	# ServiceAccountRef specifies the Kubernetes ServiceAccount to use for authentication.
spec.provider.pulumi.auth.oidcConfig.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.pulumi.auth.oidcConfig.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.pulumi.auth.oidcConfig.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.pulumi.environment <string> (required)	# Environment are YAML documents composed of static key-value pairs, programmatic expressions, dynamically retrieved values from supported providers including all major clouds, and other Pulumi ESC environments. To create a new environment, visit https://www.pulumi.com/docs/esc/environments/ for more information.
spec.provider.pulumi.organization <string> (required)	# Organization are a space to collaborate on shared projects and stacks. To create a new organization, visit https://app.pulumi.com/ and click "New Organization".
spec.provider.pulumi.project <string> (required)	# Project is the name of the Pulumi ESC project the environment belongs to.
spec.provider.scaleway <object>	# Scaleway configures this store to sync secrets using the Scaleway provider.
spec.provider.scaleway.accessKey <object> (required)	# AccessKey is the non-secret part of the api key.
spec.provider.scaleway.accessKey.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.scaleway.accessKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.scaleway.accessKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.scaleway.accessKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.scaleway.accessKey.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.scaleway.apiUrl <string>	# APIURL is the url of the api to use. Defaults to https://api.scaleway.com
spec.provider.scaleway.projectId <string> (required)	# ProjectID is the id of your project, which you can find in the console: https://console.scaleway.com/project/settings
spec.provider.scaleway.region <string> (required)	# Region where your secrets are located: https://developers.scaleway.com/en/quickstart/#region-and-zone
spec.provider.scaleway.secretKey <object> (required)	# SecretKey is the non-secret part of the api key.
spec.provider.scaleway.secretKey.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.scaleway.secretKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.scaleway.secretKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.scaleway.secretKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.scaleway.secretKey.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.secretserver <object>	# SecretServer configures this store to sync secrets using SecretServer provider https://docs.delinea.com/online-help/secret-server/start.htm
spec.provider.secretserver.caBundle <string> format=byte	# PEM/base64 encoded CA bundle used to validate Secret ServerURL. Only used if the ServerURL URL is using HTTPS protocol. If not set the system root certificates are used to validate the TLS connection.
spec.provider.secretserver.caProvider <object>	# The provider for the CA bundle to use to validate Secret ServerURL certificate.
spec.provider.secretserver.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.secretserver.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.secretserver.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.secretserver.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.secretserver.domain <string>	# Domain is the secret server domain.
spec.provider.secretserver.password <object> (required)	# Password is the secret server account password.
spec.provider.secretserver.password.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.secretserver.password.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.secretserver.password.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.secretserver.password.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.secretserver.password.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.secretserver.serverURL <string> (required)	# ServerURL URL to your secret server installation
spec.provider.secretserver.username <object> (required)	# Username is the secret server account username.
spec.provider.secretserver.username.secretRef <object>	# SecretRef references a key in a secret that will be used as value.
spec.provider.secretserver.username.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.secretserver.username.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.secretserver.username.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.secretserver.username.value <string>	# Value can be specified directly to set a value without using a secret.
spec.provider.senhasegura <object>	# Senhasegura configures this store to sync secrets using senhasegura provider
spec.provider.senhasegura.auth <object> (required)	# Auth defines parameters to authenticate in senhasegura
spec.provider.senhasegura.auth.clientId <string> (required)
spec.provider.senhasegura.auth.clientSecretSecretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.senhasegura.auth.clientSecretSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.senhasegura.auth.clientSecretSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.senhasegura.auth.clientSecretSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.senhasegura.ignoreSslCertificate <boolean> default=false	# IgnoreSslCertificate defines if SSL certificate must be ignored
spec.provider.senhasegura.module <string> (required)	# Module defines which senhasegura module should be used to get secrets
spec.provider.senhasegura.url <string> (required)	# URL of senhasegura
spec.provider.vault <object>	# Vault configures this store to sync secrets using the HashiCorp Vault provider.
spec.provider.vault.auth <object>	# Auth configures how secret-manager authenticates with the Vault server.
spec.provider.vault.auth.appRole <object>	# AppRole authenticates with Vault using the App Role auth mechanism, with the role and secret stored in a Kubernetes Secret resource.
spec.provider.vault.auth.appRole.path <string> (required) default="approle"	# Path where the App Role authentication backend is mounted in Vault, e.g: "approle"
spec.provider.vault.auth.appRole.roleId <string>	# RoleID configured in the App Role authentication backend when setting up the authentication backend in Vault.
spec.provider.vault.auth.appRole.roleRef <object>	# Reference to a key in a Secret that contains the App Role ID used to authenticate with Vault. The `key` field must be specified and denotes which entry within the Secret resource is used as the app role id.
spec.provider.vault.auth.appRole.roleRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.appRole.roleRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.appRole.roleRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.appRole.secretRef <object> (required)	# Reference to a key in a Secret that contains the App Role secret used to authenticate with Vault. The `key` field must be specified and denotes which entry within the Secret resource is used as the app role secret.
spec.provider.vault.auth.appRole.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.appRole.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.appRole.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.cert <object>	# Cert authenticates with TLS Certificates by passing client certificate, private key and ca certificate Cert authentication method
spec.provider.vault.auth.cert.clientCert <object>	# ClientCert is a certificate to authenticate using the Cert Vault authentication method
spec.provider.vault.auth.cert.clientCert.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.cert.clientCert.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.cert.clientCert.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.cert.path <string> default="cert"	# Path where the Certificate authentication backend is mounted in Vault, e.g: "cert"
spec.provider.vault.auth.cert.secretRef <object>	# SecretRef to a key in a Secret resource containing client private key to authenticate with Vault using the Cert authentication method
spec.provider.vault.auth.cert.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.cert.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.cert.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.cert.vaultRole <string>	# VaultRole specifies the Vault role to use for TLS certificate authentication.
spec.provider.vault.auth.gcp <object>	# Gcp authenticates with Vault using Google Cloud Platform authentication method GCP authentication method
spec.provider.vault.auth.gcp.location <string>	# Location optionally defines a location/region for the secret
spec.provider.vault.auth.gcp.path <string> default="gcp"	# Path where the GCP auth method is enabled in Vault, e.g: "gcp"
spec.provider.vault.auth.gcp.projectID <string>	# Project ID of the Google Cloud Platform project
spec.provider.vault.auth.gcp.role <string> (required)	# Vault Role. In Vault, a role describes an identity with a set of permissions, groups, or policies you want to attach to a user of the secrets engine.
spec.provider.vault.auth.gcp.secretRef <object>	# Specify credentials in a Secret object
spec.provider.vault.auth.gcp.secretRef.secretAccessKeySecretRef <object>	# The SecretAccessKey is used for authentication
spec.provider.vault.auth.gcp.secretRef.secretAccessKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.gcp.secretRef.secretAccessKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.gcp.secretRef.secretAccessKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.gcp.serviceAccountRef <object>	# ServiceAccountRef to a service account for impersonation
spec.provider.vault.auth.gcp.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.vault.auth.gcp.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.vault.auth.gcp.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.gcp.workloadIdentity <object>	# Specify a service account with Workload Identity
spec.provider.vault.auth.gcp.workloadIdentity.clusterLocation <string>	# ClusterLocation is the location of the cluster If not specified, it fetches information from the metadata server
spec.provider.vault.auth.gcp.workloadIdentity.clusterName <string>	# ClusterName is the name of the cluster If not specified, it fetches information from the metadata server
spec.provider.vault.auth.gcp.workloadIdentity.clusterProjectID <string>	# ClusterProjectID is the project ID of the cluster If not specified, it fetches information from the metadata server
spec.provider.vault.auth.gcp.workloadIdentity.serviceAccountRef <object> (required)	# ServiceAccountSelector is a reference to a ServiceAccount resource.
spec.provider.vault.auth.gcp.workloadIdentity.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.vault.auth.gcp.workloadIdentity.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.vault.auth.gcp.workloadIdentity.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.iam <object>	# Iam authenticates with vault by passing a special AWS request signed with AWS IAM credentials AWS IAM authentication method
spec.provider.vault.auth.iam.externalID <string>	# AWS External ID set on assumed IAM roles
spec.provider.vault.auth.iam.jwt <object>	# Specify a service account with IRSA enabled
spec.provider.vault.auth.iam.jwt.serviceAccountRef <object>	# ServiceAccountSelector is a reference to a ServiceAccount resource.
spec.provider.vault.auth.iam.jwt.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.vault.auth.iam.jwt.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.vault.auth.iam.jwt.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.iam.path <string>	# Path where the AWS auth method is enabled in Vault, e.g: "aws"
spec.provider.vault.auth.iam.region <string>	# AWS region
spec.provider.vault.auth.iam.role <string>	# This is the AWS role to be assumed before talking to vault
spec.provider.vault.auth.iam.secretRef <object>	# Specify credentials in a Secret object
spec.provider.vault.auth.iam.secretRef.accessKeyIDSecretRef <object>	# The AccessKeyID is used for authentication
spec.provider.vault.auth.iam.secretRef.accessKeyIDSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.iam.secretRef.accessKeyIDSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.iam.secretRef.accessKeyIDSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.iam.secretRef.secretAccessKeySecretRef <object>	# The SecretAccessKey is used for authentication
spec.provider.vault.auth.iam.secretRef.secretAccessKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.iam.secretRef.secretAccessKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.iam.secretRef.secretAccessKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.iam.secretRef.sessionTokenSecretRef <object>	# The SessionToken used for authentication This must be defined if AccessKeyID and SecretAccessKey are temporary credentials see: https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_use-resources.html
spec.provider.vault.auth.iam.secretRef.sessionTokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.iam.secretRef.sessionTokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.iam.secretRef.sessionTokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.iam.vaultAwsIamServerID <string>	# X-Vault-AWS-IAM-Server-ID is an additional header used by Vault IAM auth method to mitigate against different types of replay attacks. More details here: https://developer.hashicorp.com/vault/docs/auth/aws
spec.provider.vault.auth.iam.vaultRole <string> (required)	# Vault Role. In vault, a role describes an identity with a set of permissions, groups, or policies you want to attach a user of the secrets engine
spec.provider.vault.auth.jwt <object>	# Jwt authenticates with Vault by passing role and JWT token using the JWT/OIDC authentication method
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken <object>	# Optional ServiceAccountToken specifies the Kubernetes service account for which to request a token for with the `TokenRequest` API.
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken.audiences <[]string>	# Optional audiences field that will be used to request a temporary Kubernetes service account token for the service account referenced by `serviceAccountRef`. Defaults to a single audience `vault` it not specified. Deprecated: use serviceAccountRef.Audiences instead
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken.expirationSeconds <integer> format=int64	# Optional expiration time in seconds that will be used to request a temporary Kubernetes service account token for the service account referenced by `serviceAccountRef`. Deprecated: this will be removed in the future. Defaults to 10 minutes.
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken.serviceAccountRef <object> (required)	# Service account field containing the name of a kubernetes ServiceAccount.
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.vault.auth.jwt.kubernetesServiceAccountToken.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.jwt.path <string> (required) default="jwt"	# Path where the JWT authentication backend is mounted in Vault, e.g: "jwt"
spec.provider.vault.auth.jwt.role <string>	# Role is a JWT role to authenticate using the JWT/OIDC Vault authentication method
spec.provider.vault.auth.jwt.secretRef <object>	# Optional SecretRef that refers to a key in a Secret resource containing JWT token to authenticate with Vault using the JWT/OIDC authentication method.
spec.provider.vault.auth.jwt.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.jwt.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.jwt.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.kubernetes <object>	# Kubernetes authenticates with Vault by passing the ServiceAccount token stored in the named Secret resource to the Vault server.
spec.provider.vault.auth.kubernetes.mountPath <string> (required) default="kubernetes"	# Path where the Kubernetes authentication backend is mounted in Vault, e.g: "kubernetes"
spec.provider.vault.auth.kubernetes.role <string> (required)	# A required field containing the Vault Role to assume. A Role binds a Kubernetes ServiceAccount with a set of Vault policies.
spec.provider.vault.auth.kubernetes.secretRef <object>	# Optional secret field containing a Kubernetes ServiceAccount JWT used for authenticating with Vault. If a name is specified without a key, `token` is the default. If one is not specified, the one bound to the controller will be used.
spec.provider.vault.auth.kubernetes.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.kubernetes.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.kubernetes.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.kubernetes.serviceAccountRef <object>	# Optional service account field containing the name of a kubernetes ServiceAccount. If the service account is specified, the service account secret token JWT will be used for authenticating with Vault. If the service account selector is not supplied, the secretRef will be used instead.
spec.provider.vault.auth.kubernetes.serviceAccountRef.audiences <[]string>	# Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
spec.provider.vault.auth.kubernetes.serviceAccountRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ServiceAccount resource being referred to.
spec.provider.vault.auth.kubernetes.serviceAccountRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.ldap <object>	# Ldap authenticates with Vault by passing username/password pair using the LDAP authentication method
spec.provider.vault.auth.ldap.path <string> (required) default="ldap"	# Path where the LDAP authentication backend is mounted in Vault, e.g: "ldap"
spec.provider.vault.auth.ldap.secretRef <object>	# SecretRef to a key in a Secret resource containing password for the LDAP user used to authenticate with Vault using the LDAP authentication method
spec.provider.vault.auth.ldap.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.ldap.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.ldap.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.ldap.username <string> (required)	# Username is an LDAP username used to authenticate using the LDAP Vault authentication method
spec.provider.vault.auth.namespace <string>	# Name of the vault namespace to authenticate to. This can be different than the namespace your secret is in. Namespaces is a set of features within Vault Enterprise that allows Vault environments to support Secure Multi-tenancy. e.g: "ns1". More about namespaces can be found here https://www.vaultproject.io/docs/enterprise/namespaces This will default to Vault.Namespace field if set, or empty otherwise
spec.provider.vault.auth.tokenSecretRef <object>	# TokenSecretRef authenticates with Vault by presenting a token.
spec.provider.vault.auth.tokenSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.tokenSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.tokenSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.userPass <object>	# UserPass authenticates with Vault by passing username/password pair
spec.provider.vault.auth.userPass.path <string> (required) default="userpass"	# Path where the UserPassword authentication backend is mounted in Vault, e.g: "userpass"
spec.provider.vault.auth.userPass.secretRef <object>	# SecretRef to a key in a Secret resource containing password for the user used to authenticate with Vault using the UserPass authentication method
spec.provider.vault.auth.userPass.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.auth.userPass.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.auth.userPass.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.auth.userPass.username <string> (required)	# Username is a username used to authenticate using the UserPass Vault authentication method
spec.provider.vault.caBundle <string> format=byte	# PEM encoded CA bundle used to validate Vault server certificate. Only used if the Server URL is using HTTPS protocol. This parameter is ignored for plain HTTP protocol connection. If not set the system root certificates are used to validate the TLS connection.
spec.provider.vault.caProvider <object>	# The provider for the CA bundle to use to validate Vault server certificate.
spec.provider.vault.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.vault.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.vault.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in. Can only be defined when used in a ClusterSecretStore.
spec.provider.vault.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.vault.checkAndSet <object>	# CheckAndSet defines the Check-And-Set (CAS) settings for PushSecret operations. Only applies to Vault KV v2 stores. When enabled, write operations must include the current version of the secret to prevent unintentional overwrites.
spec.provider.vault.checkAndSet.required <boolean>	# Required when true, all write operations must include a check-and-set parameter. This helps prevent unintentional overwrites of secrets.
spec.provider.vault.forwardInconsistent <boolean>	# ForwardInconsistent tells Vault to forward read-after-write requests to the Vault leader instead of simply retrying within a loop. This can increase performance if the option is enabled serverside. https://www.vaultproject.io/docs/configuration/replication#allow_forwarding_via_header
spec.provider.vault.headers <map[string]string>	# Headers to be added in Vault request
spec.provider.vault.namespace <string>	# Name of the vault namespace. Namespaces is a set of features within Vault Enterprise that allows Vault environments to support Secure Multi-tenancy. e.g: "ns1". More about namespaces can be found here https://www.vaultproject.io/docs/enterprise/namespaces
spec.provider.vault.path <string>	# Path is the mount path of the Vault KV backend endpoint, e.g: "secret". The v2 KV secret engine version specific "/data" path suffix for fetching secrets from Vault is optional and will be appended if not present in specified path.
spec.provider.vault.readYourWrites <boolean>	# ReadYourWrites ensures isolated read-after-write semantics by providing discovered cluster replication states in each request. More information about eventual consistency in Vault can be found here https://www.vaultproject.io/docs/enterprise/consistency
spec.provider.vault.server <string> (required)	# Server is the connection address for the Vault server, e.g: "https://vault.example.com:8200".
spec.provider.vault.tls <object>	# The configuration used for client side related TLS communication, when the Vault server requires mutual authentication. Only used if the Server URL is using HTTPS protocol. This parameter is ignored for plain HTTP protocol connection. It's worth noting this configuration is different from the "TLS certificates auth method", which is available under the `auth.cert` section.
spec.provider.vault.tls.certSecretRef <object>	# CertSecretRef is a certificate added to the transport layer when communicating with the Vault server. If no key for the Secret is specified, external-secret will default to 'tls.crt'.
spec.provider.vault.tls.certSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.tls.certSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.tls.certSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.tls.keySecretRef <object>	# KeySecretRef to a key in a Secret resource containing client private key added to the transport layer when communicating with the Vault server. If no key for the Secret is specified, external-secret will default to 'tls.key'.
spec.provider.vault.tls.keySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.vault.tls.keySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.vault.tls.keySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.vault.version <string> enum=v1|v2 default="v2"	# Version is the Vault KV secret engine version. This can be either "v1" or "v2". Version defaults to "v2".
spec.provider.volcengine <object>	# Volcengine configures this store to sync secrets using the Volcengine provider
spec.provider.volcengine.auth <object>	# Auth defines the authentication method to use. If not specified, the provider will try to use IRSA (IAM Role for Service Account).
spec.provider.volcengine.auth.secretRef <object>	# SecretRef defines the static credentials to use for authentication. If not set, IRSA is used.
spec.provider.volcengine.auth.secretRef.accessKeyID <object> (required)	# AccessKeyID is the reference to the secret containing the Access Key ID.
spec.provider.volcengine.auth.secretRef.accessKeyID.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.volcengine.auth.secretRef.accessKeyID.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.volcengine.auth.secretRef.accessKeyID.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.volcengine.auth.secretRef.secretAccessKey <object> (required)	# SecretAccessKey is the reference to the secret containing the Secret Access Key.
spec.provider.volcengine.auth.secretRef.secretAccessKey.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.volcengine.auth.secretRef.secretAccessKey.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.volcengine.auth.secretRef.secretAccessKey.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.volcengine.auth.secretRef.token <object>	# Token is the reference to the secret containing the STS(Security Token Service) Token.
spec.provider.volcengine.auth.secretRef.token.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.volcengine.auth.secretRef.token.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.volcengine.auth.secretRef.token.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.volcengine.region <string> (required)	# Region specifies the Volcengine region to connect to.
spec.provider.webhook <object>	# Webhook configures this store to sync secrets using a generic templated webhook
spec.provider.webhook.auth <object>	# Auth specifies a authorization protocol. Only one protocol may be set.
spec.provider.webhook.auth.ntlm <object>	# NTLMProtocol configures the store to use NTLM for auth
spec.provider.webhook.auth.ntlm.passwordSecret <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.webhook.auth.ntlm.passwordSecret.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.webhook.auth.ntlm.passwordSecret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.webhook.auth.ntlm.passwordSecret.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.webhook.auth.ntlm.usernameSecret <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.webhook.auth.ntlm.usernameSecret.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.webhook.auth.ntlm.usernameSecret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.webhook.auth.ntlm.usernameSecret.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.webhook.body <string>	# Body
spec.provider.webhook.caBundle <string> format=byte	# PEM encoded CA bundle used to validate webhook server certificate. Only used if the Server URL is using HTTPS protocol. This parameter is ignored for plain HTTP protocol connection. If not set the system root certificates are used to validate the TLS connection.
spec.provider.webhook.caProvider <object>	# The provider for the CA bundle to use to validate webhook server certificate.
spec.provider.webhook.caProvider.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# The key where the CA certificate can be found in the Secret or ConfigMap.
spec.provider.webhook.caProvider.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the object located at the provider type.
spec.provider.webhook.caProvider.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace the Provider type is in.
spec.provider.webhook.caProvider.type <string> (required) enum=Secret|ConfigMap	# The type of provider to use such as "Secret", or "ConfigMap".
spec.provider.webhook.headers <map[string]string>	# Headers
spec.provider.webhook.method <string>	# Webhook Method
spec.provider.webhook.result <object>	# Result formatting
spec.provider.webhook.result.jsonPath <string>	# Json path of return value
spec.provider.webhook.secrets <[]object>	# Secrets to fill in templates These secrets will be passed to the templating function as key value pairs under the given name
spec.provider.webhook.secrets[].name <string> (required)	# Name of this secret in templates
spec.provider.webhook.secrets[].secretRef <object> (required)	# Secret ref to fill in credentials
spec.provider.webhook.secrets[].secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.webhook.secrets[].secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.webhook.secrets[].secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.webhook.timeout <string>	# Timeout
spec.provider.webhook.url <string> (required)	# Webhook url to call
spec.provider.yandexcertificatemanager <object>	# YandexCertificateManager configures this store to sync secrets using Yandex Certificate Manager provider
spec.provider.yandexcertificatemanager.apiEndpoint <string>	# Yandex.Cloud API endpoint (e.g. 'api.cloud.yandex.net:443')
spec.provider.yandexcertificatemanager.auth <object> (required)	# Auth defines the information necessary to authenticate against Yandex.Cloud
spec.provider.yandexcertificatemanager.auth.authorizedKeySecretRef <object>	# The authorized key used for authentication
spec.provider.yandexcertificatemanager.auth.authorizedKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.yandexcertificatemanager.auth.authorizedKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.yandexcertificatemanager.auth.authorizedKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.yandexcertificatemanager.caProvider <object>	# The provider for the CA bundle to use to validate Yandex.Cloud server certificate.
spec.provider.yandexcertificatemanager.caProvider.certSecretRef <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.yandexcertificatemanager.caProvider.certSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.yandexcertificatemanager.caProvider.certSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.yandexcertificatemanager.caProvider.certSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.yandexcertificatemanager.fetching <object>	# FetchingPolicy configures the provider to interpret the `data.secretKey.remoteRef.key` field in ExternalSecret as certificate ID or certificate name
spec.provider.yandexcertificatemanager.fetching.byID <object (free-form)>	# ByID configures the provider to interpret the `data.secretKey.remoteRef.key` field in ExternalSecret as secret ID.
spec.provider.yandexcertificatemanager.fetching.byName <object>	# ByName configures the provider to interpret the `data.secretKey.remoteRef.key` field in ExternalSecret as secret name.
spec.provider.yandexcertificatemanager.fetching.byName.folderID <string> (required)	# The folder to fetch secrets from
spec.provider.yandexlockbox <object>	# YandexLockbox configures this store to sync secrets using Yandex Lockbox provider
spec.provider.yandexlockbox.apiEndpoint <string>	# Yandex.Cloud API endpoint (e.g. 'api.cloud.yandex.net:443')
spec.provider.yandexlockbox.auth <object> (required)	# Auth defines the information necessary to authenticate against Yandex.Cloud
spec.provider.yandexlockbox.auth.authorizedKeySecretRef <object>	# The authorized key used for authentication
spec.provider.yandexlockbox.auth.authorizedKeySecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.yandexlockbox.auth.authorizedKeySecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.yandexlockbox.auth.authorizedKeySecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.yandexlockbox.caProvider <object>	# The provider for the CA bundle to use to validate Yandex.Cloud server certificate.
spec.provider.yandexlockbox.caProvider.certSecretRef <object>	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.provider.yandexlockbox.caProvider.certSecretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.provider.yandexlockbox.caProvider.certSecretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.provider.yandexlockbox.caProvider.certSecretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.provider.yandexlockbox.fetching <object>	# FetchingPolicy configures the provider to interpret the `data.secretKey.remoteRef.key` field in ExternalSecret as secret ID or secret name
spec.provider.yandexlockbox.fetching.byID <object (free-form)>	# ByID configures the provider to interpret the `data.secretKey.remoteRef.key` field in ExternalSecret as secret ID.
spec.provider.yandexlockbox.fetching.byName <object>	# ByName configures the provider to interpret the `data.secretKey.remoteRef.key` field in ExternalSecret as secret name.
spec.provider.yandexlockbox.fetching.byName.folderID <string> (required)	# The folder to fetch secrets from
spec.refreshInterval <integer>	# Used to configure store refresh interval in seconds. Empty or 0 will default to the controller config.
spec.retrySettings <object>	# Used to configure HTTP retries on failures.
spec.retrySettings.maxRetries <integer> format=int32
spec.retrySettings.retryInterval <string>
status <object>	# SecretStoreStatus defines the observed state of the SecretStore.
status.capabilities <string>	# SecretStoreCapabilities defines the possible operations a SecretStore can do.
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> format=date-time
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string> (required)
status.conditions[].type <string> (required)	# SecretStoreConditionType represents the condition of the SecretStore.
