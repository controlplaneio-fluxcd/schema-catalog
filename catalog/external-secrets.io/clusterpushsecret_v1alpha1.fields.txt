# schema source: External Secrets v2.7.0
apiVersion <string> enum=external-secrets.io/v1alpha1
kind <string> enum=ClusterPushSecret (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ClusterPushSecretSpec defines the configuration for a ClusterPushSecret resource.
spec.namespaceSelectors <[]object>	# A list of labels to select by to find the Namespaces to create the ExternalSecrets in. The selectors are ORed.
spec.namespaceSelectors[].matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.namespaceSelectors[].matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.namespaceSelectors[].matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.namespaceSelectors[].matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.namespaceSelectors[].matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.pushSecretMetadata <object>	# The metadata of the external secrets to be created
spec.pushSecretMetadata.annotations <map[string]string>
spec.pushSecretMetadata.labels <map[string]string>
spec.pushSecretName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the push secrets to be created. Defaults to the name of the ClusterPushSecret
spec.pushSecretSpec <object> (required)	# PushSecretSpec defines what to do with the secrets.
spec.pushSecretSpec.data <[]object>	# Secret Data that should be pushed to providers
spec.pushSecretSpec.data[].conversionStrategy <string> enum=None|ReverseUnicode default="None"	# Used to define a conversion Strategy for the secret keys
spec.pushSecretSpec.data[].match <object> (required)	# Match a given Secret Key to be pushed to the provider.
spec.pushSecretSpec.data[].match.remoteRef <object> (required)	# Remote Refs to push to providers.
spec.pushSecretSpec.data[].match.remoteRef.property <string>	# Name of the property in the resulting secret
spec.pushSecretSpec.data[].match.remoteRef.remoteKey <string> (required)	# Name of the resulting provider secret.
spec.pushSecretSpec.data[].match.secretKey <string>	# Secret Key to be pushed
spec.pushSecretSpec.data[].metadata <any>	# Metadata is metadata attached to the secret. The structure of metadata is provider specific, please look it up in the provider documentation.
spec.pushSecretSpec.dataTo <[]object>	# DataTo defines bulk push rules that expand source Secret keys into provider entries.
spec.pushSecretSpec.dataTo[].conversionStrategy <string> enum=None|ReverseUnicode default="None"	# Used to define a conversion Strategy for the secret keys
spec.pushSecretSpec.dataTo[].match <object>	# Match pattern for selecting keys from the source Secret. If not specified, all keys are selected.
spec.pushSecretSpec.dataTo[].match.regexp <string>	# Regexp matches keys by regular expression. If not specified, all keys are matched.
spec.pushSecretSpec.dataTo[].metadata <any>	# Metadata is metadata attached to the secret. The structure of metadata is provider specific, please look it up in the provider documentation.
spec.pushSecretSpec.dataTo[].remoteKey <string>	# RemoteKey is the name of the single provider secret that will receive ALL matched keys bundled as a JSON object (e.g. {"DB_HOST":"...","DB_USER":"..."}). When set, per-key expansion is skipped and a single push is performed. The provider's store prefix (if any) is still prepended to this value. When not set, each matched key is pushed as its own individual provider secret.
spec.pushSecretSpec.dataTo[].rewrite <[]object>	# Rewrite operations to transform keys before pushing to the provider. Operations are applied sequentially.
spec.pushSecretSpec.dataTo[].rewrite[].regexp <object>	# Used to rewrite with regular expressions.
spec.pushSecretSpec.dataTo[].rewrite[].regexp.source <string> (required)	# Used to define the regular expression of a re.Compiler.
spec.pushSecretSpec.dataTo[].rewrite[].regexp.target <string> (required)	# Used to define the target pattern of a ReplaceAll operation.
spec.pushSecretSpec.dataTo[].rewrite[].transform <object>	# Used to apply string transformation on the secrets.
spec.pushSecretSpec.dataTo[].rewrite[].transform.template <string> (required)	# Used to define the template to apply on the secret name. `.value ` will specify the secret name in the template.
spec.pushSecretSpec.dataTo[].storeRef <object>	# StoreRef specifies which SecretStore to push to. Required.
spec.pushSecretSpec.dataTo[].storeRef.kind <string> enum=SecretStore|ClusterSecretStore default="SecretStore"	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
spec.pushSecretSpec.dataTo[].storeRef.labelSelector <object>	# Optionally, sync to secret stores with label selector
spec.pushSecretSpec.dataTo[].storeRef.labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.pushSecretSpec.dataTo[].storeRef.labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.pushSecretSpec.dataTo[].storeRef.labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.pushSecretSpec.dataTo[].storeRef.labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.pushSecretSpec.dataTo[].storeRef.labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.pushSecretSpec.dataTo[].storeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Optionally, sync to the SecretStore of the given name
spec.pushSecretSpec.deletionPolicy <string> enum=Delete|None default="None"	# Deletion Policy to handle Secrets in the provider.
spec.pushSecretSpec.refreshInterval <string> default="1h0m0s"	# The Interval to which External Secrets will try to push a secret definition
spec.pushSecretSpec.secretStoreRefs <[]object> (required)
spec.pushSecretSpec.secretStoreRefs[].kind <string> enum=SecretStore|ClusterSecretStore default="SecretStore"	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
spec.pushSecretSpec.secretStoreRefs[].labelSelector <object>	# Optionally, sync to secret stores with label selector
spec.pushSecretSpec.secretStoreRefs[].labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.pushSecretSpec.secretStoreRefs[].labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.pushSecretSpec.secretStoreRefs[].labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.pushSecretSpec.secretStoreRefs[].labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.pushSecretSpec.secretStoreRefs[].labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.pushSecretSpec.secretStoreRefs[].name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Optionally, sync to the SecretStore of the given name
spec.pushSecretSpec.selector <object> (required)	# The Secret Selector (k8s source) for the Push Secret
spec.pushSecretSpec.selector.generatorRef <object>	# Point to a generator to create a Secret.
spec.pushSecretSpec.selector.generatorRef.apiVersion <string> default="generators.external-secrets.io/v1alpha1"	# Specify the apiVersion of the generator resource
spec.pushSecretSpec.selector.generatorRef.kind <string> (required) enum=ACRAccessToken|BeyondtrustWorkloadCredentialsDynamicSecret|ClusterGenerator|CloudsmithAccessToken|ECRAuthorizationToken|Fake|GCRAccessToken|GithubAccessToken|QuayAccessToken|Password|SSHKey|STSSessionToken|UUID|VaultDynamicSecret|Webhook|Grafana|MFA	# Specify the Kind of the generator resource
spec.pushSecretSpec.selector.generatorRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Specify the name of the generator resource
spec.pushSecretSpec.selector.secret <object>	# Select a Secret to Push.
spec.pushSecretSpec.selector.secret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the Secret. The Secret must exist in the same namespace as the PushSecret manifest.
spec.pushSecretSpec.selector.secret.selector <object>	# Selector chooses secrets using a labelSelector.
spec.pushSecretSpec.selector.secret.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.pushSecretSpec.selector.secret.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.pushSecretSpec.selector.secret.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.pushSecretSpec.selector.secret.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.pushSecretSpec.selector.secret.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.pushSecretSpec.template <object>	# Template defines a blueprint for the created Secret resource.
spec.pushSecretSpec.template.data <map[string]string>
spec.pushSecretSpec.template.engineVersion <string> enum=v2 default="v2"	# EngineVersion specifies the template engine version that should be used to compile/execute the template specified in .data and .templateFrom[].
spec.pushSecretSpec.template.mergePolicy <string> enum=Replace|Merge default="Replace"	# TemplateMergePolicy defines how the rendered template should be merged with the existing Secret data.
spec.pushSecretSpec.template.metadata <object>	# ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
spec.pushSecretSpec.template.metadata.annotations <map[string]string>
spec.pushSecretSpec.template.metadata.finalizers <[]string>
spec.pushSecretSpec.template.metadata.labels <map[string]string>
spec.pushSecretSpec.template.templateFrom <[]object>
spec.pushSecretSpec.template.templateFrom[].configMap <object>	# TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
spec.pushSecretSpec.template.templateFrom[].configMap.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.pushSecretSpec.template.templateFrom[].configMap.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.pushSecretSpec.template.templateFrom[].configMap.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope specifies how the template keys should be interpreted.
spec.pushSecretSpec.template.templateFrom[].configMap.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.pushSecretSpec.template.templateFrom[].literal <string>
spec.pushSecretSpec.template.templateFrom[].secret <object>	# TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
spec.pushSecretSpec.template.templateFrom[].secret.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.pushSecretSpec.template.templateFrom[].secret.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.pushSecretSpec.template.templateFrom[].secret.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope specifies how the template keys should be interpreted.
spec.pushSecretSpec.template.templateFrom[].secret.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.pushSecretSpec.template.templateFrom[].target <string> default="Data"	# Target specifies where to place the template result. For Secret resources, common values are: "Data", "Annotations", "Labels". For custom resources (when spec.target.manifest is set), this supports nested paths like "spec.database.config" or "data".
spec.pushSecretSpec.template.templateFrom[].valuesDecodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy for the rendered template values.
spec.pushSecretSpec.template.type <string>
spec.pushSecretSpec.updatePolicy <string> enum=Replace|IfNotExists default="Replace"	# UpdatePolicy to handle Secrets in the provider.
spec.refreshTime <string>	# The time in which the controller should reconcile its objects and recheck namespaces for labels.
status <object>	# ClusterPushSecretStatus contains the status information for the ClusterPushSecret resource.
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> format=date-time
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string> (required)
status.conditions[].type <string> (required)	# PushSecretConditionType indicates the condition of the PushSecret.
status.failedNamespaces <[]object>	# Failed namespaces are the namespaces that failed to apply an PushSecret
status.failedNamespaces[].namespace <string> (required)	# Namespace is the namespace that failed when trying to apply an PushSecret
status.failedNamespaces[].reason <string>	# Reason is why the PushSecret failed to apply to the namespace
status.provisionedNamespaces <[]string>	# ProvisionedNamespaces are the namespaces where the ClusterPushSecret has secrets
status.pushSecretName <string>
