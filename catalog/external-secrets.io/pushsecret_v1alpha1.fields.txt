# schema source: External Secrets v2.7.0 https://github.com/external-secrets/external-secrets
apiVersion <string> enum=external-secrets.io/v1alpha1
kind <string> enum=PushSecret
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# PushSecretSpec configures the behavior of the PushSecret.
spec.data <[]object>	# Secret Data that should be pushed to providers
spec.data[].conversionStrategy <string> enum=None|ReverseUnicode default="None"	# Used to define a conversion Strategy for the secret keys
spec.data[].match <object> (required)	# Match a given Secret Key to be pushed to the provider.
spec.data[].match.remoteRef <object> (required)	# Remote Refs to push to providers.
spec.data[].match.remoteRef.property <string>	# Name of the property in the resulting secret
spec.data[].match.remoteRef.remoteKey <string> (required)	# Name of the resulting provider secret.
spec.data[].match.secretKey <string>	# Secret Key to be pushed
spec.data[].metadata <any>	# Metadata is metadata attached to the secret. The structure of metadata is provider specific, please look it up in the provider documentation.
spec.dataTo <[]object>	# DataTo defines bulk push rules that expand source Secret keys into provider entries.
spec.dataTo[].conversionStrategy <string> enum=None|ReverseUnicode default="None"	# Used to define a conversion Strategy for the secret keys
spec.dataTo[].match <object>	# Match pattern for selecting keys from the source Secret. If not specified, all keys are selected.
spec.dataTo[].match.regexp <string>	# Regexp matches keys by regular expression. If not specified, all keys are matched.
spec.dataTo[].metadata <any>	# Metadata is metadata attached to the secret. The structure of metadata is provider specific, please look it up in the provider documentation.
spec.dataTo[].remoteKey <string>	# RemoteKey is the name of the single provider secret that will receive ALL matched keys bundled as a JSON object (e.g. {"DB_HOST":"...","DB_USER":"..."}). When set, per-key expansion is skipped and a single push is performed. The provider's store prefix (if any) is still prepended to this value. When not set, each matched key is pushed as its own individual provider secret.
spec.dataTo[].rewrite <[]object>	# Rewrite operations to transform keys before pushing to the provider. Operations are applied sequentially.
spec.dataTo[].rewrite[].regexp <object>	# Used to rewrite with regular expressions.
spec.dataTo[].rewrite[].regexp.source <string> (required)	# Used to define the regular expression of a re.Compiler.
spec.dataTo[].rewrite[].regexp.target <string> (required)	# Used to define the target pattern of a ReplaceAll operation.
spec.dataTo[].rewrite[].transform <object>	# Used to apply string transformation on the secrets.
spec.dataTo[].rewrite[].transform.template <string> (required)	# Used to define the template to apply on the secret name. `.value ` will specify the secret name in the template.
spec.dataTo[].storeRef <object>	# StoreRef specifies which SecretStore to push to. Required.
spec.dataTo[].storeRef.kind <string> enum=SecretStore|ClusterSecretStore default="SecretStore"	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
spec.dataTo[].storeRef.labelSelector <object>	# Optionally, sync to secret stores with label selector
spec.dataTo[].storeRef.labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.dataTo[].storeRef.labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.dataTo[].storeRef.labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.dataTo[].storeRef.labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.dataTo[].storeRef.labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.dataTo[].storeRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Optionally, sync to the SecretStore of the given name
spec.deletionPolicy <string> enum=Delete|None default="None"	# Deletion Policy to handle Secrets in the provider.
spec.refreshInterval <string> default="1h0m0s"	# The Interval to which External Secrets will try to push a secret definition
spec.secretStoreRefs <[]object> (required)
spec.secretStoreRefs[].kind <string> enum=SecretStore|ClusterSecretStore default="SecretStore"	# Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
spec.secretStoreRefs[].labelSelector <object>	# Optionally, sync to secret stores with label selector
spec.secretStoreRefs[].labelSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.secretStoreRefs[].labelSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.secretStoreRefs[].labelSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.secretStoreRefs[].labelSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.secretStoreRefs[].labelSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.secretStoreRefs[].name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Optionally, sync to the SecretStore of the given name
spec.selector <object> (required)	# The Secret Selector (k8s source) for the Push Secret
spec.selector.generatorRef <object>	# Point to a generator to create a Secret.
spec.selector.generatorRef.apiVersion <string> default="generators.external-secrets.io/v1alpha1"	# Specify the apiVersion of the generator resource
spec.selector.generatorRef.kind <string> (required) enum=ACRAccessToken|BeyondtrustWorkloadCredentialsDynamicSecret|ClusterGenerator|CloudsmithAccessToken|ECRAuthorizationToken|Fake|GCRAccessToken|GithubAccessToken|QuayAccessToken|Password|SSHKey|STSSessionToken|UUID|VaultDynamicSecret|Webhook|Grafana|MFA	# Specify the Kind of the generator resource
spec.selector.generatorRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Specify the name of the generator resource
spec.selector.secret <object>	# Select a Secret to Push.
spec.selector.secret.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of the Secret. The Secret must exist in the same namespace as the PushSecret manifest.
spec.selector.secret.selector <object>	# Selector chooses secrets using a labelSelector.
spec.selector.secret.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.selector.secret.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.selector.secret.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.selector.secret.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.selector.secret.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.template <object>	# Template defines a blueprint for the created Secret resource.
spec.template.data <map[string]string>
spec.template.engineVersion <string> enum=v2 default="v2"	# EngineVersion specifies the template engine version that should be used to compile/execute the template specified in .data and .templateFrom[].
spec.template.mergePolicy <string> enum=Replace|Merge default="Replace"	# TemplateMergePolicy defines how the rendered template should be merged with the existing Secret data.
spec.template.metadata <object>	# ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
spec.template.metadata.annotations <map[string]string>
spec.template.metadata.finalizers <[]string>
spec.template.metadata.labels <map[string]string>
spec.template.templateFrom <[]object>
spec.template.templateFrom[].configMap <object>	# TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
spec.template.templateFrom[].configMap.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.template.templateFrom[].configMap.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.template.templateFrom[].configMap.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope specifies how the template keys should be interpreted.
spec.template.templateFrom[].configMap.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.template.templateFrom[].literal <string>
spec.template.templateFrom[].secret <object>	# TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
spec.template.templateFrom[].secret.items <[]object> (required)	# A list of keys in the ConfigMap/Secret to use as templates for Secret data
spec.template.templateFrom[].secret.items[].key <string> (required) pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the ConfigMap/Secret
spec.template.templateFrom[].secret.items[].templateAs <string> enum=Values|KeysAndValues default="Values"	# TemplateScope specifies how the template keys should be interpreted.
spec.template.templateFrom[].secret.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the ConfigMap/Secret resource
spec.template.templateFrom[].target <string> default="Data"	# Target specifies where to place the template result. For Secret resources, common values are: "Data", "Annotations", "Labels". For custom resources (when spec.target.manifest is set), this supports nested paths like "spec.database.config" or "data".
spec.template.templateFrom[].valuesDecodingStrategy <string> enum=Auto|Base64|Base64URL|None default="None"	# Used to define a decoding Strategy for the rendered template values.
spec.template.type <string>
spec.updatePolicy <string> enum=Replace|IfNotExists default="Replace"	# UpdatePolicy to handle Secrets in the provider.
status <object>	# PushSecretStatus indicates the history of the status of PushSecret.
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> format=date-time
status.conditions[].message <string>
status.conditions[].reason <string>
status.conditions[].status <string> (required)
status.conditions[].type <string> (required)	# PushSecretConditionType indicates the condition of the PushSecret.
status.refreshTime <string> format=date-time	# refreshTime is the time and date the external secret was fetched and the target secret updated
status.syncedPushSecrets <map[string]object>	# Synced PushSecrets, including secrets that already exist in provider. Matches secret stores to PushSecretData that was stored to that secret store.
status.syncedResourceVersion <string>	# SyncedResourceVersion keeps track of the last synced version.
