# schema source: AWS ElastiCache Controller v1.5.2 https://github.com/aws-controllers-k8s/elasticache-controller
apiVersion <string> enum=elasticache.services.k8s.aws/v1alpha1
kind <string> enum=ServerlessCache
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ServerlessCacheSpec defines the desired state of ServerlessCache. The resource representing a serverless cache.
spec.cacheUsageLimits <object>	# Sets the cache usage limits for storage and ElastiCache Processing Units for the cache.
spec.cacheUsageLimits.dataStorage <object>	# The data storage limit.
spec.cacheUsageLimits.dataStorage.maximum <integer> format=int64
spec.cacheUsageLimits.dataStorage.minimum <integer> format=int64
spec.cacheUsageLimits.dataStorage.unit <string>
spec.cacheUsageLimits.eCPUPerSecond <object>	# The configuration for the number of ElastiCache Processing Units (ECPU) the cache can consume per second.
spec.cacheUsageLimits.eCPUPerSecond.maximum <integer> format=int64
spec.cacheUsageLimits.eCPUPerSecond.minimum <integer> format=int64
spec.dailySnapshotTime <string>	# The daily time that snapshots will be created from the new serverless cache. By default this number is populated with 0, i.e. no snapshots will be created on an automatic daily basis. Available for Valkey, Redis OSS and Serverless Memcached only.
spec.description <string>	# User-provided description for the serverless cache. The default is NULL, i.e. if no description is provided then an empty string will be returned. The maximum length is 255 characters.
spec.engine <string> (required)	# The name of the cache engine to be used for creating the serverless cache.
spec.kmsKeyID <string>	# ARN of the customer managed key for encrypting the data at rest. If no KMS key is provided, a default service key is used.
spec.majorEngineVersion <string>	# The version of the cache engine that will be used to create the serverless cache.
spec.securityGroupIDs <[]string>	# A list of the one or more VPC security groups to be associated with the serverless cache. The security group will authorize traffic access for the VPC end-point (private-link). If no other information is given this will be the VPC’s Default Security Group that is associated with the cluster VPC end-point.
spec.securityGroupRefs <[]object>
spec.securityGroupRefs[].from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.securityGroupRefs[].from.name <string>
spec.securityGroupRefs[].from.namespace <string>
spec.serverlessCacheName <string> (required)	# User-provided identifier for the serverless cache. This parameter is stored as a lowercase string.
spec.snapshotARNsToRestore <[]string>	# The ARN(s) of the snapshot that the new serverless cache will be created from. Available for Valkey, Redis OSS and Serverless Memcached only.
spec.snapshotRetentionLimit <integer> format=int64	# The number of snapshots that will be retained for the serverless cache that is being created. As new snapshots beyond this limit are added, the oldest snapshots will be deleted on a rolling basis. Available for Valkey, Redis OSS and Serverless Memcached only.
spec.subnetIDs <[]string>	# A list of the identifiers of the subnets where the VPC endpoint for the serverless cache will be deployed. All the subnetIds must belong to the same VPC.
spec.subnetRefs <[]object>
spec.subnetRefs[].from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.subnetRefs[].from.name <string>
spec.subnetRefs[].from.namespace <string>
spec.tags <[]object>	# The list of tags (key, value) pairs to be added to the serverless cache resource. Default is NULL.
spec.tags[].key <string>
spec.tags[].value <string>
spec.userGroupID <string>	# The identifier of the UserGroup to be associated with the serverless cache. Available for Valkey and Redis OSS only. Default is NULL.
status <object>	# ServerlessCacheStatus defines the observed state of ServerlessCache
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.createTime <string> format=date-time	# When the serverless cache was created.
status.endpoint <object>	# Represents the information required for client programs to connect to a cache node. This value is read-only.
status.endpoint.address <string>
status.endpoint.port <integer> format=int64
status.fullEngineVersion <string>	# The name and version number of the engine the serverless cache is compatible with.
status.readerEndpoint <object>	# Represents the information required for client programs to connect to a cache node. This value is read-only.
status.readerEndpoint.address <string>
status.readerEndpoint.port <integer> format=int64
status.status <string>	# The current status of the serverless cache. The allowed values are CREATING, AVAILABLE, DELETING, CREATE-FAILED and MODIFYING.
