# schema source: AWS ElastiCache Controller v1.5.2 https://github.com/aws-controllers-k8s/elasticache-controller
apiVersion <string> enum=elasticache.services.k8s.aws/v1alpha1
kind <string> enum=User
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.accessString <string> (required)	# Access permissions string used for this user. Regex Pattern: `\S`
spec.authenticationMode <object>	# Specifies how to authenticate the user.
spec.authenticationMode.passwords <[]object>
spec.authenticationMode.passwords[].key <string> (required)	# Key is the key within the secret
spec.authenticationMode.passwords[].name <string>	# name is unique within a namespace to reference a secret resource.
spec.authenticationMode.passwords[].namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.authenticationMode.type <string>
spec.engine <string> (required)	# The options are valkey or redis. Regex Pattern: `^[a-zA-Z]*$`
spec.noPasswordRequired <boolean>	# Indicates a password is not required for this user.
spec.passwords <[]object>	# Passwords used for this user. You can create up to two passwords for each user.
spec.passwords[].key <string> (required)	# Key is the key within the secret
spec.passwords[].name <string>	# name is unique within a namespace to reference a secret resource.
spec.passwords[].namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.tags <[]object>	# A list of tags to be added to this resource. A tag is a key-value pair. A tag key must be accompanied by a tag value, although null is accepted.
spec.tags[].key <string>
spec.tags[].value <string>
spec.userID <string> (required)	# The ID of the user. Regex Pattern: `^[a-zA-Z][a-zA-Z0-9\-]*$`
spec.userName <string> (required)	# The username of the user.
status <object>	# UserStatus defines the observed state of User
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.authentication <object>	# Denotes whether the user requires a password to authenticate.
status.authentication.passwordCount <integer> format=int64
status.authentication.type_ <string>
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.expandedAccessString <string>	# Access permissions string used for this user.
status.lastRequestedAccessString <string>	# Access permissions string used for this user.
status.minimumEngineVersion <string>	# The minimum engine version required, which is Redis OSS 6.0
status.status <string>	# Indicates the user status. Can be "active", "modifying" or "deleting".
status.userGroupIDs <[]string>	# Returns a list of the user group IDs the user belongs to.
