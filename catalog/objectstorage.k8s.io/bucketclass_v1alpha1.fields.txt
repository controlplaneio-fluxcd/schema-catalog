# schema source: Container Object Storage Interface v0.2.2 https://github.com/kubernetes-sigs/container-object-storage-interface
apiVersion <string> enum=objectstorage.k8s.io/v1alpha1
kind <string> enum=BucketClass (cluster-scoped)
metadata.name <string> (required)
deletionPolicy <string> (required) default="Retain"	# DeletionPolicy is used to specify how COSI should handle deletion of this bucket. There are 2 possible values: - Retain: Indicates that the bucket should not be deleted from the OSP - Delete: Indicates that the bucket should be deleted from the OSP once all the workloads accessing this bucket are done
driverName <string> (required)	# DriverName is the name of driver associated with this bucket
parameters <map[string]string>	# Parameters is an opaque map for passing in configuration to a driver for creating the bucket
