# schema source: Container Object Storage Interface v0.2.2 https://github.com/kubernetes-sigs/container-object-storage-interface
apiVersion <string> enum=objectstorage.k8s.io/v1alpha1
kind <string> enum=BucketAccessClass (cluster-scoped)
metadata.name <string> (required)
authenticationType <string> (required)	# AuthenticationType denotes the style of authentication It can be one of Key - access, secret tokens based authentication IAM - implicit authentication of pods to the OSP based on service account mappings
driverName <string> (required)	# DriverName is the name of driver associated with this BucketAccess
parameters <map[string]string>	# Parameters is an opaque map for passing in configuration to a driver for granting access to a bucket
