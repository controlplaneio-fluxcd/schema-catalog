# schema source: Velero v1.18.2 https://github.com/vmware-tanzu/velero
apiVersion <string> enum=velero.io/v1
kind <string> enum=BackupRepository
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BackupRepositorySpec is the specification for a BackupRepository.
spec.backupStorageLocation <string> (required)	# BackupStorageLocation is the name of the BackupStorageLocation that should contain this repository.
spec.maintenanceFrequency <string> (required)	# MaintenanceFrequency is how often maintenance should be run.
spec.repositoryConfig <map[string]string>	# RepositoryConfig is for repository-specific configuration fields.
spec.repositoryType <string> enum=kopia|restic|	# RepositoryType indicates the type of the backend repository
spec.resticIdentifier <string>	# ResticIdentifier is the full restic-compatible string for identifying this repository. This field is only used when RepositoryType is "restic".
spec.volumeNamespace <string> (required)	# VolumeNamespace is the namespace this backup repository contains pod volume backups for.
status <object>	# BackupRepositoryStatus is the current status of a BackupRepository.
status.lastMaintenanceTime <string> format=date-time	# LastMaintenanceTime is the last time repo maintenance succeeded.
status.message <string>	# Message is a message about the current status of the BackupRepository.
status.phase <string> enum=New|Ready|NotReady	# Phase is the current state of the BackupRepository.
status.recentMaintenance <[]object>	# RecentMaintenance is status of the recent repo maintenance.
status.recentMaintenance[].completeTimestamp <string> format=date-time	# CompleteTimestamp is the completion time of the repo maintenance.
status.recentMaintenance[].message <string>	# Message is a message about the current status of the repo maintenance.
status.recentMaintenance[].result <string> enum=Succeeded|Failed	# Result is the result of the repo maintenance.
status.recentMaintenance[].startTimestamp <string> format=date-time	# StartTimestamp is the start time of the repo maintenance.
