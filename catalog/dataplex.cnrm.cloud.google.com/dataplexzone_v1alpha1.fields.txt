# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=dataplex.cnrm.cloud.google.com/v1alpha1
kind <string> enum=DataplexZone
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# DataplexZoneSpec defines the desired state of DataplexZone
spec.description <string>	# Optional. Description of the zone.
spec.discoverySpec <object>	# Optional. Specification of the discovery feature applied to data in this zone.
spec.discoverySpec.csvOptions <object>	# Optional. Configuration for CSV data.
spec.discoverySpec.csvOptions.delimiter <string>	# Optional. The delimiter being used to separate values. This defaults to ','.
spec.discoverySpec.csvOptions.disableTypeInference <boolean>	# Optional. Whether to disable the inference of data type for CSV data. If true, all columns will be registered as strings.
spec.discoverySpec.csvOptions.encoding <string>	# Optional. The character encoding of the data. The default is UTF-8.
spec.discoverySpec.csvOptions.headerRows <integer> format=int32	# Optional. The number of rows to interpret as header rows that should be skipped when reading data rows.
spec.discoverySpec.enabled <boolean>	# Required. Whether discovery is enabled.
spec.discoverySpec.excludePatterns <[]string>	# Optional. The list of patterns to apply for selecting data to exclude during discovery. For Cloud Storage bucket assets, these are interpreted as glob patterns used to match object names. For BigQuery dataset assets, these are interpreted as patterns to match table names.
spec.discoverySpec.includePatterns <[]string>	# Optional. The list of patterns to apply for selecting data to include during discovery if only a subset of the data should considered. For Cloud Storage bucket assets, these are interpreted as glob patterns used to match object names. For BigQuery dataset assets, these are interpreted as patterns to match table names.
spec.discoverySpec.jsonOptions <object>	# Optional. Configuration for Json data.
spec.discoverySpec.jsonOptions.disableTypeInference <boolean>	# Optional. Whether to disable the inference of data type for Json data. If true, all columns will be registered as their primitive types (strings, number or boolean).
spec.discoverySpec.jsonOptions.encoding <string>	# Optional. The character encoding of the data. The default is UTF-8.
spec.discoverySpec.schedule <string>	# Optional. Cron schedule (https://en.wikipedia.org/wiki/Cron) for running discovery periodically. Successive discovery runs must be scheduled at least 60 minutes apart. The default value is to run discovery every 60 minutes. To explicitly set a timezone to the cron tab, apply a prefix in the cron tab: "CRON_TZ=${IANA_TIME_ZONE}" or TZ=${IANA_TIME_ZONE}". The ${IANA_TIME_ZONE} may only be a valid string from IANA time zone database. For example, `CRON_TZ=America/New_York 1 * * * *`, or `TZ=America/New_York 1 * * * *`.
spec.displayName <string>	# Optional. User friendly display name.
spec.lakeRef <object> (required)	# Reference to the parent DataplexLake that owns this Zone.
spec.lakeRef.external <string>	# A reference to an externally managed DataplexLake resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/lakes/{{lakeID}}".
spec.lakeRef.name <string>	# The name of a DataplexLake resource.
spec.lakeRef.namespace <string>	# The namespace of a DataplexLake resource.
spec.resourceID <string>	# The DataplexZone name. If not given, the metadata.name will be used.
spec.resourceSpec <object> (required)	# Required. Specification of the resources that are referenced by the assets within this zone.
spec.resourceSpec.locationType <string>	# Required. Immutable. The location type of the resources that are allowed to be attached to the assets within this zone.
spec.type <string> (required)	# Required. Immutable. The type of the zone.
status <object>	# DataplexZoneStatus defines the config connector machine state of DataplexZone
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the DataplexZone resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.assetStatus <object>	# Output only. Aggregated status of the underlying assets of the zone.
status.observedState.assetStatus.activeAssets <integer> format=int32	# Number of active assets.
status.observedState.assetStatus.securityPolicyApplyingAssets <integer> format=int32	# Number of assets that are in process of updating the security policy on attached resources.
status.observedState.assetStatus.updateTime <string>	# Last update time of the status.
status.observedState.createTime <string>	# Output only. The time when the zone was created.
status.observedState.state <string>	# Output only. Current state of the zone.
status.observedState.uid <string>	# Output only. System generated globally unique ID for the zone. This ID will be different if the zone is deleted and re-created with the same name.
status.observedState.updateTime <string>	# Output only. The time when the zone was last updated.
