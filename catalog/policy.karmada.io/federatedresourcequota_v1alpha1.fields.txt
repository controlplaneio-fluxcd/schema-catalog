# schema source: Karmada v1.18.1 https://github.com/karmada-io/karmada
apiVersion <string> enum=policy.karmada.io/v1alpha1
kind <string> enum=FederatedResourceQuota
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Spec defines the desired quota.
spec.overall <object (free-form)> (required)	# Overall is the set of desired hard limits for each named resource.
spec.staticAssignments <[]object>	# StaticAssignments specifies ResourceQuota settings for specific clusters. If non-empty, Karmada will create ResourceQuotas in the corresponding clusters. Clusters not listed here or when StaticAssignments is empty will have no ResourceQuotas created. This field addresses multi-cluster configuration management challenges by allowing centralized control over ResourceQuotas across clusters. Note: The Karmada scheduler currently does NOT use this configuration for scheduling decisions. Future updates may integrate it into the scheduling logic.
spec.staticAssignments[].clusterName <string> (required)	# ClusterName is the name of the cluster the limits enforce to.
spec.staticAssignments[].hard <object (free-form)> (required)	# Hard is the set of desired hard limits for each named resource.
status <object>	# Status defines the actual enforced quota and its current usage.
status.aggregatedStatus <[]object>	# AggregatedStatus is the observed quota usage of each cluster.
status.aggregatedStatus[].clusterName <string> (required)	# ClusterName is the name of the cluster the limits enforce to.
status.aggregatedStatus[].hard <object (free-form)>	# Hard is the set of enforced hard limits for each named resource. More info: https://kubernetes.io/docs/concepts/policy/resource-quotas/
status.aggregatedStatus[].used <object (free-form)>	# Used is the current observed total usage of the resource in the namespace.
status.overall <object (free-form)>	# Overall is the set of enforced hard limits for each named resource.
status.overallUsed <object (free-form)>	# OverallUsed is the current observed total usage of the resource in the namespace.
