# schema source: OpenShift v4.20
apiVersion <string> enum=image.openshift.io/v1
kind <string> enum=ImageSignature
metadata.name <string> (required)
metadata.namespace <string>
conditions <[]object>	# conditions represent the latest available observations of a signature's current state.
conditions[].lastProbeTime <string> format=date-time	# Last time the condition was checked.
conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transit from one status to another.
conditions[].message <string>	# Human readable message indicating details about last transition.
conditions[].reason <string>	# (brief) reason for the condition's last transition.
conditions[].status <string> (required) default=""	# status of the condition, one of True, False, Unknown.
conditions[].type <string> (required) default=""	# type of signature condition, Complete or Failed.
content <string> (required) format=byte	# Required: An opaque binary string which is an image's signature.
created <string> format=date-time	# If specified, it is the time of signature's creation.
imageIdentity <string>	# A human readable string representing image's identity. It could be a product name and version, or an image pull spec (e.g. "registry.access.redhat.com/rhel7/rhel:7.2").
issuedBy <object>	# If specified, it holds information about an issuer of signing certificate or key (a person or entity who signed the signing certificate or key).
issuedBy.commonName <string>	# Common name (e.g. openshift-signing-service).
issuedBy.organization <string>	# organization name.
issuedTo <object>	# If specified, it holds information about a subject of signing certificate or key (a person or entity who signed the image).
issuedTo.commonName <string>	# Common name (e.g. openshift-signing-service).
issuedTo.organization <string>	# organization name.
issuedTo.publicKeyID <string> (required) default=""	# If present, it is a human readable key id of public key belonging to the subject used to verify image signature. It should contain at least 64 lowest bits of public key's fingerprint (e.g. 0x685ebe62bf278440).
signedClaims <map[string]string>	# Contains claims from the signature.
type <string> (required) default=""	# Required: Describes a type of stored blob.
