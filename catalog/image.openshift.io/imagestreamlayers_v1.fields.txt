# schema source: OpenShift v4.20 https://github.com/openshift/api
apiVersion <string> enum=image.openshift.io/v1
kind <string> enum=ImageStreamLayers
metadata.name <string> (required)
metadata.namespace <string>
blobs <map[string]object> (required)	# blobs is a map of blob name to metadata about the blob.
blobs.<key>.mediaType <string> (required) default=""	# mediaType of the referenced object.
blobs.<key>.size <integer> (required) format=int64	# size of the layer in bytes as defined by the underlying store. This field is optional if the necessary information about size is not available.
images <map[string]object> (required)	# images is a map between an image name and the names of the blobs and config that comprise the image.
images.<key>.config <string>	# config, if set, is the blob that contains the image config. Some images do not have separate config blobs and this field will be set to nil if so.
images.<key>.imageMissing <boolean> default=false	# imageMissing is true if the image is referenced by the image stream but the image object has been deleted from the API by an administrator. When this field is set, layers and config fields may be empty and callers that depend on the image metadata should consider the image to be unavailable for download or viewing.
images.<key>.layers <[]string>	# layers is the list of blobs that compose this image, from base layer to top layer. All layers referenced by this array will be defined in the blobs map. Some images may have zero layers.
images.<key>.manifests <[]string>	# manifests is the list of other image names that this image points to. For a single architecture image, it is empty. For a multi-arch image, it consists of the digests of single architecture images, such images shouldn't have layers nor config.
