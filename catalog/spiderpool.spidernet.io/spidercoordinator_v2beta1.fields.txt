# schema source: Spiderpool v1.2.3-rc1 https://github.com/spidernet-io/spiderpool
apiVersion <string> enum=spiderpool.spidernet.io/v2beta1
kind <string> enum=SpiderCoordinator (cluster-scoped)
metadata.name <string> (required)
spec <object>	# CoordinationSpec defines the desired state of SpiderCoordinator.
spec.hijackCIDR <[]string>	# HijackCIDR configure static routing tables in the pod that target these subnets to ensure that when the pod accesses these subnets, packets are forwarded through the host network stack, such as nodelocaldns(169.254.0.0/16)
spec.hostRuleTable <integer> default=500	# HostRuleTable specifies the table number of the routing table used to configure the communication between the pod and the local node.
spec.logOptions <object>	# LogOptions configures the logging of the coordinator plugin.
spec.logOptions.logFileMaxAge <integer> format=int32 min=0	# LogFileMaxAge configures the maximum number of days to retain old rotated log files. nil means the default (30). 0 means retaining old log files with no day limit.
spec.logOptions.logFileMaxCount <integer> format=int32 min=0	# LogFileMaxCount configures the maximum number of old rotated log files to retain. nil means the default (10). 0 means retaining all old log files.
spec.logOptions.logFileMaxSize <integer> format=int32 min=0	# LogFileMaxSize configures the maximum size in megabytes of a log file before it gets rotated. nil means the default (100). 0 means the rotation library default.
spec.logOptions.logFilePath <string>	# LogFilePath configures the path of the log file. If unset, the plugin uses its own default log file path.
spec.logOptions.logLevel <string> enum=debug|info|warn|error	# LogLevel configures the log level. If unset, the plugin uses its own default log level.
spec.mode <string> enum=auto|underlay|overlay|disabled default="auto"	# Mode mode specifies the mode in which the coordinator runs, and the configurable values include auto (default), underlay, overlay, disabled.
spec.podCIDRType <string> enum=auto|cluster|calico|cilium|none	# CoordinatorSpec is used by SpiderCoordinator and SpiderMultusConfig in spidermultusconfig CRD , podCIDRType should not be required, which could be merged from SpiderCoordinator CR but in SpiderCoordinator CRD, podCIDRType should be required
spec.podDefaultRouteNIC <string>	# PodDefaultRouteNIC PodDefaultRouteNIC is used to configure the NIC where the pod's default route resides. the default value is empty, which means the default route will remain at eth0.
spec.podMACPrefix <string>	# PodMACPrefix the fixed MAC address prefix, the length is two bytes. the lowest bit of the first byte must be 0, which indicates the unicast MAC address. example: 0a:1b
spec.podRPFilter <integer> default=0	# PodRPFilter is used for coordiantor to help set the rp_filter parameters of the pod. Configurable values: <negative number>/0/1/2. negative number means leave it as it is. the default value is 0.
spec.policyRoutes <[]object>	# PolicyRoutes configures static routes that coordinator installs into the policy routing table of the interface where coordinator is running.
spec.policyRoutes[].dst <string> (required)
spec.policyRoutes[].gw <string> (required)
spec.tunePodRoutes <boolean> default=true	# TunePodRoutes specifies whether to tune pod routes of multiple NICs on pods.
spec.txQueueLen <integer> default=0	# TxQueueLen to set the tx_queue_len of the pod. requirement is a positive integer the default value is 0, which means leaving it as it is.
spec.vethLinkAddress <string>	# VethLinkAddress configure a ipv4 link-local address for veth0 device. empty means disable. default is empty. Format is like 169.254.100.1
spec.vethMTU <integer> min=1	# VethMTU configures the MTU of veth0 device.
status <object>	# CoordinationStatus defines the observed state of SpiderCoordinator.
status.overlayPodCIDR <[]string>
status.phase <string> (required)
status.reason <string>
status.serviceCIDR <[]string>
