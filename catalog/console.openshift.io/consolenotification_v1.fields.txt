# schema source: OpenShift v4.22 https://github.com/openshift/api
apiVersion <string> enum=console.openshift.io/v1
kind <string> enum=ConsoleNotification
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}	# ConsoleNotificationSpec is the desired console notification configuration.
spec.backgroundColor <string>	# backgroundColor is the color of the background for the notification as CSS data type color.
spec.color <string>	# color is the color of the text for the notification as CSS data type color.
spec.link <object>	# link is an object that holds notification link details.
spec.link.href <string> (required) default=""	# href is the absolute URL for the link. Must use https:// for web URLs or mailto: for email links.
spec.link.text <string> (required) default=""	# text is the display text for the link
spec.location <string>	# location is the location of the notification in the console. Valid values are: "BannerTop", "BannerBottom", "BannerTopBottom".
spec.text <string> (required) default=""	# text is the visible text of the notification.
