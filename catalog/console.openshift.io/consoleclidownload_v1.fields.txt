# schema source: OpenShift v4.20
apiVersion <string> enum=console.openshift.io/v1
kind <string> enum=ConsoleCLIDownload
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}	# ConsoleCLIDownloadSpec is the desired cli download configuration.
spec.description <string> (required) default=""	# description is the description of the CLI download (can include markdown).
spec.displayName <string> (required) default=""	# displayName is the display name of the CLI download.
spec.links <[]object> (required)	# links is a list of objects that provide CLI download link details.
spec.links[].href <string> (required) default=""	# href is the absolute secure URL for the link (must use https)
spec.links[].text <string> default=""	# text is the display text for the link
