# schema source: Loki Operator v0.10.2 https://github.com/grafana/loki
apiVersion <string> enum=loki.grafana.com/v1
kind <string> enum=RulerConfig
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# RulerConfigSpec defines the desired state of Ruler
spec.alertmanager <object>	# Defines alert manager configuration to notify on firing alerts.
spec.alertmanager.client <object>	# Client configuration for reaching the alertmanager endpoint.
spec.alertmanager.client.basicAuth <object>	# Basic authentication configuration for reaching the alertmanager endpoints.
spec.alertmanager.client.basicAuth.password <string>	# The subject's password for the basic authentication configuration.
spec.alertmanager.client.basicAuth.username <string>	# The subject's username for the basic authentication configuration.
spec.alertmanager.client.headerAuth <object>	# Header authentication configuration for reaching the alertmanager endpoints.
spec.alertmanager.client.headerAuth.credentials <string>	# The credentials for the header authentication configuration.
spec.alertmanager.client.headerAuth.credentialsFile <string>	# The credentials file for the Header authentication configuration. It is mutually exclusive with `credentials`.
spec.alertmanager.client.headerAuth.type <string>	# The authentication type for the header authentication configuration.
spec.alertmanager.client.tls <object>	# TLS configuration for reaching the alertmanager endpoints.
spec.alertmanager.client.tls.caPath <string>	# The CA certificate file path for the TLS configuration.
spec.alertmanager.client.tls.certPath <string>	# The client-side certificate file path for the TLS configuration.
spec.alertmanager.client.tls.insecureSkipVerify <boolean>	# Skip validating server certificate.
spec.alertmanager.client.tls.keyPath <string>	# The client-side key file path for the TLS configuration.
spec.alertmanager.client.tls.serverName <string>	# The server name to validate in the alertmanager server certificates.
spec.alertmanager.discovery <object>	# Defines the configuration for DNS-based discovery of AlertManager hosts.
spec.alertmanager.discovery.enableSRV <boolean>	# Use DNS SRV records to discover Alertmanager hosts.
spec.alertmanager.discovery.refreshInterval <string> default="1m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# How long to wait between refreshing DNS resolutions of Alertmanager hosts.
spec.alertmanager.enableV2 <boolean>	# If enabled, then requests to Alertmanager use the v2 API.
spec.alertmanager.endpoints <[]string> (required)	# List of AlertManager URLs to send notifications to. Each Alertmanager URL is treated as a separate group in the configuration. Multiple Alertmanagers in HA per group can be supported by using DNS resolution (See EnableDNSDiscovery).
spec.alertmanager.externalLabels <map[string]string>	# Additional labels to add to all alerts.
spec.alertmanager.externalUrl <string>	# URL for alerts return path.
spec.alertmanager.notificationQueue <object>	# Defines the configuration for the notification queue to AlertManager hosts.
spec.alertmanager.notificationQueue.capacity <integer> default=10000 format=int32	# Capacity of the queue for notifications to be sent to the Alertmanager.
spec.alertmanager.notificationQueue.forGracePeriod <string> default="10m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Minimum duration between alert and restored "for" state. This is maintained only for alerts with configured "for" time greater than the grace period.
spec.alertmanager.notificationQueue.forOutageTolerance <string> default="1h" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Max time to tolerate outage for restoring "for" state of alert.
spec.alertmanager.notificationQueue.resendDelay <string> default="1m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Minimum amount of time to wait before resending an alert to Alertmanager.
spec.alertmanager.notificationQueue.timeout <string> default="10s" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# HTTP timeout duration when sending notifications to the Alertmanager.
spec.alertmanager.relabelConfigs <[]object>	# List of alert relabel configurations.
spec.alertmanager.relabelConfigs[].action <string> enum=drop|hashmod|keep|labeldrop|labelkeep|labelmap|replace default="replace"	# Action to perform based on regex matching. Default is 'replace'
spec.alertmanager.relabelConfigs[].modulus <integer> format=int64	# Modulus to take of the hash of the source label values.
spec.alertmanager.relabelConfigs[].regex <string> default="(.*)"	# Regular expression against which the extracted value is matched. Default is '(.*)'
spec.alertmanager.relabelConfigs[].replacement <string> default="$1"	# Replacement value against which a regex replace is performed if the regular expression matches. Regex capture groups are available. Default is '$1'
spec.alertmanager.relabelConfigs[].separator <string> default=";"	# Separator placed between concatenated source label values. default is ';'.
spec.alertmanager.relabelConfigs[].sourceLabels <[]string> (required)	# The source labels select values from existing labels. Their content is concatenated using the configured separator and matched against the configured regular expression for the replace, keep, and drop actions.
spec.alertmanager.relabelConfigs[].targetLabel <string>	# Label to which the resulting value is written in a replace action. It is mandatory for replace actions. Regex capture groups are available.
spec.evaluationInterval <string> default="1m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Interval on how frequently to evaluate rules.
spec.overrides <map[string]object>	# Overrides defines the config overrides to be applied per-tenant.
spec.overrides.<key>.alertmanager <object>	# AlertManagerOverrides defines the overrides to apply to the alertmanager config.
spec.overrides.<key>.alertmanager.client <object>	# Client configuration for reaching the alertmanager endpoint.
spec.overrides.<key>.alertmanager.client.basicAuth <object>	# Basic authentication configuration for reaching the alertmanager endpoints.
spec.overrides.<key>.alertmanager.client.basicAuth.password <string>	# The subject's password for the basic authentication configuration.
spec.overrides.<key>.alertmanager.client.basicAuth.username <string>	# The subject's username for the basic authentication configuration.
spec.overrides.<key>.alertmanager.client.headerAuth <object>	# Header authentication configuration for reaching the alertmanager endpoints.
spec.overrides.<key>.alertmanager.client.headerAuth.credentials <string>	# The credentials for the header authentication configuration.
spec.overrides.<key>.alertmanager.client.headerAuth.credentialsFile <string>	# The credentials file for the Header authentication configuration. It is mutually exclusive with `credentials`.
spec.overrides.<key>.alertmanager.client.headerAuth.type <string>	# The authentication type for the header authentication configuration.
spec.overrides.<key>.alertmanager.client.tls <object>	# TLS configuration for reaching the alertmanager endpoints.
spec.overrides.<key>.alertmanager.client.tls.caPath <string>	# The CA certificate file path for the TLS configuration.
spec.overrides.<key>.alertmanager.client.tls.certPath <string>	# The client-side certificate file path for the TLS configuration.
spec.overrides.<key>.alertmanager.client.tls.insecureSkipVerify <boolean>	# Skip validating server certificate.
spec.overrides.<key>.alertmanager.client.tls.keyPath <string>	# The client-side key file path for the TLS configuration.
spec.overrides.<key>.alertmanager.client.tls.serverName <string>	# The server name to validate in the alertmanager server certificates.
spec.overrides.<key>.alertmanager.discovery <object>	# Defines the configuration for DNS-based discovery of AlertManager hosts.
spec.overrides.<key>.alertmanager.discovery.enableSRV <boolean>	# Use DNS SRV records to discover Alertmanager hosts.
spec.overrides.<key>.alertmanager.discovery.refreshInterval <string> default="1m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# How long to wait between refreshing DNS resolutions of Alertmanager hosts.
spec.overrides.<key>.alertmanager.enableV2 <boolean>	# If enabled, then requests to Alertmanager use the v2 API.
spec.overrides.<key>.alertmanager.endpoints <[]string> (required)	# List of AlertManager URLs to send notifications to. Each Alertmanager URL is treated as a separate group in the configuration. Multiple Alertmanagers in HA per group can be supported by using DNS resolution (See EnableDNSDiscovery).
spec.overrides.<key>.alertmanager.externalLabels <map[string]string>	# Additional labels to add to all alerts.
spec.overrides.<key>.alertmanager.externalUrl <string>	# URL for alerts return path.
spec.overrides.<key>.alertmanager.notificationQueue <object>	# Defines the configuration for the notification queue to AlertManager hosts.
spec.overrides.<key>.alertmanager.notificationQueue.capacity <integer> default=10000 format=int32	# Capacity of the queue for notifications to be sent to the Alertmanager.
spec.overrides.<key>.alertmanager.notificationQueue.forGracePeriod <string> default="10m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Minimum duration between alert and restored "for" state. This is maintained only for alerts with configured "for" time greater than the grace period.
spec.overrides.<key>.alertmanager.notificationQueue.forOutageTolerance <string> default="1h" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Max time to tolerate outage for restoring "for" state of alert.
spec.overrides.<key>.alertmanager.notificationQueue.resendDelay <string> default="1m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Minimum amount of time to wait before resending an alert to Alertmanager.
spec.overrides.<key>.alertmanager.notificationQueue.timeout <string> default="10s" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# HTTP timeout duration when sending notifications to the Alertmanager.
spec.overrides.<key>.alertmanager.relabelConfigs <[]object>	# List of alert relabel configurations.
spec.overrides.<key>.alertmanager.relabelConfigs[].action <string> enum=drop|hashmod|keep|labeldrop|labelkeep|labelmap|replace default="replace"	# Action to perform based on regex matching. Default is 'replace'
spec.overrides.<key>.alertmanager.relabelConfigs[].modulus <integer> format=int64	# Modulus to take of the hash of the source label values.
spec.overrides.<key>.alertmanager.relabelConfigs[].regex <string> default="(.*)"	# Regular expression against which the extracted value is matched. Default is '(.*)'
spec.overrides.<key>.alertmanager.relabelConfigs[].replacement <string> default="$1"	# Replacement value against which a regex replace is performed if the regular expression matches. Regex capture groups are available. Default is '$1'
spec.overrides.<key>.alertmanager.relabelConfigs[].separator <string> default=";"	# Separator placed between concatenated source label values. default is ';'.
spec.overrides.<key>.alertmanager.relabelConfigs[].sourceLabels <[]string> (required)	# The source labels select values from existing labels. Their content is concatenated using the configured separator and matched against the configured regular expression for the replace, keep, and drop actions.
spec.overrides.<key>.alertmanager.relabelConfigs[].targetLabel <string>	# Label to which the resulting value is written in a replace action. It is mandatory for replace actions. Regex capture groups are available.
spec.pollInterval <string> default="1m" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Interval on how frequently to poll for new rule definitions.
spec.remoteWrite <object>	# Defines a remote write endpoint to write recording rule metrics.
spec.remoteWrite.client <object>	# Defines the configuration for remote write client.
spec.remoteWrite.client.additionalHeaders <map[string]string>	# Additional HTTP headers to be sent along with each remote write request.
spec.remoteWrite.client.authorization <string> (required) enum=basic|bearer	# Type of authorzation to use to access the remote write endpoint
spec.remoteWrite.client.authorizationSecretName <string> (required)	# Name of a secret in the namespace configured for authorization secrets.
spec.remoteWrite.client.followRedirects <boolean> default=true	# Configure whether HTTP requests follow HTTP 3xx redirects.
spec.remoteWrite.client.name <string> (required)	# Name of the remote write config, which if specified must be unique among remote write configs.
spec.remoteWrite.client.proxyUrl <string>	# Optional proxy URL.
spec.remoteWrite.client.relabelConfigs <[]object>	# List of remote write relabel configurations.
spec.remoteWrite.client.relabelConfigs[].action <string> enum=drop|hashmod|keep|labeldrop|labelkeep|labelmap|replace default="replace"	# Action to perform based on regex matching. Default is 'replace'
spec.remoteWrite.client.relabelConfigs[].modulus <integer> format=int64	# Modulus to take of the hash of the source label values.
spec.remoteWrite.client.relabelConfigs[].regex <string> default="(.*)"	# Regular expression against which the extracted value is matched. Default is '(.*)'
spec.remoteWrite.client.relabelConfigs[].replacement <string> default="$1"	# Replacement value against which a regex replace is performed if the regular expression matches. Regex capture groups are available. Default is '$1'
spec.remoteWrite.client.relabelConfigs[].separator <string> default=";"	# Separator placed between concatenated source label values. default is ';'.
spec.remoteWrite.client.relabelConfigs[].sourceLabels <[]string> (required)	# The source labels select values from existing labels. Their content is concatenated using the configured separator and matched against the configured regular expression for the replace, keep, and drop actions.
spec.remoteWrite.client.relabelConfigs[].targetLabel <string>	# Label to which the resulting value is written in a replace action. It is mandatory for replace actions. Regex capture groups are available.
spec.remoteWrite.client.timeout <string> default="30s" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Timeout for requests to the remote write endpoint.
spec.remoteWrite.client.url <string> (required)	# The URL of the endpoint to send samples to.
spec.remoteWrite.enabled <boolean>	# Enable remote-write functionality.
spec.remoteWrite.queue <object>	# Defines the configuration for remote write client queue.
spec.remoteWrite.queue.batchSendDeadline <string> default="5s" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Maximum time a sample will wait in buffer.
spec.remoteWrite.queue.capacity <integer> default=2500 format=int32	# Number of samples to buffer per shard before we block reading of more
spec.remoteWrite.queue.maxBackOffPeriod <string> default="100ms" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Maximum retry delay.
spec.remoteWrite.queue.maxSamplesPerSend <integer> default=500 format=int32	# Maximum number of samples per send.
spec.remoteWrite.queue.maxShards <integer> default=200 format=int32	# Maximum number of shards, i.e. amount of concurrency.
spec.remoteWrite.queue.minBackOffPeriod <string> default="30ms" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Initial retry delay. Gets doubled for every retry.
spec.remoteWrite.queue.minShards <integer> default=200 format=int32	# Minimum number of shards, i.e. amount of concurrency.
spec.remoteWrite.refreshPeriod <string> default="10s" pattern="((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)"	# Minimum period to wait between refreshing remote-write reconfigurations.
status <object>	# RulerConfigStatus defines the observed state of RulerConfig
status.conditions <[]object>	# Conditions of the RulerConfig health.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
