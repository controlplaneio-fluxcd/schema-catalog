# schema source: Capsule v0.13.10 https://github.com/projectcapsule/capsule
apiVersion <string> enum=capsule.clastix.io/v1beta1
kind <string> enum=Tenant (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)	# TenantSpec defines the desired state of Tenant.
spec.additionalRoleBindings <[]object>	# Specifies additional RoleBindings assigned to the Tenant. Capsule will ensure that all namespaces in the Tenant always contain the RoleBinding for the given ClusterRole. Optional.
spec.additionalRoleBindings[].annotations <map[string]string>	# Additional Annotations for the synchronized rolebindings
spec.additionalRoleBindings[].clusterRoleName <string> (required)
spec.additionalRoleBindings[].labels <map[string]string>	# Additional Labels for the synchronized rolebindings
spec.additionalRoleBindings[].subjects <[]object> (required) min=1
spec.additionalRoleBindings[].subjects[].apiGroup <string>	# APIGroup holds the API group of the referenced subject. Defaults to "" for ServiceAccount subjects. Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
spec.additionalRoleBindings[].subjects[].kind <string> (required)	# Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount". If the Authorizer does not recognized the kind value, the Authorizer should report an error.
spec.additionalRoleBindings[].subjects[].name <string> (required)	# Name of the object being referenced.
spec.additionalRoleBindings[].subjects[].namespace <string>	# Namespace of the referenced object. If the object kind is non-namespace, such as "User" or "Group", and this value is not empty the Authorizer should report an error.
spec.containerRegistries <object>	# Specifies the trusted Image Registries assigned to the Tenant. Capsule assures that all Pods resources created in the Tenant can use only one of the allowed trusted registries. Optional.
spec.containerRegistries.allowed <[]string>	# Match exact elements which are allowed as class names within this tenant
spec.containerRegistries.allowedRegex <string>	# Deprecated: will be removed in a future release Match elements by regex.
spec.imagePullPolicies <[]string>	# Specify the allowed values for the imagePullPolicies option in Pod resources. Capsule assures that all Pod resources created in the Tenant can use only one of the allowed policy. Optional.
spec.ingressOptions <object>	# Specifies options for the Ingress resources, such as allowed hostnames and IngressClass. Optional.
spec.ingressOptions.allowedClasses <object>	# Specifies the allowed IngressClasses assigned to the Tenant. Capsule assures that all Ingress resources created in the Tenant can use only one of the allowed IngressClasses. Optional.
spec.ingressOptions.allowedClasses.allowed <[]string>	# Match exact elements which are allowed as class names within this tenant
spec.ingressOptions.allowedClasses.allowedRegex <string>	# Deprecated: will be removed in a future release Match elements by regex.
spec.ingressOptions.allowedHostnames <object>	# Specifies the allowed hostnames in Ingresses for the given Tenant. Capsule assures that all Ingress resources created in the Tenant can use only one of the allowed hostnames. Optional.
spec.ingressOptions.allowedHostnames.allowed <[]string>	# Match exact elements which are allowed as class names within this tenant
spec.ingressOptions.allowedHostnames.allowedRegex <string>	# Deprecated: will be removed in a future release Match elements by regex.
spec.ingressOptions.hostnameCollisionScope <string> enum=Cluster|Tenant|Namespace|Disabled default="Disabled"	# Defines the scope of hostname collision check performed when Tenant Owners create Ingress with allowed hostnames. - Cluster: disallow the creation of an Ingress if the pair hostname and path is already used across the Namespaces managed by Capsule. - Tenant: disallow the creation of an Ingress if the pair hostname and path is already used across the Namespaces of the Tenant. - Namespace: disallow the creation of an Ingress if the pair hostname and path is already used in the Ingress Namespace. Optional.
spec.limitRanges <object>	# Specifies the resource min/max usage restrictions to the Tenant. The assigned values are inherited by any namespace created in the Tenant. Optional.
spec.limitRanges.items <[]object>
spec.limitRanges.items[].limits <[]object> (required)	# Limits is the list of LimitRangeItem objects that are enforced.
spec.limitRanges.items[].limits[].default <object (free-form)>	# Default resource requirement limit value by resource name if resource limit is omitted.
spec.limitRanges.items[].limits[].defaultRequest <object (free-form)>	# DefaultRequest is the default resource requirement request value by resource name if resource request is omitted.
spec.limitRanges.items[].limits[].max <object (free-form)>	# Max usage constraints on this kind by resource name.
spec.limitRanges.items[].limits[].maxLimitRequestRatio <object (free-form)>	# MaxLimitRequestRatio if specified, the named resource must have a request and limit that are both non-zero where limit divided by request is less than or equal to the enumerated value; this represents the max burst for the named resource.
spec.limitRanges.items[].limits[].min <object (free-form)>	# Min usage constraints on this kind by resource name.
spec.limitRanges.items[].limits[].type <string> (required)	# Type of resource that this limit applies to.
spec.namespaceOptions <object>	# Specifies options for the Namespaces, such as additional metadata or maximum number of namespaces allowed for that Tenant. Once the namespace quota assigned to the Tenant has been reached, the Tenant owner cannot create further namespaces. Optional.
spec.namespaceOptions.additionalMetadata <object>	# Specifies additional labels and annotations the Capsule operator places on any Namespace resource in the Tenant. Optional.
spec.namespaceOptions.additionalMetadata.annotations <map[string]string>
spec.namespaceOptions.additionalMetadata.labels <map[string]string>
spec.namespaceOptions.quota <integer> format=int32 min=1	# Specifies the maximum number of namespaces allowed for that Tenant. Once the namespace quota assigned to the Tenant has been reached, the Tenant owner cannot create further namespaces. Optional.
spec.networkPolicies <object>	# Specifies the NetworkPolicies assigned to the Tenant. The assigned NetworkPolicies are inherited by any namespace created in the Tenant. Optional.
spec.networkPolicies.items <[]object>
spec.networkPolicies.items[].egress <[]object>	# egress is a list of egress rules to be applied to the selected pods. Outgoing traffic is allowed if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic matches at least one egress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy limits all outgoing traffic (and serves solely to ensure that the pods it selects are isolated by default). This field is beta-level in 1.8
spec.networkPolicies.items[].egress[].ports <[]object>	# ports is a list of destination ports for outgoing traffic. Each item in this list is combined using a logical OR. If this field is empty or missing, this rule matches all ports (traffic not restricted by port). If this field is present and contains at least one item, then this rule allows traffic only if the traffic matches at least one port in the list.
spec.networkPolicies.items[].egress[].ports[].endPort <integer> format=int32	# endPort indicates that the range of ports from port to endPort if set, inclusive, should be allowed by the policy. This field cannot be defined if the port field is not defined or if the port field is defined as a named (string) port. The endPort must be equal or greater than port.
spec.networkPolicies.items[].egress[].ports[].port <int-or-string>	# port represents the port on the given protocol. This can either be a numerical or named port on a pod. If this field is not provided, this matches all port names and numbers. If present, only traffic on the specified protocol AND port will be matched.
spec.networkPolicies.items[].egress[].ports[].protocol <string>	# protocol represents the protocol (TCP, UDP, or SCTP) which traffic must match. If not specified, this field defaults to TCP.
spec.networkPolicies.items[].egress[].to <[]object>	# to is a list of destinations for outgoing traffic of pods selected for this rule. Items in this list are combined using a logical OR operation. If this field is empty or missing, this rule matches all destinations (traffic not restricted by destination). If this field is present and contains at least one item, this rule allows traffic only if the traffic matches at least one item in the to list.
spec.networkPolicies.items[].egress[].to[].ipBlock <object>	# ipBlock defines policy on a particular IPBlock. If this field is set then neither of the other fields can be.
spec.networkPolicies.items[].egress[].to[].ipBlock.cidr <string> (required)	# cidr is a string representing the IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64"
spec.networkPolicies.items[].egress[].to[].ipBlock.except <[]string>	# except is a slice of CIDRs that should not be included within an IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64" Except values will be rejected if they are outside the cidr range
spec.networkPolicies.items[].egress[].to[].namespaceSelector <object>	# namespaceSelector selects namespaces using cluster-scoped labels. This field follows standard label selector semantics; if present but empty, it selects all namespaces. If podSelector is also set, then the NetworkPolicyPeer as a whole selects the pods matching podSelector in the namespaces selected by namespaceSelector. Otherwise it selects all pods in the namespaces selected by namespaceSelector.
spec.networkPolicies.items[].egress[].to[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.networkPolicies.items[].egress[].to[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.networkPolicies.items[].egress[].to[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.networkPolicies.items[].egress[].to[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.networkPolicies.items[].egress[].to[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.networkPolicies.items[].egress[].to[].podSelector <object>	# podSelector is a label selector which selects pods. This field follows standard label selector semantics; if present but empty, it selects all pods. If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects the pods matching podSelector in the Namespaces selected by NamespaceSelector. Otherwise it selects the pods matching podSelector in the policy's own namespace.
spec.networkPolicies.items[].egress[].to[].podSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.networkPolicies.items[].egress[].to[].podSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.networkPolicies.items[].egress[].to[].podSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.networkPolicies.items[].egress[].to[].podSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.networkPolicies.items[].egress[].to[].podSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.networkPolicies.items[].ingress <[]object>	# ingress is a list of ingress rules to be applied to the selected pods. Traffic is allowed to a pod if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic source is the pod's local node, OR if the traffic matches at least one ingress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy does not allow any traffic (and serves solely to ensure that the pods it selects are isolated by default)
spec.networkPolicies.items[].ingress[].from <[]object>	# from is a list of sources which should be able to access the pods selected for this rule. Items in this list are combined using a logical OR operation. If this field is empty or missing, this rule matches all sources (traffic not restricted by source). If this field is present and contains at least one item, this rule allows traffic only if the traffic matches at least one item in the from list.
spec.networkPolicies.items[].ingress[].from[].ipBlock <object>	# ipBlock defines policy on a particular IPBlock. If this field is set then neither of the other fields can be.
spec.networkPolicies.items[].ingress[].from[].ipBlock.cidr <string> (required)	# cidr is a string representing the IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64"
spec.networkPolicies.items[].ingress[].from[].ipBlock.except <[]string>	# except is a slice of CIDRs that should not be included within an IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64" Except values will be rejected if they are outside the cidr range
spec.networkPolicies.items[].ingress[].from[].namespaceSelector <object>	# namespaceSelector selects namespaces using cluster-scoped labels. This field follows standard label selector semantics; if present but empty, it selects all namespaces. If podSelector is also set, then the NetworkPolicyPeer as a whole selects the pods matching podSelector in the namespaces selected by namespaceSelector. Otherwise it selects all pods in the namespaces selected by namespaceSelector.
spec.networkPolicies.items[].ingress[].from[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.networkPolicies.items[].ingress[].from[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.networkPolicies.items[].ingress[].from[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.networkPolicies.items[].ingress[].from[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.networkPolicies.items[].ingress[].from[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.networkPolicies.items[].ingress[].from[].podSelector <object>	# podSelector is a label selector which selects pods. This field follows standard label selector semantics; if present but empty, it selects all pods. If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects the pods matching podSelector in the Namespaces selected by NamespaceSelector. Otherwise it selects the pods matching podSelector in the policy's own namespace.
spec.networkPolicies.items[].ingress[].from[].podSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.networkPolicies.items[].ingress[].from[].podSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.networkPolicies.items[].ingress[].from[].podSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.networkPolicies.items[].ingress[].from[].podSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.networkPolicies.items[].ingress[].from[].podSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.networkPolicies.items[].ingress[].ports <[]object>	# ports is a list of ports which should be made accessible on the pods selected for this rule. Each item in this list is combined using a logical OR. If this field is empty or missing, this rule matches all ports (traffic not restricted by port). If this field is present and contains at least one item, then this rule allows traffic only if the traffic matches at least one port in the list.
spec.networkPolicies.items[].ingress[].ports[].endPort <integer> format=int32	# endPort indicates that the range of ports from port to endPort if set, inclusive, should be allowed by the policy. This field cannot be defined if the port field is not defined or if the port field is defined as a named (string) port. The endPort must be equal or greater than port.
spec.networkPolicies.items[].ingress[].ports[].port <int-or-string>	# port represents the port on the given protocol. This can either be a numerical or named port on a pod. If this field is not provided, this matches all port names and numbers. If present, only traffic on the specified protocol AND port will be matched.
spec.networkPolicies.items[].ingress[].ports[].protocol <string>	# protocol represents the protocol (TCP, UDP, or SCTP) which traffic must match. If not specified, this field defaults to TCP.
spec.networkPolicies.items[].podSelector <object>	# podSelector selects the pods to which this NetworkPolicy object applies. The array of rules is applied to any pods selected by this field. An empty selector matches all pods in the policy's namespace. Multiple network policies can select the same set of pods. In this case, the ingress rules for each are combined additively. This field is optional. If it is not specified, it defaults to an empty selector.
spec.networkPolicies.items[].podSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.networkPolicies.items[].podSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.networkPolicies.items[].podSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.networkPolicies.items[].podSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.networkPolicies.items[].podSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.networkPolicies.items[].policyTypes <[]string>	# policyTypes is a list of rule types that the NetworkPolicy relates to. Valid options are ["Ingress"], ["Egress"], or ["Ingress", "Egress"]. If this field is not specified, it will default based on the existence of ingress or egress rules; policies that contain an egress section are assumed to affect egress, and all policies (whether or not they contain an ingress section) are assumed to affect ingress. If you want to write an egress-only policy, you must explicitly specify policyTypes [ "Egress" ]. Likewise, if you want to write a policy that specifies that no egress is allowed, you must specify a policyTypes value that include "Egress" (since such a policy would not include an egress section and would otherwise default to just [ "Ingress" ]). This field is beta-level in 1.8
spec.nodeSelector <map[string]string>	# Specifies the label to control the placement of pods on a given pool of worker nodes. All namespaces created within the Tenant will have the node selector annotation. This annotation tells the Kubernetes scheduler to place pods on the nodes having the selector label. Optional.
spec.owners <[]object> (required)	# Specifies the owners of the Tenant. Mandatory.
spec.owners[].kind <string> (required) enum=User|Group|ServiceAccount	# Kind of tenant owner. Possible values are "User", "Group", and "ServiceAccount"
spec.owners[].name <string> (required)	# Name of tenant owner.
spec.owners[].proxySettings <[]object>	# Proxy settings for tenant owner.
spec.owners[].proxySettings[].kind <string> (required) enum=Nodes|StorageClasses|IngressClasses|PriorityClasses
spec.owners[].proxySettings[].operations <[]string> (required)
spec.priorityClasses <object>	# Specifies the allowed priorityClasses assigned to the Tenant. Capsule assures that all Pods resources created in the Tenant can use only one of the allowed PriorityClasses. Optional.
spec.priorityClasses.allowed <[]string>	# Match exact elements which are allowed as class names within this tenant
spec.priorityClasses.allowedRegex <string>	# Deprecated: will be removed in a future release Match elements by regex.
spec.resourceQuotas <object>	# Specifies a list of ResourceQuota resources assigned to the Tenant. The assigned values are inherited by any namespace created in the Tenant. The Capsule operator aggregates ResourceQuota at Tenant level, so that the hard quota is never crossed for the given Tenant. This permits the Tenant owner to consume resources in the Tenant regardless of the namespace. Optional.
spec.resourceQuotas.items <[]object>
spec.resourceQuotas.items[].hard <object (free-form)>	# hard is the set of desired hard limits for each named resource. More info: https://kubernetes.io/docs/concepts/policy/resource-quotas/
spec.resourceQuotas.items[].scopeSelector <object>	# scopeSelector is also a collection of filters like scopes that must match each object tracked by a quota but expressed using ScopeSelectorOperator in combination with possible values. For a resource to match, both scopes AND scopeSelector (if specified in spec), must be matched.
spec.resourceQuotas.items[].scopeSelector.matchExpressions <[]object>	# A list of scope selector requirements by scope of the resources.
spec.resourceQuotas.items[].scopeSelector.matchExpressions[].operator <string> (required)	# Represents a scope's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist.
spec.resourceQuotas.items[].scopeSelector.matchExpressions[].scopeName <string> (required)	# The name of the scope that the selector applies to.
spec.resourceQuotas.items[].scopeSelector.matchExpressions[].values <[]string>	# An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.resourceQuotas.items[].scopes <[]string>	# A collection of filters that must match each object tracked by a quota. If not specified, the quota matches all objects.
spec.resourceQuotas.scope <string> enum=Tenant|Namespace default="Tenant"	# Define if the Resource Budget should compute resource across all Namespaces in the Tenant or individually per cluster. Default is Tenant
spec.serviceOptions <object>	# Specifies options for the Service, such as additional metadata or block of certain type of Services. Optional.
spec.serviceOptions.additionalMetadata <object>	# Specifies additional labels and annotations the Capsule operator places on any Service resource in the Tenant. Optional.
spec.serviceOptions.additionalMetadata.annotations <map[string]string>
spec.serviceOptions.additionalMetadata.labels <map[string]string>
spec.serviceOptions.allowedServices <object>	# Block or deny certain type of Services. Optional.
spec.serviceOptions.allowedServices.externalName <boolean> default=true	# Specifies if ExternalName service type resources are allowed for the Tenant. Default is true. Optional.
spec.serviceOptions.allowedServices.loadBalancer <boolean> default=true	# Specifies if LoadBalancer service type resources are allowed for the Tenant. Default is true. Optional.
spec.serviceOptions.allowedServices.nodePort <boolean> default=true	# Specifies if NodePort service type resources are allowed for the Tenant. Default is true. Optional.
spec.serviceOptions.externalIPs <object>	# Specifies the external IPs that can be used in Services with type ClusterIP. An empty list means no IPs are allowed. Optional.
spec.serviceOptions.externalIPs.allowed <[]string> (required)
spec.serviceOptions.forbiddenAnnotations <object>	# Define the annotations that a Tenant Owner cannot set for their Service resources.
spec.serviceOptions.forbiddenAnnotations.denied <[]string>
spec.serviceOptions.forbiddenAnnotations.deniedRegex <string>
spec.serviceOptions.forbiddenLabels <object>	# Define the labels that a Tenant Owner cannot set for their Service resources.
spec.serviceOptions.forbiddenLabels.denied <[]string>
spec.serviceOptions.forbiddenLabels.deniedRegex <string>
spec.storageClasses <object>	# Specifies the allowed StorageClasses assigned to the Tenant. Capsule assures that all PersistentVolumeClaim resources created in the Tenant can use only one of the allowed StorageClasses. Optional.
spec.storageClasses.allowed <[]string>	# Match exact elements which are allowed as class names within this tenant
spec.storageClasses.allowedRegex <string>	# Deprecated: will be removed in a future release Match elements by regex.
status <object>	# Returns the observed state of the Tenant.
status.namespaces <[]string>	# List of namespaces assigned to the Tenant.
status.size <integer> (required)	# How many namespaces are assigned to the Tenant.
status.state <string> (required) enum=Cordoned|Active default="Active"	# The operational state of the Tenant. Possible values are "Active", "Cordoned".
