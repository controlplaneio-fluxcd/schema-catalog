# schema source: Capsule v0.13.9 https://github.com/projectcapsule/capsule
apiVersion <string> enum=capsule.clastix.io/v1beta2
kind <string> enum=TenantResource
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# TenantResourceSpec defines the desired state of TenantResource.
spec.cordoned <boolean> default=false	# When cordoning a replication it will no longer execute any applies or deletions (paused). This is useful for maintenances
spec.dependsOn <[]object>	# DependsOn may contain a meta.NamespacedObjectReference slice with references to TenantResource resources that must be ready before this TenantResource can be reconciled.
spec.dependsOn[].name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" max=63	# Name of the referent.
spec.pruningOnDelete <boolean> default=true	# When the replicated resource manifest is deleted, all the objects replicated so far will be automatically deleted. Disable this to keep replicated resources although the deletion of the replication manifest.
spec.resources <[]object> (required)	# Defines the rules to select targeting Namespace, along with the objects that must be replicated.
spec.resources[].additionalMetadata <object>	# Besides the Capsule metadata required by TenantResource controller, defines additional metadata that must be added to the replicated resources.
spec.resources[].additionalMetadata.annotations <map[string]string>
spec.resources[].additionalMetadata.labels <map[string]string>
spec.resources[].context <object>	# Provide additional template context, which can be used throughout all the declared items for the replication
spec.resources[].context.resources <[]object>
spec.resources[].context.resources[].apiVersion <string>	# API version, API group, or API group/version selector of the referent. Empty APIVersion means the core Kubernetes API version "v1". Use "*" to explicitly match all API groups and versions. Examples: - "" means core "v1". - "v1" means core "v1". - "apps" means any version in the "apps" API group. - "apps/v1" means the "apps/v1" API group/version. - "apps/*" means any version in the "apps" API group.
spec.resources[].context.resources[].index <string>	# Index to mount the resource in the template context
spec.resources[].context.resources[].kind <string> (required) min=1	# Kind of the referent. Use "*" to match all kinds.
spec.resources[].context.resources[].name <string> min=1 max=253	# Name of the values referent. This is useful when you traying to get a specific resource
spec.resources[].context.resources[].namespace <string>	# Namespace of the values referent.
spec.resources[].context.resources[].optional <boolean> default=true	# Only relevant if name is set. If an item is not optional, there will be an error thrown when it does not exist
spec.resources[].context.resources[].selector <object>	# Selector which allows to get any amount of these resources based on labels
spec.resources[].context.resources[].selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.resources[].context.resources[].selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.resources[].context.resources[].selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.resources[].context.resources[].selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.resources[].context.resources[].selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.resources[].generators <[]object>	# Templates for advanced use cases
spec.resources[].generators[].missingKey <string> enum=invalid|zero|error default="zero"	# Missing Key Option for templating
spec.resources[].generators[].template <string>	# Template contains any amount of yaml which is applied to Kubernetes. This can be a single resource or multiple resources
spec.resources[].namespaceSelector <object>	# Defines the Namespace selector to select the Tenant Namespaces on which the resources must be propagated. In case of nil value, all the Tenant Namespaces are targeted.
spec.resources[].namespaceSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.resources[].namespaceSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.resources[].namespaceSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.resources[].namespaceSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.resources[].namespaceSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.resources[].namespacedItems <[]object>	# List of the resources already existing in other Namespaces that must be replicated.
spec.resources[].namespacedItems[].apiVersion <string>	# API version, API group, or API group/version selector of the referent. Empty APIVersion means the core Kubernetes API version "v1". Use "*" to explicitly match all API groups and versions. Examples: - "" means core "v1". - "v1" means core "v1". - "apps" means any version in the "apps" API group. - "apps/v1" means the "apps/v1" API group/version. - "apps/*" means any version in the "apps" API group.
spec.resources[].namespacedItems[].kind <string> (required) min=1	# Kind of the referent. Use "*" to match all kinds.
spec.resources[].namespacedItems[].name <string> min=1 max=253	# Name of the values referent. This is useful when you traying to get a specific resource
spec.resources[].namespacedItems[].namespace <string>	# Namespace of the values referent.
spec.resources[].namespacedItems[].optional <boolean> default=true	# Only relevant if name is set. If an item is not optional, there will be an error thrown when it does not exist
spec.resources[].namespacedItems[].selector <object>	# Selector which allows to get any amount of these resources based on labels
spec.resources[].namespacedItems[].selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.resources[].namespacedItems[].selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.resources[].namespacedItems[].selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.resources[].namespacedItems[].selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.resources[].namespacedItems[].selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.resources[].rawItems <[]object>	# List of raw resources that must be replicated.
spec.resyncPeriod <string> (required) default="60s"	# Define the period of time upon a second reconciliation must be invoked. Keep in mind that any change to the manifests will trigger a new reconciliation.
spec.serviceAccount <object>	# Local ServiceAccount which will perform all the actions defined in the TenantResource You must provide permissions accordingly to that ServiceAccount
spec.serviceAccount.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" max=63	# Name of the referent.
spec.settings <object> (required) default={}	# Provide additional settings
spec.settings.adopt <boolean> default=false	# Enabling this allows TenanResources to interact with objects which were not created by a TenantResource. In this case on prune no deletion of the entire object is made.
spec.settings.force <boolean> default=false	# Force indicates that in case of conflicts with server-side apply, the client should acquire ownership of the conflicting field. You may create collisions with this.
status <object>	# TenantResourceStatus defines the observed state of TenantResource.
status.conditions <[]object>	# Condition of the GlobalTenantResource.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the most recent generation the controller has observed.
status.processedItems <[]object>	# List of the replicated resources for the given TenantResource.
status.processedItems[].group <string>
status.processedItems[].kind <string>
status.processedItems[].name <string>
status.processedItems[].namespace <string>
status.processedItems[].origin <string>
status.processedItems[].status <object>
status.processedItems[].status.clusterScoped <boolean>	# Indicates whether the referenced resource is cluster-scoped.
status.processedItems[].status.created <boolean>	# Indicates wether the resource was created or adopted
status.processedItems[].status.lastApply <string> format=date-time	# An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
status.processedItems[].status.message <string> max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.processedItems[].status.status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.processedItems[].status.type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.processedItems[].tenant <string>
status.processedItems[].version <string>
status.serviceAccount <object>	# Serviceaccount used for impersonation
status.serviceAccount.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" max=63	# Name of the referent.
status.serviceAccount.namespace <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Namespace of the referent.
status.size <integer> (required)	# How many items are being replicated by the TenantResource.
