# schema source: Capsule v0.14.0 https://github.com/projectcapsule/capsule
apiVersion <string> enum=capsule.clastix.io/v1beta2
kind <string> enum=QuantityLedger
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# QuotaLedgerSpec contains the immutable target reference.
spec.targetRef <object> (required)	# TargetRef points to the quota object that this ledger belongs to.
spec.targetRef.apiGroup <string>	# APIGroup of the target quota resource, for example "capsule.clastix.io".
spec.targetRef.kind <string> (required) min=1	# Kind of the target quota resource, for example "CustomQuota" or "GlobalCustomQuota".
spec.targetRef.name <string> (required) min=1	# Name of the target quota resource.
spec.targetRef.namespace <string>	# Namespace of the target quota resource. Must be empty for cluster-scoped targets.
spec.targetRef.uid <string>	# UID of the target quota resource. Optional, but useful for stale reference detection.
status <object>	# QuantityLedgerStatus contains the mutable coordination state used by admission and quota controllers.
status.allocated <int-or-string>	# Allocated is the admission-owned total that has been accepted by the webhook. It must be updated only through optimistic concurrency on QuantityLedger.
status.conditions <[]object>	# Conditions for the resource claim
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.pendingDeletes <[]object>	# Pending delete hints carried over from admission delete handling.
status.pendingDeletes[].createdAt <string> (required) format=date-time
status.pendingDeletes[].id <string>	# ID identifies the admission request that added this hint. It allows a failed multi-quota admission to roll back only its own hint.
status.pendingDeletes[].objectRef <object> (required)	# QuotaLedgerObjectRef identifies the object for which a reservation exists. UID may be empty for CREATE admission before the object is persisted.
status.pendingDeletes[].objectRef.apiGroup <string>	# APIGroup of the tracked object.
status.pendingDeletes[].objectRef.apiVersion <string> (required) min=1	# APIVersion of the tracked object, for example "v1".
status.pendingDeletes[].objectRef.kind <string> (required) min=1	# Kind of the tracked object, for example "Pod".
status.pendingDeletes[].objectRef.name <string>	# Name of the tracked object.
status.pendingDeletes[].objectRef.namespace <string>	# Namespace of the tracked object.
status.pendingDeletes[].objectRef.uid <string>	# UID of the tracked object.
status.reservations <[]object>	# Active inflight reservations for this quota.
status.reservations[].createdAt <string> (required) format=date-time	# Time the reservation was first created.
status.reservations[].delta <int-or-string>	# Delta is the additional amount held against the quota while the admitted object is materializing. For creates this is normally equal to Usage. For updates it is max(newUsage-oldUsage, 0), so admission never releases capacity before the API server has persisted the update. A nil value is interpreted as Usage for backwards compatibility with ledgers written before this field was introduced.
status.reservations[].expiresAt <string> format=date-time	# Time after which the reservation may be considered stale.
status.reservations[].id <string> (required) min=1	# Unique reservation identifier.
status.reservations[].objectRef <object> (required)	# Object that this reservation is intended to create/update.
status.reservations[].objectRef.apiGroup <string>	# APIGroup of the tracked object.
status.reservations[].objectRef.apiVersion <string> (required) min=1	# APIVersion of the tracked object, for example "v1".
status.reservations[].objectRef.kind <string> (required) min=1	# Kind of the tracked object, for example "Pod".
status.reservations[].objectRef.name <string>	# Name of the tracked object.
status.reservations[].objectRef.namespace <string>	# Namespace of the tracked object.
status.reservations[].objectRef.uid <string>	# UID of the tracked object.
status.reservations[].updatedAt <string> (required) format=date-time	# Time the reservation was last refreshed or updated.
status.reservations[].usage <int-or-string> (required)	# Amount reserved for this request.
status.reserved <int-or-string>	# Reserved is the aggregate sum of all active reservations. Controllers/webhooks should treat this as derived data from Reservations.
status.resourceQuota <object>	# ResourceQuota contains coordination state for GlobalResourceQuota. It is unset for CustomQuota and GlobalCustomQuota ledgers.
status.resourceQuota.allocated <object (free-form)>	# Allocated is Used plus all active reservations.
status.resourceQuota.initialized <boolean>	# Initialized is true after every selected namespace has reported ResourceQuota status for this quota.
status.resourceQuota.namespaces <[]string>	# Namespaces is the selected namespace set represented by Used.
status.resourceQuota.observedGeneration <integer> format=int64	# ObservedGeneration is the GlobalResourceQuota generation represented by this ledger state.
status.resourceQuota.reservations <[]object>	# Reservations contains inflight admission operations.
status.resourceQuota.reservations[].createdAt <string> (required) format=date-time
status.resourceQuota.reservations[].delta <object (free-form)>	# Delta is the positive amount held while ResourceQuota status catches up.
status.resourceQuota.reservations[].expiresAt <string> format=date-time
status.resourceQuota.reservations[].id <string> (required) min=1	# Unique reservation identifier.
status.resourceQuota.reservations[].objectRef <object> (required)	# Object that this reservation is intended to create/update.
status.resourceQuota.reservations[].objectRef.apiGroup <string>	# APIGroup of the tracked object.
status.resourceQuota.reservations[].objectRef.apiVersion <string> (required) min=1	# APIVersion of the tracked object, for example "v1".
status.resourceQuota.reservations[].objectRef.kind <string> (required) min=1	# Kind of the tracked object, for example "Pod".
status.resourceQuota.reservations[].objectRef.name <string>	# Name of the tracked object.
status.resourceQuota.reservations[].objectRef.namespace <string>	# Namespace of the tracked object.
status.resourceQuota.reservations[].objectRef.uid <string>	# UID of the tracked object.
status.resourceQuota.reservations[].updatedAt <string> (required) format=date-time
status.resourceQuota.reservations[].usage <object (free-form)>	# Usage is the calculated usage of the admitted object.
status.resourceQuota.reserved <object (free-form)>	# Reserved is derived from Reservations.
status.resourceQuota.used <object (free-form)>	# Used is the usage observed from ResourceQuota status across all selected namespaces.
