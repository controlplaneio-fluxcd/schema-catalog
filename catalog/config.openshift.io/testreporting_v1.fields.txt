# schema source: OpenShift v4.20
apiVersion <string> enum=config.openshift.io/v1
kind <string> enum=TestReporting
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}
spec.testsForFeatureGates <[]object> (required)	# testsForFeatureGates is a list, indexed by FeatureGate and includes information about testing.
spec.testsForFeatureGates[].featureGate <string> (required) default=""	# featureGate is the name of the FeatureGate as it appears in The FeatureGate CR instance.
spec.testsForFeatureGates[].tests <[]object> (required)	# tests contains an item for every TestName
spec.testsForFeatureGates[].tests[].testName <string> (required) default=""	# testName is the name of the test as it appears in junit XMLs. It does not include the suite name since the same test can be executed in many suites.
status <object (free-form)> default={}	# status holds observed values from the cluster. They may not be overridden.
