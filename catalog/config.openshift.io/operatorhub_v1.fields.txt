# schema source: OpenShift v4.20
apiVersion <string> enum=config.openshift.io/v1
kind <string> enum=OperatorHub
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}	# OperatorHubSpec defines the desired state of OperatorHub
spec.disableAllDefaultSources <boolean>	# disableAllDefaultSources allows you to disable all the default hub sources. If this is true, a specific entry in sources can be used to enable a default source. If this is false, a specific entry in sources can be used to disable or enable a default source.
spec.sources <[]object>	# sources is the list of default hub sources and their configuration. If the list is empty, it implies that the default hub sources are enabled on the cluster unless disableAllDefaultSources is true. If disableAllDefaultSources is true and sources is not empty, the configuration present in sources will take precedence. The list of default hub sources and their current state will always be reflected in the status block.
spec.sources[].disabled <boolean> (required) default=false	# disabled is used to disable a default hub source on cluster
spec.sources[].name <string> (required) default=""	# name is the name of one of the default hub sources
status <object> (required) default={}	# OperatorHubStatus defines the observed state of OperatorHub. The current state of the default hub sources will always be reflected here.
status.sources <[]object>	# sources encapsulates the result of applying the configuration for each hub source
status.sources[].message <string>	# message provides more information regarding failures
status.sources[].status <string>	# status indicates success or failure in applying the configuration
