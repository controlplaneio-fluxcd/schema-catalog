# schema source: Azure Service Operator v2.20.0 https://github.com/Azure/azure-service-operator
apiVersion <string> enum=app.azure.com/v1api20250101
kind <string> enum=ContainerApp
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.azureName <string>	# AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it doesn't have to be.
spec.configuration <object>	# Configuration: Non versioned Container App configuration properties.
spec.configuration.activeRevisionsMode <string> enum=Multiple|Single	# ActiveRevisionsMode: ActiveRevisionsMode controls how active revisions are handled for the Container app: <list><item>Multiple: multiple revisions can be active.</item><item>Single: Only one revision can be active at a time. Revision weights can not be used in this mode. If no value if provided, this is the default.</item></list>
spec.configuration.dapr <object>	# Dapr: Dapr configuration for the Container App.
spec.configuration.dapr.appId <string>	# AppId: Dapr application identifier
spec.configuration.dapr.appPort <integer>	# AppPort: Tells Dapr which port your application is listening on
spec.configuration.dapr.appProtocol <string> enum=grpc|http	# AppProtocol: Tells Dapr which protocol your application is using. Valid options are http and grpc. Default is http
spec.configuration.dapr.enableApiLogging <boolean>	# EnableApiLogging: Enables API logging for the Dapr sidecar
spec.configuration.dapr.enabled <boolean>	# Enabled: Boolean indicating if the Dapr side car is enabled
spec.configuration.dapr.httpMaxRequestSize <integer>	# HttpMaxRequestSize: Increasing max size of request body http and grpc servers parameter in MB to handle uploading of big files. Default is 4 MB.
spec.configuration.dapr.httpReadBufferSize <integer>	# HttpReadBufferSize: Dapr max size of http header read buffer in KB to handle when sending multi-KB headers. Default is 65KB.
spec.configuration.dapr.logLevel <string> enum=debug|error|info|warn	# LogLevel: Sets the log level for the Dapr sidecar. Allowed values are debug, info, warn, error. Default is info.
spec.configuration.identitySettings <[]object>	# IdentitySettings: Optional settings for Managed Identities that are assigned to the Container App. If a Managed Identity is not specified here, default settings will be used.
spec.configuration.identitySettings[].identityReference <object> (required)	# IdentityReference: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
spec.configuration.identitySettings[].identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.configuration.identitySettings[].identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.configuration.identitySettings[].identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.configuration.identitySettings[].identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.configuration.identitySettings[].identityReference.wellKnownName <string>
spec.configuration.identitySettings[].lifecycle <string> enum=All|Init|Main|None	# Lifecycle: Use to select the lifecycle stages of a Container App during which the Managed Identity should be available.
spec.configuration.ingress <object>	# Ingress: Ingress configurations.
spec.configuration.ingress.additionalPortMappings <[]object>	# AdditionalPortMappings: Settings to expose additional ports on container app
spec.configuration.ingress.additionalPortMappings[].exposedPort <integer>	# ExposedPort: Specifies the exposed port for the target port. If not specified, it defaults to target port
spec.configuration.ingress.additionalPortMappings[].external <boolean> (required)	# External: Specifies whether the app port is accessible outside of the environment
spec.configuration.ingress.additionalPortMappings[].targetPort <integer> (required)	# TargetPort: Specifies the port user's container listens on
spec.configuration.ingress.allowInsecure <boolean>	# AllowInsecure: Bool indicating if HTTP connections to is allowed. If set to false HTTP connections are automatically redirected to HTTPS connections
spec.configuration.ingress.clientCertificateMode <string> enum=accept|ignore|require	# ClientCertificateMode: Client certificate mode for mTLS authentication. Ignore indicates server drops client certificate on forwarding. Accept indicates server forwards client certificate but does not require a client certificate. Require indicates server requires a client certificate.
spec.configuration.ingress.corsPolicy <object>	# CorsPolicy: CORS policy for container app
spec.configuration.ingress.corsPolicy.allowCredentials <boolean>	# AllowCredentials: Specifies whether the resource allows credentials
spec.configuration.ingress.corsPolicy.allowedHeaders <[]string>	# AllowedHeaders: Specifies the content for the access-control-allow-headers header
spec.configuration.ingress.corsPolicy.allowedMethods <[]string>	# AllowedMethods: Specifies the content for the access-control-allow-methods header
spec.configuration.ingress.corsPolicy.allowedOrigins <[]string> (required)	# AllowedOrigins: Specifies the content for the access-control-allow-origins header
spec.configuration.ingress.corsPolicy.exposeHeaders <[]string>	# ExposeHeaders: Specifies the content for the access-control-expose-headers header
spec.configuration.ingress.corsPolicy.maxAge <integer>	# MaxAge: Specifies the content for the access-control-max-age header
spec.configuration.ingress.customDomains <[]object>	# CustomDomains: custom domain bindings for Container Apps' hostnames.
spec.configuration.ingress.customDomains[].bindingType <string> enum=Disabled|SniEnabled	# BindingType: Custom Domain binding type.
spec.configuration.ingress.customDomains[].certificateReference <object>	# CertificateReference: Resource Id of the Certificate to be bound to this hostname. Must exist in the Managed Environment.
spec.configuration.ingress.customDomains[].certificateReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.configuration.ingress.customDomains[].certificateReference.group <string>	# Group is the Kubernetes group of the resource.
spec.configuration.ingress.customDomains[].certificateReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.configuration.ingress.customDomains[].certificateReference.name <string>	# Name is the Kubernetes name of the resource.
spec.configuration.ingress.customDomains[].name <string> (required)	# Name: Hostname.
spec.configuration.ingress.exposedPort <integer>	# ExposedPort: Exposed Port in containers for TCP traffic from ingress
spec.configuration.ingress.external <boolean>	# External: Bool indicating if app exposes an external http endpoint
spec.configuration.ingress.ipSecurityRestrictions <[]object>	# IpSecurityRestrictions: Rules to restrict incoming IP address.
spec.configuration.ingress.ipSecurityRestrictions[].action <string> (required) enum=Allow|Deny	# Action: Allow or Deny rules to determine for incoming IP. Note: Rules can only consist of ALL Allow or ALL Deny
spec.configuration.ingress.ipSecurityRestrictions[].description <string>	# Description: Describe the IP restriction rule that is being sent to the container-app. This is an optional field.
spec.configuration.ingress.ipSecurityRestrictions[].ipAddressRange <string> (required)	# IpAddressRange: CIDR notation to match incoming IP address
spec.configuration.ingress.ipSecurityRestrictions[].name <string> (required)	# Name: Name for the IP restriction rule.
spec.configuration.ingress.stickySessions <object>	# StickySessions: Sticky Sessions for Single Revision Mode
spec.configuration.ingress.stickySessions.affinity <string> enum=none|sticky	# Affinity: Sticky Session Affinity
spec.configuration.ingress.targetPort <integer>	# TargetPort: Target Port in containers for traffic from ingress
spec.configuration.ingress.traffic <[]object>	# Traffic: Traffic weights for app's revisions
spec.configuration.ingress.traffic[].label <string>	# Label: Associates a traffic label with a revision
spec.configuration.ingress.traffic[].latestRevision <boolean>	# LatestRevision: Indicates that the traffic weight belongs to a latest stable revision
spec.configuration.ingress.traffic[].revisionName <string>	# RevisionName: Name of a revision
spec.configuration.ingress.traffic[].weight <integer>	# Weight: Traffic weight assigned to a revision
spec.configuration.ingress.transport <string> enum=auto|http|http2|tcp	# Transport: Ingress transport protocol
spec.configuration.maxInactiveRevisions <integer>	# MaxInactiveRevisions: Optional. Max inactive revisions a Container App can have.
spec.configuration.registries <[]object>	# Registries: Collection of private container registry credentials for containers used by the Container app
spec.configuration.registries[].identityReference <object>	# IdentityReference: A Managed Identity to use to authenticate with Azure Container Registry. For user-assigned identities, use the full user-assigned identity Resource ID. For system-assigned identities, use 'system'
spec.configuration.registries[].identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.configuration.registries[].identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.configuration.registries[].identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.configuration.registries[].identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.configuration.registries[].identityReference.wellKnownName <string>
spec.configuration.registries[].passwordSecretRef <string>	# PasswordSecretRef: The name of the Secret that contains the registry login password
spec.configuration.registries[].server <string>	# Server: Container Registry Server
spec.configuration.registries[].username <string>	# Username: Container Registry Username
spec.configuration.runtime <object>	# Runtime: App runtime configuration for the Container App.
spec.configuration.runtime.java <object>	# Java: Java app configuration
spec.configuration.runtime.java.enableMetrics <boolean>	# EnableMetrics: Enable jmx core metrics for the java app
spec.configuration.secrets <[]object>	# Secrets: Collection of secrets used by a Container app
spec.configuration.secrets[].identityReference <object>	# IdentityReference: Resource ID of a managed identity to authenticate with Azure Key Vault, or System to use a system-assigned identity.
spec.configuration.secrets[].identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.configuration.secrets[].identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.configuration.secrets[].identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.configuration.secrets[].identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.configuration.secrets[].identityReference.wellKnownName <string>
spec.configuration.secrets[].keyVaultUrl <string>	# KeyVaultUrl: Azure Key Vault URL pointing to the secret referenced by the container app.
spec.configuration.secrets[].name <string>	# Name: Secret Name.
spec.configuration.secrets[].value <object>	# Value: Secret Value.
spec.configuration.secrets[].value.key <string> (required)	# Key is the key in the Kubernetes secret being referenced
spec.configuration.secrets[].value.name <string> (required)	# Name is the name of the Kubernetes secret being referenced. The secret must be in the same namespace as the resource
spec.configuration.service <object>	# Service: Container App to be a dev Container App Service
spec.configuration.service.type <string> (required)	# Type: Dev ContainerApp service type
spec.environmentReference <object>	# EnvironmentReference: Resource ID of environment.
spec.environmentReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.environmentReference.group <string>	# Group is the Kubernetes group of the resource.
spec.environmentReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.environmentReference.name <string>	# Name is the Kubernetes name of the resource.
spec.extendedLocation <object>	# ExtendedLocation: The complex type of the extended location.
spec.extendedLocation.name <string>	# Name: The name of the extended location.
spec.extendedLocation.type <string> enum=CustomLocation	# Type: The type of the extended location.
spec.identity <object>	# Identity: managed identities for the Container App to interact with other Azure services without maintaining any secrets or credentials in code.
spec.identity.type <string> (required) enum=None|SystemAssigned|SystemAssigned,UserAssigned|UserAssigned	# Type: Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed).
spec.identity.userAssignedIdentities <[]object>
spec.identity.userAssignedIdentities[].reference <object>	# ResourceReference represents a resource reference, either to a Kubernetes resource or directly to an Azure resource via ARMID
spec.identity.userAssignedIdentities[].reference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.identity.userAssignedIdentities[].reference.group <string>	# Group is the Kubernetes group of the resource.
spec.identity.userAssignedIdentities[].reference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.identity.userAssignedIdentities[].reference.name <string>	# Name is the Kubernetes name of the resource.
spec.location <string> (required)	# Location: The geo-location where the resource lives
spec.managedByReference <object>	# ManagedByReference: The fully qualified resource ID of the resource that manages this resource. Indicates if this resource is managed by another Azure resource. If this is present, complete mode deployment will not delete the resource if it is removed from the template since it is managed by another resource.
spec.managedByReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.managedByReference.group <string>	# Group is the Kubernetes group of the resource.
spec.managedByReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.managedByReference.name <string>	# Name is the Kubernetes name of the resource.
spec.managedEnvironmentReference <object>	# ManagedEnvironmentReference: Deprecated. Resource ID of the Container App's environment.
spec.managedEnvironmentReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.managedEnvironmentReference.group <string>	# Group is the Kubernetes group of the resource.
spec.managedEnvironmentReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.managedEnvironmentReference.name <string>	# Name is the Kubernetes name of the resource.
spec.operatorSpec <object>	# OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not passed directly to Azure
spec.operatorSpec.configMapExpressions <[]object>	# ConfigMapExpressions: configures where to place operator written dynamic ConfigMaps (created with CEL expressions).
spec.operatorSpec.configMapExpressions[].annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the destination resource.
spec.operatorSpec.configMapExpressions[].key <string>	# Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting map[string]string.
spec.operatorSpec.configMapExpressions[].labels <map[string]string>	# Labels is an optional set of labels to apply to the destination resource.
spec.operatorSpec.configMapExpressions[].name <string> (required)	# Name is the name of the Kubernetes configmap or secret to write to. The configmap or secret will be created in the same namespace as the resource.
spec.operatorSpec.configMapExpressions[].value <string> (required)	# Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
spec.operatorSpec.configMaps <object>	# ConfigMaps: configures where to place operator written ConfigMaps.
spec.operatorSpec.configMaps.eventStreamEndpoint <object>	# EventStreamEndpoint: indicates where the EventStreamEndpoint config map should be placed. If omitted, no config map will be created.
spec.operatorSpec.configMaps.eventStreamEndpoint.annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the ConfigMap.
spec.operatorSpec.configMaps.eventStreamEndpoint.key <string> (required)	# Key is the key in the ConfigMap being referenced
spec.operatorSpec.configMaps.eventStreamEndpoint.labels <map[string]string>	# Labels is an optional set of labels to apply to the ConfigMap.
spec.operatorSpec.configMaps.eventStreamEndpoint.name <string> (required)	# Name is the name of the Kubernetes ConfigMap to write to. The ConfigMap will be created in the same namespace as the resource.
spec.operatorSpec.configMaps.fqdn <object>	# Fqdn: indicates where the Fqdn config map should be placed. If omitted, no config map will be created.
spec.operatorSpec.configMaps.fqdn.annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the ConfigMap.
spec.operatorSpec.configMaps.fqdn.key <string> (required)	# Key is the key in the ConfigMap being referenced
spec.operatorSpec.configMaps.fqdn.labels <map[string]string>	# Labels is an optional set of labels to apply to the ConfigMap.
spec.operatorSpec.configMaps.fqdn.name <string> (required)	# Name is the name of the Kubernetes ConfigMap to write to. The ConfigMap will be created in the same namespace as the resource.
spec.operatorSpec.secretExpressions <[]object>	# SecretExpressions: configures where to place operator written dynamic secrets (created with CEL expressions).
spec.operatorSpec.secretExpressions[].annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the destination resource.
spec.operatorSpec.secretExpressions[].key <string>	# Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting map[string]string.
spec.operatorSpec.secretExpressions[].labels <map[string]string>	# Labels is an optional set of labels to apply to the destination resource.
spec.operatorSpec.secretExpressions[].name <string> (required)	# Name is the name of the Kubernetes configmap or secret to write to. The configmap or secret will be created in the same namespace as the resource.
spec.operatorSpec.secretExpressions[].value <string> (required)	# Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
spec.owner <object> (required)	# Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a reference to a resources.azure.com/ResourceGroup resource
spec.owner.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"
spec.owner.name <string>	# This is the name of the Kubernetes resource to reference.
spec.tags <map[string]string>	# Tags: Resource tags.
spec.template <object>	# Template: Container App versioned application definition.
spec.template.containers <[]object>	# Containers: List of container definitions for the Container App.
spec.template.containers[].args <[]string>	# Args: Container start command arguments.
spec.template.containers[].command <[]string>	# Command: Container start command.
spec.template.containers[].env <[]object>	# Env: Container environment variables.
spec.template.containers[].env[].name <string>	# Name: Environment variable name.
spec.template.containers[].env[].secretRef <string>	# SecretRef: Name of the Container App secret from which to pull the environment variable value.
spec.template.containers[].env[].value <string>	# Value: Non-secret environment variable value.
spec.template.containers[].image <string>	# Image: Container image tag.
spec.template.containers[].name <string>	# Name: Custom container name.
spec.template.containers[].probes <[]object>	# Probes: List of probes for the container.
spec.template.containers[].probes[].failureThreshold <integer>	# FailureThreshold: Minimum consecutive failures for the probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. Maximum value is 10.
spec.template.containers[].probes[].httpGet <object>	# HttpGet: HTTPGet specifies the http request to perform.
spec.template.containers[].probes[].httpGet.host <string>	# Host: Host name to connect to, defaults to the pod IP. You probably want to set "Host" in httpHeaders instead.
spec.template.containers[].probes[].httpGet.httpHeaders <[]object>	# HttpHeaders: Custom headers to set in the request. HTTP allows repeated headers.
spec.template.containers[].probes[].httpGet.httpHeaders[].name <string> (required)	# Name: The header field name
spec.template.containers[].probes[].httpGet.httpHeaders[].value <string> (required)	# Value: The header field value
spec.template.containers[].probes[].httpGet.path <string>	# Path: Path to access on the HTTP server.
spec.template.containers[].probes[].httpGet.port <integer> (required)	# Port: Name or number of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
spec.template.containers[].probes[].httpGet.scheme <string> enum=HTTP|HTTPS	# Scheme: Scheme to use for connecting to the host. Defaults to HTTP.
spec.template.containers[].probes[].initialDelaySeconds <integer>	# InitialDelaySeconds: Number of seconds after the container has started before liveness probes are initiated. Minimum value is 1. Maximum value is 60.
spec.template.containers[].probes[].periodSeconds <integer>	# PeriodSeconds: How often (in seconds) to perform the probe. Default to 10 seconds. Minimum value is 1. Maximum value is 240.
spec.template.containers[].probes[].successThreshold <integer>	# SuccessThreshold: Minimum consecutive successes for the probe to be considered successful after having failed. Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1. Maximum value is 10.
spec.template.containers[].probes[].tcpSocket <object>	# TcpSocket: TCPSocket specifies an action involving a TCP port. TCP hooks not yet supported.
spec.template.containers[].probes[].tcpSocket.host <string>	# Host: Optional: Host name to connect to, defaults to the pod IP.
spec.template.containers[].probes[].tcpSocket.port <integer> (required)	# Port: Number or name of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
spec.template.containers[].probes[].terminationGracePeriodSeconds <integer>	# TerminationGracePeriodSeconds: Optional duration in seconds the pod needs to terminate gracefully upon probe failure. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this value overrides the value provided by the pod spec. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). This is an alpha field and requires enabling ProbeTerminationGracePeriod feature gate. Maximum value is 3600 seconds (1 hour)
spec.template.containers[].probes[].timeoutSeconds <integer>	# TimeoutSeconds: Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Maximum value is 240.
spec.template.containers[].probes[].type <string> enum=Liveness|Readiness|Startup	# Type: The type of probe.
spec.template.containers[].resources <object>	# Resources: Container resource requirements.
spec.template.containers[].resources.cpu <number>	# Cpu: Required CPU in cores, e.g. 0.5
spec.template.containers[].resources.memory <string>	# Memory: Required memory, e.g. "250Mb"
spec.template.containers[].volumeMounts <[]object>	# VolumeMounts: Container volume mounts.
spec.template.containers[].volumeMounts[].mountPath <string>	# MountPath: Path within the container at which the volume should be mounted.Must not contain ':'.
spec.template.containers[].volumeMounts[].subPath <string>	# SubPath: Path within the volume from which the container's volume should be mounted. Defaults to "" (volume's root).
spec.template.containers[].volumeMounts[].volumeName <string>	# VolumeName: This must match the Name of a Volume.
spec.template.initContainers <[]object>	# InitContainers: List of specialized containers that run before app containers.
spec.template.initContainers[].args <[]string>	# Args: Container start command arguments.
spec.template.initContainers[].command <[]string>	# Command: Container start command.
spec.template.initContainers[].env <[]object>	# Env: Container environment variables.
spec.template.initContainers[].env[].name <string>	# Name: Environment variable name.
spec.template.initContainers[].env[].secretRef <string>	# SecretRef: Name of the Container App secret from which to pull the environment variable value.
spec.template.initContainers[].env[].value <string>	# Value: Non-secret environment variable value.
spec.template.initContainers[].image <string>	# Image: Container image tag.
spec.template.initContainers[].name <string>	# Name: Custom container name.
spec.template.initContainers[].resources <object>	# Resources: Container resource requirements.
spec.template.initContainers[].resources.cpu <number>	# Cpu: Required CPU in cores, e.g. 0.5
spec.template.initContainers[].resources.memory <string>	# Memory: Required memory, e.g. "250Mb"
spec.template.initContainers[].volumeMounts <[]object>	# VolumeMounts: Container volume mounts.
spec.template.initContainers[].volumeMounts[].mountPath <string>	# MountPath: Path within the container at which the volume should be mounted.Must not contain ':'.
spec.template.initContainers[].volumeMounts[].subPath <string>	# SubPath: Path within the volume from which the container's volume should be mounted. Defaults to "" (volume's root).
spec.template.initContainers[].volumeMounts[].volumeName <string>	# VolumeName: This must match the Name of a Volume.
spec.template.revisionSuffix <string>	# RevisionSuffix: User friendly suffix that is appended to the revision name
spec.template.scale <object>	# Scale: Scaling properties for the Container App.
spec.template.scale.cooldownPeriod <integer>	# CooldownPeriod: Optional. KEDA Cooldown Period in seconds. Defaults to 300 seconds if not set.
spec.template.scale.maxReplicas <integer>	# MaxReplicas: Optional. Maximum number of container replicas. Defaults to 10 if not set.
spec.template.scale.minReplicas <integer>	# MinReplicas: Optional. Minimum number of container replicas.
spec.template.scale.pollingInterval <integer>	# PollingInterval: Optional. KEDA Polling Interval in seconds. Defaults to 30 seconds if not set.
spec.template.scale.rules <[]object>	# Rules: Scaling rules.
spec.template.scale.rules[].azureQueue <object>	# AzureQueue: Azure Queue based scaling.
spec.template.scale.rules[].azureQueue.accountName <string>	# AccountName: Storage account name. required if using managed identity to authenticate
spec.template.scale.rules[].azureQueue.auth <[]object>	# Auth: Authentication secrets for the queue scale rule.
spec.template.scale.rules[].azureQueue.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
spec.template.scale.rules[].azureQueue.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
spec.template.scale.rules[].azureQueue.identityReference <object>	# IdentityReference: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
spec.template.scale.rules[].azureQueue.identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.template.scale.rules[].azureQueue.identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.template.scale.rules[].azureQueue.identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.template.scale.rules[].azureQueue.identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.template.scale.rules[].azureQueue.identityReference.wellKnownName <string>
spec.template.scale.rules[].azureQueue.queueLength <integer>	# QueueLength: Queue length.
spec.template.scale.rules[].azureQueue.queueName <string>	# QueueName: Queue name.
spec.template.scale.rules[].custom <object>	# Custom: Custom scale rule.
spec.template.scale.rules[].custom.auth <[]object>	# Auth: Authentication secrets for the custom scale rule.
spec.template.scale.rules[].custom.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
spec.template.scale.rules[].custom.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
spec.template.scale.rules[].custom.identityReference <object>	# IdentityReference: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
spec.template.scale.rules[].custom.identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.template.scale.rules[].custom.identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.template.scale.rules[].custom.identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.template.scale.rules[].custom.identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.template.scale.rules[].custom.identityReference.wellKnownName <string>
spec.template.scale.rules[].custom.metadata <map[string]string>	# Metadata: Metadata properties to describe custom scale rule.
spec.template.scale.rules[].custom.type <string>	# Type: Type of the custom scale rule eg: azure-servicebus, redis etc.
spec.template.scale.rules[].http <object>	# Http: HTTP requests based scaling.
spec.template.scale.rules[].http.auth <[]object>	# Auth: Authentication secrets for the custom scale rule.
spec.template.scale.rules[].http.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
spec.template.scale.rules[].http.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
spec.template.scale.rules[].http.identityReference <object>	# IdentityReference: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
spec.template.scale.rules[].http.identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.template.scale.rules[].http.identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.template.scale.rules[].http.identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.template.scale.rules[].http.identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.template.scale.rules[].http.identityReference.wellKnownName <string>
spec.template.scale.rules[].http.metadata <map[string]string>	# Metadata: Metadata properties to describe http scale rule.
spec.template.scale.rules[].name <string>	# Name: Scale Rule Name
spec.template.scale.rules[].tcp <object>	# Tcp: Tcp requests based scaling.
spec.template.scale.rules[].tcp.auth <[]object>	# Auth: Authentication secrets for the tcp scale rule.
spec.template.scale.rules[].tcp.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
spec.template.scale.rules[].tcp.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
spec.template.scale.rules[].tcp.identityReference <object>	# IdentityReference: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
spec.template.scale.rules[].tcp.identityReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.template.scale.rules[].tcp.identityReference.group <string>	# Group is the Kubernetes group of the resource.
spec.template.scale.rules[].tcp.identityReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.template.scale.rules[].tcp.identityReference.name <string>	# Name is the Kubernetes name of the resource.
spec.template.scale.rules[].tcp.identityReference.wellKnownName <string>
spec.template.scale.rules[].tcp.metadata <map[string]string>	# Metadata: Metadata properties to describe tcp scale rule.
spec.template.serviceBinds <[]object>	# ServiceBinds: List of container app services bound to the app
spec.template.serviceBinds[].name <string>	# Name: Name of the service bind
spec.template.serviceBinds[].serviceReference <object>	# ServiceReference: Resource id of the target service
spec.template.serviceBinds[].serviceReference.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"	# ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}. The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level ARMID is mutually exclusive with Group, Kind, Namespace and Name.
spec.template.serviceBinds[].serviceReference.group <string>	# Group is the Kubernetes group of the resource.
spec.template.serviceBinds[].serviceReference.kind <string>	# Kind is the Kubernetes kind of the resource.
spec.template.serviceBinds[].serviceReference.name <string>	# Name is the Kubernetes name of the resource.
spec.template.terminationGracePeriodSeconds <integer>	# TerminationGracePeriodSeconds: Optional duration in seconds the Container App Instance needs to terminate gracefully. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). If this value is nil, the default grace period will be used instead. Set this value longer than the expected cleanup time for your process. Defaults to 30 seconds.
spec.template.volumes <[]object>	# Volumes: List of volume definitions for the Container App.
spec.template.volumes[].mountOptions <string>	# MountOptions: Mount options used while mounting the Azure file share or NFS Azure file share. Must be a comma-separated string.
spec.template.volumes[].name <string>	# Name: Volume name.
spec.template.volumes[].secrets <[]object>	# Secrets: List of secrets to be added in volume. If no secrets are provided, all secrets in collection will be added to volume.
spec.template.volumes[].secrets[].path <string>	# Path: Path to project secret to. If no path is provided, path defaults to name of secret listed in secretRef.
spec.template.volumes[].secrets[].secretRef <string>	# SecretRef: Name of the Container App secret from which to pull the secret value.
spec.template.volumes[].storageName <string>	# StorageName: Name of storage resource. No need to provide for EmptyDir and Secret.
spec.template.volumes[].storageType <string> enum=AzureFile|EmptyDir|NfsAzureFile|Secret	# StorageType: Storage type for the volume. If not provided, use EmptyDir.
spec.workloadProfileName <string>	# WorkloadProfileName: Workload profile name to pin for container app execution.
status <object>	# Container App.
status.conditions <[]object>	# Conditions: The observed state of the resource
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Message is a human readable message indicating details about the transition. This field may be empty.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# Reason for the condition's last transition. Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False. This is omitted in all cases when Status == Unknown
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, or Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.configuration <object>	# Configuration: Non versioned Container App configuration properties.
status.configuration.activeRevisionsMode <string>	# ActiveRevisionsMode: ActiveRevisionsMode controls how active revisions are handled for the Container app: <list><item>Multiple: multiple revisions can be active.</item><item>Single: Only one revision can be active at a time. Revision weights can not be used in this mode. If no value if provided, this is the default.</item></list>
status.configuration.dapr <object>	# Dapr: Dapr configuration for the Container App.
status.configuration.dapr.appId <string>	# AppId: Dapr application identifier
status.configuration.dapr.appPort <integer>	# AppPort: Tells Dapr which port your application is listening on
status.configuration.dapr.appProtocol <string>	# AppProtocol: Tells Dapr which protocol your application is using. Valid options are http and grpc. Default is http
status.configuration.dapr.enableApiLogging <boolean>	# EnableApiLogging: Enables API logging for the Dapr sidecar
status.configuration.dapr.enabled <boolean>	# Enabled: Boolean indicating if the Dapr side car is enabled
status.configuration.dapr.httpMaxRequestSize <integer>	# HttpMaxRequestSize: Increasing max size of request body http and grpc servers parameter in MB to handle uploading of big files. Default is 4 MB.
status.configuration.dapr.httpReadBufferSize <integer>	# HttpReadBufferSize: Dapr max size of http header read buffer in KB to handle when sending multi-KB headers. Default is 65KB.
status.configuration.dapr.logLevel <string>	# LogLevel: Sets the log level for the Dapr sidecar. Allowed values are debug, info, warn, error. Default is info.
status.configuration.identitySettings <[]object>	# IdentitySettings: Optional settings for Managed Identities that are assigned to the Container App. If a Managed Identity is not specified here, default settings will be used.
status.configuration.identitySettings[].identity <string>	# Identity: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
status.configuration.identitySettings[].lifecycle <string>	# Lifecycle: Use to select the lifecycle stages of a Container App during which the Managed Identity should be available.
status.configuration.ingress <object>	# Ingress: Ingress configurations.
status.configuration.ingress.additionalPortMappings <[]object>	# AdditionalPortMappings: Settings to expose additional ports on container app
status.configuration.ingress.additionalPortMappings[].exposedPort <integer>	# ExposedPort: Specifies the exposed port for the target port. If not specified, it defaults to target port
status.configuration.ingress.additionalPortMappings[].external <boolean>	# External: Specifies whether the app port is accessible outside of the environment
status.configuration.ingress.additionalPortMappings[].targetPort <integer>	# TargetPort: Specifies the port user's container listens on
status.configuration.ingress.allowInsecure <boolean>	# AllowInsecure: Bool indicating if HTTP connections to is allowed. If set to false HTTP connections are automatically redirected to HTTPS connections
status.configuration.ingress.clientCertificateMode <string>	# ClientCertificateMode: Client certificate mode for mTLS authentication. Ignore indicates server drops client certificate on forwarding. Accept indicates server forwards client certificate but does not require a client certificate. Require indicates server requires a client certificate.
status.configuration.ingress.corsPolicy <object>	# CorsPolicy: CORS policy for container app
status.configuration.ingress.corsPolicy.allowCredentials <boolean>	# AllowCredentials: Specifies whether the resource allows credentials
status.configuration.ingress.corsPolicy.allowedHeaders <[]string>	# AllowedHeaders: Specifies the content for the access-control-allow-headers header
status.configuration.ingress.corsPolicy.allowedMethods <[]string>	# AllowedMethods: Specifies the content for the access-control-allow-methods header
status.configuration.ingress.corsPolicy.allowedOrigins <[]string>	# AllowedOrigins: Specifies the content for the access-control-allow-origins header
status.configuration.ingress.corsPolicy.exposeHeaders <[]string>	# ExposeHeaders: Specifies the content for the access-control-expose-headers header
status.configuration.ingress.corsPolicy.maxAge <integer>	# MaxAge: Specifies the content for the access-control-max-age header
status.configuration.ingress.customDomains <[]object>	# CustomDomains: custom domain bindings for Container Apps' hostnames.
status.configuration.ingress.customDomains[].bindingType <string>	# BindingType: Custom Domain binding type.
status.configuration.ingress.customDomains[].certificateId <string>	# CertificateId: Resource Id of the Certificate to be bound to this hostname. Must exist in the Managed Environment.
status.configuration.ingress.customDomains[].name <string>	# Name: Hostname.
status.configuration.ingress.exposedPort <integer>	# ExposedPort: Exposed Port in containers for TCP traffic from ingress
status.configuration.ingress.external <boolean>	# External: Bool indicating if app exposes an external http endpoint
status.configuration.ingress.fqdn <string>	# Fqdn: Hostname.
status.configuration.ingress.ipSecurityRestrictions <[]object>	# IpSecurityRestrictions: Rules to restrict incoming IP address.
status.configuration.ingress.ipSecurityRestrictions[].action <string>	# Action: Allow or Deny rules to determine for incoming IP. Note: Rules can only consist of ALL Allow or ALL Deny
status.configuration.ingress.ipSecurityRestrictions[].description <string>	# Description: Describe the IP restriction rule that is being sent to the container-app. This is an optional field.
status.configuration.ingress.ipSecurityRestrictions[].ipAddressRange <string>	# IpAddressRange: CIDR notation to match incoming IP address
status.configuration.ingress.ipSecurityRestrictions[].name <string>	# Name: Name for the IP restriction rule.
status.configuration.ingress.stickySessions <object>	# StickySessions: Sticky Sessions for Single Revision Mode
status.configuration.ingress.stickySessions.affinity <string>	# Affinity: Sticky Session Affinity
status.configuration.ingress.targetPort <integer>	# TargetPort: Target Port in containers for traffic from ingress
status.configuration.ingress.traffic <[]object>	# Traffic: Traffic weights for app's revisions
status.configuration.ingress.traffic[].label <string>	# Label: Associates a traffic label with a revision
status.configuration.ingress.traffic[].latestRevision <boolean>	# LatestRevision: Indicates that the traffic weight belongs to a latest stable revision
status.configuration.ingress.traffic[].revisionName <string>	# RevisionName: Name of a revision
status.configuration.ingress.traffic[].weight <integer>	# Weight: Traffic weight assigned to a revision
status.configuration.ingress.transport <string>	# Transport: Ingress transport protocol
status.configuration.maxInactiveRevisions <integer>	# MaxInactiveRevisions: Optional. Max inactive revisions a Container App can have.
status.configuration.registries <[]object>	# Registries: Collection of private container registry credentials for containers used by the Container app
status.configuration.registries[].identity <string>	# Identity: A Managed Identity to use to authenticate with Azure Container Registry. For user-assigned identities, use the full user-assigned identity Resource ID. For system-assigned identities, use 'system'
status.configuration.registries[].passwordSecretRef <string>	# PasswordSecretRef: The name of the Secret that contains the registry login password
status.configuration.registries[].server <string>	# Server: Container Registry Server
status.configuration.registries[].username <string>	# Username: Container Registry Username
status.configuration.runtime <object>	# Runtime: App runtime configuration for the Container App.
status.configuration.runtime.java <object>	# Java: Java app configuration
status.configuration.runtime.java.enableMetrics <boolean>	# EnableMetrics: Enable jmx core metrics for the java app
status.configuration.secrets <[]object>	# Secrets: Collection of secrets used by a Container app
status.configuration.secrets[].identity <string>	# Identity: Resource ID of a managed identity to authenticate with Azure Key Vault, or System to use a system-assigned identity.
status.configuration.secrets[].keyVaultUrl <string>	# KeyVaultUrl: Azure Key Vault URL pointing to the secret referenced by the container app.
status.configuration.secrets[].name <string>	# Name: Secret Name.
status.configuration.service <object>	# Service: Container App to be a dev Container App Service
status.configuration.service.type <string>	# Type: Dev ContainerApp service type
status.customDomainVerificationId <string>	# CustomDomainVerificationId: Id used to verify domain name ownership
status.environmentId <string>	# EnvironmentId: Resource ID of environment.
status.eventStreamEndpoint <string>	# EventStreamEndpoint: The endpoint of the eventstream of the container app.
status.extendedLocation <object>	# ExtendedLocation: The complex type of the extended location.
status.extendedLocation.name <string>	# Name: The name of the extended location.
status.extendedLocation.type <string>	# Type: The type of the extended location.
status.id <string>	# Id: Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
status.identity <object>	# Identity: managed identities for the Container App to interact with other Azure services without maintaining any secrets or credentials in code.
status.identity.principalId <string>	# PrincipalId: The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
status.identity.tenantId <string>	# TenantId: The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
status.identity.type <string>	# Type: Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed).
status.identity.userAssignedIdentities <map[string]object>
status.identity.userAssignedIdentities.<key>.clientId <string>	# ClientId: The client ID of the assigned identity.
status.identity.userAssignedIdentities.<key>.principalId <string>	# PrincipalId: The principal ID of the assigned identity.
status.latestReadyRevisionName <string>	# LatestReadyRevisionName: Name of the latest ready revision of the Container App.
status.latestRevisionFqdn <string>	# LatestRevisionFqdn: Fully Qualified Domain Name of the latest revision of the Container App.
status.latestRevisionName <string>	# LatestRevisionName: Name of the latest revision of the Container App.
status.location <string>	# Location: The geo-location where the resource lives
status.managedBy <string>	# ManagedBy: The fully qualified resource ID of the resource that manages this resource. Indicates if this resource is managed by another Azure resource. If this is present, complete mode deployment will not delete the resource if it is removed from the template since it is managed by another resource.
status.managedEnvironmentId <string>	# ManagedEnvironmentId: Deprecated. Resource ID of the Container App's environment.
status.name <string>	# Name: The name of the resource
status.outboundIpAddresses <[]string>	# OutboundIpAddresses: Outbound IP Addresses for container app.
status.provisioningState <string>	# ProvisioningState: Provisioning state of the Container App.
status.runningStatus <string>	# RunningStatus: Running status of the Container App.
status.systemData <object>	# SystemData: Azure Resource Manager metadata containing createdBy and modifiedBy information.
status.systemData.createdAt <string>	# CreatedAt: The timestamp of resource creation (UTC).
status.systemData.createdBy <string>	# CreatedBy: The identity that created the resource.
status.systemData.createdByType <string>	# CreatedByType: The type of identity that created the resource.
status.systemData.lastModifiedAt <string>	# LastModifiedAt: The timestamp of resource last modification (UTC)
status.systemData.lastModifiedBy <string>	# LastModifiedBy: The identity that last modified the resource.
status.systemData.lastModifiedByType <string>	# LastModifiedByType: The type of identity that last modified the resource.
status.tags <map[string]string>	# Tags: Resource tags.
status.template <object>	# Template: Container App versioned application definition.
status.template.containers <[]object>	# Containers: List of container definitions for the Container App.
status.template.containers[].args <[]string>	# Args: Container start command arguments.
status.template.containers[].command <[]string>	# Command: Container start command.
status.template.containers[].env <[]object>	# Env: Container environment variables.
status.template.containers[].env[].name <string>	# Name: Environment variable name.
status.template.containers[].env[].secretRef <string>	# SecretRef: Name of the Container App secret from which to pull the environment variable value.
status.template.containers[].env[].value <string>	# Value: Non-secret environment variable value.
status.template.containers[].image <string>	# Image: Container image tag.
status.template.containers[].name <string>	# Name: Custom container name.
status.template.containers[].probes <[]object>	# Probes: List of probes for the container.
status.template.containers[].probes[].failureThreshold <integer>	# FailureThreshold: Minimum consecutive failures for the probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. Maximum value is 10.
status.template.containers[].probes[].httpGet <object>	# HttpGet: HTTPGet specifies the http request to perform.
status.template.containers[].probes[].httpGet.host <string>	# Host: Host name to connect to, defaults to the pod IP. You probably want to set "Host" in httpHeaders instead.
status.template.containers[].probes[].httpGet.httpHeaders <[]object>	# HttpHeaders: Custom headers to set in the request. HTTP allows repeated headers.
status.template.containers[].probes[].httpGet.httpHeaders[].name <string>	# Name: The header field name
status.template.containers[].probes[].httpGet.httpHeaders[].value <string>	# Value: The header field value
status.template.containers[].probes[].httpGet.path <string>	# Path: Path to access on the HTTP server.
status.template.containers[].probes[].httpGet.port <integer>	# Port: Name or number of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
status.template.containers[].probes[].httpGet.scheme <string>	# Scheme: Scheme to use for connecting to the host. Defaults to HTTP.
status.template.containers[].probes[].initialDelaySeconds <integer>	# InitialDelaySeconds: Number of seconds after the container has started before liveness probes are initiated. Minimum value is 1. Maximum value is 60.
status.template.containers[].probes[].periodSeconds <integer>	# PeriodSeconds: How often (in seconds) to perform the probe. Default to 10 seconds. Minimum value is 1. Maximum value is 240.
status.template.containers[].probes[].successThreshold <integer>	# SuccessThreshold: Minimum consecutive successes for the probe to be considered successful after having failed. Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1. Maximum value is 10.
status.template.containers[].probes[].tcpSocket <object>	# TcpSocket: TCPSocket specifies an action involving a TCP port. TCP hooks not yet supported.
status.template.containers[].probes[].tcpSocket.host <string>	# Host: Optional: Host name to connect to, defaults to the pod IP.
status.template.containers[].probes[].tcpSocket.port <integer>	# Port: Number or name of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
status.template.containers[].probes[].terminationGracePeriodSeconds <integer>	# TerminationGracePeriodSeconds: Optional duration in seconds the pod needs to terminate gracefully upon probe failure. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this value overrides the value provided by the pod spec. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). This is an alpha field and requires enabling ProbeTerminationGracePeriod feature gate. Maximum value is 3600 seconds (1 hour)
status.template.containers[].probes[].timeoutSeconds <integer>	# TimeoutSeconds: Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Maximum value is 240.
status.template.containers[].probes[].type <string>	# Type: The type of probe.
status.template.containers[].resources <object>	# Resources: Container resource requirements.
status.template.containers[].resources.cpu <number>	# Cpu: Required CPU in cores, e.g. 0.5
status.template.containers[].resources.ephemeralStorage <string>	# EphemeralStorage: Ephemeral Storage, e.g. "1Gi"
status.template.containers[].resources.memory <string>	# Memory: Required memory, e.g. "250Mb"
status.template.containers[].volumeMounts <[]object>	# VolumeMounts: Container volume mounts.
status.template.containers[].volumeMounts[].mountPath <string>	# MountPath: Path within the container at which the volume should be mounted.Must not contain ':'.
status.template.containers[].volumeMounts[].subPath <string>	# SubPath: Path within the volume from which the container's volume should be mounted. Defaults to "" (volume's root).
status.template.containers[].volumeMounts[].volumeName <string>	# VolumeName: This must match the Name of a Volume.
status.template.initContainers <[]object>	# InitContainers: List of specialized containers that run before app containers.
status.template.initContainers[].args <[]string>	# Args: Container start command arguments.
status.template.initContainers[].command <[]string>	# Command: Container start command.
status.template.initContainers[].env <[]object>	# Env: Container environment variables.
status.template.initContainers[].env[].name <string>	# Name: Environment variable name.
status.template.initContainers[].env[].secretRef <string>	# SecretRef: Name of the Container App secret from which to pull the environment variable value.
status.template.initContainers[].env[].value <string>	# Value: Non-secret environment variable value.
status.template.initContainers[].image <string>	# Image: Container image tag.
status.template.initContainers[].name <string>	# Name: Custom container name.
status.template.initContainers[].resources <object>	# Resources: Container resource requirements.
status.template.initContainers[].resources.cpu <number>	# Cpu: Required CPU in cores, e.g. 0.5
status.template.initContainers[].resources.ephemeralStorage <string>	# EphemeralStorage: Ephemeral Storage, e.g. "1Gi"
status.template.initContainers[].resources.memory <string>	# Memory: Required memory, e.g. "250Mb"
status.template.initContainers[].volumeMounts <[]object>	# VolumeMounts: Container volume mounts.
status.template.initContainers[].volumeMounts[].mountPath <string>	# MountPath: Path within the container at which the volume should be mounted.Must not contain ':'.
status.template.initContainers[].volumeMounts[].subPath <string>	# SubPath: Path within the volume from which the container's volume should be mounted. Defaults to "" (volume's root).
status.template.initContainers[].volumeMounts[].volumeName <string>	# VolumeName: This must match the Name of a Volume.
status.template.revisionSuffix <string>	# RevisionSuffix: User friendly suffix that is appended to the revision name
status.template.scale <object>	# Scale: Scaling properties for the Container App.
status.template.scale.cooldownPeriod <integer>	# CooldownPeriod: Optional. KEDA Cooldown Period in seconds. Defaults to 300 seconds if not set.
status.template.scale.maxReplicas <integer>	# MaxReplicas: Optional. Maximum number of container replicas. Defaults to 10 if not set.
status.template.scale.minReplicas <integer>	# MinReplicas: Optional. Minimum number of container replicas.
status.template.scale.pollingInterval <integer>	# PollingInterval: Optional. KEDA Polling Interval in seconds. Defaults to 30 seconds if not set.
status.template.scale.rules <[]object>	# Rules: Scaling rules.
status.template.scale.rules[].azureQueue <object>	# AzureQueue: Azure Queue based scaling.
status.template.scale.rules[].azureQueue.accountName <string>	# AccountName: Storage account name. required if using managed identity to authenticate
status.template.scale.rules[].azureQueue.auth <[]object>	# Auth: Authentication secrets for the queue scale rule.
status.template.scale.rules[].azureQueue.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
status.template.scale.rules[].azureQueue.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
status.template.scale.rules[].azureQueue.identity <string>	# Identity: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
status.template.scale.rules[].azureQueue.queueLength <integer>	# QueueLength: Queue length.
status.template.scale.rules[].azureQueue.queueName <string>	# QueueName: Queue name.
status.template.scale.rules[].custom <object>	# Custom: Custom scale rule.
status.template.scale.rules[].custom.auth <[]object>	# Auth: Authentication secrets for the custom scale rule.
status.template.scale.rules[].custom.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
status.template.scale.rules[].custom.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
status.template.scale.rules[].custom.identity <string>	# Identity: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
status.template.scale.rules[].custom.metadata <map[string]string>	# Metadata: Metadata properties to describe custom scale rule.
status.template.scale.rules[].custom.type <string>	# Type: Type of the custom scale rule eg: azure-servicebus, redis etc.
status.template.scale.rules[].http <object>	# Http: HTTP requests based scaling.
status.template.scale.rules[].http.auth <[]object>	# Auth: Authentication secrets for the custom scale rule.
status.template.scale.rules[].http.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
status.template.scale.rules[].http.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
status.template.scale.rules[].http.identity <string>	# Identity: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
status.template.scale.rules[].http.metadata <map[string]string>	# Metadata: Metadata properties to describe http scale rule.
status.template.scale.rules[].name <string>	# Name: Scale Rule Name
status.template.scale.rules[].tcp <object>	# Tcp: Tcp requests based scaling.
status.template.scale.rules[].tcp.auth <[]object>	# Auth: Authentication secrets for the tcp scale rule.
status.template.scale.rules[].tcp.auth[].secretRef <string>	# SecretRef: Name of the secret from which to pull the auth params.
status.template.scale.rules[].tcp.auth[].triggerParameter <string>	# TriggerParameter: Trigger Parameter that uses the secret
status.template.scale.rules[].tcp.identity <string>	# Identity: The resource ID of a user-assigned managed identity that is assigned to the Container App, or 'system' for system-assigned identity.
status.template.scale.rules[].tcp.metadata <map[string]string>	# Metadata: Metadata properties to describe tcp scale rule.
status.template.serviceBinds <[]object>	# ServiceBinds: List of container app services bound to the app
status.template.serviceBinds[].name <string>	# Name: Name of the service bind
status.template.serviceBinds[].serviceId <string>	# ServiceId: Resource id of the target service
status.template.terminationGracePeriodSeconds <integer>	# TerminationGracePeriodSeconds: Optional duration in seconds the Container App Instance needs to terminate gracefully. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). If this value is nil, the default grace period will be used instead. Set this value longer than the expected cleanup time for your process. Defaults to 30 seconds.
status.template.volumes <[]object>	# Volumes: List of volume definitions for the Container App.
status.template.volumes[].mountOptions <string>	# MountOptions: Mount options used while mounting the Azure file share or NFS Azure file share. Must be a comma-separated string.
status.template.volumes[].name <string>	# Name: Volume name.
status.template.volumes[].secrets <[]object>	# Secrets: List of secrets to be added in volume. If no secrets are provided, all secrets in collection will be added to volume.
status.template.volumes[].secrets[].path <string>	# Path: Path to project secret to. If no path is provided, path defaults to name of secret listed in secretRef.
status.template.volumes[].secrets[].secretRef <string>	# SecretRef: Name of the Container App secret from which to pull the secret value.
status.template.volumes[].storageName <string>	# StorageName: Name of storage resource. No need to provide for EmptyDir and Secret.
status.template.volumes[].storageType <string>	# StorageType: Storage type for the volume. If not provided, use EmptyDir.
status.type <string>	# Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
status.workloadProfileName <string>	# WorkloadProfileName: Workload profile name to pin for container app execution.
