# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=configdelivery.cnrm.cloud.google.com/v1alpha1
kind <string> enum=ConfigDeliveryFleetPackage
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# ConfigDeliveryFleetPackageSpec defines the desired state of ConfigDeliveryFleetPackage
spec.deletionPropagationPolicy <string>	# Optional. Information around how to handle kubernetes resources at the target clusters when the `FleetPackage` is deleted.
spec.location <string> (required) (immutable)	# The location that this resource belongs to.
spec.projectRef <object> (required)	# The Project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceBundleSelector <object>	# Required. Information specifying the source of kubernetes configuration to deploy.
spec.resourceBundleSelector.cloudBuildRepository <object>	# Information specifying `CloudBuildRepository`.
spec.resourceBundleSelector.cloudBuildRepository.path <string>	# Optional. path to the directory or file within the repository that contains the kubernetes configuration. If unspecified, path is assumed to the top level root directory of the repository.
spec.resourceBundleSelector.cloudBuildRepository.repositoryRef <object>	# Required. Name of the cloud build repository. Format is projects/{p}/locations/{l}/connections/{c}/repositories/{r}.
spec.resourceBundleSelector.cloudBuildRepository.repositoryRef.external <string>	# A reference to an externally managed CloudBuildRepository resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/connections/{{connectionID}}/repositories/{{RepositoryID}}".
spec.resourceBundleSelector.cloudBuildRepository.repositoryRef.name <string>	# The name of a CloudBuildRepository resource.
spec.resourceBundleSelector.cloudBuildRepository.repositoryRef.namespace <string>	# The namespace of a CloudBuildRepository resource.
spec.resourceBundleSelector.cloudBuildRepository.serviceAccountRef <object>	# Required. Google service account to use in CloudBuild triggers to fetch and store kubernetes configuration.
spec.resourceBundleSelector.cloudBuildRepository.serviceAccountRef.external <string>	# The `email` field of an `IAMServiceAccount` resource.
spec.resourceBundleSelector.cloudBuildRepository.serviceAccountRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.resourceBundleSelector.cloudBuildRepository.serviceAccountRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.resourceBundleSelector.cloudBuildRepository.tag <string>	# Required. git tag of the underlying git repository. The git tag must be in the semantic version format `vX.Y.Z`.
spec.resourceBundleSelector.cloudBuildRepository.variantsPattern <string>	# Optional. variants_pattern is a glob pattern that will be used to find variants in the repository. Examples: `variants/*.yaml`, `us-*`
spec.resourceBundleSelector.resourceBundle <object>	# Information specifying `ResourceBundle`.
spec.resourceBundleSelector.resourceBundle.resourceBundleRef <object>	# Required. Name of the `ResourceBundle`. Format is projects/{p}/locations/{l}/resourceBundles/{r}.
spec.resourceBundleSelector.resourceBundle.resourceBundleRef.external <string>	# A reference to an externally managed ConfigDeliveryResourceBundle resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/resourceBundles/{{resourceBundleID}}".
spec.resourceBundleSelector.resourceBundle.resourceBundleRef.name <string>	# The name of a ConfigDeliveryResourceBundle resource.
spec.resourceBundleSelector.resourceBundle.resourceBundleRef.namespace <string>	# The namespace of a ConfigDeliveryResourceBundle resource.
spec.resourceBundleSelector.resourceBundle.tag <string>	# Required. Tag refers to a version of the release in a `ResourceBundle`. This is a Git tag in the semantic version format `vX.Y.Z`.
spec.resourceID <string>	# The ConfigDeliveryFleetPackage name. If not given, the metadata.name will be used.
spec.rolloutStrategy <object>	# Optional. The strategy to use to deploy kubernetes configuration to clusters.
spec.rolloutStrategy.allAtOnce <object (free-form)>	# AllAtOnceStrategy causes all clusters to be updated concurrently.
spec.rolloutStrategy.rolling <object>	# RollingStrategy causes a specified number of clusters to be updated concurrently until all clusters are updated.
spec.rolloutStrategy.rolling.maxConcurrent <integer> format=int32	# Optional. Maximum number of clusters to update the resource bundle on concurrently.
spec.state <string>	# Optional. The desired state of the fleet package.
spec.target <object>	# Optional. Configuration to select target clusters to deploy kubernetes configuration to.
spec.target.fleet <object>	# The GKE fleet information.
spec.target.fleet.projectRef <object>	# Required. The host project for the GKE fleet. Format is `projects/{project}`.
spec.target.fleet.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.target.fleet.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.target.fleet.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.target.fleet.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.target.fleet.selector <object>	# Optional. selector allows targeting a subset of fleet members using their labels.
spec.target.fleet.selector.matchLabels <map[string]string>	# Optional. match_labels is a map of {key,value} pairs. Each {key,value} pair must match an existing label key and value exactly in order to satisfy the match.
spec.variantSelector <object>	# Required. Information specifying how to map a `ResourceBundle` variant to a target cluster.
spec.variantSelector.variantNameTemplate <string>	# Required. variant_name_template is a template that can refer to variables containing cluster membership metadata such as location, name, and labels to generate the name of the variant for a target cluster. The variable syntax is similar to the unix shell variables. Available variables are `${membership.name}`, `${membership.location}`, `${membership.project}` and `${membership.labels['label_name']}`. If you want to deploy a specific variant, say "default" to all the clusters, you can use "default" (string without any variables) as the variant_name_template.
status <object>	# ConfigDeliveryFleetPackageStatus defines the config connector machine state of ConfigDeliveryFleetPackage
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the ConfigDeliveryFleetPackage resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.createTime <string>	# Output only. Time at which the `FleetPackage` was created.
status.observedState.info <object>	# Output only. Information containing the rollout status of the `FleetPackage` across all the target clusters.
status.observedState.info.activeRollout <string>	# Optional. The active rollout, if any. Format is `projects/{project}/locations/{location}/fleetPackages/{fleet_package}/rollouts/{rollout}`.
status.observedState.info.errors <[]object>	# Optional. Output only. Errors encountered during configuration deployment (if any).
status.observedState.info.errors[].errorMessage <string>	# Optional. A description of the error.
status.observedState.info.lastCompletedRollout <string>	# Optional. The last completed rollout, if any. Format is `projects/{project}/locations/{location}/fleetPackages/{fleet_package}/rollouts/{rollout}`.
status.observedState.info.state <string>	# Optional. Output only. The current state of the `FleetPackage`.
status.observedState.updateTime <string>	# Output only. Most recent time at which the `FleetPackage` was updated.
