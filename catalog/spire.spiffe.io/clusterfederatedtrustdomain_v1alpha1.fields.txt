# schema source: SPIRE Controller Manager v0.6.6 https://github.com/spiffe/spire-controller-manager
apiVersion <string> enum=spire.spiffe.io/v1alpha1
kind <string> enum=ClusterFederatedTrustDomain (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)	# ClusterFederatedTrustDomainSpec defines the desired state of ClusterFederatedTrustDomain
spec.bundleEndpointProfile <object> (required)	# BundleEndpointProfile is the profile for the bundle endpoint.
spec.bundleEndpointProfile.endpointSPIFFEID <string>	# EndpointSPIFFEID is the SPIFFE ID of the bundle endpoint. It is required for the "https_spiffe" profile.
spec.bundleEndpointProfile.type <string> (required) enum=https_spiffe|https_web	# Type is the type of the bundle endpoint profile.
spec.bundleEndpointURL <string> (required)	# BundleEndpointURL is the URL of the bundle endpoint. It must be an HTTPS URL and cannot contain userinfo (i.e. username/password).
spec.className <string>	# Set which Controller Class will act on this object
spec.trustDomain <string> (required) pattern="[a-z0-9._-]{1,255}"	# TrustDomain is the name of the trust domain to federate with (e.g. example.org)
spec.trustDomainBundle <string>	# TrustDomainBundle is the contents of the bundle for the referenced trust domain. This field is optional when the resource is created.
status <object (free-form)>	# ClusterFederatedTrustDomainStatus defines the observed state of ClusterFederatedTrustDomain
