# schema source: Submariner Operator v0.24.0 https://github.com/submariner-io/submariner-operator
apiVersion <string> enum=submariner.io/v1alpha1
kind <string> enum=Broker
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BrokerSpec defines the desired state of Broker.
spec.clustersetIPCIDRRange <string>	# ClustersetIP supernet range for allocating ClustersetIPCIDRs to each cluster.
spec.clustersetIPEnabled <boolean>	# Enable ClustersetIP default for connecting clusters.
spec.components <[]string>	# List of the components to be installed - any of [service-discovery, connectivity].
spec.defaultCustomDomains <[]string>	# List of domains to use for multi-cluster service discovery.
spec.defaultGlobalnetClusterSize <integer>	# Default cluster size for GlobalCIDR allocated to each cluster (amount of global IPs).
spec.globalnetCIDRRange <string>	# GlobalCIDR supernet range for allocating GlobalCIDRs to each cluster.
spec.globalnetEnabled <boolean>	# Enable support for Overlapping CIDRs in connecting clusters.
status <object (free-form)>	# BrokerStatus defines the observed state of Broker.
