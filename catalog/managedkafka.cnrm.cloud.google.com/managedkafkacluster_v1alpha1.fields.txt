# schema source: GCP Config Connector v1.152.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=managedkafka.cnrm.cloud.google.com/v1alpha1
kind <string> enum=ManagedKafkaCluster
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# ManagedKafkaClusterSpec defines the desired state of ManagedKafkaCluster
spec.capacityConfig <object> (required)	# Required. Capacity configuration for the Kafka cluster.
spec.capacityConfig.memoryBytes <integer> (required) format=int64	# Required. The memory to provision for the cluster in bytes. The CPU:memory ratio (vCPU:GiB) must be between 1:1 and 1:8. Minimum: 3221225472 (3 GiB).
spec.capacityConfig.vcpuCount <integer> (required) format=int64	# Required. The number of vCPUs to provision for the cluster. Minimum: 3.
spec.gcpConfig <object> (required)	# Required. Configuration properties for a Kafka cluster deployed to Google Cloud Platform.
spec.gcpConfig.accessConfig <object> (required)	# Required. Access configuration for the Kafka cluster.
spec.gcpConfig.accessConfig.networkConfigs <[]object> (required)	# Required. Virtual Private Cloud (VPC) networks that must be granted direct access to the Kafka cluster. Minimum of 1 network is required. Maximum 10 networks can be specified.
spec.gcpConfig.accessConfig.networkConfigs[].subnetworkRef <object> (required)	# Required. Reference to the VPC subnet in which to create Private Service Connect (PSC) endpoints for the Kafka brokers and bootstrap address. The subnet must be located in the same region as the Kafka cluster. The project may differ. Multiple subnets from the same parent network must not be specified. The CIDR range of the subnet must be within the IPv4 address ranges for private networks, as specified in RFC 1918.
spec.gcpConfig.accessConfig.networkConfigs[].subnetworkRef.external <string>	# The ComputeSubnetwork selflink of form "projects/{{project}}/regions/{{region}}/subnetworks/{{name}}", when not managed by Config Connector.
spec.gcpConfig.accessConfig.networkConfigs[].subnetworkRef.name <string>	# The `name` field of a `ComputeSubnetwork` resource.
spec.gcpConfig.accessConfig.networkConfigs[].subnetworkRef.namespace <string>	# The `namespace` field of a `ComputeSubnetwork` resource.
spec.gcpConfig.kmsKeyRef <object>	# Optional. Immutable. The Cloud KMS Key name to use for encryption. The key must be located in the same region as the cluster and cannot be changed.
spec.gcpConfig.kmsKeyRef.external <string>	# A reference to an externally managed KMSCryptoKey. Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
spec.gcpConfig.kmsKeyRef.name <string>	# The `name` of a `KMSCryptoKey` resource.
spec.gcpConfig.kmsKeyRef.namespace <string>	# The `namespace` of a `KMSCryptoKey` resource.
spec.labels <map[string]string>	# Optional. Labels as key value pairs.
spec.location <string> (required)
spec.projectRef <object> (required)	# The Project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.rebalanceConfig <object>	# Optional. Rebalance configuration for the Kafka cluster.
spec.rebalanceConfig.mode <string>	# Optional. The rebalance behavior for the cluster. When not specified, defaults to `NO_REBALANCE`.
spec.resourceID <string>	# The GCP resource identifier. If not given, the metadata.name will be used.
status <object>	# ManagedKafkaClusterStatus defines the config connector machine state of ManagedKafkaCluster
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the ManagedKafkaCluster resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.createTime <string>	# Output only. The time when the cluster was created.
status.observedState.state <string>	# Output only. The current state of the cluster.
status.observedState.updateTime <string>	# Output only. The time when the cluster was last updated.
