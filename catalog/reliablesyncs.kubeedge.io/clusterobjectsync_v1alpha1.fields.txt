# schema source: KubeEdge v1.23.0 https://github.com/kubeedge/kubeedge
apiVersion <string> enum=reliablesyncs.kubeedge.io/v1alpha1
kind <string> enum=ClusterObjectSync (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ObjectSyncSpec stores the details of objects that persist to the edge.
spec.objectAPIVersion <string>	# ObjectAPIVersion is the APIVersion of the object that was successfully persist to the edge node.
spec.objectKind <string>	# ObjectType is the kind of the object that was successfully persist to the edge node.
spec.objectName <string>	# ObjectName is the name of the object that was successfully persist to the edge node.
status <object>	# ObjectSyncStatus stores the resourceversion of objects that persist to the edge.
status.objectResourceVersion <string>	# ObjectResourceVersion is the resourceversion of the object that was successfully persist to the edge node.
