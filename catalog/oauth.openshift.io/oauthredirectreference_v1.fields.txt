# schema source: OpenShift v4.22 https://github.com/openshift/api
apiVersion <string> enum=oauth.openshift.io/v1
kind <string> enum=OAuthRedirectReference
metadata.name <string> (required)
metadata.namespace <string>
reference <object> default={}	# The reference to an redirect object in the current namespace.
reference.group <string> (required) default=""	# The group of the target that is being referred to.
reference.kind <string> (required) default=""	# The kind of the target that is being referred to. Currently, only 'Route' is allowed.
reference.name <string> (required) default=""	# The name of the target that is being referred to. e.g. name of the Route.
