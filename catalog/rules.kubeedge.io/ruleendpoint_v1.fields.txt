# schema source: KubeEdge v1.23.1 https://github.com/kubeedge/kubeedge
apiVersion <string> enum=rules.kubeedge.io/v1
kind <string> enum=RuleEndpoint
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.properties <map[string]string>	# properties is not required except for servicebus rule-endpoint type. It is a map value representing rule-endpoint properties. When ruleEndpointType is servicebus, its value is {"service_port":"8080"}. 
spec.ruleEndpointType <string> (required) enum=rest|eventbus|servicebus	# ruleEndpointType is a string value representing rule-endpoint type. its value is one of rest/eventbus/servicebus. 
