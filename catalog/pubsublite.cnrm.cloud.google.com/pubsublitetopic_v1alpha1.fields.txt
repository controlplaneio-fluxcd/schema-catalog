# schema source: GCP Config Connector v1.152.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=pubsublite.cnrm.cloud.google.com/v1alpha1
kind <string> enum=PubSubLiteTopic
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)
spec.partitionConfig <object>	# The settings for this topic's partitions.
spec.partitionConfig.capacity <object>	# The capacity configuration.
spec.partitionConfig.capacity.publishMibPerSec <integer> (required)	# Subscribe throughput capacity per partition in MiB/s. Must be >= 4 and <= 16.
spec.partitionConfig.capacity.subscribeMibPerSec <integer> (required)	# Publish throughput capacity per partition in MiB/s. Must be >= 4 and <= 16.
spec.partitionConfig.count <integer> (required)	# The number of partitions in the topic. Must be at least 1.
spec.projectRef <object> (required)	# The project that this resource belongs to.
spec.projectRef.external <string>	# Allowed value: The `name` field of a `Project` resource.
spec.projectRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.projectRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.region <string>	# The region of the pubsub lite topic.
spec.reservationConfig <object>	# The settings for this topic's Reservation usage.
spec.reservationConfig.throughputReservation <string>	# The Reservation to use for this topic's throughput capacity.
spec.resourceID <string>	# Immutable. Optional. The name of the resource. Used for creation and acquisition. When unset, the value of `metadata.name` is used as the default.
spec.retentionConfig <object>	# The settings for a topic's message retention.
spec.retentionConfig.perPartitionBytes <string> (required)	# The provisioned storage, in bytes, per partition. If the number of bytes stored in any of the topic's partitions grows beyond this value, older messages will be dropped to make room for newer ones, regardless of the value of period.
spec.retentionConfig.period <string>	# How long a published message is retained. If unset, messages will be retained as long as the bytes retained for each partition is below perPartitionBytes. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
spec.zone <string> (required)	# The zone of the pubsub lite topic.
status <object>
status.conditions <[]object>	# Conditions represent the latest available observation of the resource's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <integer>	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
