# schema source: CloudNativePG v1.30.0 https://github.com/cloudnative-pg/cloudnative-pg
apiVersion <string> enum=postgresql.cnpg.io/v1
kind <string> enum=Subscription
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# SubscriptionSpec defines the desired state of Subscription
spec.cluster <object> (required) (immutable)	# The name of the PostgreSQL cluster that identifies the "subscriber"
spec.cluster.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.dbname <string> (required) (immutable)	# The name of the database where the publication will be installed in the "subscriber" cluster
spec.externalClusterName <string> (required)	# The name of the external cluster with the publication ("publisher")
spec.name <string> (required) (immutable)	# The name of the subscription inside PostgreSQL
spec.parameters <map[string]string>	# Subscription parameters included in the `WITH` clause of the PostgreSQL `CREATE SUBSCRIPTION` command. Most parameters cannot be changed after the subscription is created and will be ignored if modified later, except for a limited set documented at: https://www.postgresql.org/docs/current/sql-altersubscription.html#SQL-ALTERSUBSCRIPTION-PARAMS-SET
spec.publicationDBName <string>	# The name of the database containing the publication on the external cluster. Defaults to the one in the external cluster definition.
spec.publicationName <string> (required)	# The name of the publication inside the PostgreSQL database in the "publisher"
spec.subscriptionReclaimPolicy <string> enum=delete|retain default="retain"	# The policy for end-of-life maintenance of this subscription
status <object>	# SubscriptionStatus defines the observed state of Subscription
status.applied <boolean>	# Applied is true if the subscription was reconciled correctly
status.message <string>	# Message is the reconciliation output message
status.observedGeneration <integer> format=int64	# A sequence number representing the latest desired state that was synchronized
