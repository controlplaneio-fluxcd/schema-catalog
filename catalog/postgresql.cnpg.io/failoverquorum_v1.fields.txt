# schema source: CloudNativePG v1.30.0 https://github.com/cloudnative-pg/cloudnative-pg
apiVersion <string> enum=postgresql.cnpg.io/v1
kind <string> enum=FailoverQuorum
metadata.name <string> (required)
metadata.namespace <string> (required)
status <object>	# Most recently observed status of the failover quorum.
status.method <string>	# Contains the latest reported Method value.
status.primary <string>	# Primary is the name of the primary instance that updated this object the latest time.
status.standbyNames <[]string>	# StandbyNames is the list of potentially synchronous instance names.
status.standbyNumber <integer>	# StandbyNumber is the number of synchronous standbys that transactions need to wait for replies from.
