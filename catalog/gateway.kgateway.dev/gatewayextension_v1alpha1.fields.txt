# schema source: kgateway v2.3.6 https://github.com/kgateway-dev/kgateway
apiVersion <string> enum=gateway.kgateway.dev/v1alpha1
kind <string> enum=GatewayExtension
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# GatewayExtensionSpec defines the desired state of GatewayExtension.
spec.extAuth <object>	# ExtAuth configuration for ExtAuth extension type.
spec.extAuth.clearRouteCache <boolean> default=false	# ClearRouteCache determines if the route cache should be cleared to allow the external authentication service to correctly affect routing decisions.
spec.extAuth.failOpen <boolean> default=false	# FailOpen determines if requests are allowed when the ext auth service is unavailable. Defaults to false, meaning requests will be denied if the ext auth service is unavailable.
spec.extAuth.grpcService <object>	# GrpcService is the GRPC service that will handle the auth. Mutually exclusive with HttpService.
spec.extAuth.grpcService.authority <string>	# Authority is the authority header to use for the GRPC service.
spec.extAuth.grpcService.backendRef <object> (required)	# BackendRef references the backend GRPC service.
spec.extAuth.grpcService.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.extAuth.grpcService.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.extAuth.grpcService.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.extAuth.grpcService.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.extAuth.grpcService.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.extAuth.grpcService.backendRef.weight <integer> default=1 format=int32 min=0 max=1000000	# Weight specifies the proportion of requests forwarded to the referenced backend. This is computed as weight/(sum of all weights in this BackendRefs list). For non-zero values, there may be some epsilon from the exact proportion defined here depending on the precision an implementation supports. Weight is not a percentage and the sum of weights does not need to equal 100. If only one backend is specified and it has a weight greater than 0, 100% of the traffic is forwarded to that backend. If weight is set to 0, no traffic should be forwarded for this entry. If unspecified, weight defaults to 1. Support for this field varies based on the context where used.
spec.extAuth.grpcService.requestTimeout <string>	# RequestTimeout is the timeout for the gRPC request. This is the timeout for a specific request.
spec.extAuth.grpcService.retry <object>	# Retry specifies the retry policy for gRPC streams associated with the service.
spec.extAuth.grpcService.retry.attempts <integer> default=1 format=int32 min=0	# Attempts specifies the number of retry attempts for a request. Defaults to 1 attempt if not set. A value of 0 effectively disables retries.
spec.extAuth.grpcService.retry.backoff <object>	# Backoff specifies the retry backoff strategy. If not set, a default backoff with a base interval of 1000ms is used. The default max interval is 10 times the base interval.
spec.extAuth.grpcService.retry.backoff.baseInterval <string> (required)	# BaseInterval specifies the base interval used with a fully jittered exponential back-off between retries.
spec.extAuth.grpcService.retry.backoff.maxInterval <string>	# MaxInterval specifies the maximum interval between retry attempts. Defaults to 10 times the BaseInterval if not set.
spec.extAuth.headersToForward <[]string>	# HeadersToForward specifies which headers from the client request should be forwarded to the external authorization service. HTTP services by default have the following headers forwarded: Host, Method, Path, Content-Length, Authorization. If this field is omitted, gRPC services will have all client request headers forwarded, while HTTP services will only receive the default headers described above. Common examples: ["cookie", "authorization", "x-forwarded-for"] More info is available on the [Envoy docs](https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/filters/http/ext_authz/v3/ext_authz.proto#envoy-v3-api-field-extensions-filters-http-ext-authz-v3-extauthz-allowed-headers)
spec.extAuth.httpService <object>	# HttpService is the HTTP service that will handle the auth. Mutually exclusive with GrpcService.
spec.extAuth.httpService.authorizationRequest <object>	# AuthorizationRequest configures the authorization request to the external service.
spec.extAuth.httpService.authorizationRequest.headersToAdd <map[string]string>	# HeadersToAdd specifies additional headers to add to the authorization request. These headers are sent to the authorization service in addition to the original request headers. Client request headers with the same key will be overridden. The keys are header names and values are envoy format specifiers, see https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/filters/http/ext_authz/v3/ext_authz.proto#envoy-v3-api-field-extensions-filters-http-ext-authz-v3-authorizationrequest-headers-to-add.
spec.extAuth.httpService.authorizationResponse <object>	# AuthorizationResponse configures the authorization response from the external service.
spec.extAuth.httpService.authorizationResponse.headersToBackend <[]string>	# HeadersToBackend specifies which headers from the authorization response should be forwarded to the upstream service when the request is authorized. Common examples: ["x-current-user", "x-user-id", "x-auth-request-email"]
spec.extAuth.httpService.backendRef <object> (required)	# BackendRef references the backend HTTP service.
spec.extAuth.httpService.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.extAuth.httpService.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.extAuth.httpService.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.extAuth.httpService.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.extAuth.httpService.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.extAuth.httpService.backendRef.weight <integer> default=1 format=int32 min=0 max=1000000	# Weight specifies the proportion of requests forwarded to the referenced backend. This is computed as weight/(sum of all weights in this BackendRefs list). For non-zero values, there may be some epsilon from the exact proportion defined here depending on the precision an implementation supports. Weight is not a percentage and the sum of weights does not need to equal 100. If only one backend is specified and it has a weight greater than 0, 100% of the traffic is forwarded to that backend. If weight is set to 0, no traffic should be forwarded for this entry. If unspecified, weight defaults to 1. Support for this field varies based on the context where used.
spec.extAuth.httpService.pathPrefix <string>	# PathPrefix specifies a prefix to the value of the authorization request's path header. This allows customizing the path at which the authorization server expects to receive requests. For example, if the authorization server expects requests at "/verify", set this to "/verify". If not specified, the original request path is used.
spec.extAuth.httpService.requestTimeout <string>	# RequestTimeout is the timeout for the HTTP request. Default timeout is 2 seconds.
spec.extAuth.httpService.retry <object>	# Retry specifies the retry policy for HTTP requests to the authorization service.
spec.extAuth.httpService.retry.attempts <integer> default=1 format=int32 min=0	# Attempts specifies the number of retry attempts for a request. Defaults to 1 attempt if not set. A value of 0 effectively disables retries.
spec.extAuth.httpService.retry.backoff <object>	# Backoff specifies the retry backoff strategy. If not set, a default backoff with a base interval of 1000ms is used. The default max interval is 10 times the base interval.
spec.extAuth.httpService.retry.backoff.baseInterval <string> (required)	# BaseInterval specifies the base interval used with a fully jittered exponential back-off between retries.
spec.extAuth.httpService.retry.backoff.maxInterval <string>	# MaxInterval specifies the maximum interval between retry attempts. Defaults to 10 times the BaseInterval if not set.
spec.extAuth.statPrefix <string> min=1	# StatPrefix is an optional prefix to include when emitting stats from the extauthz filter, enabling different instances of the filter to have unique stats.
spec.extAuth.statusOnError <integer> default=403 format=int32 min=100 max=511	# StatusOnError sets the HTTP status response code that is returned to the client when the auth server returns an error or cannot be reached. Must be in the range of 100-511 inclusive. The default matches the deny response code of 403 Forbidden.
spec.extAuth.withRequestBody <object>	# WithRequestBody allows the request body to be buffered and sent to the auth service. Warning: buffering has implications for streaming and therefore performance.
spec.extAuth.withRequestBody.allowPartialMessage <boolean> default=false	# AllowPartialMessage determines if partial messages should be allowed. When true, requests will be sent to the auth service even if they exceed maxRequestBytes. The default behavior is false.
spec.extAuth.withRequestBody.maxRequestBytes <integer> (required) format=int32 min=1	# MaxRequestBytes sets the maximum size of a message body to buffer. Requests exceeding this size will receive HTTP 413 and not be sent to the auth service.
spec.extAuth.withRequestBody.packAsBytes <boolean> default=false	# PackAsBytes determines if the body should be sent as raw bytes. When true, the body is sent as raw bytes in the raw_body field. When false, the body is sent as UTF-8 string in the body field. The default behavior is false.
spec.extProc <object>	# ExtProc configuration for ExtProc extension type.
spec.extProc.allowProcessingModeOverride <boolean> default=false	# AllowProcessingModeOverride determines if the processing mode can be overridden. Defaults to false, meaning the processing mode cannot be overridden.
spec.extProc.allowedProcessingModeOverrides <[]object>	# AllowedProcessingModeOverrides specifies which processing modes are allowed to override the default. Empty or unspecified means all overrides are allowed (if AllowProcessingModeOverride is true). This is an allowlist; any processing mode in this list will allow the override to the specified mode. If AllowProcessingModeOverride is false, this field is ignored. See: https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/filters/http/ext_proc/v3/ext_proc.proto#envoy-v3-api-field-extensions-filters-http-ext-proc-v3-externalprocessor-allowed-override-modes
spec.extProc.allowedProcessingModeOverrides[].requestBodyMode <string> enum=NONE|STREAMED|BUFFERED|BUFFERED_PARTIAL|FULL_DUPLEX_STREAMED default="NONE"	# RequestBodyMode determines how to handle the request body
spec.extProc.allowedProcessingModeOverrides[].requestHeaderMode <string> enum=DEFAULT|SEND|SKIP default="SEND"	# RequestHeaderMode determines how to handle the request headers
spec.extProc.allowedProcessingModeOverrides[].requestTrailerMode <string> enum=DEFAULT|SEND|SKIP default="SKIP"	# RequestTrailerMode determines how to handle the request trailers
spec.extProc.allowedProcessingModeOverrides[].responseBodyMode <string> enum=NONE|STREAMED|BUFFERED|BUFFERED_PARTIAL|FULL_DUPLEX_STREAMED default="NONE"	# ResponseBodyMode determines how to handle the response body
spec.extProc.allowedProcessingModeOverrides[].responseHeaderMode <string> enum=DEFAULT|SEND|SKIP default="SEND"	# ResponseHeaderMode determines how to handle the response headers
spec.extProc.allowedProcessingModeOverrides[].responseTrailerMode <string> enum=DEFAULT|SEND|SKIP default="SKIP"	# ResponseTrailerMode determines how to handle the response trailers
spec.extProc.failOpen <boolean> default=true	# FailOpen determines if requests are allowed when the ext proc service is unavailable. Defaults to true, meaning requests are allowed upstream even if the ext proc service is unavailable.
spec.extProc.grpcService <object> (required)	# GrpcService is the GRPC service that will handle the processing.
spec.extProc.grpcService.authority <string>	# Authority is the authority header to use for the GRPC service.
spec.extProc.grpcService.backendRef <object> (required)	# BackendRef references the backend GRPC service.
spec.extProc.grpcService.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.extProc.grpcService.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.extProc.grpcService.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.extProc.grpcService.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.extProc.grpcService.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.extProc.grpcService.backendRef.weight <integer> default=1 format=int32 min=0 max=1000000	# Weight specifies the proportion of requests forwarded to the referenced backend. This is computed as weight/(sum of all weights in this BackendRefs list). For non-zero values, there may be some epsilon from the exact proportion defined here depending on the precision an implementation supports. Weight is not a percentage and the sum of weights does not need to equal 100. If only one backend is specified and it has a weight greater than 0, 100% of the traffic is forwarded to that backend. If weight is set to 0, no traffic should be forwarded for this entry. If unspecified, weight defaults to 1. Support for this field varies based on the context where used.
spec.extProc.grpcService.requestTimeout <string>	# RequestTimeout is the timeout for the gRPC request. This is the timeout for a specific request.
spec.extProc.grpcService.retry <object>	# Retry specifies the retry policy for gRPC streams associated with the service.
spec.extProc.grpcService.retry.attempts <integer> default=1 format=int32 min=0	# Attempts specifies the number of retry attempts for a request. Defaults to 1 attempt if not set. A value of 0 effectively disables retries.
spec.extProc.grpcService.retry.backoff <object>	# Backoff specifies the retry backoff strategy. If not set, a default backoff with a base interval of 1000ms is used. The default max interval is 10 times the base interval.
spec.extProc.grpcService.retry.backoff.baseInterval <string> (required)	# BaseInterval specifies the base interval used with a fully jittered exponential back-off between retries.
spec.extProc.grpcService.retry.backoff.maxInterval <string>	# MaxInterval specifies the maximum interval between retry attempts. Defaults to 10 times the BaseInterval if not set.
spec.extProc.maxMessageTimeout <string>	# MaxMessageTimeout specifies the upper bound of override_message_timeout that may be sent from the external processing server. The default value 0, which effectively disables the override_message_timeout API.
spec.extProc.messageTimeout <string>	# MessageTimeout is the timeout for each message sent to the external processing server.
spec.extProc.metadataOptions <object>	# MetadataOptions allows configuring metadata namespaces to forwarded or received from the external processing server.
spec.extProc.metadataOptions.forwarding <object>	# Forwarding defines the typed or untyped dynamic metadata namespaces to forward to the external processing server.
spec.extProc.metadataOptions.forwarding.typed <[]string> min=1
spec.extProc.metadataOptions.forwarding.untyped <[]string> min=1
spec.extProc.processingMode <object>	# ProcessingMode defines how the filter should interact with the request/response streams.
spec.extProc.processingMode.requestBodyMode <string> enum=NONE|STREAMED|BUFFERED|BUFFERED_PARTIAL|FULL_DUPLEX_STREAMED default="NONE"	# RequestBodyMode determines how to handle the request body
spec.extProc.processingMode.requestHeaderMode <string> enum=DEFAULT|SEND|SKIP default="SEND"	# RequestHeaderMode determines how to handle the request headers
spec.extProc.processingMode.requestTrailerMode <string> enum=DEFAULT|SEND|SKIP default="SKIP"	# RequestTrailerMode determines how to handle the request trailers
spec.extProc.processingMode.responseBodyMode <string> enum=NONE|STREAMED|BUFFERED|BUFFERED_PARTIAL|FULL_DUPLEX_STREAMED default="NONE"	# ResponseBodyMode determines how to handle the response body
spec.extProc.processingMode.responseHeaderMode <string> enum=DEFAULT|SEND|SKIP default="SEND"	# ResponseHeaderMode determines how to handle the response headers
spec.extProc.processingMode.responseTrailerMode <string> enum=DEFAULT|SEND|SKIP default="SKIP"	# ResponseTrailerMode determines how to handle the response trailers
spec.extProc.routeCacheAction <string> enum=FromResponse|Clear|Retain default="FromResponse"	# RouteCacheAction describes the route cache action to be taken when an external processor response is received in response to request headers. The default behavior is "FromResponse" which will only clear the route cache when an external processing response has the clear_route_cache field set.
spec.extProc.statPrefix <string> min=1	# StatPrefix is an optional prefix to include when emitting stats from the extproc filter, enabling different instances of the filter to have unique stats.
spec.jwt <object>	# JWT configuration for JWT extension type.
spec.jwt.providers <[]object> max=32	# Providers configures named JWT providers. If multiple providers are specified for a given JWT policy, the providers will be `OR`-ed together and will allow validation to any of the providers.
spec.jwt.providers[].audiences <[]string> min=1 max=32	# Audiences is the list of audiences to be used for the JWT provider. If specified an incoming JWT must have an 'aud' claim, and it must be in this list. If not specified, the audiences will not be checked in the token.
spec.jwt.providers[].claimsToHeaders <[]object> min=1 max=32	# ClaimsToHeaders is the list of claims to headers to be used for the JWT provider. Optionally set the claims from the JWT payload that you want to extract and add as headers to the request before the request is forwarded to the upstream destination. Note: if ClaimsToHeaders is set, the Envoy route cache will be cleared. This allows the JWT filter to correctly affect routing decisions.
spec.jwt.providers[].claimsToHeaders[].header <string> (required) min=1 max=2048	# Header is the header the claim will be copied to, for example, "x-sub".
spec.jwt.providers[].claimsToHeaders[].name <string> (required) min=1 max=2048	# Name is the JWT claim name, for example, "sub".
spec.jwt.providers[].forwardToken <boolean>	# ForwardToken configures if the JWT token is forwarded to the upstream backend. If true, the header containing the token will be forwarded upstream. If false or not set, the header containing the token will be removed.
spec.jwt.providers[].issuer <string> max=2048	# Issuer of the JWT. the 'iss' claim of the JWT must match this.
spec.jwt.providers[].jwks <object> (required)	# JWKS is the source for the JSON Web Keys to be used to validate the JWT.
spec.jwt.providers[].jwks.local <object>	# LocalJWKS configures getting the public keys to validate the JWT from a Kubernetes configmap, or inline (raw string) JWKS.
spec.jwt.providers[].jwks.local.configMapRef <object>	# ConfigMapRef configures storing the JWK in a Kubernetes ConfigMap in the same namespace as the GatewayExtension. The ConfigMap must have a data key named 'jwks' that contains the JWKS.
spec.jwt.providers[].jwks.local.configMapRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.jwt.providers[].jwks.local.inline <string> min=1 max=16384	# Inline is the JWKS as the raw, inline JWKS string This can be an individual key, a key set or a pem block public key
spec.jwt.providers[].jwks.remote <object>	# RemoteJWKS configures getting the public keys to validate the JWT from a remote JWKS server.
spec.jwt.providers[].jwks.remote.backendRef <object> (required)	# BackendRef is reference to the backend of the JWKS server.
spec.jwt.providers[].jwks.remote.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.jwt.providers[].jwks.remote.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.jwt.providers[].jwks.remote.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.jwt.providers[].jwks.remote.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.jwt.providers[].jwks.remote.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.jwt.providers[].jwks.remote.cacheDuration <string>	# Duration after which the cached JWKS expires. If unspecified, the default cache duration is 5 minutes.
spec.jwt.providers[].jwks.remote.url <string> (required) min=1 max=2048	# URL is the URL of the remote JWKS server, it must be a full FQDN with protocol, host and path. For example, https://example.com/keys
spec.jwt.providers[].name <string> (required) min=1 max=253	# Name is the unique name of the JWT provider.
spec.jwt.providers[].tokenSource <object>	# TokenSource configures where to find the JWT of the current provider.
spec.jwt.providers[].tokenSource.header <object>	# HeaderSource configures retrieving token from a header
spec.jwt.providers[].tokenSource.header.header <string> (required) min=1 max=2048	# Header is the name of the header. for example, "Authorization"
spec.jwt.providers[].tokenSource.header.prefix <string> min=1 max=2048	# Prefix before the token. for example, "Bearer "
spec.jwt.providers[].tokenSource.queryParameter <string>	# QueryParameter configures retrieving token from the query parameter
spec.jwt.validationMode <string> enum=Strict|AllowMissing	# ValidationMode configures how JWT validation behaves. If unset or empty, Strict mode is used (JWT is required). If set to AllowMissing, unauthenticated requests without a JWT are allowed through. If using this mode, make sure to consider the security implications and consider using an `RBAC` policy to enforce authorization.
spec.oauth2 <object>	# OAuth2 configuration for OAuth2 extension type.
spec.oauth2.authorizationEndpoint <string> pattern="^https://([a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?\\.)*[a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?(:[0-9]{1,5})?(/[a-zA-Z0-9\\-._~!$&'()*+,;=:@%]*)*/?(\\?[a-zA-Z0-9\\-._~!$&'()*+,;=:@%/?]*)?$"	# AuthorizationEndpoint specifies the endpoint to redirect to for authorization in response to unauthorized requests. If both IssuerURI and this value are specified, the value discovered from the issuer will *not* be used and this takes precedence. Refer to https://datatracker.ietf.org/doc/html/rfc6749#section-3.1 for more details.
spec.oauth2.backendRef <object> (required)	# BackendRef specifies the Backend to use for the OAuth2 provider.
spec.oauth2.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.oauth2.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.oauth2.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.oauth2.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.oauth2.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.oauth2.backendRef.weight <integer> default=1 format=int32 min=0 max=1000000	# Weight specifies the proportion of requests forwarded to the referenced backend. This is computed as weight/(sum of all weights in this BackendRefs list). For non-zero values, there may be some epsilon from the exact proportion defined here depending on the precision an implementation supports. Weight is not a percentage and the sum of weights does not need to equal 100. If only one backend is specified and it has a weight greater than 0, 100% of the traffic is forwarded to that backend. If weight is set to 0, no traffic should be forwarded for this entry. If unspecified, weight defaults to 1. Support for this field varies based on the context where used.
spec.oauth2.cookies <object>	# Cookies specifies the configuration for the OAuth2 cookies.
spec.oauth2.cookies.disableAccessTokenSetCookie <boolean>	# DisableAccessTokenSetCookie specifies whether to disable setting the access token cookie. This can be used when the access token is too large to fit in a cookie. When true, the set-cookie response header for the access token will be omitted.
spec.oauth2.cookies.disableIDTokenSetCookie <boolean>	# DisableIDTokenSetCookie specifies whether to disable setting the ID token cookie. This can be used when the ID token is too large to fit in a cookie. When true, the set-cookie response header for the ID token will be omitted.
spec.oauth2.cookies.disableRefreshTokenSetCookie <boolean>	# DisableRefreshTokenSetCookie specifies whether to disable setting the refresh token cookie. This can be used when the refresh token is too large to fit in a cookie. When true, the set-cookie response header for the refresh token will be omitted.
spec.oauth2.cookies.domain <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9]))*$" min=1 max=253	# CookieDomain specifies the domain to set on the access and ID token cookies. If set, the cookies will be set for the specified domain and all its subdomains. This is useful when requests to subdomains are not required to be re-authenticated after the user has logged into the parent domain. If not set, the cookies will default to the host of the request, not including the subdomains.
spec.oauth2.cookies.names <object>	# Names specifies the names of the cookies used to store the tokens. If not set, the default names will be used.
spec.oauth2.cookies.names.accessToken <string> min=1	# AccessToken specifies the name of the cookie used to store the access token.
spec.oauth2.cookies.names.idToken <string> min=1	# IDToken specifies the name of the cookie used to store the ID token.
spec.oauth2.cookies.sameSite <string> enum=Lax|Strict|None	# SameSite specifies the SameSite attribute for the OAuth2 cookies. If not set, the default is Lax.
spec.oauth2.credentials <object> (required)	# Credentials specifies the Oauth2 client credentials to use for authentication.
spec.oauth2.credentials.clientID <string> (required) min=1	# ClientID specifies the client ID issued to the client during the registration process. Refer to https://datatracker.ietf.org/doc/html/rfc6749#section-2.3.1 for more details.
spec.oauth2.credentials.clientSecretRef <object> (required)	# ClientSecretRef specifies a Secret that contains the client secret stored in the key 'client-secret' to use in the authentication request to obtain the access token. Refer to https://datatracker.ietf.org/doc/html/rfc6749#section-2.3.1 for more details.
spec.oauth2.credentials.clientSecretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.oauth2.denyRedirect <object>	# DenyRedirectMatcher specifies the matcher to match requests that should be denied redirects to the authorization endpoint. Matching requests will receive a 401 Unauthorized response instead of being redirected. This is useful for AJAX requests where redirects should be avoided.
spec.oauth2.denyRedirect.headers <[]object> min=1 max=16	# Headers specifies the list of HTTP headers to match on requests that should be denied redirects.
spec.oauth2.denyRedirect.headers[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, only the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent. When a header is repeated in an HTTP request, it is implementation-specific behavior as to how this is represented. Generally, proxies should follow the guidance from the RFC: https://www.rfc-editor.org/rfc/rfc7230.html#section-3.2.2 regarding processing a repeated header, with special handling for "Set-Cookie".
spec.oauth2.denyRedirect.headers[].type <string> enum=Exact|RegularExpression default="Exact"	# Type specifies how to match against the value of the header. Support: Core (Exact) Support: Implementation-specific (RegularExpression) Since RegularExpression HeaderMatchType has implementation-specific conformance, implementations can support POSIX, PCRE or any other dialects of regular expressions. Please read the implementation's documentation to determine the supported dialect.
spec.oauth2.denyRedirect.headers[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.oauth2.endSessionEndpoint <string> pattern="^https://([a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?\\.)*[a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?(:[0-9]{1,5})?(/[a-zA-Z0-9\\-._~!$&'()*+,;=:@%]*)*/?(\\?[a-zA-Z0-9\\-._~!$&'()*+,;=:@%/?]*)?$"	# EndSessionEndpoint specifies the URL that redirects a user's browser to in order to initiate a single logout across all applications and the OpenID provider. Users are directed to this endpoint when they access the logout path. This should only be set when the OpenID provider supports RP-Initiated Logout and "openid" is included in the list of scopes. If both IssuerURI and this value are specified, the value discovered from the issuer will *not* be used and this takes precedence. Refer to https://openid.net/specs/openid-connect-rpinitiated-1_0.html#RPLogout for more details.
spec.oauth2.forwardAccessToken <boolean>	# ForwardAccessToken specifies whether to forward the access token to the backend service. If set to true, the token is forwarded over a cookie named BearerToken and is also set in the Authorization header. Defaults to false.
spec.oauth2.issuerURI <string> pattern="^https://([a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?\\.)*[a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?(:[0-9]{1,5})?(/[a-zA-Z0-9\\-._~!$&'()*+,;=:@%]*)*/?$"	# IssuerURI specifies the OpenID provider's issuer URL to discover the OpenID provider's configuration. The Issuer must be a URI RFC 3986 [RFC3986] with a scheme component that must be https, a host component, and optionally, port and path components and no query or fragment components. It discovers the authorizationEndpoint, tokenEndpoint, endSessionEndpoint, and jwksURI if specified in the discovery response. Explicit configuration of these options will take precedence over the discovered values. Refer to https://openid.net/specs/openid-connect-discovery-1_0.html#ProviderConfig for more details. Note that the OpenID provider configuration is cached and only refreshed periodically when the GatewayExtension object is reprocessed.
spec.oauth2.jwt <object>	# JWT specifies the configuration for whether and how to process the retrieved access and ID tokens as JSON Web Token.
spec.oauth2.jwt.accessToken <object>	# AccessToken specifies how to process the retrieved access token. This requires the access token cookie to be enabled. Requests missing the token will be rejected. The token will be verified against the provided JWKS. Successfully processed tokens have their payload made available as the 'accessToken' dynamic metadata in the 'envoy.filters.http.jwt_authn' namespace. If omitted, the token will not be attempted to be parsed and verified at all.
spec.oauth2.jwt.accessToken.audiences <[]string> min=1 max=32	# Audiences is the list of audiences to be used for the processed token. If specified the token must have an 'aud' claim, and it must be in this list. If not specified, the audiences will not be checked in the token.
spec.oauth2.jwt.accessToken.claimsToHeaders <[]object> min=1 max=32	# ClaimsToHeaders is a list of claims from the token that should be forwarded upstream as a header. Optionally set the claims from the JWT payload that you want to extract and add as headers to the request before the request is forwarded to the upstream destination. Note: if ClaimsToHeaders is set, the Envoy route cache will be cleared. This allows the JWT filter to correctly affect routing decisions.
spec.oauth2.jwt.accessToken.claimsToHeaders[].header <string> (required) min=1 max=2048	# Header is the header the claim will be copied to, for example, "x-sub".
spec.oauth2.jwt.accessToken.claimsToHeaders[].name <string> (required) min=1 max=2048	# Name is the JWT claim name, for example, "sub".
spec.oauth2.jwt.idToken <object>	# IDToken specifies how to process the retrieved ID token. This requires the ID token cookie to be enabled. Requests missing the token will be rejected. The token will be verified against the provided JWKS. Successfully processed tokens have their payload made available as the 'idToken' dynamic metadata in the 'envoy.filters.http.jwt_authn' namespace. If omitted, the token will not be attempted to be parsed and verified at all.
spec.oauth2.jwt.idToken.audiences <[]string> min=1 max=32	# Audiences is the list of audiences to be used for the processed token. If specified the token must have an 'aud' claim, and it must be in this list. If not specified, the audiences will not be checked in the token.
spec.oauth2.jwt.idToken.claimsToHeaders <[]object> min=1 max=32	# ClaimsToHeaders is a list of claims from the token that should be forwarded upstream as a header. Optionally set the claims from the JWT payload that you want to extract and add as headers to the request before the request is forwarded to the upstream destination. Note: if ClaimsToHeaders is set, the Envoy route cache will be cleared. This allows the JWT filter to correctly affect routing decisions.
spec.oauth2.jwt.idToken.claimsToHeaders[].header <string> (required) min=1 max=2048	# Header is the header the claim will be copied to, for example, "x-sub".
spec.oauth2.jwt.idToken.claimsToHeaders[].name <string> (required) min=1 max=2048	# Name is the JWT claim name, for example, "sub".
spec.oauth2.jwt.jwksURI <string> pattern="^https://([a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?\\.)*[a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?(:[0-9]{1,5})?(/[a-zA-Z0-9\\-._~!$&'()*+,;=:@%]*)*/?(\\?[a-zA-Z0-9\\-._~!$&'()*+,;=:@%/?]*)?$"	# JWKSURI specifies the URL that public keys for validating JWTs should be retrieved from. This must be set if the retrieved access or ID token need to be parsed and IssuerURI is not set for discovery. If both IssuerURI and this value are specified, the value discovered from the issuer will *not* be used and this takes precedence. The URL must point to a valid JWKS definition. Refer to https://datatracker.ietf.org/doc/html/rfc7517#section-5 for more details.
spec.oauth2.logoutPath <string> default="/logout" min=1	# LogoutPath specifies the path to log out a user, clearing their credential cookies. Defaults to /logout.
spec.oauth2.redirectURI <string>	# RedirectURI specifies the URL passed to the authorization endpoint. Defaults to <request-scheme>://<host>/oauth2/redirect, where the URL scheme and host are derived from the original request. Refer to https://datatracker.ietf.org/doc/html/rfc6749#section-3.1.2 for more details.
spec.oauth2.scopes <[]string>	# List of OAuth scopes to be claimed in the authentication request. Defaults to "user" scope if not specified. When using OpenID, the "openid" scope must be included. Refer to https://datatracker.ietf.org/doc/html/rfc6749#section-3.3 for more details.
spec.oauth2.tokenEndpoint <string> pattern="^https://([a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?\\.)*[a-zA-Z0-9]([a-zA-Z0-9\\-]{0,61}[a-zA-Z0-9])?(:[0-9]{1,5})?(/[a-zA-Z0-9\\-._~!$&'()*+,;=:@%]*)*/?(\\?[a-zA-Z0-9\\-._~!$&'()*+,;=:@%/?]*)?$"	# TokenEndpoint specifies the endpoint on the authorization server to retrieve the access token from. If both IssuerURI and this value are specified, the value discovered from the issuer will *not* be used and this takes precedence. Refer to https://datatracker.ietf.org/doc/html/rfc6749#section-3.2 for more details.
spec.rateLimit <object>	# RateLimit configuration for RateLimit extension type.
spec.rateLimit.domain <string> (required)	# Domain identifies a rate limiting configuration for the rate limit service. All rate limit requests must specify a domain, which enables the configuration to be per application without fear of overlap (e.g., "api", "web", "admin").
spec.rateLimit.failOpen <boolean> default=true	# FailOpen determines if requests are limited when the rate limit service is unavailable. Defaults to true, meaning requests are allowed upstream and not limited if the rate limit service is unavailable.
spec.rateLimit.grpcService <object> (required)	# GrpcService is the GRPC service that will handle the rate limiting.
spec.rateLimit.grpcService.authority <string>	# Authority is the authority header to use for the GRPC service.
spec.rateLimit.grpcService.backendRef <object> (required)	# BackendRef references the backend GRPC service.
spec.rateLimit.grpcService.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.rateLimit.grpcService.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.rateLimit.grpcService.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.rateLimit.grpcService.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.rateLimit.grpcService.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.rateLimit.grpcService.backendRef.weight <integer> default=1 format=int32 min=0 max=1000000	# Weight specifies the proportion of requests forwarded to the referenced backend. This is computed as weight/(sum of all weights in this BackendRefs list). For non-zero values, there may be some epsilon from the exact proportion defined here depending on the precision an implementation supports. Weight is not a percentage and the sum of weights does not need to equal 100. If only one backend is specified and it has a weight greater than 0, 100% of the traffic is forwarded to that backend. If weight is set to 0, no traffic should be forwarded for this entry. If unspecified, weight defaults to 1. Support for this field varies based on the context where used.
spec.rateLimit.grpcService.requestTimeout <string>	# RequestTimeout is the timeout for the gRPC request. This is the timeout for a specific request.
spec.rateLimit.grpcService.retry <object>	# Retry specifies the retry policy for gRPC streams associated with the service.
spec.rateLimit.grpcService.retry.attempts <integer> default=1 format=int32 min=0	# Attempts specifies the number of retry attempts for a request. Defaults to 1 attempt if not set. A value of 0 effectively disables retries.
spec.rateLimit.grpcService.retry.backoff <object>	# Backoff specifies the retry backoff strategy. If not set, a default backoff with a base interval of 1000ms is used. The default max interval is 10 times the base interval.
spec.rateLimit.grpcService.retry.backoff.baseInterval <string> (required)	# BaseInterval specifies the base interval used with a fully jittered exponential back-off between retries.
spec.rateLimit.grpcService.retry.backoff.maxInterval <string>	# MaxInterval specifies the maximum interval between retry attempts. Defaults to 10 times the BaseInterval if not set.
spec.rateLimit.timeout <string> default="100ms"	# Timeout provides an optional timeout value for requests to the rate limit service. For rate limiting, prefer using this timeout rather than setting the generic `timeout` on the `GrpcService`. See [envoy issue](https://github.com/envoyproxy/envoy/issues/20070) for more info.
spec.rateLimit.xRateLimitHeaders <string> enum=Off|DraftVersion03 default="Off"	# XRateLimitHeaders configures the standard version to use for X-RateLimit headers emitted. See [envoy docs](https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/filters/http/ratelimit/v3/rate_limit.proto#envoy-v3-api-field-extensions-filters-http-ratelimit-v3-ratelimit-enable-x-ratelimit-headers) for more info. Disabled by default.
spec.type <string> enum=ExtAuth|ExtProc|RateLimit|JWT|OAuth2	# Deprecated: Setting this field has no effect. Type indicates the type of the GatewayExtension to be used.
status <object>	# GatewayExtensionStatus defines the observed state of GatewayExtension.
status.conditions <[]object> max=8	# Conditions is the list of conditions for the GatewayExtension.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
