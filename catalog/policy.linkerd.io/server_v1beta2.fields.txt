# schema source: Linkerd 26.7.1 https://github.com/linkerd/linkerd2
apiVersion <string> enum=policy.linkerd.io/v1beta2
kind <string> enum=Server
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)
spec.externalWorkloadSelector <object>	# Selects ExternalWorkloads in the same namespace. The result of matchLabels and matchExpressions are ANDed. Selects all if empty.
spec.externalWorkloadSelector.matchExpressions <[]object>
spec.externalWorkloadSelector.matchExpressions[].key <string> (required)
spec.externalWorkloadSelector.matchExpressions[].operator <string> (required) enum=In|NotIn|Exists|DoesNotExist
spec.externalWorkloadSelector.matchExpressions[].values <[]string>
spec.externalWorkloadSelector.matchLabels <object (free-form)>
spec.podSelector <object>	# Selects pods in the same namespace. The result of matchLabels and matchExpressions are ANDed. Selects all if empty.
spec.podSelector.matchExpressions <[]object>
spec.podSelector.matchExpressions[].key <string> (required)
spec.podSelector.matchExpressions[].operator <string> (required) enum=In|NotIn|Exists|DoesNotExist
spec.podSelector.matchExpressions[].values <[]string>
spec.podSelector.matchLabels <object (free-form)>
spec.port <int-or-string> (required)	# A port name or number. Must exist in a pod spec.
spec.proxyProtocol <string> default="unknown"	# Configures protocol discovery for inbound connections. Supersedes the `config.linkerd.io/opaque-ports` annotation.
