# schema source: Linkerd 26.6.3 https://github.com/linkerd/linkerd2
apiVersion <string> enum=policy.linkerd.io/v1alpha1
kind <string> enum=MeshTLSAuthentication
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# MeshTLSAuthentication defines a list of authenticated client IDs to be referenced by an `AuthorizationPolicy`. If a client connection has the mutually-authenticated identity that matches ANY of the of the provided identities, the connection is considered authenticated.
spec.identities <[]string> min=1	# Authorizes clients with the provided proxy identity strings (as provided via MTLS) The `*` prefix can be used to match all identities in a domain. An identity string of `*` indicates that all authentication clients are authorized.
spec.identityRefs <[]object> min=1
spec.identityRefs[].group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. When empty, the Kubernetes core API group is inferred."
spec.identityRefs[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the kind of the referent.
spec.identityRefs[].name <string> min=1 max=253	# Name is the name of the referent. When unspecified, this refers to all resources of the specified Group and Kind in the specified namespace.
spec.identityRefs[].namespace <string> max=253	# Name is the name of the referent. When unspecified, this authentication refers to the local namespace.
