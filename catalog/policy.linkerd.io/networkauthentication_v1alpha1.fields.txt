# schema source: Linkerd 26.7.1 https://github.com/linkerd/linkerd2
apiVersion <string> enum=policy.linkerd.io/v1alpha1
kind <string> enum=NetworkAuthentication
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# NetworkAuthentication defines a list of authenticated client networks to be referenced by an `AuthorizationPolicy`. If a client connection originates from ANY of the of the provided networks, the connection is considered authenticated.
spec.networks <[]object> (required)
spec.networks[].cidr <string> (required)	# The CIDR of the network to be authorized.
spec.networks[].except <[]string>	# A list of IP networks/addresses not to be included in the above `cidr`.
