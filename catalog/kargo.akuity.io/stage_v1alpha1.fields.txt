# schema source: Kargo v1.10.8 https://github.com/akuity/kargo
apiVersion <string> enum=kargo.akuity.io/v1alpha1
kind <string> enum=Stage
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Spec describes sources of Freight used by the Stage and how to incorporate Freight into the Stage.
spec.promotionTemplate <object>	# PromotionTemplate describes how to incorporate Freight into the Stage using a Promotion.
spec.promotionTemplate.spec <object> (required)	# PromotionTemplateSpec describes the (partial) specification of a Promotion for a Stage. This is a template that can be used to create a Promotion for a Stage.
spec.promotionTemplate.spec.steps <[]object> min=1	# Steps specifies the directives to be executed as part of a Promotion. The order in which the directives are executed is the order in which they are listed in this field.
spec.promotionTemplate.spec.steps[].as <string>	# As is the alias this step can be referred to as.
spec.promotionTemplate.spec.steps[].config <any>	# Config is opaque configuration for the PromotionStep that is understood only by each PromotionStep's implementation. It is legal to utilize expressions in defining values at any level of this block. See https://docs.kargo.io/user-guide/reference-docs/expressions for details.
spec.promotionTemplate.spec.steps[].continueOnError <boolean>	# ContinueOnError is a boolean value that, if set to true, will cause the Promotion to continue executing the next step even if this step fails. It also will not permit this failure to impact the overall status of the Promotion.
spec.promotionTemplate.spec.steps[].if <string>	# If is an optional expression that, if present, must evaluate to a boolean value. If the expression evaluates to false, the step will be skipped. If the expression does not evaluate to a boolean value, the step will be considered to have failed.
spec.promotionTemplate.spec.steps[].retry <object>	# Retry is the retry policy for this step.
spec.promotionTemplate.spec.steps[].retry.errorThreshold <integer> format=int32	# ErrorThreshold is the number of consecutive times the step must fail (for any reason) before retries are abandoned and the entire Promotion is marked as failed. If this field is set to 0, the effective default will be a step-specific one. If no step-specific default exists (i.e. is also 0), the effective default will be the system-wide default of 1. A value of 1 will cause the Promotion to be marked as failed after just a single failure; i.e. no retries will be attempted. There is no option to specify an infinite number of retries using a value such as -1. In a future release, Kargo is likely to become capable of distinguishing between recoverable and non-recoverable step failures. At that time, it is planned that unrecoverable failures will not be subject to this threshold and will immediately cause the Promotion to be marked as failed without further condition.
spec.promotionTemplate.spec.steps[].retry.timeout <string>	# Timeout is the soft maximum interval in which a step that returns a Running status (which typically indicates it's waiting for something to happen) may be retried. The maximum is a soft one because the check for whether the interval has elapsed occurs AFTER the step has run. This effectively means a step may run ONCE beyond the close of the interval. If this field is set to nil, the effective default will be a step-specific one. If no step-specific default exists (i.e. is also nil), the effective default will be the system-wide default of 0. A value of 0 will cause the step to be retried indefinitely unless the ErrorThreshold is reached.
spec.promotionTemplate.spec.steps[].task <object>	# Task is a reference to a PromotionTask that should be inflated into a Promotion when it is built from a PromotionTemplate.
spec.promotionTemplate.spec.steps[].task.kind <string> enum=PromotionTask|ClusterPromotionTask	# Kind is the type of the PromotionTask. Can be either PromotionTask or ClusterPromotionTask, default is PromotionTask.
spec.promotionTemplate.spec.steps[].task.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name is the name of the (Cluster)PromotionTask.
spec.promotionTemplate.spec.steps[].uses <string> min=1	# Uses identifies a runner that can execute this step.
spec.promotionTemplate.spec.steps[].vars <[]object>	# Vars is a list of variables that can be referenced by expressions in the step's Config. The values override the values specified in the PromotionSpec.
spec.promotionTemplate.spec.steps[].vars[].name <string> (required) pattern="^[a-zA-Z_]\\w*$" min=1	# Name is the name of the variable.
spec.promotionTemplate.spec.steps[].vars[].value <string>	# Value is the value of the variable. It is allowed to utilize expressions in the value. See https://docs.kargo.io/user-guide/reference-docs/expressions for details.
spec.promotionTemplate.spec.vars <[]object>	# Vars is a list of variables that can be referenced by expressions in promotion steps.
spec.promotionTemplate.spec.vars[].name <string> (required) pattern="^[a-zA-Z_]\\w*$" min=1	# Name is the name of the variable.
spec.promotionTemplate.spec.vars[].value <string>	# Value is the value of the variable. It is allowed to utilize expressions in the value. See https://docs.kargo.io/user-guide/reference-docs/expressions for details.
spec.requestedFreight <[]object> (required) min=1	# RequestedFreight expresses the Stage's need for certain pieces of Freight, each having originated from a particular Warehouse. This list must be non-empty. In the common case, a Stage will request Freight having originated from just one specific Warehouse. In advanced cases, requesting Freight from multiple Warehouses provides a method of advancing new artifacts of different types through parallel pipelines at different speeds. This can be useful, for instance, if a Stage is home to multiple microservices that are independently versioned.
spec.requestedFreight[].origin <object> (required)	# Origin specifies from where the requested Freight must have originated. This is a required field.
spec.requestedFreight[].origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
spec.requestedFreight[].origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
spec.requestedFreight[].sources <object> (required)	# Sources describes where the requested Freight may be obtained from. This is a required field.
spec.requestedFreight[].sources.autoPromotionOptions <object>	# AutoPromotionOptions specifies options pertaining to auto-promotion. These settings have no effect if auto-promotion is not enabled for this Stage at the ProjectConfig level.
spec.requestedFreight[].sources.autoPromotionOptions.selectionPolicy <string> enum=NewestFreight|MatchUpstream	# SelectionPolicy specifies the rules for identifying new Freight that is eligible for auto-promotion to this Stage. This field is optional. When left unspecified, the field is implicitly treated as if its value were "NewestFreight". Accepted Values: - "NewestFreight": The newest Freight that is available to the Stage is eligible for auto-promotion. - "MatchUpstream": Only the Freight currently used immediately upstream from this Stage is eligible for auto-promotion. This policy may only be applied when the Stage has exactly one upstream Stage.
spec.requestedFreight[].sources.availabilityStrategy <string> enum=All|OneOf|	# AvailabilityStrategy specifies the semantics for how requested Freight is made available to the Stage. This field is optional. When left unspecified, the field is implicitly treated as if its value were "OneOf". Accepted Values: - "All": Freight must be verified and, if applicable, soaked in all upstream Stages to be considered available for promotion. - "OneOf": Freight must be verified and, if applicable, soaked in at least one upstream Stage to be considered available for promotion. - "": Treated the same as "OneOf".
spec.requestedFreight[].sources.direct <boolean>	# Direct indicates the requested Freight may be obtained directly from the Warehouse from which it originated. If this field's value is false, then the value of the Stages field must be non-empty. i.e. Between the two fields, at least one source must be specified.
spec.requestedFreight[].sources.requiredSoakTime <string> pattern="^([0-9]+(\\.[0-9]+)?(s|m|h))+$"	# RequiredSoakTime specifies a minimum duration for which the requested Freight must have continuously occupied ("soaked in") in an upstream Stage before becoming available for promotion to this Stage. This is an optional field. If nil or zero, no soak time is required. Any soak time requirement is in ADDITION to the requirement that Freight be verified in an upstream Stage to become available for promotion to this Stage, although a manual approval for promotion to this Stage will supersede any soak time requirement.
spec.requestedFreight[].sources.stages <[]string>	# Stages identifies other "upstream" Stages as potential sources of the requested Freight. If this field's value is empty, then the value of the Direct field must be true. i.e. Between the two fields, at least on source must be specified.
spec.shard <string>	# Shard is the name of the shard that this Stage belongs to. This is an optional field. If not specified, the Stage will belong to the default shard. A defaulting webhook will sync the value of the kargo.akuity.io/shard label with the value of this field. When this field is empty, the webhook will ensure that label is absent.
spec.vars <[]object>	# Vars is a list of variables that can be referenced anywhere in the StageSpec that supports expressions. For example, the PromotionTemplate and arguments of the Verification.
spec.vars[].name <string> (required) pattern="^[a-zA-Z_]\\w*$" min=1	# Name is the name of the variable.
spec.vars[].value <string>	# Value is the value of the variable. It is allowed to utilize expressions in the value. See https://docs.kargo.io/user-guide/reference-docs/expressions for details.
spec.verification <object>	# Verification describes how to verify a Stage's current Freight is fit for promotion downstream.
spec.verification.analysisRunMetadata <object>	# AnalysisRunMetadata contains optional metadata that should be applied to all AnalysisRuns.
spec.verification.analysisRunMetadata.annotations <map[string]string>	# Additional annotations to apply to an AnalysisRun.
spec.verification.analysisRunMetadata.labels <map[string]string>	# Additional labels to apply to an AnalysisRun.
spec.verification.analysisTemplates <[]object>	# AnalysisTemplates is a list of AnalysisTemplates from which AnalysisRuns should be created to verify a Stage's current Freight is fit to be promoted downstream.
spec.verification.analysisTemplates[].kind <string> enum=AnalysisTemplate|ClusterAnalysisTemplate	# Kind is the type of the AnalysisTemplate. Can be either AnalysisTemplate or ClusterAnalysisTemplate, default is AnalysisTemplate.
spec.verification.analysisTemplates[].name <string> (required)	# Name is the name of the AnalysisTemplate in the same project/namespace as the Stage.
spec.verification.args <[]object>	# Args lists arguments that should be added to all AnalysisRuns.
spec.verification.args[].name <string> (required)	# Name is the name of the argument.
spec.verification.args[].value <string> (required)	# Value is the value of the argument.
status <object>	# Status describes the Stage's current and recent Freight, health, and more.
status.autoPromotionEnabled <boolean>	# AutoPromotionEnabled indicates whether automatic promotion is enabled for the Stage based on the ProjectConfig.
status.conditions <[]object>	# Conditions contains the last observations of the Stage's current state.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.currentPromotion <object>	# CurrentPromotion is a reference to the currently Running promotion.
status.currentPromotion.finishedAt <string> format=date-time	# FinishedAt is the time at which the Promotion was completed.
status.currentPromotion.freight <object>	# Freight is the freight being promoted.
status.currentPromotion.freight.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.currentPromotion.freight.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.currentPromotion.freight.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.currentPromotion.freight.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.currentPromotion.freight.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.currentPromotion.freight.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.currentPromotion.freight.charts[].name <string>	# Name specifies the name of the chart.
status.currentPromotion.freight.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.currentPromotion.freight.charts[].version <string>	# Version specifies a particular version of the chart.
status.currentPromotion.freight.commits <[]object>	# Commits describes specific Git repository commits.
status.currentPromotion.freight.commits[].author <string>	# Author is the author of the commit.
status.currentPromotion.freight.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.currentPromotion.freight.commits[].committer <string>	# Committer is the person who committed the commit.
status.currentPromotion.freight.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.currentPromotion.freight.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.currentPromotion.freight.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.currentPromotion.freight.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.currentPromotion.freight.images <[]object>	# Images describes specific versions of specific container images.
status.currentPromotion.freight.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.currentPromotion.freight.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.currentPromotion.freight.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.currentPromotion.freight.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.currentPromotion.freight.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.currentPromotion.freight.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.currentPromotion.freight.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.currentPromotion.freight.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.currentPromotion.name <string> (required)	# Name is the name of the Promotion.
status.currentPromotion.status <object>	# Status is the (optional) status of the Promotion.
status.currentPromotion.status.currentStep <integer> format=int64	# CurrentStep is the index of the current promotion step being executed. This permits steps that have already run successfully to be skipped on subsequent reconciliations attempts.
status.currentPromotion.status.finishedAt <string> format=date-time	# FinishedAt is the time when the promotion was completed.
status.currentPromotion.status.freight <object>	# Freight is the detail of the piece of freight that was referenced by this promotion.
status.currentPromotion.status.freight.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.currentPromotion.status.freight.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.currentPromotion.status.freight.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.currentPromotion.status.freight.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.currentPromotion.status.freight.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.currentPromotion.status.freight.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.currentPromotion.status.freight.charts[].name <string>	# Name specifies the name of the chart.
status.currentPromotion.status.freight.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.currentPromotion.status.freight.charts[].version <string>	# Version specifies a particular version of the chart.
status.currentPromotion.status.freight.commits <[]object>	# Commits describes specific Git repository commits.
status.currentPromotion.status.freight.commits[].author <string>	# Author is the author of the commit.
status.currentPromotion.status.freight.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.currentPromotion.status.freight.commits[].committer <string>	# Committer is the person who committed the commit.
status.currentPromotion.status.freight.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.currentPromotion.status.freight.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.currentPromotion.status.freight.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.currentPromotion.status.freight.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.currentPromotion.status.freight.images <[]object>	# Images describes specific versions of specific container images.
status.currentPromotion.status.freight.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.currentPromotion.status.freight.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.currentPromotion.status.freight.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.currentPromotion.status.freight.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.currentPromotion.status.freight.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.currentPromotion.status.freight.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.currentPromotion.status.freight.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.currentPromotion.status.freight.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.currentPromotion.status.freightCollection <object>	# FreightCollection contains the details of the piece of Freight referenced by this Promotion as well as any additional Freight that is carried over from the target Stage's current state.
status.currentPromotion.status.freightCollection.id <string> (required)	# ID is a unique and deterministically calculated identifier for the FreightCollection. It is updated on each use of the UpdateOrPush method.
status.currentPromotion.status.freightCollection.items <map[string]object>	# Freight is a map of FreightReference objects, indexed by their Warehouse origin.
status.currentPromotion.status.freightCollection.items.<key>.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.currentPromotion.status.freightCollection.items.<key>.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.currentPromotion.status.freightCollection.items.<key>.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.currentPromotion.status.freightCollection.items.<key>.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.currentPromotion.status.freightCollection.items.<key>.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.currentPromotion.status.freightCollection.items.<key>.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.currentPromotion.status.freightCollection.items.<key>.charts[].name <string>	# Name specifies the name of the chart.
status.currentPromotion.status.freightCollection.items.<key>.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.currentPromotion.status.freightCollection.items.<key>.charts[].version <string>	# Version specifies a particular version of the chart.
status.currentPromotion.status.freightCollection.items.<key>.commits <[]object>	# Commits describes specific Git repository commits.
status.currentPromotion.status.freightCollection.items.<key>.commits[].author <string>	# Author is the author of the commit.
status.currentPromotion.status.freightCollection.items.<key>.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.currentPromotion.status.freightCollection.items.<key>.commits[].committer <string>	# Committer is the person who committed the commit.
status.currentPromotion.status.freightCollection.items.<key>.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.currentPromotion.status.freightCollection.items.<key>.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.currentPromotion.status.freightCollection.items.<key>.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.currentPromotion.status.freightCollection.items.<key>.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.currentPromotion.status.freightCollection.items.<key>.images <[]object>	# Images describes specific versions of specific container images.
status.currentPromotion.status.freightCollection.items.<key>.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.currentPromotion.status.freightCollection.items.<key>.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.currentPromotion.status.freightCollection.items.<key>.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.currentPromotion.status.freightCollection.items.<key>.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.currentPromotion.status.freightCollection.items.<key>.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.currentPromotion.status.freightCollection.items.<key>.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.currentPromotion.status.freightCollection.items.<key>.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.currentPromotion.status.freightCollection.items.<key>.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.currentPromotion.status.freightCollection.verificationHistory <[]object>	# VerificationHistory is a stack of recent VerificationInfo. By default, the last ten VerificationInfo are stored.
status.currentPromotion.status.freightCollection.verificationHistory[].actor <string>	# Actor is the name of the entity that initiated or aborted the Verification process.
status.currentPromotion.status.freightCollection.verificationHistory[].analysisRun <object>	# AnalysisRun is a reference to the Argo Rollouts AnalysisRun that implements the Verification process.
status.currentPromotion.status.freightCollection.verificationHistory[].analysisRun.name <string> (required)	# Name is the name of the AnalysisRun.
status.currentPromotion.status.freightCollection.verificationHistory[].analysisRun.namespace <string> (required)	# Namespace is the namespace of the AnalysisRun.
status.currentPromotion.status.freightCollection.verificationHistory[].analysisRun.phase <string> (required)	# Phase is the last observed phase of the AnalysisRun referenced by Name.
status.currentPromotion.status.freightCollection.verificationHistory[].finishTime <string> format=date-time	# FinishTime is the time at which the Verification process finished.
status.currentPromotion.status.freightCollection.verificationHistory[].id <string>	# ID is the identifier of the Verification process.
status.currentPromotion.status.freightCollection.verificationHistory[].message <string>	# Message may contain additional information about why the verification process is in its current phase.
status.currentPromotion.status.freightCollection.verificationHistory[].phase <string>	# Phase describes the current phase of the Verification process. Generally, this will be a reflection of the underlying AnalysisRun's phase, however, there are exceptions to this, such as in the case where an AnalysisRun cannot be launched successfully.
status.currentPromotion.status.freightCollection.verificationHistory[].startTime <string> format=date-time	# StartTime is the time at which the Verification process was started.
status.currentPromotion.status.healthChecks <[]object>	# HealthChecks contains the health check directives to be executed after the Promotion has completed.
status.currentPromotion.status.healthChecks[].config <any>	# Config is the configuration for the directive.
status.currentPromotion.status.healthChecks[].uses <string> (required) min=1	# Uses identifies a runner that can execute this step.
status.currentPromotion.status.lastHandledRefresh <string>	# LastHandledRefresh holds the value of the most recent AnnotationKeyRefresh annotation that was handled by the controller. This field can be used to determine whether the request to refresh the resource has been handled.
status.currentPromotion.status.message <string>	# Message is a display message about the promotion, including any errors preventing the Promotion controller from executing this Promotion. i.e. If the Phase field has a value of Failed, this field can be expected to explain why.
status.currentPromotion.status.phase <string>	# Phase describes where the Promotion currently is in its lifecycle.
status.currentPromotion.status.startedAt <string> format=date-time	# StartedAt is the time when the promotion started.
status.currentPromotion.status.state <any>	# State stores the state of the promotion process between reconciliation attempts.
status.currentPromotion.status.stepExecutionMetadata <[]object>	# StepExecutionMetadata tracks metadata pertaining to the execution of individual promotion steps.
status.currentPromotion.status.stepExecutionMetadata[].alias <string>	# Alias is the alias of the step.
status.currentPromotion.status.stepExecutionMetadata[].continueOnError <boolean>	# ContinueOnError is a boolean value that, if set to true, will cause the Promotion to continue executing the next step even if this step fails. It also will not permit this failure to impact the overall status of the Promotion.
status.currentPromotion.status.stepExecutionMetadata[].errorCount <integer> format=int32	# ErrorCount tracks consecutive failed attempts to execute the step.
status.currentPromotion.status.stepExecutionMetadata[].finishedAt <string> format=date-time	# FinishedAt is the time at which the final attempt to execute the step completed.
status.currentPromotion.status.stepExecutionMetadata[].message <string>	# Message is a display message about the step, including any errors.
status.currentPromotion.status.stepExecutionMetadata[].startedAt <string> format=date-time	# StartedAt is the time at which the first attempt to execute the step began.
status.currentPromotion.status.stepExecutionMetadata[].status <string>	# Status is the high-level outcome of the step.
status.freightHistory <[]object>	# FreightHistory is a list of recent Freight selections that were deployed to the Stage. By default, the last ten Freight selections are stored. The first item in the list is the most recent Freight selection and currently deployed to the Stage, subsequent items are older selections.
status.freightHistory[].id <string> (required)	# ID is a unique and deterministically calculated identifier for the FreightCollection. It is updated on each use of the UpdateOrPush method.
status.freightHistory[].items <map[string]object>	# Freight is a map of FreightReference objects, indexed by their Warehouse origin.
status.freightHistory[].items.<key>.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.freightHistory[].items.<key>.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.freightHistory[].items.<key>.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.freightHistory[].items.<key>.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.freightHistory[].items.<key>.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.freightHistory[].items.<key>.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.freightHistory[].items.<key>.charts[].name <string>	# Name specifies the name of the chart.
status.freightHistory[].items.<key>.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.freightHistory[].items.<key>.charts[].version <string>	# Version specifies a particular version of the chart.
status.freightHistory[].items.<key>.commits <[]object>	# Commits describes specific Git repository commits.
status.freightHistory[].items.<key>.commits[].author <string>	# Author is the author of the commit.
status.freightHistory[].items.<key>.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.freightHistory[].items.<key>.commits[].committer <string>	# Committer is the person who committed the commit.
status.freightHistory[].items.<key>.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.freightHistory[].items.<key>.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.freightHistory[].items.<key>.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.freightHistory[].items.<key>.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.freightHistory[].items.<key>.images <[]object>	# Images describes specific versions of specific container images.
status.freightHistory[].items.<key>.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.freightHistory[].items.<key>.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.freightHistory[].items.<key>.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.freightHistory[].items.<key>.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.freightHistory[].items.<key>.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.freightHistory[].items.<key>.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.freightHistory[].items.<key>.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.freightHistory[].items.<key>.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.freightHistory[].verificationHistory <[]object>	# VerificationHistory is a stack of recent VerificationInfo. By default, the last ten VerificationInfo are stored.
status.freightHistory[].verificationHistory[].actor <string>	# Actor is the name of the entity that initiated or aborted the Verification process.
status.freightHistory[].verificationHistory[].analysisRun <object>	# AnalysisRun is a reference to the Argo Rollouts AnalysisRun that implements the Verification process.
status.freightHistory[].verificationHistory[].analysisRun.name <string> (required)	# Name is the name of the AnalysisRun.
status.freightHistory[].verificationHistory[].analysisRun.namespace <string> (required)	# Namespace is the namespace of the AnalysisRun.
status.freightHistory[].verificationHistory[].analysisRun.phase <string> (required)	# Phase is the last observed phase of the AnalysisRun referenced by Name.
status.freightHistory[].verificationHistory[].finishTime <string> format=date-time	# FinishTime is the time at which the Verification process finished.
status.freightHistory[].verificationHistory[].id <string>	# ID is the identifier of the Verification process.
status.freightHistory[].verificationHistory[].message <string>	# Message may contain additional information about why the verification process is in its current phase.
status.freightHistory[].verificationHistory[].phase <string>	# Phase describes the current phase of the Verification process. Generally, this will be a reflection of the underlying AnalysisRun's phase, however, there are exceptions to this, such as in the case where an AnalysisRun cannot be launched successfully.
status.freightHistory[].verificationHistory[].startTime <string> format=date-time	# StartTime is the time at which the Verification process was started.
status.freightSummary <string>	# FreightSummary is human-readable text maintained by the controller that summarizes what Freight is currently deployed to the Stage. For Stages that request a single piece of Freight AND the request has been fulfilled, this field will simply contain the name of the Freight. For Stages that request a single piece of Freight AND the request has NOT been fulfilled, or for Stages that request multiple pieces of Freight, this field will contain a summary of fulfilled/requested Freight. The existence of this field is a workaround for kubectl limitations so that this complex but valuable information can be displayed in a column in response to `kubectl get stages`.
status.health <object>	# Health is the Stage's last observed health.
status.health.config <any>	# Config is the opaque configuration of all health checks performed on this Stage.
status.health.issues <[]string>	# Issues clarifies why a Stage in any state other than Healthy is in that state. This field will always be the empty when a Stage is Healthy.
status.health.output <any>	# Output is the opaque output of all health checks performed on this Stage.
status.health.status <string>	# Status describes the health of the Stage.
status.lastHandledRefresh <string>	# LastHandledRefresh holds the value of the most recent AnnotationKeyRefresh annotation that was handled by the controller. This field can be used to determine whether the request to refresh the resource has been handled.
status.lastPromotion <object>	# LastPromotion is a reference to the last completed promotion.
status.lastPromotion.finishedAt <string> format=date-time	# FinishedAt is the time at which the Promotion was completed.
status.lastPromotion.freight <object>	# Freight is the freight being promoted.
status.lastPromotion.freight.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.lastPromotion.freight.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.lastPromotion.freight.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.lastPromotion.freight.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.lastPromotion.freight.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.lastPromotion.freight.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.lastPromotion.freight.charts[].name <string>	# Name specifies the name of the chart.
status.lastPromotion.freight.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.lastPromotion.freight.charts[].version <string>	# Version specifies a particular version of the chart.
status.lastPromotion.freight.commits <[]object>	# Commits describes specific Git repository commits.
status.lastPromotion.freight.commits[].author <string>	# Author is the author of the commit.
status.lastPromotion.freight.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.lastPromotion.freight.commits[].committer <string>	# Committer is the person who committed the commit.
status.lastPromotion.freight.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.lastPromotion.freight.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.lastPromotion.freight.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.lastPromotion.freight.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.lastPromotion.freight.images <[]object>	# Images describes specific versions of specific container images.
status.lastPromotion.freight.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.lastPromotion.freight.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.lastPromotion.freight.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.lastPromotion.freight.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.lastPromotion.freight.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.lastPromotion.freight.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.lastPromotion.freight.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.lastPromotion.freight.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.lastPromotion.name <string> (required)	# Name is the name of the Promotion.
status.lastPromotion.status <object>	# Status is the (optional) status of the Promotion.
status.lastPromotion.status.currentStep <integer> format=int64	# CurrentStep is the index of the current promotion step being executed. This permits steps that have already run successfully to be skipped on subsequent reconciliations attempts.
status.lastPromotion.status.finishedAt <string> format=date-time	# FinishedAt is the time when the promotion was completed.
status.lastPromotion.status.freight <object>	# Freight is the detail of the piece of freight that was referenced by this promotion.
status.lastPromotion.status.freight.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.lastPromotion.status.freight.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.lastPromotion.status.freight.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.lastPromotion.status.freight.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.lastPromotion.status.freight.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.lastPromotion.status.freight.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.lastPromotion.status.freight.charts[].name <string>	# Name specifies the name of the chart.
status.lastPromotion.status.freight.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.lastPromotion.status.freight.charts[].version <string>	# Version specifies a particular version of the chart.
status.lastPromotion.status.freight.commits <[]object>	# Commits describes specific Git repository commits.
status.lastPromotion.status.freight.commits[].author <string>	# Author is the author of the commit.
status.lastPromotion.status.freight.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.lastPromotion.status.freight.commits[].committer <string>	# Committer is the person who committed the commit.
status.lastPromotion.status.freight.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.lastPromotion.status.freight.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.lastPromotion.status.freight.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.lastPromotion.status.freight.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.lastPromotion.status.freight.images <[]object>	# Images describes specific versions of specific container images.
status.lastPromotion.status.freight.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.lastPromotion.status.freight.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.lastPromotion.status.freight.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.lastPromotion.status.freight.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.lastPromotion.status.freight.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.lastPromotion.status.freight.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.lastPromotion.status.freight.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.lastPromotion.status.freight.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.lastPromotion.status.freightCollection <object>	# FreightCollection contains the details of the piece of Freight referenced by this Promotion as well as any additional Freight that is carried over from the target Stage's current state.
status.lastPromotion.status.freightCollection.id <string> (required)	# ID is a unique and deterministically calculated identifier for the FreightCollection. It is updated on each use of the UpdateOrPush method.
status.lastPromotion.status.freightCollection.items <map[string]object>	# Freight is a map of FreightReference objects, indexed by their Warehouse origin.
status.lastPromotion.status.freightCollection.items.<key>.artifacts <[]object>	# Artifacts describes specific versions of artifacts other than Git repository commits, container images, and Helm charts.
status.lastPromotion.status.freightCollection.items.<key>.artifacts[].artifactType <string> min=1	# ArtifactType specifies the type of artifact this is. Often, but not always, it will be the media type (MIME type) of the artifact referenced by this ArtifactReference.
status.lastPromotion.status.freightCollection.items.<key>.artifacts[].metadata <any>	# Metadata is a JSON object containing a mostly opaque collection of artifact attributes. (It must be an object. It may not be a list or a scalar value.) "Mostly" because Kargo may understand how to interpret some documented, well-known, top-level keys. Those aside, this metadata is only understood by a corresponding Subscriber implementation that created it.
status.lastPromotion.status.freightCollection.items.<key>.artifacts[].subscriptionName <string> (required) min=1	# SubscriptionName is the name of the Subscription that discovered this artifact.
status.lastPromotion.status.freightCollection.items.<key>.artifacts[].version <string> (required) min=1	# Version identifies a specific revision of this artifact.
status.lastPromotion.status.freightCollection.items.<key>.charts <[]object>	# Charts describes specific versions of specific Helm charts.
status.lastPromotion.status.freightCollection.items.<key>.charts[].name <string>	# Name specifies the name of the chart.
status.lastPromotion.status.freightCollection.items.<key>.charts[].repoURL <string>	# RepoURL specifies the URL of a Helm chart repository. Classic chart repositories (using HTTP/S) can contain differently named charts. When this field points to such a repository, the Name field will specify the name of the chart within the repository. In the case of a repository within an OCI registry, the URL implicitly points to a specific chart and the Name field will be empty.
status.lastPromotion.status.freightCollection.items.<key>.charts[].version <string>	# Version specifies a particular version of the chart.
status.lastPromotion.status.freightCollection.items.<key>.commits <[]object>	# Commits describes specific Git repository commits.
status.lastPromotion.status.freightCollection.items.<key>.commits[].author <string>	# Author is the author of the commit.
status.lastPromotion.status.freightCollection.items.<key>.commits[].branch <string>	# Branch denotes the branch of the repository where this commit was found.
status.lastPromotion.status.freightCollection.items.<key>.commits[].committer <string>	# Committer is the person who committed the commit.
status.lastPromotion.status.freightCollection.items.<key>.commits[].id <string>	# ID is the ID of a specific commit in the Git repository specified by RepoURL.
status.lastPromotion.status.freightCollection.items.<key>.commits[].message <string>	# Message is the message associated with the commit. At present, this only contains the first line (subject) of the commit message.
status.lastPromotion.status.freightCollection.items.<key>.commits[].repoURL <string>	# RepoURL is the URL of a Git repository.
status.lastPromotion.status.freightCollection.items.<key>.commits[].tag <string>	# Tag denotes a tag in the repository that matched selection criteria and resolved to this commit.
status.lastPromotion.status.freightCollection.items.<key>.images <[]object>	# Images describes specific versions of specific container images.
status.lastPromotion.status.freightCollection.items.<key>.images[].annotations <map[string]string>	# Annotations is a map of arbitrary metadata for the image.
status.lastPromotion.status.freightCollection.items.<key>.images[].digest <string>	# Digest identifies a specific version of the image in the repository specified by RepoURL. This is a more precise identifier than Tag.
status.lastPromotion.status.freightCollection.items.<key>.images[].repoURL <string>	# RepoURL describes the repository in which the image can be found.
status.lastPromotion.status.freightCollection.items.<key>.images[].tag <string>	# Tag identifies a specific version of the image in the repository specified by RepoURL.
status.lastPromotion.status.freightCollection.items.<key>.name <string>	# Name is a system-assigned identifier derived deterministically from the contents of the Freight. I.e., two pieces of Freight can be compared for equality by comparing their Names.
status.lastPromotion.status.freightCollection.items.<key>.origin <object>	# Origin describes a kind of Freight in terms of its origin.
status.lastPromotion.status.freightCollection.items.<key>.origin.kind <string> (required) enum=Warehouse	# Kind is the kind of resource from which Freight may have originated. At present, this can only be "Warehouse".
status.lastPromotion.status.freightCollection.items.<key>.origin.name <string> (required)	# Name is the name of the resource of the kind indicated by the Kind field from which Freight may originate.
status.lastPromotion.status.freightCollection.verificationHistory <[]object>	# VerificationHistory is a stack of recent VerificationInfo. By default, the last ten VerificationInfo are stored.
status.lastPromotion.status.freightCollection.verificationHistory[].actor <string>	# Actor is the name of the entity that initiated or aborted the Verification process.
status.lastPromotion.status.freightCollection.verificationHistory[].analysisRun <object>	# AnalysisRun is a reference to the Argo Rollouts AnalysisRun that implements the Verification process.
status.lastPromotion.status.freightCollection.verificationHistory[].analysisRun.name <string> (required)	# Name is the name of the AnalysisRun.
status.lastPromotion.status.freightCollection.verificationHistory[].analysisRun.namespace <string> (required)	# Namespace is the namespace of the AnalysisRun.
status.lastPromotion.status.freightCollection.verificationHistory[].analysisRun.phase <string> (required)	# Phase is the last observed phase of the AnalysisRun referenced by Name.
status.lastPromotion.status.freightCollection.verificationHistory[].finishTime <string> format=date-time	# FinishTime is the time at which the Verification process finished.
status.lastPromotion.status.freightCollection.verificationHistory[].id <string>	# ID is the identifier of the Verification process.
status.lastPromotion.status.freightCollection.verificationHistory[].message <string>	# Message may contain additional information about why the verification process is in its current phase.
status.lastPromotion.status.freightCollection.verificationHistory[].phase <string>	# Phase describes the current phase of the Verification process. Generally, this will be a reflection of the underlying AnalysisRun's phase, however, there are exceptions to this, such as in the case where an AnalysisRun cannot be launched successfully.
status.lastPromotion.status.freightCollection.verificationHistory[].startTime <string> format=date-time	# StartTime is the time at which the Verification process was started.
status.lastPromotion.status.healthChecks <[]object>	# HealthChecks contains the health check directives to be executed after the Promotion has completed.
status.lastPromotion.status.healthChecks[].config <any>	# Config is the configuration for the directive.
status.lastPromotion.status.healthChecks[].uses <string> (required) min=1	# Uses identifies a runner that can execute this step.
status.lastPromotion.status.lastHandledRefresh <string>	# LastHandledRefresh holds the value of the most recent AnnotationKeyRefresh annotation that was handled by the controller. This field can be used to determine whether the request to refresh the resource has been handled.
status.lastPromotion.status.message <string>	# Message is a display message about the promotion, including any errors preventing the Promotion controller from executing this Promotion. i.e. If the Phase field has a value of Failed, this field can be expected to explain why.
status.lastPromotion.status.phase <string>	# Phase describes where the Promotion currently is in its lifecycle.
status.lastPromotion.status.startedAt <string> format=date-time	# StartedAt is the time when the promotion started.
status.lastPromotion.status.state <any>	# State stores the state of the promotion process between reconciliation attempts.
status.lastPromotion.status.stepExecutionMetadata <[]object>	# StepExecutionMetadata tracks metadata pertaining to the execution of individual promotion steps.
status.lastPromotion.status.stepExecutionMetadata[].alias <string>	# Alias is the alias of the step.
status.lastPromotion.status.stepExecutionMetadata[].continueOnError <boolean>	# ContinueOnError is a boolean value that, if set to true, will cause the Promotion to continue executing the next step even if this step fails. It also will not permit this failure to impact the overall status of the Promotion.
status.lastPromotion.status.stepExecutionMetadata[].errorCount <integer> format=int32	# ErrorCount tracks consecutive failed attempts to execute the step.
status.lastPromotion.status.stepExecutionMetadata[].finishedAt <string> format=date-time	# FinishedAt is the time at which the final attempt to execute the step completed.
status.lastPromotion.status.stepExecutionMetadata[].message <string>	# Message is a display message about the step, including any errors.
status.lastPromotion.status.stepExecutionMetadata[].startedAt <string> format=date-time	# StartedAt is the time at which the first attempt to execute the step began.
status.lastPromotion.status.stepExecutionMetadata[].status <string>	# Status is the high-level outcome of the step.
status.metadata <object (free-form)>	# Metadata is a map of arbitrary metadata associated with the Stage. This is useful for storing additional information about the Stage that can be shared across promotions, verifications, or other processes.
status.observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that this Stage status was reconciled against.
