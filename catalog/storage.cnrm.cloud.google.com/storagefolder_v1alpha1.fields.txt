# schema source: GCP Config Connector v1.152.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=storage.cnrm.cloud.google.com/v1alpha1
kind <string> enum=StorageFolder
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# StorageFolderSpec defines the desired state of StorageFolder
spec.projectRef <object>	# Required. The host project of the application.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The StorageFolder name. If not given, the metadata.name will be used.
spec.storagebucketRef <object>	# Required. The storage bucket where the folder will be created in.
spec.storagebucketRef.external <string>	# A reference to an externally-managed StorageBucket resource.
spec.storagebucketRef.name <string>	# The name of a StorageBucket resource.
spec.storagebucketRef.namespace <string>	# The namespace of a StorageBucket resource.
status <object>	# StorageFolderStatus defines the config connector machine state of StorageFolder
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the StorageFolder resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.createTime <string>	# Output only. The creation time of the folder.
status.observedState.metageneration <integer> format=int64	# Output only. The version of the metadata for this folder. Used for preconditions and for detecting changes in metadata.
status.observedState.pendingRenameInfo <object>	# Output only. Only present if the folder is part of an ongoing RenameFolder operation. Contains information which can be used to query the operation status. The presence of this field also indicates all write operations are blocked for this folder, including folder, managed folder, and object operations.
status.observedState.pendingRenameInfo.operation <string>	# Output only. The name of the rename operation.
status.observedState.updateTime <string>	# Output only. The modification time of the folder.
