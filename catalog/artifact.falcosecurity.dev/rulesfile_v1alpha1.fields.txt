# schema source: Falco Operator v0.4.1 https://github.com/falcosecurity/falco-operator
apiVersion <string> enum=artifact.falcosecurity.dev/v1alpha1
kind <string> enum=Rulesfile
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# RulesfileSpec defines the desired state of Rulesfile.
spec.configMapRef <object>	# ConfigMapRef specifies a reference to a ConfigMap containing the rules.
spec.configMapRef.name <string> (required)	# Name is the name of the ConfigMap.
spec.inlineRules <any>	# InlineRules specifies the rules as a structured object in YAML format.
spec.ociArtifact <object>	# OCIArtifact specifies the reference to an OCI artifact.
spec.ociArtifact.image <object> (required)	# Image specifies the OCI image coordinates.
spec.ociArtifact.image.repository <string> (required)	# Repository is the OCI repository path (e.g. "falcosecurity/rules/falco-rules").
spec.ociArtifact.image.tag <string> default="latest"	# Tag is the image tag or digest (e.g. "latest" or "sha256:abc...").
spec.ociArtifact.registry <object>	# Registry contains inline registry configuration for authentication, TLS, and hostname.
spec.ociArtifact.registry.auth <object>	# Auth contains authentication configuration.
spec.ociArtifact.registry.auth.secretRef <object>	# SecretRef references a Secret containing registry credentials.
spec.ociArtifact.registry.auth.secretRef.name <string> (required)	# Name is the name of the Secret containing credentials.
spec.ociArtifact.registry.name <string>	# Name is the registry hostname (e.g. "ghcr.io").
spec.ociArtifact.registry.plainHTTP <boolean>	# PlainHTTP allows connections to registries over plain HTTP (no TLS). Mutually exclusive with tls.
spec.ociArtifact.registry.tls <object>	# TLS contains TLS transport configuration. Mutually exclusive with plainHTTP.
spec.ociArtifact.registry.tls.insecureSkipVerify <boolean>	# InsecureSkipVerify disables TLS certificate verification.
spec.priority <integer> default=50 format=int32 min=0 max=99	# Priority specifies the priority of the rulesfile. The higher the value, the higher the priority.
spec.selector <object>	# Selector is used to select the nodes where the rulesfile should be applied.
spec.selector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.selector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.selector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.selector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.selector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
status <object> default={"conditions":[{"lastTransitionTime":"1970-01-01T00:00:00Z","message":"Waiting for controller","reason":"Pending","status":"Unknown","type":"Programmed"}]}	# RulesfileStatus defines the observed state of Rulesfile.
status.conditions <[]object>	# Conditions represent the latest available observations of the Rulesfile's state.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
