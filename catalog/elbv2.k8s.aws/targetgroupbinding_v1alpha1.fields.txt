# schema source: AWS Load Balancer Controller v3.4.2 https://github.com/kubernetes-sigs/aws-load-balancer-controller
apiVersion <string> enum=elbv2.k8s.aws/v1alpha1
kind <string> enum=TargetGroupBinding
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# TargetGroupBindingSpec defines the desired state of TargetGroupBinding
spec.assumeRoleExternalId <string>	# IAM Role ARN to assume when calling AWS APIs. Needed to assume a role in another account and prevent the confused deputy problem. https://docs.aws.amazon.com/IAM/latest/UserGuide/confused-deputy.html
spec.iamRoleArnToAssume <string>	# IAM Role ARN to assume when calling AWS APIs. Useful if the target group is in a different AWS account
spec.multiClusterTargetGroup <boolean>	# MultiClusterTargetGroup Denotes if the TargetGroup is shared among multiple clusters
spec.networking <object>	# networking provides the networking setup for ELBV2 LoadBalancer to access targets in TargetGroup.
spec.networking.ingress <[]object>	# List of ingress rules to allow ELBV2 LoadBalancer to access targets in TargetGroup.
spec.networking.ingress[].from <[]object> (required)	# List of peers which should be able to access the targets in TargetGroup. At least one NetworkingPeer should be specified.
spec.networking.ingress[].from[].ipBlock <object>	# IPBlock defines an IPBlock peer. If specified, none of the other fields can be set.
spec.networking.ingress[].from[].ipBlock.cidr <string> (required)	# CIDR is the network CIDR. Both IPV4 or IPV6 CIDR are accepted.
spec.networking.ingress[].from[].securityGroup <object>	# SecurityGroup defines a SecurityGroup peer. If specified, none of the other fields can be set.
spec.networking.ingress[].from[].securityGroup.groupID <string> (required)	# GroupID is the EC2 SecurityGroupID.
spec.networking.ingress[].ports <[]object> (required)	# List of ports which should be made accessible on the targets in TargetGroup. If ports is empty or unspecified, it defaults to all ports with TCP.
spec.networking.ingress[].ports[].port <int-or-string>	# The port which traffic must match. When NodePort endpoints(instance TargetType) is used, this must be a numerical port. When Port endpoints(ip TargetType) is used, this can be either numerical or named port on pods. if port is unspecified, it defaults to all ports.
spec.networking.ingress[].ports[].protocol <string> enum=TCP|UDP	# The protocol which traffic must match. If protocol is unspecified, it defaults to TCP.
spec.serviceRef <object> (required)	# serviceRef is a reference to a Kubernetes Service and ServicePort.
spec.serviceRef.name <string> (required)	# Name is the name of the Service.
spec.serviceRef.port <int-or-string> (required)	# Port is the port of the ServicePort.
spec.targetGroupARN <string>	# targetGroupARN is the Amazon Resource Name (ARN) for the TargetGroup.
spec.targetGroupName <string>	# targetGroupName is the Name of the TargetGroup.
spec.targetType <string> enum=instance|ip	# targetType is the TargetType of TargetGroup. If unspecified, it will be automatically inferred.
status <object>	# TargetGroupBindingStatus defines the observed state of TargetGroupBinding
status.observedGeneration <integer> format=int64	# The generation observed by the TargetGroupBinding controller.
