# schema source: Altinity ClickHouse Operator 0.27.3 https://github.com/Altinity/clickhouse-operator
apiVersion <string> enum=clickhouse.altinity.com/v1
kind <string> enum=ClickHouseInstallation
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Specification of the desired behavior of one or more ClickHouse clusters More info: https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md 
spec.configuration <object>	# allows configure multiple aspects and behavior for `clickhouse-server` instance and also allows describe multiple `clickhouse-server` clusters inside one `chi` resource
spec.configuration.clusters <[]object>	# describes clusters layout and allows change settings on cluster-level, shard-level and replica-level every cluster is a set of StatefulSet, one StatefulSet contains only one Pod with `clickhouse-server` all Pods will rendered in <remote_server> part of ClickHouse configs, mounted from ConfigMap as `/etc/clickhouse-server/config.d/chop-generated-remote_servers.xml` Clusters will use for Distributed table engine, more details: https://clickhouse.tech/docs/en/engines/table-engines/special/distributed/ If `cluster` contains zookeeper settings (could be inherited from top `chi` level), when you can create *ReplicatedMergeTree tables 
spec.configuration.clusters[].files <object (free-form)>	# optional, allows define content of any setting file inside each `Pod` on current cluster during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` override top-level `chi.spec.configuration.files` 
spec.configuration.clusters[].insecure <any>	# optional, open insecure ports for cluster, defaults to "yes"
spec.configuration.clusters[].layout <object>	# describe current cluster layout, how much shards in cluster, how much replica in shard allows override settings on each shard and replica separatelly 
spec.configuration.clusters[].layout.replicas <[]object>	# optional, allows override top-level `chi.spec.configuration` and cluster-level `chi.spec.configuration.clusters` configuration for each replica and each shard relates to selected replica, use it only if you fully understand what you do
spec.configuration.clusters[].layout.replicas[].files <object (free-form)>	# optional, allows define content of any setting file inside each `Pod` only in one replica during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` override top-level `chi.spec.configuration.files` and cluster-level `chi.spec.configuration.clusters.files`, will ignore if `chi.spec.configuration.clusters.layout.shards` presents 
spec.configuration.clusters[].layout.replicas[].name <string> pattern="^[a-zA-Z0-9-]{0,15}$" min=1 max=15	# optional, by default replica name is generated, but you can override it and setup custom name
spec.configuration.clusters[].layout.replicas[].settings <object (free-form)>	# optional, allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in `Pod` only in one replica during generate `ConfigMap` which will mount in `/etc/clickhouse-server/conf.d/` override top-level `chi.spec.configuration.settings`, cluster-level `chi.spec.configuration.clusters.settings` and will ignore if shard-level `chi.spec.configuration.clusters.layout.shards` present More details: https://clickhouse.tech/docs/en/operations/settings/settings/ 
spec.configuration.clusters[].layout.replicas[].shards <[]object>	# optional, list of shards related to current replica, will ignore if `chi.spec.configuration.clusters.layout.shards` presents
spec.configuration.clusters[].layout.replicas[].shards[].files <object (free-form)>	# optional, allows define content of any setting file inside each `Pod` only in one shard related to current replica during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` override top-level `chi.spec.configuration.files` and cluster-level `chi.spec.configuration.clusters.files`, will ignore if `chi.spec.configuration.clusters.layout.shards` presents 
spec.configuration.clusters[].layout.replicas[].shards[].httpPort <integer> min=1 max=65535	# optional, setup `Pod.spec.containers.ports` with name `http` for selected shard, override `chi.spec.templates.hostTemplates.spec.httpPort` allows connect to `clickhouse-server` via HTTP protocol via kubernetes `Service` 
spec.configuration.clusters[].layout.replicas[].shards[].httpsPort <integer> min=1 max=65535
spec.configuration.clusters[].layout.replicas[].shards[].insecure <any>	# optional, open insecure ports for cluster, defaults to "yes" 
spec.configuration.clusters[].layout.replicas[].shards[].interserverHTTPPort <integer> min=1 max=65535	# optional, setup `Pod.spec.containers.ports` with name `interserver` for selected shard, override `chi.spec.templates.hostTemplates.spec.interserverHTTPPort` allows connect between replicas inside same shard during fetch replicated data parts HTTP protocol 
spec.configuration.clusters[].layout.replicas[].shards[].name <string> pattern="^[a-zA-Z0-9-]{0,15}$" min=1 max=15	# optional, by default shard name is generated, but you can override it and setup custom name
spec.configuration.clusters[].layout.replicas[].shards[].secure <any>	# optional, open secure ports 
spec.configuration.clusters[].layout.replicas[].shards[].settings <object (free-form)>	# optional, allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in `Pod` only in one shard related to current replica during generate `ConfigMap` which will mount in `/etc/clickhouse-server/conf.d/` override top-level `chi.spec.configuration.settings`, cluster-level `chi.spec.configuration.clusters.settings` and replica-level `chi.spec.configuration.clusters.layout.replicas.settings` More details: https://clickhouse.tech/docs/en/operations/settings/settings/ 
spec.configuration.clusters[].layout.replicas[].shards[].tcpPort <integer> min=1 max=65535	# optional, setup `Pod.spec.containers.ports` with name `tcp` for selected shard, override `chi.spec.templates.hostTemplates.spec.tcpPort` allows connect to `clickhouse-server` via TCP Native protocol via kubernetes `Service` 
spec.configuration.clusters[].layout.replicas[].shards[].templates <object>	# optional, configuration of the templates names which will use for generate Kubernetes resources according to selected replica override top-level `chi.spec.configuration.templates`, cluster-level `chi.spec.configuration.clusters.templates`, replica-level `chi.spec.configuration.clusters.layout.replicas.templates` 
spec.configuration.clusters[].layout.replicas[].shards[].templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.configuration.clusters[].layout.replicas[].shards[].templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.replicas[].shards[].templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.replicas[].shards[].templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].shards[].tlsPort <integer> min=1 max=65535
spec.configuration.clusters[].layout.replicas[].shardsCount <integer> min=1	# optional, count of shards related to current replica, you can override each shard behavior on low-level `chi.spec.configuration.clusters.layout.replicas.shards`
spec.configuration.clusters[].layout.replicas[].templates <object>	# optional, configuration of the templates names which will use for generate Kubernetes resources according to selected replica override top-level `chi.spec.configuration.templates`, cluster-level `chi.spec.configuration.clusters.templates` 
spec.configuration.clusters[].layout.replicas[].templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.configuration.clusters[].layout.replicas[].templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.replicas[].templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.replicas[].templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicas[].templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.replicasCount <integer>	# how much replicas in each shards for current cluster will run in Kubernetes, each replica is a separate `StatefulSet` which contains only one `Pod` with `clickhouse-server` instance, every shard contains 1 replica by default" 
spec.configuration.clusters[].layout.shards <[]object>	# optional, allows override top-level `chi.spec.configuration`, cluster-level `chi.spec.configuration.clusters` settings for each shard separately, use it only if you fully understand what you do" 
spec.configuration.clusters[].layout.shards[].definitionType <string>	# DEPRECATED - to be removed soon
spec.configuration.clusters[].layout.shards[].files <object (free-form)>	# optional, allows define content of any setting file inside each `Pod` only in one shard during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` override top-level `chi.spec.configuration.files` and cluster-level `chi.spec.configuration.clusters.files` 
spec.configuration.clusters[].layout.shards[].internalReplication <any>	# optional, `true` by default when `chi.spec.configuration.clusters[].layout.ReplicaCount` > 1 and 0 otherwise allows setup <internal_replication> setting which will use during insert into tables with `Distributed` engine for insert only in one live replica and other replicas will download inserted data during replication, will apply in <remote_servers> inside ConfigMap which will mount in /etc/clickhouse-server/config.d/chop-generated-remote_servers.xml More details: https://clickhouse.tech/docs/en/engines/table-engines/special/distributed/ 
spec.configuration.clusters[].layout.shards[].name <string> pattern="^[a-zA-Z0-9-]{0,15}$" min=1 max=15	# optional, by default shard name is generated, but you can override it and setup custom name
spec.configuration.clusters[].layout.shards[].replicas <[]object>	# optional, allows override behavior for selected replicas from cluster-level `chi.spec.configuration.clusters` and shard-level `chi.spec.configuration.clusters.layout.shards` 
spec.configuration.clusters[].layout.shards[].replicas[].files <object (free-form)>	# optional, allows define content of any setting file inside `Pod` only in one replica during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` override top-level `chi.spec.configuration.files`, cluster-level `chi.spec.configuration.clusters.files` and shard-level `chi.spec.configuration.clusters.layout.shards.files` 
spec.configuration.clusters[].layout.shards[].replicas[].httpPort <integer> min=1 max=65535	# optional, setup `Pod.spec.containers.ports` with name `http` for selected replica, override `chi.spec.templates.hostTemplates.spec.httpPort` allows connect to `clickhouse-server` via HTTP protocol via kubernetes `Service` 
spec.configuration.clusters[].layout.shards[].replicas[].httpsPort <integer> min=1 max=65535
spec.configuration.clusters[].layout.shards[].replicas[].insecure <any>	# optional, open insecure ports for cluster, defaults to "yes" 
spec.configuration.clusters[].layout.shards[].replicas[].interserverHTTPPort <integer> min=1 max=65535	# optional, setup `Pod.spec.containers.ports` with name `interserver` for selected replica, override `chi.spec.templates.hostTemplates.spec.interserverHTTPPort` allows connect between replicas inside same shard during fetch replicated data parts HTTP protocol 
spec.configuration.clusters[].layout.shards[].replicas[].name <string> pattern="^[a-zA-Z0-9-]{0,15}$" min=1 max=15	# optional, by default replica name is generated, but you can override it and setup custom name
spec.configuration.clusters[].layout.shards[].replicas[].secure <any>	# optional, open secure ports 
spec.configuration.clusters[].layout.shards[].replicas[].settings <object (free-form)>	# optional, allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in `Pod` only in one replica during generate `ConfigMap` which will mount in `/etc/clickhouse-server/conf.d/` override top-level `chi.spec.configuration.settings`, cluster-level `chi.spec.configuration.clusters.settings` and shard-level `chi.spec.configuration.clusters.layout.shards.settings` More details: https://clickhouse.tech/docs/en/operations/settings/settings/ 
spec.configuration.clusters[].layout.shards[].replicas[].tcpPort <integer> min=1 max=65535	# optional, setup `Pod.spec.containers.ports` with name `tcp` for selected replica, override `chi.spec.templates.hostTemplates.spec.tcpPort` allows connect to `clickhouse-server` via TCP Native protocol via kubernetes `Service` 
spec.configuration.clusters[].layout.shards[].replicas[].templates <object>	# optional, configuration of the templates names which will use for generate Kubernetes resources according to selected replica override top-level `chi.spec.configuration.templates`, cluster-level `chi.spec.configuration.clusters.templates` and shard-level `chi.spec.configuration.clusters.layout.shards.templates` 
spec.configuration.clusters[].layout.shards[].replicas[].templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.configuration.clusters[].layout.shards[].replicas[].templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.shards[].replicas[].templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.shards[].replicas[].templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].replicas[].tlsPort <integer> min=1 max=65535
spec.configuration.clusters[].layout.shards[].replicasCount <integer> min=1	# optional, how much replicas in selected shard for selected ClickHouse cluster will run in Kubernetes, each replica is a separate `StatefulSet` which contains only one `Pod` with `clickhouse-server` instance, shard contains 1 replica by default override cluster-level `chi.spec.configuration.clusters.layout.replicasCount` 
spec.configuration.clusters[].layout.shards[].settings <object (free-form)>	# optional, allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in each `Pod` only in one shard during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` override top-level `chi.spec.configuration.settings` and cluster-level `chi.spec.configuration.clusters.settings` More details: https://clickhouse.tech/docs/en/operations/settings/settings/ 
spec.configuration.clusters[].layout.shards[].templates <object>	# optional, configuration of the templates names which will use for generate Kubernetes resources according to selected shard override top-level `chi.spec.configuration.templates` and cluster-level `chi.spec.configuration.clusters.templates` 
spec.configuration.clusters[].layout.shards[].templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.configuration.clusters[].layout.shards[].templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.shards[].templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].layout.shards[].templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].layout.shards[].weight <integer>	# optional, 1 by default, allows setup shard <weight> setting which will use during insert into tables with `Distributed` engine, will apply in <remote_servers> inside ConfigMap which will mount in /etc/clickhouse-server/config.d/chop-generated-remote_servers.xml More details: https://clickhouse.tech/docs/en/engines/table-engines/special/distributed/ 
spec.configuration.clusters[].layout.shardsCount <integer>	# how much shards for current ClickHouse cluster will run in Kubernetes, each shard contains shared-nothing part of data and contains set of replicas, cluster contains 1 shard by default" 
spec.configuration.clusters[].name <string> pattern="^[a-zA-Z0-9-]{0,15}$" min=1 max=15	# cluster name, used to identify set of servers and wide used during generate names of related Kubernetes resources
spec.configuration.clusters[].pdbManaged <any>	# Specifies whether the Pod Disruption Budget (PDB) should be managed. During the next installation, if PDB management is enabled, the operator will attempt to retrieve any existing PDB. If none is found, it will create a new one and initiate a reconciliation loop. If PDB management is disabled, the existing PDB will remain intact, and the reconciliation loop will not be executed. By default, PDB management is enabled. 
spec.configuration.clusters[].pdbMaxUnavailable <integer> min=0 max=65535	# Pod eviction is allowed if at most "pdbMaxUnavailable" pods are unavailable after the eviction, i.e. even in absence of the evicted pod. For example, one can prevent all voluntary evictions by specifying 0. This is a mutually exclusive setting with "minAvailable". 
spec.configuration.clusters[].reconcile <object>	# allow tuning reconciling process
spec.configuration.clusters[].reconcile.hooks <object>	# cluster-level hooks to execute before and after cluster reconcile
spec.configuration.clusters[].reconcile.hooks.post <[]object>	# actions to execute after cluster reconcile
spec.configuration.clusters[].reconcile.hooks.post[].events <[]string> (required) min=1	# Cluster-scope reconcile lifecycle events. Required, non-empty. Any - wildcard match: fires on every cluster reconcile pass and the cluster delete sweep ClusterCreate - all hosts in the cluster are new (no ancestor); fires only on the first reconcile of a brand-new cluster ClusterReconcile - ongoing reconcile pass over an existing cluster (at least one host has prior state). Fires on every operator reconcile cycle the upstream gates allow, including taskID-only force-reconciles. NOT a "spec changed" signal. ClusterDelete - cluster is being removed (delete sweep — wiring follow-up) 
spec.configuration.clusters[].reconcile.hooks.post[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default) propagates the error; Ignore logs a warning and continues. 
spec.configuration.clusters[].reconcile.hooks.post[].http <object>
spec.configuration.clusters[].reconcile.hooks.post[].http.method <string>
spec.configuration.clusters[].reconcile.hooks.post[].http.url <string>
spec.configuration.clusters[].reconcile.hooks.post[].shell <object>
spec.configuration.clusters[].reconcile.hooks.post[].shell.command <[]string>
spec.configuration.clusters[].reconcile.hooks.post[].shell.container <string>
spec.configuration.clusters[].reconcile.hooks.post[].sql <object>
spec.configuration.clusters[].reconcile.hooks.post[].sql.queries <[]string>
spec.configuration.clusters[].reconcile.hooks.post[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.configuration.clusters[].reconcile.hooks.pre <[]object>	# actions to execute before cluster reconcile
spec.configuration.clusters[].reconcile.hooks.pre[].events <[]string> (required) min=1	# Cluster-scope reconcile lifecycle events. Required, non-empty. Any - wildcard match: fires on every cluster reconcile pass and the cluster delete sweep ClusterCreate - all hosts in the cluster are new (no ancestor); fires only on the first reconcile of a brand-new cluster ClusterReconcile - ongoing reconcile pass over an existing cluster (at least one host has prior state). Fires on every operator reconcile cycle the upstream gates allow, including taskID-only force-reconciles. NOT a "spec changed" signal. ClusterDelete - cluster is being removed (delete sweep — wiring follow-up) 
spec.configuration.clusters[].reconcile.hooks.pre[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default) propagates the error; Ignore logs a warning and continues. 
spec.configuration.clusters[].reconcile.hooks.pre[].http <object>
spec.configuration.clusters[].reconcile.hooks.pre[].http.method <string>
spec.configuration.clusters[].reconcile.hooks.pre[].http.url <string>
spec.configuration.clusters[].reconcile.hooks.pre[].shell <object>
spec.configuration.clusters[].reconcile.hooks.pre[].shell.command <[]string>
spec.configuration.clusters[].reconcile.hooks.pre[].shell.container <string>
spec.configuration.clusters[].reconcile.hooks.pre[].sql <object>
spec.configuration.clusters[].reconcile.hooks.pre[].sql.queries <[]string>
spec.configuration.clusters[].reconcile.hooks.pre[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.configuration.clusters[].reconcile.host <object>	# Whether the operator during reconcile procedure should wait for a ClickHouse host: - to be excluded from a ClickHouse cluster - to complete all running queries - to be included into a ClickHouse cluster respectfully before moving forward 
spec.configuration.clusters[].reconcile.host.drop <object>
spec.configuration.clusters[].reconcile.host.drop.replicas <object>	# Whether the operator during reconcile procedure should drop replicas when replica is deleted or recreated 
spec.configuration.clusters[].reconcile.host.drop.replicas.active <any>	# Whether the operator during reconcile procedure should drop active replicas when replica is deleted or recreated 
spec.configuration.clusters[].reconcile.host.drop.replicas.onDelete <any>	# Whether the operator during reconcile procedure should drop replicas when replica is deleted 
spec.configuration.clusters[].reconcile.host.drop.replicas.onLostVolume <any>	# Whether the operator during reconcile procedure should drop replicas when replica volume is lost 
spec.configuration.clusters[].reconcile.host.hooks <object>	# hooks to execute before and after host reconcile
spec.configuration.clusters[].reconcile.host.hooks.post <[]object>	# actions to execute after reconcile
spec.configuration.clusters[].reconcile.host.hooks.post[].events <[]string> (required) min=1	# Reconcile lifecycle events that trigger this hook. Required, must be non-empty. The hook is skipped on any reconcile whose classifier does not emit at least one of the listed events. Supported values: Any - wildcard match: fires on every hook-evaluation point, including the pre-delete sweep on the dying host HostCreate - first reconcile that creates a host (no ancestor); best paired with POST hooks because PRE hooks on first creation are skipped (no live pod yet) HostUpdate - reconcile that has prior state for the host; catch-all for ongoing reconciles HostStart - host transitions from stopped to running HostStop - host is being stopped (current spec marks it stopped) HostConfigRestart - in-place software restart for a configuration change HostRollout - pod-template change forces a StatefulSet rollout HostShutdown - aggregate: fires whenever the pod is going down for any reason (Stop, ConfigRestart, Rollout, or Delete) HostDelete - host is being removed from the cluster (downsize); fires on the dying host before tear-down. Always emitted alongside HostShutdown. 
spec.configuration.clusters[].reconcile.host.hooks.post[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default): error propagates — pre-hook aborts reconcile / host deletion. Ignore: error is logged and the reconcile continues. 
spec.configuration.clusters[].reconcile.host.hooks.post[].http <object>
spec.configuration.clusters[].reconcile.host.hooks.post[].http.method <string>
spec.configuration.clusters[].reconcile.host.hooks.post[].http.url <string>
spec.configuration.clusters[].reconcile.host.hooks.post[].shell <object>
spec.configuration.clusters[].reconcile.host.hooks.post[].shell.command <[]string>
spec.configuration.clusters[].reconcile.host.hooks.post[].shell.container <string>
spec.configuration.clusters[].reconcile.host.hooks.post[].sql <object>
spec.configuration.clusters[].reconcile.host.hooks.post[].sql.queries <[]string>
spec.configuration.clusters[].reconcile.host.hooks.post[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.configuration.clusters[].reconcile.host.hooks.pre <[]object>	# actions to execute before reconcile
spec.configuration.clusters[].reconcile.host.hooks.pre[].events <[]string> (required) min=1	# Reconcile lifecycle events that trigger this hook. Required, must be non-empty. The hook is skipped on any reconcile whose classifier does not emit at least one of the listed events. Supported values: Any - wildcard match: fires on every hook-evaluation point, including the pre-delete sweep on the dying host HostCreate - first reconcile that creates a host (no ancestor); best paired with POST hooks because PRE hooks on first creation are skipped (no live pod yet) HostUpdate - reconcile that has prior state for the host; catch-all for ongoing reconciles HostStart - host transitions from stopped to running HostStop - host is being stopped (current spec marks it stopped) HostConfigRestart - in-place software restart for a configuration change HostRollout - pod-template change forces a StatefulSet rollout HostShutdown - aggregate: fires whenever the pod is going down for any reason (Stop, ConfigRestart, Rollout, or Delete) HostDelete - host is being removed from the cluster (downsize); fires on the dying host before tear-down. Always emitted alongside HostShutdown. 
spec.configuration.clusters[].reconcile.host.hooks.pre[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default): error propagates — pre-hook aborts reconcile / host deletion. Ignore: error is logged and the reconcile continues. 
spec.configuration.clusters[].reconcile.host.hooks.pre[].http <object>
spec.configuration.clusters[].reconcile.host.hooks.pre[].http.method <string>
spec.configuration.clusters[].reconcile.host.hooks.pre[].http.url <string>
spec.configuration.clusters[].reconcile.host.hooks.pre[].shell <object>
spec.configuration.clusters[].reconcile.host.hooks.pre[].shell.command <[]string>
spec.configuration.clusters[].reconcile.host.hooks.pre[].shell.container <string>
spec.configuration.clusters[].reconcile.host.hooks.pre[].sql <object>
spec.configuration.clusters[].reconcile.host.hooks.pre[].sql.queries <[]string>
spec.configuration.clusters[].reconcile.host.hooks.pre[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.configuration.clusters[].reconcile.host.wait <object>
spec.configuration.clusters[].reconcile.host.wait.exclude <any>	# Allows to stop all ClickHouse clusters defined in a CHI. Works as the following: - When `stop` is `1` operator sets `Replicas: 0` in each StatefulSet. Thie leads to having all `Pods` and `Service` deleted. All PVCs are kept intact. - When `stop` is `0` operator sets `Replicas: 1` and `Pod`s and `Service`s will created again and all retained PVCs will be attached to `Pod`s. 
spec.configuration.clusters[].reconcile.host.wait.include <any>	# Whether the operator during reconcile procedure should wait for a ClickHouse host to be included into a ClickHouse cluster
spec.configuration.clusters[].reconcile.host.wait.probes <object>	# What probes the operator should wait during host launch procedure
spec.configuration.clusters[].reconcile.host.wait.probes.readiness <any>	# Whether the operator during host launch procedure should wait for ready probe to succeed. In case probe is unspecified wait is assumed to be completed successfully. Default option value is to wait. 
spec.configuration.clusters[].reconcile.host.wait.probes.startup <any>	# Whether the operator during host launch procedure should wait for startup probe to succeed. In case probe is unspecified wait is assumed to be completed successfully. Default option value is to do not wait. 
spec.configuration.clusters[].reconcile.host.wait.queries <any>	# Whether the operator during reconcile procedure should wait for a ClickHouse host to complete all running queries
spec.configuration.clusters[].reconcile.host.wait.replicas <object>	# Whether the operator during reconcile procedure should wait for replicas to catch-up
spec.configuration.clusters[].reconcile.host.wait.replicas.all <any>	# Whether the operator during reconcile procedure should wait for all replicas to catch-up
spec.configuration.clusters[].reconcile.host.wait.replicas.delay <integer>	# replication max absolute delay to consider replica is not delayed
spec.configuration.clusters[].reconcile.host.wait.replicas.new <any>	# Whether the operator during reconcile procedure should wait for new replicas to catch-up
spec.configuration.clusters[].reconcile.runtime <object>	# runtime parameters for clickhouse-operator process which are used during reconcile cycle
spec.configuration.clusters[].reconcile.runtime.reconcileShardsMaxConcurrencyPercent <integer> min=0 max=100	# The maximum percentage of cluster shards that may be reconciled in parallel, 50 percent by default.
spec.configuration.clusters[].reconcile.runtime.reconcileShardsThreadsNumber <integer> min=1 max=65535	# The maximum number of cluster shards that may be reconciled in parallel, 1 by default
spec.configuration.clusters[].schemaPolicy <object>	# describes how schema is propagated within replicas and shards 
spec.configuration.clusters[].schemaPolicy.replica <string> enum=|None|none|All|all	# how schema is propagated within a replica (case-insensitive)
spec.configuration.clusters[].schemaPolicy.shard <string> enum=|None|none|All|all|DistributedTablesOnly|distributedtablesonly	# how schema is propagated between shards (case-insensitive)
spec.configuration.clusters[].secret <object>	# optional, shared secret value to secure cluster communications
spec.configuration.clusters[].secret.auto <any>	# Auto-generate shared secret value to secure cluster communications
spec.configuration.clusters[].secret.value <string>	# Cluster shared secret value in plain text
spec.configuration.clusters[].secret.valueFrom <object>	# Cluster shared secret source
spec.configuration.clusters[].secret.valueFrom.secretKeyRef <object>	# Selects a key of a secret in the clickhouse installation namespace. Should not be used if value is not empty. 
spec.configuration.clusters[].secret.valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.configuration.clusters[].secret.valueFrom.secretKeyRef.name <string> (required)	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names 
spec.configuration.clusters[].secret.valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.configuration.clusters[].secure <any>	# optional, open secure ports for cluster
spec.configuration.clusters[].security <object (free-form)>	# Per-cluster security toggles for outbound TLS connections the operator makes to this cluster's ClickHouse and ZooKeeper / Keeper hosts. Nil fields fall through to the operator-wide defaults in ClickHouseOperatorConfiguration. See docs/security_hardening.md for details. 
spec.configuration.clusters[].settings <object (free-form)>	# optional, allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in each `Pod` only in one cluster during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` override top-level `chi.spec.configuration.settings` More details: https://clickhouse.tech/docs/en/operations/settings/settings/ 
spec.configuration.clusters[].templates <object>	# optional, configuration of the templates names which will use for generate Kubernetes resources according to selected cluster override top-level `chi.spec.configuration.templates` 
spec.configuration.clusters[].templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.configuration.clusters[].templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.configuration.clusters[].templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.configuration.clusters[].zookeeper <object>	# optional, allows configure <yandex><zookeeper>..</zookeeper></yandex> section in each `Pod` only in current ClickHouse cluster, during generate `ConfigMap` which will mounted in `/etc/clickhouse-server/config.d/` override top-level `chi.spec.configuration.zookeeper` settings 
spec.configuration.clusters[].zookeeper.identity <string>	# optional access credentials string with `user:password` format used when use digest authorization in Zookeeper
spec.configuration.clusters[].zookeeper.keeper <object>	# reference to a ClickHouseKeeperInstallation (CHK) resource. The operator resolves this to ZooKeeper node addresses automatically. 
spec.configuration.clusters[].zookeeper.keeper.name <string>	# name of the ClickHouseKeeperInstallation custom resource
spec.configuration.clusters[].zookeeper.keeper.namespace <string>	# namespace of the CHK resource, defaults to the CHI namespace if omitted
spec.configuration.clusters[].zookeeper.keeper.serviceType <string> enum=|Replicas|replicas|Service|service	# how to discover keeper endpoints (case-insensitive): Replicas (default) — enumerate per-host services, one ZK node per keeper replica Service — use the CR-level headless service as a single ZK node entry 
spec.configuration.clusters[].zookeeper.nodes <[]object>	# describe every available zookeeper cluster node for interaction
spec.configuration.clusters[].zookeeper.nodes[].availabilityZone <string>	# availability zone for Zookeeper node
spec.configuration.clusters[].zookeeper.nodes[].host <string>	# dns name or ip address for Zookeeper node
spec.configuration.clusters[].zookeeper.nodes[].port <integer> min=0 max=65535	# TCP port which used to connect to Zookeeper node
spec.configuration.clusters[].zookeeper.nodes[].secure <any>	# if a secure connection to Zookeeper is required
spec.configuration.clusters[].zookeeper.operation_timeout_ms <integer>	# one operation timeout during Zookeeper transactions
spec.configuration.clusters[].zookeeper.root <string>	# optional root znode path inside zookeeper to store ClickHouse related data (replication queue or distributed DDL)
spec.configuration.clusters[].zookeeper.session_timeout_ms <integer>	# session timeout during connect to Zookeeper
spec.configuration.clusters[].zookeeper.use_compression <any>	# Enables compression in Keeper protocol if set to true
spec.configuration.files <object (free-form)>	# allows define content of any setting file inside each `Pod` during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` every key in this object is the file name every value in this object is the file content you can use `!!binary |` and base64 for binary files, see details here https://yaml.org/type/binary.html each key could contains prefix like {common}, {users}, {hosts} or config.d, users.d, conf.d, wrong prefixes will be ignored, subfolders also will be ignored More details: https://github.com/Altinity/clickhouse-operator/blob/master/docs/chi-examples/05-settings-05-files-nested.yaml any key could contains `valueFrom` with `secretKeyRef` which allow pass values from kubernetes secrets secrets will mounted into pod as separate volume in /etc/clickhouse-server/secrets.d/ and will automatically update when update secret it useful for pass SSL certificates from cert-manager or similar tool look into https://github.com/Altinity/clickhouse-operator/blob/master/docs/chi-examples/05-settings-01-overview.yaml for examples 
spec.configuration.profiles <object (free-form)>	# allows configure <yandex><profiles>..</profiles></yandex> section in each `Pod` during generate `ConfigMap` which will mount in `/etc/clickhouse-server/users.d/` you can configure any aspect of settings profile More details: https://clickhouse.tech/docs/en/operations/settings/settings-profiles/ Your yaml code will convert to XML, see examples https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#specconfigurationprofiles 
spec.configuration.quotas <object (free-form)>	# allows configure <yandex><quotas>..</quotas></yandex> section in each `Pod` during generate `ConfigMap` which will mount in `/etc/clickhouse-server/users.d/` you can configure any aspect of resource quotas More details: https://clickhouse.tech/docs/en/operations/quotas/ Your yaml code will convert to XML, see examples https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#specconfigurationquotas 
spec.configuration.settings <object (free-form)>	# allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in each `Pod` during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` More details: https://clickhouse.tech/docs/en/operations/settings/settings/ Your yaml code will convert to XML, see examples https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#specconfigurationsettings any key could contains `valueFrom` with `secretKeyRef` which allow pass password from kubernetes secrets look into https://github.com/Altinity/clickhouse-operator/blob/master/docs/chi-examples/05-settings-01-overview.yaml for examples secret value will pass in `pod.spec.env`, and generate with from_env=XXX in XML in /etc/clickhouse-server/config.d/chop-generated-settings.xml it not allow automatically updates when updates `secret`, change spec.taskID for manually trigger reconcile cycle 
spec.configuration.users <object (free-form)>	# allows configure <yandex><users>..</users></yandex> section in each `Pod` during generate `ConfigMap` which will mount in `/etc/clickhouse-server/users.d/` you can configure password hashed, authorization restrictions, database level security row filters etc. More details: https://clickhouse.tech/docs/en/operations/settings/settings-users/ Your yaml code will convert to XML, see examples https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#specconfigurationusers any key could contains `valueFrom` with `secretKeyRef` which allow pass password from kubernetes secrets secret value will pass in `pod.spec.containers.evn`, and generate with from_env=XXX in XML in /etc/clickhouse-server/users.d/chop-generated-users.xml it not allow automatically updates when updates `secret`, change spec.taskID for manually trigger reconcile cycle look into https://github.com/Altinity/clickhouse-operator/blob/master/docs/chi-examples/05-settings-01-overview.yaml for examples any key with prefix `k8s_secret_` shall has value with format namespace/secret/key or secret/key in this case value from secret will write directly into XML tag during render *-usersd ConfigMap any key with prefix `k8s_secret_env` shall has value with format namespace/secret/key or secret/key in this case value from secret will write into environment variable and write to XML tag via from_env=XXX look into https://github.com/Altinity/clickhouse-operator/blob/master/docs/chi-examples/05-settings-01-overview.yaml for examples 
spec.configuration.zookeeper <object>	# allows configure <yandex><zookeeper>..</zookeeper></yandex> section in each `Pod` during generate `ConfigMap` which will mounted in `/etc/clickhouse-server/config.d/` `clickhouse-operator` itself doesn't manage Zookeeper, please install Zookeeper separatelly look examples on https://github.com/Altinity/clickhouse-operator/tree/master/deploy/zookeeper/ currently, zookeeper (or clickhouse-keeper replacement) used for *ReplicatedMergeTree table engines and for `distributed_ddl` More details: https://clickhouse.tech/docs/en/operations/server-configuration-parameters/settings/#server-settings_zookeeper 
spec.configuration.zookeeper.identity <string>	# optional access credentials string with `user:password` format used when use digest authorization in Zookeeper
spec.configuration.zookeeper.keeper <object>	# reference to a ClickHouseKeeperInstallation (CHK) resource. The operator resolves this to ZooKeeper node addresses automatically. 
spec.configuration.zookeeper.keeper.name <string>	# name of the ClickHouseKeeperInstallation custom resource
spec.configuration.zookeeper.keeper.namespace <string>	# namespace of the CHK resource, defaults to the CHI namespace if omitted
spec.configuration.zookeeper.keeper.serviceType <string> enum=|Replicas|replicas|Service|service	# how to discover keeper endpoints (case-insensitive): Replicas (default) — enumerate per-host services, one ZK node per keeper replica Service — use the CR-level headless service as a single ZK node entry 
spec.configuration.zookeeper.nodes <[]object>	# describe every available zookeeper cluster node for interaction
spec.configuration.zookeeper.nodes[].availabilityZone <string>	# availability zone for Zookeeper node
spec.configuration.zookeeper.nodes[].host <string>	# dns name or ip address for Zookeeper node
spec.configuration.zookeeper.nodes[].port <integer> min=0 max=65535	# TCP port which used to connect to Zookeeper node
spec.configuration.zookeeper.nodes[].secure <any>	# if a secure connection to Zookeeper is required
spec.configuration.zookeeper.operation_timeout_ms <integer>	# one operation timeout during Zookeeper transactions
spec.configuration.zookeeper.root <string>	# optional root znode path inside zookeeper to store ClickHouse related data (replication queue or distributed DDL)
spec.configuration.zookeeper.session_timeout_ms <integer>	# session timeout during connect to Zookeeper
spec.configuration.zookeeper.use_compression <any>	# Enables compression in Keeper protocol if set to true
spec.defaults <object>	# define default behavior for whole ClickHouseInstallation, some behavior can be re-define on cluster, shard and replica level More info: https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#specdefaults 
spec.defaults.distributedDDL <object>	# allows change `<yandex><distributed_ddl></distributed_ddl></yandex>` settings More info: https://clickhouse.tech/docs/en/operations/server-configuration-parameters/settings/#server-settings-distributed_ddl 
spec.defaults.distributedDDL.profile <string>	# Settings from this profile will be used to execute DDL queries
spec.defaults.replicasUseFQDN <any>	# define should replicas be specified by FQDN in `<host></host>`. In case of "no" will use short hostname and clickhouse-server will use kubernetes default suffixes for DNS lookup "no" by default 
spec.defaults.storageManagement <object>	# default storage management options
spec.defaults.storageManagement.provisioner <string> enum=|StatefulSet|statefulset|Operator|operator	# defines `PVC` provisioner - be it StatefulSet or the Operator (case-insensitive)
spec.defaults.storageManagement.reclaimPolicy <string> enum=|Retain|retain|Delete|delete	# defines behavior of `PVC` deletion (case-insensitive). `Delete` by default, if `Retain` specified then `PVC` will be kept when deleting StatefulSet 
spec.defaults.templates <object>	# optional, configuration of the templates names which will use for generate Kubernetes resources according to one or more ClickHouse clusters described in current ClickHouseInstallation (chi) resource
spec.defaults.templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.defaults.templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.defaults.templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.defaults.templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.defaults.templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.defaults.templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.defaults.templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.defaults.templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.defaults.templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.defaults.templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.namespaceDomainPattern <string>	# Custom domain pattern which will be used for DNS names of `Service` or `Pod`. Typical use scenario - custom cluster domain in Kubernetes cluster Example: %s.svc.my.test 
spec.reconcile <object>	# Optional, allows tuning reconciling cycle for ClickhouseInstallation from clickhouse-operator side
spec.reconcile.cleanup <object>	# Optional, defines behavior for cleanup Kubernetes resources during reconcile cycle
spec.reconcile.cleanup.reconcileFailedObjects <object>	# Describes what clickhouse-operator should do with Kubernetes resources which are failed during reconcile. Default behavior is `Retain`" 
spec.reconcile.cleanup.reconcileFailedObjects.configMap <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed ConfigMap, `Retain` by default
spec.reconcile.cleanup.reconcileFailedObjects.pvc <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed PVC, `Retain` by default
spec.reconcile.cleanup.reconcileFailedObjects.service <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed Service, `Retain` by default
spec.reconcile.cleanup.reconcileFailedObjects.statefulSet <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed StatefulSet, `Retain` by default
spec.reconcile.cleanup.unknownObjects <object>	# Describes what clickhouse-operator should do with found Kubernetes resources which should be managed by clickhouse-operator, but do not have `ownerReference` to any currently managed `ClickHouseInstallation` resource. Default behavior is `Delete`" 
spec.reconcile.cleanup.unknownObjects.configMap <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown ConfigMap, `Delete` by default
spec.reconcile.cleanup.unknownObjects.pvc <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown PVC, `Delete` by default
spec.reconcile.cleanup.unknownObjects.service <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown Service, `Delete` by default
spec.reconcile.cleanup.unknownObjects.statefulSet <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown StatefulSet, `Delete` by default (case-insensitive)
spec.reconcile.cluster <object>	# CHI-level cluster reconcile defaults inherited by every cluster's spec.configuration.clusters[N].reconcile section. Use this as a single place to define cluster-level hooks that should apply to all clusters in this CHI; per-cluster hooks (under clusters[N].reconcile.hooks) are appended to (and dedup'd against) the inherited set. 
spec.reconcile.cluster.hooks <object>	# cluster-level hooks inherited by every cluster
spec.reconcile.cluster.hooks.post <[]object>	# actions to execute after each cluster reconcile
spec.reconcile.cluster.hooks.post[].events <[]string> (required) min=1	# Cluster-scope reconcile lifecycle events. Required, non-empty. Any - wildcard match: fires on every cluster reconcile pass and the cluster delete sweep ClusterCreate - all hosts in the cluster are new (no ancestor); fires only on the first reconcile of a brand-new cluster ClusterReconcile - ongoing reconcile pass over an existing cluster (at least one host has prior state). Fires on every operator reconcile cycle the upstream gates allow, including taskID-only force-reconciles. NOT a "spec changed" signal. ClusterDelete - cluster is being removed (delete sweep — wiring follow-up) 
spec.reconcile.cluster.hooks.post[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default) propagates the error; Ignore logs a warning and continues. 
spec.reconcile.cluster.hooks.post[].http <object>
spec.reconcile.cluster.hooks.post[].http.method <string>
spec.reconcile.cluster.hooks.post[].http.url <string>
spec.reconcile.cluster.hooks.post[].shell <object>
spec.reconcile.cluster.hooks.post[].shell.command <[]string>
spec.reconcile.cluster.hooks.post[].shell.container <string>
spec.reconcile.cluster.hooks.post[].sql <object>
spec.reconcile.cluster.hooks.post[].sql.queries <[]string>
spec.reconcile.cluster.hooks.post[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconcile.cluster.hooks.pre <[]object>	# actions to execute before each cluster reconcile
spec.reconcile.cluster.hooks.pre[].events <[]string> (required) min=1	# Cluster-scope reconcile lifecycle events. Required, non-empty. Any - wildcard match: fires on every cluster reconcile pass and the cluster delete sweep ClusterCreate - all hosts in the cluster are new (no ancestor); fires only on the first reconcile of a brand-new cluster ClusterReconcile - ongoing reconcile pass over an existing cluster (at least one host has prior state). Fires on every operator reconcile cycle the upstream gates allow, including taskID-only force-reconciles. NOT a "spec changed" signal. ClusterDelete - cluster is being removed (delete sweep — wiring follow-up) 
spec.reconcile.cluster.hooks.pre[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default) propagates the error; Ignore logs a warning and continues. 
spec.reconcile.cluster.hooks.pre[].http <object>
spec.reconcile.cluster.hooks.pre[].http.method <string>
spec.reconcile.cluster.hooks.pre[].http.url <string>
spec.reconcile.cluster.hooks.pre[].shell <object>
spec.reconcile.cluster.hooks.pre[].shell.command <[]string>
spec.reconcile.cluster.hooks.pre[].shell.container <string>
spec.reconcile.cluster.hooks.pre[].sql <object>
spec.reconcile.cluster.hooks.pre[].sql.queries <[]string>
spec.reconcile.cluster.hooks.pre[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconcile.configMapPropagationTimeout <integer> min=0 max=3600	# Timeout in seconds for `clickhouse-operator` to wait for modified `ConfigMap` to propagate into the `Pod` More details: https://kubernetes.io/docs/concepts/configuration/configmap/#mounted-configmaps-are-updated-automatically 
spec.reconcile.host <object>	# Whether the operator during reconcile procedure should wait for a ClickHouse host: - to be excluded from a ClickHouse cluster - to complete all running queries - to be included into a ClickHouse cluster respectfully before moving forward 
spec.reconcile.host.drop <object>
spec.reconcile.host.drop.replicas <object>	# Whether the operator during reconcile procedure should drop replicas when replica is deleted or recreated 
spec.reconcile.host.drop.replicas.active <any>	# Whether the operator during reconcile procedure should drop active replicas when replica is deleted or recreated 
spec.reconcile.host.drop.replicas.onDelete <any>	# Whether the operator during reconcile procedure should drop replicas when replica is deleted 
spec.reconcile.host.drop.replicas.onLostVolume <any>	# Whether the operator during reconcile procedure should drop replicas when replica volume is lost 
spec.reconcile.host.hooks <object>	# hooks to execute before and after host reconcile
spec.reconcile.host.hooks.post <[]object>	# actions to execute after reconcile
spec.reconcile.host.hooks.post[].events <[]string> (required) min=1	# Reconcile lifecycle events that trigger this hook. Required, must be non-empty. The hook is skipped on any reconcile whose classifier does not emit at least one of the listed events. Supported values: Any - wildcard match: fires on every hook-evaluation point, including the pre-delete sweep on the dying host HostCreate - first reconcile that creates a host (no ancestor); best paired with POST hooks because PRE hooks on first creation are skipped (no live pod yet) HostUpdate - reconcile that has prior state for the host; catch-all for ongoing reconciles HostStart - host transitions from stopped to running HostStop - host is being stopped (current spec marks it stopped) HostConfigRestart - in-place software restart for a configuration change HostRollout - pod-template change forces a StatefulSet rollout HostShutdown - aggregate: fires whenever the pod is going down for any reason (Stop, ConfigRestart, Rollout, or Delete) HostDelete - host is being removed from the cluster (downsize); fires on the dying host before tear-down. Always emitted alongside HostShutdown. 
spec.reconcile.host.hooks.post[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default): error propagates — pre-hook aborts reconcile / host deletion. Ignore: error is logged and the reconcile continues. 
spec.reconcile.host.hooks.post[].http <object>
spec.reconcile.host.hooks.post[].http.method <string>
spec.reconcile.host.hooks.post[].http.url <string>
spec.reconcile.host.hooks.post[].shell <object>
spec.reconcile.host.hooks.post[].shell.command <[]string>
spec.reconcile.host.hooks.post[].shell.container <string>
spec.reconcile.host.hooks.post[].sql <object>
spec.reconcile.host.hooks.post[].sql.queries <[]string>
spec.reconcile.host.hooks.post[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconcile.host.hooks.pre <[]object>	# actions to execute before reconcile
spec.reconcile.host.hooks.pre[].events <[]string> (required) min=1	# Reconcile lifecycle events that trigger this hook. Required, must be non-empty. The hook is skipped on any reconcile whose classifier does not emit at least one of the listed events. Supported values: Any - wildcard match: fires on every hook-evaluation point, including the pre-delete sweep on the dying host HostCreate - first reconcile that creates a host (no ancestor); best paired with POST hooks because PRE hooks on first creation are skipped (no live pod yet) HostUpdate - reconcile that has prior state for the host; catch-all for ongoing reconciles HostStart - host transitions from stopped to running HostStop - host is being stopped (current spec marks it stopped) HostConfigRestart - in-place software restart for a configuration change HostRollout - pod-template change forces a StatefulSet rollout HostShutdown - aggregate: fires whenever the pod is going down for any reason (Stop, ConfigRestart, Rollout, or Delete) HostDelete - host is being removed from the cluster (downsize); fires on the dying host before tear-down. Always emitted alongside HostShutdown. 
spec.reconcile.host.hooks.pre[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default): error propagates — pre-hook aborts reconcile / host deletion. Ignore: error is logged and the reconcile continues. 
spec.reconcile.host.hooks.pre[].http <object>
spec.reconcile.host.hooks.pre[].http.method <string>
spec.reconcile.host.hooks.pre[].http.url <string>
spec.reconcile.host.hooks.pre[].shell <object>
spec.reconcile.host.hooks.pre[].shell.command <[]string>
spec.reconcile.host.hooks.pre[].shell.container <string>
spec.reconcile.host.hooks.pre[].sql <object>
spec.reconcile.host.hooks.pre[].sql.queries <[]string>
spec.reconcile.host.hooks.pre[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconcile.host.wait <object>
spec.reconcile.host.wait.exclude <any>	# Allows to stop all ClickHouse clusters defined in a CHI. Works as the following: - When `stop` is `1` operator sets `Replicas: 0` in each StatefulSet. Thie leads to having all `Pods` and `Service` deleted. All PVCs are kept intact. - When `stop` is `0` operator sets `Replicas: 1` and `Pod`s and `Service`s will created again and all retained PVCs will be attached to `Pod`s. 
spec.reconcile.host.wait.include <any>	# Whether the operator during reconcile procedure should wait for a ClickHouse host to be included into a ClickHouse cluster
spec.reconcile.host.wait.probes <object>	# What probes the operator should wait during host launch procedure
spec.reconcile.host.wait.probes.readiness <any>	# Whether the operator during host launch procedure should wait for ready probe to succeed. In case probe is unspecified wait is assumed to be completed successfully. Default option value is to wait. 
spec.reconcile.host.wait.probes.startup <any>	# Whether the operator during host launch procedure should wait for startup probe to succeed. In case probe is unspecified wait is assumed to be completed successfully. Default option value is to do not wait. 
spec.reconcile.host.wait.queries <any>	# Whether the operator during reconcile procedure should wait for a ClickHouse host to complete all running queries
spec.reconcile.host.wait.replicas <object>	# Whether the operator during reconcile procedure should wait for replicas to catch-up
spec.reconcile.host.wait.replicas.all <any>	# Whether the operator during reconcile procedure should wait for all replicas to catch-up
spec.reconcile.host.wait.replicas.delay <integer>	# replication max absolute delay to consider replica is not delayed
spec.reconcile.host.wait.replicas.new <any>	# Whether the operator during reconcile procedure should wait for new replicas to catch-up
spec.reconcile.macros <object>	# macros parameters
spec.reconcile.macros.sections <object>	# sections behaviour for macros
spec.reconcile.macros.sections.files <object>	# sections behaviour for macros on files
spec.reconcile.macros.sections.files.enabled <any>	# enabled or not
spec.reconcile.macros.sections.profiles <object>	# sections behaviour for macros on profiles
spec.reconcile.macros.sections.profiles.enabled <any>	# enabled or not
spec.reconcile.macros.sections.quotas <object>	# sections behaviour for macros on quotas
spec.reconcile.macros.sections.quotas.enabled <any>	# enabled or not
spec.reconcile.macros.sections.settings <object>	# sections behaviour for macros on settings
spec.reconcile.macros.sections.settings.enabled <any>	# enabled or not
spec.reconcile.macros.sections.users <object>	# sections behaviour for macros on users
spec.reconcile.macros.sections.users.enabled <any>	# enabled or not
spec.reconcile.policy <string> enum=|Wait|wait|NoWait|nowait	# DISCUSSED TO BE DEPRECATED Syntax sugar Overrides all three 'reconcile.host.wait.{exclude, queries, include}' values from the operator's config Possible values: - wait - should wait to exclude host, complete queries and include host back into the cluster - nowait - should NOT wait to exclude host, complete queries and include host back into the cluster (case-insensitive) 
spec.reconcile.runtime <object>	# runtime parameters for clickhouse-operator process which are used during reconcile cycle
spec.reconcile.runtime.reconcileShardsMaxConcurrencyPercent <integer> min=0 max=100	# The maximum percentage of cluster shards that may be reconciled in parallel, 50 percent by default.
spec.reconcile.runtime.reconcileShardsThreadsNumber <integer> min=1 max=65535	# The maximum number of cluster shards that may be reconciled in parallel, 1 by default
spec.reconcile.statefulSet <object>	# Optional, StatefulSet reconcile behavior tuning
spec.reconcile.statefulSet.create <object>	# Behavior during create StatefulSet
spec.reconcile.statefulSet.create.onFailure <string> enum=|Abort|abort|Delete|delete|Ignore|ignore	# What to do in case created StatefulSet is not in 'Ready' after `reconcile.statefulSet.update.timeout` seconds. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet, leave it as it is. 2. delete - delete newly created problematic StatefulSet and follow 'abort' path afterwards. 3. ignore - ignore an error, pretend nothing happened, continue reconcile and move on to the next StatefulSet. 
spec.reconcile.statefulSet.recreate <object>	# Behavior during recreate StatefulSet
spec.reconcile.statefulSet.recreate.onDataLoss <string> enum=|Abort|abort|Recreate|recreate	# What to do in case operator needs to recreate StatefulSet due to PVC data loss or missing volumes. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet. 2. recreate - proceed and recreate StatefulSet. 
spec.reconcile.statefulSet.recreate.onUpdateFailure <string> enum=|Abort|abort|Recreate|recreate	# What to do in case operator needs to recreate StatefulSet due to update failure or StatefulSet not ready. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet. 2. recreate - proceed and recreate StatefulSet. 
spec.reconcile.statefulSet.update <object>	# Behavior during update StatefulSet
spec.reconcile.statefulSet.update.onFailure <string> enum=|Abort|abort|Rollback|rollback|Ignore|ignore	# What to do in case updated StatefulSet is not in 'Ready' after `reconcile.statefulSet.update.timeout` seconds. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet, leave it as it is. 2. rollback - delete Pod and rollback StatefulSet to previous Generation. Follow 'abort' path afterwards. 3. ignore - ignore an error, pretend nothing happened, continue reconcile and move on to the next StatefulSet. 
spec.reconcile.statefulSet.update.pollInterval <integer> min=1 max=600	# How many seconds to wait between checks for StatefulSet status during update
spec.reconcile.statefulSet.update.timeout <integer> min=0 max=3600	# How many seconds to wait for StatefulSet to be 'Ready' during update
spec.reconciling <object>	# [OBSOLETED] Optional, allows tuning reconciling cycle for ClickhouseInstallation from clickhouse-operator side
spec.reconciling.cleanup <object>	# Optional, defines behavior for cleanup Kubernetes resources during reconcile cycle
spec.reconciling.cleanup.reconcileFailedObjects <object>	# Describes what clickhouse-operator should do with Kubernetes resources which are failed during reconcile. Default behavior is `Retain`" 
spec.reconciling.cleanup.reconcileFailedObjects.configMap <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed ConfigMap, `Retain` by default
spec.reconciling.cleanup.reconcileFailedObjects.pvc <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed PVC, `Retain` by default
spec.reconciling.cleanup.reconcileFailedObjects.service <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed Service, `Retain` by default
spec.reconciling.cleanup.reconcileFailedObjects.statefulSet <string> enum=|Retain|retain|Delete|delete	# Behavior policy for failed StatefulSet, `Retain` by default
spec.reconciling.cleanup.unknownObjects <object>	# Describes what clickhouse-operator should do with found Kubernetes resources which should be managed by clickhouse-operator, but do not have `ownerReference` to any currently managed `ClickHouseInstallation` resource. Default behavior is `Delete`" 
spec.reconciling.cleanup.unknownObjects.configMap <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown ConfigMap, `Delete` by default
spec.reconciling.cleanup.unknownObjects.pvc <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown PVC, `Delete` by default
spec.reconciling.cleanup.unknownObjects.service <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown Service, `Delete` by default
spec.reconciling.cleanup.unknownObjects.statefulSet <string> enum=|Retain|retain|Delete|delete	# Behavior policy for unknown StatefulSet, `Delete` by default (case-insensitive)
spec.reconciling.cluster <object>	# CHI-level cluster reconcile defaults inherited by every cluster's spec.configuration.clusters[N].reconcile section. Use this as a single place to define cluster-level hooks that should apply to all clusters in this CHI; per-cluster hooks (under clusters[N].reconcile.hooks) are appended to (and dedup'd against) the inherited set. 
spec.reconciling.cluster.hooks <object>	# cluster-level hooks inherited by every cluster
spec.reconciling.cluster.hooks.post <[]object>	# actions to execute after each cluster reconcile
spec.reconciling.cluster.hooks.post[].events <[]string> (required) min=1	# Cluster-scope reconcile lifecycle events. Required, non-empty. Any - wildcard match: fires on every cluster reconcile pass and the cluster delete sweep ClusterCreate - all hosts in the cluster are new (no ancestor); fires only on the first reconcile of a brand-new cluster ClusterReconcile - ongoing reconcile pass over an existing cluster (at least one host has prior state). Fires on every operator reconcile cycle the upstream gates allow, including taskID-only force-reconciles. NOT a "spec changed" signal. ClusterDelete - cluster is being removed (delete sweep — wiring follow-up) 
spec.reconciling.cluster.hooks.post[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default) propagates the error; Ignore logs a warning and continues. 
spec.reconciling.cluster.hooks.post[].http <object>
spec.reconciling.cluster.hooks.post[].http.method <string>
spec.reconciling.cluster.hooks.post[].http.url <string>
spec.reconciling.cluster.hooks.post[].shell <object>
spec.reconciling.cluster.hooks.post[].shell.command <[]string>
spec.reconciling.cluster.hooks.post[].shell.container <string>
spec.reconciling.cluster.hooks.post[].sql <object>
spec.reconciling.cluster.hooks.post[].sql.queries <[]string>
spec.reconciling.cluster.hooks.post[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconciling.cluster.hooks.pre <[]object>	# actions to execute before each cluster reconcile
spec.reconciling.cluster.hooks.pre[].events <[]string> (required) min=1	# Cluster-scope reconcile lifecycle events. Required, non-empty. Any - wildcard match: fires on every cluster reconcile pass and the cluster delete sweep ClusterCreate - all hosts in the cluster are new (no ancestor); fires only on the first reconcile of a brand-new cluster ClusterReconcile - ongoing reconcile pass over an existing cluster (at least one host has prior state). Fires on every operator reconcile cycle the upstream gates allow, including taskID-only force-reconciles. NOT a "spec changed" signal. ClusterDelete - cluster is being removed (delete sweep — wiring follow-up) 
spec.reconciling.cluster.hooks.pre[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default) propagates the error; Ignore logs a warning and continues. 
spec.reconciling.cluster.hooks.pre[].http <object>
spec.reconciling.cluster.hooks.pre[].http.method <string>
spec.reconciling.cluster.hooks.pre[].http.url <string>
spec.reconciling.cluster.hooks.pre[].shell <object>
spec.reconciling.cluster.hooks.pre[].shell.command <[]string>
spec.reconciling.cluster.hooks.pre[].shell.container <string>
spec.reconciling.cluster.hooks.pre[].sql <object>
spec.reconciling.cluster.hooks.pre[].sql.queries <[]string>
spec.reconciling.cluster.hooks.pre[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconciling.configMapPropagationTimeout <integer> min=0 max=3600	# Timeout in seconds for `clickhouse-operator` to wait for modified `ConfigMap` to propagate into the `Pod` More details: https://kubernetes.io/docs/concepts/configuration/configmap/#mounted-configmaps-are-updated-automatically 
spec.reconciling.host <object>	# Whether the operator during reconcile procedure should wait for a ClickHouse host: - to be excluded from a ClickHouse cluster - to complete all running queries - to be included into a ClickHouse cluster respectfully before moving forward 
spec.reconciling.host.drop <object>
spec.reconciling.host.drop.replicas <object>	# Whether the operator during reconcile procedure should drop replicas when replica is deleted or recreated 
spec.reconciling.host.drop.replicas.active <any>	# Whether the operator during reconcile procedure should drop active replicas when replica is deleted or recreated 
spec.reconciling.host.drop.replicas.onDelete <any>	# Whether the operator during reconcile procedure should drop replicas when replica is deleted 
spec.reconciling.host.drop.replicas.onLostVolume <any>	# Whether the operator during reconcile procedure should drop replicas when replica volume is lost 
spec.reconciling.host.hooks <object>	# hooks to execute before and after host reconcile
spec.reconciling.host.hooks.post <[]object>	# actions to execute after reconcile
spec.reconciling.host.hooks.post[].events <[]string> (required) min=1	# Reconcile lifecycle events that trigger this hook. Required, must be non-empty. The hook is skipped on any reconcile whose classifier does not emit at least one of the listed events. Supported values: Any - wildcard match: fires on every hook-evaluation point, including the pre-delete sweep on the dying host HostCreate - first reconcile that creates a host (no ancestor); best paired with POST hooks because PRE hooks on first creation are skipped (no live pod yet) HostUpdate - reconcile that has prior state for the host; catch-all for ongoing reconciles HostStart - host transitions from stopped to running HostStop - host is being stopped (current spec marks it stopped) HostConfigRestart - in-place software restart for a configuration change HostRollout - pod-template change forces a StatefulSet rollout HostShutdown - aggregate: fires whenever the pod is going down for any reason (Stop, ConfigRestart, Rollout, or Delete) HostDelete - host is being removed from the cluster (downsize); fires on the dying host before tear-down. Always emitted alongside HostShutdown. 
spec.reconciling.host.hooks.post[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default): error propagates — pre-hook aborts reconcile / host deletion. Ignore: error is logged and the reconcile continues. 
spec.reconciling.host.hooks.post[].http <object>
spec.reconciling.host.hooks.post[].http.method <string>
spec.reconciling.host.hooks.post[].http.url <string>
spec.reconciling.host.hooks.post[].shell <object>
spec.reconciling.host.hooks.post[].shell.command <[]string>
spec.reconciling.host.hooks.post[].shell.container <string>
spec.reconciling.host.hooks.post[].sql <object>
spec.reconciling.host.hooks.post[].sql.queries <[]string>
spec.reconciling.host.hooks.post[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconciling.host.hooks.pre <[]object>	# actions to execute before reconcile
spec.reconciling.host.hooks.pre[].events <[]string> (required) min=1	# Reconcile lifecycle events that trigger this hook. Required, must be non-empty. The hook is skipped on any reconcile whose classifier does not emit at least one of the listed events. Supported values: Any - wildcard match: fires on every hook-evaluation point, including the pre-delete sweep on the dying host HostCreate - first reconcile that creates a host (no ancestor); best paired with POST hooks because PRE hooks on first creation are skipped (no live pod yet) HostUpdate - reconcile that has prior state for the host; catch-all for ongoing reconciles HostStart - host transitions from stopped to running HostStop - host is being stopped (current spec marks it stopped) HostConfigRestart - in-place software restart for a configuration change HostRollout - pod-template change forces a StatefulSet rollout HostShutdown - aggregate: fires whenever the pod is going down for any reason (Stop, ConfigRestart, Rollout, or Delete) HostDelete - host is being removed from the cluster (downsize); fires on the dying host before tear-down. Always emitted alongside HostShutdown. 
spec.reconciling.host.hooks.pre[].failurePolicy <string> enum=Fail|fail|Ignore|ignore	# Controls what happens when this hook returns an error. Fail (default): error propagates — pre-hook aborts reconcile / host deletion. Ignore: error is logged and the reconcile continues. 
spec.reconciling.host.hooks.pre[].http <object>
spec.reconciling.host.hooks.pre[].http.method <string>
spec.reconciling.host.hooks.pre[].http.url <string>
spec.reconciling.host.hooks.pre[].shell <object>
spec.reconciling.host.hooks.pre[].shell.command <[]string>
spec.reconciling.host.hooks.pre[].shell.container <string>
spec.reconciling.host.hooks.pre[].sql <object>
spec.reconciling.host.hooks.pre[].sql.queries <[]string>
spec.reconciling.host.hooks.pre[].target <string> enum=|FirstHost|firsthost|AllHosts|allhosts|AllShards|allshards	# where to execute hook for cluster-level hooks: FirstHost (default), AllHosts, AllShards
spec.reconciling.host.wait <object>
spec.reconciling.host.wait.exclude <any>	# Allows to stop all ClickHouse clusters defined in a CHI. Works as the following: - When `stop` is `1` operator sets `Replicas: 0` in each StatefulSet. Thie leads to having all `Pods` and `Service` deleted. All PVCs are kept intact. - When `stop` is `0` operator sets `Replicas: 1` and `Pod`s and `Service`s will created again and all retained PVCs will be attached to `Pod`s. 
spec.reconciling.host.wait.include <any>	# Whether the operator during reconcile procedure should wait for a ClickHouse host to be included into a ClickHouse cluster
spec.reconciling.host.wait.probes <object>	# What probes the operator should wait during host launch procedure
spec.reconciling.host.wait.probes.readiness <any>	# Whether the operator during host launch procedure should wait for ready probe to succeed. In case probe is unspecified wait is assumed to be completed successfully. Default option value is to wait. 
spec.reconciling.host.wait.probes.startup <any>	# Whether the operator during host launch procedure should wait for startup probe to succeed. In case probe is unspecified wait is assumed to be completed successfully. Default option value is to do not wait. 
spec.reconciling.host.wait.queries <any>	# Whether the operator during reconcile procedure should wait for a ClickHouse host to complete all running queries
spec.reconciling.host.wait.replicas <object>	# Whether the operator during reconcile procedure should wait for replicas to catch-up
spec.reconciling.host.wait.replicas.all <any>	# Whether the operator during reconcile procedure should wait for all replicas to catch-up
spec.reconciling.host.wait.replicas.delay <integer>	# replication max absolute delay to consider replica is not delayed
spec.reconciling.host.wait.replicas.new <any>	# Whether the operator during reconcile procedure should wait for new replicas to catch-up
spec.reconciling.macros <object>	# macros parameters
spec.reconciling.macros.sections <object>	# sections behaviour for macros
spec.reconciling.macros.sections.files <object>	# sections behaviour for macros on files
spec.reconciling.macros.sections.files.enabled <any>	# enabled or not
spec.reconciling.macros.sections.profiles <object>	# sections behaviour for macros on profiles
spec.reconciling.macros.sections.profiles.enabled <any>	# enabled or not
spec.reconciling.macros.sections.quotas <object>	# sections behaviour for macros on quotas
spec.reconciling.macros.sections.quotas.enabled <any>	# enabled or not
spec.reconciling.macros.sections.settings <object>	# sections behaviour for macros on settings
spec.reconciling.macros.sections.settings.enabled <any>	# enabled or not
spec.reconciling.macros.sections.users <object>	# sections behaviour for macros on users
spec.reconciling.macros.sections.users.enabled <any>	# enabled or not
spec.reconciling.policy <string> enum=|Wait|wait|NoWait|nowait	# DISCUSSED TO BE DEPRECATED Syntax sugar Overrides all three 'reconcile.host.wait.{exclude, queries, include}' values from the operator's config Possible values: - wait - should wait to exclude host, complete queries and include host back into the cluster - nowait - should NOT wait to exclude host, complete queries and include host back into the cluster (case-insensitive) 
spec.reconciling.runtime <object>	# runtime parameters for clickhouse-operator process which are used during reconcile cycle
spec.reconciling.runtime.reconcileShardsMaxConcurrencyPercent <integer> min=0 max=100	# The maximum percentage of cluster shards that may be reconciled in parallel, 50 percent by default.
spec.reconciling.runtime.reconcileShardsThreadsNumber <integer> min=1 max=65535	# The maximum number of cluster shards that may be reconciled in parallel, 1 by default
spec.reconciling.statefulSet <object>	# Optional, StatefulSet reconcile behavior tuning
spec.reconciling.statefulSet.create <object>	# Behavior during create StatefulSet
spec.reconciling.statefulSet.create.onFailure <string> enum=|Abort|abort|Delete|delete|Ignore|ignore	# What to do in case created StatefulSet is not in 'Ready' after `reconcile.statefulSet.update.timeout` seconds. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet, leave it as it is. 2. delete - delete newly created problematic StatefulSet and follow 'abort' path afterwards. 3. ignore - ignore an error, pretend nothing happened, continue reconcile and move on to the next StatefulSet. 
spec.reconciling.statefulSet.recreate <object>	# Behavior during recreate StatefulSet
spec.reconciling.statefulSet.recreate.onDataLoss <string> enum=|Abort|abort|Recreate|recreate	# What to do in case operator needs to recreate StatefulSet due to PVC data loss or missing volumes. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet. 2. recreate - proceed and recreate StatefulSet. 
spec.reconciling.statefulSet.recreate.onUpdateFailure <string> enum=|Abort|abort|Recreate|recreate	# What to do in case operator needs to recreate StatefulSet due to update failure or StatefulSet not ready. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet. 2. recreate - proceed and recreate StatefulSet. 
spec.reconciling.statefulSet.update <object>	# Behavior during update StatefulSet
spec.reconciling.statefulSet.update.onFailure <string> enum=|Abort|abort|Rollback|rollback|Ignore|ignore	# What to do in case updated StatefulSet is not in 'Ready' after `reconcile.statefulSet.update.timeout` seconds. Possible options: 1. abort - abort the process, do nothing with the problematic StatefulSet, leave it as it is. 2. rollback - delete Pod and rollback StatefulSet to previous Generation. Follow 'abort' path afterwards. 3. ignore - ignore an error, pretend nothing happened, continue reconcile and move on to the next StatefulSet. 
spec.reconciling.statefulSet.update.pollInterval <integer> min=1 max=600	# How many seconds to wait between checks for StatefulSet status during update
spec.reconciling.statefulSet.update.timeout <integer> min=0 max=3600	# How many seconds to wait for StatefulSet to be 'Ready' during update
spec.restart <string> enum=|RollingUpdate|rollingupdate	# In case 'RollingUpdate' specified, the operator will always restart ClickHouse pods during reconcile. This options is used in rare cases when force restart is required and is typically removed after the use in order to avoid unneeded restarts. 
spec.security <object (free-form)>	# CHI-level security defaults, applied to every cluster that does not override them. Each cluster can shadow these via spec.configuration.clusters[].security. See docs/security_hardening.md for details. 
spec.stop <any>	# Allows to stop all ClickHouse clusters defined in a CHI. Works as the following: - When `stop` is `1` operator sets `Replicas: 0` in each StatefulSet. Thie leads to having all `Pods` and `Service` deleted. All PVCs are kept intact. - When `stop` is `0` operator sets `Replicas: 1` and `Pod`s and `Service`s will created again and all retained PVCs will be attached to `Pod`s. 
spec.suspend <any>	# Suspend reconciliation of resources managed by a ClickHouse Installation. Works as the following: - When `suspend` is `true` operator stops reconciling all resources. - When `suspend` is `false` or not set, operator reconciles all resources. 
spec.taskID <string>	# Allows to define custom taskID for CHI update and watch status of this update execution. Displayed in all .status.taskID* fields. By default (if not filled) every update of CHI manifest will generate random taskID 
spec.templates <object>	# allows define templates which will use for render Kubernetes resources like StatefulSet, ConfigMap, Service, PVC, by default, clickhouse-operator have own templates, but you can override it
spec.templates.hostTemplates <[]object>	# hostTemplate will use during apply to generate `clickhose-server` config files
spec.templates.hostTemplates[].name <string>	# template name, could use to link inside top-level `chi.spec.defaults.templates.hostTemplate`, cluster-level `chi.spec.configuration.clusters.templates.hostTemplate`, shard-level `chi.spec.configuration.clusters.layout.shards.temlates.hostTemplate`, replica-level `chi.spec.configuration.clusters.layout.replicas.templates.hostTemplate`
spec.templates.hostTemplates[].portDistribution <[]object>	# define how will distribute numeric values of named ports in `Pod.spec.containers.ports` and clickhouse-server configs
spec.templates.hostTemplates[].portDistribution[].type <string> enum=|Unspecified|unspecified|ClusterScopeIndex|clusterscopeindex	# type of distribution, when `Unspecified` (default value) then all listen ports on clickhouse-server configuration in all Pods will have the same value, when `ClusterScopeIndex` then ports will increment to offset from base value depends on shard and replica index inside cluster with combination of `chi.spec.templates.podTemlates.spec.HostNetwork` it allows setup ClickHouse cluster inside Kubernetes and provide access via external network bypass Kubernetes internal network
spec.templates.hostTemplates[].spec <object>
spec.templates.hostTemplates[].spec.files <object (free-form)>	# optional, allows define content of any setting file inside each `Pod` where this template will apply during generate `ConfigMap` which will mount in `/etc/clickhouse-server/config.d/` or `/etc/clickhouse-server/conf.d/` or `/etc/clickhouse-server/users.d/` 
spec.templates.hostTemplates[].spec.httpPort <integer> min=1 max=65535	# optional, setup `http_port` inside `clickhouse-server` settings for each Pod where current template will apply if specified, should have equal value with `chi.spec.templates.podTemplates.spec.containers.ports[name=http]` More info: https://clickhouse.tech/docs/en/interfaces/http/ 
spec.templates.hostTemplates[].spec.httpsPort <integer> min=1 max=65535
spec.templates.hostTemplates[].spec.insecure <any>	# optional, open insecure ports for cluster, defaults to "yes" 
spec.templates.hostTemplates[].spec.interserverHTTPPort <integer> min=1 max=65535	# optional, setup `interserver_http_port` inside `clickhouse-server` settings for each Pod where current template will apply if specified, should have equal value with `chi.spec.templates.podTemplates.spec.containers.ports[name=interserver]` More info: https://clickhouse.tech/docs/en/operations/server-configuration-parameters/settings/#interserver-http-port 
spec.templates.hostTemplates[].spec.name <string> pattern="^[a-zA-Z0-9-]{0,15}$" min=1 max=15	# by default, hostname will generate, but this allows define custom name for each `clickhouse-server`
spec.templates.hostTemplates[].spec.secure <any>	# optional, open secure ports 
spec.templates.hostTemplates[].spec.settings <object (free-form)>	# optional, allows configure `clickhouse-server` settings inside <yandex>...</yandex> tag in each `Pod` where this template will apply during generate `ConfigMap` which will mount in `/etc/clickhouse-server/conf.d/` More details: https://clickhouse.tech/docs/en/operations/settings/settings/ 
spec.templates.hostTemplates[].spec.tcpPort <integer> min=1 max=65535	# optional, setup `tcp_port` inside `clickhouse-server` settings for each Pod where current template will apply if specified, should have equal value with `chi.spec.templates.podTemplates.spec.containers.ports[name=tcp]` More info: https://clickhouse.tech/docs/en/interfaces/tcp/ 
spec.templates.hostTemplates[].spec.templates <object>	# be careful, this part of CRD allows override template inside template, don't use it if you don't understand what you do
spec.templates.hostTemplates[].spec.templates.clusterServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.templates.dataVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.templates.hostTemplate <string>	# optional, template name from chi.spec.templates.hostTemplates, which will apply to configure every `clickhouse-server` instance during render ConfigMap resources which will mount into `Pod`
spec.templates.hostTemplates[].spec.templates.logVolumeClaimTemplate <string>	# optional, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse log directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.templates.podTemplate <string>	# optional, template name from chi.spec.templates.podTemplates, allows customization each `Pod` resource during render and reconcile each StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.templates.replicaServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each replica inside each shard inside each clickhouse cluster described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.templates.serviceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates. used for customization of the `Service` resource, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.templates.hostTemplates[].spec.templates.serviceTemplates <[]string>	# optional, template names from chi.spec.templates.serviceTemplates. used for customization of the `Service` resources, created by `clickhouse-operator` to cover all clusters in whole `chi` resource
spec.templates.hostTemplates[].spec.templates.shardServiceTemplate <string>	# optional, template name from chi.spec.templates.serviceTemplates, allows customization for each `Service` resource which will created by `clickhouse-operator` which cover each shard inside clickhouse cluster described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.templates.volumeClaimTemplate <string>	# optional, alias for dataVolumeClaimTemplate, template name from chi.spec.templates.volumeClaimTemplates, allows customization each `PVC` which will mount for clickhouse data directory in each `Pod` during render and reconcile every StatefulSet.spec resource described in `chi.spec.configuration.clusters`
spec.templates.hostTemplates[].spec.tlsPort <integer> min=1 max=65535
spec.templates.podTemplates <[]object>	# podTemplate will use during render `Pod` inside `StatefulSet.spec` and allows define rendered `Pod.spec`, pod scheduling distribution and pod zone More information: https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#spectemplatespodtemplates 
spec.templates.podTemplates[].distribution <string> enum=|Unspecified|unspecified|OnePerHost|oneperhost	# DEPRECATED, shortcut for `chi.spec.templates.podTemplates.spec.affinity.podAntiAffinity`
spec.templates.podTemplates[].generateName <string>	# allows define format for generated `Pod` name, look to https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#spectemplatesservicetemplates for details about available template variables
spec.templates.podTemplates[].metadata <object (free-form)>	# allows pass standard object's metadata from template to Pod More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata 
spec.templates.podTemplates[].name <string>	# template name, could use to link inside top-level `chi.spec.defaults.templates.podTemplate`, cluster-level `chi.spec.configuration.clusters.templates.podTemplate`, shard-level `chi.spec.configuration.clusters.layout.shards.temlates.podTemplate`, replica-level `chi.spec.configuration.clusters.layout.replicas.templates.podTemplate`
spec.templates.podTemplates[].podDistribution <[]object>	# define ClickHouse Pod distribution policy between Kubernetes Nodes inside Shard, Replica, Namespace, CHI, another ClickHouse cluster
spec.templates.podTemplates[].podDistribution[].number <integer> min=0 max=65535	# define, how much ClickHouse Pods could be inside selected scope with selected distribution type
spec.templates.podTemplates[].podDistribution[].scope <string> enum=|Unspecified|unspecified|Shard|shard|Replica|replica|Cluster|cluster|ClickHouseInstallation|clickhouseinstallation|Namespace|namespace	# scope for apply each podDistribution
spec.templates.podTemplates[].podDistribution[].topologyKey <string>	# use for inter-pod affinity look to `pod.spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution.podAffinityTerm.topologyKey`, more info: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#inter-pod-affinity-and-anti-affinity" 
spec.templates.podTemplates[].podDistribution[].type <string> enum=|Unspecified|unspecified|ClickHouseAntiAffinity|clickhouseantiaffinity|ShardAntiAffinity|shardantiaffinity|ReplicaAntiAffinity|replicaantiaffinity|AnotherNamespaceAntiAffinity|anothernamespaceantiaffinity|AnotherClickHouseInstallationAntiAffinity|anotherclickhouseinstallationantiaffinity|AnotherClusterAntiAffinity|anotherclusterantiaffinity|MaxNumberPerNode|maxnumberpernode|NamespaceAffinity|namespaceaffinity|ClickHouseInstallationAffinity|clickhouseinstallationaffinity|ClusterAffinity|clusteraffinity|ShardAffinity|shardaffinity|ReplicaAffinity|replicaaffinity|PreviousTailAffinity|previoustailaffinity|CircularReplication|circularreplication	# you can define multiple affinity policy types
spec.templates.podTemplates[].spec <object (free-form)>	# allows define whole Pod.spec inside StaefulSet.spec, look to https://kubernetes.io/docs/concepts/workloads/pods/#pod-templates for details
spec.templates.podTemplates[].zone <object>	# allows define custom zone name and will separate ClickHouse `Pods` between nodes, shortcut for `chi.spec.templates.podTemplates.spec.affinity.podAntiAffinity`
spec.templates.podTemplates[].zone.key <string>	# optional, if defined, allows select kubernetes nodes by label with `name` equal `key`
spec.templates.podTemplates[].zone.values <[]string>	# optional, if defined, allows select kubernetes nodes by label with `value` in `values`
spec.templates.serviceTemplates <[]object>	# allows define template for rendering `Service` which would get endpoint from Pods which scoped chi-wide, cluster-wide, shard-wide, replica-wide level 
spec.templates.serviceTemplates[].generateName <string>	# allows define format for generated `Service` name, look to https://github.com/Altinity/clickhouse-operator/blob/master/docs/custom_resource_explained.md#spectemplatesservicetemplates for details about available template variables" 
spec.templates.serviceTemplates[].metadata <object (free-form)>	# allows pass standard object's metadata from template to Service Could be use for define specificly for Cloud Provider metadata which impact to behavior of service More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata 
spec.templates.serviceTemplates[].name <string>	# template name, could use to link inside chi-level `chi.spec.defaults.templates.serviceTemplate` cluster-level `chi.spec.configuration.clusters.templates.clusterServiceTemplate` shard-level `chi.spec.configuration.clusters.layout.shards.temlates.shardServiceTemplate` replica-level `chi.spec.configuration.clusters.layout.replicas.templates.replicaServiceTemplate` or `chi.spec.configuration.clusters.layout.shards.replicas.replicaServiceTemplate` 
spec.templates.serviceTemplates[].spec <object (free-form)>	# describe behavior of generated Service More info: https://kubernetes.io/docs/concepts/services-networking/service/ 
spec.templates.volumeClaimTemplates <[]object>	# allows define template for rendering `PVC` kubernetes resource, which would use inside `Pod` for mount clickhouse `data`, clickhouse `logs` or something else 
spec.templates.volumeClaimTemplates[].metadata <object (free-form)>	# allows to pass standard object's metadata from template to PVC More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata 
spec.templates.volumeClaimTemplates[].name <string>	# template name, could use to link inside top-level `chi.spec.defaults.templates.dataVolumeClaimTemplate` or `chi.spec.defaults.templates.logVolumeClaimTemplate`, cluster-level `chi.spec.configuration.clusters.templates.dataVolumeClaimTemplate` or `chi.spec.configuration.clusters.templates.logVolumeClaimTemplate`, shard-level `chi.spec.configuration.clusters.layout.shards.temlates.dataVolumeClaimTemplate` or `chi.spec.configuration.clusters.layout.shards.temlates.logVolumeClaimTemplate` replica-level `chi.spec.configuration.clusters.layout.replicas.templates.dataVolumeClaimTemplate` or `chi.spec.configuration.clusters.layout.replicas.templates.logVolumeClaimTemplate` 
spec.templates.volumeClaimTemplates[].provisioner <string> enum=|StatefulSet|statefulset|Operator|operator	# defines `PVC` provisioner - be it StatefulSet or the Operator (case-insensitive)
spec.templates.volumeClaimTemplates[].reclaimPolicy <string> enum=|Retain|retain|Delete|delete	# defines behavior of `PVC` deletion (case-insensitive). `Delete` by default, if `Retain` specified then `PVC` will be kept when deleting StatefulSet 
spec.templates.volumeClaimTemplates[].spec <object (free-form)>	# allows define all aspects of `PVC` resource More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes/#persistentvolumeclaims 
spec.templating <object>	# Optional, applicable inside ClickHouseInstallationTemplate only. Defines current ClickHouseInstallationTemplate application options to target ClickHouseInstallation(s)." 
spec.templating.chiSelector <object (free-form)>	# Optional, defines selector for ClickHouseInstallation(s) to be templated with ClickhouseInstallationTemplate
spec.templating.policy <string> enum=|Auto|auto|Manual|manual	# When defined as `auto` inside ClickhouseInstallationTemplate, this ClickhouseInstallationTemplate will be auto-added into ClickHouseInstallation, selectable by `chiSelector`. Default value is `manual`, meaning ClickHouseInstallation should request this ClickhouseInstallationTemplate explicitly. 
spec.troubleshoot <any>	# Allows to troubleshoot Pods during CrashLoopBack state. This may happen when wrong configuration applied, in this case `clickhouse-server` wouldn't start. Command within ClickHouse container is modified with `sleep` in order to avoid quick restarts and give time to troubleshoot via CLI. Liveness and Readiness probes are disabled as well. 
spec.useTemplates <[]object>	# list of `ClickHouseInstallationTemplate` (chit) resource names which will merge with current `CHI` manifest during render Kubernetes resources to create related ClickHouse clusters" 
spec.useTemplates[].name <string>	# name of `ClickHouseInstallationTemplate` (chit) resource
spec.useTemplates[].namespace <string>	# Kubernetes namespace where need search `chit` resource, depending on `watchNamespaces` settings in `clickhouse-operator`
spec.useTemplates[].useType <string> enum=|Merge|merge	# optional, current strategy is only merge, and current `chi` settings have more priority than merged template `chit` (case-insensitive)
status <object>	# Status contains many fields like a normalized configuration, clickhouse-operator version, current action and all applied action list, current taskID and all applied taskIDs and other 
status.action <string>	# Action
status.actionPlan <object (free-form)>	# Action Plan
status.actions <[]string>	# Actions
status.chop-commit <string>	# Operator git commit SHA
status.chop-date <string>	# Operator build date
status.chop-ip <string>	# IP address of the operator's pod which managed this resource
status.chop-version <string>	# Operator version
status.clusters <integer> min=0	# Clusters count
status.endpoint <string>	# Endpoint
status.endpoints <[]string>	# All endpoints
status.error <string>	# Last error
status.errors <[]string>	# Errors
status.fqdns <[]string>	# Pods FQDNs
status.generation <integer> min=0	# Generation
status.hosts <integer> min=0	# Hosts count
status.hostsAdded <integer> min=0	# Added Hosts count
status.hostsCompleted <integer> min=0	# Completed Hosts count
status.hostsDelete <integer> min=0	# About to delete Hosts count
status.hostsDeleted <integer> min=0	# Deleted Hosts count
status.hostsUnchanged <integer> min=0	# Unchanged Hosts count
status.hostsUpdated <integer> min=0	# Updated Hosts count
status.hostsWithReplicaCaughtUp <[]string>	# List of hosts with replica caught up
status.hostsWithTablesCreated <[]string>	# List of hosts with tables created by the operator
status.normalized <object (free-form)>	# Normalized resource requested
status.normalizedCompleted <object (free-form)>	# Normalized resource completed
status.pod-ips <[]string>	# Pod IPs
status.pods <[]string>	# Pods
status.replicas <integer> min=0	# Replicas count
status.shards <integer> min=0	# Shards count
status.status <string>	# Status
status.taskID <string>	# Current task id
status.taskIDsCompleted <[]string>	# Completed task ids
status.taskIDsStarted <[]string>	# Started task ids
status.usedTemplates <[]object>	# List of templates used to build this CHI
