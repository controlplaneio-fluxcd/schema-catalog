# schema source: OpenShift v4.20
apiVersion <string> enum=network.operator.openshift.io/v1
kind <string> enum=EgressRouter
metadata.name <string> (required)
metadata.namespace <string>
spec <object> (required) default={}	# Specification of the desired egress router.
spec.addresses <[]object> (required)	# List of IP addresses to configure on the pod's secondary interface.
spec.mode <string> (required) default=""	# mode depicts the mode that is used for the egress router. The default mode is "Redirect" and is the only supported mode currently.
spec.networkInterface <object (free-form)> (required) default={}	# Specification of interface to create/use. The default is macvlan. Currently only macvlan is supported.
spec.redirect <object (free-form)>	# redirect represents the configuration parameters specific to redirect mode.
status <object (free-form)> default={}	# Observed status of EgressRouter.
