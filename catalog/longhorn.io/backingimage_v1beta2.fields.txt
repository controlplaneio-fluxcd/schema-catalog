# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=BackingImage
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BackingImageSpec defines the desired state of the Longhorn backing image
spec.checksum <string>
spec.dataEngine <string> enum=v1|v2 default="v1"
spec.diskFileSpecMap <map[string]object>
spec.diskFileSpecMap.<key>.dataEngine <string> enum=v1|v2
spec.diskFileSpecMap.<key>.evictionRequested <boolean>
spec.diskSelector <[]string>
spec.disks <map[string]string>	# Deprecated. We are now using DiskFileSpecMap to assign different spec to the file on different disks.
spec.minNumberOfCopies <integer>
spec.nodeSelector <[]string>
spec.secret <string>
spec.secretNamespace <string>
spec.sourceParameters <map[string]string>
spec.sourceType <string> enum=download|upload|export-from-volume|restore|clone
status <object>	# BackingImageStatus defines the observed state of the Longhorn backing image status
status.checksum <string>
status.diskFileStatusMap <map[string]object>
status.diskFileStatusMap.<key>.dataEngine <string> enum=v1|v2
status.diskFileStatusMap.<key>.lastStateTransitionTime <string>
status.diskFileStatusMap.<key>.message <string>
status.diskFileStatusMap.<key>.progress <integer>
status.diskFileStatusMap.<key>.state <string>
status.diskLastRefAtMap <map[string]string>
status.ownerID <string>
status.realSize <integer> format=int64	# Real size of image in bytes, which may be smaller than the size when the file is a sparse file. Will be zero until known (e.g. while a backing image is uploading)
status.size <integer> format=int64
status.uuid <string>
status.v2FirstCopyDisk <string>
status.v2FirstCopyStatus <string>	# It is pending -> in-progress -> ready/failed
status.virtualSize <integer> format=int64	# Virtual size of image in bytes, which may be larger than physical size. Will be zero until known (e.g. while a backing image is uploading)
