# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=BackupBackingImage
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BackupBackingImageSpec defines the desired state of the Longhorn backing image backup
spec.backingImage <string> (required)	# The backing image name.
spec.backupTargetName <string>	# The backup target name.
spec.labels <map[string]string>	# The labels of backing image backup.
spec.syncRequestedAt <string> format=date-time	# The time to request run sync the remote backing image backup.
spec.userCreated <boolean> (required)	# Is this CR created by user through API or UI.
status <object>	# BackupBackingImageStatus defines the observed state of the Longhorn backing image backup
status.backingImage <string>	# The backing image name.
status.backupCreatedAt <string>	# The backing image backup upload finished time.
status.checksum <string>	# The checksum of the backing image.
status.compressionMethod <string>	# Compression method
status.error <string>	# The error message when taking the backing image backup.
status.labels <map[string]string>	# The labels of backing image backup.
status.lastSyncedAt <string> format=date-time	# The last time that the backing image backup was synced with the remote backup target.
status.managerAddress <string>	# The address of the backing image manager that runs backing image backup.
status.messages <map[string]string>	# The error messages when listing or inspecting backing image backup.
status.ownerID <string>	# The node ID on which the controller is responsible to reconcile this CR.
status.progress <integer>	# The backing image backup progress.
status.secret <string>	# Record the secret if this backup backing image is encrypted
status.secretNamespace <string>	# Record the secret namespace if this backup backing image is encrypted
status.size <integer> format=int64	# The backing image size.
status.state <string>	# The backing image backup creation state. Can be "", "InProgress", "Completed", "Error", "Unknown".
status.url <string>	# The backing image backup URL.
