# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=BackupVolume
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BackupVolumeSpec defines the desired state of the Longhorn backup volume
spec.backupTargetName <string>	# The backup target name that the backup volume was synced.
spec.syncRequestedAt <string> format=date-time	# The time to request run sync the remote backup volume.
spec.volumeName <string>	# The volume name that the backup volume was used to backup.
status <object>	# BackupVolumeStatus defines the observed state of the Longhorn backup volume
status.backingImageChecksum <string>	# the backing image checksum.
status.backingImageName <string>	# The backing image name.
status.createdAt <string>	# The backup volume creation time.
status.dataStored <string>	# The backup volume block count.
status.labels <map[string]string>	# The backup volume labels.
status.lastBackupAt <string>	# The latest volume backup time.
status.lastBackupName <string>	# The latest volume backup name.
status.lastModificationTime <string> format=date-time	# The backup volume config last modification time.
status.lastSyncedAt <string> format=date-time	# The last time that the backup volume was synced into the cluster.
status.messages <map[string]string>	# The error messages when call longhorn engine on list or inspect backup volumes.
status.ownerID <string>	# The node ID on which the controller is responsible to reconcile this backup volume CR.
status.size <string>	# The backup volume size.
status.storageClassName <string>	# the storage class name of pv/pvc binding with the volume.
