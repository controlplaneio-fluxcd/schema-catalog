# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=ShareManager
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ShareManagerSpec defines the desired state of the Longhorn share manager
spec.image <string>	# Share manager image used for creating a share manager pod
status <object>	# ShareManagerStatus defines the observed state of the Longhorn share manager
status.currentImage <string>	# The image currently used by the share manager pod
status.endpoint <string>	# NFS endpoint that can access the mounted filesystem of the volume
status.ownerID <string>	# The node ID on which the controller is responsible to reconcile this share manager resource
status.state <string>	# The state of the share manager resource
