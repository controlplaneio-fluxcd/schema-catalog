# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=Snapshot
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# SnapshotSpec defines the desired state of Longhorn Snapshot
spec.createSnapshot <boolean>	# require creating a new snapshot
spec.labels <map[string]string>	# The labels of snapshot
spec.volume <string> (required)	# the volume that this snapshot belongs to. This field is immutable after creation.
status <object>	# SnapshotStatus defines the observed state of Longhorn Snapshot
status.checksum <string>
status.checksumCalculatedAt <string>	# ChecksumCalculatedAt is the RFC3339 timestamp indicating when the checksum for this snapshot was last calculated or updated.
status.children <map[string]boolean>
status.creationTime <string>
status.error <string>
status.labels <map[string]string>
status.markRemoved <boolean>
status.ownerID <string>
status.parent <string>
status.readyToUse <boolean>
status.requestedTime <string>
status.restoreSize <integer> format=int64
status.size <integer> format=int64
status.userCreated <boolean>
