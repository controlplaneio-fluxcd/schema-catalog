# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=EngineFrontend
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# EngineFrontendSpec defines the desired state of the Longhorn engine frontend (v2 initiator)
spec.active <boolean>
spec.dataEngine <string> enum=v1|v2
spec.desireState <string>
spec.disableFrontend <boolean>
spec.engineName <string>	# EngineName is the name of the v2 engine target (required for EngineFrontend instance creation)
spec.frontend <string> enum=blockdev|iscsi|nvmf|ublk|
spec.image <string>
spec.logRequested <boolean>
spec.nodeID <string>
spec.salvageRequested <boolean>
spec.size <string> format=int64	# Size is the desired size of the frontend device in bytes, as requested by the volume owner. The EngineFrontend controller drives the frontend device toward this size independently of the engine's target size.
spec.targetIP <string>	# TargetIP is the IP address of the v2 engine target
spec.targetPort <integer>	# TargetPort is the port of the v2 engine target
spec.ublkNumberOfQueue <integer>	# ublkNumberOfQueue controls the number of queues for ublk frontend.
spec.ublkQueueDepth <integer>	# ublkQueueDepth controls the depth of each queue for ublk frontend.
spec.volumeName <string>
spec.volumeSize <string> format=int64
status <object>	# EngineFrontendStatus defines the observed state of the Longhorn engine frontend
status.activePath <string>	# ActivePath is the currently active frontend path address.
status.conditions <[]object>
status.conditions[].lastProbeTime <string>	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.currentImage <string>
status.currentSize <string> format=int64	# CurrentSize is the current size of the frontend device in bytes, as observed from the data plane. It is 0 while the engine frontend is not running.
status.currentState <string>
status.endpoint <string>	# Endpoint is the initiator endpoint (e.g., /dev/longhorn/vol-name)
status.instanceManagerName <string>
status.ip <string>
status.logFetched <boolean>
status.ownerID <string>
status.paths <[]object>	# Paths describes the currently known frontend multipath state.
status.paths[].anaState <string>
status.paths[].engineName <string>
status.paths[].nguid <string>
status.paths[].nqn <string>
status.paths[].targetIP <string>
status.paths[].targetPort <integer>
status.port <integer>
status.preferredPath <string>	# PreferredPath is the preferred frontend path address.
status.salvageExecuted <boolean>
status.started <boolean>
status.starting <boolean>
status.storageIP <string>
status.switchoverPhase <string>	# SwitchoverPhase is the last completed switchover phase reported by the data plane.
status.targetIP <string>	# TargetIP is the currently connected IP address of the v2 engine target
status.targetPort <integer>	# TargetPort is the currently connected port of the v2 engine target
status.ublkID <integer> format=int32
status.uuid <string>
