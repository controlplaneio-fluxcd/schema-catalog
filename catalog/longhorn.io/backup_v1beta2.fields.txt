# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=Backup
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BackupSpec defines the desired state of the Longhorn backup
spec.backupBlockSize <string> enum=-1|2097152|16777216 format=int64	# The backup block size. 0 means the legacy default size 2MiB, and -1 indicate the block size is invalid.
spec.backupMode <string> enum=full|incremental	# The backup mode of this backup. Can be "full" or "incremental"
spec.labels <map[string]string>	# The labels of snapshot backup.
spec.snapshotName <string>	# The snapshot name.
spec.syncRequestedAt <string> format=date-time	# The time to request run sync the remote backup.
status <object>	# BackupStatus defines the observed state of the Longhorn backup
status.backupCreatedAt <string>	# The snapshot backup upload finished time.
status.backupTargetName <string>	# The backup target name.
status.compressionMethod <string>	# Compression method
status.error <string>	# The error message when taking the snapshot backup.
status.labels <map[string]string>	# The labels of snapshot backup.
status.lastSyncedAt <string> format=date-time	# The last time that the backup was synced with the remote backup target.
status.messages <map[string]string>	# The error messages when calling longhorn engine on listing or inspecting backups.
status.newlyUploadDataSize <string>	# Size in bytes of newly uploaded data
status.ownerID <string>	# The node ID on which the controller is responsible to reconcile this backup CR.
status.progress <integer>	# The snapshot backup progress.
status.reUploadedDataSize <string>	# Size in bytes of reuploaded data
status.replicaAddress <string>	# The address of the replica that runs snapshot backup.
status.size <string>	# The snapshot size.
status.snapshotCreatedAt <string>	# The snapshot creation time.
status.snapshotName <string>	# The snapshot name.
status.state <string>	# The backup creation state. Can be "", "InProgress", "Completed", "Error", "Unknown".
status.url <string>	# The snapshot backup URL.
status.volumeBackingImageName <string>	# The volume's backing image name.
status.volumeCreated <string>	# The volume creation time.
status.volumeName <string>	# The volume name.
status.volumeSize <string>	# The volume size.
