# schema source: Longhorn v1.12.0 https://github.com/longhorn/longhorn
apiVersion <string> enum=longhorn.io/v1beta2
kind <string> enum=BackupTarget
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BackupTargetSpec defines the desired state of the Longhorn backup target
spec.backupTargetURL <string>	# The backup target URL.
spec.credentialSecret <string>	# The backup target credential secret.
spec.pollInterval <string>	# The interval that the cluster needs to run sync with the backup target.
spec.syncRequestedAt <string> format=date-time	# The time to request run sync the remote backup target.
status <object>	# BackupTargetStatus defines the observed state of the Longhorn backup target
status.available <boolean>	# Available indicates if the remote backup target is available or not.
status.conditions <[]object>	# Records the reason on why the backup target is unavailable.
status.conditions[].lastProbeTime <string>	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.lastSyncedAt <string> format=date-time	# The last time that the controller synced with the remote backup target.
status.ownerID <string>	# The node ID on which the controller is responsible to reconcile this backup target CR.
