# schema source: Volcano v1.15.0 https://github.com/volcano-sh/volcano
apiVersion <string> enum=nodeinfo.volcano.sh/v1alpha1
kind <string> enum=Numatopology (cluster-scoped)
metadata.name <string> (required)
spec <object>	# Specification of the numa information of the worker node
spec.cpuDetail <map[string]object>	# Specifies the cpu topology info Key is cpu id
spec.cpuDetail.<key>.core <integer> min=0
spec.cpuDetail.<key>.numa <integer> min=0
spec.cpuDetail.<key>.socket <integer> min=0
spec.numares <map[string]object>	# Specifies the numa info for the resource Key is resource name
spec.numares.<key>.allocatable <string>
spec.numares.<key>.capacity <integer>
spec.policies <map[string]string>	# Specifies the policy of the manager
spec.resReserved <map[string]string>	# Specifies the reserved resource of the node Key is resource name
