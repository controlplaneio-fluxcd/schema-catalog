# schema source: Azure Service Operator v2.20.0 https://github.com/Azure/azure-service-operator
apiVersion <string> enum=network.frontdoor.azure.com/v1api20220501storage
kind <string> enum=WebApplicationFirewallPolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Storage version of v1api20220501.WebApplicationFirewallPolicy_Spec
spec.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.azureName <string>	# AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it doesn't have to be.
spec.customRules <object>	# Storage version of v1api20220501.CustomRuleList Defines contents of custom rules
spec.customRules.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.customRules.rules <[]object>
spec.customRules.rules[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.customRules.rules[].action <string>
spec.customRules.rules[].enabledState <string>
spec.customRules.rules[].matchConditions <[]object>
spec.customRules.rules[].matchConditions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.customRules.rules[].matchConditions[].matchValue <[]string>
spec.customRules.rules[].matchConditions[].matchVariable <string>
spec.customRules.rules[].matchConditions[].negateCondition <boolean>
spec.customRules.rules[].matchConditions[].operator <string>
spec.customRules.rules[].matchConditions[].selector <string>
spec.customRules.rules[].matchConditions[].transforms <[]string>
spec.customRules.rules[].name <string>
spec.customRules.rules[].priority <integer>
spec.customRules.rules[].rateLimitDurationInMinutes <integer>
spec.customRules.rules[].rateLimitThreshold <integer>
spec.customRules.rules[].ruleType <string>
spec.etag <string>
spec.location <string>
spec.managedRules <object>	# Storage version of v1api20220501.ManagedRuleSetList Defines the list of managed rule sets for the policy.
spec.managedRules.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets <[]object>
spec.managedRules.managedRuleSets[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets[].exclusions <[]object>
spec.managedRules.managedRuleSets[].exclusions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets[].exclusions[].matchVariable <string>
spec.managedRules.managedRuleSets[].exclusions[].selector <string>
spec.managedRules.managedRuleSets[].exclusions[].selectorMatchOperator <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides <[]object>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions <[]object>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].matchVariable <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].selector <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].selectorMatchOperator <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].ruleGroupName <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules <[]object>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].action <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].enabledState <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions <[]object>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].matchVariable <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].selector <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].selectorMatchOperator <string>
spec.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].ruleId <string>
spec.managedRules.managedRuleSets[].ruleSetAction <string>
spec.managedRules.managedRuleSets[].ruleSetType <string>
spec.managedRules.managedRuleSets[].ruleSetVersion <string>
spec.operatorSpec <object>	# Storage version of v1api20220501.WebApplicationFirewallPolicyOperatorSpec Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
spec.operatorSpec.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.operatorSpec.configMapExpressions <[]object>
spec.operatorSpec.configMapExpressions[].annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the destination resource.
spec.operatorSpec.configMapExpressions[].key <string>	# Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting map[string]string.
spec.operatorSpec.configMapExpressions[].labels <map[string]string>	# Labels is an optional set of labels to apply to the destination resource.
spec.operatorSpec.configMapExpressions[].name <string> (required)	# Name is the name of the Kubernetes configmap or secret to write to. The configmap or secret will be created in the same namespace as the resource.
spec.operatorSpec.configMapExpressions[].value <string> (required)	# Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
spec.operatorSpec.secretExpressions <[]object>
spec.operatorSpec.secretExpressions[].annotations <map[string]string>	# Annotations is an optional set of annotations to apply to the destination resource.
spec.operatorSpec.secretExpressions[].key <string>	# Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting map[string]string.
spec.operatorSpec.secretExpressions[].labels <map[string]string>	# Labels is an optional set of labels to apply to the destination resource.
spec.operatorSpec.secretExpressions[].name <string> (required)	# Name is the name of the Kubernetes configmap or secret to write to. The configmap or secret will be created in the same namespace as the resource.
spec.operatorSpec.secretExpressions[].value <string> (required)	# Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
spec.originalVersion <string>
spec.owner <object> (required)	# Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a reference to a resources.azure.com/ResourceGroup resource
spec.owner.armId <string> pattern="(?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)"
spec.owner.name <string>	# This is the name of the Kubernetes resource to reference.
spec.policySettings <object>	# Storage version of v1api20220501.PolicySettings Defines top-level WebApplicationFirewallPolicy configuration settings.
spec.policySettings.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.policySettings.customBlockResponseBody <string>
spec.policySettings.customBlockResponseStatusCode <integer>
spec.policySettings.enabledState <string>
spec.policySettings.mode <string>
spec.policySettings.redirectUrl <string>
spec.policySettings.requestBodyCheck <string>
spec.sku <object>	# Storage version of v1api20220501.Sku The pricing tier of the web application firewall policy.
spec.sku.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
spec.sku.name <string>
spec.tags <map[string]string>
status <object>	# Storage version of v1api20220501.WebApplicationFirewallPolicy_STATUS
status.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Message is a human readable message indicating details about the transition. This field may be empty.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# Reason for the condition's last transition. Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False. This is omitted in all cases when Status == Unknown
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, or Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.customRules <object>	# Storage version of v1api20220501.CustomRuleList_STATUS Defines contents of custom rules
status.customRules.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.customRules.rules <[]object>
status.customRules.rules[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.customRules.rules[].action <string>
status.customRules.rules[].enabledState <string>
status.customRules.rules[].matchConditions <[]object>
status.customRules.rules[].matchConditions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.customRules.rules[].matchConditions[].matchValue <[]string>
status.customRules.rules[].matchConditions[].matchVariable <string>
status.customRules.rules[].matchConditions[].negateCondition <boolean>
status.customRules.rules[].matchConditions[].operator <string>
status.customRules.rules[].matchConditions[].selector <string>
status.customRules.rules[].matchConditions[].transforms <[]string>
status.customRules.rules[].name <string>
status.customRules.rules[].priority <integer>
status.customRules.rules[].rateLimitDurationInMinutes <integer>
status.customRules.rules[].rateLimitThreshold <integer>
status.customRules.rules[].ruleType <string>
status.etag <string>
status.frontendEndpointLinks <[]object>
status.frontendEndpointLinks[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.frontendEndpointLinks[].id <string>
status.id <string>
status.location <string>
status.managedRules <object>	# Storage version of v1api20220501.ManagedRuleSetList_STATUS Defines the list of managed rule sets for the policy.
status.managedRules.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets <[]object>
status.managedRules.managedRuleSets[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets[].exclusions <[]object>
status.managedRules.managedRuleSets[].exclusions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets[].exclusions[].matchVariable <string>
status.managedRules.managedRuleSets[].exclusions[].selector <string>
status.managedRules.managedRuleSets[].exclusions[].selectorMatchOperator <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides <[]object>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions <[]object>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].matchVariable <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].selector <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].exclusions[].selectorMatchOperator <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].ruleGroupName <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules <[]object>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].action <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].enabledState <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions <[]object>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].matchVariable <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].selector <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].exclusions[].selectorMatchOperator <string>
status.managedRules.managedRuleSets[].ruleGroupOverrides[].rules[].ruleId <string>
status.managedRules.managedRuleSets[].ruleSetAction <string>
status.managedRules.managedRuleSets[].ruleSetType <string>
status.managedRules.managedRuleSets[].ruleSetVersion <string>
status.name <string>
status.policySettings <object>	# Storage version of v1api20220501.PolicySettings_STATUS Defines top-level WebApplicationFirewallPolicy configuration settings.
status.policySettings.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.policySettings.customBlockResponseBody <string>
status.policySettings.customBlockResponseStatusCode <integer>
status.policySettings.enabledState <string>
status.policySettings.mode <string>
status.policySettings.redirectUrl <string>
status.policySettings.requestBodyCheck <string>
status.provisioningState <string>
status.resourceState <string>
status.routingRuleLinks <[]object>
status.routingRuleLinks[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.routingRuleLinks[].id <string>
status.securityPolicyLinks <[]object>
status.securityPolicyLinks[].$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.securityPolicyLinks[].id <string>
status.sku <object>	# Storage version of v1api20220501.Sku_STATUS The pricing tier of the web application firewall policy.
status.sku.$propertyBag <map[string]string>	# PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage resources, allowing for full fidelity round trip conversions
status.sku.name <string>
status.tags <map[string]string>
status.type <string>
