# schema source: Traefik v3.7.7 https://github.com/traefik/traefik
apiVersion <string> enum=traefik.io/v1alpha1
kind <string> enum=MiddlewareTCP
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# MiddlewareTCPSpec defines the desired state of a MiddlewareTCP.
spec.inFlightConn <object>	# InFlightConn defines the InFlightConn middleware configuration.
spec.inFlightConn.amount <integer> format=int64 min=0	# Amount defines the maximum amount of allowed simultaneous connections. The middleware closes the connection if there are already amount connections opened.
spec.ipAllowList <object>	# IPAllowList defines the IPAllowList middleware configuration. This middleware accepts/refuses connections based on the client IP. More info: https://doc.traefik.io/traefik/v3.7/reference/routing-configuration/tcp/middlewares/ipallowlist/
spec.ipAllowList.sourceRange <[]string>	# SourceRange defines the allowed IPs (or ranges of allowed IPs by using CIDR notation).
spec.ipWhiteList <object>	# IPWhiteList defines the IPWhiteList middleware configuration. This middleware accepts/refuses connections based on the client IP. More info: https://doc.traefik.io/traefik/v3.7/reference/routing-configuration/tcp/middlewares/ipwhitelist/ Deprecated: please use IPAllowList instead.
spec.ipWhiteList.sourceRange <[]string>	# SourceRange defines the allowed IPs (or ranges of allowed IPs by using CIDR notation).
