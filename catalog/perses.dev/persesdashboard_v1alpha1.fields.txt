# schema source: Perses Operator v0.4.0 https://github.com/perses/perses-operator
apiVersion <string> enum=perses.dev/v1alpha1 (deprecated)	# perses.dev/v1alpha1 is deprecated; use perses.dev/v1alpha2
kind <string> enum=PersesDashboard
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.datasources <map[string]object>	# Datasources is an optional list of datasource definition.
spec.datasources.<key>.default <boolean> (required)
spec.datasources.<key>.display <object>
spec.datasources.<key>.display.description <string>
spec.datasources.<key>.display.name <string>
spec.datasources.<key>.plugin <object> (required)	# Plugin will contain the datasource configuration. The data typed is available in Cue.
spec.datasources.<key>.plugin.kind <string> (required)	# Kind is the type of the plugin (e.g., Panel, Variable, Datasource, etc.).
spec.datasources.<key>.plugin.metadata <object>	# Metadata is an optional field that contains additional information such as version and registry of the plugin.
spec.datasources.<key>.plugin.metadata.registry <string>	# Registry is optional. If not provided, it means the default registry: "perses.dev".
spec.datasources.<key>.plugin.metadata.version <string>	# Version is optional. If not provided, it means the latest version available in the Perses instance.
spec.datasources.<key>.plugin.spec <any> (required)
spec.display <object>
spec.display.description <string>
spec.display.name <string>
spec.duration <string> (required) format=duration pattern="^(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?$"	# Duration is the default time range to use when getting data to fill the dashboard
spec.layouts <[]object> (required)
spec.layouts[].kind <string> (required)
spec.layouts[].spec <any> (required)
spec.links <[]object>	# Links is an optional list of links to display at the dashboard level
spec.links[].name <string>
spec.links[].renderVariables <boolean>
spec.links[].targetBlank <boolean>
spec.links[].tooltip <string>
spec.links[].url <string> (required)
spec.panels <map[string]object> (required)
spec.panels.<key>.kind <string> (required)
spec.panels.<key>.spec <object> (required)
spec.panels.<key>.spec.display <object>
spec.panels.<key>.spec.display.description <string>
spec.panels.<key>.spec.display.name <string>
spec.panels.<key>.spec.links <[]object>
spec.panels.<key>.spec.links[].name <string>
spec.panels.<key>.spec.links[].renderVariables <boolean>
spec.panels.<key>.spec.links[].targetBlank <boolean>
spec.panels.<key>.spec.links[].tooltip <string>
spec.panels.<key>.spec.links[].url <string> (required)
spec.panels.<key>.spec.plugin <object> (required)
spec.panels.<key>.spec.plugin.kind <string> (required)	# Kind is the type of the plugin (e.g., Panel, Variable, Datasource, etc.).
spec.panels.<key>.spec.plugin.metadata <object>	# Metadata is an optional field that contains additional information such as version and registry of the plugin.
spec.panels.<key>.spec.plugin.metadata.registry <string>	# Registry is optional. If not provided, it means the default registry: "perses.dev".
spec.panels.<key>.spec.plugin.metadata.version <string>	# Version is optional. If not provided, it means the latest version available in the Perses instance.
spec.panels.<key>.spec.plugin.spec <any> (required)
spec.panels.<key>.spec.queries <[]object>
spec.panels.<key>.spec.queries[].kind <string> (required)
spec.panels.<key>.spec.queries[].spec <object> (required)
spec.panels.<key>.spec.queries[].spec.name <string>
spec.panels.<key>.spec.queries[].spec.plugin <object> (required)
spec.panels.<key>.spec.queries[].spec.plugin.kind <string> (required)	# Kind is the type of the plugin (e.g., Panel, Variable, Datasource, etc.).
spec.panels.<key>.spec.queries[].spec.plugin.metadata <object>	# Metadata is an optional field that contains additional information such as version and registry of the plugin.
spec.panels.<key>.spec.queries[].spec.plugin.metadata.registry <string>	# Registry is optional. If not provided, it means the default registry: "perses.dev".
spec.panels.<key>.spec.queries[].spec.plugin.metadata.version <string>	# Version is optional. If not provided, it means the latest version available in the Perses instance.
spec.panels.<key>.spec.queries[].spec.plugin.spec <any> (required)
spec.refreshInterval <string> format=duration pattern="^(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?$"	# RefreshInterval is the default refresh interval to use when landing on the dashboard
spec.variables <[]object>
spec.variables[].kind <string> (required)	# Kind is the type of the variable. Depending on the value of Kind, it will change the content of Spec.
spec.variables[].spec <any> (required)
status <object>	# PersesDashboardStatus defines the observed state of PersesDashboard
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
