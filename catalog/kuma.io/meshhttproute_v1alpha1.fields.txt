# schema source: Kuma v2.14.0 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshHTTPRoute
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshHTTPRoute resource.
spec.targetRef <object>	# TargetRef is a reference to the resource the policy takes an effect on. The resource could be either a real store object or virtual resource defined inplace.
spec.targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to <[]object>	# To matches destination services of requests and holds configuration.
spec.to[].hostnames <[]string>	# Hostnames is only valid when targeting MeshGateway and limits the effects of the rules to requests to this hostname. Given hostnames must intersect with the hostname of the listeners the route attaches to.
spec.to[].rules <[]object> (required)	# Rules contains the routing rules applies to a combination of top-level targetRef and the targetRef in this entry.
spec.to[].rules[].default <object> (required)	# Default holds routing rules that can be merged with rules from other policies.
spec.to[].rules[].default.backendRefs <[]object>
spec.to[].rules[].default.backendRefs[].kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].rules[].default.backendRefs[].labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].rules[].default.backendRefs[].mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].rules[].default.backendRefs[].name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].rules[].default.backendRefs[].namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].rules[].default.backendRefs[].port <integer> format=int32	# Port is only supported when this ref refers to a real MeshService object
spec.to[].rules[].default.backendRefs[].proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].rules[].default.backendRefs[].sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].rules[].default.backendRefs[].tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to[].rules[].default.backendRefs[].weight <integer> default=1 min=0
spec.to[].rules[].default.filters <[]object>
spec.to[].rules[].default.filters[].requestHeaderModifier <object>	# Only one action is supported per header name. Configuration to set or add multiple values for a header must use RFC 7230 header value formatting, separating each value with a comma.
spec.to[].rules[].default.filters[].requestHeaderModifier.add <[]object> max=16
spec.to[].rules[].default.filters[].requestHeaderModifier.add[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].rules[].default.filters[].requestHeaderModifier.add[].value <string> (required)
spec.to[].rules[].default.filters[].requestHeaderModifier.remove <[]string> max=16
spec.to[].rules[].default.filters[].requestHeaderModifier.set <[]object> max=16
spec.to[].rules[].default.filters[].requestHeaderModifier.set[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].rules[].default.filters[].requestHeaderModifier.set[].value <string> (required)
spec.to[].rules[].default.filters[].requestMirror <object>
spec.to[].rules[].default.filters[].requestMirror.backendRef <object> (required)	# BackendRef defines where to forward traffic.
spec.to[].rules[].default.filters[].requestMirror.backendRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].rules[].default.filters[].requestMirror.backendRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].rules[].default.filters[].requestMirror.backendRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].rules[].default.filters[].requestMirror.backendRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].rules[].default.filters[].requestMirror.backendRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].rules[].default.filters[].requestMirror.backendRef.port <integer> format=int32	# Port is only supported when this ref refers to a real MeshService object
spec.to[].rules[].default.filters[].requestMirror.backendRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].rules[].default.filters[].requestMirror.backendRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].rules[].default.filters[].requestMirror.backendRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to[].rules[].default.filters[].requestMirror.backendRef.weight <integer> default=1 min=0
spec.to[].rules[].default.filters[].requestMirror.percentage <int-or-string>	# Percentage of requests to mirror. If not specified, all requests to the target cluster will be mirrored.
spec.to[].rules[].default.filters[].requestRedirect <object>
spec.to[].rules[].default.filters[].requestRedirect.hostname <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# PreciseHostname is the fully qualified domain name of a network host. This matches the RFC 1123 definition of a hostname with 1 notable exception that numeric IP addresses are not allowed. Note that as per RFC1035 and RFC1123, a *label* must consist of lower case alphanumeric characters or '-', and must start and end with an alphanumeric character. No other punctuation is allowed.
spec.to[].rules[].default.filters[].requestRedirect.path <object>	# Path defines parameters used to modify the path of the incoming request. The modified path is then used to construct the location header. When empty, the request path is used as-is.
spec.to[].rules[].default.filters[].requestRedirect.path.replaceFullPath <string>
spec.to[].rules[].default.filters[].requestRedirect.path.replacePrefixMatch <string>
spec.to[].rules[].default.filters[].requestRedirect.path.type <string> (required) enum=ReplaceFullPath|ReplacePrefixMatch
spec.to[].rules[].default.filters[].requestRedirect.port <integer> format=int32 min=1 max=65535	# Port is the port to be used in the value of the `Location` header in the response. When empty, port (if specified) of the request is used.
spec.to[].rules[].default.filters[].requestRedirect.scheme <string> enum=http|https
spec.to[].rules[].default.filters[].requestRedirect.statusCode <integer> enum=301|302|303|307|308 default=302	# StatusCode is the HTTP status code to be used in response.
spec.to[].rules[].default.filters[].responseHeaderModifier <object>	# Only one action is supported per header name. Configuration to set or add multiple values for a header must use RFC 7230 header value formatting, separating each value with a comma.
spec.to[].rules[].default.filters[].responseHeaderModifier.add <[]object> max=16
spec.to[].rules[].default.filters[].responseHeaderModifier.add[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].rules[].default.filters[].responseHeaderModifier.add[].value <string> (required)
spec.to[].rules[].default.filters[].responseHeaderModifier.remove <[]string> max=16
spec.to[].rules[].default.filters[].responseHeaderModifier.set <[]object> max=16
spec.to[].rules[].default.filters[].responseHeaderModifier.set[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].rules[].default.filters[].responseHeaderModifier.set[].value <string> (required)
spec.to[].rules[].default.filters[].type <string> (required) enum=RequestHeaderModifier|ResponseHeaderModifier|RequestRedirect|URLRewrite|RequestMirror
spec.to[].rules[].default.filters[].urlRewrite <object>
spec.to[].rules[].default.filters[].urlRewrite.hostToBackendHostname <boolean>	# HostToBackendHostname rewrites the hostname to the hostname of the upstream host. This option is only available when targeting MeshGateways.
spec.to[].rules[].default.filters[].urlRewrite.hostname <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Hostname is the value to be used to replace the host header value during forwarding.
spec.to[].rules[].default.filters[].urlRewrite.path <object>	# Path defines a path rewrite.
spec.to[].rules[].default.filters[].urlRewrite.path.replaceFullPath <string>
spec.to[].rules[].default.filters[].urlRewrite.path.replacePrefixMatch <string>
spec.to[].rules[].default.filters[].urlRewrite.path.type <string> (required) enum=ReplaceFullPath|ReplacePrefixMatch
spec.to[].rules[].matches <[]object> (required) min=1	# Matches describes how to match HTTP requests this rule should be applied to.
spec.to[].rules[].matches[].headers <[]object>
spec.to[].rules[].matches[].headers[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name MUST be lower case as they will be handled with case insensitivity (See https://tools.ietf.org/html/rfc7230#section-3.2).
spec.to[].rules[].matches[].headers[].type <string> enum=Exact|Present|RegularExpression|Absent|Prefix default="Exact"	# Type specifies how to match against the value of the header.
spec.to[].rules[].matches[].headers[].value <string>	# Value is the value of HTTP Header to be matched.
spec.to[].rules[].matches[].method <string> enum=CONNECT|DELETE|GET|HEAD|OPTIONS|PATCH|POST|PUT|TRACE
spec.to[].rules[].matches[].path <object>
spec.to[].rules[].matches[].path.type <string> (required) enum=Exact|PathPrefix|RegularExpression
spec.to[].rules[].matches[].path.value <string> (required) min=1	# Exact or prefix matches must be an absolute path. A prefix matches only if separated by a slash or the entire path.
spec.to[].rules[].matches[].queryParams <[]object>	# QueryParams matches based on HTTP URL query parameters. Multiple matches are ANDed together such that all listed matches must succeed.
spec.to[].rules[].matches[].queryParams[].name <string> (required) min=1
spec.to[].rules[].matches[].queryParams[].type <string> (required) enum=Exact|RegularExpression
spec.to[].rules[].matches[].queryParams[].value <string> (required)
spec.to[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of request destinations.
spec.to[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
