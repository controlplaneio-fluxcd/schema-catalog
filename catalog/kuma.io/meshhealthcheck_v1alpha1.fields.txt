# schema source: Kuma v2.14.0 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshHealthCheck
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshHealthCheck resource.
spec.targetRef <object>	# TargetRef is a reference to the resource the policy takes an effect on. The resource could be either a real store object or virtual resource defined inplace.
spec.targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to <[]object>	# To list makes a match between the consumed services and corresponding configurations
spec.to[].default <object>	# Default is a configuration specific to the group of destinations referenced in 'targetRef'
spec.to[].default.alwaysLogHealthCheckFailures <boolean>	# If set to true, health check failure events will always be logged. If set to false, only the initial health check failure event will be logged. The default value is false.
spec.to[].default.eventLogPath <string>	# Specifies the path to the file where Envoy can log health check events. If empty, no event log will be written.
spec.to[].default.failTrafficOnPanic <boolean>	# If set to true, Envoy will not consider any hosts when the cluster is in 'panic mode'. Instead, the cluster will fail all requests as if all hosts are unhealthy. This can help avoid potentially overwhelming a failing service.
spec.to[].default.grpc <object>	# GrpcHealthCheck defines gRPC configuration which will instruct the service the health check will be made for is a gRPC service.
spec.to[].default.grpc.authority <string>	# The value of the :authority header in the gRPC health check request, by default name of the cluster this health check is associated with
spec.to[].default.grpc.disabled <boolean>	# If true the GrpcHealthCheck is disabled
spec.to[].default.grpc.serviceName <string>	# Service name parameter which will be sent to gRPC service
spec.to[].default.healthyPanicThreshold <int-or-string>	# Allows to configure panic threshold for Envoy cluster. If not specified, the default is 50%. To disable panic mode, set to 0%. Either int or decimal represented as string. Deprecated: the setting has been moved to MeshCircuitBreaker policy, please use MeshCircuitBreaker policy instead.
spec.to[].default.healthyThreshold <integer> format=int32	# Number of consecutive healthy checks before considering a host healthy. If not specified then the default value is 1
spec.to[].default.http <object>	# HttpHealthCheck defines HTTP configuration which will instruct the service the health check will be made for is an HTTP service.
spec.to[].default.http.disabled <boolean>	# If true the HttpHealthCheck is disabled
spec.to[].default.http.expectedStatuses <[]integer>	# List of HTTP response statuses which are considered healthy
spec.to[].default.http.path <string>	# The HTTP path which will be requested during the health check (ie. /health) If not specified then the default value is "/"
spec.to[].default.http.requestHeadersToAdd <object>	# The list of HTTP headers which should be added to each health check request
spec.to[].default.http.requestHeadersToAdd.add <[]object> max=16
spec.to[].default.http.requestHeadersToAdd.add[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].default.http.requestHeadersToAdd.add[].value <string> (required)
spec.to[].default.http.requestHeadersToAdd.set <[]object> max=16
spec.to[].default.http.requestHeadersToAdd.set[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].default.http.requestHeadersToAdd.set[].value <string> (required)
spec.to[].default.initialJitter <string>	# If specified, Envoy will start health checking after a random time in ms between 0 and initialJitter. This only applies to the first health check.
spec.to[].default.interval <string>	# Interval between consecutive health checks. If not specified then the default value is 1m
spec.to[].default.intervalJitter <string>	# If specified, during every interval Envoy will add IntervalJitter to the wait time.
spec.to[].default.intervalJitterPercent <integer> format=int32	# If specified, during every interval Envoy will add IntervalJitter * IntervalJitterPercent / 100 to the wait time. If IntervalJitter and IntervalJitterPercent are both set, both of them will be used to increase the wait time.
spec.to[].default.noTrafficInterval <string>	# The "no traffic interval" is a special health check interval that is used when a cluster has never had traffic routed to it. This lower interval allows cluster information to be kept up to date, without sending a potentially large amount of active health checking traffic for no reason. Once a cluster has been used for traffic routing, Envoy will shift back to using the standard health check interval that is defined. Note that this interval takes precedence over any other. The default value for "no traffic interval" is 60 seconds.
spec.to[].default.reuseConnection <boolean>	# Reuse health check connection between health checks. Default is true.
spec.to[].default.tcp <object>	# TcpHealthCheck defines configuration for specifying bytes to send and expected response during the health check
spec.to[].default.tcp.disabled <boolean>	# If true the TcpHealthCheck is disabled
spec.to[].default.tcp.receive <[]string>	# List of Base64 encoded blocks of strings expected as a response. When checking the response, "fuzzy" matching is performed such that each block must be found, and in the order specified, but not necessarily contiguous. If not provided or empty, checks will be performed as "connect only" and be marked as successful when TCP connection is successfully established.
spec.to[].default.tcp.send <string>	# Base64 encoded content of the message which will be sent during the health check to the target
spec.to[].default.timeout <string>	# Maximum time to wait for a health check response. If not specified then the default value is 15s
spec.to[].default.unhealthyThreshold <integer> format=int32	# Number of consecutive unhealthy checks before considering a host unhealthy. If not specified then the default value is 5
spec.to[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of destinations.
spec.to[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
