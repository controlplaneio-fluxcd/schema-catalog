# schema source: Kuma v2.14.1 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshAccessLog
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshAccessLog resource.
spec.from <[]object>	# From list makes a match between clients and corresponding configurations
spec.from[].default <object> (required)	# Default is a configuration specific to the group of clients referenced in 'targetRef'
spec.from[].default.backends <[]object>
spec.from[].default.backends[].file <object>	# FileBackend defines configuration for file based access logs
spec.from[].default.backends[].file.format <object>	# Format of access logs. Placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.from[].default.backends[].file.format.json <[]object>
spec.from[].default.backends[].file.format.json[].key <string> (required)
spec.from[].default.backends[].file.format.json[].value <string> (required)
spec.from[].default.backends[].file.format.omitEmptyValues <boolean> default=false
spec.from[].default.backends[].file.format.plain <string>
spec.from[].default.backends[].file.format.type <string> (required) enum=Plain|Json
spec.from[].default.backends[].file.path <string> (required) min=1	# Path to a file that logs will be written to
spec.from[].default.backends[].openTelemetry <object>	# Defines an OpenTelemetry logging backend.
spec.from[].default.backends[].openTelemetry.attributes <[]object>	# Attributes defines custom OpenTelemetry attributes. Keys must be static OpenTelemetry attribute names. Values can contain placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.from[].default.backends[].openTelemetry.attributes[].key <string> (required) pattern="^[a-z]([a-z0-9]|[._][a-z0-9])*$"	# Key is the OpenTelemetry attribute name.
spec.from[].default.backends[].openTelemetry.attributes[].value <string> (required)	# Value can contain Kuma placeholders.
spec.from[].default.backends[].openTelemetry.backendRef <object>	# BackendRef is a reference to a MeshOpenTelemetryBackend resource that defines the collector endpoint. Mutually exclusive with Endpoint.
spec.from[].default.backends[].openTelemetry.backendRef.kind <string> (required) enum=MeshOpenTelemetryBackend	# Kind of the backend resource.
spec.from[].default.backends[].openTelemetry.backendRef.labels <map[string]string>	# Labels to match the referenced resource. When multiple resources match, the oldest by creation time wins.
spec.from[].default.backends[].openTelemetry.body <any>	# Body is a raw string or an OTLP any value as described at https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md#field-body It can contain placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.from[].default.backends[].openTelemetry.endpoint <string> default=""	# Endpoint of OpenTelemetry collector. An empty port defaults to 4317. Deprecated: use BackendRef instead.
spec.from[].default.backends[].tcp <object>	# TCPBackend defines a TCP logging backend.
spec.from[].default.backends[].tcp.address <string> (required) min=1	# Address of the TCP logging backend
spec.from[].default.backends[].tcp.format <object>	# Format of access logs. Placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.from[].default.backends[].tcp.format.json <[]object>
spec.from[].default.backends[].tcp.format.json[].key <string> (required)
spec.from[].default.backends[].tcp.format.json[].value <string> (required)
spec.from[].default.backends[].tcp.format.omitEmptyValues <boolean> default=false
spec.from[].default.backends[].tcp.format.plain <string>
spec.from[].default.backends[].tcp.format.type <string> (required) enum=Plain|Json
spec.from[].default.backends[].type <string> (required) enum=Tcp|File|OpenTelemetry
spec.from[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of clients.
spec.from[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.from[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.from[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.from[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.from[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.from[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.from[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.from[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.rules <[]object>	# Rules defines inbound access log configurations.
spec.rules[].default <object> (required)	# Default contains configuration of the inbound access logging
spec.rules[].default.backends <[]object>
spec.rules[].default.backends[].file <object>	# FileBackend defines configuration for file based access logs
spec.rules[].default.backends[].file.format <object>	# Format of access logs. Placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.rules[].default.backends[].file.format.json <[]object>
spec.rules[].default.backends[].file.format.json[].key <string> (required)
spec.rules[].default.backends[].file.format.json[].value <string> (required)
spec.rules[].default.backends[].file.format.omitEmptyValues <boolean> default=false
spec.rules[].default.backends[].file.format.plain <string>
spec.rules[].default.backends[].file.format.type <string> (required) enum=Plain|Json
spec.rules[].default.backends[].file.path <string> (required) min=1	# Path to a file that logs will be written to
spec.rules[].default.backends[].openTelemetry <object>	# Defines an OpenTelemetry logging backend.
spec.rules[].default.backends[].openTelemetry.attributes <[]object>	# Attributes defines custom OpenTelemetry attributes. Keys must be static OpenTelemetry attribute names. Values can contain placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.rules[].default.backends[].openTelemetry.attributes[].key <string> (required) pattern="^[a-z]([a-z0-9]|[._][a-z0-9])*$"	# Key is the OpenTelemetry attribute name.
spec.rules[].default.backends[].openTelemetry.attributes[].value <string> (required)	# Value can contain Kuma placeholders.
spec.rules[].default.backends[].openTelemetry.backendRef <object>	# BackendRef is a reference to a MeshOpenTelemetryBackend resource that defines the collector endpoint. Mutually exclusive with Endpoint.
spec.rules[].default.backends[].openTelemetry.backendRef.kind <string> (required) enum=MeshOpenTelemetryBackend	# Kind of the backend resource.
spec.rules[].default.backends[].openTelemetry.backendRef.labels <map[string]string>	# Labels to match the referenced resource. When multiple resources match, the oldest by creation time wins.
spec.rules[].default.backends[].openTelemetry.body <any>	# Body is a raw string or an OTLP any value as described at https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md#field-body It can contain placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.rules[].default.backends[].openTelemetry.endpoint <string> default=""	# Endpoint of OpenTelemetry collector. An empty port defaults to 4317. Deprecated: use BackendRef instead.
spec.rules[].default.backends[].tcp <object>	# TCPBackend defines a TCP logging backend.
spec.rules[].default.backends[].tcp.address <string> (required) min=1	# Address of the TCP logging backend
spec.rules[].default.backends[].tcp.format <object>	# Format of access logs. Placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.rules[].default.backends[].tcp.format.json <[]object>
spec.rules[].default.backends[].tcp.format.json[].key <string> (required)
spec.rules[].default.backends[].tcp.format.json[].value <string> (required)
spec.rules[].default.backends[].tcp.format.omitEmptyValues <boolean> default=false
spec.rules[].default.backends[].tcp.format.plain <string>
spec.rules[].default.backends[].tcp.format.type <string> (required) enum=Plain|Json
spec.rules[].default.backends[].type <string> (required) enum=Tcp|File|OpenTelemetry
spec.rules[].matches <[]object>	# Matches defines a list of conditions (by SpiffeID or SNI) that select the traffic this rule applies to. Rules fire independently: a connection that satisfies multiple rules is logged to every matching rule's backends.
spec.rules[].matches[].sni <object>	# SNI defines a matcher configuration for matching by SNI value carried on the TLS connection
spec.rules[].matches[].sni.type <string> (required) enum=Exact	# Type defines how to match traffic by SNI. Only `Exact` is supported.
spec.rules[].matches[].sni.value <string> (required)	# Value is the SNI carried on the TLS connection that needs to match for the configuration to be applied
spec.rules[].matches[].spiffeID <object>	# SpiffeID defines a matcher configuration for SpiffeID matching
spec.rules[].matches[].spiffeID.type <string> (required) enum=Exact|Prefix	# Type defines how to match incoming traffic by SpiffeID. `Exact` or `Prefix` are allowed.
spec.rules[].matches[].spiffeID.value <string> (required)	# Value is SpiffeID of a client that needs to match for the configuration to be applied
spec.targetRef <object>	# TargetRef is a reference to the resource the policy takes an effect on. The resource could be either a real store object or virtual resource defined in-place.
spec.targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to <[]object>	# To list makes a match between the consumed services and corresponding configurations
spec.to[].default <object> (required)	# Default is a configuration specific to the group of destinations referenced in 'targetRef'
spec.to[].default.backends <[]object>
spec.to[].default.backends[].file <object>	# FileBackend defines configuration for file based access logs
spec.to[].default.backends[].file.format <object>	# Format of access logs. Placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.to[].default.backends[].file.format.json <[]object>
spec.to[].default.backends[].file.format.json[].key <string> (required)
spec.to[].default.backends[].file.format.json[].value <string> (required)
spec.to[].default.backends[].file.format.omitEmptyValues <boolean> default=false
spec.to[].default.backends[].file.format.plain <string>
spec.to[].default.backends[].file.format.type <string> (required) enum=Plain|Json
spec.to[].default.backends[].file.path <string> (required) min=1	# Path to a file that logs will be written to
spec.to[].default.backends[].openTelemetry <object>	# Defines an OpenTelemetry logging backend.
spec.to[].default.backends[].openTelemetry.attributes <[]object>	# Attributes defines custom OpenTelemetry attributes. Keys must be static OpenTelemetry attribute names. Values can contain placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.to[].default.backends[].openTelemetry.attributes[].key <string> (required) pattern="^[a-z]([a-z0-9]|[._][a-z0-9])*$"	# Key is the OpenTelemetry attribute name.
spec.to[].default.backends[].openTelemetry.attributes[].value <string> (required)	# Value can contain Kuma placeholders.
spec.to[].default.backends[].openTelemetry.backendRef <object>	# BackendRef is a reference to a MeshOpenTelemetryBackend resource that defines the collector endpoint. Mutually exclusive with Endpoint.
spec.to[].default.backends[].openTelemetry.backendRef.kind <string> (required) enum=MeshOpenTelemetryBackend	# Kind of the backend resource.
spec.to[].default.backends[].openTelemetry.backendRef.labels <map[string]string>	# Labels to match the referenced resource. When multiple resources match, the oldest by creation time wins.
spec.to[].default.backends[].openTelemetry.body <any>	# Body is a raw string or an OTLP any value as described at https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md#field-body It can contain placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.to[].default.backends[].openTelemetry.endpoint <string> default=""	# Endpoint of OpenTelemetry collector. An empty port defaults to 4317. Deprecated: use BackendRef instead.
spec.to[].default.backends[].tcp <object>	# TCPBackend defines a TCP logging backend.
spec.to[].default.backends[].tcp.address <string> (required) min=1	# Address of the TCP logging backend
spec.to[].default.backends[].tcp.format <object>	# Format of access logs. Placeholders available on https://www.envoyproxy.io/docs/envoy/latest/configuration/observability/access_log/usage#command-operators
spec.to[].default.backends[].tcp.format.json <[]object>
spec.to[].default.backends[].tcp.format.json[].key <string> (required)
spec.to[].default.backends[].tcp.format.json[].value <string> (required)
spec.to[].default.backends[].tcp.format.omitEmptyValues <boolean> default=false
spec.to[].default.backends[].tcp.format.plain <string>
spec.to[].default.backends[].tcp.format.type <string> (required) enum=Plain|Json
spec.to[].default.backends[].type <string> (required) enum=Tcp|File|OpenTelemetry
spec.to[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of destinations.
spec.to[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
status <object>	# Status is the current status of the Kuma MeshAccessLog resource.
status.conditions <[]object>
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
