# schema source: Kuma v2.14.0 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=ContainerPatch
metadata.name <string> (required)
metadata.namespace <string> (required)
mesh <string>
spec <object>	# ContainerPatchSpec specifies the options available for a ContainerPatch
spec.initPatch <[]object>	# InitPatch specifies jsonpatch to apply to an init container.
spec.initPatch[].from <string>	# From is a jsonpatch from string, used by move and copy operations.
spec.initPatch[].op <string> (required) enum=add|remove|replace|move|copy	# Op is a jsonpatch operation string.
spec.initPatch[].path <string> (required)	# Path is a jsonpatch path string.
spec.initPatch[].value <string>	# Value must be a string representing a valid json object used by replace and add operations. String has to be escaped with " to be valid a json object.
spec.sidecarPatch <[]object>	# SidecarPatch specifies jsonpatch to apply to a sidecar container.
spec.sidecarPatch[].from <string>	# From is a jsonpatch from string, used by move and copy operations.
spec.sidecarPatch[].op <string> (required) enum=add|remove|replace|move|copy	# Op is a jsonpatch operation string.
spec.sidecarPatch[].path <string> (required)	# Path is a jsonpatch path string.
spec.sidecarPatch[].value <string>	# Value must be a string representing a valid json object used by replace and add operations. String has to be escaped with " to be valid a json object.
