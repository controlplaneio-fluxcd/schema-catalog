# schema source: Kuma v2.14.1 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshLoadBalancingStrategy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshLoadBalancingStrategy resource.
spec.targetRef <object>	# TargetRef is a reference to the resource the policy takes an effect on. The resource could be either a real store object or virtual resource defined inplace.
spec.targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to <[]object>	# To list makes a match between the consumed services and corresponding configurations
spec.to[].default <object>	# Default is a configuration specific to the group of destinations referenced in 'targetRef'
spec.to[].default.hashPolicies <[]object>	# HashPolicies specify a list of request/connection properties that are used to calculate a hash. These hash policies are executed in the specified order. If a hash policy has the “terminal” attribute set to true, and there is already a hash generated, the hash is returned immediately, ignoring the rest of the hash policy list.
spec.to[].default.hashPolicies[].connection <object>
spec.to[].default.hashPolicies[].connection.sourceIP <boolean>	# Hash on source IP address.
spec.to[].default.hashPolicies[].cookie <object>
spec.to[].default.hashPolicies[].cookie.name <string> (required) min=1	# The name of the cookie that will be used to obtain the hash key.
spec.to[].default.hashPolicies[].cookie.path <string>	# The name of the path for the cookie.
spec.to[].default.hashPolicies[].cookie.ttl <string>	# If specified, a cookie with the TTL will be generated if the cookie is not present.
spec.to[].default.hashPolicies[].filterState <object>
spec.to[].default.hashPolicies[].filterState.key <string> (required) min=1	# The name of the Object in the per-request filterState, which is an Envoy::Hashable object. If there is no data associated with the key, or the stored object is not Envoy::Hashable, no hash will be produced.
spec.to[].default.hashPolicies[].header <object>
spec.to[].default.hashPolicies[].header.name <string> (required) min=1	# The name of the request header that will be used to obtain the hash key.
spec.to[].default.hashPolicies[].queryParameter <object>
spec.to[].default.hashPolicies[].queryParameter.name <string> (required) min=1	# The name of the URL query parameter that will be used to obtain the hash key. If the parameter is not present, no hash will be produced. Query parameter names are case-sensitive.
spec.to[].default.hashPolicies[].terminal <boolean>	# Terminal is a flag that short-circuits the hash computing. This field provides a ‘fallback’ style of configuration: “if a terminal policy doesn’t work, fallback to rest of the policy list”, it saves time when the terminal policy works. If true, and there is already a hash computed, ignore rest of the list of hash polices.
spec.to[].default.hashPolicies[].type <string> (required) enum=Header|Cookie|Connection|SourceIP|QueryParameter|FilterState
spec.to[].default.loadBalancer <object>	# LoadBalancer allows to specify load balancing algorithm.
spec.to[].default.loadBalancer.leastRequest <object>	# LeastRequest selects N random available hosts as specified in 'choiceCount' (2 by default) and picks the host which has the fewest active requests
spec.to[].default.loadBalancer.leastRequest.activeRequestBias <int-or-string>	# ActiveRequestBias refers to dynamic weights applied when hosts have varying load balancing weights. A higher value here aggressively reduces the weight of endpoints that are currently handling active requests. In essence, the higher the ActiveRequestBias value, the more forcefully it reduces the load balancing weight of endpoints that are actively serving requests.
spec.to[].default.loadBalancer.leastRequest.choiceCount <integer> format=int32 min=2	# ChoiceCount is the number of random healthy hosts from which the host with the fewest active requests will be chosen. Defaults to 2 so that Envoy performs two-choice selection if the field is not set.
spec.to[].default.loadBalancer.maglev <object>	# Maglev implements consistent hashing to upstream hosts. Maglev can be used as a drop in replacement for the ring hash load balancer any place in which consistent hashing is desired.
spec.to[].default.loadBalancer.maglev.hashPolicies <[]object>	# HashPolicies specify a list of request/connection properties that are used to calculate a hash. These hash policies are executed in the specified order. If a hash policy has the “terminal” attribute set to true, and there is already a hash generated, the hash is returned immediately, ignoring the rest of the hash policy list.
spec.to[].default.loadBalancer.maglev.hashPolicies[].connection <object>
spec.to[].default.loadBalancer.maglev.hashPolicies[].connection.sourceIP <boolean>	# Hash on source IP address.
spec.to[].default.loadBalancer.maglev.hashPolicies[].cookie <object>
spec.to[].default.loadBalancer.maglev.hashPolicies[].cookie.name <string> (required) min=1	# The name of the cookie that will be used to obtain the hash key.
spec.to[].default.loadBalancer.maglev.hashPolicies[].cookie.path <string>	# The name of the path for the cookie.
spec.to[].default.loadBalancer.maglev.hashPolicies[].cookie.ttl <string>	# If specified, a cookie with the TTL will be generated if the cookie is not present.
spec.to[].default.loadBalancer.maglev.hashPolicies[].filterState <object>
spec.to[].default.loadBalancer.maglev.hashPolicies[].filterState.key <string> (required) min=1	# The name of the Object in the per-request filterState, which is an Envoy::Hashable object. If there is no data associated with the key, or the stored object is not Envoy::Hashable, no hash will be produced.
spec.to[].default.loadBalancer.maglev.hashPolicies[].header <object>
spec.to[].default.loadBalancer.maglev.hashPolicies[].header.name <string> (required) min=1	# The name of the request header that will be used to obtain the hash key.
spec.to[].default.loadBalancer.maglev.hashPolicies[].queryParameter <object>
spec.to[].default.loadBalancer.maglev.hashPolicies[].queryParameter.name <string> (required) min=1	# The name of the URL query parameter that will be used to obtain the hash key. If the parameter is not present, no hash will be produced. Query parameter names are case-sensitive.
spec.to[].default.loadBalancer.maglev.hashPolicies[].terminal <boolean>	# Terminal is a flag that short-circuits the hash computing. This field provides a ‘fallback’ style of configuration: “if a terminal policy doesn’t work, fallback to rest of the policy list”, it saves time when the terminal policy works. If true, and there is already a hash computed, ignore rest of the list of hash polices.
spec.to[].default.loadBalancer.maglev.hashPolicies[].type <string> (required) enum=Header|Cookie|Connection|SourceIP|QueryParameter|FilterState
spec.to[].default.loadBalancer.maglev.tableSize <integer> format=int32 min=1 max=5000011	# The table size for Maglev hashing. Maglev aims for “minimal disruption” rather than an absolute guarantee. Minimal disruption means that when the set of upstream hosts change, a connection will likely be sent to the same upstream as it was before. Increasing the table size reduces the amount of disruption. The table size must be prime number limited to 5000011. If it is not specified, the default is 65537.
spec.to[].default.loadBalancer.random <object (free-form)>	# Random selects a random available host. The random load balancer generally performs better than round-robin if no health checking policy is configured. Random selection avoids bias towards the host in the set that comes after a failed host.
spec.to[].default.loadBalancer.ringHash <object>	# RingHash implements consistent hashing to upstream hosts. Each host is mapped onto a circle (the “ring”) by hashing its address; each request is then routed to a host by hashing some property of the request, and finding the nearest corresponding host clockwise around the ring.
spec.to[].default.loadBalancer.ringHash.hashFunction <string> enum=XXHash|MurmurHash2	# HashFunction is a function used to hash hosts onto the ketama ring. The value defaults to XX_HASH. Available values – XX_HASH, MURMUR_HASH_2.
spec.to[].default.loadBalancer.ringHash.hashPolicies <[]object>	# HashPolicies specify a list of request/connection properties that are used to calculate a hash. These hash policies are executed in the specified order. If a hash policy has the “terminal” attribute set to true, and there is already a hash generated, the hash is returned immediately, ignoring the rest of the hash policy list.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].connection <object>
spec.to[].default.loadBalancer.ringHash.hashPolicies[].connection.sourceIP <boolean>	# Hash on source IP address.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].cookie <object>
spec.to[].default.loadBalancer.ringHash.hashPolicies[].cookie.name <string> (required) min=1	# The name of the cookie that will be used to obtain the hash key.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].cookie.path <string>	# The name of the path for the cookie.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].cookie.ttl <string>	# If specified, a cookie with the TTL will be generated if the cookie is not present.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].filterState <object>
spec.to[].default.loadBalancer.ringHash.hashPolicies[].filterState.key <string> (required) min=1	# The name of the Object in the per-request filterState, which is an Envoy::Hashable object. If there is no data associated with the key, or the stored object is not Envoy::Hashable, no hash will be produced.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].header <object>
spec.to[].default.loadBalancer.ringHash.hashPolicies[].header.name <string> (required) min=1	# The name of the request header that will be used to obtain the hash key.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].queryParameter <object>
spec.to[].default.loadBalancer.ringHash.hashPolicies[].queryParameter.name <string> (required) min=1	# The name of the URL query parameter that will be used to obtain the hash key. If the parameter is not present, no hash will be produced. Query parameter names are case-sensitive.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].terminal <boolean>	# Terminal is a flag that short-circuits the hash computing. This field provides a ‘fallback’ style of configuration: “if a terminal policy doesn’t work, fallback to rest of the policy list”, it saves time when the terminal policy works. If true, and there is already a hash computed, ignore rest of the list of hash polices.
spec.to[].default.loadBalancer.ringHash.hashPolicies[].type <string> (required) enum=Header|Cookie|Connection|SourceIP|QueryParameter|FilterState
spec.to[].default.loadBalancer.ringHash.maxRingSize <integer> format=int32 min=1 max=8000000	# Maximum hash ring size. Defaults to 8M entries, and limited to 8M entries, but can be lowered to further constrain resource use.
spec.to[].default.loadBalancer.ringHash.minRingSize <integer> format=int32 min=1 max=8000000	# Minimum hash ring size. The larger the ring is (that is, the more hashes there are for each provided host) the better the request distribution will reflect the desired weights. Defaults to 1024 entries, and limited to 8M entries.
spec.to[].default.loadBalancer.roundRobin <object (free-form)>	# RoundRobin is a load balancing algorithm that distributes requests across available upstream hosts in round-robin order.
spec.to[].default.loadBalancer.type <string> (required) enum=RoundRobin|LeastRequest|RingHash|Random|Maglev
spec.to[].default.localityAwareness <object>	# LocalityAwareness contains configuration for locality aware load balancing.
spec.to[].default.localityAwareness.crossZone <object>	# CrossZone defines locality aware load balancing priorities when dataplane proxies inside local zone are unavailable
spec.to[].default.localityAwareness.crossZone.failover <[]object>	# Failover defines list of load balancing rules in order of priority
spec.to[].default.localityAwareness.crossZone.failover[].from <object>	# From defines the list of zones to which the rule applies
spec.to[].default.localityAwareness.crossZone.failover[].from.zones <[]string> (required)
spec.to[].default.localityAwareness.crossZone.failover[].to <object> (required)	# To defines to which zones the traffic should be load balanced
spec.to[].default.localityAwareness.crossZone.failover[].to.type <string> (required) enum=None|Only|Any|AnyExcept	# Type defines how target zones will be picked from available zones
spec.to[].default.localityAwareness.crossZone.failover[].to.zones <[]string>
spec.to[].default.localityAwareness.crossZone.failoverThreshold <object>	# FailoverThreshold defines the percentage of live destination dataplane proxies below which load balancing to the next priority starts. Example: If you configure failoverThreshold to 70, and you have deployed 10 destination dataplane proxies. Load balancing to next priority will start when number of live destination dataplane proxies drops below 7. Default 50
spec.to[].default.localityAwareness.crossZone.failoverThreshold.percentage <int-or-string> (required)
spec.to[].default.localityAwareness.disabled <boolean>	# Disabled allows to disable locality-aware load balancing. When disabled requests are distributed across all endpoints regardless of locality.
spec.to[].default.localityAwareness.localZone <object>	# LocalZone defines locality aware load balancing priorities between dataplane proxies inside a zone
spec.to[].default.localityAwareness.localZone.affinityTags <[]object>	# AffinityTags list of tags for local zone load balancing.
spec.to[].default.localityAwareness.localZone.affinityTags[].key <string> (required)	# Key defines tag for which affinity is configured
spec.to[].default.localityAwareness.localZone.affinityTags[].weight <integer> format=int32	# Weight of the tag used for load balancing. The bigger the weight the bigger the priority. Percentage of local traffic load balanced to tag is computed by dividing weight by sum of weights from all tags. For example with two affinity tags first with weight 80 and second with weight 20, then 80% of traffic will be redirected to the first tag, and 20% of traffic will be redirected to second one. Setting weights is not mandatory. When weights are not set control plane will compute default weight based on list order. Default: If you do not specify weight we will adjust them so that 90% traffic goes to first tag, 9% to next, and 1% to third and so on.
spec.to[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of destinations.
spec.to[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
