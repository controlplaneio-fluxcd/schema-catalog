# schema source: Kuma v2.14.0 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshRateLimit
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshRateLimit resource.
spec.from <[]object>	# From list makes a match between clients and corresponding configurations
spec.from[].default <object>	# Default is a configuration specific to the group of clients referenced in 'targetRef'
spec.from[].default.local <object>	# LocalConf defines local http or/and tcp rate limit configuration
spec.from[].default.local.http <object>	# LocalHTTP defines configuration of local HTTP rate limiting https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/local_rate_limit_filter
spec.from[].default.local.http.disabled <boolean>	# Define if rate limiting should be disabled.
spec.from[].default.local.http.onRateLimit <object>	# Describes the actions to take on a rate limit event
spec.from[].default.local.http.onRateLimit.headers <object>	# The Headers to be added to the HTTP response on a rate limit event
spec.from[].default.local.http.onRateLimit.headers.add <[]object> max=16
spec.from[].default.local.http.onRateLimit.headers.add[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.from[].default.local.http.onRateLimit.headers.add[].value <string> (required)
spec.from[].default.local.http.onRateLimit.headers.set <[]object> max=16
spec.from[].default.local.http.onRateLimit.headers.set[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.from[].default.local.http.onRateLimit.headers.set[].value <string> (required)
spec.from[].default.local.http.onRateLimit.status <integer> format=int32	# The HTTP status code to be set on a rate limit event
spec.from[].default.local.http.requestRate <object>	# Defines how many requests are allowed per interval.
spec.from[].default.local.http.requestRate.interval <string> (required)	# The interval the number of units is accounted for.
spec.from[].default.local.http.requestRate.num <integer> (required) format=int32	# Number of units per interval (depending on usage it can be a number of requests, or a number of connections).
spec.from[].default.local.tcp <object>	# LocalTCP defines confguration of local TCP rate limiting https://www.envoyproxy.io/docs/envoy/latest/configuration/listeners/network_filters/local_rate_limit_filter
spec.from[].default.local.tcp.connectionRate <object>	# Defines how many connections are allowed per interval.
spec.from[].default.local.tcp.connectionRate.interval <string> (required)	# The interval the number of units is accounted for.
spec.from[].default.local.tcp.connectionRate.num <integer> (required) format=int32	# Number of units per interval (depending on usage it can be a number of requests, or a number of connections).
spec.from[].default.local.tcp.disabled <boolean>	# Define if rate limiting should be disabled. Default: false
spec.from[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of clients.
spec.from[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.from[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.from[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.from[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.from[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.from[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.from[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.from[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.rules <[]object>	# Rules defines inbound rate limiting configurations.
spec.rules[].default <object>	# Default contains configuration of the inbound rate limits
spec.rules[].default.local <object>	# LocalConf defines local http or/and tcp rate limit configuration
spec.rules[].default.local.http <object>	# LocalHTTP defines configuration of local HTTP rate limiting https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/local_rate_limit_filter
spec.rules[].default.local.http.disabled <boolean>	# Define if rate limiting should be disabled.
spec.rules[].default.local.http.onRateLimit <object>	# Describes the actions to take on a rate limit event
spec.rules[].default.local.http.onRateLimit.headers <object>	# The Headers to be added to the HTTP response on a rate limit event
spec.rules[].default.local.http.onRateLimit.headers.add <[]object> max=16
spec.rules[].default.local.http.onRateLimit.headers.add[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.rules[].default.local.http.onRateLimit.headers.add[].value <string> (required)
spec.rules[].default.local.http.onRateLimit.headers.set <[]object> max=16
spec.rules[].default.local.http.onRateLimit.headers.set[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.rules[].default.local.http.onRateLimit.headers.set[].value <string> (required)
spec.rules[].default.local.http.onRateLimit.status <integer> format=int32	# The HTTP status code to be set on a rate limit event
spec.rules[].default.local.http.requestRate <object>	# Defines how many requests are allowed per interval.
spec.rules[].default.local.http.requestRate.interval <string> (required)	# The interval the number of units is accounted for.
spec.rules[].default.local.http.requestRate.num <integer> (required) format=int32	# Number of units per interval (depending on usage it can be a number of requests, or a number of connections).
spec.rules[].default.local.tcp <object>	# LocalTCP defines confguration of local TCP rate limiting https://www.envoyproxy.io/docs/envoy/latest/configuration/listeners/network_filters/local_rate_limit_filter
spec.rules[].default.local.tcp.connectionRate <object>	# Defines how many connections are allowed per interval.
spec.rules[].default.local.tcp.connectionRate.interval <string> (required)	# The interval the number of units is accounted for.
spec.rules[].default.local.tcp.connectionRate.num <integer> (required) format=int32	# Number of units per interval (depending on usage it can be a number of requests, or a number of connections).
spec.rules[].default.local.tcp.disabled <boolean>	# Define if rate limiting should be disabled. Default: false
spec.rules[].matches <[]object>	# Matches define additional conditions for applying this rate limit rule.
spec.rules[].matches[].sni <object>	# SNI defines a matcher configuration for matching by SNI value carried on the TLS connection
spec.rules[].matches[].sni.type <string> (required) enum=Exact	# Type defines how to match traffic by SNI. Only `Exact` is supported.
spec.rules[].matches[].sni.value <string> (required)	# Value is the SNI carried on the TLS connection that needs to match for the configuration to be applied
spec.rules[].matches[].spiffeID <object>	# SpiffeID defines a matcher configuration for SpiffeID matching
spec.rules[].matches[].spiffeID.type <string> (required) enum=Exact|Prefix	# Type defines how to match incoming traffic by SpiffeID. `Exact` or `Prefix` are allowed.
spec.rules[].matches[].spiffeID.value <string> (required)	# Value is SpiffeID of a client that needs to match for the configuration to be applied
spec.targetRef <object>	# TargetRef is a reference to the resource the policy takes an effect on. The resource could be either a real store object or virtual resource defined inplace.
spec.targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
spec.to <[]object>	# To list makes a match between clients and corresponding configurations
spec.to[].default <object>	# Default is a configuration specific to the group of clients referenced in 'targetRef'
spec.to[].default.local <object>	# LocalConf defines local http or/and tcp rate limit configuration
spec.to[].default.local.http <object>	# LocalHTTP defines configuration of local HTTP rate limiting https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/local_rate_limit_filter
spec.to[].default.local.http.disabled <boolean>	# Define if rate limiting should be disabled.
spec.to[].default.local.http.onRateLimit <object>	# Describes the actions to take on a rate limit event
spec.to[].default.local.http.onRateLimit.headers <object>	# The Headers to be added to the HTTP response on a rate limit event
spec.to[].default.local.http.onRateLimit.headers.add <[]object> max=16
spec.to[].default.local.http.onRateLimit.headers.add[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].default.local.http.onRateLimit.headers.add[].value <string> (required)
spec.to[].default.local.http.onRateLimit.headers.set <[]object> max=16
spec.to[].default.local.http.onRateLimit.headers.set[].name <string> (required) pattern="^[a-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256
spec.to[].default.local.http.onRateLimit.headers.set[].value <string> (required)
spec.to[].default.local.http.onRateLimit.status <integer> format=int32	# The HTTP status code to be set on a rate limit event
spec.to[].default.local.http.requestRate <object>	# Defines how many requests are allowed per interval.
spec.to[].default.local.http.requestRate.interval <string> (required)	# The interval the number of units is accounted for.
spec.to[].default.local.http.requestRate.num <integer> (required) format=int32	# Number of units per interval (depending on usage it can be a number of requests, or a number of connections).
spec.to[].default.local.tcp <object>	# LocalTCP defines confguration of local TCP rate limiting https://www.envoyproxy.io/docs/envoy/latest/configuration/listeners/network_filters/local_rate_limit_filter
spec.to[].default.local.tcp.connectionRate <object>	# Defines how many connections are allowed per interval.
spec.to[].default.local.tcp.connectionRate.interval <string> (required)	# The interval the number of units is accounted for.
spec.to[].default.local.tcp.connectionRate.num <integer> (required) format=int32	# Number of units per interval (depending on usage it can be a number of requests, or a number of connections).
spec.to[].default.local.tcp.disabled <boolean>	# Define if rate limiting should be disabled. Default: false
spec.to[].targetRef <object> (required)	# TargetRef is a reference to the resource that represents a group of clients.
spec.to[].targetRef.kind <string> (required) enum=Mesh|MeshSubset|MeshGateway|MeshService|MeshExternalService|MeshMultiZoneService|MeshServiceSubset|MeshHTTPRoute|Dataplane	# Kind of the referenced resource
spec.to[].targetRef.labels <map[string]string>	# Labels are used to select group of MeshServices that match labels. Either Labels or Name and Namespace can be used.
spec.to[].targetRef.mesh <string>	# Mesh is reserved for future use to identify cross mesh resources.
spec.to[].targetRef.name <string>	# Name of the referenced resource. Can only be used with kinds: `MeshService`, `MeshServiceSubset` and `MeshGatewayRoute`
spec.to[].targetRef.namespace <string>	# Namespace specifies the namespace of target resource. If empty only resources in policy namespace will be targeted.
spec.to[].targetRef.proxyTypes <[]string>	# ProxyTypes specifies the data plane types that are subject to the policy. When not specified, all data plane types are targeted by the policy.
spec.to[].targetRef.sectionName <string>	# SectionName is used to target specific section of resource. For example, you can target port from MeshService.ports[] by its name. Only traffic to this port will be affected.
spec.to[].targetRef.tags <map[string]string>	# Tags used to select a subset of proxies by tags. Can only be used with kinds `MeshSubset` and `MeshServiceSubset`
