# schema source: Kuma v2.14.1 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshTrust
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshTrust resource.
spec.caBundles <[]object> (required) min=1	# CABundles contains a list of CA bundles supported by this TrustDomain. At least one CA bundle must be specified.
spec.caBundles[].pem <object>	# Pem contains the PEM-encoded CA bundle if the Type is set to a PEM-based format.
spec.caBundles[].pem.value <string> (required)	# Value holds the PEM-encoded CA bundle as a string.
spec.caBundles[].type <string> (required) enum=Pem	# Type specifies the format or source type of the CA bundle.
spec.origin <object>	# Origin specifies whether the resource was created from a MeshIdentity. Deprecated: use Status.Origin instead
spec.origin.kri <string>	# Resource identifier
spec.trustDomain <string> (required) max=253	# TrustDomain is the trust domain associated with this resource.
status <object>	# Status is the current status of the Kuma MeshTrust resource.
status.origin <object>	# Origin specifies whether the resource was created from a MeshIdentity.
status.origin.kri <string>	# Resource identifier
