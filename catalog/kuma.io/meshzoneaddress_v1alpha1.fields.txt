# schema source: Kuma v2.14.0 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshZoneAddress
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshZoneAddress resource.
spec.address <string> (required) min=1	# Address is the publicly reachable address of the zone ingress.
spec.port <integer> (required) format=int32 min=1 max=65535	# Port is the publicly reachable port of the zone ingress.
