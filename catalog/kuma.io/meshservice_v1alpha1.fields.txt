# schema source: Kuma v2.14.0 https://github.com/kumahq/kuma
apiVersion <string> enum=kuma.io/v1alpha1
kind <string> enum=MeshService
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the specification of the Kuma MeshService resource.
spec.identities <[]object>
spec.identities[].type <string> (required) enum=ServiceTag|SpiffeID
spec.identities[].value <string> (required)
spec.ports <[]object>
spec.ports[].appProtocol <string> default="tcp"	# Protocol identifies a protocol supported by a service.
spec.ports[].name <string>
spec.ports[].port <integer> (required) format=int32
spec.ports[].targetPort <int-or-string>
spec.selector <object>
spec.selector.dataplaneLabels <object>
spec.selector.dataplaneLabels.matchLabels <map[string]string>
spec.selector.dataplaneRef <object>
spec.selector.dataplaneRef.name <string>
spec.selector.dataplaneTags <map[string]string>
spec.state <string> enum=Available|Unavailable default="Unavailable"	# State of MeshService. Available if there is at least one healthy endpoint. Otherwise, Unavailable. It's used for cross zone communication to check if we should send traffic to it, when MeshService is aggregated into MeshMultiZoneService.
status <object>	# Status is the current status of the Kuma MeshService resource.
status.addresses <[]object>
status.addresses[].hostname <string>
status.addresses[].hostnameGeneratorRef <object>
status.addresses[].hostnameGeneratorRef.coreName <string> (required)
status.addresses[].origin <string>
status.dataplaneProxies <object>	# Data plane proxies statistics selected by this MeshService.
status.dataplaneProxies.connected <integer>	# Number of data plane proxies connected to the zone control plane
status.dataplaneProxies.healthy <integer>	# Number of data plane proxies with all healthy inbounds selected by this MeshService.
status.dataplaneProxies.total <integer>	# Total number of data plane proxies.
status.hostnameGenerators <[]object>
status.hostnameGenerators[].conditions <[]object>	# Conditions is an array of hostname generator conditions.
status.hostnameGenerators[].conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.hostnameGenerators[].conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.hostnameGenerators[].conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.hostnameGenerators[].conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.hostnameGenerators[].hostnameGeneratorRef <object> (required)
status.hostnameGenerators[].hostnameGeneratorRef.coreName <string> (required)
status.tls <object>
status.tls.status <string> enum=Ready|NotReady
status.vips <[]object>
status.vips[].ip <string>
