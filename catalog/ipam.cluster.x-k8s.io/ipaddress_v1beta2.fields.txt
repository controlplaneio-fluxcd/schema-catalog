# schema source: Cluster API v1.13.3 https://github.com/kubernetes-sigs/cluster-api
apiVersion <string> enum=ipam.cluster.x-k8s.io/v1beta2
kind <string> enum=IPAddress
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# spec is the desired state of IPAddress.
spec.address <string> (required) min=1 max=39	# address is the IP address.
spec.claimRef <object> (required)	# claimRef is a reference to the claim this IPAddress was created for.
spec.claimRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPAddressClaim. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.gateway <string> min=1 max=39	# gateway is the network gateway of the network the address is from.
spec.poolRef <object> (required)	# poolRef is a reference to the pool that this IPAddress was created from.
spec.poolRef.apiGroup <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.poolRef.kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.poolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.prefix <integer> (required) format=int32 min=0 max=128	# prefix is the prefix of the address.
