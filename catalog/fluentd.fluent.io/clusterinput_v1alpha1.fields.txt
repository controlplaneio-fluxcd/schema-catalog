# schema source: Fluent Operator v3.9.0 https://github.com/fluent/fluent-operator
apiVersion <string> enum=fluentd.fluent.io/v1alpha1
kind <string> enum=ClusterInput (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ClusterInputSpec defines the desired state of ClusterInput
spec.inputs <[]object>
spec.inputs[].customPlugin <object>	# Custom plugin type
spec.inputs[].customPlugin.config <string> (required)
spec.inputs[].forward <object>	# in_forward plugin
spec.inputs[].forward.addTagPrefix <string>	# Adds the prefix to the incoming event's tag.
spec.inputs[].forward.bind <string>	# The port to listen to, default is "0.0.0.0"
spec.inputs[].forward.chunkSizeLimit <string> pattern="^\\d+(KB|MB|GB|TB)$"	# The size limit of the received chunk. If the chunk size is larger than this value, the received chunk is dropped.
spec.inputs[].forward.chunkSizeWarnLimit <string> pattern="^\\d+(KB|MB|GB|TB)$"	# The warning size limit of the received chunk. If the chunk size is larger than this value, a warning message will be sent.
spec.inputs[].forward.client <object>	# The security section of client plugin
spec.inputs[].forward.client.host <string>	# The IP address or hostname of the client. This is exclusive with Network.
spec.inputs[].forward.client.network <string>	# The network address specification. This is exclusive with Host.
spec.inputs[].forward.client.sharedKey <string>	# The shared key per client.
spec.inputs[].forward.client.users <string>	# The array of usernames.
spec.inputs[].forward.denyKeepalive <boolean>	# The connections will be disconnected right after receiving a message, if true.
spec.inputs[].forward.lingerTimeout <integer>	# The timeout used to set the linger option.
spec.inputs[].forward.port <integer> format=int32 min=1 max=65535	# The port to listen to, default is 24224.
spec.inputs[].forward.resolveHostname <boolean>	# Tries to resolve hostname from IP addresses or not.
spec.inputs[].forward.security <object>	# The security section of forward plugin
spec.inputs[].forward.security.allowAnonymousSource <string>	# Allows the anonymous source. <client> sections are required, if disabled.
spec.inputs[].forward.security.selfHostname <string>	# The hostname.
spec.inputs[].forward.security.sharedKey <string>	# The shared key for authentication.
spec.inputs[].forward.security.user <object>	# Defines user section directly.
spec.inputs[].forward.security.user.password <object>	# Secret defines the key of a value.
spec.inputs[].forward.security.user.password.valueFrom <object>	# ValueSource defines how to find a value's key.
spec.inputs[].forward.security.user.password.valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.inputs[].forward.security.user.password.valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.inputs[].forward.security.user.password.valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.inputs[].forward.security.user.password.valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.inputs[].forward.security.user.username <object>	# Secret defines the key of a value.
spec.inputs[].forward.security.user.username.valueFrom <object>	# ValueSource defines how to find a value's key.
spec.inputs[].forward.security.user.username.valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.inputs[].forward.security.user.username.valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.inputs[].forward.security.user.username.valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.inputs[].forward.security.user.username.valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.inputs[].forward.security.userAuth <string>	# If true, user-based authentication is used.
spec.inputs[].forward.sendKeepalivePacket <boolean>	# Enables the TCP keepalive for sockets.
spec.inputs[].forward.skipInvalidEvent <boolean>	# Skips the invalid incoming event.
spec.inputs[].forward.sourceAddressKey <string>	# The field name of the client's source address. If set, the client's address will be set to its key.
spec.inputs[].forward.sourceHostnameKey <string>	# The field name of the client's hostname. If set, the client's hostname will be set to its key.
spec.inputs[].forward.tag <string>	# in_forward uses incoming event's tag by default (See Protocol Section). If the tag parameter is set, its value is used instead.
spec.inputs[].forward.transport <object>	# The transport section of forward plugin
spec.inputs[].forward.transport.caCertPath <string>	# for Cert generated
spec.inputs[].forward.transport.caPath <string>	# for Cert signed by public CA
spec.inputs[].forward.transport.caPrivateKeyPassphrase <string>
spec.inputs[].forward.transport.caPrivateKeyPath <string>
spec.inputs[].forward.transport.certPath <string>
spec.inputs[].forward.transport.certVerifier <string>	# other parameters
spec.inputs[].forward.transport.ciphers <string>
spec.inputs[].forward.transport.clientCertAuth <boolean>
spec.inputs[].forward.transport.insecure <boolean>
spec.inputs[].forward.transport.privateKeyPassphrase <string>
spec.inputs[].forward.transport.privateKeyPath <string>
spec.inputs[].forward.transport.protocol <string>	# The protocol name of this plugin, i.e: tls
spec.inputs[].forward.transport.version <string>
spec.inputs[].forward.user <object>	# The security section of user plugin
spec.inputs[].forward.user.password <object>	# Secret defines the key of a value.
spec.inputs[].forward.user.password.valueFrom <object>	# ValueSource defines how to find a value's key.
spec.inputs[].forward.user.password.valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.inputs[].forward.user.password.valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.inputs[].forward.user.password.valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.inputs[].forward.user.password.valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.inputs[].forward.user.username <object>	# Secret defines the key of a value.
spec.inputs[].forward.user.username.valueFrom <object>	# ValueSource defines how to find a value's key.
spec.inputs[].forward.user.username.valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.inputs[].forward.user.username.valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.inputs[].forward.user.username.valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.inputs[].forward.user.username.valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.inputs[].http <object>	# in_http plugin
spec.inputs[].http.addHttpHeaders <boolean>	# Adds HTTP_ prefix headers to the record.
spec.inputs[].http.addRemoteAddr <string>	# Adds REMOTE_ADDR field to the record. The value of REMOTE_ADDR is the client's address. i.e: X-Forwarded-For: host1, host2
spec.inputs[].http.bind <string>	# The port to listen to, default is "0.0.0.0"
spec.inputs[].http.bodySizeLimit <string> pattern="^\\d+(KB|MB|GB|TB)$"	# The size limit of the POSTed element.
spec.inputs[].http.corsAllOrigins <string>	# Whitelist domains for CORS.
spec.inputs[].http.corsAllowCredentials <string>	# Add Access-Control-Allow-Credentials header. It's needed when a request's credentials mode is include
spec.inputs[].http.keepaliveTimeout <string> pattern="^\\d+(\\.[0-9]{0,2})?(s|m|h|d)?$"	# The timeout limit for keeping the connection alive.
spec.inputs[].http.parse <object>	# The parse section of http plugin
spec.inputs[].http.parse.customPatternPath <string>	# Path to the file that includes custom grok patterns.
spec.inputs[].http.parse.estimateCurrentEvent <boolean>	# If true, use Fluent::Eventnow(current time) as a timestamp when time_key is specified.
spec.inputs[].http.parse.expression <string>	# Specifies the regular expression for matching logs. Regular expression also supports i and m suffix.
spec.inputs[].http.parse.grok <[]object>	# Grok Sections
spec.inputs[].http.parse.grok[].keepTimeKey <boolean>	# If true, keep time field in the record.
spec.inputs[].http.parse.grok[].name <string>	# The name of this grok section.
spec.inputs[].http.parse.grok[].pattern <string>	# The pattern of grok. Required parameter.
spec.inputs[].http.parse.grok[].timeFormat <string>	# Process value using specified format. This is available only when time_type is string
spec.inputs[].http.parse.grok[].timeKey <string>	# Specify time field for event time. If the event doesn't have this field, current time is used.
spec.inputs[].http.parse.grok[].timeZone <string>	# Use specified timezone. one can parse/format the time value in the specified timezone.
spec.inputs[].http.parse.grokFailureKey <string>	# The key has grok failure reason.
spec.inputs[].http.parse.grokPattern <string>	# The pattern of grok.
spec.inputs[].http.parse.grokPatternSeries <string>	# Specify grok pattern series set.
spec.inputs[].http.parse.id <string>	# The @id parameter specifies a unique name for the configuration.
spec.inputs[].http.parse.keepTimeKey <boolean>	# If true, keep time field in th record.
spec.inputs[].http.parse.localtime <boolean>	# If true, uses local time.
spec.inputs[].http.parse.logLevel <string>	# The @log_level parameter specifies the plugin-specific logging level
spec.inputs[].http.parse.multiLineStartRegexp <string>	# The regexp to match beginning of multiline. This is only for "multiline_grok".
spec.inputs[].http.parse.timeFormat <string>	# Process value according to the specified format. This is available only when time_type is string
spec.inputs[].http.parse.timeFormatFallbacks <string>	# Uses the specified time format as a fallback in the specified order. You can parse undetermined time format by using time_format_fallbacks. This options is enabled when time_type is mixed.
spec.inputs[].http.parse.timeKey <string>	# Specify time field for event time. If the event doesn't have this field, current time is used.
spec.inputs[].http.parse.timeType <string> enum=float|unixtime|string|mixed	# parses/formats value according to this type, default is string
spec.inputs[].http.parse.timeout <string> pattern="^\\d+(\\.[0-9]{0,2})?(s|m|h|d)?$"	# Specify timeout for parse processing.
spec.inputs[].http.parse.timezone <string>	# Uses the specified timezone.
spec.inputs[].http.parse.type <string> (required) enum=regexp|apache2|apache_error|nginx|syslog|csv|tsv|ltsv|json|multiline|none|grok|multiline_grok	# The @type parameter specifies the type of the plugin.
spec.inputs[].http.parse.types <string>	# Specify types for converting field into another, i.e: types user_id:integer,paid:bool,paid_usd_amount:float
spec.inputs[].http.parse.utc <boolean>	# If true, uses UTC.
spec.inputs[].http.port <integer> format=int32 min=1 max=65535	# The port to listen to, default is 9880.
spec.inputs[].http.respondsWithEmptyImg <boolean>	# Responds with an empty GIF image of 1x1 pixel (rather than an empty string).
spec.inputs[].http.transport <object>	# The transport section of http plugin
spec.inputs[].http.transport.caCertPath <string>	# for Cert generated
spec.inputs[].http.transport.caPath <string>	# for Cert signed by public CA
spec.inputs[].http.transport.caPrivateKeyPassphrase <string>
spec.inputs[].http.transport.caPrivateKeyPath <string>
spec.inputs[].http.transport.certPath <string>
spec.inputs[].http.transport.certVerifier <string>	# other parameters
spec.inputs[].http.transport.ciphers <string>
spec.inputs[].http.transport.clientCertAuth <boolean>
spec.inputs[].http.transport.insecure <boolean>
spec.inputs[].http.transport.privateKeyPassphrase <string>
spec.inputs[].http.transport.privateKeyPath <string>
spec.inputs[].http.transport.protocol <string>	# The protocol name of this plugin, i.e: tls
spec.inputs[].http.transport.version <string>
spec.inputs[].id <string>	# The @id parameter specifies a unique name for the configuration.
spec.inputs[].label <string>	# The @label parameter is to route the input events to <label> sections.
spec.inputs[].logLevel <string>	# The @log_level parameter specifies the plugin-specific logging level
spec.inputs[].monitorAgent <object>	# monitor_agent plugin
spec.inputs[].monitorAgent.bind <string>	# The bind address to listen to.
spec.inputs[].monitorAgent.emitInterval <integer> format=int64	# The interval time between event emits. This will be used when "tag" is configured.
spec.inputs[].monitorAgent.includeConfig <boolean>	# You can set this option to false to remove the config field from the response.
spec.inputs[].monitorAgent.includeRetry <boolean>	# You can set this option to false to remove the retry field from the response.
spec.inputs[].monitorAgent.port <integer> format=int64	# The port to listen to.
spec.inputs[].monitorAgent.tag <string>	# If you set this parameter, this plugin emits metrics as records.
spec.inputs[].sample <object>	# in_sample plugin
spec.inputs[].sample.autoIncrementKey <string>	# If specified, each generated event has an auto-incremented key field.
spec.inputs[].sample.rate <integer> format=int64	# It configures how many events to generate per second.
spec.inputs[].sample.sample <string>	# The sample data to be generated. It should be either an array of JSON hashes or a single JSON hash. If it is an array of JSON hashes, the hashes in the array are cycled through in order.
spec.inputs[].sample.size <integer> format=int64	# The number of events in the event stream of each emit.
spec.inputs[].sample.tag <string>	# The tag of the event. The value is the tag assigned to the generated events.
spec.inputs[].tail <object>	# in_tail plugin
spec.inputs[].tail.emitUnmatchedLines <boolean>	# Emits unmatched lines when <parse> format is not matched for incoming logs.
spec.inputs[].tail.enableStatWatcher <boolean>	# Enables the additional inotify-based watcher. Setting this parameter to false will disable the inotify events and use only timer watcher for file tailing. This option is mainly for avoiding the stuck issue with inotify.
spec.inputs[].tail.enableWatchTimer <boolean>	# Enables the additional watch timer. Setting this parameter to false will significantly reduce CPU and I/O consumption when tailing a large number of files on systems with inotify support. The default is true which results in an additional 1 second timer being used.
spec.inputs[].tail.encoding <string>	# Specifies the encoding of reading lines. By default, in_tail emits string value as ASCII-8BIT encoding. If encoding is specified, in_tail changes string to encoding. If encoding and fromEncoding both are specified, in_tail tries to encode string from fromEncoding to encoding.
spec.inputs[].tail.excludePath <[]string>	# The paths excluded from the watcher list.
spec.inputs[].tail.followInodes <boolean>	# Avoid to read rotated files duplicately. You should set true when you use * or strftime format in path.
spec.inputs[].tail.fromEncoding <string>	# Specifies the encoding of reading lines. By default, in_tail emits string value as ASCII-8BIT encoding. If encoding is specified, in_tail changes string to encoding. If encoding and fromEncoding both are specified, in_tail tries to encode string from fromEncoding to encoding.
spec.inputs[].tail.group <object>	# The in_tail plugin can assign each log file to a group, based on user defined rules. The limit parameter controls the total number of lines collected for a group within a rate_period time interval.
spec.inputs[].tail.group.pattern <string>	# Specifies the regular expression for extracting metadata (namespace, podname) from log file path. Default value of the pattern regexp extracts information about namespace, podname, docker_id, container of the log (K8s specific).
spec.inputs[].tail.group.ratePeriod <integer> format=int32	# Time period in which the group line limit is applied. in_tail resets the counter after every rate_period interval.
spec.inputs[].tail.group.rule <object> (required)	# Grouping rules for log files.
spec.inputs[].tail.group.rule.limit <integer> format=int32	# Maximum number of lines allowed from a group in rate_period time interval. The default value of -1 doesn't throttle log files of that group.
spec.inputs[].tail.group.rule.match <map[string]string>	# match parameter is used to check if a file belongs to a particular group based on hash keys (named captures from pattern) and hash values (regexp in string)
spec.inputs[].tail.ignoreRepeatedPermissionError <boolean>	# If you have to exclude the non-permission files from the watch list, set this parameter to true. It suppresses the repeated permission error logs.
spec.inputs[].tail.limitRecentlyModified <integer> format=int32	# Limits the watching files that the modification time is within the specified time range when using * in path.
spec.inputs[].tail.maxLineSize <integer> format=int32	# The maximum length of a line. Longer lines than it will be just skipped.
spec.inputs[].tail.multilineFlushInterval <integer> format=int32	# The interval of flushing the buffer for multiline format.
spec.inputs[].tail.openOnEveryUpdate <boolean>	# Opens and closes the file on every update instead of leaving it open until it gets rotated.
spec.inputs[].tail.parse <object> (required)	# Parse defines various parameters for the parse plugin
spec.inputs[].tail.parse.customPatternPath <string>	# Path to the file that includes custom grok patterns.
spec.inputs[].tail.parse.estimateCurrentEvent <boolean>	# If true, use Fluent::Eventnow(current time) as a timestamp when time_key is specified.
spec.inputs[].tail.parse.expression <string>	# Specifies the regular expression for matching logs. Regular expression also supports i and m suffix.
spec.inputs[].tail.parse.grok <[]object>	# Grok Sections
spec.inputs[].tail.parse.grok[].keepTimeKey <boolean>	# If true, keep time field in the record.
spec.inputs[].tail.parse.grok[].name <string>	# The name of this grok section.
spec.inputs[].tail.parse.grok[].pattern <string>	# The pattern of grok. Required parameter.
spec.inputs[].tail.parse.grok[].timeFormat <string>	# Process value using specified format. This is available only when time_type is string
spec.inputs[].tail.parse.grok[].timeKey <string>	# Specify time field for event time. If the event doesn't have this field, current time is used.
spec.inputs[].tail.parse.grok[].timeZone <string>	# Use specified timezone. one can parse/format the time value in the specified timezone.
spec.inputs[].tail.parse.grokFailureKey <string>	# The key has grok failure reason.
spec.inputs[].tail.parse.grokPattern <string>	# The pattern of grok.
spec.inputs[].tail.parse.grokPatternSeries <string>	# Specify grok pattern series set.
spec.inputs[].tail.parse.id <string>	# The @id parameter specifies a unique name for the configuration.
spec.inputs[].tail.parse.keepTimeKey <boolean>	# If true, keep time field in th record.
spec.inputs[].tail.parse.localtime <boolean>	# If true, uses local time.
spec.inputs[].tail.parse.logLevel <string>	# The @log_level parameter specifies the plugin-specific logging level
spec.inputs[].tail.parse.multiLineStartRegexp <string>	# The regexp to match beginning of multiline. This is only for "multiline_grok".
spec.inputs[].tail.parse.timeFormat <string>	# Process value according to the specified format. This is available only when time_type is string
spec.inputs[].tail.parse.timeFormatFallbacks <string>	# Uses the specified time format as a fallback in the specified order. You can parse undetermined time format by using time_format_fallbacks. This options is enabled when time_type is mixed.
spec.inputs[].tail.parse.timeKey <string>	# Specify time field for event time. If the event doesn't have this field, current time is used.
spec.inputs[].tail.parse.timeType <string> enum=float|unixtime|string|mixed	# parses/formats value according to this type, default is string
spec.inputs[].tail.parse.timeout <string> pattern="^\\d+(\\.[0-9]{0,2})?(s|m|h|d)?$"	# Specify timeout for parse processing.
spec.inputs[].tail.parse.timezone <string>	# Uses the specified timezone.
spec.inputs[].tail.parse.type <string> (required) enum=regexp|apache2|apache_error|nginx|syslog|csv|tsv|ltsv|json|multiline|none|grok|multiline_grok	# The @type parameter specifies the type of the plugin.
spec.inputs[].tail.parse.types <string>	# Specify types for converting field into another, i.e: types user_id:integer,paid:bool,paid_usd_amount:float
spec.inputs[].tail.parse.utc <boolean>	# If true, uses UTC.
spec.inputs[].tail.path <string> (required)	# The path(s) to read. Multiple paths can be specified, separated by comma ','.
spec.inputs[].tail.pathKey <string>	# Adds the watching file path to the path_key field.
spec.inputs[].tail.pathTimezone <string>	# This parameter is for strftime formatted path like /path/to/%Y/%m/%d/.
spec.inputs[].tail.posFile <string>	# (recommended) Fluentd will record the position it last read from this file. pos_file handles multiple positions in one file so no need to have multiple pos_file parameters per source. Don't share pos_file between in_tail configurations. It causes unexpected behavior e.g. corrupt pos_file content.
spec.inputs[].tail.posFileCompactionInterval <integer> format=int32	# The interval of doing compaction of pos file.
spec.inputs[].tail.readBytesLimitPerSecond <integer> format=int32	# The number of reading bytes per second to read with I/O operation. This value should be equal or greater than 8192.
spec.inputs[].tail.readFromHead <boolean>	# Starts to read the logs from the head of the file or the last read position recorded in pos_file, not tail.
spec.inputs[].tail.readLinesLimit <integer> format=int32	# The number of lines to read with each I/O operation.
spec.inputs[].tail.refreshInterval <integer> format=int32	# The interval to refresh the list of watch files. This is used when the path includes *.
spec.inputs[].tail.rotateWait <integer> format=int32	# in_tail actually does a bit more than tail -F itself. When rotating a file, some data may still need to be written to the old file as opposed to the new one. in_tail takes care of this by keeping a reference to the old file (even after it has been rotated) for some time before transitioning completely to the new file. This helps prevent data designated for the old file from getting lost. By default, this time interval is 5 seconds. The rotate_wait parameter accepts a single integer representing the number of seconds you want this time interval to be.
spec.inputs[].tail.skipRefreshOnStartup <boolean>	# Skips the refresh of the watch list on startup. This reduces the startup time when * is used in path.
spec.inputs[].tail.tag <string> (required)	# The tag of the event.
status <object (free-form)>	# ClusterInputStatus defines the observed state of ClusterInput
