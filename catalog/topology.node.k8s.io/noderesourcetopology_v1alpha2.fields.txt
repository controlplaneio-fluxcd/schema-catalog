# schema source: NFD NodeResourceTopology v0.18.3 https://github.com/kubernetes-sigs/node-feature-discovery
apiVersion <string> enum=topology.node.k8s.io/v1alpha2
kind <string> enum=NodeResourceTopology (cluster-scoped)
metadata.name <string> (required)
attributes <[]object>	# AttributeList contains an array of AttributeInfo objects.
attributes[].name <string> (required)
attributes[].value <string> (required)
topologyPolicies <[]string>	# DEPRECATED (to be removed in v1beta1): use top level attributes if needed
zones <[]object> (required)	# ZoneList contains an array of Zone objects.
zones[].attributes <[]object>	# AttributeList contains an array of AttributeInfo objects.
zones[].attributes[].name <string> (required)
zones[].attributes[].value <string> (required)
zones[].costs <[]object>	# CostList contains an array of CostInfo objects.
zones[].costs[].name <string> (required)
zones[].costs[].value <integer> (required) format=int64
zones[].name <string> (required)
zones[].parent <string>
zones[].resources <[]object>	# ResourceInfoList contains an array of ResourceInfo objects.
zones[].resources[].allocatable <int-or-string> (required)	# Allocatable quantity of the resource, corresponding to allocatable in node status, i.e. total amount of this resource available to be used by pods.
zones[].resources[].available <int-or-string> (required)	# Available is the amount of this resource currently available for new (to be scheduled) pods, i.e. Allocatable minus the resources reserved by currently running pods.
zones[].resources[].capacity <int-or-string> (required)	# Capacity of the resource, corresponding to capacity in node status, i.e. total amount of this resource that the node has.
zones[].resources[].name <string> (required)	# Name of the resource.
zones[].type <string> (required)
