# schema source: Istio 1.30.3 https://github.com/istio/istio
apiVersion <string> enum=security.istio.io/v1
kind <string> enum=RequestAuthentication
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Request authentication configuration for workloads. See more details at: https://istio.io/docs/reference/config/security/request_authentication.html
spec.jwtRules <[]object> max=4096	# Define the list of JWTs that can be validated at the selected workloads' proxy.
spec.jwtRules[].audiences <[]string>	# The list of JWT [audiences](https://tools.ietf.org/html/rfc7519#section-4.1.3) that are allowed to access.
spec.jwtRules[].forwardOriginalToken <boolean>	# If set to true, the original token will be kept for the upstream request.
spec.jwtRules[].fromCookies <[]string>	# List of cookie names from which JWT is expected.
spec.jwtRules[].fromHeaders <[]object>	# List of header locations from which JWT is expected.
spec.jwtRules[].fromHeaders[].name <string> (required) min=1	# The HTTP header name.
spec.jwtRules[].fromHeaders[].prefix <string>	# The prefix that should be stripped before decoding the token.
spec.jwtRules[].fromParams <[]string>	# List of query parameters from which JWT is expected.
spec.jwtRules[].issuer <string> min=1	# Identifies the issuer that issued the JWT.
spec.jwtRules[].jwks <string>	# JSON Web Key Set of public keys to validate signature of the JWT.
spec.jwtRules[].jwksUri <string> min=1 max=2048	# URL of the provider's public key set to validate signature of the JWT.
spec.jwtRules[].jwks_uri <string> min=1 max=2048	# URL of the provider's public key set to validate signature of the JWT.
spec.jwtRules[].outputClaimToHeaders <[]object>	# This field specifies a list of operations to copy the claim to HTTP headers on a successfully verified token.
spec.jwtRules[].outputClaimToHeaders[].claim <string> (required) min=1	# The name of the claim to be copied from.
spec.jwtRules[].outputClaimToHeaders[].header <string> (required) pattern="^[-_A-Za-z0-9]+$" min=1	# The name of the header to be created.
spec.jwtRules[].outputPayloadToHeader <string>	# This field specifies the header name to output a successfully verified JWT payload to the backend.
spec.jwtRules[].spaceDelimitedClaims <[]string> max=64	# List of JWT claim names that should be treated as space-delimited strings.
spec.jwtRules[].timeout <string>	# The maximum amount of time that the resolver, determined by the PILOT_JWT_ENABLE_REMOTE_JWKS environment variable, will spend waiting for the JWKS to be fetched.
spec.selector <object>	# Optional.
spec.selector.matchLabels <map[string]string>	# One or more labels that indicate a specific set of pods/VMs on which a policy should be applied.
spec.targetRef <object>
spec.targetRef.group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRef.kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRef.name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRef.namespace <string>	# namespace is the namespace of the referent.
spec.targetRefs <[]object> max=16	# Optional.
spec.targetRefs[].group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRefs[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRefs[].name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRefs[].namespace <string>	# namespace is the namespace of the referent.
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
