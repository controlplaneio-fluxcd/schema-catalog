# schema source: MetalLB v0.16.0 https://github.com/metallb/metallb
apiVersion <string> enum=metallb.io/v1beta1
kind <string> enum=BFDProfile
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# BFDProfileSpec defines the desired state of BFDProfile.
spec.detectMultiplier <integer> format=int32 min=2 max=255	# Configures the detection multiplier to determine packet loss. The remote transmission interval will be multiplied by this value to determine the connection loss detection timer.
spec.echoInterval <integer> format=int32 min=10 max=60000	# Configures the minimal echo receive transmission interval that this system is capable of handling in milliseconds. Defaults to 50ms
spec.echoMode <boolean>	# Enables or disables the echo transmission mode. This mode is disabled by default, and not supported on multi hops setups.
spec.minimumTtl <integer> format=int32 min=1 max=254	# For multi hop sessions only: configure the minimum expected TTL for an incoming BFD control packet.
spec.passiveMode <boolean>	# Mark session as passive: a passive session will not attempt to start the connection and will wait for control packets from peer before it begins replying.
spec.receiveInterval <integer> format=int32 min=10 max=60000	# The minimum interval that this system is capable of receiving control packets in milliseconds. Defaults to 300ms.
spec.transmitInterval <integer> format=int32 min=10 max=60000	# The minimum transmission interval (less jitter) that this system wants to use to send BFD control packets in milliseconds. Defaults to 300ms
status <object (free-form)>	# BFDProfileStatus defines the observed state of BFDProfile.
