# schema source: Flagger v1.43.0
apiVersion <string> enum=flagger.app/v1beta1
kind <string> enum=MetricTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# MetricTemplateSpec defines the desired state of a MetricTemplate.
spec.provider <object> (required)	# Provider of this metric template
spec.provider.address <string>	# API address of this provider
spec.provider.headers <map[string]array>	# Headers to add to HTTP(S) requests
spec.provider.insecureSkipVerify <boolean>	# Disable SSL certificate validation for the provider address
spec.provider.region <string>	# Region of the provider
spec.provider.secretRef <object>	# Kubernetes secret reference containing the provider credentials
spec.provider.secretRef.name <string> (required)	# Name of the Kubernetes secret
spec.provider.type <string> (required) enum=prometheus|influxdb|datadog|externalmetrics|stackdriver|cloudwatch|newrelic|graphite|dynatrace|keptn|splunk	# Type of this provider
spec.query <string> (required)	# Query of this metric template
