# schema source: Istio 1.30.3 https://github.com/istio/istio
apiVersion <string> enum=networking.istio.io/v1alpha3
kind <string> enum=Gateway
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Configuration affecting edge load balancer. See more details at: https://istio.io/docs/reference/config/networking/gateway.html
spec.selector <map[string]string>	# One or more labels that indicate a specific set of pods/VMs on which this gateway configuration should be applied.
spec.servers <[]object>	# A list of server specifications.
spec.servers[].bind <string>	# The ip or the Unix domain socket to which the listener should be bound to.
spec.servers[].defaultEndpoint <string>
spec.servers[].hosts <[]string> (required)	# One or more hosts exposed by this gateway.
spec.servers[].name <string>	# An optional name of the server, when set must be unique across all servers.
spec.servers[].port <object> (required)	# The Port on which the proxy should listen for incoming connections.
spec.servers[].port.name <string> (required)	# Label assigned to the port.
spec.servers[].port.number <integer> (required) min=0 max=4294967295	# A valid non-negative integer port number.
spec.servers[].port.protocol <string> (required)	# The protocol exposed on the port.
spec.servers[].port.targetPort <integer> min=0 max=4294967295
spec.servers[].tls <object>	# Set of TLS related options that govern the server's behavior.
spec.servers[].tls.caCertCredentialName <string>	# For mutual TLS, the name of the secret or the configmap that holds CA certificates.
spec.servers[].tls.caCertificates <string>	# REQUIRED if mode is `MUTUAL` or `OPTIONAL_MUTUAL`.
spec.servers[].tls.caCrl <string>	# OPTIONAL: The path to the file containing the certificate revocation list (CRL) to use in verifying a presented client side certificate.
spec.servers[].tls.cipherSuites <[]string>	# Optional: If specified, only support the specified cipher list.
spec.servers[].tls.credentialName <string>	# For gateways running on Kubernetes, the name of the secret that holds the TLS certs including the CA certificates.
spec.servers[].tls.credentialNames <[]string> min=1 max=2	# Same as CredentialName but for multiple certificates.
spec.servers[].tls.httpsRedirect <boolean>	# If set to true, the load balancer will send a 301 redirect for all http connections, asking the clients to use HTTPS.
spec.servers[].tls.maxProtocolVersion <string> enum=TLS_AUTO|TLSV1_0|TLSV1_1|TLSV1_2|TLSV1_3	# Optional: Maximum TLS protocol version. Valid Options: TLS_AUTO, TLSV1_0, TLSV1_1, TLSV1_2, TLSV1_3
spec.servers[].tls.minProtocolVersion <string> enum=TLS_AUTO|TLSV1_0|TLSV1_1|TLSV1_2|TLSV1_3	# Optional: Minimum TLS protocol version. Valid Options: TLS_AUTO, TLSV1_0, TLSV1_1, TLSV1_2, TLSV1_3
spec.servers[].tls.mode <string> enum=PASSTHROUGH|SIMPLE|MUTUAL|AUTO_PASSTHROUGH|ISTIO_MUTUAL|OPTIONAL_MUTUAL	# Optional: Indicates whether connections to this port should be secured using TLS. Valid Options: PASSTHROUGH, SIMPLE, MUTUAL, AUTO_PASSTHROUGH, ISTIO_MUTUAL, OPTIONAL_MUTUAL
spec.servers[].tls.privateKey <string>	# REQUIRED if mode is `SIMPLE` or `MUTUAL`.
spec.servers[].tls.serverCertificate <string>	# REQUIRED if mode is `SIMPLE` or `MUTUAL`.
spec.servers[].tls.subjectAltNames <[]string>	# A list of alternate names to verify the subject identity in the certificate presented by the client.
spec.servers[].tls.tlsCertificates <[]object> min=1 max=2	# Only one of `server_certificate`, `private_key` or `credential_name` or `credential_names` or `tls_certificates` should be specified.
spec.servers[].tls.tlsCertificates[].caCertificates <string>
spec.servers[].tls.tlsCertificates[].privateKey <string>	# REQUIRED if mode is `SIMPLE` or `MUTUAL`.
spec.servers[].tls.tlsCertificates[].serverCertificate <string>	# REQUIRED if mode is `SIMPLE` or `MUTUAL`.
spec.servers[].tls.verifyCertificateHash <[]string>	# An optional list of hex-encoded SHA-256 hashes of the authorized client certificates.
spec.servers[].tls.verifyCertificateSpki <[]string>	# An optional list of base64-encoded SHA-256 hashes of the SPKIs of authorized client certificates.
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
