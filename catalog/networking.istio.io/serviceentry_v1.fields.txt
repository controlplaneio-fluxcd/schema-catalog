# schema source: Istio 1.30.3 https://github.com/istio/istio
apiVersion <string> enum=networking.istio.io/v1
kind <string> enum=ServiceEntry
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Configuration affecting service registry. See more details at: https://istio.io/docs/reference/config/networking/service-entry.html
spec.addresses <[]string> max=256	# The virtual IP addresses associated with the service.
spec.endpoints <[]object> max=4096	# One or more endpoints associated with the service.
spec.endpoints[].address <string> max=256	# Address associated with the network endpoint without the port.
spec.endpoints[].labels <map[string]string>	# One or more labels associated with the endpoint.
spec.endpoints[].locality <string> max=2048	# The locality associated with the endpoint.
spec.endpoints[].network <string> max=2048	# Network enables Istio to group endpoints resident in the same L3 domain/network.
spec.endpoints[].ports <map[string]integer>	# Set of ports associated with the endpoint.
spec.endpoints[].serviceAccount <string> max=253	# The service account associated with the workload if a sidecar is present in the workload.
spec.endpoints[].weight <integer> min=0 max=4294967295	# The load balancing weight associated with the endpoint.
spec.exportTo <[]string>	# A list of namespaces to which this service is exported.
spec.hosts <[]string> (required) min=1 max=256	# The hosts associated with the ServiceEntry.
spec.location <string> enum=MESH_EXTERNAL|MESH_INTERNAL	# Specify whether the service should be considered external to the mesh or part of the mesh. Valid Options: MESH_EXTERNAL, MESH_INTERNAL
spec.ports <[]object> max=256	# The ports associated with the external service.
spec.ports[].name <string> (required) max=256	# Label assigned to the port.
spec.ports[].number <integer> (required) min=0 max=4294967295	# A valid non-negative integer port number.
spec.ports[].protocol <string> max=256	# The protocol exposed on the port.
spec.ports[].targetPort <integer> min=0 max=4294967295	# The port number on the endpoint where the traffic will be received.
spec.resolution <string> enum=NONE|STATIC|DNS|DNS_ROUND_ROBIN|DYNAMIC_DNS	# Service resolution mode for the hosts. Valid Options: NONE, STATIC, DNS, DNS_ROUND_ROBIN, DYNAMIC_DNS
spec.subjectAltNames <[]string>	# If specified, the proxy will verify that the server certificate's subject alternate name matches one of the specified values.
spec.workloadSelector <object>	# Applicable only for MESH_INTERNAL services.
spec.workloadSelector.labels <map[string]string>	# One or more labels that indicate a specific set of pods/VMs on which the configuration should be applied.
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
