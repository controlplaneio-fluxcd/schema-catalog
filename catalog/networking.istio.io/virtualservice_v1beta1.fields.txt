# schema source: Istio 1.30.3 https://github.com/istio/istio
apiVersion <string> enum=networking.istio.io/v1beta1
kind <string> enum=VirtualService
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Configuration affecting label/content routing, sni routing, etc. See more details at: https://istio.io/docs/reference/config/networking/virtual-service.html
spec.exportTo <[]string>	# A list of namespaces to which this virtual service is exported.
spec.gateways <[]string>	# The names of gateways and sidecars that should apply these routes.
spec.hosts <[]string>	# The destination hosts to which traffic is being sent.
spec.http <[]object>	# An ordered list of route rules for HTTP traffic.
spec.http[].corsPolicy <object>	# Cross-Origin Resource Sharing policy (CORS).
spec.http[].corsPolicy.allowCredentials <boolean>	# Indicates whether the caller is allowed to send the actual request (not the preflight) using credentials.
spec.http[].corsPolicy.allowHeaders <[]string>	# List of HTTP headers that can be used when requesting the resource.
spec.http[].corsPolicy.allowMethods <[]string>	# List of HTTP methods allowed to access the resource.
spec.http[].corsPolicy.allowOrigin <[]string>
spec.http[].corsPolicy.allowOrigins <[]object>	# String patterns that match allowed origins.
spec.http[].corsPolicy.allowOrigins[].exact <string>
spec.http[].corsPolicy.allowOrigins[].prefix <string>
spec.http[].corsPolicy.allowOrigins[].regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].corsPolicy.exposeHeaders <[]string>	# A list of HTTP headers that the browsers are allowed to access.
spec.http[].corsPolicy.maxAge <string>	# Specifies how long the results of a preflight request can be cached.
spec.http[].corsPolicy.unmatchedPreflights <string> enum=UNSPECIFIED|FORWARD|IGNORE	# Indicates whether preflight requests not matching the configured allowed origin shouldn't be forwarded to the upstream. Valid Options: FORWARD, IGNORE
spec.http[].delegate <object>	# Delegate is used to specify the particular VirtualService which can be used to define delegate HTTPRoute.
spec.http[].delegate.name <string>	# Name specifies the name of the delegate VirtualService.
spec.http[].delegate.namespace <string>	# Namespace specifies the namespace where the delegate VirtualService resides.
spec.http[].directResponse <object>	# A HTTP rule can either return a direct_response, redirect or forward (default) traffic.
spec.http[].directResponse.body <object>	# Specifies the content of the response body.
spec.http[].directResponse.body.bytes <string> format=byte	# response body as base64 encoded bytes.
spec.http[].directResponse.body.string <string>
spec.http[].directResponse.status <integer> (required) min=0 max=4294967295	# Specifies the HTTP response status to be returned.
spec.http[].fault <object>	# Fault injection policy to apply on HTTP traffic at the client side.
spec.http[].fault.abort <object>	# Abort Http request attempts and return error codes back to downstream service, giving the impression that the upstream service is faulty.
spec.http[].fault.abort.grpcStatus <string>	# GRPC status code to use to abort the request.
spec.http[].fault.abort.http2Error <string>
spec.http[].fault.abort.httpStatus <integer> format=int32	# HTTP status code to use to abort the Http request.
spec.http[].fault.abort.percentage <object>	# Percentage of requests to be aborted with the error code provided.
spec.http[].fault.abort.percentage.value <number> format=double
spec.http[].fault.delay <object>	# Delay requests before forwarding, emulating various failures such as network issues, overloaded upstream service, etc.
spec.http[].fault.delay.exponentialDelay <string>
spec.http[].fault.delay.fixedDelay <string>	# Add a fixed delay before forwarding the request.
spec.http[].fault.delay.percent <integer> format=int32	# Percentage of requests on which the delay will be injected (0-100).
spec.http[].fault.delay.percentage <object>	# Percentage of requests on which the delay will be injected.
spec.http[].fault.delay.percentage.value <number> format=double
spec.http[].headers <object>
spec.http[].headers.request <object>
spec.http[].headers.request.add <map[string]string>
spec.http[].headers.request.remove <[]string>
spec.http[].headers.request.set <map[string]string>
spec.http[].headers.response <object>
spec.http[].headers.response.add <map[string]string>
spec.http[].headers.response.remove <[]string>
spec.http[].headers.response.set <map[string]string>
spec.http[].match <[]object>	# Match conditions to be satisfied for the rule to be activated.
spec.http[].match[].authority <object>	# HTTP Authority values are case-sensitive and formatted as follows: - `exact: "value"` for exact string match - `prefix: "value"` for prefix-based match - `regex: "value"` for [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].authority.exact <string>
spec.http[].match[].authority.prefix <string>
spec.http[].match[].authority.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].gateways <[]string>	# Names of gateways where the rule should be applied.
spec.http[].match[].headers <map[string]object>	# The header keys must be lowercase and use hyphen as the separator, e.g.
spec.http[].match[].headers.<key>.exact <string>
spec.http[].match[].headers.<key>.prefix <string>
spec.http[].match[].headers.<key>.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].ignoreUriCase <boolean>	# Flag to specify whether the URI matching should be case-insensitive.
spec.http[].match[].method <object>	# HTTP Method values are case-sensitive and formatted as follows: - `exact: "value"` for exact string match - `prefix: "value"` for prefix-based match - `regex: "value"` for [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].method.exact <string>
spec.http[].match[].method.prefix <string>
spec.http[].match[].method.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].name <string>	# The name assigned to a match.
spec.http[].match[].port <integer> min=0 max=4294967295	# Specifies the ports on the host that is being addressed.
spec.http[].match[].queryParams <map[string]object>	# Query parameters for matching.
spec.http[].match[].queryParams.<key>.exact <string>
spec.http[].match[].queryParams.<key>.prefix <string>
spec.http[].match[].queryParams.<key>.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].scheme <object>	# URI Scheme values are case-sensitive and formatted as follows: - `exact: "value"` for exact string match - `prefix: "value"` for prefix-based match - `regex: "value"` for [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].scheme.exact <string>
spec.http[].match[].scheme.prefix <string>
spec.http[].match[].scheme.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].sourceLabels <map[string]string>	# One or more labels that constrain the applicability of a rule to source (client) workloads with the given labels.
spec.http[].match[].sourceNamespace <string>	# Source namespace constraining the applicability of a rule to workloads in that namespace.
spec.http[].match[].statPrefix <string>	# The human readable prefix to use when emitting statistics for this route.
spec.http[].match[].uri <object>	# URI to match values are case-sensitive and formatted as follows: - `exact: "value"` for exact string match - `prefix: "value"` for prefix-based match - `regex: "value"` for [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].uri.exact <string>
spec.http[].match[].uri.prefix <string>
spec.http[].match[].uri.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].match[].withoutHeaders <map[string]object>	# withoutHeader has the same syntax with the header, but has opposite meaning.
spec.http[].match[].withoutHeaders.<key>.exact <string>
spec.http[].match[].withoutHeaders.<key>.prefix <string>
spec.http[].match[].withoutHeaders.<key>.regex <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].mirror <object>	# Mirror HTTP traffic to a another destination in addition to forwarding the requests to the intended destination.
spec.http[].mirror.host <string> (required)	# The name of a service from the service registry.
spec.http[].mirror.port <object>	# Specifies the port on the host that is being addressed.
spec.http[].mirror.port.number <integer> min=0 max=4294967295
spec.http[].mirror.subset <string>	# The name of a subset within the service.
spec.http[].mirrorPercent <integer> min=0 max=4294967295
spec.http[].mirrorPercentage <object>	# Percentage of the traffic to be mirrored by the `mirror` field.
spec.http[].mirrorPercentage.value <number> format=double
spec.http[].mirror_percent <integer> min=0 max=4294967295
spec.http[].mirrors <[]object>	# Specifies the destinations to mirror HTTP traffic in addition to the original destination.
spec.http[].mirrors[].destination <object> (required)	# Destination specifies the target of the mirror operation.
spec.http[].mirrors[].destination.host <string> (required)	# The name of a service from the service registry.
spec.http[].mirrors[].destination.port <object>	# Specifies the port on the host that is being addressed.
spec.http[].mirrors[].destination.port.number <integer> min=0 max=4294967295
spec.http[].mirrors[].destination.subset <string>	# The name of a subset within the service.
spec.http[].mirrors[].percentage <object>	# Percentage of the traffic to be mirrored by the `destination` field.
spec.http[].mirrors[].percentage.value <number> format=double
spec.http[].name <string>	# The name assigned to the route for debugging purposes.
spec.http[].redirect <object>	# A HTTP rule can either return a direct_response, redirect or forward (default) traffic.
spec.http[].redirect.authority <string>	# On a redirect, overwrite the Authority/Host portion of the URL with this value.
spec.http[].redirect.derivePort <string> enum=FROM_PROTOCOL_DEFAULT|FROM_REQUEST_PORT	# On a redirect, dynamically set the port: * FROM_PROTOCOL_DEFAULT: automatically set to 80 for HTTP and 443 for HTTPS. Valid Options: FROM_PROTOCOL_DEFAULT, FROM_REQUEST_PORT
spec.http[].redirect.port <integer> min=0 max=4294967295	# On a redirect, overwrite the port portion of the URL with this value.
spec.http[].redirect.redirectCode <integer> min=0 max=4294967295	# On a redirect, Specifies the HTTP status code to use in the redirect response.
spec.http[].redirect.scheme <string>	# On a redirect, overwrite the scheme portion of the URL with this value.
spec.http[].redirect.uri <string>	# On a redirect, overwrite the Path portion of the URL with this value.
spec.http[].retries <object>	# Retry policy for HTTP requests.
spec.http[].retries.attempts <integer> format=int32	# Number of retries to be allowed for a given request.
spec.http[].retries.backoff <string>	# Specifies the minimum duration between retry attempts.
spec.http[].retries.perTryTimeout <string>	# Timeout per attempt for a given request, including the initial call and any retries.
spec.http[].retries.retryIgnorePreviousHosts <boolean>	# Flag to specify whether the retries should ignore previously tried hosts during retry.
spec.http[].retries.retryOn <string>	# Specifies the conditions under which retry takes place.
spec.http[].retries.retryRemoteLocalities <boolean>	# Flag to specify whether the retries should retry to other localities.
spec.http[].rewrite <object>	# Rewrite HTTP URIs and Authority headers.
spec.http[].rewrite.authority <string>	# rewrite the Authority/Host header with this value.
spec.http[].rewrite.uri <string>	# rewrite the path (or the prefix) portion of the URI with this value.
spec.http[].rewrite.uriRegexRewrite <object>	# rewrite the path portion of the URI with the specified regex.
spec.http[].rewrite.uriRegexRewrite.match <string>	# [RE2 style regex-based match](https://github.com/google/re2/wiki/Syntax).
spec.http[].rewrite.uriRegexRewrite.rewrite <string>	# The string that should replace into matching portions of original URI.
spec.http[].route <[]object>	# A HTTP rule can either return a direct_response, redirect or forward (default) traffic.
spec.http[].route[].destination <object> (required)	# Destination uniquely identifies the instances of a service to which the request/connection should be forwarded to.
spec.http[].route[].destination.host <string> (required)	# The name of a service from the service registry.
spec.http[].route[].destination.port <object>	# Specifies the port on the host that is being addressed.
spec.http[].route[].destination.port.number <integer> min=0 max=4294967295
spec.http[].route[].destination.subset <string>	# The name of a subset within the service.
spec.http[].route[].headers <object>
spec.http[].route[].headers.request <object>
spec.http[].route[].headers.request.add <map[string]string>
spec.http[].route[].headers.request.remove <[]string>
spec.http[].route[].headers.request.set <map[string]string>
spec.http[].route[].headers.response <object>
spec.http[].route[].headers.response.add <map[string]string>
spec.http[].route[].headers.response.remove <[]string>
spec.http[].route[].headers.response.set <map[string]string>
spec.http[].route[].weight <integer> format=int32	# Weight specifies the relative proportion of traffic to be forwarded to the destination.
spec.http[].timeout <string>	# Timeout for HTTP requests, default is disabled.
spec.tcp <[]object>	# An ordered list of route rules for opaque TCP traffic.
spec.tcp[].match <[]object>	# Match conditions to be satisfied for the rule to be activated.
spec.tcp[].match[].destinationSubnets <[]string>	# IPv4 or IPv6 ip addresses of destination with optional subnet.
spec.tcp[].match[].gateways <[]string>	# Names of gateways where the rule should be applied.
spec.tcp[].match[].port <integer> min=0 max=4294967295	# Specifies the port on the host that is being addressed.
spec.tcp[].match[].sourceLabels <map[string]string>	# One or more labels that constrain the applicability of a rule to workloads with the given labels.
spec.tcp[].match[].sourceNamespace <string>	# Source namespace constraining the applicability of a rule to workloads in that namespace.
spec.tcp[].match[].sourceSubnet <string>
spec.tcp[].route <[]object>	# The destination to which the connection should be forwarded to.
spec.tcp[].route[].destination <object> (required)	# Destination uniquely identifies the instances of a service to which the request/connection should be forwarded to.
spec.tcp[].route[].destination.host <string> (required)	# The name of a service from the service registry.
spec.tcp[].route[].destination.port <object>	# Specifies the port on the host that is being addressed.
spec.tcp[].route[].destination.port.number <integer> min=0 max=4294967295
spec.tcp[].route[].destination.subset <string>	# The name of a subset within the service.
spec.tcp[].route[].weight <integer> format=int32	# Weight specifies the relative proportion of traffic to be forwarded to the destination.
spec.tls <[]object>	# An ordered list of route rule for non-terminated TLS & HTTPS traffic.
spec.tls[].match <[]object> (required)	# Match conditions to be satisfied for the rule to be activated.
spec.tls[].match[].destinationSubnets <[]string>	# IPv4 or IPv6 ip addresses of destination with optional subnet.
spec.tls[].match[].gateways <[]string>	# Names of gateways where the rule should be applied.
spec.tls[].match[].port <integer> min=0 max=4294967295	# Specifies the port on the host that is being addressed.
spec.tls[].match[].sniHosts <[]string> (required)	# SNI (server name indicator) to match on.
spec.tls[].match[].sourceLabels <map[string]string>	# One or more labels that constrain the applicability of a rule to workloads with the given labels.
spec.tls[].match[].sourceNamespace <string>	# Source namespace constraining the applicability of a rule to workloads in that namespace.
spec.tls[].route <[]object>	# The destination to which the connection should be forwarded to.
spec.tls[].route[].destination <object> (required)	# Destination uniquely identifies the instances of a service to which the request/connection should be forwarded to.
spec.tls[].route[].destination.host <string> (required)	# The name of a service from the service registry.
spec.tls[].route[].destination.port <object>	# Specifies the port on the host that is being addressed.
spec.tls[].route[].destination.port.number <integer> min=0 max=4294967295
spec.tls[].route[].destination.subset <string>	# The name of a subset within the service.
spec.tls[].route[].weight <integer> format=int32	# Weight specifies the relative proportion of traffic to be forwarded to the destination.
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
