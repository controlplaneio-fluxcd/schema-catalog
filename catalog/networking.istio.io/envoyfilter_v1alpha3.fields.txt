# schema source: Istio 1.30.3 https://github.com/istio/istio
apiVersion <string> enum=networking.istio.io/v1alpha3
kind <string> enum=EnvoyFilter
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Customizing Envoy configuration generated by Istio. See more details at: https://istio.io/docs/reference/config/networking/envoy-filter.html
spec.configPatches <[]object>	# One or more patches with match conditions.
spec.configPatches[].applyTo <string> enum=INVALID|LISTENER|FILTER_CHAIN|NETWORK_FILTER|HTTP_FILTER|ROUTE_CONFIGURATION|VIRTUAL_HOST|HTTP_ROUTE|CLUSTER|EXTENSION_CONFIG|BOOTSTRAP|LISTENER_FILTER	# Specifies where in the Envoy configuration, the patch should be applied. Valid Options: LISTENER, FILTER_CHAIN, NETWORK_FILTER, HTTP_FILTER, ROUTE_CONFIGURATION, VIRTUAL_HOST, HTTP_ROUTE, CLUSTER, EXTENSION_CONFIG, BOOTSTRAP, LISTENER_FILTER
spec.configPatches[].match <object>	# Match on listener/route configuration/cluster.
spec.configPatches[].match.cluster <object>	# Match on envoy cluster attributes.
spec.configPatches[].match.cluster.name <string>	# The exact name of the cluster to match.
spec.configPatches[].match.cluster.portNumber <integer> min=0 max=4294967295	# The service port for which this cluster was generated.
spec.configPatches[].match.cluster.service <string>	# The fully qualified service name for this cluster.
spec.configPatches[].match.cluster.subset <string>	# The subset associated with the service.
spec.configPatches[].match.context <string> enum=ANY|SIDECAR_INBOUND|SIDECAR_OUTBOUND|GATEWAY|WAYPOINT	# The specific config generation context to match on. Valid Options: ANY, SIDECAR_INBOUND, SIDECAR_OUTBOUND, GATEWAY, WAYPOINT
spec.configPatches[].match.listener <object>	# Match on envoy listener attributes.
spec.configPatches[].match.listener.filterChain <object>	# Match a specific filter chain in a listener.
spec.configPatches[].match.listener.filterChain.applicationProtocols <string>	# Applies only to sidecars.
spec.configPatches[].match.listener.filterChain.destinationPort <integer> min=0 max=4294967295	# The destination_port value used by a filter chain's match condition.
spec.configPatches[].match.listener.filterChain.filter <object>	# The name of a specific filter to apply the patch to.
spec.configPatches[].match.listener.filterChain.filter.name <string>	# The filter name to match on.
spec.configPatches[].match.listener.filterChain.filter.subFilter <object>	# The next level filter within this filter to match upon.
spec.configPatches[].match.listener.filterChain.filter.subFilter.name <string>	# The filter name to match on.
spec.configPatches[].match.listener.filterChain.name <string>	# The name assigned to the filter chain.
spec.configPatches[].match.listener.filterChain.sni <string>	# The SNI value used by a filter chain's match condition.
spec.configPatches[].match.listener.filterChain.transportProtocol <string>	# Applies only to `SIDECAR_INBOUND` context.
spec.configPatches[].match.listener.listenerFilter <string>	# Match a specific listener filter.
spec.configPatches[].match.listener.name <string>	# Match a specific listener by its name.
spec.configPatches[].match.listener.portName <string>
spec.configPatches[].match.listener.portNumber <integer> min=0 max=4294967295	# The service port/gateway port to which traffic is being sent/received.
spec.configPatches[].match.proxy <object>	# Match on properties associated with a proxy.
spec.configPatches[].match.proxy.metadata <map[string]string>	# Match on the node metadata supplied by a proxy when connecting to istiod.
spec.configPatches[].match.proxy.proxyVersion <string>	# A regular expression in golang regex format (RE2) that can be used to select proxies using a specific version of istio proxy.
spec.configPatches[].match.routeConfiguration <object>	# Match on envoy HTTP route configuration attributes.
spec.configPatches[].match.routeConfiguration.gateway <string>	# The Istio gateway config's namespace/name for which this route configuration was generated.
spec.configPatches[].match.routeConfiguration.name <string>	# Route configuration name to match on.
spec.configPatches[].match.routeConfiguration.portName <string>	# Applicable only for GATEWAY context.
spec.configPatches[].match.routeConfiguration.portNumber <integer> min=0 max=4294967295	# The service port number or gateway server port number for which this route configuration was generated.
spec.configPatches[].match.routeConfiguration.vhost <object>	# Match a specific virtual host in a route configuration and apply the patch to the virtual host.
spec.configPatches[].match.routeConfiguration.vhost.domainName <string>	# Match a domain name in a virtual host.
spec.configPatches[].match.routeConfiguration.vhost.name <string>	# The VirtualHosts objects generated by Istio are named as host:port, where the host typically corresponds to the VirtualService's host field or the hostname of a service in the registry.
spec.configPatches[].match.routeConfiguration.vhost.route <object>	# Match a specific route within the virtual host.
spec.configPatches[].match.routeConfiguration.vhost.route.action <string> enum=ANY|ROUTE|REDIRECT|DIRECT_RESPONSE	# Match a route with specific action type. Valid Options: ANY, ROUTE, REDIRECT, DIRECT_RESPONSE
spec.configPatches[].match.routeConfiguration.vhost.route.name <string>	# The Route objects generated by default are named as default.
spec.configPatches[].match.waypoint <object>
spec.configPatches[].match.waypoint.filter <object>	# The name of a specific filter to apply the patch to.
spec.configPatches[].match.waypoint.filter.name <string>	# The filter name to match on.
spec.configPatches[].match.waypoint.filter.subFilter <object>	# The next level filter within this filter to match on.
spec.configPatches[].match.waypoint.filter.subFilter.name <string>	# The filter name to match on.
spec.configPatches[].match.waypoint.portNumber <integer> min=0 max=4294967295	# The service port to match on.
spec.configPatches[].match.waypoint.route <object>	# Match a specific route.
spec.configPatches[].match.waypoint.route.name <string>	# The Route objects generated by default are named as default.
spec.configPatches[].patch <object>	# The patch to apply along with the operation.
spec.configPatches[].patch.filterClass <string> enum=UNSPECIFIED|AUTHN|AUTHZ|STATS	# Determines the filter insertion order. Valid Options: AUTHN, AUTHZ, STATS
spec.configPatches[].patch.operation <string> enum=INVALID|MERGE|ADD|REMOVE|INSERT_BEFORE|INSERT_AFTER|INSERT_FIRST|REPLACE	# Determines how the patch should be applied. Valid Options: MERGE, ADD, REMOVE, INSERT_BEFORE, INSERT_AFTER, INSERT_FIRST, REPLACE
spec.configPatches[].patch.value <object (free-form)>	# The JSON config of the object being patched.
spec.priority <integer> format=int32	# Priority defines the order in which patch sets are applied within a context.
spec.targetRefs <[]object> max=16	# Optional.
spec.targetRefs[].group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRefs[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRefs[].name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRefs[].namespace <string>	# namespace is the namespace of the referent.
spec.workloadSelector <object>	# Criteria used to select the specific set of pods/VMs on which this patch configuration should be applied.
spec.workloadSelector.labels <map[string]string>	# One or more labels that indicate a specific set of pods/VMs on which the configuration should be applied.
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
