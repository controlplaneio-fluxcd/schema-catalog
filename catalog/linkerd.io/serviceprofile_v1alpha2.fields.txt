# schema source: Linkerd 26.6.3 https://github.com/linkerd/linkerd2
apiVersion <string> enum=linkerd.io/v1alpha2
kind <string> enum=ServiceProfile
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec is the custom resource spec
spec.dstOverrides <[]object>
spec.dstOverrides[].authority <string>
spec.dstOverrides[].weight <int-or-string>
spec.opaquePorts <[]string>
spec.retryBudget <object>	# RetryBudget describes the maximum number of retries that should be issued to this service.
spec.retryBudget.minRetriesPerSecond <integer> (required) format=int32
spec.retryBudget.retryRatio <number> (required) format=float
spec.retryBudget.ttl <string> (required)
spec.routes <[]object>
spec.routes[].condition <object> (required)	# RequestMatch describes the conditions under which to match a Route.
spec.routes[].condition.all <[]object>
spec.routes[].condition.any <[]object>
spec.routes[].condition.method <string>
spec.routes[].condition.not <[]object>
spec.routes[].condition.pathRegex <string>
spec.routes[].isRetryable <boolean>
spec.routes[].name <string> (required)
spec.routes[].responseClasses <[]object>
spec.routes[].responseClasses[].condition <object> (required)	# ResponseMatch describes the conditions under which to classify a response.
spec.routes[].responseClasses[].condition.all <[]object>
spec.routes[].responseClasses[].condition.any <[]object>
spec.routes[].responseClasses[].condition.not <object (free-form)>
spec.routes[].responseClasses[].condition.status <object>	# Range describes a range of integers (e.g. status codes).
spec.routes[].responseClasses[].condition.status.max <integer> format=int32
spec.routes[].responseClasses[].condition.status.min <integer> format=int32
spec.routes[].responseClasses[].isFailure <boolean>
spec.routes[].timeout <string>
