# schema source: Istio 1.30.2 https://github.com/istio/istio
apiVersion <string> enum=telemetry.istio.io/v1
kind <string> enum=Telemetry
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Telemetry configuration for workloads. See more details at: https://istio.io/docs/reference/config/telemetry.html
spec.accessLogging <[]object>	# Optional.
spec.accessLogging[].disabled <boolean>	# Controls logging.
spec.accessLogging[].filter <object>	# Optional.
spec.accessLogging[].filter.expression <string>	# CEL expression for selecting when requests/connections should be logged.
spec.accessLogging[].match <object>	# Allows tailoring of logging behavior to specific conditions.
spec.accessLogging[].match.mode <string> enum=CLIENT_AND_SERVER|CLIENT|SERVER	# This determines whether or not to apply the access logging configuration based on the direction of traffic relative to the proxied workload. Valid Options: CLIENT_AND_SERVER, CLIENT, SERVER
spec.accessLogging[].providers <[]object>	# Optional.
spec.accessLogging[].providers[].name <string> (required) min=1	# Required.
spec.metrics <[]object>	# Optional.
spec.metrics[].overrides <[]object>	# Optional.
spec.metrics[].overrides[].disabled <boolean>	# Optional.
spec.metrics[].overrides[].match <object>	# Match allows providing the scope of the override.
spec.metrics[].overrides[].match.customMetric <string> min=1	# Allows free-form specification of a metric.
spec.metrics[].overrides[].match.metric <string> enum=ALL_METRICS|REQUEST_COUNT|REQUEST_DURATION|REQUEST_SIZE|RESPONSE_SIZE|TCP_OPENED_CONNECTIONS|TCP_CLOSED_CONNECTIONS|TCP_SENT_BYTES|TCP_RECEIVED_BYTES|GRPC_REQUEST_MESSAGES|GRPC_RESPONSE_MESSAGES	# One of the well-known [Istio Standard Metrics](https://istio.io/latest/docs/reference/config/metrics/). Valid Options: ALL_METRICS, REQUEST_COUNT, REQUEST_DURATION, REQUEST_SIZE, RESPONSE_SIZE, TCP_OPENED_CONNECTIONS, TCP_CLOSED_CONNECTIONS, TCP_SENT_BYTES, TCP_RECEIVED_BYTES, GRPC_REQUEST_MESSAGES, GRPC_RESPONSE_MESSAGES
spec.metrics[].overrides[].match.mode <string> enum=CLIENT_AND_SERVER|CLIENT|SERVER	# Controls which mode of metrics generation is selected: `CLIENT`, `SERVER`, or `CLIENT_AND_SERVER`. Valid Options: CLIENT_AND_SERVER, CLIENT, SERVER
spec.metrics[].overrides[].tagOverrides <map[string]object>	# Optional.
spec.metrics[].overrides[].tagOverrides.<key>.operation <string> enum=UPSERT|REMOVE	# Operation controls whether or not to update/add a tag, or to remove it. Valid Options: UPSERT, REMOVE
spec.metrics[].overrides[].tagOverrides.<key>.value <string>	# Value is only considered if the operation is `UPSERT`.
spec.metrics[].providers <[]object>	# Optional.
spec.metrics[].providers[].name <string> (required) min=1	# Required.
spec.metrics[].reportingInterval <string>	# Optional.
spec.selector <object>	# Optional.
spec.selector.matchLabels <map[string]string>	# One or more labels that indicate a specific set of pods/VMs on which a policy should be applied.
spec.targetRef <object>
spec.targetRef.group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRef.kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRef.name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRef.namespace <string>	# namespace is the namespace of the referent.
spec.targetRefs <[]object> max=16	# Optional.
spec.targetRefs[].group <string> pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# group is the group of the target resource.
spec.targetRefs[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind is kind of the target resource.
spec.targetRefs[].name <string> (required) min=1 max=253	# name is the name of the target resource.
spec.targetRefs[].namespace <string>	# namespace is the namespace of the referent.
spec.tracing <[]object>	# Optional.
spec.tracing[].customTags <map[string]object>	# Optional.
spec.tracing[].customTags.<key>.environment <object>	# Environment adds the value of an environment variable to each span.
spec.tracing[].customTags.<key>.environment.defaultValue <string>	# Optional.
spec.tracing[].customTags.<key>.environment.name <string> (required) min=1	# Name of the environment variable from which to extract the tag value.
spec.tracing[].customTags.<key>.formatter <object>	# Formatter adds the value of access logging substitution formatter.
spec.tracing[].customTags.<key>.formatter.value <string> (required) min=1	# The formatter tag value to use, same formatter as HTTP access logging (e.g.
spec.tracing[].customTags.<key>.header <object>	# RequestHeader adds the value of an header from the request to each span.
spec.tracing[].customTags.<key>.header.defaultValue <string>	# Optional.
spec.tracing[].customTags.<key>.header.name <string> (required) min=1	# Name of the header from which to extract the tag value.
spec.tracing[].customTags.<key>.literal <object>	# Literal adds the same, hard-coded value to each span.
spec.tracing[].customTags.<key>.literal.value <string> (required) min=1	# The tag value to use.
spec.tracing[].disableContextPropagation <boolean>	# Controls whether trace context headers (e.g., `traceparent`/`tracestate` for W3C, `X-B3-*` for Zipkin) are propagated in forwarded requests.
spec.tracing[].disableSpanReporting <boolean>	# Controls span reporting.
spec.tracing[].enableIstioTags <boolean>	# Determines whether or not trace spans generated by Envoy will include Istio specific tags.
spec.tracing[].match <object>	# Allows tailoring of behavior to specific conditions.
spec.tracing[].match.mode <string> enum=CLIENT_AND_SERVER|CLIENT|SERVER	# This determines whether or not to apply the tracing configuration based on the direction of traffic relative to the proxied workload. Valid Options: CLIENT_AND_SERVER, CLIENT, SERVER
spec.tracing[].providers <[]object>	# Optional.
spec.tracing[].providers[].name <string> (required) min=1	# Required.
spec.tracing[].randomSamplingPercentage <number> format=double min=0 max=100	# Controls the rate at which traffic will be selected for tracing if no prior sampling decision has been made.
spec.tracing[].useRequestIdForTraceSampling <boolean>
status <object>
status.conditions <[]object>	# Current service state of the resource.
status.conditions[].lastProbeTime <string> format=date-time	# Last time we probed the condition.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].observedGeneration <int-or-string>	# Resource Generation to which the Condition refers.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition.
status.conditions[].type <string>	# Type is the type of the condition.
status.observedGeneration <int-or-string>
status.validationMessages <[]object>	# Includes any errors or warnings detected by Istio's analyzers.
status.validationMessages[].documentationUrl <string>	# A url pointing to the Istio documentation for this specific error type.
status.validationMessages[].level <string> enum=UNKNOWN|ERROR|WARNING|INFO	# Represents how severe a message is. Valid Options: UNKNOWN, ERROR, WARNING, INFO
status.validationMessages[].type <object>
status.validationMessages[].type.code <string>	# A 7 character code matching `^IST[0-9]{4}$` intended to uniquely identify the message type.
status.validationMessages[].type.name <string>	# A human-readable name for the message type.
