# schema source: Rancher Elemental Operator v1.9.2 https://github.com/rancher/elemental-operator
apiVersion <string> enum=elemental.cattle.io/v1beta1
kind <string> enum=ManagedOSImage
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.cloudConfig <any>
spec.clusterRolloutStrategy <object>	# RolloutStrategy controls the rollout of the bundle across clusters.
spec.clusterRolloutStrategy.autoPartitionSize <int-or-string>	# A number or percentage of how to automatically partition clusters if no specific partitioning strategy is configured. default: 25%
spec.clusterRolloutStrategy.maxUnavailable <int-or-string>	# A number or percentage of clusters that can be unavailable during an update of a bundle. This follows the same basic approach as a deployment rollout strategy. Once the number of clusters meets unavailable state update will be paused. Default value is 100% which doesn't take effect on update. default: 100%
spec.clusterRolloutStrategy.maxUnavailablePartitions <int-or-string>	# A number or percentage of cluster partitions that can be unavailable during an update of a bundle. default: 0
spec.clusterRolloutStrategy.partitions <[]object>	# A list of definitions of partitions. If any target clusters do not match the configuration they are added to partitions at the end following the autoPartitionSize.
spec.clusterRolloutStrategy.partitions[].clusterGroup <string>	# A cluster group name to include in this partition
spec.clusterRolloutStrategy.partitions[].clusterGroupSelector <object>	# Selector matching cluster group labels to include in this partition
spec.clusterRolloutStrategy.partitions[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.clusterRolloutStrategy.partitions[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.clusterRolloutStrategy.partitions[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.clusterRolloutStrategy.partitions[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.clusterRolloutStrategy.partitions[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.clusterRolloutStrategy.partitions[].clusterName <string>	# ClusterName is the name of a cluster to include in this partition
spec.clusterRolloutStrategy.partitions[].clusterSelector <object>	# Selector matching cluster labels to include in this partition
spec.clusterRolloutStrategy.partitions[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.clusterRolloutStrategy.partitions[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.clusterRolloutStrategy.partitions[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.clusterRolloutStrategy.partitions[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.clusterRolloutStrategy.partitions[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.clusterRolloutStrategy.partitions[].maxUnavailable <int-or-string>	# A number or percentage of clusters that can be unavailable in this partition before this partition is treated as done. default: 10%
spec.clusterRolloutStrategy.partitions[].name <string>	# A user-friendly name given to the partition used for Display (optional).
spec.clusterTargets <[]object>
spec.clusterTargets[].clusterGroup <string>	# ClusterGroup to match a specific cluster group by name.
spec.clusterTargets[].clusterGroupSelector <object>	# ClusterGroupSelector is a selector to match cluster groups.
spec.clusterTargets[].clusterGroupSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.clusterTargets[].clusterGroupSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.clusterTargets[].clusterGroupSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.clusterTargets[].clusterGroupSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.clusterTargets[].clusterGroupSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.clusterTargets[].clusterName <string>	# ClusterName to match a specific cluster by name that will be selected
spec.clusterTargets[].clusterSelector <object>	# ClusterSelector is a selector to match clusters. The structure is the standard metav1.LabelSelector format. If clusterGroupSelector or clusterGroup is specified, clusterSelector will be used only to further refine the selection after clusterGroupSelector and clusterGroup is evaluated.
spec.clusterTargets[].clusterSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.clusterTargets[].clusterSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.clusterTargets[].clusterSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.clusterTargets[].clusterSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.clusterTargets[].clusterSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.clusterTargets[].correctDrift <object>	# CorrectDrift specifies how drift correction should work.
spec.clusterTargets[].correctDrift.enabled <boolean>	# Enabled correct drift if true.
spec.clusterTargets[].correctDrift.force <boolean>	# Force helm rollback with --force option will be used if true. This will try to recreate all resources in the release.
spec.clusterTargets[].correctDrift.keepFailHistory <boolean>	# KeepFailHistory keeps track of failed rollbacks in the helm history.
spec.clusterTargets[].defaultNamespace <string>	# DefaultNamespace is the namespace to use for resources that do not specify a namespace. This field is not used to enforce or lock down the deployment to a specific namespace.
spec.clusterTargets[].deleteCRDResources <boolean>	# DeleteCRDResources deletes CRDs. Warning! this will also delete all your Custom Resources.
spec.clusterTargets[].deleteNamespace <boolean>	# DeleteNamespace can be used to delete the deployed namespace when removing the bundle
spec.clusterTargets[].diff <object>	# Diff can be used to ignore the modified state of objects which are amended at runtime.
spec.clusterTargets[].diff.comparePatches <[]object>	# ComparePatches match a resource and remove fields, or the resource itself from the check for modifications.
spec.clusterTargets[].diff.comparePatches[].apiVersion <string>	# APIVersion is the apiVersion of the resource to match.
spec.clusterTargets[].diff.comparePatches[].jsonPointers <[]string>	# JSONPointers ignore diffs at a certain JSON path.
spec.clusterTargets[].diff.comparePatches[].kind <string>	# Kind is the kind of the resource to match.
spec.clusterTargets[].diff.comparePatches[].name <string>	# Name is the name of the resource to match.
spec.clusterTargets[].diff.comparePatches[].namespace <string>	# Namespace is the namespace of the resource to match.
spec.clusterTargets[].diff.comparePatches[].operations <[]object>	# Operations remove a JSON path from the resource.
spec.clusterTargets[].diff.comparePatches[].operations[].op <string>	# Op is usually "remove" or "ignore"
spec.clusterTargets[].diff.comparePatches[].operations[].path <string>	# Path is the JSON path to remove. Not needed if Op is "ignore".
spec.clusterTargets[].diff.comparePatches[].operations[].value <string>	# Value is usually empty.
spec.clusterTargets[].doNotDeploy <boolean>	# DoNotDeploy if set to true, will not deploy to this target.
spec.clusterTargets[].downstreamResources <[]object>	# DownstreamResources points to resources to be copied into downstream clusters, from the bundle's namespace.
spec.clusterTargets[].downstreamResources[].kind <string>
spec.clusterTargets[].downstreamResources[].name <string>
spec.clusterTargets[].forceSyncGeneration <integer> format=int64	# ForceSyncGeneration is used to force a redeployment
spec.clusterTargets[].helm <object>	# Helm options for the deployment, like the chart name, repo and values.
spec.clusterTargets[].helm.atomic <boolean>	# Atomic sets the --atomic flag when Helm is performing an upgrade
spec.clusterTargets[].helm.chart <string>	# Chart can refer to any go-getter URL or OCI registry based helm chart URL. The chart will be downloaded.
spec.clusterTargets[].helm.disableDNS <boolean>	# DisableDNS can be used to customize Helm's EnableDNS option, which Fleet sets to `true` by default.
spec.clusterTargets[].helm.disableDependencyUpdate <boolean>	# DisableDependencyUpdate allows skipping chart dependencies update
spec.clusterTargets[].helm.disablePreProcess <boolean>	# DisablePreProcess disables template processing in values
spec.clusterTargets[].helm.force <boolean>	# Force allows to override immutable resources. This could be dangerous.
spec.clusterTargets[].helm.maxHistory <integer>	# MaxHistory limits the maximum number of revisions saved per release by Helm.
spec.clusterTargets[].helm.releaseName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=53	# ReleaseName sets a custom release name to deploy the chart as. If not specified a release name will be generated by combining the invoking GitRepo.name + GitRepo.path.
spec.clusterTargets[].helm.repo <string>	# Repo is the name of the HTTPS helm repo to download the chart from.
spec.clusterTargets[].helm.skipSchemaValidation <boolean>	# SkipSchemaValidation allows skipping schema validation against the chart values
spec.clusterTargets[].helm.takeOwnership <boolean>	# TakeOwnership makes helm skip the check for its own annotations
spec.clusterTargets[].helm.templateValues <map[string]string>	# Template Values passed to Helm. It is possible to specify the keys and values as go template strings. Unlike .values, content of each key will be templated first, before serializing to yaml. This allows to template complex values, like ranges and maps. templateValues keys have precedence over values keys in case of conflict.
spec.clusterTargets[].helm.timeoutSeconds <integer>	# TimeoutSeconds is the time to wait for Helm operations.
spec.clusterTargets[].helm.values <object (free-form)>	# Values passed to Helm. It is possible to specify the keys and values as go template strings.
spec.clusterTargets[].helm.valuesFiles <[]string>	# ValuesFiles is a list of files to load values from.
spec.clusterTargets[].helm.valuesFrom <[]object>	# ValuesFrom loads the values from configmaps and secrets.
spec.clusterTargets[].helm.valuesFrom[].configMapKeyRef <object>	# The reference to a config map with release values.
spec.clusterTargets[].helm.valuesFrom[].configMapKeyRef.key <string>
spec.clusterTargets[].helm.valuesFrom[].configMapKeyRef.name <string>	# Name of a resource in the same namespace as the referent.
spec.clusterTargets[].helm.valuesFrom[].configMapKeyRef.namespace <string>
spec.clusterTargets[].helm.valuesFrom[].secretKeyRef <object>	# The reference to a secret with release values.
spec.clusterTargets[].helm.valuesFrom[].secretKeyRef.key <string>
spec.clusterTargets[].helm.valuesFrom[].secretKeyRef.name <string>	# Name of a resource in the same namespace as the referent.
spec.clusterTargets[].helm.valuesFrom[].secretKeyRef.namespace <string>
spec.clusterTargets[].helm.version <string>	# Version of the chart to download
spec.clusterTargets[].helm.waitForJobs <boolean>	# WaitForJobs if set and timeoutSeconds provided, will wait until all Jobs have been completed before marking the GitRepo as ready. It will wait for as long as timeoutSeconds
spec.clusterTargets[].ignore <object>	# IgnoreOptions can be used to ignore fields when monitoring the bundle.
spec.clusterTargets[].ignore.conditions <[]object>	# Conditions is a list of conditions to be ignored when monitoring the Bundle.
spec.clusterTargets[].keepResources <boolean>	# KeepResources can be used to keep the deployed resources when removing the bundle
spec.clusterTargets[].kustomize <object>	# Kustomize options for the deployment, like the dir containing the kustomization.yaml file.
spec.clusterTargets[].kustomize.dir <string>	# Dir points to a custom folder for kustomize resources. This folder must contain a kustomization.yaml file.
spec.clusterTargets[].name <string>	# Name of target. This value is largely for display and logging. If not specified a default name of the format "target000" will be used
spec.clusterTargets[].namespace <string>	# TargetNamespace if present will assign all resource to this namespace and if any cluster scoped resource exists the deployment will fail.
spec.clusterTargets[].namespaceAnnotations <map[string]string>	# NamespaceAnnotations are annotations that will be appended to the namespace created by Fleet.
spec.clusterTargets[].namespaceLabels <map[string]string>	# NamespaceLabels are labels that will be appended to the namespace created by Fleet.
spec.clusterTargets[].serviceAccount <string>	# ServiceAccount which will be used to perform this deployment.
spec.clusterTargets[].yaml <object>	# YAML options, if using raw YAML these are names that map to overlays/{name} files that will be used to replace or patch a resource.
spec.clusterTargets[].yaml.overlays <[]string>	# Overlays is a list of names that maps to folders in "overlays/". If you wish to customize the file ./subdir/resource.yaml then a file ./overlays/myoverlay/subdir/resource.yaml will replace the base file. A file named ./overlays/myoverlay/subdir/resource_patch.yaml will patch the base file.
spec.concurrency <integer> format=int64
spec.cordon <boolean>
spec.drain <object> (required) default={"deleteLocalData":true,"force":true,"ignoreDaemonSets":true,"skipWaitForDeleteTimeout":60}	# DrainSpec encapsulates `kubectl drain` parameters minus node/pod selectors.
spec.drain.deleteEmptydirData <boolean>
spec.drain.deleteLocalData <boolean>
spec.drain.disableEviction <boolean>
spec.drain.force <boolean>
spec.drain.gracePeriod <integer> format=int32
spec.drain.ignoreDaemonSets <boolean>
spec.drain.podSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.drain.podSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.drain.podSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.drain.podSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.drain.podSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.drain.podSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.drain.skipWaitForDeleteTimeout <integer>
spec.drain.timeout <integer> format=int64	# A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.
spec.managedOSVersionName <string>
spec.nodeSelector <object>	# A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
spec.nodeSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.nodeSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.nodeSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.nodeSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.nodeSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.osImage <string>
spec.prepare <object>	# ContainerSpec is a simplified container template.
spec.prepare.args <[]string>
spec.prepare.command <[]string>
spec.prepare.envFrom <[]object>
spec.prepare.envFrom[].configMapRef <object>	# The ConfigMap to select from
spec.prepare.envFrom[].configMapRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.prepare.envFrom[].configMapRef.optional <boolean>	# Specify whether the ConfigMap must be defined
spec.prepare.envFrom[].prefix <string>	# Optional text to prepend to the name of each environment variable. May consist of any printable ASCII characters except '='.
spec.prepare.envFrom[].secretRef <object>	# The Secret to select from
spec.prepare.envFrom[].secretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.prepare.envFrom[].secretRef.optional <boolean>	# Specify whether the Secret must be defined
spec.prepare.envs <[]object>
spec.prepare.envs[].name <string> (required)	# Name of the environment variable. May consist of any printable ASCII characters except '='.
spec.prepare.envs[].value <string>	# Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
spec.prepare.envs[].valueFrom <object>	# Source for the environment variable's value. Cannot be used if value is not empty.
spec.prepare.envs[].valueFrom.configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.prepare.envs[].valueFrom.configMapKeyRef.key <string> (required)	# The key to select.
spec.prepare.envs[].valueFrom.configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.prepare.envs[].valueFrom.configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.prepare.envs[].valueFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
spec.prepare.envs[].valueFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.prepare.envs[].valueFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.prepare.envs[].valueFrom.fileKeyRef <object>	# FileKeyRef selects a key of the env file. Requires the EnvFiles feature gate to be enabled.
spec.prepare.envs[].valueFrom.fileKeyRef.key <string> (required)	# The key within the env file. An invalid key will prevent the pod from starting. The keys defined within a source may consist of any printable ASCII characters except '='. During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
spec.prepare.envs[].valueFrom.fileKeyRef.optional <boolean> default=false	# Specify whether the file or its key must be defined. If the file or key does not exist, then the env var is not published. If optional is set to true and the specified key does not exist, the environment variable will not be set in the Pod's containers. If optional is set to false and the specified key does not exist, an error will be returned during Pod creation.
spec.prepare.envs[].valueFrom.fileKeyRef.path <string> (required)	# The path within the volume from which to select the file. Must be relative and may not contain the '..' path or start with '..'.
spec.prepare.envs[].valueFrom.fileKeyRef.volumeName <string> (required)	# The name of the volume mount containing the env file.
spec.prepare.envs[].valueFrom.resourceFieldRef <object>	# Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
spec.prepare.envs[].valueFrom.resourceFieldRef.containerName <string>	# Container name: required for volumes, optional for env vars
spec.prepare.envs[].valueFrom.resourceFieldRef.divisor <int-or-string>	# Specifies the output format of the exposed resources, defaults to "1"
spec.prepare.envs[].valueFrom.resourceFieldRef.resource <string> (required)	# Required: resource to select
spec.prepare.envs[].valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.prepare.envs[].valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.prepare.envs[].valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.prepare.envs[].valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.prepare.image <string>
spec.prepare.securityContext <object>	# SecurityContext holds security configuration that will be applied to a container. Some fields are present in both SecurityContext and PodSecurityContext. When both are set, the values in SecurityContext take precedence.
spec.prepare.securityContext.allowPrivilegeEscalation <boolean>	# AllowPrivilegeEscalation controls whether a process can gain more privileges than its parent process. This bool directly controls if the no_new_privs flag will be set on the container process. AllowPrivilegeEscalation is true always when the container is: 1) run as Privileged 2) has CAP_SYS_ADMIN Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.appArmorProfile <object>	# appArmorProfile is the AppArmor options to use by this container. If set, this profile overrides the pod's appArmorProfile. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.appArmorProfile.localhostProfile <string>	# localhostProfile indicates a profile loaded on the node that should be used. The profile must be preconfigured on the node to work. Must match the loaded name of the profile. Must be set if and only if type is "Localhost".
spec.prepare.securityContext.appArmorProfile.type <string> (required)	# type indicates which kind of AppArmor profile will be applied. Valid options are: Localhost - a profile pre-loaded on the node. RuntimeDefault - the container runtime's default profile. Unconfined - no AppArmor enforcement.
spec.prepare.securityContext.capabilities <object>	# The capabilities to add/drop when running containers. Defaults to the default set of capabilities granted by the container runtime. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.capabilities.add <[]string>	# Added capabilities
spec.prepare.securityContext.capabilities.drop <[]string>	# Removed capabilities
spec.prepare.securityContext.privileged <boolean>	# Run container in privileged mode. Processes in privileged containers are essentially equivalent to root on the host. Defaults to false. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.procMount <string>	# procMount denotes the type of proc mount to use for the containers. The default value is Default which uses the container runtime defaults for readonly paths and masked paths. This requires the ProcMountType feature flag to be enabled. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.readOnlyRootFilesystem <boolean>	# Whether this container has a read-only root filesystem. Default is false. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.runAsGroup <integer> format=int64	# The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.runAsNonRoot <boolean>	# Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
spec.prepare.securityContext.runAsUser <integer> format=int64	# The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.seLinuxOptions <object>	# The SELinux context to be applied to the container. If unspecified, the container runtime will allocate a random SELinux context for each container. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.seLinuxOptions.level <string>	# Level is SELinux level label that applies to the container.
spec.prepare.securityContext.seLinuxOptions.role <string>	# Role is a SELinux role label that applies to the container.
spec.prepare.securityContext.seLinuxOptions.type <string>	# Type is a SELinux type label that applies to the container.
spec.prepare.securityContext.seLinuxOptions.user <string>	# User is a SELinux user label that applies to the container.
spec.prepare.securityContext.seccompProfile <object>	# The seccomp options to use by this container. If seccomp options are provided at both the pod & container level, the container options override the pod options. Note that this field cannot be set when spec.os.name is windows.
spec.prepare.securityContext.seccompProfile.localhostProfile <string>	# localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet's configured seccomp profile location. Must be set if type is "Localhost". Must NOT be set for any other type.
spec.prepare.securityContext.seccompProfile.type <string> (required)	# type indicates which kind of seccomp profile will be applied. Valid options are: Localhost - a profile defined in a file on the node should be used. RuntimeDefault - the container runtime default profile should be used. Unconfined - no profile should be applied.
spec.prepare.securityContext.windowsOptions <object>	# The Windows specific settings applied to all containers. If unspecified, the options from the PodSecurityContext will be used. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is linux.
spec.prepare.securityContext.windowsOptions.gmsaCredentialSpec <string>	# GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field.
spec.prepare.securityContext.windowsOptions.gmsaCredentialSpecName <string>	# GMSACredentialSpecName is the name of the GMSA credential spec to use.
spec.prepare.securityContext.windowsOptions.hostProcess <boolean>	# HostProcess determines if a container should be run as a 'Host Process' container. All of a Pod's containers must have the same effective HostProcess value (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers). In addition, if HostProcess is true then HostNetwork must also be set to true.
spec.prepare.securityContext.windowsOptions.runAsUserName <string>	# The UserName in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
spec.prepare.volumes <[]object>
spec.prepare.volumes[].destination <string>
spec.prepare.volumes[].name <string>
spec.prepare.volumes[].source <string>
spec.upgradeContainer <object>	# ContainerSpec is a simplified container template.
spec.upgradeContainer.args <[]string>
spec.upgradeContainer.command <[]string>
spec.upgradeContainer.envFrom <[]object>
spec.upgradeContainer.envFrom[].configMapRef <object>	# The ConfigMap to select from
spec.upgradeContainer.envFrom[].configMapRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.upgradeContainer.envFrom[].configMapRef.optional <boolean>	# Specify whether the ConfigMap must be defined
spec.upgradeContainer.envFrom[].prefix <string>	# Optional text to prepend to the name of each environment variable. May consist of any printable ASCII characters except '='.
spec.upgradeContainer.envFrom[].secretRef <object>	# The Secret to select from
spec.upgradeContainer.envFrom[].secretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.upgradeContainer.envFrom[].secretRef.optional <boolean>	# Specify whether the Secret must be defined
spec.upgradeContainer.envs <[]object>
spec.upgradeContainer.envs[].name <string> (required)	# Name of the environment variable. May consist of any printable ASCII characters except '='.
spec.upgradeContainer.envs[].value <string>	# Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
spec.upgradeContainer.envs[].valueFrom <object>	# Source for the environment variable's value. Cannot be used if value is not empty.
spec.upgradeContainer.envs[].valueFrom.configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.upgradeContainer.envs[].valueFrom.configMapKeyRef.key <string> (required)	# The key to select.
spec.upgradeContainer.envs[].valueFrom.configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.upgradeContainer.envs[].valueFrom.configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.upgradeContainer.envs[].valueFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
spec.upgradeContainer.envs[].valueFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.upgradeContainer.envs[].valueFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.upgradeContainer.envs[].valueFrom.fileKeyRef <object>	# FileKeyRef selects a key of the env file. Requires the EnvFiles feature gate to be enabled.
spec.upgradeContainer.envs[].valueFrom.fileKeyRef.key <string> (required)	# The key within the env file. An invalid key will prevent the pod from starting. The keys defined within a source may consist of any printable ASCII characters except '='. During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
spec.upgradeContainer.envs[].valueFrom.fileKeyRef.optional <boolean> default=false	# Specify whether the file or its key must be defined. If the file or key does not exist, then the env var is not published. If optional is set to true and the specified key does not exist, the environment variable will not be set in the Pod's containers. If optional is set to false and the specified key does not exist, an error will be returned during Pod creation.
spec.upgradeContainer.envs[].valueFrom.fileKeyRef.path <string> (required)	# The path within the volume from which to select the file. Must be relative and may not contain the '..' path or start with '..'.
spec.upgradeContainer.envs[].valueFrom.fileKeyRef.volumeName <string> (required)	# The name of the volume mount containing the env file.
spec.upgradeContainer.envs[].valueFrom.resourceFieldRef <object>	# Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
spec.upgradeContainer.envs[].valueFrom.resourceFieldRef.containerName <string>	# Container name: required for volumes, optional for env vars
spec.upgradeContainer.envs[].valueFrom.resourceFieldRef.divisor <int-or-string>	# Specifies the output format of the exposed resources, defaults to "1"
spec.upgradeContainer.envs[].valueFrom.resourceFieldRef.resource <string> (required)	# Required: resource to select
spec.upgradeContainer.envs[].valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.upgradeContainer.envs[].valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.upgradeContainer.envs[].valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.upgradeContainer.envs[].valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
spec.upgradeContainer.image <string>
spec.upgradeContainer.securityContext <object>	# SecurityContext holds security configuration that will be applied to a container. Some fields are present in both SecurityContext and PodSecurityContext. When both are set, the values in SecurityContext take precedence.
spec.upgradeContainer.securityContext.allowPrivilegeEscalation <boolean>	# AllowPrivilegeEscalation controls whether a process can gain more privileges than its parent process. This bool directly controls if the no_new_privs flag will be set on the container process. AllowPrivilegeEscalation is true always when the container is: 1) run as Privileged 2) has CAP_SYS_ADMIN Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.appArmorProfile <object>	# appArmorProfile is the AppArmor options to use by this container. If set, this profile overrides the pod's appArmorProfile. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.appArmorProfile.localhostProfile <string>	# localhostProfile indicates a profile loaded on the node that should be used. The profile must be preconfigured on the node to work. Must match the loaded name of the profile. Must be set if and only if type is "Localhost".
spec.upgradeContainer.securityContext.appArmorProfile.type <string> (required)	# type indicates which kind of AppArmor profile will be applied. Valid options are: Localhost - a profile pre-loaded on the node. RuntimeDefault - the container runtime's default profile. Unconfined - no AppArmor enforcement.
spec.upgradeContainer.securityContext.capabilities <object>	# The capabilities to add/drop when running containers. Defaults to the default set of capabilities granted by the container runtime. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.capabilities.add <[]string>	# Added capabilities
spec.upgradeContainer.securityContext.capabilities.drop <[]string>	# Removed capabilities
spec.upgradeContainer.securityContext.privileged <boolean>	# Run container in privileged mode. Processes in privileged containers are essentially equivalent to root on the host. Defaults to false. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.procMount <string>	# procMount denotes the type of proc mount to use for the containers. The default value is Default which uses the container runtime defaults for readonly paths and masked paths. This requires the ProcMountType feature flag to be enabled. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.readOnlyRootFilesystem <boolean>	# Whether this container has a read-only root filesystem. Default is false. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.runAsGroup <integer> format=int64	# The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.runAsNonRoot <boolean>	# Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
spec.upgradeContainer.securityContext.runAsUser <integer> format=int64	# The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.seLinuxOptions <object>	# The SELinux context to be applied to the container. If unspecified, the container runtime will allocate a random SELinux context for each container. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.seLinuxOptions.level <string>	# Level is SELinux level label that applies to the container.
spec.upgradeContainer.securityContext.seLinuxOptions.role <string>	# Role is a SELinux role label that applies to the container.
spec.upgradeContainer.securityContext.seLinuxOptions.type <string>	# Type is a SELinux type label that applies to the container.
spec.upgradeContainer.securityContext.seLinuxOptions.user <string>	# User is a SELinux user label that applies to the container.
spec.upgradeContainer.securityContext.seccompProfile <object>	# The seccomp options to use by this container. If seccomp options are provided at both the pod & container level, the container options override the pod options. Note that this field cannot be set when spec.os.name is windows.
spec.upgradeContainer.securityContext.seccompProfile.localhostProfile <string>	# localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet's configured seccomp profile location. Must be set if type is "Localhost". Must NOT be set for any other type.
spec.upgradeContainer.securityContext.seccompProfile.type <string> (required)	# type indicates which kind of seccomp profile will be applied. Valid options are: Localhost - a profile defined in a file on the node should be used. RuntimeDefault - the container runtime default profile should be used. Unconfined - no profile should be applied.
spec.upgradeContainer.securityContext.windowsOptions <object>	# The Windows specific settings applied to all containers. If unspecified, the options from the PodSecurityContext will be used. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is linux.
spec.upgradeContainer.securityContext.windowsOptions.gmsaCredentialSpec <string>	# GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field.
spec.upgradeContainer.securityContext.windowsOptions.gmsaCredentialSpecName <string>	# GMSACredentialSpecName is the name of the GMSA credential spec to use.
spec.upgradeContainer.securityContext.windowsOptions.hostProcess <boolean>	# HostProcess determines if a container should be run as a 'Host Process' container. All of a Pod's containers must have the same effective HostProcess value (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers). In addition, if HostProcess is true then HostNetwork must also be set to true.
spec.upgradeContainer.securityContext.windowsOptions.runAsUserName <string>	# The UserName in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
spec.upgradeContainer.volumes <[]object>
spec.upgradeContainer.volumes[].destination <string>
spec.upgradeContainer.volumes[].name <string>
spec.upgradeContainer.volumes[].source <string>
status <object>
status.conditions <[]object>
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
