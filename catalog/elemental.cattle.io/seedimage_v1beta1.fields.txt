# schema source: Rancher Elemental Operator v1.9.2 https://github.com/rancher/elemental-operator
apiVersion <string> enum=elemental.cattle.io/v1beta1
kind <string> enum=SeedImage
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.baseImage <string>	# BaseImg the base elemental image used to build the seed image.
spec.buildContainer <object>	# BuildContainer settings for a custom container used to generate the downloadable image.
spec.buildContainer.args <[]string>	# Args same as corev1.Container.Args
spec.buildContainer.command <[]string>	# Command same as corev1.Container.Command
spec.buildContainer.image <string>	# Image container image to run
spec.buildContainer.imagePullPolicy <string>	# Args same as corev1.Container.ImagePullPolicy
spec.buildContainer.name <string>	# Name of the spawned container
spec.cleanupAfterMinutes <integer> default=60 format=int32	# LifetimeMinutes the time at which the built seed image will be cleaned up. If when the lifetime elapses the built image is being downloaded, the active download will be completed before removing the built image. Default is 60 minutes, set to 0 to disable.
spec.cloud-config <any>	# CloudConfig contains cloud-config data to be put in the generated iso.
spec.registrationRef <object> (required)	# MachineRegistrationRef a reference to the related MachineRegistration.
spec.registrationRef.apiVersion <string>	# API version of the referent.
spec.registrationRef.fieldPath <string>	# If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object.
spec.registrationRef.kind <string>	# Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.registrationRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.registrationRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.registrationRef.resourceVersion <string>	# Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
spec.registrationRef.uid <string>	# UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
spec.retriggerBuild <boolean>	# RetriggerBuild triggers to build again a cleaned up seed image.
spec.size <int-or-string> default=6442450944	# Size specifies the size of the volume used to store the image. Defaults to 6Gi
spec.targetPlatform <string> pattern="^$|^\\S+\\/\\S+$"	# Platform specifies the target platform for the built image. Example: linux/amd64
spec.type <string> (required) enum=iso|raw default="iso"	# Type specifies the type of seed image to built. Valid values are iso|raw Defaults to "iso"
status <object>
status.checksumURL <string>	# ChecksumURL the URL from which the SeedImage checksum can be downloaded once the image is built.
status.conditions <[]object>	# Conditions describe the state of the seedimage object.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.downloadToken <string>	# DownloadToken a token to identify the seed image to download.
status.downloadURL <string>	# DownloadURL the URL from which the SeedImage can be downloaded once built.
status.state <string> enum=Initialized|Started|Completed|Failed|NotStarted	# State reflect the state of the seed image build process.
