# schema source: Kubernetes v1.36.3 https://github.com/kubernetes/kubernetes
apiVersion <string> enum=apps/v1
kind <string> enum=ControllerRevision
metadata.name <string> (required)
metadata.namespace <string> (required)
data <object (free-form)>	# Data is the serialized representation of the state.
revision <integer> (required) format=int64	# Revision indicates the revision of the state represented by Data.
