# schema source: Cluster API k0smotron Bootstrap v2.0.4 https://github.com/k0sproject/k0smotron
apiVersion <string> enum=bootstrap.cluster.x-k8s.io/v1beta2
kind <string> enum=K0sWorkerConfigTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# K0sWorkerConfigTemplateSpec describes a k0s worker configuration template's spec.
spec.template <object>	# K0sWorkerConfigTemplateResource describes the data needed to create a K0sWorkerConfig from a template.
spec.template.metadata <object>
spec.template.metadata.annotations <map[string]string>
spec.template.metadata.finalizers <[]string>
spec.template.metadata.labels <map[string]string>
spec.template.metadata.name <string>
spec.template.metadata.namespace <string>
spec.template.spec <object>	# K0sWorkerConfigSpec describes a k0s worker configuration's spec.
spec.template.spec.args <[]string>	# Args specifies extra arguments to be passed to k0s worker. See: https://docs.k0sproject.io/stable/worker-node-config/ See: https://docs.k0sproject.io/stable/cli/k0s_worker/
spec.template.spec.downloadURL <string>	# DownloadURL specifies the URL to download k0s binary from. If specified the version field is ignored and what ever version is downloaded from the URL is used. When no value is specified, k0smotron will use the default URL: https://get.k0s.sh
spec.template.spec.files <[]object>	# Files specifies extra files to be passed to user_data upon creation.
spec.template.spec.files[].content <string>
spec.template.spec.files[].contentFrom <object>	# ContentFrom specifies the source of the content.
spec.template.spec.files[].contentFrom.configMapRef <object>	# ConfigMapRef is a reference to a configmap that contains the content.
spec.template.spec.files[].contentFrom.configMapRef.key <string> (required)	# Key is the key in the source that contains the content
spec.template.spec.files[].contentFrom.configMapRef.name <string> (required)	# Name is the name of the source
spec.template.spec.files[].contentFrom.secretRef <object>	# SecretRef is a reference to a secret that contains the content.
spec.template.spec.files[].contentFrom.secretRef.key <string> (required)	# Key is the key in the source that contains the content
spec.template.spec.files[].contentFrom.secretRef.name <string> (required)	# Name is the name of the source
spec.template.spec.files[].path <string>
spec.template.spec.files[].permissions <string>
spec.template.spec.k0sInstallDir <string> default="/usr/local/bin"	# K0sInstallDir specifies the directory where k0s binary will be installed. If empty, k0smotron will use /usr/local/bin, which is the default install path used by k0s get script.
spec.template.spec.postK0sCommands <[]string>	# PostK0sCommands specifies commands to be run after starting k0s worker.
spec.template.spec.preInstalledK0s <boolean>	# PreInstallK0s specifies whether k0s binary is pre-installed on the node.
spec.template.spec.preK0sCommands <[]string>	# PreK0sCommands specifies commands to be run before starting k0s worker.
spec.template.spec.provisioner <object>	# Provisioner defines the provisioner configuration. Defaults to cloud-init.
spec.template.spec.provisioner.customUserDataRef <object>	# CustomUserDataRef is a reference to a secret or a configmap that contains the custom user data. Provided user-data will be merged with the one generated by k0smotron. Note that you may want to specify the merge type. See: https://cloudinit.readthedocs.io/en/latest/reference/merging.html
spec.template.spec.provisioner.customUserDataRef.configMapRef <object>	# ConfigMapRef is a reference to a configmap that contains the content.
spec.template.spec.provisioner.customUserDataRef.configMapRef.key <string> (required)	# Key is the key in the source that contains the content
spec.template.spec.provisioner.customUserDataRef.configMapRef.name <string> (required)	# Name is the name of the source
spec.template.spec.provisioner.customUserDataRef.secretRef <object>	# SecretRef is a reference to a secret that contains the content.
spec.template.spec.provisioner.customUserDataRef.secretRef.key <string> (required)	# Key is the key in the source that contains the content
spec.template.spec.provisioner.customUserDataRef.secretRef.name <string> (required)	# Name is the name of the source
spec.template.spec.provisioner.ignition <object>	# Ignition defines the ignition configuration. If empty, k0smotron will use cloud-init.
spec.template.spec.provisioner.ignition.additionalConfig <string>	# AdditionalConfig is an unstructured object that contains additional config to be merged with the generated one. The format follows Butane spec: https://coreos.github.io/butane/
spec.template.spec.provisioner.ignition.variant <string> (required) enum=fcos|flatcar|openshift|r4e|fiot	# Variant declares which distribution variant the generated config is for. Check the supported variants and versions here: https://coreos.github.io/butane/specs/#butane-specifications-and-ignition-specifications
spec.template.spec.provisioner.ignition.version <string> (required)	# Version is the schema version of the Butane config to use Check the supported variants and versions here: https://coreos.github.io/butane/specs/#butane-specifications-and-ignition-specifications
spec.template.spec.provisioner.platform <string> enum=linux|windows default="linux"	# Platform specifies the target platform for the worker node.
spec.template.spec.provisioner.type <string> enum=cloud-config|ignition|powershell|powershell-xml default="cloud-config"	# Type is the provisioner format type.
spec.template.spec.secretMetadata <object>	# SecretMetadata specifies metadata (labels and annotations) to be propagated to the bootstrap Secret.
spec.template.spec.secretMetadata.annotations <map[string]string>	# Annotations to be added to the bootstrap Secret
spec.template.spec.secretMetadata.labels <map[string]string>	# Labels to be added to the bootstrap Secret
spec.template.spec.useSystemHostname <boolean> default=false	# UseSystemHostname specifies whether to use the system hostname for the kubernetes node name. By default, k0smotron will use Machine name as a node name. If true, it will pick it from `hostname` command output.
spec.template.spec.version <string>	# Version is the version of k0s to use. In case this is not set, k0smotron will use a version field of the Machine object. If it's empty, the latest version is used. Make sure the version is compatible with the k0s version running on the control plane. For reference see the Kubernetes version skew policy: https://kubernetes.io/docs/setup/release/version-skew-policy/
spec.template.spec.workingDir <string>	# WorkingDir specifies the working directory where k0smotron will place its files.
