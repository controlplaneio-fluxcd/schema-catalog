# schema source: Knative Eventing knative-v1.22.2 https://github.com/knative/eventing
apiVersion <string> enum=sinks.knative.dev/v1alpha1
kind <string> enum=IntegrationSink
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec defines the desired state of the IntegrationSink.
spec.aws <object>
spec.aws.auth <object>	# Auth configurations
spec.aws.auth.secret <object>	# Auth secret
spec.aws.auth.secret.ref <object>	# Secret reference. 
spec.aws.auth.secret.ref.name <string> (required)	# Secret name
spec.aws.auth.serviceAccountName <string>	# Optional ServiceAccount to assign to pod. This enables the pod default credentials to be used instead of the auth secret.
spec.aws.s3 <object>
spec.aws.s3.arn <string>	# The S3 Bucket name or Amazon Resource Name (ARN).
spec.aws.s3.autoCreateBucket <boolean> default=false	# Specifies to automatically create the S3 bucket.
spec.aws.s3.delay <integer> default=500	# The number of milliseconds before the next poll of the selected bucket.
spec.aws.s3.deleteAfterRead <boolean> default=true	# Specifies to delete objects after consuming them.
spec.aws.s3.destinationBucket <string>	# Define the destination bucket where an object must be moved when moveAfterRead is set to true.
spec.aws.s3.destinationBucketPrefix <string>	# Define the destination bucket prefix to use when an object must be moved, and moveAfterRead is set to true.
spec.aws.s3.destinationBucketSuffix <string>	# Define the destination bucket suffix to use when an object must be moved, and moveAfterRead is set to true.
spec.aws.s3.forcePathStyle <boolean> default=false	# Forces path style when accessing AWS S3 buckets.
spec.aws.s3.ignoreBody <boolean> default=false	# If true, the S3 Object body is ignored. Setting this to true overrides any behavior defined by the `includeBody` option. If false, the S3 object is put in the body.
spec.aws.s3.maxMessagesPerPoll <integer> default=10	# Gets the maximum number of messages as a limit to poll at each polling. Gets the maximum number of messages as a limit to poll at each polling. The default value is 10. Use 0 or a negative number to set it as unlimited.
spec.aws.s3.moveAfterRead <boolean> default=false	# Move objects from S3 bucket to a different bucket after they have been retrieved.
spec.aws.s3.overrideEndpoint <boolean> default=false	# Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
spec.aws.s3.prefix <string>	# The AWS S3 bucket prefix to consider while searching.
spec.aws.s3.region <string>	# The AWS region to access.
spec.aws.s3.uriEndpointOverride <string>	# The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
spec.aws.sns <object>
spec.aws.sns.arn <string>	# The SNS topic name name or Amazon Resource Name (ARN).
spec.aws.sns.autoCreateTopic <boolean> default=false	# Setting the autocreation of the SNS topic.
spec.aws.sns.overrideEndpoint <boolean> default=false	# Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
spec.aws.sns.region <string>	# The AWS region to access.
spec.aws.sns.uriEndpointOverride <string>	# The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
spec.aws.sqs <object>
spec.aws.sqs.arn <string>	# The SQS Queue Name or ARN
spec.aws.sqs.autoCreateQueue <boolean> default=false	# Setting the autocreation of the SQS queue.
spec.aws.sqs.delay <integer> default=500	# The number of milliseconds before the next poll of the selected stream
spec.aws.sqs.deleteAfterRead <boolean> default=true	# Delete messages after consuming them
spec.aws.sqs.greedy <boolean> default=false	# If greedy is enabled, then the polling will happen immediately again, if the previous run polled 1 or more messages.
spec.aws.sqs.host <string> default="amazonaws.com"	# The hostname of the Amazon AWS cloud.
spec.aws.sqs.maxMessagesPerPoll <integer> default=1	# The maximum number of messages to return. Amazon SQS never returns more messages than this value (however, fewer messages might be returned). Valid values 1 to 10. Default 1.
spec.aws.sqs.overrideEndpoint <boolean> default=false	# Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
spec.aws.sqs.protocol <string> default="https"	# The underlying protocol used to communicate with SQS
spec.aws.sqs.queueURL <string>	# The full SQS Queue URL (required if using KEDA)
spec.aws.sqs.region <string>	# The AWS region to access.
spec.aws.sqs.uriEndpointOverride <string>	# The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
spec.aws.sqs.visibilityTimeout <integer>	# The duration (in seconds) that the received messages are hidden from subsequent retrieve requests after being retrieved by a ReceiveMessage request.
spec.aws.sqs.waitTimeSeconds <integer>	# The duration (in seconds) for which the call waits for a message to arrive in the queue before returning. If a message is available, the call returns sooner than WaitTimeSeconds. If no messages are available and the wait time expires, the call does not return a message list.
spec.log <object>
spec.log.level <string> default="INFO"	# Logging level to use
spec.log.logMask <boolean> default=false	# Mask sensitive information like password or passphrase in the log
spec.log.loggerName <string> default="log-sink"	# Name of the logging category to use
spec.log.marker <string>	# An optional Marker name to use
spec.log.multiline <boolean> default=false	# If enabled then each information is outputted on a newline
spec.log.showAllProperties <boolean> default=false	# Show all of the exchange properties (both internal and custom)
spec.log.showBody <boolean> default=true	# Show the message body
spec.log.showBodyType <boolean> default=true	# Show the body Java type
spec.log.showCachedStreams <boolean> default=true	# Whether Camel should show cached stream bodies or not.
spec.log.showExchangePattern <boolean> default=true	# Shows the Message Exchange Pattern (or MEP for short)
spec.log.showHeaders <boolean> default=false	# Show the headers received
spec.log.showProperties <boolean> default=false	# Show the exchange properties (only custom). Use showAllProperties to show both internal and custom properties.
spec.log.showStreams <boolean> default=false	# Show the stream bodies (they may not be available in following steps)
status <object>	# Status represents the current state of the IntegrationSink. This data may be out of date.
status.address <object>	# IntegrationSink is Addressable. It exposes the endpoints as URIs to get events delivered into the used Kamelet.
status.address.CACerts <string>
status.address.audience <string>
status.address.name <string>
status.address.url <string>
status.addresses <[]object>	# IntegrationSink is Addressable. It exposes the endpoints as URIs to get events delivered into the used Kamelet.
status.addresses[].CACerts <string>
status.addresses[].audience <string>
status.addresses[].name <string>
status.addresses[].url <string>
status.annotations <object (free-form)>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
status.policies <[]object>	# List of applied EventPolicies
status.policies[].apiVersion <string>	# The API version of the applied EventPolicy. This indicates, which version of EventPolicy is supported by the resource.
status.policies[].name <string>	# The name of the applied EventPolicy
