# schema source: Cilium v1.20.0 https://github.com/cilium/cilium
apiVersion <string> enum=cilium.io/v2alpha1 (deprecated)
kind <string> enum=CiliumDatapathPlugin (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)
spec.attachmentPolicy <string> (required) enum=Always|BestEffort	# AttachmentPolicy dictates how Cilium behaves when it cannot talk to a plugin.
spec.version <string> (required)	# Version is an opaque string used to indicate the datapath plugin version. Update this when deploying a new version of a datapath plugin to trigger a datapath reinitialization.
