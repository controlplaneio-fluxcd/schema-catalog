# schema source: AWS EC2 Controller v1.18.3 https://github.com/aws-controllers-k8s/ec2-controller
apiVersion <string> enum=ec2.services.k8s.aws/v1alpha1
kind <string> enum=NATGateway
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# NatGatewaySpec defines the desired state of NatGateway. Describes a NAT gateway.
spec.allocationID <string>	# [Public NAT gateways only] The allocation ID of an Elastic IP address to associate with the NAT gateway. You cannot specify an Elastic IP address with a private NAT gateway. If the Elastic IP address is associated with another resource, you must first disassociate it.
spec.allocationRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.allocationRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.allocationRef.from.name <string>
spec.allocationRef.from.namespace <string>
spec.availabilityMode <string>	# Specifies whether to create a zonal (single-AZ) or regional (multi-AZ) NAT gateway. Defaults to zonal. A zonal NAT gateway is a NAT Gateway that provides redundancy and scalability within a single availability zone. A regional NAT gateway is a single NAT Gateway that works across multiple availability zones (AZs) in your VPC, providing redundancy, scalability and availability across all the AZs in a Region. For more information, see Regional NAT gateways for automatic multi-AZ expansion (https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateways-regional.html) in the Amazon VPC User Guide.
spec.availabilityZoneAddresses <[]object>	# For regional NAT gateways only: Specifies which Availability Zones you want the NAT gateway to support and the Elastic IP addresses (EIPs) to use in each AZ. The regional NAT gateway uses these EIPs to handle outbound NAT traffic from their respective AZs. If not specified, the NAT gateway will automatically expand to new AZs and associate EIPs upon detection of an elastic network interface. If you specify this parameter, auto-expansion is disabled and you must manually manage AZ coverage. A regional NAT gateway is a single NAT Gateway that works across multiple availability zones (AZs) in your VPC, providing redundancy, scalability and availability across all the AZs in a Region. For more information, see Regional NAT gateways for automatic multi-AZ expansion (https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateways-regional.html) in the Amazon VPC User Guide.
spec.availabilityZoneAddresses[].allocationIDs <[]string>
spec.availabilityZoneAddresses[].availabilityZone <string>
spec.availabilityZoneAddresses[].availabilityZoneID <string>
spec.connectivityType <string>	# Indicates whether the NAT gateway supports public or private connectivity. The default is public connectivity.
spec.subnetID <string>	# The ID of the subnet in which to create the NAT gateway.
spec.subnetRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.subnetRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.subnetRef.from.name <string>
spec.subnetRef.from.namespace <string>
spec.tags <[]object>	# The tags. The value parameter is required, but if you don't want the tag to have a value, specify the parameter with no value, and we set the value to an empty string.
spec.tags[].key <string>
spec.tags[].value <string>
spec.vpcID <string>	# The ID of the VPC where you want to create a regional NAT gateway.
spec.vpcRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.vpcRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.vpcRef.from.name <string>
spec.vpcRef.from.namespace <string>
status <object>	# NATGatewayStatus defines the observed state of NATGateway
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.createTime <string> format=date-time	# The date and time the NAT gateway was created.
status.deleteTime <string> format=date-time	# The date and time the NAT gateway was deleted, if applicable.
status.failureCode <string>	# If the NAT gateway could not be created, specifies the error code for the failure. (InsufficientFreeAddressesInSubnet | Gateway.NotAttached | InvalidAllocationID.NotFound | Resource.AlreadyAssociated | InternalError | InvalidSubnetID.NotFound)
status.failureMessage <string>	# If the NAT gateway could not be created, specifies the error message for the failure, that corresponds to the error code. * For InsufficientFreeAddressesInSubnet: "Subnet has insufficient free addresses to create this NAT gateway" * For Gateway.NotAttached: "Network vpc-xxxxxxxx has no Internet gateway attached" * For InvalidAllocationID.NotFound: "Elastic IP address eipalloc-xxxxxxxx could not be associated with this NAT gateway" * For Resource.AlreadyAssociated: "Elastic IP address eipalloc-xxxxxxxx is already associated" * For InternalError: "Network interface eni-xxxxxxxx, created and used internally by this NAT gateway is in an invalid state. Please try again." * For InvalidSubnetID.NotFound: "The specified subnet subnet-xxxxxxxx does not exist or could not be found."
status.natGatewayAddresses <[]object>	# Information about the IP addresses and network interface associated with the NAT gateway.
status.natGatewayAddresses[].allocationID <string>
status.natGatewayAddresses[].availabilityZone <string>
status.natGatewayAddresses[].availabilityZoneID <string>
status.natGatewayAddresses[].networkInterfaceID <string>
status.natGatewayAddresses[].privateIP <string>
status.natGatewayAddresses[].publicIP <string>
status.natGatewayID <string>	# The ID of the NAT gateway.
status.provisionedBandwidth <object>	# Reserved. If you need to sustain traffic greater than the documented limits (https://docs.aws.amazon.com/vpc/latest/userguide/amazon-vpc-limits.html#vpc-limits-gateways), contact Amazon Web Services Support.
status.provisionedBandwidth.provisionTime <string> format=date-time
status.provisionedBandwidth.provisioned <string>
status.provisionedBandwidth.requestTime <string> format=date-time
status.provisionedBandwidth.requested <string>
status.provisionedBandwidth.status <string>
status.state <string>	# The state of the NAT gateway. * pending: The NAT gateway is being created and is not ready to process traffic. * failed: The NAT gateway could not be created. Check the failureCode and failureMessage fields for the reason. * available: The NAT gateway is able to process traffic. This status remains until you delete the NAT gateway, and does not indicate the health of the NAT gateway. * deleting: The NAT gateway is in the process of being terminated and may still be processing traffic. * deleted: The NAT gateway has been terminated and is no longer processing traffic.
status.vpcID <string>	# The ID of the VPC in which the NAT gateway is located.
