# schema source: Knative Eventing knative-v1.22.2 https://github.com/knative/eventing
apiVersion <string> enum=eventing.knative.dev/v1alpha1
kind <string> enum=RequestReply
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Spec defines the desired state of the RequestReply.
spec.brokerRef <object>	# A KReference referring to the broker this RequestReply forwards events to. CrossNamespace references are not allowed.
spec.brokerRef.apiVersion <string>	# API Version of the broker.
spec.brokerRef.kind <string>	# Kind of the broker. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.brokerRef.name <string>	# Name of the broker. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.correlationAttribute <string>	# The name of the cloudevent attribute where the correlation id will be set on new events.
spec.delivery <object>	# Delivery contains the delivery spec for each trigger to this Broker. Each trigger delivery spec, if any, overrides this global delivery spec.
spec.delivery.backoffDelay <string>	# BackoffDelay is the delay before retrying. More information on Duration format: - https://www.iso.org/iso-8601-date-and-time-format.html - https://en.wikipedia.org/wiki/ISO_8601 For linear policy, backoff delay is backoffDelay*<numberOfRetries>. For exponential policy, backoff delay is backoffDelay*2^<numberOfRetries>.
spec.delivery.backoffPolicy <string>	# BackoffPolicy is the retry backoff policy (linear, exponential).
spec.delivery.deadLetterSink <object>	# DeadLetterSink is the sink receiving event that could not be sent to a destination.
spec.delivery.deadLetterSink.CACerts <string>	# Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink.
spec.delivery.deadLetterSink.audience <string>	# Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can't be received from the target itself. If specified, it takes precedence over the target's Audience.
spec.delivery.deadLetterSink.ref <object>	# Ref points to an Addressable.
spec.delivery.deadLetterSink.ref.apiVersion <string>	# API version of the referent.
spec.delivery.deadLetterSink.ref.kind <string>	# Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.delivery.deadLetterSink.ref.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.delivery.deadLetterSink.ref.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out.
spec.delivery.deadLetterSink.uri <string>	# URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref.
spec.delivery.retry <integer> format=int32	# Retry is the minimum number of retries the sender should attempt when sending an event before moving it to the dead letter sink.
spec.replyAttribute <string>	# The name of the cloudevents attribute which will hold the correlation id for an event which will be treated as a reply.
spec.timeout <string>	# A ISO8601 string representing how long RequestReply holds onto an incoming request before it times out without a reply.
status <object>	# Status represents the current state of the RequestReply. This data may be out of date.
status.address <object>	# RequestReply is Addressable. It exposes the endpoint as an URI to get events delivered.
status.address.CACerts <string>
status.address.audience <string>
status.address.name <string>
status.address.url <string>
status.addresses <[]object>	# RequestReply is Addressable. It exposes the endpoints as URIs to get events delivered.
status.addresses[].CACerts <string>
status.addresses[].audience <string>
status.addresses[].name <string>
status.addresses[].url <string>
status.annotations <object (free-form)>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.desiredReplicas <integer>	# The current replicas (StatefulSet pod + trigger) that are desired
status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
status.policies <[]object>	# List of applied EventPolicies
status.policies[].apiVersion <string>	# The API version of the applied EventPolicy. This indicates whichversion of EventPolicy is supported by the resource.
status.policies[].name <string>	# The name of the applied EventPolicy
status.readyReplicas <integer>	# The current replicas (StatefulSet pod + trigger) that are ready
