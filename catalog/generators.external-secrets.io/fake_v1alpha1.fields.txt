# schema source: External Secrets v2.7.0 https://github.com/external-secrets/external-secrets
apiVersion <string> enum=generators.external-secrets.io/v1alpha1
kind <string> enum=Fake
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# FakeSpec contains the static data.
spec.controller <string>	# Used to select the correct ESO controller (think: ingress.ingressClassName) The ESO controller is instantiated with a specific controller name and filters VDS based on this property
spec.data <map[string]string>	# Data defines the static data returned by this generator.
