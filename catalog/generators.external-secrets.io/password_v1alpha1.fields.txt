# schema source: External Secrets v2.7.0 https://github.com/external-secrets/external-secrets
apiVersion <string> enum=generators.external-secrets.io/v1alpha1
kind <string> enum=Password
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# PasswordSpec controls the behavior of the password generator.
spec.allowRepeat <boolean> (required) default=false	# set AllowRepeat to true to allow repeating characters.
spec.digits <integer>	# Digits specifies the number of digits in the generated password. If omitted it defaults to 25% of the length of the password
spec.encoding <string> enum=base64|base64url|base32|hex|raw default="raw"	# Encoding specifies the encoding of the generated password. Valid values are: - "raw" (default): no encoding - "base64": standard base64 encoding - "base64url": base64url encoding - "base32": base32 encoding - "hex": hexadecimal encoding
spec.length <integer> (required) default=24	# Length of the password to be generated. Defaults to 24
spec.noUpper <boolean> (required) default=false	# Set NoUpper to disable uppercase characters
spec.secretKeys <[]string> min=1	# SecretKeys defines the keys that will be populated with generated passwords. Defaults to "password" when not set.
spec.symbolCharacters <string>	# SymbolCharacters specifies the special characters that should be used in the generated password.
spec.symbols <integer>	# Symbols specifies the number of symbol characters in the generated password. If omitted it defaults to 25% of the length of the password
