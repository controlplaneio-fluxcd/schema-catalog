# schema source: External Secrets v2.7.0
apiVersion <string> enum=generators.external-secrets.io/v1alpha1
kind <string> enum=GithubAccessToken
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# GithubAccessTokenSpec defines the desired state to generate a GitHub access token.
spec.appID <string> (required)
spec.auth <object> (required)	# Auth configures how ESO authenticates with a Github instance.
spec.auth.privateKey <object> (required)	# GithubSecretRef references a secret containing GitHub credentials.
spec.auth.privateKey.secretRef <object> (required)	# SecretKeySelector is a reference to a specific 'key' within a Secret resource. In some instances, `key` is a required field.
spec.auth.privateKey.secretRef.key <string> pattern="^[-._a-zA-Z0-9]+$" min=1 max=253	# A key in the referenced Secret. Some instances of this field may be defaulted, in others it may be required.
spec.auth.privateKey.secretRef.name <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The name of the Secret resource being referred to.
spec.auth.privateKey.secretRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# The namespace of the Secret resource being referred to. Ignored if referent is not cluster-scoped, otherwise defaults to the namespace of the referent.
spec.installID <string> (required)
spec.permissions <map[string]string>	# Map of permissions the token will have. If omitted, defaults to all permissions the GitHub App has.
spec.repositories <[]string>	# List of repositories the token will have access to. If omitted, defaults to all repositories the GitHub App is installed to.
spec.url <string>	# URL configures the GitHub instance URL. Defaults to https://github.com/.
