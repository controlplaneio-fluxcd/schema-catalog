# schema source: AWS SageMaker Controller v1.8.3 https://github.com/aws-controllers-k8s/sagemaker-controller
apiVersion <string> enum=sagemaker.services.k8s.aws/v1alpha1
kind <string> enum=InferenceComponent
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# InferenceComponentSpec defines the desired state of InferenceComponent.
spec.endpointName <string> (required)	# The name of an existing endpoint where you host the inference component. Regex Pattern: `^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,62}$`
spec.inferenceComponentName <string> (required)	# A unique name to assign to the inference component. Regex Pattern: `^[a-zA-Z0-9]([\-a-zA-Z0-9]*[a-zA-Z0-9])?$`
spec.runtimeConfig <object>	# Runtime settings for a model that is deployed with an inference component.
spec.runtimeConfig.copyCount <integer> format=int64
spec.specification <object> (required)	# Details about the resources to deploy with this inference component, including the model, container, and compute resources.
spec.specification.computeResourceRequirements <object>	# Defines the compute resources to allocate to run a model, plus any adapter models, that you assign to an inference component. These resources include CPU cores, accelerators, and memory.
spec.specification.computeResourceRequirements.maxMemoryRequiredInMb <integer> format=int64
spec.specification.computeResourceRequirements.minMemoryRequiredInMb <integer> format=int64
spec.specification.computeResourceRequirements.numberOfAcceleratorDevicesRequired <number>
spec.specification.computeResourceRequirements.numberOfCPUCoresRequired <number>
spec.specification.container <object>	# Defines a container that provides the runtime environment for a model that you deploy with an inference component.
spec.specification.container.artifactURL <string>
spec.specification.container.environment <map[string]string>
spec.specification.container.image <string>
spec.specification.modelName <string>
spec.specification.startupParameters <object>	# Settings that take effect while the model container starts up.
spec.specification.startupParameters.containerStartupHealthCheckTimeoutInSeconds <integer> format=int64
spec.specification.startupParameters.modelDataDownloadTimeoutInSeconds <integer> format=int64
spec.tags <[]object>	# A list of key-value pairs associated with the model. For more information, see Tagging Amazon Web Services resources (https://docs.aws.amazon.com/general/latest/gr/aws_tagging.html) in the Amazon Web Services General Reference.
spec.tags[].key <string>
spec.tags[].value <string>
spec.variantName <string>	# The name of an existing production variant where you host the inference component. Regex Pattern: `^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,62}$`
status <object>	# InferenceComponentStatus defines the observed state of InferenceComponent
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.creationTime <string> format=date-time	# The time when the inference component was created.
status.endpointName <string>	# The name of the endpoint that hosts the inference component. Regex Pattern: `^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,62}$`
status.failureReason <string>	# If the inference component status is Failed, the reason for the failure.
status.inferenceComponentStatus <string>	# The status of the inference component.
status.lastModifiedTime <string> format=date-time	# The time when the inference component was last updated.
status.variantName <string>	# The name of the production variant that hosts the inference component. Regex Pattern: `^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,62}$`
