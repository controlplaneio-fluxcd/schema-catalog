# schema source: AWS SageMaker Controller v1.8.1 https://github.com/aws-controllers-k8s/sagemaker-controller
apiVersion <string> enum=sagemaker.services.k8s.aws/v1alpha1
kind <string> enum=PipelineExecution
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# PipelineExecutionSpec defines the desired state of PipelineExecution. An execution of a pipeline.
spec.parallelismConfiguration <object>	# This configuration, if specified, overrides the parallelism configuration of the parent pipeline for this specific run.
spec.parallelismConfiguration.maxParallelExecutionSteps <integer> format=int64
spec.pipelineExecutionDescription <string>	# The description of the pipeline execution. Regex Pattern: `.*`
spec.pipelineExecutionDisplayName <string>	# The display name of the pipeline execution. Regex Pattern: `^[a-zA-Z0-9](-*[a-zA-Z0-9]){0,81}$`
spec.pipelineName <string> (required)	# The name or Amazon Resource Name (ARN) of the pipeline. Regex Pattern: `^(arn:aws[a-z\-]*:sagemaker:[a-z0-9\-]*:[0-9]{12}:pipeline/.*)?([a-zA-Z0-9](-*[a-zA-Z0-9]){0,255})$`
spec.pipelineParameters <[]object>	# Contains a list of pipeline parameters. This list can be empty.
spec.pipelineParameters[].name <string>
spec.pipelineParameters[].value <string>
spec.pipelineVersionID <integer> format=int64	# The ID of the pipeline version to start execution from.
spec.selectiveExecutionConfig <object>	# The selective execution configuration applied to the pipeline run.
spec.selectiveExecutionConfig.selectedSteps <[]object>
spec.selectiveExecutionConfig.selectedSteps[].stepName <string>
spec.selectiveExecutionConfig.sourcePipelineExecutionARN <string>
status <object>	# PipelineExecutionStatus defines the observed state of PipelineExecution
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.creationTime <string> format=date-time	# The time when the pipeline execution was created.
status.failureReason <string>	# If the execution failed, a message describing why. Regex Pattern: `.*`
status.lastModifiedTime <string> format=date-time	# The time when the pipeline execution was modified last.
status.pipelineExecutionStatus <string>	# The status of the pipeline execution.
