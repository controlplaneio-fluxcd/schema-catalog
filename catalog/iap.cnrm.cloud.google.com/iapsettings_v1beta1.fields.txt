# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=iap.cnrm.cloud.google.com/v1beta1
kind <string> enum=IAPSettings
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# IAPSettingsSpec defines the desired state of IAPSettings
spec.accessSettings <object>	# Top level wrapper for all access related setting in IAP
spec.accessSettings.allowedDomainsSettings <object>	# Settings to configure and enable allowed domains.
spec.accessSettings.allowedDomainsSettings.domains <[]string>	# List of trusted domains.
spec.accessSettings.allowedDomainsSettings.enable <boolean>	# Configuration for customers to opt in for the feature.
spec.accessSettings.corsSettings <object>	# Configuration to allow cross-origin requests via IAP.
spec.accessSettings.corsSettings.allowHTTPOptions <boolean>	# Configuration to allow HTTP OPTIONS calls to skip authorization. If undefined, IAP will not apply any special logic to OPTIONS requests.
spec.accessSettings.gcipSettings <object>	# GCIP claims and endpoint configurations for 3p identity providers.
spec.accessSettings.gcipSettings.loginPageURI <string>	# Login page URI associated with the GCIP tenants. Typically, all resources within the same project share the same login page, though it could be overridden at the sub resource level.
spec.accessSettings.gcipSettings.tenantIDs <[]string>	# GCIP tenant ids that are linked to the IAP resource. tenant_ids could be a string beginning with a number character to indicate authenticating with GCIP tenant flow, or in the format of _<ProjectNumber> to indicate authenticating with GCIP agent flow. If agent flow is used, tenant_ids should only contain one single element, while for tenant flow, tenant_ids can contain multiple elements.
spec.accessSettings.oauthSettings <object>	# Settings to configure IAP's OAuth behavior.
spec.accessSettings.oauthSettings.loginHint <string>	# Domain hint to send as hd=? parameter in OAuth request flow. Enables redirect to primary IDP by skipping Google's login screen. https://developers.google.com/identity/protocols/OpenIDConnect#hd-param Note: IAP does not verify that the id token's hd claim matches this value since access behavior is managed by IAM policies.
spec.accessSettings.oauthSettings.programmaticClients <[]string>	# List of OAuth client IDs allowed to programmatically authenticate with IAP.
spec.accessSettings.reauthSettings <object>	# Settings to configure reauthentication policies in IAP.
spec.accessSettings.reauthSettings.maxAge <string>	# Reauth session lifetime, how long before a user has to reauthenticate again.
spec.accessSettings.reauthSettings.method <string>	# Reauth method requested.
spec.accessSettings.reauthSettings.policyType <string>	# How IAP determines the effective policy in cases of hierarchial policies. Policies are merged from higher in the hierarchy to lower in the hierarchy.
spec.appEngineRef <object>	# Project-wide App Engine service settings
spec.appEngineRef.applicationRef <object> (required)
spec.appEngineRef.applicationRef.external <string>	# Format: projects/{projects_id}/iap_web/appengine-{app_id}
spec.appEngineRef.projectRef <object> (required)	# The Project that this resource belongs to.
spec.appEngineRef.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.appEngineRef.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.appEngineRef.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.appEngineRef.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.appEngineRef.serviceRef <object>	# Optional. If specified, settings apply to the service
spec.appEngineRef.serviceRef.external <string>	# Format: projects/{projects_id}/iap_web/appengine-{app_id}/service/{service_id}
spec.appEngineRef.versionRef <object>	# Optional. If specified, settings apply to the version
spec.appEngineRef.versionRef.external <string>	# Format: projects/{projects_id}/iap_web/appengine-{app_id}/service/{service_id}/version/{version_id}
spec.applicationSettings <object>	# Top level wrapper for all application related settings in IAP
spec.applicationSettings.accessDeniedPageSettings <object>	# Customization for Access Denied page.
spec.applicationSettings.accessDeniedPageSettings.accessDeniedPageURI <string>	# The URI to be redirected to when access is denied.
spec.applicationSettings.accessDeniedPageSettings.generateTroubleshootingURI <boolean>	# Whether to generate a troubleshooting URL on access denied events to this application.
spec.applicationSettings.accessDeniedPageSettings.remediationTokenGenerationEnabled <boolean>	# Whether to generate remediation token on access denied events to this application.
spec.applicationSettings.attributePropagationSettings <object>	# Settings to configure attribute propagation.
spec.applicationSettings.attributePropagationSettings.enable <boolean>	# Whether the provided attribute propagation settings should be evaluated on user requests. If set to true, attributes returned from the expression will be propagated in the set output credentials.
spec.applicationSettings.attributePropagationSettings.expression <string>	# Raw string CEL expression. Must return a list of attributes. A maximum of 45 attributes can be selected. Expressions can select different attribute types from `attributes`: `attributes.saml_attributes`, `attributes.iap_attributes`. The following functions are supported: - filter `<list>.filter(<iter_var>, <predicate>)`: Returns a subset of `<list>` where `<predicate>` is true for every item. - in `<var> in <list>`: Returns true if `<list>` contains `<var>`. - selectByName `<list>.selectByName(<string>)`: Returns the attribute in `<list>` with the given `<string>` name, otherwise returns empty. - emitAs `<attribute>.emitAs(<string>)`: Sets the `<attribute>` name field to the given `<string>` for propagation in selected output credentials. - strict `<attribute>.strict()`: Ignores the `x-goog-iap-attr-` prefix for the provided `<attribute>` when propagating with the `HEADER` output credential, such as request headers. - append `<target_list>.append(<attribute>)` OR `<target_list>.append(<list>)`: Appends the provided `<attribute>` or `<list>` to the end of `<target_list>`. Example expression: `attributes.saml_attributes.filter(x, x.name in ['test']).append(attributes.iap_attributes.selectByName('exact').emitAs('custom').strict())`
spec.applicationSettings.attributePropagationSettings.outputCredentials <[]string>	# Which output credentials attributes selected by the CEL expression should be propagated in. All attributes will be fully duplicated in each selected output credential.
spec.applicationSettings.cookieDomain <string>	# The Domain value to set for cookies generated by IAP. This value is not validated by the API, but will be ignored at runtime if invalid.
spec.applicationSettings.csmSettings <object>	# Settings to configure IAP's behavior for a service mesh.
spec.applicationSettings.csmSettings.rctokenAud <string>	# Audience claim set in the generated RCToken. This value is not validated by IAP.
spec.computeServiceRef <object>	# Project-wide Compute service settings
spec.computeServiceRef.projectRef <object> (required)	# The Project that this resource belongs to.
spec.computeServiceRef.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.computeServiceRef.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.computeServiceRef.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.computeServiceRef.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.computeServiceRef.region <string>	# Optional. If specified, settings apply to the region
spec.computeServiceRef.serviceRef <object>	# Optional. If specified, settings apply to the service
spec.computeServiceRef.serviceRef.external <string>	# The value of an externally managed ComputeBackendService resource.
spec.computeServiceRef.serviceRef.name <string>	# The name of a ComputeBackendService resource.
spec.computeServiceRef.serviceRef.namespace <string>	# The namespace of a ComputeBackendService resource.
spec.folderRef <object>	# Folder-level settings
spec.folderRef.external <string>	# The 'name' field of a folder, when not managed by Config Connector. This field must be set when 'name' field is not set.
spec.folderRef.name <string>	# The 'name' field of a 'Folder' resource. This field must be set when 'external' field is not set.
spec.folderRef.namespace <string>	# The 'namespace' field of a 'Folder' resource. If unset, the namespace is defaulted to the namespace of the referencer resource.
spec.organizationRef <object>	# Organization-level settings
spec.organizationRef.external <string> (required)	# The 'name' field of an organization, when not managed by Config Connector.
spec.projectRef <object>	# Project-level settings
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.projectWebRef <object>	# Project-wide web service settings
spec.projectWebRef.projectRef <object> (required)	# The Project that this resource belongs to.
spec.projectWebRef.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectWebRef.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectWebRef.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectWebRef.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The IAPSettings name.
status <object>	# IAPSettingsStatus defines the config connector machine state of IAPSettings
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the IAPSettings resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
