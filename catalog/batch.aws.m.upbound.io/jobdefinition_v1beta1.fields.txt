# schema source: Upbound AWS Provider v2.6.0 https://github.com/crossplane-contrib/provider-upjet-aws
apiVersion <string> enum=batch.aws.m.upbound.io/v1beta1
kind <string> enum=JobDefinition
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# JobDefinitionSpec defines the desired state of JobDefinition
spec.forProvider <object> (required)
spec.forProvider.containerProperties <string>	# Valid container properties provided as a single valid JSON document. This parameter is only valid if the type parameter is container.
spec.forProvider.deregisterOnNewRevision <boolean>	# When updating a job definition a new revision is created. This parameter determines if the previous version is deregistered (INACTIVE) or left ACTIVE. Defaults to true.
spec.forProvider.ecsProperties <string>	# Valid ECS properties provided as a single valid JSON document. This parameter is only valid if the type parameter is container.
spec.forProvider.eksProperties <object>	# Valid eks properties. This parameter is only valid if the type parameter is container.
spec.forProvider.eksProperties.podProperties <object>	# Properties for the Kubernetes pod resources of a job. See pod_properties below.
spec.forProvider.eksProperties.podProperties.containers <[]object>	# Properties of the container that's used on the Amazon EKS pod. See containers below.
spec.forProvider.eksProperties.podProperties.containers[].args <[]string>	# Array of arguments to the entrypoint. If this isn't specified, the CMD of the container image is used. This corresponds to the args member in the Entrypoint portion of the Pod in Kubernetes. Environment variable references are expanded using the container's environment.
spec.forProvider.eksProperties.podProperties.containers[].command <[]string>	# Entrypoint for the container. This isn't run within a shell. If this isn't specified, the ENTRYPOINT of the container image is used. Environment variable references are expanded using the container's environment.
spec.forProvider.eksProperties.podProperties.containers[].env <[]object>	# Environment variables to pass to a container. See EKS Environment below.
spec.forProvider.eksProperties.podProperties.containers[].env[].name <string>	# Name of the job definition.
spec.forProvider.eksProperties.podProperties.containers[].env[].value <string>	# Value of the environment variable.
spec.forProvider.eksProperties.podProperties.containers[].image <string>	# Docker image used to start the container.
spec.forProvider.eksProperties.podProperties.containers[].imagePullPolicy <string>	# Image pull policy for the container. Supported values are Always, IfNotPresent, and Never.
spec.forProvider.eksProperties.podProperties.containers[].name <string>	# Name of the container. If the name isn't specified, the default name "Default" is used. Each container in a pod must have a unique name.
spec.forProvider.eksProperties.podProperties.containers[].resources <object>	# Type and amount of resources to assign to a container. The supported resources include memory, cpu, and nvidia.com/gpu.
spec.forProvider.eksProperties.podProperties.containers[].resources.limits <map[string]string>
spec.forProvider.eksProperties.podProperties.containers[].resources.requests <map[string]string>
spec.forProvider.eksProperties.podProperties.containers[].securityContext <object>	# Security context for a job. See security_context below.
spec.forProvider.eksProperties.podProperties.containers[].securityContext.allowPrivilegeEscalation <boolean>	# Whether or not a container or a Kubernetes pod is allowed to gain more privileges than its parent process. The default value is false.
spec.forProvider.eksProperties.podProperties.containers[].securityContext.privileged <boolean>	# When this parameter is true, the container is given elevated permissions on the host container instance. The level of permissions are similar to the root user permissions. The default value is false.
spec.forProvider.eksProperties.podProperties.containers[].securityContext.readOnlyRootFileSystem <boolean>
spec.forProvider.eksProperties.podProperties.containers[].securityContext.runAsGroup <number>	# When this parameter is specified, the container is run as the specified group ID (gid). If this parameter isn't specified, the default is the group that's specified in the image metadata.
spec.forProvider.eksProperties.podProperties.containers[].securityContext.runAsNonRoot <boolean>	# When this parameter is specified, the container is run as a user with a uid other than 0. If this parameter isn't specified, so such rule is enforced.
spec.forProvider.eksProperties.podProperties.containers[].securityContext.runAsUser <number>	# When this parameter is specified, the container is run as the specified user ID (uid). If this parameter isn't specified, the default is the user that's specified in the image metadata.
spec.forProvider.eksProperties.podProperties.containers[].volumeMounts <[]object>	# Volume mounts for the container.
spec.forProvider.eksProperties.podProperties.containers[].volumeMounts[].mountPath <string>	# Path of the file or directory on the host to mount into containers on the pod.
spec.forProvider.eksProperties.podProperties.containers[].volumeMounts[].name <string>	# Name of the job definition.
spec.forProvider.eksProperties.podProperties.containers[].volumeMounts[].readOnly <boolean>
spec.forProvider.eksProperties.podProperties.dnsPolicy <string>	# DNS policy for the pod. The default value is ClusterFirst. If the host_network argument is not specified, the default is ClusterFirstWithHostNet. ClusterFirst indicates that any DNS query that does not match the configured cluster domain suffix is forwarded to the upstream nameserver inherited from the node. For more information, see Pod's DNS policy in the Kubernetes documentation.
spec.forProvider.eksProperties.podProperties.hostNetwork <boolean>	# Whether the pod uses the hosts' network IP address. The default value is true. Setting this to false enables the Kubernetes pod networking model. Most AWS Batch workloads are egress-only and don't require the overhead of IP allocation for each pod for incoming connections.
spec.forProvider.eksProperties.podProperties.imagePullSecret <[]object>	# List of Kubernetes secret resources. See image_pull_secret below.
spec.forProvider.eksProperties.podProperties.imagePullSecret[].name <string>	# Unique identifier.
spec.forProvider.eksProperties.podProperties.initContainers <[]object>	# Containers which run before application containers, always runs to completion, and must complete successfully before the next container starts. These containers are registered with the Amazon EKS Connector agent and persists the registration information in the Kubernetes backend data store. See containers below.
spec.forProvider.eksProperties.podProperties.initContainers[].args <[]string>	# Array of arguments to the entrypoint. If this isn't specified, the CMD of the container image is used. This corresponds to the args member in the Entrypoint portion of the Pod in Kubernetes. Environment variable references are expanded using the container's environment.
spec.forProvider.eksProperties.podProperties.initContainers[].command <[]string>	# Entrypoint for the container. This isn't run within a shell. If this isn't specified, the ENTRYPOINT of the container image is used. Environment variable references are expanded using the container's environment.
spec.forProvider.eksProperties.podProperties.initContainers[].env <[]object>	# Environment variables to pass to a container. See EKS Environment below.
spec.forProvider.eksProperties.podProperties.initContainers[].env[].name <string>	# Name of the job definition.
spec.forProvider.eksProperties.podProperties.initContainers[].env[].value <string>	# Value of the environment variable.
spec.forProvider.eksProperties.podProperties.initContainers[].image <string>	# Docker image used to start the container.
spec.forProvider.eksProperties.podProperties.initContainers[].imagePullPolicy <string>	# Image pull policy for the container. Supported values are Always, IfNotPresent, and Never.
spec.forProvider.eksProperties.podProperties.initContainers[].name <string>	# Name of the job definition.
spec.forProvider.eksProperties.podProperties.initContainers[].resources <object>	# Type and amount of resources to assign to a container. The supported resources include memory, cpu, and nvidia.com/gpu.
spec.forProvider.eksProperties.podProperties.initContainers[].resources.limits <map[string]string>
spec.forProvider.eksProperties.podProperties.initContainers[].resources.requests <map[string]string>
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext <object>	# Security context for a job. See security_context below.
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext.allowPrivilegeEscalation <boolean>	# Whether or not a container or a Kubernetes pod is allowed to gain more privileges than its parent process. The default value is false.
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext.privileged <boolean>	# When this parameter is true, the container is given elevated permissions on the host container instance. The level of permissions are similar to the root user permissions. The default value is false.
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext.readOnlyRootFileSystem <boolean>
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext.runAsGroup <number>	# When this parameter is specified, the container is run as the specified group ID (gid). If this parameter isn't specified, the default is the group that's specified in the image metadata.
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext.runAsNonRoot <boolean>	# When this parameter is specified, the container is run as a user with a uid other than 0. If this parameter isn't specified, so such rule is enforced.
spec.forProvider.eksProperties.podProperties.initContainers[].securityContext.runAsUser <number>	# When this parameter is specified, the container is run as the specified user ID (uid). If this parameter isn't specified, the default is the user that's specified in the image metadata.
spec.forProvider.eksProperties.podProperties.initContainers[].volumeMounts <[]object>	# Volume mounts for the container.
spec.forProvider.eksProperties.podProperties.initContainers[].volumeMounts[].mountPath <string>	# Path of the file or directory on the host to mount into containers on the pod.
spec.forProvider.eksProperties.podProperties.initContainers[].volumeMounts[].name <string>	# Name of the job definition.
spec.forProvider.eksProperties.podProperties.initContainers[].volumeMounts[].readOnly <boolean>
spec.forProvider.eksProperties.podProperties.metadata <object>	# Metadata about the Kubernetes pod.
spec.forProvider.eksProperties.podProperties.metadata.labels <map[string]string>	# Key-value pairs used to identify, sort, and organize kubernetes resources.
spec.forProvider.eksProperties.podProperties.serviceAccountName <string>	# Name of the service account that's used to run the pod.
spec.forProvider.eksProperties.podProperties.shareProcessNamespace <boolean>	# Indicates if the processes in a container are shared, or visible, to other containers in the same pod.
spec.forProvider.eksProperties.podProperties.volumes <[]object>	# Volumes for a job definition that uses Amazon EKS resources. AWS Batch supports emptyDir, hostPath, and secret volume types.
spec.forProvider.eksProperties.podProperties.volumes[].emptyDir <object>
spec.forProvider.eksProperties.podProperties.volumes[].emptyDir.medium <string>	# Medium to store the volume. The default value is an empty string, which uses the storage of the node.
spec.forProvider.eksProperties.podProperties.volumes[].emptyDir.sizeLimit <string>	# Maximum size of the volume. By default, there's no maximum size defined.
spec.forProvider.eksProperties.podProperties.volumes[].hostPath <object>	# Path of the file or directory on the host to mount into containers on the pod.
spec.forProvider.eksProperties.podProperties.volumes[].hostPath.path <string>	# Path of the file or directory on the host to mount into containers on the pod.
spec.forProvider.eksProperties.podProperties.volumes[].name <string>	# Name of the job definition.
spec.forProvider.eksProperties.podProperties.volumes[].secret <object>
spec.forProvider.eksProperties.podProperties.volumes[].secret.optional <boolean>	# Whether the secret or the secret's keys must be defined.
spec.forProvider.eksProperties.podProperties.volumes[].secret.secretName <string>	# Name of the secret. The name must be allowed as a DNS subdomain name.
spec.forProvider.name <string>	# Name of the job definition.
spec.forProvider.nodeProperties <string>	# Valid node properties provided as a single valid JSON document. This parameter is required if the type parameter is multinode.
spec.forProvider.parameters <map[string]string>	# Parameter substitution placeholders to set in the job definition.
spec.forProvider.platformCapabilities <[]string>	# Platform capabilities required by the job definition. If no value is specified, it defaults to EC2. To run the job on Fargate resources, specify FARGATE.
spec.forProvider.propagateTags <boolean>	# Whether to propagate the tags from the job definition to the corresponding Amazon ECS task. Default is false.
spec.forProvider.region <string> (required)	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
spec.forProvider.retryStrategy <object>	# Retry strategy to use for failed jobs that are submitted with this job definition. Maximum number of retry_strategy is 1. Defined below.
spec.forProvider.retryStrategy.attempts <number>	# Number of times to move a job to the RUNNABLE status. You may specify between 1 and 10 attempts.
spec.forProvider.retryStrategy.evaluateOnExit <[]object>	# Evaluate on exit conditions under which the job should be retried or failed. If this parameter is specified, then the attempts parameter must also be specified. You may specify up to 5 configuration blocks.
spec.forProvider.retryStrategy.evaluateOnExit[].action <string>	# Action to take if all of the specified conditions are met. The values are not case sensitive. Valid values: retry, exit.
spec.forProvider.retryStrategy.evaluateOnExit[].onExitCode <string>	# Glob pattern to match against the decimal representation of the exit code returned for a job.
spec.forProvider.retryStrategy.evaluateOnExit[].onReason <string>	# Glob pattern to match against the reason returned for a job.
spec.forProvider.retryStrategy.evaluateOnExit[].onStatusReason <string>	# Glob pattern to match against the status reason returned for a job.
spec.forProvider.schedulingPriority <number>	# Scheduling priority of the job definition. This only affects jobs in job queues with a fair share policy. Jobs with a higher scheduling priority are scheduled before jobs with a lower scheduling priority. Allowed values 0 through 9999.
spec.forProvider.tags <map[string]string>	# Key-value map of resource tags.
spec.forProvider.timeout <object>	# Timeout for jobs so that if a job runs longer, AWS Batch terminates the job. Maximum number of timeout is 1. Defined below.
spec.forProvider.timeout.attemptDurationSeconds <number>	# Time duration in seconds after which AWS Batch terminates your jobs if they have not finished. The minimum value for the timeout is 60 seconds.
spec.forProvider.type <string>	# Type of job definition. Must be container or multinode.
spec.initProvider <object>	# THIS IS A BETA FIELD. It will be honored unless the Management Policies feature flag is disabled. InitProvider holds the same fields as ForProvider, with the exception of Identifier and other resource reference fields. The fields that are in InitProvider are merged into ForProvider when the resource is created. The same fields are also added to the terraform ignore_changes hook, to avoid updating them after creation. This is useful for fields that are required on creation, but we do not desire to update them after creation, for example because of an external controller is managing them, like an autoscaler.
spec.initProvider.containerProperties <string>	# Valid container properties provided as a single valid JSON document. This parameter is only valid if the type parameter is container.
spec.initProvider.deregisterOnNewRevision <boolean>	# When updating a job definition a new revision is created. This parameter determines if the previous version is deregistered (INACTIVE) or left ACTIVE. Defaults to true.
spec.initProvider.ecsProperties <string>	# Valid ECS properties provided as a single valid JSON document. This parameter is only valid if the type parameter is container.
spec.initProvider.eksProperties <object>	# Valid eks properties. This parameter is only valid if the type parameter is container.
spec.initProvider.eksProperties.podProperties <object>	# Properties for the Kubernetes pod resources of a job. See pod_properties below.
spec.initProvider.eksProperties.podProperties.containers <[]object>	# Properties of the container that's used on the Amazon EKS pod. See containers below.
spec.initProvider.eksProperties.podProperties.containers[].args <[]string>	# Array of arguments to the entrypoint. If this isn't specified, the CMD of the container image is used. This corresponds to the args member in the Entrypoint portion of the Pod in Kubernetes. Environment variable references are expanded using the container's environment.
spec.initProvider.eksProperties.podProperties.containers[].command <[]string>	# Entrypoint for the container. This isn't run within a shell. If this isn't specified, the ENTRYPOINT of the container image is used. Environment variable references are expanded using the container's environment.
spec.initProvider.eksProperties.podProperties.containers[].env <[]object>	# Environment variables to pass to a container. See EKS Environment below.
spec.initProvider.eksProperties.podProperties.containers[].env[].name <string>	# Name of the job definition.
spec.initProvider.eksProperties.podProperties.containers[].env[].value <string>	# Value of the environment variable.
spec.initProvider.eksProperties.podProperties.containers[].image <string>	# Docker image used to start the container.
spec.initProvider.eksProperties.podProperties.containers[].imagePullPolicy <string>	# Image pull policy for the container. Supported values are Always, IfNotPresent, and Never.
spec.initProvider.eksProperties.podProperties.containers[].name <string>	# Name of the container. If the name isn't specified, the default name "Default" is used. Each container in a pod must have a unique name.
spec.initProvider.eksProperties.podProperties.containers[].resources <object>	# Type and amount of resources to assign to a container. The supported resources include memory, cpu, and nvidia.com/gpu.
spec.initProvider.eksProperties.podProperties.containers[].resources.limits <map[string]string>
spec.initProvider.eksProperties.podProperties.containers[].resources.requests <map[string]string>
spec.initProvider.eksProperties.podProperties.containers[].securityContext <object>	# Security context for a job. See security_context below.
spec.initProvider.eksProperties.podProperties.containers[].securityContext.allowPrivilegeEscalation <boolean>	# Whether or not a container or a Kubernetes pod is allowed to gain more privileges than its parent process. The default value is false.
spec.initProvider.eksProperties.podProperties.containers[].securityContext.privileged <boolean>	# When this parameter is true, the container is given elevated permissions on the host container instance. The level of permissions are similar to the root user permissions. The default value is false.
spec.initProvider.eksProperties.podProperties.containers[].securityContext.readOnlyRootFileSystem <boolean>
spec.initProvider.eksProperties.podProperties.containers[].securityContext.runAsGroup <number>	# When this parameter is specified, the container is run as the specified group ID (gid). If this parameter isn't specified, the default is the group that's specified in the image metadata.
spec.initProvider.eksProperties.podProperties.containers[].securityContext.runAsNonRoot <boolean>	# When this parameter is specified, the container is run as a user with a uid other than 0. If this parameter isn't specified, so such rule is enforced.
spec.initProvider.eksProperties.podProperties.containers[].securityContext.runAsUser <number>	# When this parameter is specified, the container is run as the specified user ID (uid). If this parameter isn't specified, the default is the user that's specified in the image metadata.
spec.initProvider.eksProperties.podProperties.containers[].volumeMounts <[]object>	# Volume mounts for the container.
spec.initProvider.eksProperties.podProperties.containers[].volumeMounts[].mountPath <string>	# Path of the file or directory on the host to mount into containers on the pod.
spec.initProvider.eksProperties.podProperties.containers[].volumeMounts[].name <string>	# Name of the job definition.
spec.initProvider.eksProperties.podProperties.containers[].volumeMounts[].readOnly <boolean>
spec.initProvider.eksProperties.podProperties.dnsPolicy <string>	# DNS policy for the pod. The default value is ClusterFirst. If the host_network argument is not specified, the default is ClusterFirstWithHostNet. ClusterFirst indicates that any DNS query that does not match the configured cluster domain suffix is forwarded to the upstream nameserver inherited from the node. For more information, see Pod's DNS policy in the Kubernetes documentation.
spec.initProvider.eksProperties.podProperties.hostNetwork <boolean>	# Whether the pod uses the hosts' network IP address. The default value is true. Setting this to false enables the Kubernetes pod networking model. Most AWS Batch workloads are egress-only and don't require the overhead of IP allocation for each pod for incoming connections.
spec.initProvider.eksProperties.podProperties.imagePullSecret <[]object>	# List of Kubernetes secret resources. See image_pull_secret below.
spec.initProvider.eksProperties.podProperties.imagePullSecret[].name <string>	# Unique identifier.
spec.initProvider.eksProperties.podProperties.initContainers <[]object>	# Containers which run before application containers, always runs to completion, and must complete successfully before the next container starts. These containers are registered with the Amazon EKS Connector agent and persists the registration information in the Kubernetes backend data store. See containers below.
spec.initProvider.eksProperties.podProperties.initContainers[].args <[]string>	# Array of arguments to the entrypoint. If this isn't specified, the CMD of the container image is used. This corresponds to the args member in the Entrypoint portion of the Pod in Kubernetes. Environment variable references are expanded using the container's environment.
spec.initProvider.eksProperties.podProperties.initContainers[].command <[]string>	# Entrypoint for the container. This isn't run within a shell. If this isn't specified, the ENTRYPOINT of the container image is used. Environment variable references are expanded using the container's environment.
spec.initProvider.eksProperties.podProperties.initContainers[].env <[]object>	# Environment variables to pass to a container. See EKS Environment below.
spec.initProvider.eksProperties.podProperties.initContainers[].env[].name <string>	# Name of the job definition.
spec.initProvider.eksProperties.podProperties.initContainers[].env[].value <string>	# Value of the environment variable.
spec.initProvider.eksProperties.podProperties.initContainers[].image <string>	# Docker image used to start the container.
spec.initProvider.eksProperties.podProperties.initContainers[].imagePullPolicy <string>	# Image pull policy for the container. Supported values are Always, IfNotPresent, and Never.
spec.initProvider.eksProperties.podProperties.initContainers[].name <string>	# Name of the job definition.
spec.initProvider.eksProperties.podProperties.initContainers[].resources <object>	# Type and amount of resources to assign to a container. The supported resources include memory, cpu, and nvidia.com/gpu.
spec.initProvider.eksProperties.podProperties.initContainers[].resources.limits <map[string]string>
spec.initProvider.eksProperties.podProperties.initContainers[].resources.requests <map[string]string>
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext <object>	# Security context for a job. See security_context below.
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext.allowPrivilegeEscalation <boolean>	# Whether or not a container or a Kubernetes pod is allowed to gain more privileges than its parent process. The default value is false.
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext.privileged <boolean>	# When this parameter is true, the container is given elevated permissions on the host container instance. The level of permissions are similar to the root user permissions. The default value is false.
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext.readOnlyRootFileSystem <boolean>
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext.runAsGroup <number>	# When this parameter is specified, the container is run as the specified group ID (gid). If this parameter isn't specified, the default is the group that's specified in the image metadata.
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext.runAsNonRoot <boolean>	# When this parameter is specified, the container is run as a user with a uid other than 0. If this parameter isn't specified, so such rule is enforced.
spec.initProvider.eksProperties.podProperties.initContainers[].securityContext.runAsUser <number>	# When this parameter is specified, the container is run as the specified user ID (uid). If this parameter isn't specified, the default is the user that's specified in the image metadata.
spec.initProvider.eksProperties.podProperties.initContainers[].volumeMounts <[]object>	# Volume mounts for the container.
spec.initProvider.eksProperties.podProperties.initContainers[].volumeMounts[].mountPath <string>	# Path of the file or directory on the host to mount into containers on the pod.
spec.initProvider.eksProperties.podProperties.initContainers[].volumeMounts[].name <string>	# Name of the job definition.
spec.initProvider.eksProperties.podProperties.initContainers[].volumeMounts[].readOnly <boolean>
spec.initProvider.eksProperties.podProperties.metadata <object>	# Metadata about the Kubernetes pod.
spec.initProvider.eksProperties.podProperties.metadata.labels <map[string]string>	# Key-value pairs used to identify, sort, and organize kubernetes resources.
spec.initProvider.eksProperties.podProperties.serviceAccountName <string>	# Name of the service account that's used to run the pod.
spec.initProvider.eksProperties.podProperties.shareProcessNamespace <boolean>	# Indicates if the processes in a container are shared, or visible, to other containers in the same pod.
spec.initProvider.eksProperties.podProperties.volumes <[]object>	# Volumes for a job definition that uses Amazon EKS resources. AWS Batch supports emptyDir, hostPath, and secret volume types.
spec.initProvider.eksProperties.podProperties.volumes[].emptyDir <object>
spec.initProvider.eksProperties.podProperties.volumes[].emptyDir.medium <string>	# Medium to store the volume. The default value is an empty string, which uses the storage of the node.
spec.initProvider.eksProperties.podProperties.volumes[].emptyDir.sizeLimit <string>	# Maximum size of the volume. By default, there's no maximum size defined.
spec.initProvider.eksProperties.podProperties.volumes[].hostPath <object>	# Path of the file or directory on the host to mount into containers on the pod.
spec.initProvider.eksProperties.podProperties.volumes[].hostPath.path <string>	# Path of the file or directory on the host to mount into containers on the pod.
spec.initProvider.eksProperties.podProperties.volumes[].name <string>	# Name of the job definition.
spec.initProvider.eksProperties.podProperties.volumes[].secret <object>
spec.initProvider.eksProperties.podProperties.volumes[].secret.optional <boolean>	# Whether the secret or the secret's keys must be defined.
spec.initProvider.eksProperties.podProperties.volumes[].secret.secretName <string>	# Name of the secret. The name must be allowed as a DNS subdomain name.
spec.initProvider.name <string>	# Name of the job definition.
spec.initProvider.nodeProperties <string>	# Valid node properties provided as a single valid JSON document. This parameter is required if the type parameter is multinode.
spec.initProvider.parameters <map[string]string>	# Parameter substitution placeholders to set in the job definition.
spec.initProvider.platformCapabilities <[]string>	# Platform capabilities required by the job definition. If no value is specified, it defaults to EC2. To run the job on Fargate resources, specify FARGATE.
spec.initProvider.propagateTags <boolean>	# Whether to propagate the tags from the job definition to the corresponding Amazon ECS task. Default is false.
spec.initProvider.retryStrategy <object>	# Retry strategy to use for failed jobs that are submitted with this job definition. Maximum number of retry_strategy is 1. Defined below.
spec.initProvider.retryStrategy.attempts <number>	# Number of times to move a job to the RUNNABLE status. You may specify between 1 and 10 attempts.
spec.initProvider.retryStrategy.evaluateOnExit <[]object>	# Evaluate on exit conditions under which the job should be retried or failed. If this parameter is specified, then the attempts parameter must also be specified. You may specify up to 5 configuration blocks.
spec.initProvider.retryStrategy.evaluateOnExit[].action <string>	# Action to take if all of the specified conditions are met. The values are not case sensitive. Valid values: retry, exit.
spec.initProvider.retryStrategy.evaluateOnExit[].onExitCode <string>	# Glob pattern to match against the decimal representation of the exit code returned for a job.
spec.initProvider.retryStrategy.evaluateOnExit[].onReason <string>	# Glob pattern to match against the reason returned for a job.
spec.initProvider.retryStrategy.evaluateOnExit[].onStatusReason <string>	# Glob pattern to match against the status reason returned for a job.
spec.initProvider.schedulingPriority <number>	# Scheduling priority of the job definition. This only affects jobs in job queues with a fair share policy. Jobs with a higher scheduling priority are scheduled before jobs with a lower scheduling priority. Allowed values 0 through 9999.
spec.initProvider.tags <map[string]string>	# Key-value map of resource tags.
spec.initProvider.timeout <object>	# Timeout for jobs so that if a job runs longer, AWS Batch terminates the job. Maximum number of timeout is 1. Defined below.
spec.initProvider.timeout.attemptDurationSeconds <number>	# Time duration in seconds after which AWS Batch terminates your jobs if they have not finished. The minimum value for the timeout is 60 seconds.
spec.initProvider.type <string>	# Type of job definition. Must be container or multinode.
spec.managementPolicies <[]string> default=["*"]	# THIS IS A BETA FIELD. It is on by default but can be opted out through a Crossplane feature flag. ManagementPolicies specify the array of actions Crossplane is allowed to take on the managed and external resources. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223 and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
spec.providerConfigRef <object> default={"kind":"ClusterProviderConfig","name":"default"}	# ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
spec.providerConfigRef.kind <string> (required)	# Kind of the referenced object.
spec.providerConfigRef.name <string> (required)	# Name of the referenced object.
spec.writeConnectionSecretToRef <object>	# WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
spec.writeConnectionSecretToRef.name <string> (required)	# Name of the secret.
status <object>	# JobDefinitionStatus defines the observed state of JobDefinition.
status.atProvider <object>
status.atProvider.arn <string>	# ARN of the job definition, includes revision (:#).
status.atProvider.arnPrefix <string>	# ARN without the revision number.
status.atProvider.containerProperties <string>	# Valid container properties provided as a single valid JSON document. This parameter is only valid if the type parameter is container.
status.atProvider.deregisterOnNewRevision <boolean>	# When updating a job definition a new revision is created. This parameter determines if the previous version is deregistered (INACTIVE) or left ACTIVE. Defaults to true.
status.atProvider.ecsProperties <string>	# Valid ECS properties provided as a single valid JSON document. This parameter is only valid if the type parameter is container.
status.atProvider.eksProperties <object>	# Valid eks properties. This parameter is only valid if the type parameter is container.
status.atProvider.eksProperties.podProperties <object>	# Properties for the Kubernetes pod resources of a job. See pod_properties below.
status.atProvider.eksProperties.podProperties.containers <[]object>	# Properties of the container that's used on the Amazon EKS pod. See containers below.
status.atProvider.eksProperties.podProperties.containers[].args <[]string>	# Array of arguments to the entrypoint. If this isn't specified, the CMD of the container image is used. This corresponds to the args member in the Entrypoint portion of the Pod in Kubernetes. Environment variable references are expanded using the container's environment.
status.atProvider.eksProperties.podProperties.containers[].command <[]string>	# Entrypoint for the container. This isn't run within a shell. If this isn't specified, the ENTRYPOINT of the container image is used. Environment variable references are expanded using the container's environment.
status.atProvider.eksProperties.podProperties.containers[].env <[]object>	# Environment variables to pass to a container. See EKS Environment below.
status.atProvider.eksProperties.podProperties.containers[].env[].name <string>	# Name of the job definition.
status.atProvider.eksProperties.podProperties.containers[].env[].value <string>	# Value of the environment variable.
status.atProvider.eksProperties.podProperties.containers[].image <string>	# Docker image used to start the container.
status.atProvider.eksProperties.podProperties.containers[].imagePullPolicy <string>	# Image pull policy for the container. Supported values are Always, IfNotPresent, and Never.
status.atProvider.eksProperties.podProperties.containers[].name <string>	# Name of the container. If the name isn't specified, the default name "Default" is used. Each container in a pod must have a unique name.
status.atProvider.eksProperties.podProperties.containers[].resources <object>	# Type and amount of resources to assign to a container. The supported resources include memory, cpu, and nvidia.com/gpu.
status.atProvider.eksProperties.podProperties.containers[].resources.limits <map[string]string>
status.atProvider.eksProperties.podProperties.containers[].resources.requests <map[string]string>
status.atProvider.eksProperties.podProperties.containers[].securityContext <object>	# Security context for a job. See security_context below.
status.atProvider.eksProperties.podProperties.containers[].securityContext.allowPrivilegeEscalation <boolean>	# Whether or not a container or a Kubernetes pod is allowed to gain more privileges than its parent process. The default value is false.
status.atProvider.eksProperties.podProperties.containers[].securityContext.privileged <boolean>	# When this parameter is true, the container is given elevated permissions on the host container instance. The level of permissions are similar to the root user permissions. The default value is false.
status.atProvider.eksProperties.podProperties.containers[].securityContext.readOnlyRootFileSystem <boolean>
status.atProvider.eksProperties.podProperties.containers[].securityContext.runAsGroup <number>	# When this parameter is specified, the container is run as the specified group ID (gid). If this parameter isn't specified, the default is the group that's specified in the image metadata.
status.atProvider.eksProperties.podProperties.containers[].securityContext.runAsNonRoot <boolean>	# When this parameter is specified, the container is run as a user with a uid other than 0. If this parameter isn't specified, so such rule is enforced.
status.atProvider.eksProperties.podProperties.containers[].securityContext.runAsUser <number>	# When this parameter is specified, the container is run as the specified user ID (uid). If this parameter isn't specified, the default is the user that's specified in the image metadata.
status.atProvider.eksProperties.podProperties.containers[].volumeMounts <[]object>	# Volume mounts for the container.
status.atProvider.eksProperties.podProperties.containers[].volumeMounts[].mountPath <string>	# Path of the file or directory on the host to mount into containers on the pod.
status.atProvider.eksProperties.podProperties.containers[].volumeMounts[].name <string>	# Name of the job definition.
status.atProvider.eksProperties.podProperties.containers[].volumeMounts[].readOnly <boolean>
status.atProvider.eksProperties.podProperties.dnsPolicy <string>	# DNS policy for the pod. The default value is ClusterFirst. If the host_network argument is not specified, the default is ClusterFirstWithHostNet. ClusterFirst indicates that any DNS query that does not match the configured cluster domain suffix is forwarded to the upstream nameserver inherited from the node. For more information, see Pod's DNS policy in the Kubernetes documentation.
status.atProvider.eksProperties.podProperties.hostNetwork <boolean>	# Whether the pod uses the hosts' network IP address. The default value is true. Setting this to false enables the Kubernetes pod networking model. Most AWS Batch workloads are egress-only and don't require the overhead of IP allocation for each pod for incoming connections.
status.atProvider.eksProperties.podProperties.imagePullSecret <[]object>	# List of Kubernetes secret resources. See image_pull_secret below.
status.atProvider.eksProperties.podProperties.imagePullSecret[].name <string>	# Unique identifier.
status.atProvider.eksProperties.podProperties.initContainers <[]object>	# Containers which run before application containers, always runs to completion, and must complete successfully before the next container starts. These containers are registered with the Amazon EKS Connector agent and persists the registration information in the Kubernetes backend data store. See containers below.
status.atProvider.eksProperties.podProperties.initContainers[].args <[]string>	# Array of arguments to the entrypoint. If this isn't specified, the CMD of the container image is used. This corresponds to the args member in the Entrypoint portion of the Pod in Kubernetes. Environment variable references are expanded using the container's environment.
status.atProvider.eksProperties.podProperties.initContainers[].command <[]string>	# Entrypoint for the container. This isn't run within a shell. If this isn't specified, the ENTRYPOINT of the container image is used. Environment variable references are expanded using the container's environment.
status.atProvider.eksProperties.podProperties.initContainers[].env <[]object>	# Environment variables to pass to a container. See EKS Environment below.
status.atProvider.eksProperties.podProperties.initContainers[].env[].name <string>	# Name of the job definition.
status.atProvider.eksProperties.podProperties.initContainers[].env[].value <string>	# Value of the environment variable.
status.atProvider.eksProperties.podProperties.initContainers[].image <string>	# Docker image used to start the container.
status.atProvider.eksProperties.podProperties.initContainers[].imagePullPolicy <string>	# Image pull policy for the container. Supported values are Always, IfNotPresent, and Never.
status.atProvider.eksProperties.podProperties.initContainers[].name <string>	# Name of the job definition.
status.atProvider.eksProperties.podProperties.initContainers[].resources <object>	# Type and amount of resources to assign to a container. The supported resources include memory, cpu, and nvidia.com/gpu.
status.atProvider.eksProperties.podProperties.initContainers[].resources.limits <map[string]string>
status.atProvider.eksProperties.podProperties.initContainers[].resources.requests <map[string]string>
status.atProvider.eksProperties.podProperties.initContainers[].securityContext <object>	# Security context for a job. See security_context below.
status.atProvider.eksProperties.podProperties.initContainers[].securityContext.allowPrivilegeEscalation <boolean>	# Whether or not a container or a Kubernetes pod is allowed to gain more privileges than its parent process. The default value is false.
status.atProvider.eksProperties.podProperties.initContainers[].securityContext.privileged <boolean>	# When this parameter is true, the container is given elevated permissions on the host container instance. The level of permissions are similar to the root user permissions. The default value is false.
status.atProvider.eksProperties.podProperties.initContainers[].securityContext.readOnlyRootFileSystem <boolean>
status.atProvider.eksProperties.podProperties.initContainers[].securityContext.runAsGroup <number>	# When this parameter is specified, the container is run as the specified group ID (gid). If this parameter isn't specified, the default is the group that's specified in the image metadata.
status.atProvider.eksProperties.podProperties.initContainers[].securityContext.runAsNonRoot <boolean>	# When this parameter is specified, the container is run as a user with a uid other than 0. If this parameter isn't specified, so such rule is enforced.
status.atProvider.eksProperties.podProperties.initContainers[].securityContext.runAsUser <number>	# When this parameter is specified, the container is run as the specified user ID (uid). If this parameter isn't specified, the default is the user that's specified in the image metadata.
status.atProvider.eksProperties.podProperties.initContainers[].volumeMounts <[]object>	# Volume mounts for the container.
status.atProvider.eksProperties.podProperties.initContainers[].volumeMounts[].mountPath <string>	# Path of the file or directory on the host to mount into containers on the pod.
status.atProvider.eksProperties.podProperties.initContainers[].volumeMounts[].name <string>	# Name of the job definition.
status.atProvider.eksProperties.podProperties.initContainers[].volumeMounts[].readOnly <boolean>
status.atProvider.eksProperties.podProperties.metadata <object>	# Metadata about the Kubernetes pod.
status.atProvider.eksProperties.podProperties.metadata.labels <map[string]string>	# Key-value pairs used to identify, sort, and organize kubernetes resources.
status.atProvider.eksProperties.podProperties.serviceAccountName <string>	# Name of the service account that's used to run the pod.
status.atProvider.eksProperties.podProperties.shareProcessNamespace <boolean>	# Indicates if the processes in a container are shared, or visible, to other containers in the same pod.
status.atProvider.eksProperties.podProperties.volumes <[]object>	# Volumes for a job definition that uses Amazon EKS resources. AWS Batch supports emptyDir, hostPath, and secret volume types.
status.atProvider.eksProperties.podProperties.volumes[].emptyDir <object>
status.atProvider.eksProperties.podProperties.volumes[].emptyDir.medium <string>	# Medium to store the volume. The default value is an empty string, which uses the storage of the node.
status.atProvider.eksProperties.podProperties.volumes[].emptyDir.sizeLimit <string>	# Maximum size of the volume. By default, there's no maximum size defined.
status.atProvider.eksProperties.podProperties.volumes[].hostPath <object>	# Path of the file or directory on the host to mount into containers on the pod.
status.atProvider.eksProperties.podProperties.volumes[].hostPath.path <string>	# Path of the file or directory on the host to mount into containers on the pod.
status.atProvider.eksProperties.podProperties.volumes[].name <string>	# Name of the job definition.
status.atProvider.eksProperties.podProperties.volumes[].secret <object>
status.atProvider.eksProperties.podProperties.volumes[].secret.optional <boolean>	# Whether the secret or the secret's keys must be defined.
status.atProvider.eksProperties.podProperties.volumes[].secret.secretName <string>	# Name of the secret. The name must be allowed as a DNS subdomain name.
status.atProvider.id <string>
status.atProvider.name <string>	# Name of the job definition.
status.atProvider.nodeProperties <string>	# Valid node properties provided as a single valid JSON document. This parameter is required if the type parameter is multinode.
status.atProvider.parameters <map[string]string>	# Parameter substitution placeholders to set in the job definition.
status.atProvider.platformCapabilities <[]string>	# Platform capabilities required by the job definition. If no value is specified, it defaults to EC2. To run the job on Fargate resources, specify FARGATE.
status.atProvider.propagateTags <boolean>	# Whether to propagate the tags from the job definition to the corresponding Amazon ECS task. Default is false.
status.atProvider.region <string>	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
status.atProvider.retryStrategy <object>	# Retry strategy to use for failed jobs that are submitted with this job definition. Maximum number of retry_strategy is 1. Defined below.
status.atProvider.retryStrategy.attempts <number>	# Number of times to move a job to the RUNNABLE status. You may specify between 1 and 10 attempts.
status.atProvider.retryStrategy.evaluateOnExit <[]object>	# Evaluate on exit conditions under which the job should be retried or failed. If this parameter is specified, then the attempts parameter must also be specified. You may specify up to 5 configuration blocks.
status.atProvider.retryStrategy.evaluateOnExit[].action <string>	# Action to take if all of the specified conditions are met. The values are not case sensitive. Valid values: retry, exit.
status.atProvider.retryStrategy.evaluateOnExit[].onExitCode <string>	# Glob pattern to match against the decimal representation of the exit code returned for a job.
status.atProvider.retryStrategy.evaluateOnExit[].onReason <string>	# Glob pattern to match against the reason returned for a job.
status.atProvider.retryStrategy.evaluateOnExit[].onStatusReason <string>	# Glob pattern to match against the status reason returned for a job.
status.atProvider.revision <number>	# Revision of the job definition.
status.atProvider.schedulingPriority <number>	# Scheduling priority of the job definition. This only affects jobs in job queues with a fair share policy. Jobs with a higher scheduling priority are scheduled before jobs with a lower scheduling priority. Allowed values 0 through 9999.
status.atProvider.tags <map[string]string>	# Key-value map of resource tags.
status.atProvider.tagsAll <map[string]string>	# Map of tags assigned to the resource, including those inherited from the provider default_tags configuration block.
status.atProvider.timeout <object>	# Timeout for jobs so that if a job runs longer, AWS Batch terminates the job. Maximum number of timeout is 1. Defined below.
status.atProvider.timeout.attemptDurationSeconds <number>	# Time duration in seconds after which AWS Batch terminates your jobs if they have not finished. The minimum value for the timeout is 60 seconds.
status.atProvider.type <string>	# Type of job definition. Must be container or multinode.
status.conditions <[]object>	# Conditions of the resource.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time this condition transitioned from one status to another.
status.conditions[].message <string>	# A Message containing details about this condition's last transition from one status to another, if any.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# A Reason for this condition's last transition from one status to another.
status.conditions[].status <string> (required)	# Status of this condition; is it currently True, False, or Unknown?
status.conditions[].type <string> (required)	# Type of this condition. At most one of each condition type may apply to a resource at any point in time.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the latest metadata.generation which resulted in either a ready state, or stalled due to error it can not recover from without human intervention.
