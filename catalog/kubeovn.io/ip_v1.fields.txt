# schema source: Kube-OVN v1.16.2 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=IP (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.attachIps <[]string>	# Additional IP addresses from attached subnets
spec.attachMacs <[]string>	# MAC addresses for attached IPs
spec.attachSubnets <[]string>	# Additional attached subnets
spec.containerID <string>	# Container ID
spec.ipAddress <string>	# IP address (deprecated, use v4IpAddress or v6IpAddress)
spec.macAddress <string>	# MAC address for the primary IP
spec.namespace <string>	# Namespace of the pod
spec.nodeName <string>	# Node name where the pod resides
spec.podName <string>	# Pod name that this IP belongs to
spec.podType <string>	# Pod type (e.g., pod, vm)
spec.subnet <string>	# Primary subnet name for the IP. This field is immutable after creation.
spec.v4IpAddress <string>	# IPv4 address
spec.v6IpAddress <string>	# IPv6 address
