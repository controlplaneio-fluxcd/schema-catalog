# schema source: Kube-OVN v1.14.42 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=VpcEgressGateway
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.bfd <object>	# BFD (Bidirectional Forwarding Detection) configuration
spec.bfd.enabled <boolean> default=false
spec.bfd.minRX <integer> default=1000 min=1 max=3600000
spec.bfd.minTX <integer> default=1000 min=1 max=3600000
spec.bfd.multiplier <integer> default=3 min=1 max=3600000
spec.externalIPs <[]string>	# External IP addresses for the egress gateway
spec.externalSubnet <string> (required)	# External subnet name for the egress gateway. This field is immutable after creation and is required.
spec.image <string>	# Container image for the egress gateway
spec.internalIPs <[]string>	# Internal IP addresses for the egress gateway
spec.internalSubnet <string>	# Internal subnet name for the egress gateway. This field is immutable after creation.
spec.nodeSelector <[]object>	# Node selector for egress gateway placement
spec.nodeSelector[].matchExpressions <[]object>
spec.nodeSelector[].matchExpressions[].key <string> (required)
spec.nodeSelector[].matchExpressions[].operator <string> (required) enum=In|NotIn|Exists|DoesNotExist|Gt|Lt
spec.nodeSelector[].matchExpressions[].values <[]string>
spec.nodeSelector[].matchFields <[]object>
spec.nodeSelector[].matchFields[].key <string> (required)
spec.nodeSelector[].matchFields[].operator <string> (required) enum=In|NotIn|Exists|DoesNotExist|Gt|Lt
spec.nodeSelector[].matchFields[].values <[]string>
spec.nodeSelector[].matchLabels <map[string]string>
spec.policies <[]object>	# Egress policies for the gateway
spec.policies[].ipBlocks <[]string>
spec.policies[].snat <boolean> default=false
spec.policies[].subnets <[]string>
spec.prefix <string> (immutable)	# Name prefix for egress gateway pods. This field is immutable after creation.
spec.replicas <integer> default=1 min=0 max=10	# Number of egress gateway replicas
spec.selectors <[]object>	# Selectors for pods to use this egress gateway
spec.selectors[].namespaceSelector <object>
spec.selectors[].namespaceSelector.matchExpressions <[]object>
spec.selectors[].namespaceSelector.matchExpressions[].key <string> (required)
spec.selectors[].namespaceSelector.matchExpressions[].operator <string> (required)
spec.selectors[].namespaceSelector.matchExpressions[].values <[]string>
spec.selectors[].namespaceSelector.matchLabels <map[string]string>
spec.selectors[].podSelector <object>
spec.selectors[].podSelector.matchExpressions <[]object>
spec.selectors[].podSelector.matchExpressions[].key <string> (required)
spec.selectors[].podSelector.matchExpressions[].operator <string> (required)
spec.selectors[].podSelector.matchExpressions[].values <[]string>
spec.selectors[].podSelector.matchLabels <map[string]string>
spec.tolerations <[]object>	# optional tolerations applied to the workload pods
spec.tolerations[].effect <string> enum=NoSchedule|PreferNoSchedule|NoExecute	# Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
spec.tolerations[].key <string>	# Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.
spec.tolerations[].operator <string> enum=Exists|Equal	# Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.
spec.tolerations[].tolerationSeconds <integer> format=int64	# TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.
spec.tolerations[].value <string>	# Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.
spec.trafficPolicy <string> enum=Local|Cluster default="Cluster"	# Traffic policy for the egress gateway (Local or Cluster)
spec.vpc <string>	# VPC name for the egress gateway. This field is immutable after creation.
status <object>
status.conditions <[]object> (required)	# Conditions represent the latest available observations of the egress gateway's current state
status.conditions[].lastTransitionTime <string> (required) format=date-time
status.conditions[].lastUpdateTime <string> (required) format=date-time
status.conditions[].message <string> max=32768
status.conditions[].observedGeneration <integer> (required) min=0
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024
status.conditions[].status <string> (required) enum=True|False|Unknown
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316
status.externalIPs <[]string>	# External IP addresses assigned to the egress gateway
status.internalIPs <[]string>	# Internal IP addresses assigned to the egress gateway
status.labelSelector <string>	# Label selector for the egress gateway
status.phase <string> (required) enum=Pending|Processing|Completed default="Pending"	# Current phase of the egress gateway (Pending, Processing, or Completed)
status.ready <boolean> default=false	# Indicates whether the egress gateway is ready
status.replicas <integer> min=0 max=10	# Number of egress gateway replicas
status.workload <object>	# Workload information for the egress gateway
status.workload.apiVersion <string>
status.workload.kind <string>
status.workload.name <string>
status.workload.nodes <[]string>
