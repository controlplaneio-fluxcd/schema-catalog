# schema source: Kube-OVN v1.16.2 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=Vpc (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.bfdPort <object>	# optional BFD LRP configuration currently the LRP is used for vpc external gateway only
spec.bfdPort.enabled <boolean> default=false	# Enable BFD port
spec.bfdPort.ip <string>	# ip address(es) of the BFD port
spec.bfdPort.nodeSelector <object>	# Optional node selector used to select the nodes where the BFD LRP will be hosted. If not specified, at most 3 nodes will be selected.
spec.bfdPort.nodeSelector.matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.bfdPort.nodeSelector.matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.bfdPort.nodeSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.bfdPort.nodeSelector.matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.bfdPort.nodeSelector.matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.defaultSubnet <string>	# The default subnet name for the VPC
spec.enableBfd <boolean>	# Enable BFD (Bidirectional Forwarding Detection) for the VPC
spec.enableExternal <boolean>	# Enable external network access for the VPC
spec.extraExternalSubnets <[]string>	# Extra external subnets for provider-network VLAN. Immutable after creation.
spec.namespaces <[]string>	# List of namespaces that can use this VPC
spec.policyRoutes <[]object>	# Policy routes for the VPC.
spec.policyRoutes[].action <string>	# Action of the policy route
spec.policyRoutes[].match <string>	# Match of the policy route
spec.policyRoutes[].nextHopIP <string>	# NextHopIP is an optional parameter. It needs to be provided only when 'action' is 'reroute'.
spec.policyRoutes[].priority <integer>	# Priority of the policy route (0-32767)
spec.staticRoutes <[]object>	# Static routes for the VPC.
spec.staticRoutes[].bfdId <string>
spec.staticRoutes[].cidr <string>
spec.staticRoutes[].ecmpMode <string>
spec.staticRoutes[].nextHopIP <string>
spec.staticRoutes[].policy <string>
spec.staticRoutes[].routeTable <string>
spec.vpcPeerings <[]object>	# VPC peering configurations.
spec.vpcPeerings[].localConnectIP <string>
spec.vpcPeerings[].remoteVpc <string>
status <object>
status.bfdPort <object>
status.bfdPort.ip <string>	# BFD port IP address
status.bfdPort.name <string>	# BFD port name
status.bfdPort.nodes <[]string>	# Nodes where BFD port is deployed
status.conditions <[]object>	# Conditions represents the latest state of the object
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string> format=date-time	# Last time the condition was probed
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string>	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string>	# Type of condition.
status.default <boolean>	# Whether this is the default VPC.
status.defaultLogicalSwitch <string>
status.enableBfd <boolean>
status.enableExternal <boolean>
status.extraExternalSubnets <[]string>	# Extra external subnets for provider-network VLAN. Immutable after creation.
status.router <string>
status.sctpLoadBalancer <string>
status.sctpSessionLoadBalancer <string>
status.standby <boolean>
status.subnets <[]string>
status.tcpLoadBalancer <string>
status.tcpSessionLoadBalancer <string>
status.udpLoadBalancer <string>
status.udpSessionLoadBalancer <string>
status.vpcPeerings <[]string>	# VPC peering configurations.
