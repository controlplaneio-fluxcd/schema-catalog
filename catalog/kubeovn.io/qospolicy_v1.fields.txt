# schema source: Kube-OVN v1.16.2 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=QoSPolicy (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.bandwidthLimitRules <[]object>	# Bandwidth limit rules to apply
spec.bandwidthLimitRules[].burstMax <string>	# Maximum burst in MB (e.g., 10 or 0.5 for 500KB)
spec.bandwidthLimitRules[].direction <string>	# Traffic direction (ingress/egress)
spec.bandwidthLimitRules[].interface <string>	# Interface name
spec.bandwidthLimitRules[].matchType <string>	# Match type
spec.bandwidthLimitRules[].matchValue <string>	# Match value
spec.bandwidthLimitRules[].name <string> (required)	# Rule name
spec.bandwidthLimitRules[].priority <integer>	# Rule priority
spec.bandwidthLimitRules[].rateMax <string>	# Maximum rate in Mbps (e.g., 100 or 0.5 for 500Kbps)
spec.bindingType <string>	# Binding type (e.g., pod, namespace)
spec.shared <boolean>	# Whether the QoS policy is shared across multiple pods
status <object>
status.bandwidthLimitRules <[]object>	# Active bandwidth limit rules
status.bandwidthLimitRules[].burstMax <string>	# Maximum burst in MB (e.g., 10 or 0.5 for 500KB)
status.bandwidthLimitRules[].direction <string>	# Traffic direction (ingress/egress)
status.bandwidthLimitRules[].interface <string>	# Interface name
status.bandwidthLimitRules[].matchType <string>	# Match type
status.bandwidthLimitRules[].matchValue <string>	# Match value
status.bandwidthLimitRules[].name <string> (required)	# Rule name
status.bandwidthLimitRules[].priority <integer>	# Rule priority
status.bandwidthLimitRules[].rateMax <string>	# Maximum rate in Mbps (e.g., 100 or 0.5 for 500Kbps)
status.bindingType <string>	# Binding type of the QoS policy
status.conditions <[]object>	# Conditions represents the latest state of the object
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string> format=date-time	# Last time the condition was probed
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string>	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string>	# Type of condition.
status.shared <boolean>	# Whether the QoS policy is shared
