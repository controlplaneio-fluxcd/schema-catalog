# schema source: Kube-OVN v1.14.42 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=SwitchLBRule (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.endpoints <[]string>
spec.namespace <string>
spec.ports <[]object>
spec.ports[].name <string>	# Port name
spec.ports[].port <integer> min=1 max=65535
spec.ports[].protocol <string>	# Protocol (TCP or UDP)
spec.ports[].targetPort <integer> min=1 max=65535
spec.selector <[]string>
spec.sessionAffinity <string>
spec.vip <string>
status <object>
status.ports <string>	# Configured ports
status.service <string>	# Associated service name
