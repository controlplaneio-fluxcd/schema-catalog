# schema source: Kube-OVN v1.16.2 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=Subnet (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.acls <[]object>	# ACL rules for the subnet.
spec.acls[].action <string>
spec.acls[].direction <string>
spec.acls[].match <string>
spec.acls[].priority <integer>
spec.allowEWTraffic <boolean>	# Allow east-west traffic across subnets.
spec.allowSubnets <[]string>	# Allowed subnets for east-west traffic.
spec.cidrBlock <string>	# CIDR block for the subnet. Immutable after creation.
spec.default <boolean>	# Whether this is the default subnet.
spec.dhcpV4Options <string>	# DHCPv4 options UUID.
spec.dhcpV6Options <string>	# DHCPv6 options UUID.
spec.disableGatewayCheck <boolean>	# Disable gateway readiness check.
spec.disableInterConnection <boolean>	# Disable interconnection for the subnet.
spec.enableDHCP <boolean>	# Enable DHCP for the subnet.
spec.enableEcmp <boolean>	# Enable ECMP for centralized gateway.
spec.enableExternalLBAddress <boolean>	# Enable external LB address support.
spec.enableIPv6RA <boolean>	# Enable IPv6 Router Advertisement.
spec.enableLb <boolean>	# Enable LoadBalancer on this subnet.
spec.enableMulticastSnoop <boolean>	# Enable multicast snoop.
spec.excludeIps <[]string>	# IP addresses to exclude from allocation.
spec.externalEgressGateway <string>	# External egress gateway IPs.
spec.gateway <string>	# Gateway IP address for the subnet.
spec.gatewayNode <string>	# Gateway node(s) for centralized gateway type.
spec.gatewayNodeSelectors <[]object>	# Selectors to choose gateway nodes.
spec.gatewayNodeSelectors[].matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.gatewayNodeSelectors[].matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.gatewayNodeSelectors[].matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.gatewayNodeSelectors[].matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.gatewayNodeSelectors[].matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.gatewayType <string>	# Gateway type (distributed or centralized).
spec.ipv6RAConfigs <string>	# IPv6 RA configuration options.
spec.logicalGateway <boolean>	# Enable logical gateway.
spec.mtu <integer> format=int32	# MTU for pods in this subnet.
spec.namespaceSelectors <[]object>	# Namespace label selectors to associate with this subnet.
spec.namespaceSelectors[].matchExpressions <[]object>	# matchExpressions is a list of label selector requirements. The requirements are ANDed.
spec.namespaceSelectors[].matchExpressions[].key <string> (required)	# key is the label key that the selector applies to.
spec.namespaceSelectors[].matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
spec.namespaceSelectors[].matchExpressions[].values <[]string>	# values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
spec.namespaceSelectors[].matchLabels <map[string]string>	# matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
spec.namespaces <[]string>	# List of namespaces associated with this subnet.
spec.natOutgoing <boolean>	# Enable NAT outgoing for the subnet.
spec.natOutgoingPolicyRules <[]object>	# NAT outgoing policy rules.
spec.natOutgoingPolicyRules[].action <string>
spec.natOutgoingPolicyRules[].match <object>
spec.natOutgoingPolicyRules[].match.dstIPs <string>
spec.natOutgoingPolicyRules[].match.srcIPs <string>
spec.nodeNetwork <string>	# Node network name for underlay.
spec.policyRoutingPriority <integer> format=int32	# Policy routing priority.
spec.policyRoutingTableID <integer> format=int32	# Policy routing table ID.
spec.private <boolean>	# Whether the subnet is private.
spec.protocol <string>	# Network protocol (IPv4, IPv6, or Dual). Immutable after creation.
spec.provider <string>	# Provider network name.
spec.routeTable <string>	# Route table associated with the subnet.
spec.u2oFeatures <object>
spec.u2oFeatures.overlayOnlyRouting <boolean>
spec.u2oInterconnection <boolean>	# Enable underlay to overlay interconnection.
spec.u2oInterconnectionIP <string>	# Underlay to overlay interconnection IP.
spec.vips <[]string>	# Virtual IP addresses for the subnet.
spec.vlan <string>	# VLAN ID or provider network name.
spec.vpc <string>	# VPC name for the subnet. Immutable after creation.
status <object>
status.activateGateway <string>
status.conditions <[]object>	# Conditions represents the latest state of the object
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string> format=date-time	# Last time the condition was probed
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string>	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string>	# Type of condition.
status.dhcpV4OptionsUUID <string>
status.dhcpV6OptionsUUID <string>
status.mcastQuerierIP <string>
status.mcastQuerierMAC <string>
status.natOutgoingPolicyRules <[]object>	# NAT outgoing policy rules.
status.natOutgoingPolicyRules[].action <string>
status.natOutgoingPolicyRules[].match <object>
status.natOutgoingPolicyRules[].match.dstIPs <string>
status.natOutgoingPolicyRules[].match.srcIPs <string>
status.natOutgoingPolicyRules[].ruleID <string>
status.u2oInterconnectionIP <string>	# Underlay to overlay interconnection IP.
status.u2oInterconnectionMAC <string>
status.u2oInterconnectionVPC <string>
status.v4availableIPrange <string>
status.v4availableIPs <number>
status.v4usingIPrange <string>
status.v4usingIPs <number>
status.v6availableIPrange <string>
status.v6availableIPs <number>
status.v6usingIPrange <string>
status.v6usingIPs <number>
