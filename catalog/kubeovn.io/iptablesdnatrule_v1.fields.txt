# schema source: Kube-OVN v1.14.42 https://github.com/kubeovn/kube-ovn
apiVersion <string> enum=kubeovn.io/v1
kind <string> enum=IptablesDnatRule (cluster-scoped)
metadata.name <string> (required)
spec <object>
spec.eip <string>	# EIP name for DNAT rule
spec.externalPort <string>	# External port number
spec.internalIp <string>	# Internal IP address to forward traffic to
spec.internalPort <string>	# Internal port number to forward traffic to
spec.protocol <string>	# Protocol type (TCP or UDP)
status <object>
status.conditions <[]object>	# Conditions represent the latest available observations of the DNAT rule's current state
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another
status.conditions[].lastUpdateTime <string>	# Last time this condition was updated
status.conditions[].message <string>	# Human-readable message indicating details about the condition
status.conditions[].reason <string>	# Reason for the condition's last transition
status.conditions[].status <string>	# Status of the condition (True, False, Unknown)
status.conditions[].type <string>	# Type of condition
status.externalPort <string>	# External port configured in the DNAT rule
status.internalIp <string>	# Internal IP address configured in the DNAT rule
status.internalPort <string>	# Internal port configured in the DNAT rule
status.natGwDp <string>	# NAT gateway datapath where the DNAT rule is configured
status.protocol <string>	# Protocol type of the DNAT rule
status.ready <boolean>	# Indicates whether the DNAT rule is ready
status.redo <string>	# Redo operation status
status.v4ip <string>	# IPv4 address of the EIP
status.v6ip <string>	# IPv6 address of the EIP
