# schema source: Secrets Store CSI Driver v1.6.0 https://github.com/kubernetes-sigs/secrets-store-csi-driver
apiVersion <string> enum=secrets-store.csi.x-k8s.io/v1
kind <string> enum=SecretProviderClass
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# SecretProviderClassSpec defines the desired state of SecretProviderClass
spec.parameters <map[string]string>	# Configuration for specific provider
spec.provider <string>	# Configuration for provider name
spec.secretObjects <[]object>
spec.secretObjects[].annotations <map[string]string>	# annotations of k8s secret object
spec.secretObjects[].data <[]object>
spec.secretObjects[].data[].key <string>	# data field to populate
spec.secretObjects[].data[].objectName <string>	# name of the object to sync
spec.secretObjects[].labels <map[string]string>	# labels of K8s secret object
spec.secretObjects[].secretName <string>	# name of the K8s secret object
spec.secretObjects[].type <string>	# type of K8s secret object
status <object (free-form)>	# SecretProviderClassStatus defines the observed state of SecretProviderClass
