# schema source: Cluster API Metal3 v1.13.2 https://github.com/metal3-io/cluster-api-provider-metal3
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta2
kind <string> enum=Metal3Machine
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# spec defines the desired state of Metal3Machine.
spec.automatedCleaningMode <string> enum=metadata|disabled	# automatedCleaningMode determines the mode of automated cleaning. When set to disabled, automated cleaning of host disks will be skipped during provisioning and deprovisioning.
spec.customDeploy <object>	# customDeploy is a custom deploy procedure. Either image or customDeploy is required.
spec.customDeploy.method <string> (required) min=1 max=512	# method is the name of the deploy method. This name is specific to the deploy ramdisk used. If you don't have a custom deploy ramdisk, you shouldn't use CustomDeploy.
spec.dataTemplate <object>	# dataTemplate is a reference to a Metal3DataTemplate object containing a template of metadata to be rendered. Metadata keys defined in the metadataTemplate take precedence over keys defined in metadata field.
spec.dataTemplate.name <string> min=1 max=253	# name of the resource.
spec.dataTemplate.namespace <string> min=1 max=253	# namespace of the resource.
spec.failureDomain <string> min=1 max=256	# failureDomain is the failure domain unique identifier this machine should be attached to, as defined in Cluster API.
spec.hostSelector <object>	# hostSelector specifies matching criteria for labels on BareMetalHosts. This is used to limit the set of BareMetalHost objects considered for claiming for a metal3machine.
spec.hostSelector.matchExpressions <[]object> min=1 max=512	# matchExpressions specifies match expressions that must be true on a chosen BareMetalHost
spec.hostSelector.matchExpressions[].key <string> (required) min=1 max=512	# key is the label key that the selector applies to.
spec.hostSelector.matchExpressions[].operator <string> (required)	# operator represents a key's relationship to a set of values.
spec.hostSelector.matchExpressions[].values <[]string> (required) max=512	# values is an array of string of required values.
spec.hostSelector.matchLabels <map[string]string>	# matchLabels specifies key/value pairs of labels that must exist on a chosen BareMetalHost
spec.image <object>	# image is the image to be provisioned. Either image or customDeploy is required.
spec.image.checksum <string> (required) min=0 max=512	# checksum is a md5sum, sha256sum or sha512sum value or a URL to retrieve one. When diskFormat is live-iso, empty string is also a valid value.
spec.image.checksumType <string> enum=md5|sha256|sha512	# checksumType is the checksum algorithm for the image. e.g md5, sha256, sha512
spec.image.diskFormat <string> enum=raw|qcow2|vdi|vmdk|live-iso	# diskFormat contains the image disk format.
spec.image.url <string> (required) format=uri min=1 max=512	# url is a location of an image to deploy.
spec.metaData <object>	# metaData is an object storing the reference to the secret containing the metadata given by the user.
spec.metaData.name <string>	# name is unique within a namespace to reference a secret resource.
spec.metaData.namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.networkData <object>	# networkData is an object storing the reference to the secret containing the network data given by the user.
spec.networkData.name <string>	# name is unique within a namespace to reference a secret resource.
spec.networkData.namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.providerID <string> min=1 max=512	# providerID must match the provider ID as seen on the node object corresponding to this machine. For Kubernetes Nodes running on the Metal3 provider, this value is set by the corresponding CPI component and it has the format metal3://<namespace>/<bmh-name>/<m3m-name>. The legacy format (metal3://<bmh-uuid>) will be deprecated in CAPM3 v1.13 and removed in CAPM3 v1.14.
spec.userData <object>	# userData references the Secret that holds user data needed by the bare metal operator. The Namespace is optional; it will default to the metal3machine's namespace if not specified.
spec.userData.name <string>	# name is unique within a namespace to reference a secret resource.
spec.userData.namespace <string>	# namespace defines the space within which the secret name must be unique.
status <object>	# status defines the observed state of Metal3Machine.
status.addresses <[]object> min=1 max=32	# addresses contains the associated addresses for the machine.
status.addresses[].address <string> (required) min=1 max=256	# address is the machine address.
status.addresses[].type <string> (required) enum=Hostname|ExternalIP|InternalIP|ExternalDNS|InternalDNS	# type is the machine address type, one of Hostname, ExternalIP, InternalIP, ExternalDNS or InternalDNS.
status.conditions <[]object> max=32	# conditions represents the observations of a Metal3Machine's current state. Known condition types are Ready, AssociateBareMetalHost, AssociateMetal3MachineMetaData, Metal3DataReady and Paused.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.deprecated <object>	# deprecated groups all the status fields that are deprecated and will be removed when all the nested field are removed.
status.deprecated.v1beta1 <object>	# v1beta1 groups all the status fields that are deprecated and will be removed when support for v1beta1 will be dropped. Deprecated: This field is deprecated and is going to be removed when support for v1beta1 will be dropped. Please see https://github.com/kubernetes-sigs/cluster-api/blob/main/docs/proposals/20240916-improve-status-in-CAPI-resources.md for more details.
status.deprecated.v1beta1.conditions <[]object>	# conditions defines current service state of the Metal3Machine. Deprecated: This field is deprecated and is going to be removed when support for v1beta1 will be dropped. Please see https://github.com/kubernetes-sigs/cluster-api/blob/main/docs/proposals/20240916-improve-status-in-CAPI-resources.md for more details.
status.deprecated.v1beta1.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.deprecated.v1beta1.conditions[].message <string> min=1 max=10240	# message is a human readable message indicating details about the transition. This field may be empty.
status.deprecated.v1beta1.conditions[].reason <string> min=1 max=256	# reason is the reason for the condition's last transition in CamelCase. The specific API may choose whether or not this field is considered a guaranteed API. This field may be empty.
status.deprecated.v1beta1.conditions[].severity <string> max=32	# severity provides an explicit classification of Reason code, so the users or machines can immediately understand the current situation and act accordingly. The Severity field MUST be set only when Status=False.
status.deprecated.v1beta1.conditions[].status <string> (required)	# status of the condition, one of True, False, Unknown.
status.deprecated.v1beta1.conditions[].type <string> (required) min=1 max=256	# type of condition in CamelCase or in foo.example.com/CamelCase. Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important.
status.deprecated.v1beta1.failureMessage <string>	# failureMessage will be set in the event that there is a terminal problem reconciling the metal3machine and will contain a more verbose string suitable for logging and human consumption. This field should not be set for transitive errors that a controller faces that are expected to be fixed automatically over time (like service outages), but instead indicate that something is fundamentally wrong with the metal3machine's spec or the configuration of the controller, and that manual intervention is required. Examples of terminal errors would be invalid combinations of settings in the spec, values that are unsupported by the controller, or the responsible controller itself being critically misconfigured. Any transient errors that occur during the reconciliation of metal3machines can be added as events to the metal3machine object and/or logged in the controller's output. Deprecated: This field is deprecated and is going to be removed when support for v1beta1 will be dropped. Please see https://github.com/kubernetes-sigs/cluster-api/blob/main/docs/proposals/20240916-improve-status-in-CAPI-resources.md for more details.
status.deprecated.v1beta1.failureReason <string>	# failureReason will be set in the event that there is a terminal problem reconciling the metal3machine and will contain a succinct value suitable for machine interpretation. This field should not be set for transitive errors that a controller faces that are expected to be fixed automatically over time (like service outages), but instead indicate that something is fundamentally wrong with the metal3machine's spec or the configuration of the controller, and that manual intervention is required. Examples of terminal errors would be invalid combinations of settings in the spec, values that are unsupported by the controller, or the responsible controller itself being critically misconfigured. Any transient errors that occur during the reconciliation of metal3machines can be added as events to the metal3machine object and/or logged in the controller's output. Deprecated: This field is deprecated and is going to be removed when support for v1beta1 will be dropped. Please see https://github.com/kubernetes-sigs/cluster-api/blob/main/docs/proposals/20240916-improve-status-in-CAPI-resources.md for more details.
status.initialization <object>	# initialization provides observations of the Metal3Machine initialization process. NOTE: Fields in this struct are part of the Cluster API contract and are used to orchestrate initial Machine provisioning.
status.initialization.provisioned <boolean>	# provisioned is true when the infrastructure provider reports that the Machine's infrastructure is fully provisioned. NOTE: this field is part of the Cluster API contract, and it is used to orchestrate initial Machine provisioning.
status.lastUpdated <string> format=date-time	# lastUpdated identifies when this status was last observed.
status.metaData <object>	# metaData is an object storing the reference to the secret containing the metadata used to deploy the BareMetalHost.
status.metaData.name <string>	# name is unique within a namespace to reference a secret resource.
status.metaData.namespace <string>	# namespace defines the space within which the secret name must be unique.
status.networkData <object>	# networkData is an object storing the reference to the secret containing the network data used to deploy the BareMetalHost.
status.networkData.name <string>	# name is unique within a namespace to reference a secret resource.
status.networkData.namespace <string>	# namespace defines the space within which the secret name must be unique.
status.renderedData <object>	# renderedData is a reference to a rendered Metal3Data object containing the references to metaData and networkData secrets.
status.renderedData.name <string> min=1 max=253	# name of the resource.
status.renderedData.namespace <string> min=1 max=253	# namespace of the resource.
status.userData <object>	# userData references the Secret that holds user data needed by the bare metal operator. The Namespace is optional; it will default to the metal3machine's namespace if not specified.
status.userData.name <string>	# name is unique within a namespace to reference a secret resource.
status.userData.namespace <string>	# namespace defines the space within which the secret name must be unique.
