# schema source: Cluster API OpenStack v0.14.6 https://github.com/kubernetes-sigs/cluster-api-provider-openstack
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta1
kind <string> enum=OpenStackMachineTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# OpenStackMachineTemplateSpec defines the desired state of OpenStackMachineTemplate.
spec.template <object> (required)	# OpenStackMachineTemplateResource describes the data needed to create a OpenStackMachine from a template.
spec.template.spec <object> (required)	# Spec is the specification of the desired behavior of the machine.
spec.template.spec.additionalBlockDevices <[]object>	# AdditionalBlockDevices is a list of specifications for additional block devices to attach to the server instance
spec.template.spec.additionalBlockDevices[].name <string> (required)	# Name of the block device in the context of a machine. If the block device is a volume, the Cinder volume will be named as a combination of the machine name and this name. Also, this name will be used for tagging the block device. Information about the block device tag can be obtained from the OpenStack metadata API or the config drive. Name cannot be 'root', which is reserved for the root volume.
spec.template.spec.additionalBlockDevices[].sizeGiB <integer> (required) min=1	# SizeGiB is the size of the block device in gibibytes (GiB).
spec.template.spec.additionalBlockDevices[].storage <object> (required)	# Storage specifies the storage type of the block device and additional storage options.
spec.template.spec.additionalBlockDevices[].storage.type <string> (required)	# Type is the type of block device to create. This can be either "Volume" or "Local".
spec.template.spec.additionalBlockDevices[].storage.volume <object>	# Volume contains additional storage options for a volume block device.
spec.template.spec.additionalBlockDevices[].storage.volume.availabilityZone <object>	# AvailabilityZone is the volume availability zone to create the volume in. If not specified, the volume will be created without an explicit availability zone.
spec.template.spec.additionalBlockDevices[].storage.volume.availabilityZone.from <string> enum=Name|Machine default="Name"	# From specifies where we will obtain the availability zone for the volume. The options are "Name" and "Machine". If "Name" is specified then the Name field must also be specified. If "Machine" is specified the volume will use the value of FailureDomain, if any, from the associated Machine.
spec.template.spec.additionalBlockDevices[].storage.volume.availabilityZone.name <string> pattern="^[^ ]+$" min=1	# Name is the name of a volume availability zone to use. It is required if From is "Name". The volume availability zone name may not contain spaces.
spec.template.spec.additionalBlockDevices[].storage.volume.type <string>	# Type is the Cinder volume type of the volume. If omitted, the default Cinder volume type that is configured in the OpenStack cloud will be used.
spec.template.spec.configDrive <boolean>	# Config Drive support
spec.template.spec.flavor <string> min=1	# The flavor reference for the flavor for your server instance.
spec.template.spec.flavorID <string> min=1	# FlavorID allows flavors to be specified by ID. This field takes precedence over Flavor.
spec.template.spec.floatingIPPoolRef <object>	# floatingIPPoolRef is a reference to a IPPool that will be assigned to an IPAddressClaim. Once the IPAddressClaim is fulfilled, the FloatingIP will be assigned to the OpenStackMachine.
spec.template.spec.floatingIPPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.template.spec.floatingIPPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.template.spec.floatingIPPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.template.spec.identityRef <object>	# IdentityRef is a reference to a secret holding OpenStack credentials to be used when reconciling this machine. If not specified, the credentials specified in the cluster will be used.
spec.template.spec.identityRef.cloudName <string> (required) min=1	# CloudName specifies the name of the entry in the clouds.yaml file to use.
spec.template.spec.identityRef.name <string> (required) min=1	# Name is the name of a Secret (type=Secret) in the same namespace as the resource being provisioned, or the name of an OpenStackClusterIdentity (type=ClusterIdentity). The Secret must contain a key named `clouds.yaml` which contains an OpenStack clouds.yaml file. The Secret may optionally contain a key named `cacert` containing a PEM-encoded CA certificate.
spec.template.spec.identityRef.region <string>	# Region specifies an OpenStack region to use. If specified, it overrides any value in clouds.yaml. If specified for an OpenStackMachine, its value will be included in providerID.
spec.template.spec.identityRef.type <string> (required) enum=Secret|ClusterIdentity default="Secret"	# Type specifies the identity reference type. Defaults to Secret for backward compatibility.
spec.template.spec.image <object> (required)	# The image to use for your server instance. If the rootVolume is specified, this will be used when creating the root volume.
spec.template.spec.image.filter <object>	# Filter describes a query for an image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.template.spec.image.filter.name <string>	# The name of the desired image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.template.spec.image.filter.tags <[]string>	# The tags associated with the desired image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.template.spec.image.id <string> format=uuid	# ID is the uuid of the image. ID will not be validated before use.
spec.template.spec.image.imageRef <object>	# ImageRef is a reference to an ORC Image in the same namespace as the referring object.
spec.template.spec.image.imageRef.name <string> (required)	# Name is the name of the referenced resource
spec.template.spec.ports <[]object>	# Ports to be attached to the server instance. They are created if a port with the given name does not already exist. If not specified a default port will be added for the default cluster network.
spec.template.spec.ports[].adminStateUp <boolean>	# AdminStateUp specifies whether the port should be created in the up (true) or down (false) state. The default is up.
spec.template.spec.ports[].allowedAddressPairs <[]object>	# AllowedAddressPairs is a list of address pairs which Neutron will allow the port to send traffic from in addition to the port's addresses. If not specified, the MAC Address will be the MAC Address of the port. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
spec.template.spec.ports[].allowedAddressPairs[].ipAddress <string> (required)	# IPAddress is the IP address of the allowed address pair. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
spec.template.spec.ports[].allowedAddressPairs[].macAddress <string>	# MACAddress is the MAC address of the allowed address pair. If not specified, the MAC address will be the MAC address of the port.
spec.template.spec.ports[].description <string>	# Description is a human-readable description for the port.
spec.template.spec.ports[].disablePortSecurity <boolean>	# DisablePortSecurity enables or disables the port security when set. When not set, it takes the value of the corresponding field at the network level.
spec.template.spec.ports[].fixedIPs <[]object>	# FixedIPs is a list of pairs of subnet and/or IP address to assign to the port. If specified, these must be subnets of the port's network.
spec.template.spec.ports[].fixedIPs[].ipAddress <string>	# IPAddress is a specific IP address to assign to the port. If Subnet is also specified, IPAddress must be a valid IP address in the subnet. If Subnet is not specified, IPAddress must be a valid IP address in any subnet of the port's network.
spec.template.spec.ports[].fixedIPs[].subnet <object>	# Subnet is an openstack subnet query that will return the id of a subnet to create the fixed IP of a port in. This query must not return more than one subnet.
spec.template.spec.ports[].fixedIPs[].subnet.filter <object>	# Filter specifies a filter to select the subnet. It must match exactly one subnet.
spec.template.spec.ports[].fixedIPs[].subnet.filter.cidr <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.description <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.gatewayIP <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.ipVersion <integer>
spec.template.spec.ports[].fixedIPs[].subnet.filter.ipv6AddressMode <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.ipv6RAMode <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.name <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.ports[].fixedIPs[].subnet.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.ports[].fixedIPs[].subnet.filter.projectID <string>
spec.template.spec.ports[].fixedIPs[].subnet.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.ports[].fixedIPs[].subnet.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.ports[].fixedIPs[].subnet.id <string> format=uuid	# ID is the uuid of the subnet. It will not be validated.
spec.template.spec.ports[].hostID <string>	# HostID specifies the ID of the host where the port resides.
spec.template.spec.ports[].macAddress <string>	# MACAddress specifies the MAC address of the port. If not specified, the MAC address will be generated.
spec.template.spec.ports[].nameSuffix <string>	# NameSuffix will be appended to the name of the port if specified. If unspecified, instead the 0-based index of the port in the list is used.
spec.template.spec.ports[].network <object>	# Network is a query for an openstack network that the port will be created or discovered on. This will fail if the query returns more than one network.
spec.template.spec.ports[].network.filter <object>	# Filter specifies a filter to select an OpenStack network. If provided, cannot be empty.
spec.template.spec.ports[].network.filter.description <string>
spec.template.spec.ports[].network.filter.name <string>
spec.template.spec.ports[].network.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.ports[].network.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.ports[].network.filter.projectID <string>
spec.template.spec.ports[].network.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.ports[].network.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.ports[].network.id <string> format=uuid	# ID is the ID of the network to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.ports[].profile <object>	# Profile is a set of key-value pairs that are used for binding details. We intentionally don't expose this as a map[string]string because we only want to enable the users to set the values of the keys that are known to work in OpenStack Networking API. See https://docs.openstack.org/api-ref/network/v2/index.html?expanded=create-port-detail#create-port To set profiles, your tenant needs permissions rule:create_port, and rule:create_port:binding:profile
spec.template.spec.ports[].profile.ovsHWOffload <boolean>	# OVSHWOffload enables or disables the OVS hardware offload feature. This flag is not required on OpenStack clouds since Yoga as Nova will set it automatically when the port is attached. See: https://bugs.launchpad.net/nova/+bug/2020813
spec.template.spec.ports[].profile.trustedVF <boolean>	# TrustedVF enables or disables the “trusted mode” for the VF.
spec.template.spec.ports[].propagateUplinkStatus <boolean>	# PropageteUplinkStatus enables or disables the propagate uplink status on the port.
spec.template.spec.ports[].securityGroups <[]object>	# SecurityGroups is a list of the names, uuids, filters or any combination these of the security groups to assign to the instance.
spec.template.spec.ports[].securityGroups[].filter <object>	# Filter specifies a query to select an OpenStack security group. If provided, cannot be empty.
spec.template.spec.ports[].securityGroups[].filter.description <string>
spec.template.spec.ports[].securityGroups[].filter.name <string>
spec.template.spec.ports[].securityGroups[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.ports[].securityGroups[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.ports[].securityGroups[].filter.projectID <string>
spec.template.spec.ports[].securityGroups[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.ports[].securityGroups[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.ports[].securityGroups[].id <string> format=uuid	# ID is the ID of the security group to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.ports[].tags <[]string>	# Tags applied to the port (and corresponding trunk, if a trunk is configured.) These tags are applied in addition to the instance's tags, which will also be applied to the port.
spec.template.spec.ports[].trunk <boolean>	# Trunk specifies whether trunking is enabled at the port level. If not provided the value is inherited from the machine, or false for a bastion host.
spec.template.spec.ports[].valueSpecs <[]object>	# Value specs are extra parameters to include in the API request with OpenStack. This is an extension point for the API, so what they do and if they are supported, depends on the specific OpenStack implementation.
spec.template.spec.ports[].valueSpecs[].key <string> (required)	# Key is the key in the key-value pair.
spec.template.spec.ports[].valueSpecs[].name <string> (required)	# Name is the name of the key-value pair. This is just for identifying the pair and will not be sent to the OpenStack API.
spec.template.spec.ports[].valueSpecs[].value <string> (required)	# Value is the value in the key-value pair.
spec.template.spec.ports[].vnicType <string>	# VNICType specifies the type of vNIC which this port should be attached to. This is used to determine which mechanism driver(s) to be used to bind the port. The valid values are normal, macvtap, direct, baremetal, direct-physical, virtio-forwarder, smart-nic and remote-managed, although these values will not be validated in this API to ensure compatibility with future neutron changes or custom implementations. What type of vNIC is actually available depends on deployments. If not specified, the Neutron default value is used.
spec.template.spec.providerID <string>	# ProviderID is the unique identifier as specified by the cloud provider.
spec.template.spec.rootVolume <object>	# The volume metadata to boot from
spec.template.spec.rootVolume.availabilityZone <object>	# AvailabilityZone is the volume availability zone to create the volume in. If not specified, the volume will be created without an explicit availability zone.
spec.template.spec.rootVolume.availabilityZone.from <string> enum=Name|Machine default="Name"	# From specifies where we will obtain the availability zone for the volume. The options are "Name" and "Machine". If "Name" is specified then the Name field must also be specified. If "Machine" is specified the volume will use the value of FailureDomain, if any, from the associated Machine.
spec.template.spec.rootVolume.availabilityZone.name <string> pattern="^[^ ]+$" min=1	# Name is the name of a volume availability zone to use. It is required if From is "Name". The volume availability zone name may not contain spaces.
spec.template.spec.rootVolume.sizeGiB <integer> (required) min=1	# SizeGiB is the size of the block device in gibibytes (GiB).
spec.template.spec.rootVolume.type <string>	# Type is the Cinder volume type of the volume. If omitted, the default Cinder volume type that is configured in the OpenStack cloud will be used.
spec.template.spec.schedulerHintAdditionalProperties <[]object>	# SchedulerHintAdditionalProperties are arbitrary key/value pairs that provide additional hints to the OpenStack scheduler. These hints can influence how instances are placed on the infrastructure, such as specifying certain host aggregates or availability zones.
spec.template.spec.schedulerHintAdditionalProperties[].name <string> (required) min=1	# Name is the name of the scheduler hint property. It is a unique identifier for the property.
spec.template.spec.schedulerHintAdditionalProperties[].value <object> (required)	# Value is the value of the scheduler hint property, which can be of various types (e.g., bool, string, int). The type is indicated by the Value.Type field.
spec.template.spec.schedulerHintAdditionalProperties[].value.bool <boolean>	# Bool is the boolean value of the scheduler hint, used when Type is "Bool". This field is required if type is 'Bool', and must not be set otherwise.
spec.template.spec.schedulerHintAdditionalProperties[].value.number <integer>	# Number is the integer value of the scheduler hint, used when Type is "Number". This field is required if type is 'Number', and must not be set otherwise.
spec.template.spec.schedulerHintAdditionalProperties[].value.string <string> min=1 max=255	# String is the string value of the scheduler hint, used when Type is "String". This field is required if type is 'String', and must not be set otherwise.
spec.template.spec.schedulerHintAdditionalProperties[].value.type <string> (required) enum=Bool|String|Number	# Type represents the type of the value. Valid values are Bool, String, and Number.
spec.template.spec.securityGroups <[]object>	# The names of the security groups to assign to the instance
spec.template.spec.securityGroups[].filter <object>	# Filter specifies a query to select an OpenStack security group. If provided, cannot be empty.
spec.template.spec.securityGroups[].filter.description <string>
spec.template.spec.securityGroups[].filter.name <string>
spec.template.spec.securityGroups[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.securityGroups[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.securityGroups[].filter.projectID <string>
spec.template.spec.securityGroups[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.securityGroups[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.securityGroups[].id <string> format=uuid	# ID is the ID of the security group to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.serverGroup <object>	# The server group to assign the machine to.
spec.template.spec.serverGroup.filter <object>	# Filter specifies a query to select an OpenStack server group. If provided, it cannot be empty.
spec.template.spec.serverGroup.filter.name <string>	# Name is the name of a server group to look for.
spec.template.spec.serverGroup.id <string> format=uuid	# ID is the ID of the server group to use.
spec.template.spec.serverMetadata <[]object>	# Metadata mapping. Allows you to create a map of key value pairs to add to the server instance.
spec.template.spec.serverMetadata[].key <string> (required) max=255	# Key is the server metadata key
spec.template.spec.serverMetadata[].value <string> (required) max=255	# Value is the server metadata value
spec.template.spec.sshKeyName <string>	# The ssh key to inject in the instance
spec.template.spec.tags <[]string>	# Tags which will be added to the machine and all dependent resources which support them. These are in addition to Tags defined on the cluster. Requires Nova api 2.52 minimum!
spec.template.spec.trunk <boolean>	# Whether the server instance is created on a trunk port or not.
status <object>	# OpenStackMachineTemplateStatus defines the observed state of OpenStackMachineTemplate.
status.capacity <object (free-form)>	# Capacity defines the resource capacity for this machine. This value is used for autoscaling from zero operations as defined in: https://github.com/kubernetes-sigs/cluster-api/blob/main/docs/proposals/20210310-opt-in-autoscaling-from-zero.md
status.nodeInfo <object>	# NodeInfo contains information about the node's architecture and operating system.
status.nodeInfo.operatingSystem <string>	# operatingSystem is a string representing the operating system of the node. This may be a string like 'linux' or 'windows'.
