# schema source: Cluster API AWS v2.12.1 https://github.com/kubernetes-sigs/cluster-api-provider-aws
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta2
kind <string> enum=AWSManagedClusterTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# AWSManagedClusterTemplateSpec defines the desired state of AWSManagedClusterTemplate.
spec.template <object> (required)	# AWSManagedClusterTemplateResource describes the data needed to create an AWSManagedCluster from a template.
spec.template.spec <object> (required)	# AWSManagedClusterSpec defines the desired state of AWSManagedCluster
spec.template.spec.controlPlaneEndpoint <object>	# ControlPlaneEndpoint represents the endpoint used to communicate with the control plane.
spec.template.spec.controlPlaneEndpoint.host <string> max=512	# host is the hostname on which the API server is serving.
spec.template.spec.controlPlaneEndpoint.port <integer> format=int32	# port is the port on which the API server is serving.
