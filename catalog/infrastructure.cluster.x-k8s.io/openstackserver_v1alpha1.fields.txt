# schema source: Cluster API OpenStack v0.14.6 https://github.com/kubernetes-sigs/cluster-api-provider-openstack
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1alpha1
kind <string> enum=OpenStackServer
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# OpenStackServerSpec defines the desired state of OpenStackServer.
spec.additionalBlockDevices <[]object>	# AdditionalBlockDevices is a list of specifications for additional block devices to attach to the server instance.
spec.additionalBlockDevices[].name <string> (required)	# Name of the block device in the context of a machine. If the block device is a volume, the Cinder volume will be named as a combination of the machine name and this name. Also, this name will be used for tagging the block device. Information about the block device tag can be obtained from the OpenStack metadata API or the config drive. Name cannot be 'root', which is reserved for the root volume.
spec.additionalBlockDevices[].sizeGiB <integer> (required) min=1	# SizeGiB is the size of the block device in gibibytes (GiB).
spec.additionalBlockDevices[].storage <object> (required)	# Storage specifies the storage type of the block device and additional storage options.
spec.additionalBlockDevices[].storage.type <string> (required)	# Type is the type of block device to create. This can be either "Volume" or "Local".
spec.additionalBlockDevices[].storage.volume <object>	# Volume contains additional storage options for a volume block device.
spec.additionalBlockDevices[].storage.volume.availabilityZone <object>	# AvailabilityZone is the volume availability zone to create the volume in. If not specified, the volume will be created without an explicit availability zone.
spec.additionalBlockDevices[].storage.volume.availabilityZone.from <string> enum=Name|Machine default="Name"	# From specifies where we will obtain the availability zone for the volume. The options are "Name" and "Machine". If "Name" is specified then the Name field must also be specified. If "Machine" is specified the volume will use the value of FailureDomain, if any, from the associated Machine.
spec.additionalBlockDevices[].storage.volume.availabilityZone.name <string> pattern="^[^ ]+$" min=1	# Name is the name of a volume availability zone to use. It is required if From is "Name". The volume availability zone name may not contain spaces.
spec.additionalBlockDevices[].storage.volume.type <string>	# Type is the Cinder volume type of the volume. If omitted, the default Cinder volume type that is configured in the OpenStack cloud will be used.
spec.availabilityZone <string>	# AvailabilityZone is the availability zone in which to create the server instance.
spec.configDrive <boolean>	# ConfigDrive is a flag to enable config drive for the server instance.
spec.flavor <string> min=1	# The flavor reference for the flavor for the server instance.
spec.flavorID <string> min=1	# FlavorID allows flavors to be specified by ID. This field takes precedence over Flavor.
spec.floatingIPPoolRef <object>	# FloatingIPPoolRef is a reference to a FloatingIPPool to allocate a floating IP from.
spec.floatingIPPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.floatingIPPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.floatingIPPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.identityRef <object> (required)	# IdentityRef is a reference to a secret holding OpenStack credentials.
spec.identityRef.cloudName <string> (required) min=1	# CloudName specifies the name of the entry in the clouds.yaml file to use.
spec.identityRef.name <string> (required) min=1	# Name is the name of a Secret (type=Secret) in the same namespace as the resource being provisioned, or the name of an OpenStackClusterIdentity (type=ClusterIdentity). The Secret must contain a key named `clouds.yaml` which contains an OpenStack clouds.yaml file. The Secret may optionally contain a key named `cacert` containing a PEM-encoded CA certificate.
spec.identityRef.region <string>	# Region specifies an OpenStack region to use. If specified, it overrides any value in clouds.yaml. If specified for an OpenStackMachine, its value will be included in providerID.
spec.identityRef.type <string> (required) enum=Secret|ClusterIdentity default="Secret"	# Type specifies the identity reference type. Defaults to Secret for backward compatibility.
spec.image <object> (required)	# The image to use for the server instance.
spec.image.filter <object>	# Filter describes a query for an image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.image.filter.name <string>	# The name of the desired image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.image.filter.tags <[]string>	# The tags associated with the desired image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.image.id <string> format=uuid	# ID is the uuid of the image. ID will not be validated before use.
spec.image.imageRef <object>	# ImageRef is a reference to an ORC Image in the same namespace as the referring object.
spec.image.imageRef.name <string> (required)	# Name is the name of the referenced resource
spec.ports <[]object> (required)	# Ports to be attached to the server instance.
spec.ports[].adminStateUp <boolean>	# AdminStateUp specifies whether the port should be created in the up (true) or down (false) state. The default is up.
spec.ports[].allowedAddressPairs <[]object>	# AllowedAddressPairs is a list of address pairs which Neutron will allow the port to send traffic from in addition to the port's addresses. If not specified, the MAC Address will be the MAC Address of the port. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
spec.ports[].allowedAddressPairs[].ipAddress <string> (required)	# IPAddress is the IP address of the allowed address pair. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
spec.ports[].allowedAddressPairs[].macAddress <string>	# MACAddress is the MAC address of the allowed address pair. If not specified, the MAC address will be the MAC address of the port.
spec.ports[].description <string>	# Description is a human-readable description for the port.
spec.ports[].disablePortSecurity <boolean>	# DisablePortSecurity enables or disables the port security when set. When not set, it takes the value of the corresponding field at the network level.
spec.ports[].fixedIPs <[]object>	# FixedIPs is a list of pairs of subnet and/or IP address to assign to the port. If specified, these must be subnets of the port's network.
spec.ports[].fixedIPs[].ipAddress <string>	# IPAddress is a specific IP address to assign to the port. If Subnet is also specified, IPAddress must be a valid IP address in the subnet. If Subnet is not specified, IPAddress must be a valid IP address in any subnet of the port's network.
spec.ports[].fixedIPs[].subnet <object>	# Subnet is an openstack subnet query that will return the id of a subnet to create the fixed IP of a port in. This query must not return more than one subnet.
spec.ports[].fixedIPs[].subnet.filter <object>	# Filter specifies a filter to select the subnet. It must match exactly one subnet.
spec.ports[].fixedIPs[].subnet.filter.cidr <string>
spec.ports[].fixedIPs[].subnet.filter.description <string>
spec.ports[].fixedIPs[].subnet.filter.gatewayIP <string>
spec.ports[].fixedIPs[].subnet.filter.ipVersion <integer>
spec.ports[].fixedIPs[].subnet.filter.ipv6AddressMode <string>
spec.ports[].fixedIPs[].subnet.filter.ipv6RAMode <string>
spec.ports[].fixedIPs[].subnet.filter.name <string>
spec.ports[].fixedIPs[].subnet.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.ports[].fixedIPs[].subnet.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.ports[].fixedIPs[].subnet.filter.projectID <string>
spec.ports[].fixedIPs[].subnet.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.ports[].fixedIPs[].subnet.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.ports[].fixedIPs[].subnet.id <string> format=uuid	# ID is the uuid of the subnet. It will not be validated.
spec.ports[].hostID <string>	# HostID specifies the ID of the host where the port resides.
spec.ports[].macAddress <string>	# MACAddress specifies the MAC address of the port. If not specified, the MAC address will be generated.
spec.ports[].nameSuffix <string>	# NameSuffix will be appended to the name of the port if specified. If unspecified, instead the 0-based index of the port in the list is used.
spec.ports[].network <object>	# Network is a query for an openstack network that the port will be created or discovered on. This will fail if the query returns more than one network.
spec.ports[].network.filter <object>	# Filter specifies a filter to select an OpenStack network. If provided, cannot be empty.
spec.ports[].network.filter.description <string>
spec.ports[].network.filter.name <string>
spec.ports[].network.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.ports[].network.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.ports[].network.filter.projectID <string>
spec.ports[].network.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.ports[].network.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.ports[].network.id <string> format=uuid	# ID is the ID of the network to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.ports[].profile <object>	# Profile is a set of key-value pairs that are used for binding details. We intentionally don't expose this as a map[string]string because we only want to enable the users to set the values of the keys that are known to work in OpenStack Networking API. See https://docs.openstack.org/api-ref/network/v2/index.html?expanded=create-port-detail#create-port To set profiles, your tenant needs permissions rule:create_port, and rule:create_port:binding:profile
spec.ports[].profile.ovsHWOffload <boolean>	# OVSHWOffload enables or disables the OVS hardware offload feature. This flag is not required on OpenStack clouds since Yoga as Nova will set it automatically when the port is attached. See: https://bugs.launchpad.net/nova/+bug/2020813
spec.ports[].profile.trustedVF <boolean>	# TrustedVF enables or disables the “trusted mode” for the VF.
spec.ports[].propagateUplinkStatus <boolean>	# PropageteUplinkStatus enables or disables the propagate uplink status on the port.
spec.ports[].securityGroups <[]object>	# SecurityGroups is a list of the names, uuids, filters or any combination these of the security groups to assign to the instance.
spec.ports[].securityGroups[].filter <object>	# Filter specifies a query to select an OpenStack security group. If provided, cannot be empty.
spec.ports[].securityGroups[].filter.description <string>
spec.ports[].securityGroups[].filter.name <string>
spec.ports[].securityGroups[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.ports[].securityGroups[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.ports[].securityGroups[].filter.projectID <string>
spec.ports[].securityGroups[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.ports[].securityGroups[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.ports[].securityGroups[].id <string> format=uuid	# ID is the ID of the security group to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.ports[].tags <[]string>	# Tags applied to the port (and corresponding trunk, if a trunk is configured.) These tags are applied in addition to the instance's tags, which will also be applied to the port.
spec.ports[].trunk <boolean>	# Trunk specifies whether trunking is enabled at the port level. If not provided the value is inherited from the machine, or false for a bastion host.
spec.ports[].valueSpecs <[]object>	# Value specs are extra parameters to include in the API request with OpenStack. This is an extension point for the API, so what they do and if they are supported, depends on the specific OpenStack implementation.
spec.ports[].valueSpecs[].key <string> (required)	# Key is the key in the key-value pair.
spec.ports[].valueSpecs[].name <string> (required)	# Name is the name of the key-value pair. This is just for identifying the pair and will not be sent to the OpenStack API.
spec.ports[].valueSpecs[].value <string> (required)	# Value is the value in the key-value pair.
spec.ports[].vnicType <string>	# VNICType specifies the type of vNIC which this port should be attached to. This is used to determine which mechanism driver(s) to be used to bind the port. The valid values are normal, macvtap, direct, baremetal, direct-physical, virtio-forwarder, smart-nic and remote-managed, although these values will not be validated in this API to ensure compatibility with future neutron changes or custom implementations. What type of vNIC is actually available depends on deployments. If not specified, the Neutron default value is used.
spec.rootVolume <object>	# RootVolume is the specification for the root volume of the server instance.
spec.rootVolume.availabilityZone <object>	# AvailabilityZone is the volume availability zone to create the volume in. If not specified, the volume will be created without an explicit availability zone.
spec.rootVolume.availabilityZone.from <string> enum=Name|Machine default="Name"	# From specifies where we will obtain the availability zone for the volume. The options are "Name" and "Machine". If "Name" is specified then the Name field must also be specified. If "Machine" is specified the volume will use the value of FailureDomain, if any, from the associated Machine.
spec.rootVolume.availabilityZone.name <string> pattern="^[^ ]+$" min=1	# Name is the name of a volume availability zone to use. It is required if From is "Name". The volume availability zone name may not contain spaces.
spec.rootVolume.sizeGiB <integer> (required) min=1	# SizeGiB is the size of the block device in gibibytes (GiB).
spec.rootVolume.type <string>	# Type is the Cinder volume type of the volume. If omitted, the default Cinder volume type that is configured in the OpenStack cloud will be used.
spec.schedulerHintAdditionalProperties <[]object>	# SchedulerHintAdditionalProperties are arbitrary key/value pairs that provide additional hints to the OpenStack scheduler. These hints can influence how instances are placed on the infrastructure, such as specifying certain host aggregates or availability zones.
spec.schedulerHintAdditionalProperties[].name <string> (required) min=1	# Name is the name of the scheduler hint property. It is a unique identifier for the property.
spec.schedulerHintAdditionalProperties[].value <object> (required)	# Value is the value of the scheduler hint property, which can be of various types (e.g., bool, string, int). The type is indicated by the Value.Type field.
spec.schedulerHintAdditionalProperties[].value.bool <boolean>	# Bool is the boolean value of the scheduler hint, used when Type is "Bool". This field is required if type is 'Bool', and must not be set otherwise.
spec.schedulerHintAdditionalProperties[].value.number <integer>	# Number is the integer value of the scheduler hint, used when Type is "Number". This field is required if type is 'Number', and must not be set otherwise.
spec.schedulerHintAdditionalProperties[].value.string <string> min=1 max=255	# String is the string value of the scheduler hint, used when Type is "String". This field is required if type is 'String', and must not be set otherwise.
spec.schedulerHintAdditionalProperties[].value.type <string> (required) enum=Bool|String|Number	# Type represents the type of the value. Valid values are Bool, String, and Number.
spec.securityGroups <[]object>	# SecurityGroups is a list of security groups names to assign to the instance.
spec.securityGroups[].filter <object>	# Filter specifies a query to select an OpenStack security group. If provided, cannot be empty.
spec.securityGroups[].filter.description <string>
spec.securityGroups[].filter.name <string>
spec.securityGroups[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.securityGroups[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.securityGroups[].filter.projectID <string>
spec.securityGroups[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.securityGroups[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.securityGroups[].id <string> format=uuid	# ID is the ID of the security group to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.serverGroup <object>	# ServerGroup is the server group to which the server instance belongs.
spec.serverGroup.filter <object>	# Filter specifies a query to select an OpenStack server group. If provided, it cannot be empty.
spec.serverGroup.filter.name <string>	# Name is the name of a server group to look for.
spec.serverGroup.id <string> format=uuid	# ID is the ID of the server group to use.
spec.serverMetadata <[]object>	# ServerMetadata is a map of key value pairs to add to the server instance.
spec.serverMetadata[].key <string> (required) max=255	# Key is the server metadata key
spec.serverMetadata[].value <string> (required) max=255	# Value is the server metadata value
spec.sshKeyName <string> (required)	# SSHKeyName is the name of the SSH key to inject in the instance.
spec.tags <[]string>	# Tags which will be added to the machine and all dependent resources which support them. These are in addition to Tags defined on the cluster. Requires Nova api 2.52 minimum!
spec.trunk <boolean>	# Trunk is a flag to indicate if the server instance is created on a trunk port or not.
spec.userDataRef <object>	# UserDataRef is a reference to a secret containing the user data to be injected into the server instance.
spec.userDataRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status <object>	# OpenStackServerStatus defines the observed state of OpenStackServer.
status.addresses <[]object>	# Addresses is the list of addresses of the server instance.
status.addresses[].address <string> (required)	# The node address.
status.addresses[].type <string> (required)	# Node address type, one of Hostname, ExternalIP or InternalIP.
status.conditions <[]object>	# Conditions defines current service state of the OpenStackServer.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> min=1 max=10240	# message is a human readable message indicating details about the transition. This field may be empty.
status.conditions[].reason <string> min=1 max=256	# reason is the reason for the condition's last transition in CamelCase. The specific API may choose whether or not this field is considered a guaranteed API. This field may be empty.
status.conditions[].severity <string> max=32	# severity provides an explicit classification of Reason code, so the users or machines can immediately understand the current situation and act accordingly. The Severity field MUST be set only when Status=False.
status.conditions[].status <string> (required)	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) min=1 max=256	# type of condition in CamelCase or in foo.example.com/CamelCase. Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important.
status.instanceID <string>	# InstanceID is the ID of the server instance.
status.instanceState <string>	# InstanceState is the state of the server instance.
status.ready <boolean> (required) default=false	# Ready is true when the OpenStack server is ready.
status.resolved <object>	# Resolved contains parts of the machine spec with all external references fully resolved.
status.resolved.flavorID <string>	# FlavorID is the ID of the flavor to use.
status.resolved.imageID <string>	# ImageID is the ID of the image to use for the server and is calculated based on ImageFilter.
status.resolved.ports <[]object>	# Ports is the fully resolved list of ports to create for the server.
status.resolved.ports[].adminStateUp <boolean>	# AdminStateUp specifies whether the port should be created in the up (true) or down (false) state. The default is up.
status.resolved.ports[].allowedAddressPairs <[]object>	# AllowedAddressPairs is a list of address pairs which Neutron will allow the port to send traffic from in addition to the port's addresses. If not specified, the MAC Address will be the MAC Address of the port. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
status.resolved.ports[].allowedAddressPairs[].ipAddress <string> (required)	# IPAddress is the IP address of the allowed address pair. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
status.resolved.ports[].allowedAddressPairs[].macAddress <string>	# MACAddress is the MAC address of the allowed address pair. If not specified, the MAC address will be the MAC address of the port.
status.resolved.ports[].description <string> (required)	# Description is a human-readable description for the port.
status.resolved.ports[].disablePortSecurity <boolean>	# DisablePortSecurity enables or disables the port security when set. When not set, it takes the value of the corresponding field at the network level.
status.resolved.ports[].fixedIPs <[]object>	# FixedIPs is a list of pairs of subnet and/or IP address to assign to the port. If specified, these must be subnets of the port's network.
status.resolved.ports[].fixedIPs[].ipAddress <string>	# IPAddress is a specific IP address to assign to the port. If SubnetID is also specified, IPAddress must be a valid IP address in the subnet. If Subnet is not specified, IPAddress must be a valid IP address in any subnet of the port's network.
status.resolved.ports[].fixedIPs[].subnet <string>	# SubnetID is the id of a subnet to create the fixed IP of a port in.
status.resolved.ports[].hostID <string>	# HostID specifies the ID of the host where the port resides.
status.resolved.ports[].macAddress <string>	# MACAddress specifies the MAC address of the port. If not specified, the MAC address will be generated.
status.resolved.ports[].name <string> (required)	# Name is the name of the port.
status.resolved.ports[].networkID <string> (required)	# NetworkID is the ID of the network the port will be created in.
status.resolved.ports[].profile <object>	# Profile is a set of key-value pairs that are used for binding details. We intentionally don't expose this as a map[string]string because we only want to enable the users to set the values of the keys that are known to work in OpenStack Networking API. See https://docs.openstack.org/api-ref/network/v2/index.html?expanded=create-port-detail#create-port To set profiles, your tenant needs permissions rule:create_port, and rule:create_port:binding:profile
status.resolved.ports[].profile.ovsHWOffload <boolean>	# OVSHWOffload enables or disables the OVS hardware offload feature. This flag is not required on OpenStack clouds since Yoga as Nova will set it automatically when the port is attached. See: https://bugs.launchpad.net/nova/+bug/2020813
status.resolved.ports[].profile.trustedVF <boolean>	# TrustedVF enables or disables the “trusted mode” for the VF.
status.resolved.ports[].propagateUplinkStatus <boolean>	# PropageteUplinkStatus enables or disables the propagate uplink status on the port.
status.resolved.ports[].securityGroups <[]string>	# SecurityGroups is a list of security group IDs to assign to the port.
status.resolved.ports[].tags <[]string>	# Tags applied to the port (and corresponding trunk, if a trunk is configured.)
status.resolved.ports[].trunk <boolean>	# Trunk specifies whether trunking is enabled at the port level.
status.resolved.ports[].valueSpecs <[]object>	# Value specs are extra parameters to include in the API request with OpenStack. This is an extension point for the API, so what they do and if they are supported, depends on the specific OpenStack implementation.
status.resolved.ports[].valueSpecs[].key <string> (required)	# Key is the key in the key-value pair.
status.resolved.ports[].valueSpecs[].name <string> (required)	# Name is the name of the key-value pair. This is just for identifying the pair and will not be sent to the OpenStack API.
status.resolved.ports[].valueSpecs[].value <string> (required)	# Value is the value in the key-value pair.
status.resolved.ports[].vnicType <string>	# VNICType specifies the type of vNIC which this port should be attached to. This is used to determine which mechanism driver(s) to be used to bind the port. The valid values are normal, macvtap, direct, baremetal, direct-physical, virtio-forwarder, smart-nic and remote-managed, although these values will not be validated in this API to ensure compatibility with future neutron changes or custom implementations. What type of vNIC is actually available depends on deployments. If not specified, the Neutron default value is used.
status.resolved.serverGroupID <string>	# ServerGroupID is the ID of the server group the server should be added to and is calculated based on ServerGroupFilter.
status.resources <object>	# Resources contains references to OpenStack resources created for the machine.
status.resources.ports <[]object>	# Ports is the status of the ports created for the server.
status.resources.ports[].id <string> (required)	# ID is the unique identifier of the port.
