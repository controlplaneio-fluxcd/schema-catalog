# schema source: Cluster API GCP v1.12.0 https://github.com/kubernetes-sigs/cluster-api-provider-gcp
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta1
kind <string> enum=GCPManagedMachinePoolTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# GCPManagedMachinePoolTemplateSpec defines the desired state of GCPManagedMachinePoolTemplate.
spec.template <object> (required)	# GCPManagedMachinePoolTemplateResource describes the data needed to create an GCPManagedCluster from a template.
spec.template.spec <object> (required)	# GCPManagedMachinePoolTemplateResourceSpec specifies an GCP managed control plane template resource.
spec.template.spec.additionalLabels <map[string]string>	# AdditionalLabels is an optional set of tags to add to GCP resources managed by the GCP provider, in addition to the ones added by default.
spec.template.spec.diskSizeGB <integer> format=int64 min=10	# DiskSizeGB is size of the disk attached to each node, specified in GB.
spec.template.spec.diskSizeGb <integer> format=int32	# DiskSizeGb is the size of the disk attached to each node, specified in GB. The smallest allowed disk size is 10GB. If unspecified, the default disk size is 100GB.
spec.template.spec.diskType <string> enum=pd-standard|pd-ssd|pd-balanced	# DiskType is type of the disk attached to each node.
spec.template.spec.imageType <string>	# ImageType is image type to use for this nodepool.
spec.template.spec.instanceType <string>	# InstanceType is name of Compute Engine machine type.
spec.template.spec.kubernetesLabels <map[string]string>	# KubernetesLabels specifies the labels to apply to the nodes of the node pool.
spec.template.spec.kubernetesTaints <[]object>	# KubernetesTaints specifies the taints to apply to the nodes of the node pool.
spec.template.spec.kubernetesTaints[].effect <string> (required) enum=NoSchedule|NoExecute|PreferNoSchedule	# Effect specifies the effect for the taint.
spec.template.spec.kubernetesTaints[].key <string> (required)	# Key is the key of the taint
spec.template.spec.kubernetesTaints[].value <string> (required)	# Value is the value of the taint
spec.template.spec.linuxNodeConfig <object>	# LinuxNodeConfig specifies the settings for Linux agent nodes.
spec.template.spec.linuxNodeConfig.cgroupMode <integer> format=int32	# CgroupMode specifies the cgroup mode for this node pool.
spec.template.spec.linuxNodeConfig.sysctls <[]object>	# Sysctls specifies the sysctl settings for this node pool.
spec.template.spec.linuxNodeConfig.sysctls[].parameter <string>	# Parameter specifies sysctl parameter name.
spec.template.spec.linuxNodeConfig.sysctls[].value <string>	# Value specifies sysctl parameter value.
spec.template.spec.localSsdCount <integer> format=int32	# LocalSsdCount is the number of local SSD disks to be attached to the node.
spec.template.spec.machineType <string>	# MachineType is the name of a Google Compute Engine [machine type](https://cloud.google.com/compute/docs/machine-types). If unspecified, the default machine type is `e2-medium`.
spec.template.spec.management <object>	# Management specifies the node pool management options.
spec.template.spec.management.autoRepair <boolean>	# AutoRepair specifies whether the node auto-repair is enabled for the node pool. If enabled, the nodes in this node pool will be monitored and, if they fail health checks too many times, an automatic repair action will be triggered.
spec.template.spec.management.autoUpgrade <boolean>	# AutoUpgrade specifies whether node auto-upgrade is enabled for the node pool. If enabled, node auto-upgrade helps keep the nodes in your node pool up to date with the latest release version of Kubernetes.
spec.template.spec.maxPodsPerNode <integer> format=int64 min=8 max=256	# MaxPodsPerNode is constraint enforced on the max num of pods per node.
spec.template.spec.nodeLocations <[]string>	# NodeLocations is the list of zones in which the NodePool's nodes should be located.
spec.template.spec.nodeNetwork <object>	# NodeNetwork specifies the node network configuration options.
spec.template.spec.nodeNetwork.createPodRange <boolean>	# CreatePodRange specifies whether to create a new range for pod IPs in this node pool.
spec.template.spec.nodeNetwork.podRangeCidrBlock <string>	# PodRangeCidrBlock is the IP address range for pod IPs in this node pool.
spec.template.spec.nodeNetwork.podRangeName <string>	# PodRangeName is ID of the secondary range for pod IPs.
spec.template.spec.nodeNetwork.tags <[]string>	# Tags is list of instance tags applied to all nodes. Tags are used to identify valid sources or targets for network firewalls.
spec.template.spec.nodePoolName <string>	# NodePoolName specifies the name of the GKE node pool corresponding to this MachinePool. If you don't specify a name then a default name will be created based on the namespace and name of the managed machine pool.
spec.template.spec.nodeSecurity <object>	# NodeSecurity specifies the node security options.
spec.template.spec.nodeSecurity.enableIntegrityMonitoring <boolean>	# EnableIntegrityMonitoring defines whether the instance has integrity monitoring enabled.
spec.template.spec.nodeSecurity.enableSecureBoot <boolean>	# EnableSecureBoot defines whether the instance has Secure Boot enabled.
spec.template.spec.nodeSecurity.sandboxType <string>	# SandboxType is type of the sandbox to use for the node.
spec.template.spec.nodeSecurity.serviceAccount <object>	# ServiceAccount specifies the identity details for node pool.
spec.template.spec.nodeSecurity.serviceAccount.email <string>	# Email is the Google Cloud Platform Service Account to be used by the node VMs.
spec.template.spec.nodeSecurity.serviceAccount.scopes <[]string>	# Scopes is a set of Google API scopes to be made available on all of the node VMs under the "default" service account.
spec.template.spec.scaling <object>	# Scaling specifies scaling for the node pool
spec.template.spec.scaling.enableAutoscaling <boolean>	# Is autoscaling enabled for this node pool. If unspecified, the default value is true.
spec.template.spec.scaling.locationPolicy <string> enum=balanced|any	# Location policy used when scaling up a nodepool.
spec.template.spec.scaling.maxCount <integer> format=int32	# MaxCount specifies the maximum number of nodes in the node pool
spec.template.spec.scaling.minCount <integer> format=int32	# MinCount specifies the minimum number of nodes in the node pool
