# schema source: Cluster API Metal3 v1.13.1 https://github.com/metal3-io/cluster-api-provider-metal3
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta2
kind <string> enum=Metal3DataTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# spec defines the desired state of Metal3DataTemplate.
spec.clusterName <string> (required) min=1 max=512	# clusterName is the name of the Cluster this object belongs to.
spec.metaData <object>	# metaData contains the information needed to generate the metadata secret
spec.metaData.dnsServersFromPool <[]object> min=1 max=512	# dnsServersFromPool is the list of metadata items to be rendered as dns servers.
spec.metaData.dnsServersFromPool[].apiGroup <string> default="ipam.metal3.io" min=1 max=512	# apiGroup is the api group of the IP pool.
spec.metaData.dnsServersFromPool[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.dnsServersFromPool[].kind <string> default="IPPool" min=1 max=512	# kind is the kind of the IP pool
spec.metaData.dnsServersFromPool[].name <string> (required) min=1 max=512	# name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.fromAnnotations <[]object> min=1 max=512	# fromAnnotations is the list of metadata items to be fetched from object Annotations
spec.metaData.fromAnnotations[].annotation <string> (required) min=1 max=512	# annotation is the key of the annotation to fetch
spec.metaData.fromAnnotations[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.fromAnnotations[].object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the name
spec.metaData.fromHostInterfaces <[]object> min=1 max=512	# fromHostInterfaces is the list of metadata items to be rendered as MAC addresses of the host interfaces.
spec.metaData.fromHostInterfaces[].fromBootMAC <boolean>	# fromBootMAC will fetch the MAC address from the BareMetalHost Spec BootMACAddress field.
spec.metaData.fromHostInterfaces[].interface <string> min=1 max=512	# interface is the name of the interface in the BareMetalHost Status Hardware Details list of interfaces from which to fetch the MAC address.
spec.metaData.fromHostInterfaces[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.fromLabels <[]object> min=1 max=512	# fromLabels is the list of metadata items to be fetched from object labels
spec.metaData.fromLabels[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.fromLabels[].label <string> (required) min=1 max=512	# label is the key of the label to fetch
spec.metaData.fromLabels[].object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the name
spec.metaData.gatewaysFromPool <[]object> min=1 max=512	# gatewaysFromPool is the list of metadata items to be rendered as gateway addresses.
spec.metaData.gatewaysFromPool[].apiGroup <string> default="ipam.metal3.io" min=1 max=512	# apiGroup is the api group of the IP pool.
spec.metaData.gatewaysFromPool[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.gatewaysFromPool[].kind <string> default="IPPool" min=1 max=512	# kind is the kind of the IP pool
spec.metaData.gatewaysFromPool[].name <string> (required) min=1 max=512	# name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.indexes <[]object> min=1 max=512	# indexes is the list of metadata items to be rendered from the index of the Metal3Data
spec.metaData.indexes[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.indexes[].offset <integer> format=int32 min=0	# offset is the offset to apply to the index when rendering it
spec.metaData.indexes[].prefix <string> min=1 max=512	# prefix is the prefix string
spec.metaData.indexes[].step <integer> default=1 format=int32 min=1	# step is the multiplier of the index
spec.metaData.indexes[].suffix <string> min=1 max=512	# suffix is the suffix string
spec.metaData.ipAddressesFromPool <[]object> min=1 max=512	# ipAddressesFromPool is the list of metadata items to be rendered as ip addresses.
spec.metaData.ipAddressesFromPool[].apiGroup <string> default="ipam.metal3.io" min=1 max=512	# apiGroup is the api group of the IP pool.
spec.metaData.ipAddressesFromPool[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.ipAddressesFromPool[].kind <string> default="IPPool" min=1 max=512	# kind is the kind of the IP pool
spec.metaData.ipAddressesFromPool[].name <string> (required) min=1 max=512	# name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.namespaces <[]object> min=1 max=512	# namespaces is the list of metadata items to be rendered from the namespace
spec.metaData.namespaces[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.objectNames <[]object> min=1 max=512	# objectNames is the list of metadata items to be rendered from the name of objects.
spec.metaData.objectNames[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.objectNames[].object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the name
spec.metaData.prefixesFromPool <[]object> min=1 max=512	# prefixesFromPool is the list of metadata items to be rendered as network prefixes.
spec.metaData.prefixesFromPool[].apiGroup <string> default="ipam.metal3.io" min=1 max=512	# apiGroup is the api group of the IP pool.
spec.metaData.prefixesFromPool[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.prefixesFromPool[].kind <string> default="IPPool" min=1 max=512	# kind is the kind of the IP pool
spec.metaData.prefixesFromPool[].name <string> (required) min=1 max=512	# name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.strings <[]object> min=1 max=512	# strings is the list of metadata items to be rendered from strings
spec.metaData.strings[].key <string> (required) min=1 max=512	# key will be used as the key to set in the metadata map for cloud-init
spec.metaData.strings[].value <string> (required) min=1 max=512	# value is the string to render.
spec.networkData <object>	# networkData contains the information needed to generate the networkdata secret
spec.networkData.links <object>	# links is a structure containing lists of different types objects
spec.networkData.links.bonds <[]object> min=1 max=512	# bonds contains a list of Bond links
spec.networkData.links.bonds[].bondLinks <[]string> max=512	# bondLinks is the list of links that are part of the bond.
spec.networkData.links.bonds[].bondMode <string> (required) enum=balance-rr|active-backup|balance-xor|broadcast|balance-tlb|balance-alb|802.3ad	# bondMode is the mode of bond used. It can be one of balance-rr, active-backup, balance-xor, broadcast, balance-tlb, balance-alb, 802.3ad
spec.networkData.links.bonds[].bondXmitHashPolicy <string> enum=layer2|layer3+4|layer2+3	# bondXmitHashPolicy selects the transmit hash policy used for port selection in balance-xor and 802.3ad modes
spec.networkData.links.bonds[].id <string> (required) min=1 max=512	# id is the ID of the interface (used for naming)
spec.networkData.links.bonds[].macAddress <object> (required)	# macAddress is the MAC address of the interface, containing the object used to render it.
spec.networkData.links.bonds[].macAddress.fromAnnotation <object>	# fromAnnotation references an object annotation to retrieve the MAC address from
spec.networkData.links.bonds[].macAddress.fromAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation to fetch
spec.networkData.links.bonds[].macAddress.fromAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the name
spec.networkData.links.bonds[].macAddress.fromHostInterface <string> min=1 max=512	# fromHostInterface contains the name of the interface in the BareMetalHost Introspection details from which to fetch the MAC address
spec.networkData.links.bonds[].macAddress.string <string> min=1 max=512	# string contains the MAC address given as a string
spec.networkData.links.bonds[].mtu <integer> default=1500 format=int32 min=1280 max=9000	# mtu is the MTU of the interface
spec.networkData.links.bonds[].name <string> pattern="^[a-zA-Z][a-zA-Z0-9._-]*$" min=1 max=15	# name is the interface name to be used by cloud-init. When combined with macAddress, cloud-init will rename the interface matching the MAC to this name. When macAddress is omitted, cloud-init will use this name directly.
spec.networkData.links.bonds[].parameters <[]object> (required) min=1 max=512	# parameters blob passed without any validation/modifications into cloud-init config
spec.networkData.links.bonds[].parameters[].name <string> (required) min=1 max=512	# name is the parameter name
spec.networkData.links.bonds[].parameters[].value <object (free-form)>	# value is the parameter value
spec.networkData.links.ethernets <[]object> min=1 max=32	# ethernets contains a list of Ethernet links
spec.networkData.links.ethernets[].id <string> (required) min=1 max=512	# id is the ID of the interface (used for naming)
spec.networkData.links.ethernets[].macAddress <object> (required)	# macAddress is the MAC address of the interface, containing the object used to render it.
spec.networkData.links.ethernets[].macAddress.fromAnnotation <object>	# fromAnnotation references an object annotation to retrieve the MAC address from
spec.networkData.links.ethernets[].macAddress.fromAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation to fetch
spec.networkData.links.ethernets[].macAddress.fromAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the name
spec.networkData.links.ethernets[].macAddress.fromHostInterface <string> min=1 max=512	# fromHostInterface contains the name of the interface in the BareMetalHost Introspection details from which to fetch the MAC address
spec.networkData.links.ethernets[].macAddress.string <string> min=1 max=512	# string contains the MAC address given as a string
spec.networkData.links.ethernets[].mtu <integer> default=1500 format=int32 min=1280 max=9000	# mtu is the MTU of the interface
spec.networkData.links.ethernets[].name <string> pattern="^[a-zA-Z][a-zA-Z0-9._-]*$" min=1 max=15	# name is the interface name to be used by cloud-init. When combined with MACAddress, cloud-init will rename the interface matching the MAC to this name. When MACAddress is omitted, cloud-init will use this name directly.
spec.networkData.links.ethernets[].type <string> (required) enum=bridge|dvs|hw_veb|hyperv|ovs|tap|vhostuser|vif|phy	# type is the type of the ethernet link. It can be one of: bridge, dvs, hw_veb, hyperv, ovs, tap, vhostuser, vif, phy
spec.networkData.links.vlans <[]object> min=1 max=512	# vlans contains a list of Vlan links
spec.networkData.links.vlans[].id <string> (required) min=1 max=512	# id is the ID of the interface (used for naming)
spec.networkData.links.vlans[].macAddress <object> (required)	# macAddress is the MAC address of the interface, containing the object used to render it.
spec.networkData.links.vlans[].macAddress.fromAnnotation <object>	# fromAnnotation references an object annotation to retrieve the MAC address from
spec.networkData.links.vlans[].macAddress.fromAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation to fetch
spec.networkData.links.vlans[].macAddress.fromAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the name
spec.networkData.links.vlans[].macAddress.fromHostInterface <string> min=1 max=512	# fromHostInterface contains the name of the interface in the BareMetalHost Introspection details from which to fetch the MAC address
spec.networkData.links.vlans[].macAddress.string <string> min=1 max=512	# string contains the MAC address given as a string
spec.networkData.links.vlans[].mtu <integer> default=1500 format=int32 min=1280 max=9000	# mtu is the MTU of the interface
spec.networkData.links.vlans[].name <string> pattern="^[a-zA-Z][a-zA-Z0-9._-]*$" min=1 max=15	# name is the interface name to be used by cloud-init. When combined with macAddress, cloud-init will rename the interface matching the MAC to this name. When macAddress is omitted, cloud-init will use this name directly.
spec.networkData.links.vlans[].vlanID <integer> (required) format=int32 min=0 max=4096	# vlanID is the Vlan ID
spec.networkData.links.vlans[].vlanLink <string> (required) min=1 max=512	# vlanLink is the name of the link on which the vlan should be added
spec.networkData.networks <object>	# networks is a structure containing lists of different types objects
spec.networkData.networks.ipv4 <[]object> min=1 max=512	# ipv4 contains a list of IPv4 static allocations
spec.networkData.networks.ipv4[].fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool reference via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, ipAddressFromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv4[].fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv4[].fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv4[].fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to allocate an address from.
spec.networkData.networks.ipv4[].fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv4[].fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv4[].fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv4[].id <string> (required) min=1 max=512	# id is the network ID (name)
spec.networkData.networks.ipv4[].ipAddressFromIPPool <string> min=1 max=512	# ipAddressFromIPPool contains the name of the IP pool to use to get an ip address
spec.networkData.networks.ipv4[].link <string> (required) min=1 max=512	# link is the link on which the network applies
spec.networkData.networks.ipv4[].routes <[]object> min=1 max=512	# routes contains a list of IPv4 routes
spec.networkData.networks.ipv4[].routes[].gateway <object> (required)	# gateway is the IPv4 address of the gateway
spec.networkData.networks.ipv4[].routes[].gateway.fromIPPool <string> min=1 max=512	# fromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, fromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv4[].routes[].gateway.string <string> pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# string is the gateway given as a string
spec.networkData.networks.ipv4[].routes[].network <string> (required) pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# network is the IPv4 network address
spec.networkData.networks.ipv4[].routes[].prefix <integer> format=int32 min=0 max=32	# prefix is the mask of the network as integer (max 32)
spec.networkData.networks.ipv4[].routes[].services <object>	# services is a list of IPv4 services
spec.networkData.networks.ipv4[].routes[].services.dns <[]string> min=1 max=512	# dns is a list of IPv4 DNS services
spec.networkData.networks.ipv4[].routes[].services.dnsFromIPPool <string> min=1 max=512	# dnsFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv4DHCP <[]object> min=1 max=512	# ipv4DHCP contains a list of IPv4 DHCP allocations
spec.networkData.networks.ipv4DHCP[].id <string> (required) min=1 max=512	# id is the network ID (name)
spec.networkData.networks.ipv4DHCP[].link <string> (required) min=1 max=512	# link is the link on which the network applies
spec.networkData.networks.ipv4DHCP[].routes <[]object> min=1 max=512	# routes contains a list of IPv4 routes
spec.networkData.networks.ipv4DHCP[].routes[].gateway <object> (required)	# gateway is the IPv4 address of the gateway
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromIPPool <string> min=1 max=512	# fromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, fromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.string <string> pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# string is the gateway given as a string
spec.networkData.networks.ipv4DHCP[].routes[].network <string> (required) pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# network is the IPv4 network address
spec.networkData.networks.ipv4DHCP[].routes[].prefix <integer> format=int32 min=0 max=32	# prefix is the mask of the network as integer (max 32)
spec.networkData.networks.ipv4DHCP[].routes[].services <object>	# services is a list of IPv4 services
spec.networkData.networks.ipv4DHCP[].routes[].services.dns <[]string> min=1 max=512	# dns is a list of IPv4 DNS services
spec.networkData.networks.ipv4DHCP[].routes[].services.dnsFromIPPool <string> min=1 max=512	# dnsFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv6 <[]object> min=1 max=512	# ipv6 contains a list of IPv6 static allocations
spec.networkData.networks.ipv6[].fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool reference via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, ipAddressFromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv6[].fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6[].fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6[].fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to allocate an address from.
spec.networkData.networks.ipv6[].fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv6[].fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv6[].fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv6[].id <string> (required) min=1 max=512	# id is the network ID (name)
spec.networkData.networks.ipv6[].ipAddressFromIPPool <string> min=1 max=512	# ipAddressFromIPPool contains the name of the IPPool to use to get an ip address
spec.networkData.networks.ipv6[].link <string> (required) min=1 max=512	# link is the link on which the network applies
spec.networkData.networks.ipv6[].routes <[]object> min=1 max=512	# routes contains a list of IPv6 routes
spec.networkData.networks.ipv6[].routes[].gateway <object> (required)	# gateway is the IPv6 address of the gateway
spec.networkData.networks.ipv6[].routes[].gateway.fromIPPool <string> min=1 max=512	# fromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, fromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv6[].routes[].gateway.string <string> pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# string is the gateway given as a string
spec.networkData.networks.ipv6[].routes[].network <string> (required) pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# network is the IPv6 network address
spec.networkData.networks.ipv6[].routes[].prefix <integer> format=int32 min=0 max=128	# prefix is the mask of the network as integer (max 128)
spec.networkData.networks.ipv6[].routes[].services <object>	# services is a list of IPv6 services
spec.networkData.networks.ipv6[].routes[].services.dns <[]string> min=1 max=512	# dns is a list of IPv6 DNS services
spec.networkData.networks.ipv6[].routes[].services.dnsFromIPPool <string> min=1 max=512	# dnsFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv6DHCP <[]object> min=1 max=512	# ipv6DHCP contains a list of IPv6 DHCP allocations
spec.networkData.networks.ipv6DHCP[].id <string> (required) min=1 max=512	# id is the network ID (name)
spec.networkData.networks.ipv6DHCP[].link <string> (required) min=1 max=512	# link is the link on which the network applies
spec.networkData.networks.ipv6DHCP[].routes <[]object> min=1 max=512	# routes contains a list of IPv6 routes
spec.networkData.networks.ipv6DHCP[].routes[].gateway <object> (required)	# gateway is the IPv6 address of the gateway
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromIPPool <string> min=1 max=512	# fromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, fromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.string <string> pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# string is the gateway given as a string
spec.networkData.networks.ipv6DHCP[].routes[].network <string> (required) pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# network is the IPv6 network address
spec.networkData.networks.ipv6DHCP[].routes[].prefix <integer> format=int32 min=0 max=128	# prefix is the mask of the network as integer (max 128)
spec.networkData.networks.ipv6DHCP[].routes[].services <object>	# services is a list of IPv6 services
spec.networkData.networks.ipv6DHCP[].routes[].services.dns <[]string> min=1 max=512	# dns is a list of IPv6 DNS services
spec.networkData.networks.ipv6DHCP[].routes[].services.dnsFromIPPool <string> min=1 max=512	# dnsFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv6SLAAC <[]object> min=1 max=512	# ipv6SLAAC contains a list of IPv6 SLAAC allocations
spec.networkData.networks.ipv6SLAAC[].id <string> (required) min=1 max=512	# id is the network ID (name)
spec.networkData.networks.ipv6SLAAC[].link <string> (required) min=1 max=512	# link is the link on which the network applies
spec.networkData.networks.ipv6SLAAC[].routes <[]object> min=1 max=512	# routes contains a list of IPv6 routes
spec.networkData.networks.ipv6SLAAC[].routes[].gateway <object> (required)	# gateway is the IPv6 address of the gateway
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromIPPool <string> min=1 max=512	# fromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolAnnotation <object>	# fromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, fromIPPool and fromPoolRef are ignored.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolAnnotation.annotation <string> (required) min=1 max=512	# annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef <object>	# fromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef.apiGroup <string> default="ipam.metal3.io" pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# apiGroup of the IPPool. apiGroup must be fully qualified domain name.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef.kind <string> default="IPPool" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# kind of the IPPool. kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef.name <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# name of the IPPool. name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.string <string> pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# string is the gateway given as a string
spec.networkData.networks.ipv6SLAAC[].routes[].network <string> (required) pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# network is the IPv6 network address
spec.networkData.networks.ipv6SLAAC[].routes[].prefix <integer> format=int32 min=0 max=128	# prefix is the mask of the network as integer (max 128)
spec.networkData.networks.ipv6SLAAC[].routes[].services <object>	# services is a list of IPv6 services
spec.networkData.networks.ipv6SLAAC[].routes[].services.dns <[]string> min=1 max=512	# dns is a list of IPv6 DNS services
spec.networkData.networks.ipv6SLAAC[].routes[].services.dnsFromIPPool <string> min=1 max=512	# dnsFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.services <object>	# services is a structure containing lists of different types objects
spec.networkData.services.dns <[]string> min=1 max=512	# dns is a list of DNS services
spec.networkData.services.dnsFromIPPool <string> min=1 max=512	# dnsFromIPPool is the name of the IPPool from which to get the DNS servers
status <object>	# status defines the observed state of Metal3DataTemplate.
status.indexes <[]object> min=1 max=512	# indexes contains the list of Metal3Machines and their assigned indexes
status.indexes[].index <integer> (required) format=int32 min=0	# index is the index value assigned to the Machine
status.indexes[].name <string> (required) min=1 max=512	# name is the name of the Metal3Machine
status.lastUpdated <string> format=date-time	# lastUpdated identifies when this status was last observed.
