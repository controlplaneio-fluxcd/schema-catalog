# schema source: Cluster API Metal3 v1.13.2 https://github.com/metal3-io/cluster-api-provider-metal3
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta2
kind <string> enum=Metal3RemediationTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# spec defines the desired state of Metal3RemediationTemplate.
spec.template <object> (required)	# template describes the data needed to create a Metal3Remediation from a template
spec.template.spec <object> (required)	# spec is the specification of the desired behavior of the Metal3Remediation.
spec.template.spec.strategy <object>	# strategy field defines remediation strategy.
spec.template.spec.strategy.retryLimit <integer> default=1 format=int32 min=1	# retryLimit sets maximum number of remediation retries. retryLimit must be a positive integer. The default is 1.
spec.template.spec.strategy.timeoutSeconds <integer> default=600 format=int32 min=100	# timeoutSeconds defines the timeout between remediation retries. The minimum allowed value is 100 seconds. The default is 600 seconds.
spec.template.spec.strategy.type <string> enum=Reboot	# type of remediation.
status <object>	# status defines the observed state of Metal3RemediationTemplate.
status.status <object>	# status defines the observed state of Metal3Remediation
status.status.lastRemediated <string> format=date-time	# lastRemediated identifies when the host was last remediated
status.status.phase <string> enum=Running|Waiting|"Deleting machine"|Failed	# phase represents the current phase of machine remediation. E.g. Pending, Running, Done etc.
status.status.retryCount <integer> format=int32 min=1	# retryCount can be used as a counter during the remediation. Field can hold number of reboots etc.
