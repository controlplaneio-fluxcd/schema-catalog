# schema source: Cluster API Hetzner v1.1.7 https://github.com/syself/cluster-api-provider-hetzner
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta1
kind <string> enum=HetznerBareMetalRemediationTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# HetznerBareMetalRemediationTemplateSpec defines the desired state of HetznerBareMetalRemediationTemplate.
spec.template <object> (required)	# HetznerBareMetalRemediationTemplateResource describes the data needed to create a HetznerBareMetalRemediation from a template.
spec.template.spec <object> (required)	# Spec is the specification of the desired behavior of the HetznerBareMetalRemediation.
spec.template.spec.strategy <object>	# Strategy field defines the remediation strategy to be applied.
spec.template.spec.strategy.cooldown <string> default="30m"	# Cooldown is the minimum time between successive remediations of the same machine. If a new remediation starts within this window of the previous successful one, the reboot is skipped and the machine is deleted. Set to 0s to disable the guard.
spec.template.spec.strategy.retryLimit <integer>	# RetryLimit sets the maximum number of remediation retries. Zero retries if not set.
spec.template.spec.strategy.timeout <string> (required)	# Timeout sets the timeout between remediation retries. It should be of the form "10m", or "40s".
spec.template.spec.strategy.type <string> default="Reboot"	# Type represents the type of the remediation strategy. At the moment, only "Reboot" is supported.
status <object>	# HetznerBareMetalRemediationTemplateStatus defines the observed state of HetznerBareMetalRemediationTemplate.
status.status <object> (required)	# HetznerBareMetalRemediationStatus defines the observed state of HetznerBareMetalRemediation
status.status.lastRemediated <string> format=date-time	# LastRemediated identifies when the host was last remediated
status.status.phase <string>	# Phase represents the current phase of machine remediation. E.g. Pending, Running, Done etc.
status.status.retryCount <integer>	# RetryCount can be used as a counter during the remediation. Field can hold number of reboots etc.
