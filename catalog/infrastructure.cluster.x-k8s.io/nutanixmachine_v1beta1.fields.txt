# schema source: Cluster API Nutanix v1.8.5 https://github.com/nutanix-cloud-native/cluster-api-provider-nutanix
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta1
kind <string> enum=NutanixMachine
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# NutanixMachineSpec defines the desired state of NutanixMachine
spec.additionalCategories <[]object>	# List of categories that need to be added to the machines. Categories must already exist in Prism Central
spec.additionalCategories[].key <string>	# key is the Key of category in PC.
spec.additionalCategories[].value <string>	# value is the category value linked to the category key in PC
spec.bootType <string> enum=legacy|uefi	# Defines the boot type of the virtual machine. Only supports UEFI and Legacy
spec.bootstrapRef <object>	# BootstrapRef is a reference to a bootstrap provider-specific resource that holds configuration details.
spec.bootstrapRef.apiVersion <string>	# API version of the referent.
spec.bootstrapRef.fieldPath <string>	# If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object.
spec.bootstrapRef.kind <string>	# Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
spec.bootstrapRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.bootstrapRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.bootstrapRef.resourceVersion <string>	# Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
spec.bootstrapRef.uid <string>	# UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
spec.cluster <object>	# cluster is to identify the cluster (the Prism Element under management of the Prism Central), in which the Machine's VM will be created. The cluster identifier (uuid or name) can be obtained from the Prism Central console or using the prism_central API.
spec.cluster.name <string>	# name is the resource name in the PC
spec.cluster.type <string> (required) enum=uuid|name	# Type is the identifier type to use for this resource.
spec.cluster.uuid <string>	# uuid is the UUID of the resource in the PC.
spec.dataDisks <[]object>	# dataDisks hold the list of data disks to be attached to the VM
spec.dataDisks[].dataSource <object>	# dataSource refers to a data source image for the VM disk.
spec.dataDisks[].dataSource.name <string>	# name is the resource name in the PC
spec.dataDisks[].dataSource.type <string> (required) enum=uuid|name	# Type is the identifier type to use for this resource.
spec.dataDisks[].dataSource.uuid <string>	# uuid is the UUID of the resource in the PC.
spec.dataDisks[].deviceProperties <object>	# deviceProperties are the properties of the disk device.
spec.dataDisks[].deviceProperties.adapterType <string> (required) enum=SCSI|IDE|PCI|SATA|SPAPR	# adapterType is the adapter type of the disk address. If the deviceType is "Disk", the valid adapterType can be "SCSI", "IDE", "PCI", "SATA" or "SPAPR". If the deviceType is "CDRom", the valid adapterType can be "IDE" or "SATA".
spec.dataDisks[].deviceProperties.deviceIndex <integer> default=0 format=int32 min=0	# deviceIndex is the index of the disk address. The valid values are non-negative integers, with the default value 0. For a Machine VM, the deviceIndex for the disks with the same deviceType.adapterType combination should start from 0 and increase consecutively afterwards. Note that for each Machine VM, the Disk.SCSI.0 and CDRom.IDE.0 are reserved to be used by the VM's system. So for dataDisks of Disk.SCSI and CDRom.IDE, the deviceIndex should start from 1.
spec.dataDisks[].deviceProperties.deviceType <string> (required) enum=Disk|CDRom default="Disk"	# deviceType specifies the disk device type. The valid values are "Disk" and "CDRom", and the default is "Disk".
spec.dataDisks[].diskSize <int-or-string> (required)	# diskSize is the size (in Quantity format) of the disk attached to the VM. See https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Format for the Quantity format and example documentation. The minimum diskSize is 1GB.
spec.dataDisks[].storageConfig <object>	# storageConfig are the storage configuration parameters of the VM disks.
spec.dataDisks[].storageConfig.diskMode <string> (required) enum=Standard|Flash default="Standard"	# diskMode specifies the disk mode. The valid values are Standard and Flash, and the default is Standard.
spec.dataDisks[].storageConfig.storageContainer <object>	# storageContainer refers to the storage_container used by the VM disk.
spec.dataDisks[].storageConfig.storageContainer.name <string>	# name is the resource name in the PC
spec.dataDisks[].storageConfig.storageContainer.type <string> (required) enum=uuid|name	# Type is the identifier type to use for this resource.
spec.dataDisks[].storageConfig.storageContainer.uuid <string>	# uuid is the UUID of the resource in the PC.
spec.gpus <[]object>	# List of GPU devices that need to be added to the machines.
spec.gpus[].deviceID <integer> format=int64	# deviceID is the id of the GPU entity.
spec.gpus[].name <string>	# name is the GPU name
spec.gpus[].type <string> (required) enum=deviceID|name	# Type is the identifier type to use for this resource.
spec.image <object>	# image is to identify the nutanix machine image uploaded to the Prism Central (PC) The image identifier (uuid or name) can be obtained from the Prism Central console or using the prism_central API.
spec.image.name <string>	# name is the resource name in the PC
spec.image.type <string> (required) enum=uuid|name	# Type is the identifier type to use for this resource.
spec.image.uuid <string>	# uuid is the UUID of the resource in the PC.
spec.imageLookup <object>	# imageLookup is a container that holds how to look up rhcos images for the cluster.
spec.imageLookup.baseOS <string> (required) min=1	# BaseOS is the name of the base operating system to use for image lookup.
spec.imageLookup.format <string> default="capx-{{.BaseOS}}-{{.K8sVersion}}-*"	# Format is the naming format to look up the image for this machine It will be ignored if an explicit image is set. Supports substitutions for {{.BaseOS}} and {{.K8sVersion}} with the base OS and kubernetes version, respectively. The BaseOS will be the value in BaseOS and the K8sVersion is the value in the Machine .spec.version, with the v prefix removed. This is effectively the defined by the packages produced by kubernetes/release without v as a prefix: 1.13.0, 1.12.5-mybuild.1, or 1.17.3. For example, the default image format of {{.BaseOS}}-?{{.K8sVersion}}-* and BaseOS as "rhel-8.10" will end up searching for images that match the pattern rhel-8.10-1.30.5-* for a Machine that is targeting kubernetes v1.30.5. See also: https://golang.org/pkg/text/template/
spec.memorySize <int-or-string> (required)	# memorySize is the memory size (in Quantity format) of the VM The minimum memorySize is 2Gi bytes
spec.project <object>	# Add the machine resources to a Prism Central project
spec.project.name <string>	# name is the resource name in the PC
spec.project.type <string> (required) enum=uuid|name	# Type is the identifier type to use for this resource.
spec.project.uuid <string>	# uuid is the UUID of the resource in the PC.
spec.providerID <string>	# ProviderID is the unique identifier as specified by the cloud provider.
spec.subnet <[]object>	# subnet is to identify the cluster's network subnet to use for the Machine's VM The cluster identifier (uuid or name) can be obtained from the Prism Central console or using the prism_central API.
spec.subnet[].name <string>	# name is the resource name in the PC
spec.subnet[].type <string> (required) enum=uuid|name	# Type is the identifier type to use for this resource.
spec.subnet[].uuid <string>	# uuid is the UUID of the resource in the PC.
spec.systemDiskSize <int-or-string> (required)	# systemDiskSize is size (in Quantity format) of the system disk of the VM The minimum systemDiskSize is 20Gi bytes
spec.vcpuSockets <integer> (required) format=int32 min=1	# vcpuSockets is the number of vCPU sockets of the VM
spec.vcpusPerSocket <integer> (required) format=int32 min=1	# vcpusPerSocket is the number of vCPUs per socket of the VM
status <object>	# NutanixMachineStatus defines the observed state of NutanixMachine
status.addresses <[]object>	# Addresses contains the Nutanix VM associated addresses. Address type is one of Hostname, ExternalIP, InternalIP, ExternalDNS, InternalDNS
status.addresses[].address <string> (required) min=1 max=256	# address is the machine address.
status.addresses[].type <string> (required) enum=Hostname|ExternalIP|InternalIP|ExternalDNS|InternalDNS	# type is the machine address type, one of Hostname, ExternalIP, InternalIP, ExternalDNS or InternalDNS.
status.conditions <[]object>	# Conditions defines current service state of the NutanixMachine.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> min=1 max=10240	# message is a human readable message indicating details about the transition. This field may be empty.
status.conditions[].reason <string> min=1 max=256	# reason is the reason for the condition's last transition in CamelCase. The specific API may choose whether or not this field is considered a guaranteed API. This field may be empty.
status.conditions[].severity <string> max=32	# severity provides an explicit classification of Reason code, so the users or machines can immediately understand the current situation and act accordingly. The Severity field MUST be set only when Status=False.
status.conditions[].status <string> (required)	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) min=1 max=256	# type of condition in CamelCase or in foo.example.com/CamelCase. Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important.
status.failureDomain <string>	# failureDomain is the name of the failure domain where this Machine has been placed in.
status.failureMessage <string>	# Will be set in case of failure of Machine instance
status.failureReason <string>	# Will be set in case of failure of Machine instance
status.nodeRef <object>	# NodeRef is a reference to the corresponding workload cluster Node if it exists. Deprecated: Do not use. Will be removed in a future release.
status.nodeRef.apiVersion <string>	# API version of the referent.
status.nodeRef.fieldPath <string>	# If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object.
status.nodeRef.kind <string>	# Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
status.nodeRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
status.nodeRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
status.nodeRef.resourceVersion <string>	# Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
status.nodeRef.uid <string>	# UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
status.ready <boolean>	# Ready is true when the provider resource is ready.
status.vmUUID <string>	# The Nutanix VM's UUID
