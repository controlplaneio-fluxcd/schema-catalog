# schema source: Cluster API Metal3 v1.13.1 https://github.com/metal3-io/cluster-api-provider-metal3
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta2
kind <string> enum=Metal3Data
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# spec defines the desired state of Metal3Data.
spec.claim <object>	# claim points to the Metal3DataClaim the Metal3Data was created for.
spec.claim.name <string> min=1 max=253	# name of the resource.
spec.claim.namespace <string> min=1 max=253	# namespace of the resource.
spec.index <integer> format=int32 min=0	# index stores the index value of this instance in the Metal3DataTemplate.
spec.metaData <object>	# metaData points to the rendered MetaData secret.
spec.metaData.name <string>	# name is unique within a namespace to reference a secret resource.
spec.metaData.namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.networkData <object>	# networkData points to the rendered NetworkData secret.
spec.networkData.name <string>	# name is unique within a namespace to reference a secret resource.
spec.networkData.namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.template <object>	# template is the Metal3DataTemplate this was generated from.
spec.template.name <string> min=1 max=253	# name of the resource.
spec.template.namespace <string> min=1 max=253	# namespace of the resource.
status <object>	# status defines the observed state of Metal3Data.
status.errorMessage <string> min=1 max=512	# errorMessage contains the error message
status.ready <boolean>	# ready is a flag set to True if the secrets were rendered properly
