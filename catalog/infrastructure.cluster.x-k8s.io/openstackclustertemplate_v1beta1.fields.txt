# schema source: Cluster API OpenStack v0.14.6 https://github.com/kubernetes-sigs/cluster-api-provider-openstack
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta1
kind <string> enum=OpenStackClusterTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# OpenStackClusterTemplateSpec defines the desired state of OpenStackClusterTemplate.
spec.template <object> (required)	# OpenStackClusterTemplateResource describes the data needed to create a OpenStackCluster from a template.
spec.template.spec <object> (required)	# OpenStackClusterSpec defines the desired state of OpenStackCluster.
spec.template.spec.apiServerFixedIP <string>	# APIServerFixedIP is the fixed IP which will be associated with the API server. In the case where the API server has a floating IP but not a managed load balancer, this field is not used. If a managed load balancer is used and this field is not specified, a fixed IP will be dynamically allocated for the load balancer. If a managed load balancer is not used AND the API server floating IP is disabled, this field MUST be specified and should correspond to a pre-allocated port that holds the fixed IP to be used as a VIP.
spec.template.spec.apiServerFloatingIP <string>	# APIServerFloatingIP is the floatingIP which will be associated with the API server. The floatingIP will be created if it does not already exist. If not specified, a new floatingIP is allocated. This field is not used if DisableAPIServerFloatingIP is set to true.
spec.template.spec.apiServerLoadBalancer <object>	# APIServerLoadBalancer configures the optional LoadBalancer for the APIServer. If not specified, no load balancer will be created for the API server.
spec.template.spec.apiServerLoadBalancer.additionalPorts <[]integer>	# AdditionalPorts adds additional tcp ports to the load balancer.
spec.template.spec.apiServerLoadBalancer.allowedCIDRs <[]string>	# AllowedCIDRs restrict access to all API-Server listeners to the given address CIDRs.
spec.template.spec.apiServerLoadBalancer.availabilityZone <string>	# AvailabilityZone is the failure domain that will be used to create the APIServerLoadBalancer Spec.
spec.template.spec.apiServerLoadBalancer.enabled <boolean> (required) default=true	# Enabled defines whether a load balancer should be created. This value defaults to true if an APIServerLoadBalancer is given. There is no reason to set this to false. To disable creation of the API server loadbalancer, omit the APIServerLoadBalancer field in the cluster spec instead.
spec.template.spec.apiServerLoadBalancer.flavor <string>	# Flavor is the flavor name that will be used to create the APIServerLoadBalancer Spec.
spec.template.spec.apiServerLoadBalancer.monitor <object>	# Monitor contains configuration for the load balancer health monitor.
spec.template.spec.apiServerLoadBalancer.monitor.delay <integer> min=0	# Delay is the time in seconds between sending probes to members.
spec.template.spec.apiServerLoadBalancer.monitor.maxRetries <integer> min=0 max=10	# MaxRetries is the number of successful checks before changing the operating status of the member to ONLINE.
spec.template.spec.apiServerLoadBalancer.monitor.maxRetriesDown <integer> min=1 max=10	# MaxRetriesDown is the number of allowed check failures before changing the operating status of the member to ERROR.
spec.template.spec.apiServerLoadBalancer.monitor.timeout <integer> min=0	# Timeout is the maximum time in seconds for a monitor to wait for a connection to be established before it times out.
spec.template.spec.apiServerLoadBalancer.network <object>	# Network defines which network should the load balancer be allocated on.
spec.template.spec.apiServerLoadBalancer.network.filter <object>	# Filter specifies a filter to select an OpenStack network. If provided, cannot be empty.
spec.template.spec.apiServerLoadBalancer.network.filter.description <string>
spec.template.spec.apiServerLoadBalancer.network.filter.name <string>
spec.template.spec.apiServerLoadBalancer.network.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.apiServerLoadBalancer.network.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.apiServerLoadBalancer.network.filter.projectID <string>
spec.template.spec.apiServerLoadBalancer.network.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.apiServerLoadBalancer.network.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.apiServerLoadBalancer.network.id <string> format=uuid	# ID is the ID of the network to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.apiServerLoadBalancer.provider <string>	# Provider specifies name of a specific Octavia provider to use for the API load balancer. The Octavia default will be used if it is not specified.
spec.template.spec.apiServerLoadBalancer.subnets <[]object>	# Subnets define which subnets should the load balancer be allocated on. It is expected that subnets are located on the network specified in this resource. Only the first element is taken into account. kubebuilder:validation:MaxLength:=2
spec.template.spec.apiServerLoadBalancer.subnets[].filter <object>	# Filter specifies a filter to select the subnet. It must match exactly one subnet.
spec.template.spec.apiServerLoadBalancer.subnets[].filter.cidr <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.description <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.gatewayIP <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.ipVersion <integer>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.ipv6AddressMode <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.ipv6RAMode <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.name <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.apiServerLoadBalancer.subnets[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.apiServerLoadBalancer.subnets[].filter.projectID <string>
spec.template.spec.apiServerLoadBalancer.subnets[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.apiServerLoadBalancer.subnets[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.apiServerLoadBalancer.subnets[].id <string> format=uuid	# ID is the uuid of the subnet. It will not be validated.
spec.template.spec.apiServerPort <integer> min=0 max=65535	# APIServerPort is the port on which the listener on the APIServer will be created. If specified, it must be an integer between 0 and 65535.
spec.template.spec.bastion <object>	# Bastion is the OpenStack instance to login the nodes As a rolling update is not ideal during a bastion host session, we prevent changes to a running bastion configuration. To make changes, it's required to first set `enabled: false` which will remove the bastion and then changes can be made.
spec.template.spec.bastion.availabilityZone <string>	# AvailabilityZone is the failure domain that will be used to create the Bastion Spec.
spec.template.spec.bastion.enabled <boolean> default=true	# Enabled means that bastion is enabled. The bastion is enabled by default if this field is not specified. Set this field to false to disable the bastion. It is not currently possible to remove the bastion from the cluster spec without first disabling it by setting this field to false and waiting until the bastion has been deleted.
spec.template.spec.bastion.floatingIP <string> format=ipv4	# FloatingIP which will be associated to the bastion machine. It's the IP address, not UUID. The floating IP should already exist and should not be associated with a port. If FIP of this address does not exist, CAPO will try to create it, but by default only OpenStack administrators have privileges to do so.
spec.template.spec.bastion.spec <object>	# Spec for the bastion itself
spec.template.spec.bastion.spec.additionalBlockDevices <[]object>	# AdditionalBlockDevices is a list of specifications for additional block devices to attach to the server instance
spec.template.spec.bastion.spec.additionalBlockDevices[].name <string> (required)	# Name of the block device in the context of a machine. If the block device is a volume, the Cinder volume will be named as a combination of the machine name and this name. Also, this name will be used for tagging the block device. Information about the block device tag can be obtained from the OpenStack metadata API or the config drive. Name cannot be 'root', which is reserved for the root volume.
spec.template.spec.bastion.spec.additionalBlockDevices[].sizeGiB <integer> (required) min=1	# SizeGiB is the size of the block device in gibibytes (GiB).
spec.template.spec.bastion.spec.additionalBlockDevices[].storage <object> (required)	# Storage specifies the storage type of the block device and additional storage options.
spec.template.spec.bastion.spec.additionalBlockDevices[].storage.type <string> (required)	# Type is the type of block device to create. This can be either "Volume" or "Local".
spec.template.spec.bastion.spec.additionalBlockDevices[].storage.volume <object>	# Volume contains additional storage options for a volume block device.
spec.template.spec.bastion.spec.additionalBlockDevices[].storage.volume.availabilityZone <object>	# AvailabilityZone is the volume availability zone to create the volume in. If not specified, the volume will be created without an explicit availability zone.
spec.template.spec.bastion.spec.additionalBlockDevices[].storage.volume.availabilityZone.from <string> enum=Name|Machine default="Name"	# From specifies where we will obtain the availability zone for the volume. The options are "Name" and "Machine". If "Name" is specified then the Name field must also be specified. If "Machine" is specified the volume will use the value of FailureDomain, if any, from the associated Machine.
spec.template.spec.bastion.spec.additionalBlockDevices[].storage.volume.availabilityZone.name <string> pattern="^[^ ]+$" min=1	# Name is the name of a volume availability zone to use. It is required if From is "Name". The volume availability zone name may not contain spaces.
spec.template.spec.bastion.spec.additionalBlockDevices[].storage.volume.type <string>	# Type is the Cinder volume type of the volume. If omitted, the default Cinder volume type that is configured in the OpenStack cloud will be used.
spec.template.spec.bastion.spec.configDrive <boolean>	# Config Drive support
spec.template.spec.bastion.spec.flavor <string> min=1	# The flavor reference for the flavor for your server instance.
spec.template.spec.bastion.spec.flavorID <string> min=1	# FlavorID allows flavors to be specified by ID. This field takes precedence over Flavor.
spec.template.spec.bastion.spec.floatingIPPoolRef <object>	# floatingIPPoolRef is a reference to a IPPool that will be assigned to an IPAddressClaim. Once the IPAddressClaim is fulfilled, the FloatingIP will be assigned to the OpenStackMachine.
spec.template.spec.bastion.spec.floatingIPPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.template.spec.bastion.spec.floatingIPPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.template.spec.bastion.spec.floatingIPPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.template.spec.bastion.spec.identityRef <object>	# IdentityRef is a reference to a secret holding OpenStack credentials to be used when reconciling this machine. If not specified, the credentials specified in the cluster will be used.
spec.template.spec.bastion.spec.identityRef.cloudName <string> (required) min=1	# CloudName specifies the name of the entry in the clouds.yaml file to use.
spec.template.spec.bastion.spec.identityRef.name <string> (required) min=1	# Name is the name of a Secret (type=Secret) in the same namespace as the resource being provisioned, or the name of an OpenStackClusterIdentity (type=ClusterIdentity). The Secret must contain a key named `clouds.yaml` which contains an OpenStack clouds.yaml file. The Secret may optionally contain a key named `cacert` containing a PEM-encoded CA certificate.
spec.template.spec.bastion.spec.identityRef.region <string>	# Region specifies an OpenStack region to use. If specified, it overrides any value in clouds.yaml. If specified for an OpenStackMachine, its value will be included in providerID.
spec.template.spec.bastion.spec.identityRef.type <string> (required) enum=Secret|ClusterIdentity default="Secret"	# Type specifies the identity reference type. Defaults to Secret for backward compatibility.
spec.template.spec.bastion.spec.image <object> (required)	# The image to use for your server instance. If the rootVolume is specified, this will be used when creating the root volume.
spec.template.spec.bastion.spec.image.filter <object>	# Filter describes a query for an image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.template.spec.bastion.spec.image.filter.name <string>	# The name of the desired image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.template.spec.bastion.spec.image.filter.tags <[]string>	# The tags associated with the desired image. If specified, the combination of name and tags must return a single matching image or an error will be raised.
spec.template.spec.bastion.spec.image.id <string> format=uuid	# ID is the uuid of the image. ID will not be validated before use.
spec.template.spec.bastion.spec.image.imageRef <object>	# ImageRef is a reference to an ORC Image in the same namespace as the referring object.
spec.template.spec.bastion.spec.image.imageRef.name <string> (required)	# Name is the name of the referenced resource
spec.template.spec.bastion.spec.ports <[]object>	# Ports to be attached to the server instance. They are created if a port with the given name does not already exist. If not specified a default port will be added for the default cluster network.
spec.template.spec.bastion.spec.ports[].adminStateUp <boolean>	# AdminStateUp specifies whether the port should be created in the up (true) or down (false) state. The default is up.
spec.template.spec.bastion.spec.ports[].allowedAddressPairs <[]object>	# AllowedAddressPairs is a list of address pairs which Neutron will allow the port to send traffic from in addition to the port's addresses. If not specified, the MAC Address will be the MAC Address of the port. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
spec.template.spec.bastion.spec.ports[].allowedAddressPairs[].ipAddress <string> (required)	# IPAddress is the IP address of the allowed address pair. Depending on the configuration of Neutron, it may be supported to specify a CIDR instead of a specific IP address.
spec.template.spec.bastion.spec.ports[].allowedAddressPairs[].macAddress <string>	# MACAddress is the MAC address of the allowed address pair. If not specified, the MAC address will be the MAC address of the port.
spec.template.spec.bastion.spec.ports[].description <string>	# Description is a human-readable description for the port.
spec.template.spec.bastion.spec.ports[].disablePortSecurity <boolean>	# DisablePortSecurity enables or disables the port security when set. When not set, it takes the value of the corresponding field at the network level.
spec.template.spec.bastion.spec.ports[].fixedIPs <[]object>	# FixedIPs is a list of pairs of subnet and/or IP address to assign to the port. If specified, these must be subnets of the port's network.
spec.template.spec.bastion.spec.ports[].fixedIPs[].ipAddress <string>	# IPAddress is a specific IP address to assign to the port. If Subnet is also specified, IPAddress must be a valid IP address in the subnet. If Subnet is not specified, IPAddress must be a valid IP address in any subnet of the port's network.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet <object>	# Subnet is an openstack subnet query that will return the id of a subnet to create the fixed IP of a port in. This query must not return more than one subnet.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter <object>	# Filter specifies a filter to select the subnet. It must match exactly one subnet.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.cidr <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.description <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.gatewayIP <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.ipVersion <integer>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.ipv6AddressMode <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.ipv6RAMode <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.name <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.projectID <string>
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.bastion.spec.ports[].fixedIPs[].subnet.id <string> format=uuid	# ID is the uuid of the subnet. It will not be validated.
spec.template.spec.bastion.spec.ports[].hostID <string>	# HostID specifies the ID of the host where the port resides.
spec.template.spec.bastion.spec.ports[].macAddress <string>	# MACAddress specifies the MAC address of the port. If not specified, the MAC address will be generated.
spec.template.spec.bastion.spec.ports[].nameSuffix <string>	# NameSuffix will be appended to the name of the port if specified. If unspecified, instead the 0-based index of the port in the list is used.
spec.template.spec.bastion.spec.ports[].network <object>	# Network is a query for an openstack network that the port will be created or discovered on. This will fail if the query returns more than one network.
spec.template.spec.bastion.spec.ports[].network.filter <object>	# Filter specifies a filter to select an OpenStack network. If provided, cannot be empty.
spec.template.spec.bastion.spec.ports[].network.filter.description <string>
spec.template.spec.bastion.spec.ports[].network.filter.name <string>
spec.template.spec.bastion.spec.ports[].network.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.ports[].network.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.ports[].network.filter.projectID <string>
spec.template.spec.bastion.spec.ports[].network.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.bastion.spec.ports[].network.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.bastion.spec.ports[].network.id <string> format=uuid	# ID is the ID of the network to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.bastion.spec.ports[].profile <object>	# Profile is a set of key-value pairs that are used for binding details. We intentionally don't expose this as a map[string]string because we only want to enable the users to set the values of the keys that are known to work in OpenStack Networking API. See https://docs.openstack.org/api-ref/network/v2/index.html?expanded=create-port-detail#create-port To set profiles, your tenant needs permissions rule:create_port, and rule:create_port:binding:profile
spec.template.spec.bastion.spec.ports[].profile.ovsHWOffload <boolean>	# OVSHWOffload enables or disables the OVS hardware offload feature. This flag is not required on OpenStack clouds since Yoga as Nova will set it automatically when the port is attached. See: https://bugs.launchpad.net/nova/+bug/2020813
spec.template.spec.bastion.spec.ports[].profile.trustedVF <boolean>	# TrustedVF enables or disables the “trusted mode” for the VF.
spec.template.spec.bastion.spec.ports[].propagateUplinkStatus <boolean>	# PropageteUplinkStatus enables or disables the propagate uplink status on the port.
spec.template.spec.bastion.spec.ports[].securityGroups <[]object>	# SecurityGroups is a list of the names, uuids, filters or any combination these of the security groups to assign to the instance.
spec.template.spec.bastion.spec.ports[].securityGroups[].filter <object>	# Filter specifies a query to select an OpenStack security group. If provided, cannot be empty.
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.description <string>
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.name <string>
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.projectID <string>
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.bastion.spec.ports[].securityGroups[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.bastion.spec.ports[].securityGroups[].id <string> format=uuid	# ID is the ID of the security group to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.bastion.spec.ports[].tags <[]string>	# Tags applied to the port (and corresponding trunk, if a trunk is configured.) These tags are applied in addition to the instance's tags, which will also be applied to the port.
spec.template.spec.bastion.spec.ports[].trunk <boolean>	# Trunk specifies whether trunking is enabled at the port level. If not provided the value is inherited from the machine, or false for a bastion host.
spec.template.spec.bastion.spec.ports[].valueSpecs <[]object>	# Value specs are extra parameters to include in the API request with OpenStack. This is an extension point for the API, so what they do and if they are supported, depends on the specific OpenStack implementation.
spec.template.spec.bastion.spec.ports[].valueSpecs[].key <string> (required)	# Key is the key in the key-value pair.
spec.template.spec.bastion.spec.ports[].valueSpecs[].name <string> (required)	# Name is the name of the key-value pair. This is just for identifying the pair and will not be sent to the OpenStack API.
spec.template.spec.bastion.spec.ports[].valueSpecs[].value <string> (required)	# Value is the value in the key-value pair.
spec.template.spec.bastion.spec.ports[].vnicType <string>	# VNICType specifies the type of vNIC which this port should be attached to. This is used to determine which mechanism driver(s) to be used to bind the port. The valid values are normal, macvtap, direct, baremetal, direct-physical, virtio-forwarder, smart-nic and remote-managed, although these values will not be validated in this API to ensure compatibility with future neutron changes or custom implementations. What type of vNIC is actually available depends on deployments. If not specified, the Neutron default value is used.
spec.template.spec.bastion.spec.providerID <string>	# ProviderID is the unique identifier as specified by the cloud provider.
spec.template.spec.bastion.spec.rootVolume <object>	# The volume metadata to boot from
spec.template.spec.bastion.spec.rootVolume.availabilityZone <object>	# AvailabilityZone is the volume availability zone to create the volume in. If not specified, the volume will be created without an explicit availability zone.
spec.template.spec.bastion.spec.rootVolume.availabilityZone.from <string> enum=Name|Machine default="Name"	# From specifies where we will obtain the availability zone for the volume. The options are "Name" and "Machine". If "Name" is specified then the Name field must also be specified. If "Machine" is specified the volume will use the value of FailureDomain, if any, from the associated Machine.
spec.template.spec.bastion.spec.rootVolume.availabilityZone.name <string> pattern="^[^ ]+$" min=1	# Name is the name of a volume availability zone to use. It is required if From is "Name". The volume availability zone name may not contain spaces.
spec.template.spec.bastion.spec.rootVolume.sizeGiB <integer> (required) min=1	# SizeGiB is the size of the block device in gibibytes (GiB).
spec.template.spec.bastion.spec.rootVolume.type <string>	# Type is the Cinder volume type of the volume. If omitted, the default Cinder volume type that is configured in the OpenStack cloud will be used.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties <[]object>	# SchedulerHintAdditionalProperties are arbitrary key/value pairs that provide additional hints to the OpenStack scheduler. These hints can influence how instances are placed on the infrastructure, such as specifying certain host aggregates or availability zones.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties[].name <string> (required) min=1	# Name is the name of the scheduler hint property. It is a unique identifier for the property.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties[].value <object> (required)	# Value is the value of the scheduler hint property, which can be of various types (e.g., bool, string, int). The type is indicated by the Value.Type field.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties[].value.bool <boolean>	# Bool is the boolean value of the scheduler hint, used when Type is "Bool". This field is required if type is 'Bool', and must not be set otherwise.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties[].value.number <integer>	# Number is the integer value of the scheduler hint, used when Type is "Number". This field is required if type is 'Number', and must not be set otherwise.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties[].value.string <string> min=1 max=255	# String is the string value of the scheduler hint, used when Type is "String". This field is required if type is 'String', and must not be set otherwise.
spec.template.spec.bastion.spec.schedulerHintAdditionalProperties[].value.type <string> (required) enum=Bool|String|Number	# Type represents the type of the value. Valid values are Bool, String, and Number.
spec.template.spec.bastion.spec.securityGroups <[]object>	# The names of the security groups to assign to the instance
spec.template.spec.bastion.spec.securityGroups[].filter <object>	# Filter specifies a query to select an OpenStack security group. If provided, cannot be empty.
spec.template.spec.bastion.spec.securityGroups[].filter.description <string>
spec.template.spec.bastion.spec.securityGroups[].filter.name <string>
spec.template.spec.bastion.spec.securityGroups[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.securityGroups[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.bastion.spec.securityGroups[].filter.projectID <string>
spec.template.spec.bastion.spec.securityGroups[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.bastion.spec.securityGroups[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.bastion.spec.securityGroups[].id <string> format=uuid	# ID is the ID of the security group to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.bastion.spec.serverGroup <object>	# The server group to assign the machine to.
spec.template.spec.bastion.spec.serverGroup.filter <object>	# Filter specifies a query to select an OpenStack server group. If provided, it cannot be empty.
spec.template.spec.bastion.spec.serverGroup.filter.name <string>	# Name is the name of a server group to look for.
spec.template.spec.bastion.spec.serverGroup.id <string> format=uuid	# ID is the ID of the server group to use.
spec.template.spec.bastion.spec.serverMetadata <[]object>	# Metadata mapping. Allows you to create a map of key value pairs to add to the server instance.
spec.template.spec.bastion.spec.serverMetadata[].key <string> (required) max=255	# Key is the server metadata key
spec.template.spec.bastion.spec.serverMetadata[].value <string> (required) max=255	# Value is the server metadata value
spec.template.spec.bastion.spec.sshKeyName <string>	# The ssh key to inject in the instance
spec.template.spec.bastion.spec.tags <[]string>	# Tags which will be added to the machine and all dependent resources which support them. These are in addition to Tags defined on the cluster. Requires Nova api 2.52 minimum!
spec.template.spec.bastion.spec.trunk <boolean>	# Whether the server instance is created on a trunk port or not.
spec.template.spec.controlPlaneAvailabilityZones <[]string>	# ControlPlaneAvailabilityZones is the set of availability zones which control plane machines may be deployed to.
spec.template.spec.controlPlaneEndpoint <object>	# ControlPlaneEndpoint represents the endpoint used to communicate with the control plane. It is normally populated automatically by the OpenStackCluster controller during cluster provisioning. If it is set on creation the control plane endpoint will use the values set here in preference to values set elsewhere. ControlPlaneEndpoint cannot be modified after ControlPlaneEndpoint.Host has been set.
spec.template.spec.controlPlaneEndpoint.host <string> max=512	# host is the hostname on which the API server is serving.
spec.template.spec.controlPlaneEndpoint.port <integer> format=int32	# port is the port on which the API server is serving.
spec.template.spec.controlPlaneOmitAvailabilityZone <boolean>	# ControlPlaneOmitAvailabilityZone causes availability zone to be omitted when creating control plane nodes, allowing the Nova scheduler to make a decision on which availability zone to use based on other scheduling constraints
spec.template.spec.disableAPIServerFloatingIP <boolean>	# DisableAPIServerFloatingIP determines whether or not to attempt to attach a floating IP to the API server. This allows for the creation of clusters when attaching a floating IP to the API server (and hence, in many cases, exposing the API server to the internet) is not possible or desirable, e.g. if using a shared VLAN for communication between management and workload clusters or when the management cluster is inside the project network. This option requires that the API server use a VIP on the cluster network so that the underlying machines can change without changing ControlPlaneEndpoint.Host. When using a managed load balancer, this VIP will be managed automatically. If not using a managed load balancer, cluster configuration will fail without additional configuration to manage the VIP on the control plane machines, which falls outside of the scope of this controller.
spec.template.spec.disableExternalNetwork <boolean>	# DisableExternalNetwork specifies whether or not to attempt to connect the cluster to an external network. This allows for the creation of clusters when connecting to an external network is not possible or desirable, e.g. if using a provider network.
spec.template.spec.disablePortSecurity <boolean>	# DisablePortSecurity disables the port security of the network created for the Kubernetes cluster, which also disables SecurityGroups
spec.template.spec.externalNetwork <object>	# ExternalNetwork is the OpenStack Network to be used to get public internet to the VMs. This option is ignored if DisableExternalNetwork is set to true. If ExternalNetwork is defined it must refer to exactly one external network. If ExternalNetwork is not defined or is empty the controller will use any existing external network as long as there is only one. It is an error if ExternalNetwork is not defined and there are multiple external networks unless DisableExternalNetwork is also set. If ExternalNetwork is not defined and there are no external networks the controller will proceed as though DisableExternalNetwork was set.
spec.template.spec.externalNetwork.filter <object>	# Filter specifies a filter to select an OpenStack network. If provided, cannot be empty.
spec.template.spec.externalNetwork.filter.description <string>
spec.template.spec.externalNetwork.filter.name <string>
spec.template.spec.externalNetwork.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.externalNetwork.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.externalNetwork.filter.projectID <string>
spec.template.spec.externalNetwork.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.externalNetwork.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.externalNetwork.id <string> format=uuid	# ID is the ID of the network to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.externalRouterIPs <[]object>	# ExternalRouterIPs is an array of externalIPs on the respective subnets. This is necessary if the router needs a fixed ip in a specific subnet.
spec.template.spec.externalRouterIPs[].fixedIP <string>	# The FixedIP in the corresponding subnet
spec.template.spec.externalRouterIPs[].subnet <object> (required)	# The subnet in which the FixedIP is used for the Gateway of this router
spec.template.spec.externalRouterIPs[].subnet.filter <object>	# Filter specifies a filter to select the subnet. It must match exactly one subnet.
spec.template.spec.externalRouterIPs[].subnet.filter.cidr <string>
spec.template.spec.externalRouterIPs[].subnet.filter.description <string>
spec.template.spec.externalRouterIPs[].subnet.filter.gatewayIP <string>
spec.template.spec.externalRouterIPs[].subnet.filter.ipVersion <integer>
spec.template.spec.externalRouterIPs[].subnet.filter.ipv6AddressMode <string>
spec.template.spec.externalRouterIPs[].subnet.filter.ipv6RAMode <string>
spec.template.spec.externalRouterIPs[].subnet.filter.name <string>
spec.template.spec.externalRouterIPs[].subnet.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.externalRouterIPs[].subnet.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.externalRouterIPs[].subnet.filter.projectID <string>
spec.template.spec.externalRouterIPs[].subnet.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.externalRouterIPs[].subnet.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.externalRouterIPs[].subnet.id <string> format=uuid	# ID is the uuid of the subnet. It will not be validated.
spec.template.spec.identityRef <object> (required)	# IdentityRef is a reference to a secret holding OpenStack credentials to be used when reconciling this cluster. It is also to reconcile machines unless overridden in the machine spec.
spec.template.spec.identityRef.cloudName <string> (required) min=1	# CloudName specifies the name of the entry in the clouds.yaml file to use.
spec.template.spec.identityRef.name <string> (required) min=1	# Name is the name of a Secret (type=Secret) in the same namespace as the resource being provisioned, or the name of an OpenStackClusterIdentity (type=ClusterIdentity). The Secret must contain a key named `clouds.yaml` which contains an OpenStack clouds.yaml file. The Secret may optionally contain a key named `cacert` containing a PEM-encoded CA certificate.
spec.template.spec.identityRef.region <string>	# Region specifies an OpenStack region to use. If specified, it overrides any value in clouds.yaml. If specified for an OpenStackMachine, its value will be included in providerID.
spec.template.spec.identityRef.type <string> (required) enum=Secret|ClusterIdentity default="Secret"	# Type specifies the identity reference type. Defaults to Secret for backward compatibility.
spec.template.spec.managedSecurityGroups <object>	# ManagedSecurityGroups determines whether OpenStack security groups for the cluster will be managed by the OpenStack provider or whether pre-existing security groups will be specified as part of the configuration. By default, the managed security groups have rules that allow the Kubelet, etcd, and the Kubernetes API server to function correctly. It's possible to add additional rules to the managed security groups. When defined to an empty struct, the managed security groups will be created with the default rules.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules <[]object>	# allNodesSecurityGroupRules defines the rules that should be applied to all nodes.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].description <string>	# description of the security group rule.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].direction <string> (required) enum=ingress|egress	# direction in which the security group rule is applied. The only values allowed are "ingress" or "egress". For a compute instance, an ingress security group rule is applied to incoming (ingress) traffic for that instance. An egress rule is applied to traffic leaving the instance.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].etherType <string> enum=IPv4|IPv6	# etherType must be IPv4 or IPv6, and addresses represented in CIDR must match the ingress or egress rules.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].name <string> (required)	# name of the security group rule. It's used to identify the rule so it can be patched and will not be sent to the OpenStack API.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].portRangeMax <integer>	# portRangeMax is a number in the range that is matched by the security group rule. The portRangeMin attribute constrains the portRangeMax attribute.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].portRangeMin <integer>	# portRangeMin is a number in the range that is matched by the security group rule. If the protocol is TCP or UDP, this value must be less than or equal to the value of the portRangeMax attribute.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].protocol <string>	# protocol is the protocol that is matched by the security group rule.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].remoteGroupID <string>	# remoteGroupID is the remote group ID to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].remoteIPPrefix <string>	# remoteIPPrefix is the remote IP prefix to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.allNodesSecurityGroupRules[].remoteManagedGroups <[]string>	# remoteManagedGroups is the remote managed groups to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.allowAllInClusterTraffic <boolean> (required) default=false	# AllowAllInClusterTraffic allows all ingress and egress traffic between cluster nodes when set to true.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules <[]object>	# controlPlaneNodesSecurityGroupRules defines the rules that should be applied to control plane nodes.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].description <string>	# description of the security group rule.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].direction <string> (required) enum=ingress|egress	# direction in which the security group rule is applied. The only values allowed are "ingress" or "egress". For a compute instance, an ingress security group rule is applied to incoming (ingress) traffic for that instance. An egress rule is applied to traffic leaving the instance.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].etherType <string> enum=IPv4|IPv6	# etherType must be IPv4 or IPv6, and addresses represented in CIDR must match the ingress or egress rules.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].name <string> (required)	# name of the security group rule. It's used to identify the rule so it can be patched and will not be sent to the OpenStack API.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].portRangeMax <integer>	# portRangeMax is a number in the range that is matched by the security group rule. The portRangeMin attribute constrains the portRangeMax attribute.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].portRangeMin <integer>	# portRangeMin is a number in the range that is matched by the security group rule. If the protocol is TCP or UDP, this value must be less than or equal to the value of the portRangeMax attribute.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].protocol <string>	# protocol is the protocol that is matched by the security group rule.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].remoteGroupID <string>	# remoteGroupID is the remote group ID to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].remoteIPPrefix <string>	# remoteIPPrefix is the remote IP prefix to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.controlPlaneNodesSecurityGroupRules[].remoteManagedGroups <[]string>	# remoteManagedGroups is the remote managed groups to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules <[]object>	# workerNodesSecurityGroupRules defines the rules that should be applied to worker nodes.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].description <string>	# description of the security group rule.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].direction <string> (required) enum=ingress|egress	# direction in which the security group rule is applied. The only values allowed are "ingress" or "egress". For a compute instance, an ingress security group rule is applied to incoming (ingress) traffic for that instance. An egress rule is applied to traffic leaving the instance.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].etherType <string> enum=IPv4|IPv6	# etherType must be IPv4 or IPv6, and addresses represented in CIDR must match the ingress or egress rules.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].name <string> (required)	# name of the security group rule. It's used to identify the rule so it can be patched and will not be sent to the OpenStack API.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].portRangeMax <integer>	# portRangeMax is a number in the range that is matched by the security group rule. The portRangeMin attribute constrains the portRangeMax attribute.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].portRangeMin <integer>	# portRangeMin is a number in the range that is matched by the security group rule. If the protocol is TCP or UDP, this value must be less than or equal to the value of the portRangeMax attribute.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].protocol <string>	# protocol is the protocol that is matched by the security group rule.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].remoteGroupID <string>	# remoteGroupID is the remote group ID to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].remoteIPPrefix <string>	# remoteIPPrefix is the remote IP prefix to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSecurityGroups.workerNodesSecurityGroupRules[].remoteManagedGroups <[]string>	# remoteManagedGroups is the remote managed groups to be associated with this security group rule. You can specify either remoteGroupID or remoteIPPrefix or remoteManagedGroups.
spec.template.spec.managedSubnets <[]object> max=1	# ManagedSubnets describe OpenStack Subnets to be created. Cluster actuator will create a network, subnets with the defined CIDR, and a router connected to these subnets. Currently only one IPv4 subnet is supported. If you leave this empty, no network will be created.
spec.template.spec.managedSubnets[].allocationPools <[]object>	# AllocationPools is an array of AllocationPool objects that will be applied to OpenStack Subnet being created. If set, OpenStack will only allocate these IPs for Machines. It will still be possible to create ports from outside of these ranges manually.
spec.template.spec.managedSubnets[].allocationPools[].end <string> (required)	# End represents the end of the AlloctionPool, that is the highest IP of the pool.
spec.template.spec.managedSubnets[].allocationPools[].start <string> (required)	# Start represents the start of the AllocationPool, that is the lowest IP of the pool.
spec.template.spec.managedSubnets[].cidr <string> (required)	# CIDR is representing the IP address range used to create the subnet, e.g. 10.0.0.0/24. This field is required when defining a subnet.
spec.template.spec.managedSubnets[].dnsNameservers <[]string>	# DNSNameservers holds a list of DNS server addresses that will be provided when creating the subnet. These addresses need to have the same IP version as CIDR.
spec.template.spec.network <object>	# Network specifies an existing network to use if no ManagedSubnets are specified.
spec.template.spec.network.filter <object>	# Filter specifies a filter to select an OpenStack network. If provided, cannot be empty.
spec.template.spec.network.filter.description <string>
spec.template.spec.network.filter.name <string>
spec.template.spec.network.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.network.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.network.filter.projectID <string>
spec.template.spec.network.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.network.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.network.id <string> format=uuid	# ID is the ID of the network to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.networkMTU <integer>	# NetworkMTU sets the maximum transmission unit (MTU) value to address fragmentation for the private network ID. This value will be used only if the Cluster actuator creates the network. If left empty, the network will have the default MTU defined in Openstack network service. To use this field, the Openstack installation requires the net-mtu neutron API extension.
spec.template.spec.router <object>	# Router specifies an existing router to be used if ManagedSubnets are specified. If specified, no new router will be created.
spec.template.spec.router.filter <object>	# Filter specifies a filter to select an OpenStack router. If provided, cannot be empty.
spec.template.spec.router.filter.description <string>
spec.template.spec.router.filter.name <string>
spec.template.spec.router.filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.router.filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.router.filter.projectID <string>
spec.template.spec.router.filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.router.filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.router.id <string> format=uuid	# ID is the ID of the router to use. If ID is provided, the other filters cannot be provided. Must be in UUID format.
spec.template.spec.subnets <[]object> max=2	# Subnets specifies existing subnets to use if not ManagedSubnets are specified. All subnets must be in the network specified by Network. There can be zero, one, or two subnets. If no subnets are specified, all subnets in Network will be used. If 2 subnets are specified, one must be IPv4 and the other IPv6.
spec.template.spec.subnets[].filter <object>	# Filter specifies a filter to select the subnet. It must match exactly one subnet.
spec.template.spec.subnets[].filter.cidr <string>
spec.template.spec.subnets[].filter.description <string>
spec.template.spec.subnets[].filter.gatewayIP <string>
spec.template.spec.subnets[].filter.ipVersion <integer>
spec.template.spec.subnets[].filter.ipv6AddressMode <string>
spec.template.spec.subnets[].filter.ipv6RAMode <string>
spec.template.spec.subnets[].filter.name <string>
spec.template.spec.subnets[].filter.notTags <[]string>	# NotTags is a list of tags to filter by. If specified, resources which contain all of the given tags will be excluded from the result.
spec.template.spec.subnets[].filter.notTagsAny <[]string>	# NotTagsAny is a list of tags to filter by. If specified, resources which contain any of the given tags will be excluded from the result.
spec.template.spec.subnets[].filter.projectID <string>
spec.template.spec.subnets[].filter.tags <[]string>	# Tags is a list of tags to filter by. If specified, the resource must have all of the tags specified to be included in the result.
spec.template.spec.subnets[].filter.tagsAny <[]string>	# TagsAny is a list of tags to filter by. If specified, the resource must have at least one of the tags specified to be included in the result.
spec.template.spec.subnets[].id <string> format=uuid	# ID is the uuid of the subnet. It will not be validated.
spec.template.spec.tags <[]string>	# Tags to set on all resources in cluster which support tags
