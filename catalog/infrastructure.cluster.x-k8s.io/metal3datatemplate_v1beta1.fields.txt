# schema source: Cluster API Metal3 v1.13.1 https://github.com/metal3-io/cluster-api-provider-metal3
apiVersion <string> enum=infrastructure.cluster.x-k8s.io/v1beta1 (deprecated)
kind <string> enum=Metal3DataTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# Metal3DataTemplateSpec defines the desired state of Metal3DataTemplate.
spec.clusterName <string> (required) min=1	# ClusterName is the name of the Cluster this object belongs to.
spec.metaData <object>	# MetaData contains the information needed to generate the metadata secret
spec.metaData.dnsServersFromIPPool <[]object>	# DNSServersFromPool is the list of metadata items to be rendered as dns servers.
spec.metaData.dnsServersFromIPPool[].apiGroup <string> (required)	# APIGroup is the api group of the IP pool.
spec.metaData.dnsServersFromIPPool[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.dnsServersFromIPPool[].kind <string> (required)	# Kind is the kind of the IP pool
spec.metaData.dnsServersFromIPPool[].name <string> (required)	# Name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.fromAnnotations <[]object>	# FromAnnotations is the list of metadata items to be fetched from object Annotations
spec.metaData.fromAnnotations[].annotation <string> (required)	# Annotation is the key of the Annotation to fetch
spec.metaData.fromAnnotations[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.fromAnnotations[].object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the name
spec.metaData.fromHostInterfaces <[]object>	# FromHostInterfaces is the list of metadata items to be rendered as MAC addresses of the host interfaces.
spec.metaData.fromHostInterfaces[].fromBootMAC <boolean>	# FromBootMAC will fetch the MAC address from the BareMetalHost Spec BootMACAddress field.
spec.metaData.fromHostInterfaces[].interface <string>	# Interface is the name of the interface in the BareMetalHost Status Hardware Details list of interfaces from which to fetch the MAC address.
spec.metaData.fromHostInterfaces[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.fromLabels <[]object>	# FromLabels is the list of metadata items to be fetched from object labels
spec.metaData.fromLabels[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.fromLabels[].label <string> (required)	# Label is the key of the label to fetch
spec.metaData.fromLabels[].object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the name
spec.metaData.gatewaysFromIPPool <[]object>	# GatewaysFromPool is the list of metadata items to be rendered as gateway addresses.
spec.metaData.gatewaysFromIPPool[].apiGroup <string> (required)	# APIGroup is the api group of the IP pool.
spec.metaData.gatewaysFromIPPool[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.gatewaysFromIPPool[].kind <string> (required)	# Kind is the kind of the IP pool
spec.metaData.gatewaysFromIPPool[].name <string> (required)	# Name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.indexes <[]object>	# Indexes is the list of metadata items to be rendered from the index of the Metal3Data
spec.metaData.indexes[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.indexes[].offset <integer>	# Offset is the offset to apply to the index when rendering it
spec.metaData.indexes[].prefix <string>	# Prefix is the prefix string
spec.metaData.indexes[].step <integer> default=1	# Step is the multiplier of the index
spec.metaData.indexes[].suffix <string>	# Suffix is the suffix string
spec.metaData.ipAddressesFromIPPool <[]object>	# IPAddressesFromPool is the list of metadata items to be rendered as ip addresses.
spec.metaData.ipAddressesFromIPPool[].apiGroup <string> (required)	# APIGroup is the api group of the IP pool.
spec.metaData.ipAddressesFromIPPool[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.ipAddressesFromIPPool[].kind <string> (required)	# Kind is the kind of the IP pool
spec.metaData.ipAddressesFromIPPool[].name <string> (required)	# Name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.namespaces <[]object>	# Namespaces is the list of metadata items to be rendered from the namespace
spec.metaData.namespaces[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.objectNames <[]object>	# ObjectNames is the list of metadata items to be rendered from the name of objects.
spec.metaData.objectNames[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.objectNames[].object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the name
spec.metaData.prefixesFromIPPool <[]object>	# PrefixesFromPool is the list of metadata items to be rendered as network prefixes.
spec.metaData.prefixesFromIPPool[].apiGroup <string> (required)	# APIGroup is the api group of the IP pool.
spec.metaData.prefixesFromIPPool[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.prefixesFromIPPool[].kind <string> (required)	# Kind is the kind of the IP pool
spec.metaData.prefixesFromIPPool[].name <string> (required)	# Name is the name of the IP pool used to fetch the value to set in the metadata map for cloud-init
spec.metaData.strings <[]object>	# Strings is the list of metadata items to be rendered from strings
spec.metaData.strings[].key <string> (required)	# Key will be used as the key to set in the metadata map for cloud-init
spec.metaData.strings[].value <string> (required)	# Value is the string to render.
spec.networkData <object>	# NetworkData contains the information needed to generate the networkdata secret
spec.networkData.links <object>	# Links is a structure containing lists of different types objects
spec.networkData.links.bonds <[]object>	# Bonds contains a list of Bond links
spec.networkData.links.bonds[].bondLinks <[]string>	# BondLinks is the list of links that are part of the bond.
spec.networkData.links.bonds[].bondMode <string> (required) enum=balance-rr|active-backup|balance-xor|broadcast|balance-tlb|balance-alb|802.3ad	# BondMode is the mode of bond used. It can be one of balance-rr, active-backup, balance-xor, broadcast, balance-tlb, balance-alb, 802.3ad
spec.networkData.links.bonds[].bondXmitHashPolicy <string> enum=layer2|layer3+4|layer2+3	# Selects the transmit hash policy used for port selection in balance-xor and 802.3ad modes
spec.networkData.links.bonds[].id <string> (required)	# Id is the ID of the interface (used for naming)
spec.networkData.links.bonds[].macAddress <object> (required)	# MACAddress is the MAC address of the interface, containing the object used to render it.
spec.networkData.links.bonds[].macAddress.fromAnnotation <object>	# FromAnnotation references an object Annotation to retrieve the MAC address from
spec.networkData.links.bonds[].macAddress.fromAnnotation.annotation <string> (required)	# Annotation is the key of the Annotation to fetch
spec.networkData.links.bonds[].macAddress.fromAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the name
spec.networkData.links.bonds[].macAddress.fromHostInterface <string>	# FromHostInterface contains the name of the interface in the BareMetalHost Introspection details from which to fetch the MAC address
spec.networkData.links.bonds[].macAddress.string <string>	# String contains the MAC address given as a string
spec.networkData.links.bonds[].mtu <integer> default=1500 max=9000	# MTU is the MTU of the interface
spec.networkData.links.bonds[].name <string> pattern="^[a-zA-Z][a-zA-Z0-9._-]*$" max=15	# Name is the interface name to be used by cloud-init. When combined with MACAddress, cloud-init will rename the interface matching the MAC to this name. When MACAddress is omitted, cloud-init will use this name directly.
spec.networkData.links.bonds[].parameters <object (free-form)>	# params blob passed without any validation/modifications into cloud-init config
spec.networkData.links.ethernets <[]object>	# Ethernets contains a list of Ethernet links
spec.networkData.links.ethernets[].id <string> (required)	# Id is the ID of the interface (used for naming)
spec.networkData.links.ethernets[].macAddress <object> (required)	# MACAddress is the MAC address of the interface, containing the object used to render it.
spec.networkData.links.ethernets[].macAddress.fromAnnotation <object>	# FromAnnotation references an object Annotation to retrieve the MAC address from
spec.networkData.links.ethernets[].macAddress.fromAnnotation.annotation <string> (required)	# Annotation is the key of the Annotation to fetch
spec.networkData.links.ethernets[].macAddress.fromAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the name
spec.networkData.links.ethernets[].macAddress.fromHostInterface <string>	# FromHostInterface contains the name of the interface in the BareMetalHost Introspection details from which to fetch the MAC address
spec.networkData.links.ethernets[].macAddress.string <string>	# String contains the MAC address given as a string
spec.networkData.links.ethernets[].mtu <integer> default=1500 max=9000	# MTU is the MTU of the interface
spec.networkData.links.ethernets[].name <string> pattern="^[a-zA-Z][a-zA-Z0-9._-]*$" max=15	# Name is the interface name to be used by cloud-init. When combined with MACAddress, cloud-init will rename the interface matching the MAC to this name. When MACAddress is omitted, cloud-init will use this name directly.
spec.networkData.links.ethernets[].type <string> (required) enum=bridge|dvs|hw_veb|hyperv|ovs|tap|vhostuser|vif|phy	# Type is the type of the ethernet link. It can be one of: bridge, dvs, hw_veb, hyperv, ovs, tap, vhostuser, vif, phy
spec.networkData.links.vlans <[]object>	# Vlans contains a list of Vlan links
spec.networkData.links.vlans[].id <string> (required)	# Id is the ID of the interface (used for naming)
spec.networkData.links.vlans[].macAddress <object> (required)	# MACAddress is the MAC address of the interface, containing the object used to render it.
spec.networkData.links.vlans[].macAddress.fromAnnotation <object>	# FromAnnotation references an object Annotation to retrieve the MAC address from
spec.networkData.links.vlans[].macAddress.fromAnnotation.annotation <string> (required)	# Annotation is the key of the Annotation to fetch
spec.networkData.links.vlans[].macAddress.fromAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the name
spec.networkData.links.vlans[].macAddress.fromHostInterface <string>	# FromHostInterface contains the name of the interface in the BareMetalHost Introspection details from which to fetch the MAC address
spec.networkData.links.vlans[].macAddress.string <string>	# String contains the MAC address given as a string
spec.networkData.links.vlans[].mtu <integer> default=1500 max=9000	# MTU is the MTU of the interface
spec.networkData.links.vlans[].name <string> pattern="^[a-zA-Z][a-zA-Z0-9._-]*$" max=15	# Name is the interface name to be used by cloud-init. When combined with MACAddress, cloud-init will rename the interface matching the MAC to this name. When MACAddress is omitted, cloud-init will use this name directly.
spec.networkData.links.vlans[].vlanID <integer> (required) max=4096	# VlanID is the Vlan ID
spec.networkData.links.vlans[].vlanLink <string> (required)	# VlanLink is the name of the link on which the vlan should be added
spec.networkData.networks <object>	# Networks is a structure containing lists of different types objects
spec.networkData.networks.ipv4 <[]object>	# IPv4 contains a list of IPv4 static allocations
spec.networkData.networks.ipv4[].fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool reference via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, IPAddressFromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv4[].fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv4[].fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv4[].fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to allocate an address from.
spec.networkData.networks.ipv4[].fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv4[].fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv4[].fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv4[].id <string> (required)	# ID is the network ID (name)
spec.networkData.networks.ipv4[].ipAddressFromIPPool <string>	# IPAddressFromIPPool contains the name of the IP pool to use to get an ip address
spec.networkData.networks.ipv4[].link <string> (required)	# Link is the link on which the network applies
spec.networkData.networks.ipv4[].routes <[]object>	# Routes contains a list of IPv4 routes
spec.networkData.networks.ipv4[].routes[].gateway <object> (required)	# Gateway is the IPv4 address of the gateway
spec.networkData.networks.ipv4[].routes[].gateway.fromIPPool <string>	# FromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, FromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv4[].routes[].gateway.fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv4[].routes[].gateway.string <string> pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# String is the gateway given as a string
spec.networkData.networks.ipv4[].routes[].network <string> (required) pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# Network is the IPv4 network address
spec.networkData.networks.ipv4[].routes[].prefix <integer> max=32	# Prefix is the mask of the network as integer (max 32)
spec.networkData.networks.ipv4[].routes[].services <object>	# Services is a list of IPv4 services
spec.networkData.networks.ipv4[].routes[].services.dns <[]string>	# DNS is a list of IPv4 DNS services
spec.networkData.networks.ipv4[].routes[].services.dnsFromIPPool <string>	# DNSFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv4DHCP <[]object>	# IPv4 contains a list of IPv4 DHCP allocations
spec.networkData.networks.ipv4DHCP[].id <string> (required)	# ID is the network ID (name)
spec.networkData.networks.ipv4DHCP[].link <string> (required)	# Link is the link on which the network applies
spec.networkData.networks.ipv4DHCP[].routes <[]object>	# Routes contains a list of IPv4 routes
spec.networkData.networks.ipv4DHCP[].routes[].gateway <object> (required)	# Gateway is the IPv4 address of the gateway
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromIPPool <string>	# FromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, FromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv4DHCP[].routes[].gateway.fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv4DHCP[].routes[].gateway.string <string> pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# String is the gateway given as a string
spec.networkData.networks.ipv4DHCP[].routes[].network <string> (required) pattern="^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$"	# Network is the IPv4 network address
spec.networkData.networks.ipv4DHCP[].routes[].prefix <integer> max=32	# Prefix is the mask of the network as integer (max 32)
spec.networkData.networks.ipv4DHCP[].routes[].services <object>	# Services is a list of IPv4 services
spec.networkData.networks.ipv4DHCP[].routes[].services.dns <[]string>	# DNS is a list of IPv4 DNS services
spec.networkData.networks.ipv4DHCP[].routes[].services.dnsFromIPPool <string>	# DNSFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv6 <[]object>	# IPv4 contains a list of IPv6 static allocations
spec.networkData.networks.ipv6[].fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool reference via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, IPAddressFromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv6[].fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6[].fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6[].fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to allocate an address from.
spec.networkData.networks.ipv6[].fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv6[].fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv6[].fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv6[].id <string> (required)	# ID is the network ID (name)
spec.networkData.networks.ipv6[].ipAddressFromIPPool <string> (required)	# IPAddressFromIPPool contains the name of the IPPool to use to get an ip address
spec.networkData.networks.ipv6[].link <string> (required)	# Link is the link on which the network applies
spec.networkData.networks.ipv6[].routes <[]object>	# Routes contains a list of IPv6 routes
spec.networkData.networks.ipv6[].routes[].gateway <object> (required)	# Gateway is the IPv6 address of the gateway
spec.networkData.networks.ipv6[].routes[].gateway.fromIPPool <string>	# FromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, FromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv6[].routes[].gateway.fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv6[].routes[].gateway.string <string> pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# String is the gateway given as a string
spec.networkData.networks.ipv6[].routes[].network <string> (required) pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# Network is the IPv6 network address
spec.networkData.networks.ipv6[].routes[].prefix <integer> max=128	# Prefix is the mask of the network as integer (max 128)
spec.networkData.networks.ipv6[].routes[].services <object>	# Services is a list of IPv6 services
spec.networkData.networks.ipv6[].routes[].services.dns <[]string>	# DNS is a list of IPv6 DNS services
spec.networkData.networks.ipv6[].routes[].services.dnsFromIPPool <string>	# DNSFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv6DHCP <[]object>	# IPv4 contains a list of IPv6 DHCP allocations
spec.networkData.networks.ipv6DHCP[].id <string> (required)	# ID is the network ID (name)
spec.networkData.networks.ipv6DHCP[].link <string> (required)	# Link is the link on which the network applies
spec.networkData.networks.ipv6DHCP[].routes <[]object>	# Routes contains a list of IPv6 routes
spec.networkData.networks.ipv6DHCP[].routes[].gateway <object> (required)	# Gateway is the IPv6 address of the gateway
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromIPPool <string>	# FromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, FromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv6DHCP[].routes[].gateway.fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv6DHCP[].routes[].gateway.string <string> pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# String is the gateway given as a string
spec.networkData.networks.ipv6DHCP[].routes[].network <string> (required) pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# Network is the IPv6 network address
spec.networkData.networks.ipv6DHCP[].routes[].prefix <integer> max=128	# Prefix is the mask of the network as integer (max 128)
spec.networkData.networks.ipv6DHCP[].routes[].services <object>	# Services is a list of IPv6 services
spec.networkData.networks.ipv6DHCP[].routes[].services.dns <[]string>	# DNS is a list of IPv6 DNS services
spec.networkData.networks.ipv6DHCP[].routes[].services.dnsFromIPPool <string>	# DNSFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.networks.ipv6SLAAC <[]object>	# IPv4 contains a list of IPv6 SLAAC allocations
spec.networkData.networks.ipv6SLAAC[].id <string> (required)	# ID is the network ID (name)
spec.networkData.networks.ipv6SLAAC[].link <string> (required)	# Link is the link on which the network applies
spec.networkData.networks.ipv6SLAAC[].routes <[]object>	# Routes contains a list of IPv6 routes
spec.networkData.networks.ipv6SLAAC[].routes[].gateway <object> (required)	# Gateway is the IPv6 address of the gateway
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromIPPool <string>	# FromIPPool is the name of the IPPool to fetch the gateway from
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolAnnotation <object>	# FromPoolAnnotation allows specifying the pool name via an annotation on a Machine, Metal3Machine, or BareMetalHost object. When set, FromIPPool and FromPoolRef are ignored.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolAnnotation.annotation <string> (required)	# Annotation is the key of the annotation that contains the pool name. The annotation value should be a string containing the pool name.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolAnnotation.object <string> (required) enum=machine|metal3machine|baremetalhost	# Object is the type of the object from which we retrieve the annotation
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef <object>	# FromPoolRef is a reference to a IP pool to fetch the gateway from
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef.apiGroup <string>	# APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef.kind <string> (required)	# Kind is the type of resource being referenced
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.fromPoolRef.name <string> (required)	# Name is the name of resource being referenced
spec.networkData.networks.ipv6SLAAC[].routes[].gateway.string <string> pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# String is the gateway given as a string
spec.networkData.networks.ipv6SLAAC[].routes[].network <string> (required) pattern="^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:))$"	# Network is the IPv6 network address
spec.networkData.networks.ipv6SLAAC[].routes[].prefix <integer> max=128	# Prefix is the mask of the network as integer (max 128)
spec.networkData.networks.ipv6SLAAC[].routes[].services <object>	# Services is a list of IPv6 services
spec.networkData.networks.ipv6SLAAC[].routes[].services.dns <[]string>	# DNS is a list of IPv6 DNS services
spec.networkData.networks.ipv6SLAAC[].routes[].services.dnsFromIPPool <string>	# DNSFromIPPool is the name of the IPPool from which to get the DNS servers
spec.networkData.services <object>	# Services is a structure containing lists of different types objects
spec.networkData.services.dns <[]string>	# DNS is a list of DNS services
spec.networkData.services.dnsFromIPPool <string>	# DNSFromIPPool is the name of the IPPool from which to get the DNS servers
spec.templateReference <string>	# TemplateReference refers to the Template the Metal3MachineTemplate refers to. It can be matched against the key or it may also point to the name of the template Metal3Data refers to. Deprecated: This field is deprecated and will be removed in a future release.
status <object>	# Metal3DataTemplateStatus defines the observed state of Metal3DataTemplate.
status.indexes <map[string]integer>	# Indexes contains the map of Metal3Machine and index used
status.lastUpdated <string> format=date-time	# LastUpdated identifies when this status was last observed.
