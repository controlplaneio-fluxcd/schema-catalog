# schema source: OPA Gatekeeper v3.23.0 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=templates.gatekeeper.sh/v1alpha1
kind <string> enum=ConstraintTemplate (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ConstraintTemplateSpec defines the desired state of ConstraintTemplate.
spec.crd <object>	# CRD defines the custom resource definition specification for the constraint.
spec.crd.spec <object>	# CRDSpec defines the spec for the CRD.
spec.crd.spec.names <object>	# Names defines the naming conventions for the constraint kind.
spec.crd.spec.names.kind <string>
spec.crd.spec.names.shortNames <[]string>
spec.crd.spec.validation <object> default={"legacySchema":true}	# Validation defines the schema for constraint parameters.
spec.crd.spec.validation.legacySchema <boolean> default=true
spec.crd.spec.validation.openAPIV3Schema <object (free-form)>
spec.targets <[]object>
spec.targets[].code <[]object>	# The source code options for the constraint template. "Rego" can only be specified in one place (either here or in the "rego" field)
spec.targets[].code[].engine <string> (required)	# The engine used to evaluate the code. Example: "Rego". Required.
spec.targets[].code[].source <any> (required)	# The source code for the template. Required.
spec.targets[].libs <[]string>
spec.targets[].operations <[]string>
spec.targets[].rego <string>
spec.targets[].target <string>
status <object>	# ConstraintTemplateStatus defines the observed state of ConstraintTemplate.
status.byPod <[]object>
status.byPod[].errors <[]object>
status.byPod[].errors[].code <string> (required)
status.byPod[].errors[].location <string>
status.byPod[].errors[].message <string> (required)
status.byPod[].id <string>	# a unique identifier for the pod that wrote the status
status.byPod[].observedGeneration <integer> format=int64
status.created <boolean>
