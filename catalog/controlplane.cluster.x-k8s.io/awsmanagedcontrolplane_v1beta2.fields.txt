# schema source: Cluster API AWS v2.11.1 https://github.com/kubernetes-sigs/cluster-api-provider-aws
apiVersion <string> enum=controlplane.cluster.x-k8s.io/v1beta2
kind <string> enum=AWSManagedControlPlane
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# AWSManagedControlPlaneSpec defines the desired state of an Amazon EKS Cluster.
spec.accessConfig <object>	# AccessConfig specifies the access configuration information for the cluster
spec.accessConfig.authenticationMode <string> enum=config_map|api|api_and_config_map default="config_map"	# AuthenticationMode specifies the desired authentication mode for the cluster Defaults to config_map
spec.accessConfig.bootstrapClusterCreatorAdminPermissions <boolean> default=true	# BootstrapClusterCreatorAdminPermissions grants cluster admin permissions to the IAM identity creating the cluster. Only applied during creation, ignored when updating existing clusters. Defaults to true.
spec.accessEntries <[]object>	# AccessEntries specifies the access entries for the cluster Access entries require AuthenticationMode to be either "api" or "api_and_config_map"
spec.accessEntries[].accessPolicies <[]object> max=20	# AccessPolicies specifies the policies to associate with this access entry Cannot be specified if Type is "ec2_linux" or "ec2_windows"
spec.accessEntries[].accessPolicies[].accessScope <object> (required)	# AccessScope specifies the scope for the policy
spec.accessEntries[].accessPolicies[].accessScope.namespaces <[]string> min=1	# Namespaces are the namespaces for the access scope Only valid when Type is namespace
spec.accessEntries[].accessPolicies[].accessScope.type <string> (required) enum=cluster|namespace default="cluster"	# Type is the type of access scope. Defaults to "cluster".
spec.accessEntries[].accessPolicies[].policyARN <string> (required)	# PolicyARN is the Amazon Resource Name (ARN) of the access policy
spec.accessEntries[].kubernetesGroups <[]string>	# KubernetesGroups represents the Kubernetes groups for the access entry Cannot be specified if Type is "ec2_linux" or "ec2_windows"
spec.accessEntries[].principalARN <string> (required)	# PrincipalARN is the Amazon Resource Name (ARN) of the IAM principal
spec.accessEntries[].type <string> enum=standard|ec2_linux|ec2_windows|fargate_linux|ec2|hybrid_linux|hyperpod_linux default="standard"	# Type is the type of access entry. Defaults to standard if not specified.
spec.accessEntries[].username <string>	# Username is the username for the access entry
spec.additionalTags <map[string]string>	# AdditionalTags is an optional set of tags to add to AWS resources managed by the AWS provider, in addition to the ones added by default.
spec.addons <[]object>	# Addons defines the EKS addons to enable with the EKS cluster.
spec.addons[].configuration <string>	# Configuration of the EKS addon
spec.addons[].conflictResolution <string> enum=overwrite|none|preserve default="overwrite"	# ConflictResolution is used to declare what should happen if there are parameter conflicts. Defaults to overwrite
spec.addons[].name <string> (required) min=2	# Name is the name of the addon
spec.addons[].preserveOnDelete <boolean>	# PreserveOnDelete indicates that the addon resources should be preserved in the cluster on delete.
spec.addons[].serviceAccountRoleARN <string>	# ServiceAccountRoleArn is the ARN of an IAM role to bind to the addons service account
spec.addons[].version <string> (required)	# Version is the version of the addon to use
spec.associateOIDCProvider <boolean> default=false	# AssociateOIDCProvider can be enabled to automatically create an identity provider for the controller for use with IAM roles for service accounts
spec.bastion <object>	# Bastion contains options to configure the bastion host.
spec.bastion.allowedCIDRBlocks <[]string>	# AllowedCIDRBlocks is a list of CIDR blocks allowed to access the bastion host. They are set as ingress rules for the Bastion host's Security Group (defaults to 0.0.0.0/0). If the cluster has IPv6 enabled, defaults to ::/0 and 0.0.0.0/0.
spec.bastion.ami <string>	# AMI will use the specified AMI to boot the bastion. If not specified, the AMI will default to one picked out in public space.
spec.bastion.disableIngressRules <boolean>	# DisableIngressRules will ensure there are no Ingress rules in the bastion host's security group. Requires AllowedCIDRBlocks to be empty.
spec.bastion.enabled <boolean>	# Enabled allows this provider to create a bastion host instance with a public ip to access the VPC private network.
spec.bastion.instanceType <string>	# InstanceType will use the specified instance type for the bastion. If not specified, Cluster API Provider AWS will use t3.micro for all regions except us-east-1, where t2.micro will be the default.
spec.bootstrapSelfManagedAddons <boolean> default=true	# BootstrapSelfManagedAddons is used to set configuration options for bare EKS cluster without EKS default networking addons If you set this value to false when creating a cluster, the default networking add-ons will not be installed
spec.controlPlaneEndpoint <object>	# ControlPlaneEndpoint represents the endpoint used to communicate with the control plane.
spec.controlPlaneEndpoint.host <string> (required) max=512	# host is the hostname on which the API server is serving.
spec.controlPlaneEndpoint.port <integer> (required) format=int32	# port is the port on which the API server is serving.
spec.eksClusterName <string>	# EKSClusterName allows you to specify the name of the EKS cluster in AWS. If you don't specify a name then a default name will be created based on the namespace and name of the managed control plane.
spec.encryptionConfig <object>	# EncryptionConfig specifies the encryption configuration for the cluster
spec.encryptionConfig.provider <string>	# Provider specifies the ARN or alias of the CMK (in AWS KMS)
spec.encryptionConfig.resources <[]string>	# Resources specifies the resources to be encrypted
spec.endpointAccess <object>	# Endpoints specifies access to this cluster's control plane endpoints
spec.endpointAccess.private <boolean>	# Private points VPC-internal control plane access to the private endpoint
spec.endpointAccess.public <boolean>	# Public controls whether control plane endpoints are publicly accessible
spec.endpointAccess.publicCIDRs <[]string>	# PublicCIDRs specifies which blocks can access the public endpoint
spec.iamAuthenticatorConfig <object>	# IAMAuthenticatorConfig allows the specification of any additional user or role mappings for use when generating the aws-iam-authenticator configuration. If this is nil the default configuration is still generated for the cluster.
spec.iamAuthenticatorConfig.mapRoles <[]object>	# RoleMappings is a list of role mappings
spec.iamAuthenticatorConfig.mapRoles[].groups <[]string> (required)	# Groups is a list of kubernetes RBAC groups
spec.iamAuthenticatorConfig.mapRoles[].rolearn <string> (required) min=31	# RoleARN is the AWS ARN for the role to map
spec.iamAuthenticatorConfig.mapRoles[].username <string> (required)	# UserName is a kubernetes RBAC user subject
spec.iamAuthenticatorConfig.mapUsers <[]object>	# UserMappings is a list of user mappings
spec.iamAuthenticatorConfig.mapUsers[].groups <[]string> (required)	# Groups is a list of kubernetes RBAC groups
spec.iamAuthenticatorConfig.mapUsers[].userarn <string> (required) min=31	# UserARN is the AWS ARN for the user to map
spec.iamAuthenticatorConfig.mapUsers[].username <string> (required)	# UserName is a kubernetes RBAC user subject
spec.identityRef <object>	# IdentityRef is a reference to an identity to be used when reconciling the managed control plane. If no identity is specified, the default identity for this controller will be used.
spec.identityRef.kind <string> (required) enum=AWSClusterControllerIdentity|AWSClusterRoleIdentity|AWSClusterStaticIdentity	# Kind of the identity.
spec.identityRef.name <string> (required) min=1	# Name of the identity.
spec.imageLookupBaseOS <string>	# ImageLookupBaseOS is the name of the base operating system used to look up machine images when a machine does not specify an AMI. When set, this will be used for all cluster machines unless a machine specifies a different ImageLookupBaseOS.
spec.imageLookupFormat <string>	# ImageLookupFormat is the AMI naming format to look up machine images when a machine does not specify an AMI. When set, this will be used for all cluster machines unless a machine specifies a different ImageLookupOrg. Supports substitutions for {{.BaseOS}} and {{.K8sVersion}} with the base OS and kubernetes version, respectively. The BaseOS will be the value in ImageLookupBaseOS or ubuntu (the default), and the kubernetes version as defined by the packages produced by kubernetes/release without v as a prefix: 1.13.0, 1.12.5-mybuild.1, or 1.17.3. For example, the default image format of capa-ami-{{.BaseOS}}-?{{.K8sVersion}}-* will end up searching for AMIs that match the pattern capa-ami-ubuntu-?1.18.0-* for a Machine that is targeting kubernetes v1.18.0 and the ubuntu base OS. See also: https://golang.org/pkg/text/template/
spec.imageLookupOrg <string>	# ImageLookupOrg is the AWS Organization ID to look up machine images when a machine does not specify an AMI. When set, this will be used for all cluster machines unless a machine specifies a different ImageLookupOrg.
spec.kubeProxy <object>	# KubeProxy defines managed attributes of the kube-proxy daemonset
spec.kubeProxy.disable <boolean> default=false	# Disable set to true indicates that kube-proxy should be disabled. With EKS clusters kube-proxy is automatically installed into the cluster. For clusters where you want to use kube-proxy functionality that is provided with an alternate CNI, this option provides a way to specify that the kube-proxy daemonset should be deleted. You cannot set this to true if you are using the Amazon kube-proxy addon.
spec.logging <object>	# Logging specifies which EKS Cluster logs should be enabled. Entries for each of the enabled logs will be sent to CloudWatch
spec.logging.apiServer <boolean> (required) default=false	# APIServer indicates if the Kubernetes API Server log (kube-apiserver) shoulkd be enabled
spec.logging.audit <boolean> (required) default=false	# Audit indicates if the Kubernetes API audit log should be enabled
spec.logging.authenticator <boolean> (required) default=false	# Authenticator indicates if the iam authenticator log should be enabled
spec.logging.controllerManager <boolean> (required) default=false	# ControllerManager indicates if the controller manager (kube-controller-manager) log should be enabled
spec.logging.scheduler <boolean> (required) default=false	# Scheduler indicates if the Kubernetes scheduler (kube-scheduler) log should be enabled
spec.network <object>	# NetworkSpec encapsulates all things related to AWS network.
spec.network.additionalControlPlaneIngressRules <[]object>	# AdditionalControlPlaneIngressRules is an optional set of ingress rules to add to the control plane
spec.network.additionalControlPlaneIngressRules[].cidrBlocks <[]string>	# List of CIDR blocks to allow access from. Cannot be specified with SourceSecurityGroupID.
spec.network.additionalControlPlaneIngressRules[].description <string> (required)	# Description provides extended information about the ingress rule.
spec.network.additionalControlPlaneIngressRules[].fromPort <integer> (required) format=int64	# FromPort is the start of port range.
spec.network.additionalControlPlaneIngressRules[].ipv6CidrBlocks <[]string>	# List of IPv6 CIDR blocks to allow access from. Cannot be specified with SourceSecurityGroupID.
spec.network.additionalControlPlaneIngressRules[].natGatewaysIPsSource <boolean>	# NatGatewaysIPsSource use the NAT gateways IPs as the source for the ingress rule.
spec.network.additionalControlPlaneIngressRules[].protocol <string> (required) enum=-1|4|tcp|udp|icmp|58|50	# Protocol is the protocol for the ingress rule. Accepted values are "-1" (all), "4" (IP in IP),"tcp", "udp", "icmp", and "58" (ICMPv6), "50" (ESP).
spec.network.additionalControlPlaneIngressRules[].sourceSecurityGroupIds <[]string>	# The security group id to allow access from. Cannot be specified with CidrBlocks.
spec.network.additionalControlPlaneIngressRules[].sourceSecurityGroupRoles <[]string>	# The security group role to allow access from. Cannot be specified with CidrBlocks. The field will be combined with source security group IDs if specified.
spec.network.additionalControlPlaneIngressRules[].toPort <integer> (required) format=int64	# ToPort is the end of port range.
spec.network.additionalNodeIngressRules <[]object>	# AdditionalNodeIngressRules is an optional set of ingress rules to add to every node
spec.network.additionalNodeIngressRules[].cidrBlocks <[]string>	# List of CIDR blocks to allow access from. Cannot be specified with SourceSecurityGroupID.
spec.network.additionalNodeIngressRules[].description <string> (required)	# Description provides extended information about the ingress rule.
spec.network.additionalNodeIngressRules[].fromPort <integer> (required) format=int64	# FromPort is the start of port range.
spec.network.additionalNodeIngressRules[].ipv6CidrBlocks <[]string>	# List of IPv6 CIDR blocks to allow access from. Cannot be specified with SourceSecurityGroupID.
spec.network.additionalNodeIngressRules[].natGatewaysIPsSource <boolean>	# NatGatewaysIPsSource use the NAT gateways IPs as the source for the ingress rule.
spec.network.additionalNodeIngressRules[].protocol <string> (required) enum=-1|4|tcp|udp|icmp|58|50	# Protocol is the protocol for the ingress rule. Accepted values are "-1" (all), "4" (IP in IP),"tcp", "udp", "icmp", and "58" (ICMPv6), "50" (ESP).
spec.network.additionalNodeIngressRules[].sourceSecurityGroupIds <[]string>	# The security group id to allow access from. Cannot be specified with CidrBlocks.
spec.network.additionalNodeIngressRules[].sourceSecurityGroupRoles <[]string>	# The security group role to allow access from. Cannot be specified with CidrBlocks. The field will be combined with source security group IDs if specified.
spec.network.additionalNodeIngressRules[].toPort <integer> (required) format=int64	# ToPort is the end of port range.
spec.network.cni <object>	# CNI configuration
spec.network.cni.cniIngressRules <[]object>	# CNIIngressRules specify rules to apply to control plane and worker node security groups. The source for the rule will be set to control plane and worker security group IDs.
spec.network.cni.cniIngressRules[].description <string> (required)
spec.network.cni.cniIngressRules[].fromPort <integer> (required) format=int64
spec.network.cni.cniIngressRules[].protocol <string> (required)	# SecurityGroupProtocol defines the protocol type for a security group rule.
spec.network.cni.cniIngressRules[].toPort <integer> (required) format=int64
spec.network.nodePortIngressRuleCidrBlocks <[]string>	# NodePortIngressRuleCidrBlocks is an optional set of CIDR blocks to allow traffic to nodes' NodePort services. If none are specified here, all IPs are allowed to connect.
spec.network.securityGroupOverrides <map[string]string>	# SecurityGroupOverrides is an optional set of security groups to use for cluster instances This is optional - if not provided new security groups will be created for the cluster
spec.network.subnets <[]object>	# Subnets configuration.
spec.network.subnets[].availabilityZone <string>	# AvailabilityZone defines the availability zone to use for this subnet in the cluster's region.
spec.network.subnets[].cidrBlock <string>	# CidrBlock is the CIDR block to be used when the provider creates a managed VPC.
spec.network.subnets[].id <string> (required)	# ID defines a unique identifier to reference this resource. If you're bringing your subnet, set the AWS subnet-id here, it must start with `subnet-`. When the VPC is managed by CAPA, and you'd like the provider to create a subnet for you, the id can be set to any placeholder value that does not start with `subnet-`; upon creation, the subnet AWS identifier will be populated in the `ResourceID` field and the `id` field is going to be used as the subnet name. If you specify a tag called `Name`, it takes precedence.
spec.network.subnets[].ipv6CidrBlock <string>	# IPv6CidrBlock is the IPv6 CIDR block to be used when the provider creates a managed VPC. A subnet can have an IPv4 and an IPv6 address.
spec.network.subnets[].isIpv6 <boolean>	# IsIPv6 defines the subnet as an IPv6 subnet. A subnet is IPv6 when it is associated with an IPv6 CIDR.
spec.network.subnets[].isPublic <boolean>	# IsPublic defines the subnet as a public subnet. A subnet is public when it is associated with a route table that has a route to an internet gateway.
spec.network.subnets[].natGatewayId <string>	# NatGatewayID is the NAT gateway id associated with the subnet. Ignored unless the subnet is managed by the provider, in which case this is set on the public subnet where the NAT gateway resides. It is then used to determine routes for private subnets in the same AZ as the public subnet.
spec.network.subnets[].parentZoneName <string>	# ParentZoneName is the zone name where the current subnet's zone is tied when the zone is a Local Zone. The subnets in Local Zone or Wavelength Zone locations consume the ParentZoneName to select the correct private route table to egress traffic to the internet.
spec.network.subnets[].resourceID <string>	# ResourceID is the subnet identifier from AWS, READ ONLY. This field is populated when the provider manages the subnet.
spec.network.subnets[].routeTableId <string>	# RouteTableID is the routing table id associated with the subnet.
spec.network.subnets[].tags <map[string]string>	# Tags is a collection of tags describing the resource.
spec.network.subnets[].zoneType <string> enum=availability-zone|local-zone|wavelength-zone	# ZoneType defines the type of the zone where the subnet is created. The valid values are availability-zone, local-zone, and wavelength-zone. Subnet with zone type availability-zone (regular) is always selected to create cluster resources, like Load Balancers, NAT Gateways, Contol Plane nodes, etc. Subnet with zone type local-zone or wavelength-zone is not eligible to automatically create regular cluster resources. The public subnet in availability-zone or local-zone is associated with regular public route table with default route entry to a Internet Gateway. The public subnet in wavelength-zone is associated with a carrier public route table with default route entry to a Carrier Gateway. The private subnet in the availability-zone is associated with a private route table with the default route entry to a NAT Gateway created in that zone. The private subnet in the local-zone or wavelength-zone is associated with a private route table with the default route entry re-using the NAT Gateway in the Region (preferred from the parent zone, the zone type availability-zone in the region, or first table available).
spec.network.vpc <object>	# VPC configuration.
spec.network.vpc.availabilityZoneSelection <string> enum=Ordered|Random default="Ordered"	# AvailabilityZoneSelection specifies how AZs should be selected if there are more AZs in a region than specified by AvailabilityZoneUsageLimit. There are 2 selection schemes: Ordered - selects based on alphabetical order Random - selects AZs randomly in a region Defaults to Ordered
spec.network.vpc.availabilityZoneUsageLimit <integer> default=3 min=1	# AvailabilityZoneUsageLimit specifies the maximum number of availability zones (AZ) that should be used in a region when automatically creating subnets. If a region has more than this number of AZs then this number of AZs will be picked randomly when creating default subnets. Defaults to 3
spec.network.vpc.carrierGatewayId <string>	# CarrierGatewayID is the id of the internet gateway associated with the VPC, for carrier network (Wavelength Zones).
spec.network.vpc.cidrBlock <string>	# CidrBlock is the CIDR block to be used when the provider creates a managed VPC. Defaults to 10.0.0.0/16. Mutually exclusive with IPAMPool.
spec.network.vpc.elasticIpPool <object>	# ElasticIPPool contains specific configuration to allocate Public IPv4 address (Elastic IP) from user-defined pool brought to AWS for core infrastructure resources, like NAT Gateways and Public Network Load Balancers for the API Server.
spec.network.vpc.elasticIpPool.publicIpv4Pool <string> max=30	# PublicIpv4Pool sets a custom Public IPv4 Pool used to create Elastic IP address for resources created in public IPv4 subnets. Every IPv4 address, Elastic IP, will be allocated from the custom Public IPv4 pool that you brought to AWS, instead of Amazon-provided pool. The public IPv4 pool resource ID starts with 'ipv4pool-ec2'.
spec.network.vpc.elasticIpPool.publicIpv4PoolFallbackOrder <string> enum=amazon-pool|none	# PublicIpv4PoolFallBackOrder defines the fallback action when the Public IPv4 Pool has been exhausted, no more IPv4 address available in the pool. When set to 'amazon-pool', the controller check if the pool has available IPv4 address, when pool has reached the IPv4 limit, the address will be claimed from Amazon-pool (default). When set to 'none', the controller will fail the Elastic IP allocation when the publicIpv4Pool is exhausted.
spec.network.vpc.emptyRoutesDefaultVPCSecurityGroup <boolean>	# EmptyRoutesDefaultVPCSecurityGroup specifies whether the default VPC security group ingress and egress rules should be removed. By default, when creating a VPC, AWS creates a security group called `default` with ingress and egress rules that allow traffic from anywhere. The group could be used as a potential surface attack and it's generally suggested that the group rules are removed or modified appropriately. NOTE: This only applies when the VPC is managed by the Cluster API AWS controller.
spec.network.vpc.id <string>	# ID is the vpc-id of the VPC this provider should use to create resources.
spec.network.vpc.internetGatewayId <string>	# InternetGatewayID is the id of the internet gateway associated with the VPC.
spec.network.vpc.ipamPool <object>	# IPAMPool defines the IPAMv4 pool to be used for VPC. Mutually exclusive with CidrBlock.
spec.network.vpc.ipamPool.id <string>	# ID is the ID of the IPAM pool this provider should use to create VPC.
spec.network.vpc.ipamPool.name <string>	# Name is the name of the IPAM pool this provider should use to create VPC.
spec.network.vpc.ipamPool.netmaskLength <integer> format=int64	# The netmask length of the IPv4 CIDR you want to allocate to VPC from an Amazon VPC IP Address Manager (IPAM) pool. Defaults to /16 for IPv4 if not specified. Defaults to /56 for IPv6 if not specified.
spec.network.vpc.ipv6 <object>	# IPv6 contains ipv6 specific settings for the network.
spec.network.vpc.ipv6.cidrBlock <string>	# CidrBlock is the CIDR block provided by Amazon when VPC has enabled IPv6. Mutually exclusive with IPAMPool.
spec.network.vpc.ipv6.egressOnlyInternetGatewayId <string>	# EgressOnlyInternetGatewayID is the id of the egress only internet gateway associated with an IPv6 enabled VPC.
spec.network.vpc.ipv6.ipamPool <object>	# IPAMPool defines the IPAMv6 pool to be used for VPC. Mutually exclusive with CidrBlock.
spec.network.vpc.ipv6.ipamPool.id <string>	# ID is the ID of the IPAM pool this provider should use to create VPC.
spec.network.vpc.ipv6.ipamPool.name <string>	# Name is the name of the IPAM pool this provider should use to create VPC.
spec.network.vpc.ipv6.ipamPool.netmaskLength <integer> format=int64	# The netmask length of the IPv4 CIDR you want to allocate to VPC from an Amazon VPC IP Address Manager (IPAM) pool. Defaults to /16 for IPv4 if not specified. Defaults to /56 for IPv6 if not specified.
spec.network.vpc.ipv6.poolId <string>	# PoolID is the IP pool which must be defined in case of BYO IP is defined. Must be specified if CidrBlock is set. Mutually exclusive with IPAMPool.
spec.network.vpc.privateDnsHostnameTypeOnLaunch <string> enum=ip-name|resource-name	# PrivateDNSHostnameTypeOnLaunch is the type of hostname to assign to instances in the subnet at launch. For IPv4-only and dual-stack (IPv4 and IPv6) subnets, an instance DNS name can be based on the instance IPv4 address (ip-name) or the instance ID (resource-name). For IPv6 only subnets, an instance DNS name must be based on the instance ID (resource-name).
spec.network.vpc.secondaryCidrBlocks <[]object>	# SecondaryCidrBlocks are additional CIDR blocks to be associated when the provider creates a managed VPC. Defaults to none. Mutually exclusive with IPAMPool. This makes sense to use if, for example, you want to use a separate IP range for pods (e.g. Cilium ENI mode).
spec.network.vpc.secondaryCidrBlocks[].ipv4CidrBlock <string> (required) min=1	# IPv4CidrBlock is the IPv4 CIDR block to associate with the managed VPC.
spec.network.vpc.subnetSchema <string> enum=PreferPrivate|PreferPublic default="PreferPrivate"	# SubnetSchema specifies how CidrBlock should be divided on subnets in the VPC depending on the number of AZs. PreferPrivate - one private subnet for each AZ plus one other subnet that will be further sub-divided for the public subnets. PreferPublic - have the reverse logic of PreferPrivate, one public subnet for each AZ plus one other subnet that will be further sub-divided for the private subnets. Defaults to PreferPrivate
spec.network.vpc.tags <map[string]string>	# Tags is a collection of tags describing the resource.
spec.oidcIdentityProviderConfig <object>	# OIDCIdentityProviderConfig is used to specify the OIDC provider config to be attached with this eks cluster
spec.oidcIdentityProviderConfig.clientId <string> (required)	# This is also known as audience. The ID for the client application that makes authentication requests to the OpenID identity provider.
spec.oidcIdentityProviderConfig.groupsClaim <string>	# The JWT claim that the provider uses to return your groups.
spec.oidcIdentityProviderConfig.groupsPrefix <string>	# The prefix that is prepended to group claims to prevent clashes with existing names (such as system: groups). For example, the valueoidc: will create group names like oidc:engineering and oidc:infra.
spec.oidcIdentityProviderConfig.identityProviderConfigName <string> (required)	# The name of the OIDC provider configuration. IdentityProviderConfigName is a required field
spec.oidcIdentityProviderConfig.issuerUrl <string> (required)	# The URL of the OpenID identity provider that allows the API server to discover public signing keys for verifying tokens. The URL must begin with https:// and should correspond to the iss claim in the provider's OIDC ID tokens. Per the OIDC standard, path components are allowed but query parameters are not. Typically the URL consists of only a hostname, like https://server.example.org or https://example.com. This URL should point to the level below .well-known/openid-configuration and must be publicly accessible over the internet.
spec.oidcIdentityProviderConfig.requiredClaims <map[string]string>	# The key value pairs that describe required claims in the identity token. If set, each claim is verified to be present in the token with a matching value. For the maximum number of claims that you can require, see Amazon EKS service quotas (https://docs.aws.amazon.com/eks/latest/userguide/service-quotas.html) in the Amazon EKS User Guide.
spec.oidcIdentityProviderConfig.tags <map[string]string>	# tags to apply to oidc identity provider association
spec.oidcIdentityProviderConfig.usernameClaim <string>	# The JSON Web Token (JWT) claim to use as the username. The default is sub, which is expected to be a unique identifier of the end user. You can choose other claims, such as email or name, depending on the OpenID identity provider. Claims other than email are prefixed with the issuer URL to prevent naming clashes with other plug-ins.
spec.oidcIdentityProviderConfig.usernamePrefix <string>	# The prefix that is prepended to username claims to prevent clashes with existing names. If you do not provide this field, and username is a value other than email, the prefix defaults to issuerurl#. You can use the value - to disable all prefixing.
spec.partition <string>	# Partition is the AWS security partition being used. Defaults to "aws"
spec.region <string>	# The AWS Region the cluster lives in.
spec.restrictPrivateSubnets <boolean> default=false	# RestrictPrivateSubnets indicates that the EKS control plane should only use private subnets.
spec.roleAdditionalPolicies <[]string>	# RoleAdditionalPolicies allows you to attach additional polices to the control plane role. You must enable the EKSAllowAddRoles feature flag to incorporate these into the created role.
spec.roleName <string> min=2	# RoleName specifies the name of IAM role that gives EKS permission to make API calls. If the role is pre-existing we will treat it as unmanaged and not delete it on deletion. If the EKSEnableIAM feature flag is true and no name is supplied then a role is created.
spec.rolePath <string>	# RolePath sets the path to the role. For more information about paths, see IAM Identifiers (https://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the IAM User Guide. This parameter is optional. If it is not included, it defaults to a slash (/).
spec.rolePermissionsBoundary <string>	# RolePermissionsBoundary sets the ARN of the managed policy that is used to set the permissions boundary for the role. A permissions boundary policy defines the maximum permissions that identity-based policies can grant to an entity, but does not grant permissions. Permissions boundaries do not define the maximum permissions that a resource-based policy can grant to an entity. To learn more, see Permissions boundaries for IAM entities (https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_boundaries.html) in the IAM User Guide. For more information about policy types, see Policy types (https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies.html#access_policy-types) in the IAM User Guide.
spec.secondaryCidrBlock <string>	# SecondaryCidrBlock is the additional CIDR range to use for pod IPs. Must be within the 100.64.0.0/10 or 198.19.0.0/16 range.
spec.sshKeyName <string>	# SSHKeyName is the name of the ssh key to attach to the bastion host. Valid values are empty string (do not use SSH keys), a valid SSH key name, or omitted (use the default SSH key name)
spec.tokenMethod <string> enum=iam-authenticator|aws-cli default="iam-authenticator"	# TokenMethod is used to specify the method for obtaining a client token for communicating with EKS iam-authenticator - obtains a client token using iam-authentictor aws-cli - obtains a client token using the AWS CLI Defaults to iam-authenticator
spec.upgradePolicy <string> enum=extended|standard	# The cluster upgrade policy to use for the cluster. (Official AWS docs for this policy: https://docs.aws.amazon.com/eks/latest/userguide/view-upgrade-policy.html) `extended` upgrade policy indicates that the cluster will enter into extended support once the Kubernetes version reaches end of standard support. You will incur extended support charges with this setting. You can upgrade your cluster to a standard supported Kubernetes version to stop incurring extended support charges. `standard` upgrade policy indicates that the cluster is eligible for automatic upgrade at the end of standard support. You will not incur extended support charges with this setting but your EKS cluster will automatically upgrade to the next supported Kubernetes version in standard support. If omitted, new clusters will use the AWS default upgrade policy (which at the time of writing is "extended") and existing clusters will have their upgrade policy unchanged.
spec.version <string> pattern="^v?(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.?(\\.0|[1-9][0-9]*)?$" min=2	# Version defines the desired Kubernetes version. If no version number is supplied then the latest version of Kubernetes that EKS supports will be used.
spec.vpcCni <object>	# VpcCni is used to set configuration options for the VPC CNI plugin
spec.vpcCni.disable <boolean> default=false	# Disable indicates that the Amazon VPC CNI should be disabled. With EKS clusters the Amazon VPC CNI is automatically installed into the cluster. For clusters where you want to use an alternate CNI this option provides a way to specify that the Amazon VPC CNI should be deleted. You cannot set this to true if you are using the Amazon VPC CNI addon.
spec.vpcCni.env <[]object>	# Env defines a list of environment variables to apply to the `aws-node` DaemonSet
spec.vpcCni.env[].name <string> (required)	# Name of the environment variable. Must be a C_IDENTIFIER.
spec.vpcCni.env[].value <string>	# Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
spec.vpcCni.env[].valueFrom <object>	# Source for the environment variable's value. Cannot be used if value is not empty.
spec.vpcCni.env[].valueFrom.configMapKeyRef <object>	# Selects a key of a ConfigMap.
spec.vpcCni.env[].valueFrom.configMapKeyRef.key <string> (required)	# The key to select.
spec.vpcCni.env[].valueFrom.configMapKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.vpcCni.env[].valueFrom.configMapKeyRef.optional <boolean>	# Specify whether the ConfigMap or its key must be defined
spec.vpcCni.env[].valueFrom.fieldRef <object>	# Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
spec.vpcCni.env[].valueFrom.fieldRef.apiVersion <string>	# Version of the schema the FieldPath is written in terms of, defaults to "v1".
spec.vpcCni.env[].valueFrom.fieldRef.fieldPath <string> (required)	# Path of the field to select in the specified API version.
spec.vpcCni.env[].valueFrom.resourceFieldRef <object>	# Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
spec.vpcCni.env[].valueFrom.resourceFieldRef.containerName <string>	# Container name: required for volumes, optional for env vars
spec.vpcCni.env[].valueFrom.resourceFieldRef.divisor <int-or-string>	# Specifies the output format of the exposed resources, defaults to "1"
spec.vpcCni.env[].valueFrom.resourceFieldRef.resource <string> (required)	# Required: resource to select
spec.vpcCni.env[].valueFrom.secretKeyRef <object>	# Selects a key of a secret in the pod's namespace
spec.vpcCni.env[].valueFrom.secretKeyRef.key <string> (required)	# The key of the secret to select from. Must be a valid secret key.
spec.vpcCni.env[].valueFrom.secretKeyRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.vpcCni.env[].valueFrom.secretKeyRef.optional <boolean>	# Specify whether the Secret or its key must be defined
status <object>	# AWSManagedControlPlaneStatus defines the observed state of an Amazon EKS Cluster.
status.addons <[]object>	# Addons holds the current status of the EKS addons
status.addons[].arn <string> (required)	# ARN is the AWS ARN of the addon
status.addons[].createdAt <string> format=date-time	# CreatedAt is the date and time the addon was created at
status.addons[].issues <[]object>	# Issues is a list of issue associated with the addon
status.addons[].issues[].code <string>	# Code is the issue code
status.addons[].issues[].message <string>	# Message is the textual description of the issue
status.addons[].issues[].resourceIds <[]string>	# ResourceIDs is a list of resource ids for the issue
status.addons[].modifiedAt <string> format=date-time	# ModifiedAt is the date and time the addon was last modified
status.addons[].name <string> (required)	# Name is the name of the addon
status.addons[].serviceAccountRoleARN <string>	# ServiceAccountRoleArn is the ARN of the IAM role used for the service account
status.addons[].status <string>	# Status is the status of the addon
status.addons[].version <string> (required)	# Version is the version of the addon to use
status.bastion <object>	# Bastion holds details of the instance that is used as a bastion jump box
status.bastion.addresses <[]object>	# Addresses contains the AWS instance associated addresses.
status.bastion.addresses[].address <string> (required) min=1 max=256	# address is the machine address.
status.bastion.addresses[].type <string> (required) enum=Hostname|ExternalIP|InternalIP|ExternalDNS|InternalDNS	# type is the machine address type, one of Hostname, ExternalIP, InternalIP, ExternalDNS or InternalDNS.
status.bastion.assignPrimaryIPv6 <string>	# AssignPrimaryIPv6 specifies whether to enable assigning a primary IPv6 address to the primary network Interface.
status.bastion.availabilityZone <string>	# Availability zone of instance
status.bastion.capacityReservationId <string>	# CapacityReservationID specifies the target Capacity Reservation into which the instance should be launched.
status.bastion.capacityReservationPreference <string>	# CapacityReservationPreference specifies the preference for use of Capacity Reservations by the instance. Valid values include: "Open": The instance may make use of open Capacity Reservations that match its AZ and InstanceType "None": The instance may not make use of any Capacity Reservations. This is to conserve open reservations for desired workloads "CapacityReservationsOnly": The instance will only run if matched or targeted to a Capacity Reservation. Note that this is incompatible with a MarketType of `Spot`
status.bastion.cpuOptions <object>	# CPUOptions defines CPU-related settings for the instance, including the confidential computing policy. When omitted, this means no opinion and the AWS platform is left to choose a reasonable default.
status.bastion.cpuOptions.confidentialCompute <string> enum=Disabled|AMDEncryptedVirtualizationNestedPaging	# ConfidentialCompute specifies whether confidential computing should be enabled for the instance, and, if so, which confidential computing technology to use. Valid values are: Disabled, AMDEncryptedVirtualizationNestedPaging When set to Disabled, confidential computing will be disabled for the instance. When set to AMDEncryptedVirtualizationNestedPaging, AMD SEV-SNP will be used as the confidential computing technology for the instance. In this case, ensure the following conditions are met: 1) The selected instance type supports AMD SEV-SNP. 2) The selected AWS region supports AMD SEV-SNP. 3) The selected AMI supports AMD SEV-SNP. More details can be checked at https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/sev-snp.html When omitted, this means no opinion and the AWS platform is left to choose a reasonable default, which is subject to change without notice. The current default is Disabled.
status.bastion.cpuOptions.nestedVirtualization <string> enum=enabled|disabled	# NestedVirtualization specifies whether to enable nested virtualization on the instance. Nested virtualization is supported on C8i, M8i, and R8i instance types. Valid values are: enabled, disabled
status.bastion.dynamicHostAllocation <object>	# DynamicHostAllocation enables automatic allocation of dedicated hosts. This field is mutually exclusive with HostID.
status.bastion.dynamicHostAllocation.tags <map[string]string>	# Tags to apply to the allocated dedicated host.
status.bastion.ebsOptimized <boolean>	# Indicates whether the instance is optimized for Amazon EBS I/O.
status.bastion.enaSupport <boolean>	# Specifies whether enhanced networking with ENA is enabled.
status.bastion.hostAffinity <string> enum=default|host	# HostAffinity specifies the dedicated host affinity setting for the instance. When HostAffinity is set to "host", an instance started onto a specific host always restarts on the same host if stopped: - If HostID is set, the instance launches on the specific host and must return to that same host after any stop/start (Targeted & Pinned). - If HostID is not set, the instance gets launched on any available and must returns to the same host after any stop/start (Auto-placed & Pinned). When HostAffinity is set to "default" (the default value), the instance (when restarted) can return on any available host: - If HostID is set, the instance launches on the specified host now, but (when restarted) can return to any available hosts (Targeted & Flexible). - If HostID is not set, the instance launches on any available host now, and (when restarted) can return to any available hosts (Auto-placed & Flexible).
status.bastion.hostID <string>	# HostID specifies the dedicated host on which the instance should be started.
status.bastion.iamProfile <string>	# The name of the IAM instance profile associated with the instance, if applicable.
status.bastion.id <string> (required)
status.bastion.imageId <string>	# The ID of the AMI used to launch the instance.
status.bastion.instanceMetadataOptions <object>	# InstanceMetadataOptions is the metadata options for the EC2 instance.
status.bastion.instanceMetadataOptions.httpEndpoint <string> enum=enabled|disabled default="enabled"	# Enables or disables the HTTP metadata endpoint on your instances. If you specify a value of disabled, you cannot access your instance metadata. Default: enabled
status.bastion.instanceMetadataOptions.httpProtocolIpv6 <string> enum=enabled|disabled default="disabled"	# Enables or disables the IPv6 endpoint for the instance metadata service. This applies only if you enabled the HTTP metadata endpoint. Default: disabled
status.bastion.instanceMetadataOptions.httpPutResponseHopLimit <integer> default=1 format=int64 min=1 max=64	# The desired HTTP PUT response hop limit for instance metadata requests. The larger the number, the further instance metadata requests can travel. Default: 1
status.bastion.instanceMetadataOptions.httpTokens <string> enum=optional|required default="optional"	# The state of token usage for your instance metadata requests. If the state is optional, you can choose to retrieve instance metadata with or without a session token on your request. If you retrieve the IAM role credentials without a token, the version 1.0 role credentials are returned. If you retrieve the IAM role credentials using a valid session token, the version 2.0 role credentials are returned. If the state is required, you must send a session token with any instance metadata retrieval requests. In this state, retrieving the IAM role credentials always returns the version 2.0 credentials; the version 1.0 credentials are not available. Default: optional
status.bastion.instanceMetadataOptions.instanceMetadataTags <string> enum=enabled|disabled default="disabled"	# Set to enabled to allow access to instance tags from the instance metadata. Set to disabled to turn off access to instance tags from the instance metadata. For more information, see Work with instance tags using the instance metadata (https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html#work-with-tags-in-IMDS). Default: disabled
status.bastion.instanceState <string>	# The current state of the instance.
status.bastion.ipv6Address <string>	# The IPv6 address assigned to the instance.
status.bastion.marketType <string> enum=OnDemand|Spot|CapacityBlock	# MarketType specifies the type of market for the EC2 instance. Valid values include: "OnDemand" (default): The instance runs as a standard OnDemand instance. "Spot": The instance runs as a Spot instance. When SpotMarketOptions is provided, the marketType defaults to "Spot". "CapacityBlock": The instance utilizes pre-purchased compute capacity (capacity blocks) with AWS Capacity Reservations. If this value is selected, CapacityReservationID must be specified to identify the target reservation. If marketType is not specified and spotMarketOptions is provided, the marketType defaults to "Spot".
status.bastion.networkInterfaceType <string>	# NetworkInterfaceType is the interface type of the primary network Interface.
status.bastion.networkInterfaces <[]string>	# Specifies ENIs attached to instance
status.bastion.nonRootVolumes <[]object>	# Configuration options for the non root storage volumes.
status.bastion.nonRootVolumes[].deviceName <string>	# Device name
status.bastion.nonRootVolumes[].encrypted <boolean>	# Encrypted is whether the volume should be encrypted or not.
status.bastion.nonRootVolumes[].encryptionKey <string>	# EncryptionKey is the KMS key to use to encrypt the volume. Can be either a KMS key ID or ARN. If Encrypted is set and this is omitted, the default AWS key will be used. The key must already exist and be accessible by the controller.
status.bastion.nonRootVolumes[].iops <integer> format=int64	# IOPS is the number of IOPS requested for the disk. Not applicable to all types.
status.bastion.nonRootVolumes[].size <integer> (required) format=int64 min=8	# Size specifies size (in Gi) of the storage device. Must be greater than the image snapshot size or 8 (whichever is greater).
status.bastion.nonRootVolumes[].throughput <integer> format=int64	# Throughput to provision in MiB/s supported for the volume type. Not applicable to all types.
status.bastion.nonRootVolumes[].type <string>	# Type is the type of the volume (e.g. gp2, io1, etc...).
status.bastion.placementGroupName <string>	# PlacementGroupName specifies the name of the placement group in which to launch the instance.
status.bastion.placementGroupPartition <integer> format=int64 min=1 max=7	# PlacementGroupPartition is the partition number within the placement group in which to launch the instance. This value is only valid if the placement group, referred in `PlacementGroupName`, was created with strategy set to partition.
status.bastion.privateDnsName <object>	# PrivateDNSName is the options for the instance hostname.
status.bastion.privateDnsName.enableResourceNameDnsAAAARecord <boolean>	# EnableResourceNameDNSAAAARecord indicates whether to respond to DNS queries for instance hostnames with DNS AAAA records.
status.bastion.privateDnsName.enableResourceNameDnsARecord <boolean>	# EnableResourceNameDNSARecord indicates whether to respond to DNS queries for instance hostnames with DNS A records.
status.bastion.privateDnsName.hostnameType <string> enum=ip-name|resource-name	# The type of hostname to assign to an instance.
status.bastion.privateIp <string>	# The private IPv4 address assigned to the instance.
status.bastion.publicIPOnLaunch <boolean>	# PublicIPOnLaunch is the option to associate a public IP on instance launch
status.bastion.publicIp <string>	# The public IPv4 address assigned to the instance, if applicable.
status.bastion.rootVolume <object>	# Configuration options for the root storage volume.
status.bastion.rootVolume.deviceName <string>	# Device name
status.bastion.rootVolume.encrypted <boolean>	# Encrypted is whether the volume should be encrypted or not.
status.bastion.rootVolume.encryptionKey <string>	# EncryptionKey is the KMS key to use to encrypt the volume. Can be either a KMS key ID or ARN. If Encrypted is set and this is omitted, the default AWS key will be used. The key must already exist and be accessible by the controller.
status.bastion.rootVolume.iops <integer> format=int64	# IOPS is the number of IOPS requested for the disk. Not applicable to all types.
status.bastion.rootVolume.size <integer> (required) format=int64 min=8	# Size specifies size (in Gi) of the storage device. Must be greater than the image snapshot size or 8 (whichever is greater).
status.bastion.rootVolume.throughput <integer> format=int64	# Throughput to provision in MiB/s supported for the volume type. Not applicable to all types.
status.bastion.rootVolume.type <string>	# Type is the type of the volume (e.g. gp2, io1, etc...).
status.bastion.securityGroupIds <[]string>	# SecurityGroupIDs are one or more security group IDs this instance belongs to.
status.bastion.spotMarketOptions <object>	# SpotMarketOptions option for configuring instances to be run using AWS Spot instances.
status.bastion.spotMarketOptions.maxPrice <string>	# MaxPrice defines the maximum price the user is willing to pay for Spot VM instances
status.bastion.sshKeyName <string>	# The name of the SSH key pair.
status.bastion.subnetId <string>	# The ID of the subnet of the instance.
status.bastion.tags <map[string]string>	# The tags associated with the instance.
status.bastion.tenancy <string>	# Tenancy indicates if instance should run on shared or single-tenant hardware.
status.bastion.type <string>	# The instance type.
status.bastion.userData <string>	# UserData is the raw data script passed to the instance which is run upon bootstrap. This field must not be base64 encoded and should only be used when running a new instance.
status.bastion.volumeIDs <[]string>	# IDs of the instance's volumes
status.conditions <[]object>	# Conditions specifies the cpnditions for the managed control plane
status.conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.conditions[].message <string> min=1 max=10240	# message is a human readable message indicating details about the transition. This field may be empty.
status.conditions[].reason <string> min=1 max=256	# reason is the reason for the condition's last transition in CamelCase. The specific API may choose whether or not this field is considered a guaranteed API. This field may be empty.
status.conditions[].severity <string> max=32	# severity provides an explicit classification of Reason code, so the users or machines can immediately understand the current situation and act accordingly. The Severity field MUST be set only when Status=False.
status.conditions[].status <string> (required)	# status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required) min=1 max=256	# type of condition in CamelCase or in foo.example.com/CamelCase. Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important.
status.externalManagedControlPlane <boolean> default=true	# ExternalManagedControlPlane indicates to cluster-api that the control plane is managed by an external service such as AKS, EKS, GKE, etc.
status.failureDomains <map[string]object>	# FailureDomains specifies a list fo available availability zones that can be used
status.failureDomains.<key>.attributes <map[string]string>	# attributes is a free form map of attributes an infrastructure provider might use or require.
status.failureDomains.<key>.controlPlane <boolean>	# controlPlane determines if this failure domain is suitable for use by control plane machines.
status.failureMessage <string>	# ErrorMessage indicates that there is a terminal problem reconciling the state, and will be set to a descriptive error message.
status.identityProviderStatus <object>	# IdentityProviderStatus holds the status for associated identity provider
status.identityProviderStatus.arn <string>	# ARN holds the ARN of associated identity provider
status.identityProviderStatus.status <string>	# Status holds current status of associated identity provider
status.initialized <boolean>	# Initialized denotes whether or not the control plane has the uploaded kubernetes config-map.
status.networkStatus <object>	# Networks holds details about the AWS networking resources used by the control plane
status.networkStatus.apiServerElb <object>	# APIServerELB is the Kubernetes api server load balancer.
status.networkStatus.apiServerElb.arn <string>	# ARN of the load balancer. Unlike the ClassicLB, ARN is used mostly to define and get it.
status.networkStatus.apiServerElb.attributes <object>	# ClassicElbAttributes defines extra attributes associated with the load balancer.
status.networkStatus.apiServerElb.attributes.crossZoneLoadBalancing <boolean>	# CrossZoneLoadBalancing enables the classic load balancer load balancing.
status.networkStatus.apiServerElb.attributes.idleTimeout <integer> format=int64	# IdleTimeout is time that the connection is allowed to be idle (no data has been sent over the connection) before it is closed by the load balancer.
status.networkStatus.apiServerElb.availabilityZones <[]string>	# AvailabilityZones is an array of availability zones in the VPC attached to the load balancer.
status.networkStatus.apiServerElb.dnsName <string>	# DNSName is the dns name of the load balancer.
status.networkStatus.apiServerElb.elbAttributes <map[string]string>	# ELBAttributes defines extra attributes associated with v2 load balancers.
status.networkStatus.apiServerElb.elbListeners <[]object>	# ELBListeners is an array of listeners associated with the load balancer. There must be at least one.
status.networkStatus.apiServerElb.elbListeners[].port <integer> (required) format=int64
status.networkStatus.apiServerElb.elbListeners[].protocol <string> (required)	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.apiServerElb.elbListeners[].targetGroup <object> (required)	# TargetGroupSpec specifies target group settings for a given listener. This is created first, and the ARN is then passed to the listener.
status.networkStatus.apiServerElb.elbListeners[].targetGroup.ipType <string>	# IPType is the IP address type for the target group.
status.networkStatus.apiServerElb.elbListeners[].targetGroup.name <string> (required) max=32	# Name of the TargetGroup. Must be unique over the same group of listeners.
status.networkStatus.apiServerElb.elbListeners[].targetGroup.port <integer> (required) format=int64	# Port is the exposed port
status.networkStatus.apiServerElb.elbListeners[].targetGroup.protocol <string> (required) enum=tcp|tls|udp|TCP|TLS|UDP	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck <object>	# HealthCheck is the elb health check associated with the load balancer.
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.intervalSeconds <integer> format=int64
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.path <string>
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.port <string>
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.protocol <string>
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.thresholdCount <integer> format=int64
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.timeoutSeconds <integer> format=int64
status.networkStatus.apiServerElb.elbListeners[].targetGroup.targetGroupHealthCheck.unhealthyThresholdCount <integer> format=int64
status.networkStatus.apiServerElb.elbListeners[].targetGroup.vpcId <string> (required)
status.networkStatus.apiServerElb.healthChecks <object>	# HealthCheck is the classic elb health check associated with the load balancer.
status.networkStatus.apiServerElb.healthChecks.healthyThreshold <integer> (required) format=int64
status.networkStatus.apiServerElb.healthChecks.interval <integer> (required) format=int64	# A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.
status.networkStatus.apiServerElb.healthChecks.target <string> (required)
status.networkStatus.apiServerElb.healthChecks.timeout <integer> (required) format=int64	# A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.
status.networkStatus.apiServerElb.healthChecks.unhealthyThreshold <integer> (required) format=int64
status.networkStatus.apiServerElb.listeners <[]object>	# ClassicELBListeners is an array of classic elb listeners associated with the load balancer. There must be at least one.
status.networkStatus.apiServerElb.listeners[].instancePort <integer> (required) format=int64
status.networkStatus.apiServerElb.listeners[].instanceProtocol <string> (required)	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.apiServerElb.listeners[].port <integer> (required) format=int64
status.networkStatus.apiServerElb.listeners[].protocol <string> (required)	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.apiServerElb.loadBalancerIPAddressType <string> enum=ipv4|dualstack|dualstack-without-public-ipv4	# LoadBalancerIPAddressType specifies the IP address type for the load balancer.
status.networkStatus.apiServerElb.loadBalancerType <string> enum=classic|elb|alb|nlb	# LoadBalancerType sets the type for a load balancer. The default type is classic.
status.networkStatus.apiServerElb.name <string>	# The name of the load balancer. It must be unique within the set of load balancers defined in the region. It also serves as identifier.
status.networkStatus.apiServerElb.scheme <string>	# Scheme is the load balancer scheme, either internet-facing or private.
status.networkStatus.apiServerElb.securityGroupIds <[]string>	# SecurityGroupIDs is an array of security groups assigned to the load balancer.
status.networkStatus.apiServerElb.subnetIds <[]string>	# SubnetIDs is an array of subnets in the VPC attached to the load balancer.
status.networkStatus.apiServerElb.tags <map[string]string>	# Tags is a map of tags associated with the load balancer.
status.networkStatus.natGatewaysIPs <[]string>	# NatGatewaysIPs contains the public IPs of the NAT Gateways
status.networkStatus.secondaryAPIServerELB <object>	# SecondaryAPIServerELB is the secondary Kubernetes api server load balancer.
status.networkStatus.secondaryAPIServerELB.arn <string>	# ARN of the load balancer. Unlike the ClassicLB, ARN is used mostly to define and get it.
status.networkStatus.secondaryAPIServerELB.attributes <object>	# ClassicElbAttributes defines extra attributes associated with the load balancer.
status.networkStatus.secondaryAPIServerELB.attributes.crossZoneLoadBalancing <boolean>	# CrossZoneLoadBalancing enables the classic load balancer load balancing.
status.networkStatus.secondaryAPIServerELB.attributes.idleTimeout <integer> format=int64	# IdleTimeout is time that the connection is allowed to be idle (no data has been sent over the connection) before it is closed by the load balancer.
status.networkStatus.secondaryAPIServerELB.availabilityZones <[]string>	# AvailabilityZones is an array of availability zones in the VPC attached to the load balancer.
status.networkStatus.secondaryAPIServerELB.dnsName <string>	# DNSName is the dns name of the load balancer.
status.networkStatus.secondaryAPIServerELB.elbAttributes <map[string]string>	# ELBAttributes defines extra attributes associated with v2 load balancers.
status.networkStatus.secondaryAPIServerELB.elbListeners <[]object>	# ELBListeners is an array of listeners associated with the load balancer. There must be at least one.
status.networkStatus.secondaryAPIServerELB.elbListeners[].port <integer> (required) format=int64
status.networkStatus.secondaryAPIServerELB.elbListeners[].protocol <string> (required)	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup <object> (required)	# TargetGroupSpec specifies target group settings for a given listener. This is created first, and the ARN is then passed to the listener.
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.ipType <string>	# IPType is the IP address type for the target group.
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.name <string> (required) max=32	# Name of the TargetGroup. Must be unique over the same group of listeners.
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.port <integer> (required) format=int64	# Port is the exposed port
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.protocol <string> (required) enum=tcp|tls|udp|TCP|TLS|UDP	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck <object>	# HealthCheck is the elb health check associated with the load balancer.
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.intervalSeconds <integer> format=int64
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.path <string>
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.port <string>
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.protocol <string>
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.thresholdCount <integer> format=int64
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.timeoutSeconds <integer> format=int64
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.targetGroupHealthCheck.unhealthyThresholdCount <integer> format=int64
status.networkStatus.secondaryAPIServerELB.elbListeners[].targetGroup.vpcId <string> (required)
status.networkStatus.secondaryAPIServerELB.healthChecks <object>	# HealthCheck is the classic elb health check associated with the load balancer.
status.networkStatus.secondaryAPIServerELB.healthChecks.healthyThreshold <integer> (required) format=int64
status.networkStatus.secondaryAPIServerELB.healthChecks.interval <integer> (required) format=int64	# A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.
status.networkStatus.secondaryAPIServerELB.healthChecks.target <string> (required)
status.networkStatus.secondaryAPIServerELB.healthChecks.timeout <integer> (required) format=int64	# A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.
status.networkStatus.secondaryAPIServerELB.healthChecks.unhealthyThreshold <integer> (required) format=int64
status.networkStatus.secondaryAPIServerELB.listeners <[]object>	# ClassicELBListeners is an array of classic elb listeners associated with the load balancer. There must be at least one.
status.networkStatus.secondaryAPIServerELB.listeners[].instancePort <integer> (required) format=int64
status.networkStatus.secondaryAPIServerELB.listeners[].instanceProtocol <string> (required)	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.secondaryAPIServerELB.listeners[].port <integer> (required) format=int64
status.networkStatus.secondaryAPIServerELB.listeners[].protocol <string> (required)	# ELBProtocol defines listener protocols for a load balancer.
status.networkStatus.secondaryAPIServerELB.loadBalancerIPAddressType <string> enum=ipv4|dualstack|dualstack-without-public-ipv4	# LoadBalancerIPAddressType specifies the IP address type for the load balancer.
status.networkStatus.secondaryAPIServerELB.loadBalancerType <string> enum=classic|elb|alb|nlb	# LoadBalancerType sets the type for a load balancer. The default type is classic.
status.networkStatus.secondaryAPIServerELB.name <string>	# The name of the load balancer. It must be unique within the set of load balancers defined in the region. It also serves as identifier.
status.networkStatus.secondaryAPIServerELB.scheme <string>	# Scheme is the load balancer scheme, either internet-facing or private.
status.networkStatus.secondaryAPIServerELB.securityGroupIds <[]string>	# SecurityGroupIDs is an array of security groups assigned to the load balancer.
status.networkStatus.secondaryAPIServerELB.subnetIds <[]string>	# SubnetIDs is an array of subnets in the VPC attached to the load balancer.
status.networkStatus.secondaryAPIServerELB.tags <map[string]string>	# Tags is a map of tags associated with the load balancer.
status.networkStatus.securityGroups <map[string]object>	# SecurityGroups is a map from the role/kind of the security group to its unique name, if any.
status.networkStatus.securityGroups.<key>.id <string> (required)	# ID is a unique identifier.
status.networkStatus.securityGroups.<key>.ingressRule <[]object>	# IngressRules is the inbound rules associated with the security group.
status.networkStatus.securityGroups.<key>.ingressRule[].cidrBlocks <[]string>	# List of CIDR blocks to allow access from. Cannot be specified with SourceSecurityGroupID.
status.networkStatus.securityGroups.<key>.ingressRule[].description <string> (required)	# Description provides extended information about the ingress rule.
status.networkStatus.securityGroups.<key>.ingressRule[].fromPort <integer> (required) format=int64	# FromPort is the start of port range.
status.networkStatus.securityGroups.<key>.ingressRule[].ipv6CidrBlocks <[]string>	# List of IPv6 CIDR blocks to allow access from. Cannot be specified with SourceSecurityGroupID.
status.networkStatus.securityGroups.<key>.ingressRule[].natGatewaysIPsSource <boolean>	# NatGatewaysIPsSource use the NAT gateways IPs as the source for the ingress rule.
status.networkStatus.securityGroups.<key>.ingressRule[].protocol <string> (required) enum=-1|4|tcp|udp|icmp|58|50	# Protocol is the protocol for the ingress rule. Accepted values are "-1" (all), "4" (IP in IP),"tcp", "udp", "icmp", and "58" (ICMPv6), "50" (ESP).
status.networkStatus.securityGroups.<key>.ingressRule[].sourceSecurityGroupIds <[]string>	# The security group id to allow access from. Cannot be specified with CidrBlocks.
status.networkStatus.securityGroups.<key>.ingressRule[].sourceSecurityGroupRoles <[]string>	# The security group role to allow access from. Cannot be specified with CidrBlocks. The field will be combined with source security group IDs if specified.
status.networkStatus.securityGroups.<key>.ingressRule[].toPort <integer> (required) format=int64	# ToPort is the end of port range.
status.networkStatus.securityGroups.<key>.name <string> (required)	# Name is the security group name.
status.networkStatus.securityGroups.<key>.tags <map[string]string>	# Tags is a map of tags associated with the security group.
status.oidcProvider <object>	# OIDCProvider holds the status of the identity provider for this cluster
status.oidcProvider.arn <string>	# ARN holds the ARN of the provider
status.oidcProvider.trustPolicy <string>	# TrustPolicy contains the boilerplate IAM trust policy to use for IRSA
status.ready <boolean> (required) default=false	# Ready denotes that the AWSManagedControlPlane API Server is ready to receive requests and that the VPC infra is ready.
status.version <string>	# Version represents the minimum Kubernetes version for the control plane machines in the cluster.
