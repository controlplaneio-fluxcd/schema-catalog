# schema source: Seldon Core v2 v2.10.2 https://github.com/SeldonIO/seldon-core
apiVersion <string> enum=mlops.seldon.io/v1alpha1
kind <string> enum=Pipeline
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# PipelineSpec defines the desired state of Pipeline
spec.allowCycles <boolean>	# Allow cyclic pipeline
spec.dataflow <object>	# Dataflow specs
spec.dataflow.cleanTopicsOnDelete <boolean>	# Flag to indicate whether the kafka input/output topics should be cleaned up when the model is deleted Default false
spec.input <object>	# External inputs to this pipeline, optional
spec.input.externalInputs <[]string>	# Previous external pipeline steps to receive data from
spec.input.externalTriggers <[]string>	# Triggers required to activate inputs
spec.input.joinType <string> enum=inner|outer|any default="inner"
spec.input.joinWindowMs <integer> format=int32	# msecs to wait for messages from multiple inputs to arrive before joining the inputs
spec.input.tensorMap <map[string]string>	# Map of tensor name conversions to use e.g. output1 -> input1
spec.input.triggersJoinType <string> enum=inner|outer|any default="inner"
spec.maxStepRevisits <integer> format=int32	# Maximum number of times a step can be revisited
spec.output <object>	# Synchronous output from this pipeline, optional
spec.output.joinWindowMs <integer> format=int32	# msecs to wait for messages from multiple inputs to arrive before joining the inputs
spec.output.steps <[]string>	# Previous step to receive data from
spec.output.stepsJoin <string> enum=inner|outer|any default="inner"
spec.output.tensorMap <map[string]string>	# Map of tensor name conversions to use e.g. output1 -> input1
spec.steps <[]object> (required)	# The steps of this inference graph pipeline
spec.steps[].batch <object>	# Batch size of request required before data will be sent to this step
spec.steps[].batch.rolling <boolean>
spec.steps[].batch.size <integer> format=int32
spec.steps[].batch.windowMs <integer> format=int32
spec.steps[].inputs <[]string>	# Previous step to receive data from
spec.steps[].inputsJoinType <string> enum=inner|outer|any default="inner"
spec.steps[].joinWindowMs <integer> format=int32	# msecs to wait for messages from multiple inputs to arrive before joining the inputs
spec.steps[].name <string> (required)	# Name of the step
spec.steps[].tensorMap <map[string]string>	# Map of tensor name conversions to use e.g. output1 -> input1
spec.steps[].triggers <[]string>	# Triggers required to activate step
spec.steps[].triggersJoinType <string> enum=inner|outer|any
status <object>	# PipelineStatus defines the observed state of Pipeline
status.annotations <map[string]string>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
