# schema source: Seldon Core v2 v2.10.2 https://github.com/SeldonIO/seldon-core
apiVersion <string> enum=mlops.seldon.io/v1alpha1
kind <string> enum=Model
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ModelSpec defines the desired state of Model
spec.artifactVersion <integer> format=int32	# Artifact Version A v2 version folder to select from storage bucket
spec.dataflow <object>	# Dataflow spec
spec.dataflow.cleanTopicsOnDelete <boolean>	# Flag to indicate whether the kafka input/output topics should be cleaned up when the model is deleted Default false
spec.dedicated <boolean>	# Dedicated server exclusive to this model Default false
spec.explainer <object>	# Explainer spec
spec.explainer.modelRef <string>	# one of the following need to be set for blackbox explainers Reference to Model
spec.explainer.pipelineRef <string>	# Reference to Pipeline
spec.explainer.type <string>	# type of explainer
spec.llm <object>	# Llm spec
spec.llm.modelRef <string>	# one of the following need to be set for the llm Reference to Model
spec.llm.pipelineRef <string>	# Reference to Pipeline
spec.logger <object>	# Payload logging
spec.logger.percent <integer>	# Percentage of payloads to log
spec.maxReplicas <integer> format=int32	# Max number of replicas - default equal to 0
spec.memory <int-or-string>	# Memory needed for model
spec.minReplicas <integer> format=int32	# Min number of replicas - default equal to 0
spec.modelType <string>	# Model type
spec.parameters <[]object>	# Parameters to load with model
spec.parameters[].name <string> (required)
spec.parameters[].value <string> (required)
spec.preloaded <boolean>	# Model already loaded on a server. Don't schedule. Default false
spec.replicas <integer> format=int32	# Number of replicas - default 1
spec.requirements <[]string>	# List of extra requirements for this model to be loaded on a compatible server
spec.schemaUri <string>	# Schema URI
spec.secretName <string>	# Secret name
spec.server <string>	# Name of the Server to deploy this artifact
spec.storageUri <string> (required)	# Storage URI for the model repository
status <object>	# ModelStatus defines the observed state of Model
status.annotations <map[string]string>	# Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
status.availableReplicas <integer> format=int32	# Number of available replicas
status.conditions <[]object>	# Conditions the latest available observations of a resource's current state.
status.conditions[].lastTransitionTime <string>	# LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].severity <string>	# Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type of condition.
status.modelgwReady <string>	# Model Gateway ready status
status.observedGeneration <integer> format=int64	# ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
status.replicas <integer> format=int32	# Total number of replicas targeted by this model
status.selector <string>
