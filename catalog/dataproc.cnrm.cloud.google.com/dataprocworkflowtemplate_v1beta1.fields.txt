# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=dataproc.cnrm.cloud.google.com/v1beta1
kind <string> enum=DataprocWorkflowTemplate
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)
spec.dagTimeout <string>	# Immutable. Optional. Timeout duration for the DAG of jobs, expressed in seconds (see [JSON representation of duration](https://developers.google.com/protocol-buffers/docs/proto3#json)). The timeout duration must be from 10 minutes ("600s") to 24 hours ("86400s"). The timer begins when the first job is submitted. If the workflow is running at the end of the timeout period, any remaining jobs are cancelled, the workflow is ended, and if the workflow was running on a [managed cluster](/dataproc/docs/concepts/workflows/using-workflows#configuring_or_selecting_a_cluster), the cluster is deleted.
spec.jobs <[]object> (required)	# Immutable. Required. The Directed Acyclic Graph of Jobs to submit.
spec.jobs[].hadoopJob <object>	# Immutable. Optional. Job is a Hadoop job.
spec.jobs[].hadoopJob.additionalProperties <any>
spec.jobs[].hadoopJob.archiveUris <[]string>	# Immutable. Optional. HCFS URIs of archives to be extracted in the working directory of Hadoop drivers and tasks. Supported file types: .jar, .tar, .tar.gz, .tgz, or .zip.
spec.jobs[].hadoopJob.args <[]string>	# Immutable. Optional. The arguments to pass to the driver. Do not include arguments, such as `-libjars` or `-Dfoo=bar`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
spec.jobs[].hadoopJob.fileUris <[]string>	# Immutable. Optional. HCFS (Hadoop Compatible Filesystem) URIs of files to be copied to the working directory of Hadoop drivers and distributed tasks. Useful for naively parallel tasks.
spec.jobs[].hadoopJob.jarFileUris <[]string>	# Immutable. Optional. Jar file URIs to add to the CLASSPATHs of the Hadoop driver and tasks.
spec.jobs[].hadoopJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].hadoopJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].hadoopJob.mainClass <string>	# Immutable. The name of the driver's main class. The jar file containing the class must be in the default CLASSPATH or specified in `jar_file_uris`.
spec.jobs[].hadoopJob.mainJarFileUri <string>	# Immutable. The HCFS URI of the jar file containing the main class. Examples: 'gs://foo-bucket/analytics-binaries/extract-useful-metrics-mr.jar' 'hdfs:/tmp/test-samples/custom-wordcount.jar' 'file:///home/usr/lib/hadoop-mapreduce/hadoop-mapreduce-examples.jar'
spec.jobs[].hadoopJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values, used to configure Hadoop. Properties that conflict with values set by the Dataproc API may be overwritten. Can include properties set in /etc/hadoop/conf/*-site and classes in user code.
spec.jobs[].hiveJob <object>	# Immutable. Optional. Job is a Hive job.
spec.jobs[].hiveJob.additionalProperties <any>
spec.jobs[].hiveJob.continueOnFailure <boolean>	# Immutable. Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
spec.jobs[].hiveJob.jarFileUris <[]string>	# Immutable. Optional. HCFS URIs of jar files to add to the CLASSPATH of the Hive server and Hadoop MapReduce (MR) tasks. Can contain Hive SerDes and UDFs.
spec.jobs[].hiveJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names and values, used to configure Hive. Properties that conflict with values set by the Dataproc API may be overwritten. Can include properties set in /etc/hadoop/conf/*-site.xml, /etc/hive/conf/hive-site.xml, and classes in user code.
spec.jobs[].hiveJob.queryFileUri <string>	# Immutable. The HCFS URI of the script that contains Hive queries.
spec.jobs[].hiveJob.queryList <object>	# Immutable. A list of queries.
spec.jobs[].hiveJob.queryList.queries <[]string> (required)	# Immutable. Required. The queries to execute. You do not need to end a query expression with a semicolon. Multiple queries can be specified in one string by separating each with a semicolon. Here is an example of a Dataproc API snippet that uses a QueryList to specify a HiveJob: "hiveJob": { "queryList": { "queries": [ "query1", "query2", "query3;query4", ] } }
spec.jobs[].hiveJob.scriptVariables <map[string]string>	# Immutable. Optional. Mapping of query variable names to values (equivalent to the Hive command: `SET name="value";`).
spec.jobs[].labels <map[string]string>	# Immutable. Optional. The labels to associate with this job. Label keys must be between 1 and 63 characters long, and must conform to the following regular expression: p{Ll}p{Lo}{0,62} Label values must be between 1 and 63 characters long, and must conform to the following regular expression: [p{Ll}p{Lo}p{N}_-]{0,63} No more than 32 labels can be associated with a given job.
spec.jobs[].pigJob <object>	# Immutable. Optional. Job is a Pig job.
spec.jobs[].pigJob.additionalProperties <any>
spec.jobs[].pigJob.continueOnFailure <boolean>	# Immutable. Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
spec.jobs[].pigJob.jarFileUris <[]string>	# Immutable. Optional. HCFS URIs of jar files to add to the CLASSPATH of the Pig Client and Hadoop MapReduce (MR) tasks. Can contain Pig UDFs.
spec.jobs[].pigJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].pigJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].pigJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values, used to configure Pig. Properties that conflict with values set by the Dataproc API may be overwritten. Can include properties set in /etc/hadoop/conf/*-site.xml, /etc/pig/conf/pig.properties, and classes in user code.
spec.jobs[].pigJob.queryFileUri <string>	# Immutable. The HCFS URI of the script that contains the Pig queries.
spec.jobs[].pigJob.queryList <object>	# Immutable. A list of queries.
spec.jobs[].pigJob.queryList.queries <[]string> (required)	# Immutable. Required. The queries to execute. You do not need to end a query expression with a semicolon. Multiple queries can be specified in one string by separating each with a semicolon. Here is an example of a Dataproc API snippet that uses a QueryList to specify a HiveJob: "hiveJob": { "queryList": { "queries": [ "query1", "query2", "query3;query4", ] } }
spec.jobs[].pigJob.scriptVariables <map[string]string>	# Immutable. Optional. Mapping of query variable names to values (equivalent to the Pig command: `name=[value]`).
spec.jobs[].prerequisiteStepIds <[]string>	# Immutable. Optional. The optional list of prerequisite job step_ids. If not specified, the job will start at the beginning of workflow.
spec.jobs[].prestoJob <object>	# Immutable. Optional. Job is a Presto job.
spec.jobs[].prestoJob.additionalProperties <any>
spec.jobs[].prestoJob.clientTags <[]string>	# Immutable. Optional. Presto client tags to attach to this query
spec.jobs[].prestoJob.continueOnFailure <boolean>	# Immutable. Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
spec.jobs[].prestoJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].prestoJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].prestoJob.outputFormat <string>	# Immutable. Optional. The format in which query output will be displayed. See the Presto documentation for supported output formats
spec.jobs[].prestoJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values. Used to set Presto [session properties](https://prestodb.io/docs/current/sql/set-session.html) Equivalent to using the --session flag in the Presto CLI
spec.jobs[].prestoJob.queryFileUri <string>	# Immutable. The HCFS URI of the script that contains SQL queries.
spec.jobs[].prestoJob.queryList <object>	# Immutable. A list of queries.
spec.jobs[].prestoJob.queryList.queries <[]string> (required)	# Immutable. Required. The queries to execute. You do not need to end a query expression with a semicolon. Multiple queries can be specified in one string by separating each with a semicolon. Here is an example of a Dataproc API snippet that uses a QueryList to specify a HiveJob: "hiveJob": { "queryList": { "queries": [ "query1", "query2", "query3;query4", ] } }
spec.jobs[].pysparkJob <object>	# Immutable. Optional. Job is a PySpark job.
spec.jobs[].pysparkJob.additionalProperties <any>
spec.jobs[].pysparkJob.archiveUris <[]string>	# Immutable. Optional. HCFS URIs of archives to be extracted into the working directory of each executor. Supported file types: .jar, .tar, .tar.gz, .tgz, and .zip.
spec.jobs[].pysparkJob.args <[]string>	# Immutable. Optional. The arguments to pass to the driver. Do not include arguments, such as `--conf`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
spec.jobs[].pysparkJob.fileUris <[]string>	# Immutable. Optional. HCFS URIs of files to be placed in the working directory of each executor. Useful for naively parallel tasks.
spec.jobs[].pysparkJob.jarFileUris <[]string>	# Immutable. Optional. HCFS URIs of jar files to add to the CLASSPATHs of the Python driver and tasks.
spec.jobs[].pysparkJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].pysparkJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].pysparkJob.mainPythonFileUri <string> (required)	# Immutable. Required. The HCFS URI of the main Python file to use as the driver. Must be a .py file.
spec.jobs[].pysparkJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values, used to configure PySpark. Properties that conflict with values set by the Dataproc API may be overwritten. Can include properties set in /etc/spark/conf/spark-defaults.conf and classes in user code.
spec.jobs[].pysparkJob.pythonFileUris <[]string>	# Immutable. Optional. HCFS file URIs of Python files to pass to the PySpark framework. Supported file types: .py, .egg, and .zip.
spec.jobs[].scheduling <object>	# Immutable. Optional. Job scheduling configuration.
spec.jobs[].scheduling.maxFailuresPerHour <integer> format=int64	# Immutable. Optional. Maximum number of times per hour a driver may be restarted as a result of driver exiting with non-zero code before job is reported failed. A job may be reported as thrashing if driver exits with non-zero code 4 times within 10 minute window. Maximum value is 10.
spec.jobs[].scheduling.maxFailuresTotal <integer> format=int64	# Immutable. Optional. Maximum number of times in total a driver may be restarted as a result of driver exiting with non-zero code before job is reported failed. Maximum value is 240.
spec.jobs[].sparkJob <object>	# Immutable. Optional. Job is a Spark job.
spec.jobs[].sparkJob.additionalProperties <any>
spec.jobs[].sparkJob.archiveUris <[]string>	# Immutable. Optional. HCFS URIs of archives to be extracted into the working directory of each executor. Supported file types: .jar, .tar, .tar.gz, .tgz, and .zip.
spec.jobs[].sparkJob.args <[]string>	# Immutable. Optional. The arguments to pass to the driver. Do not include arguments, such as `--conf`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
spec.jobs[].sparkJob.fileUris <[]string>	# Immutable. Optional. HCFS URIs of files to be placed in the working directory of each executor. Useful for naively parallel tasks.
spec.jobs[].sparkJob.jarFileUris <[]string>	# Immutable. Optional. HCFS URIs of jar files to add to the CLASSPATHs of the Spark driver and tasks.
spec.jobs[].sparkJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].sparkJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].sparkJob.mainClass <string>	# Immutable. The name of the driver's main class. The jar file that contains the class must be in the default CLASSPATH or specified in `jar_file_uris`.
spec.jobs[].sparkJob.mainJarFileUri <string>	# Immutable. The HCFS URI of the jar file that contains the main class.
spec.jobs[].sparkJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values, used to configure Spark. Properties that conflict with values set by the Dataproc API may be overwritten. Can include properties set in /etc/spark/conf/spark-defaults.conf and classes in user code.
spec.jobs[].sparkRJob <object>	# Immutable. Optional. Job is a SparkR job.
spec.jobs[].sparkRJob.additionalProperties <any>
spec.jobs[].sparkRJob.archiveUris <[]string>	# Immutable. Optional. HCFS URIs of archives to be extracted into the working directory of each executor. Supported file types: .jar, .tar, .tar.gz, .tgz, and .zip.
spec.jobs[].sparkRJob.args <[]string>	# Immutable. Optional. The arguments to pass to the driver. Do not include arguments, such as `--conf`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
spec.jobs[].sparkRJob.fileUris <[]string>	# Immutable. Optional. HCFS URIs of files to be placed in the working directory of each executor. Useful for naively parallel tasks.
spec.jobs[].sparkRJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].sparkRJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].sparkRJob.mainRFileUri <string> (required)	# Immutable. Required. The HCFS URI of the main R file to use as the driver. Must be a .R file.
spec.jobs[].sparkRJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values, used to configure SparkR. Properties that conflict with values set by the Dataproc API may be overwritten. Can include properties set in /etc/spark/conf/spark-defaults.conf and classes in user code.
spec.jobs[].sparkSqlJob <object>	# Immutable. Optional. Job is a SparkSql job.
spec.jobs[].sparkSqlJob.additionalProperties <any>
spec.jobs[].sparkSqlJob.jarFileUris <[]string>	# Immutable. Optional. HCFS URIs of jar files to be added to the Spark CLASSPATH.
spec.jobs[].sparkSqlJob.loggingConfig <object>	# Immutable. Optional. The runtime log config for job execution.
spec.jobs[].sparkSqlJob.loggingConfig.driverLogLevels <map[string]string>	# Immutable. The per-package log levels for the driver. This may include "root" package name to configure rootLogger. Examples: 'com.google = FATAL', 'root = INFO', 'org.apache = DEBUG'
spec.jobs[].sparkSqlJob.properties <map[string]string>	# Immutable. Optional. A mapping of property names to values, used to configure Spark SQL's SparkConf. Properties that conflict with values set by the Dataproc API may be overwritten.
spec.jobs[].sparkSqlJob.queryFileUri <string>	# Immutable. The HCFS URI of the script that contains SQL queries.
spec.jobs[].sparkSqlJob.queryList <object>	# Immutable. A list of queries.
spec.jobs[].sparkSqlJob.queryList.queries <[]string> (required)	# Immutable. Required. The queries to execute. You do not need to end a query expression with a semicolon. Multiple queries can be specified in one string by separating each with a semicolon. Here is an example of a Dataproc API snippet that uses a QueryList to specify a HiveJob: "hiveJob": { "queryList": { "queries": [ "query1", "query2", "query3;query4", ] } }
spec.jobs[].sparkSqlJob.scriptVariables <map[string]string>	# Immutable. Optional. Mapping of query variable names to values (equivalent to the Spark SQL command: SET `name="value";`).
spec.jobs[].stepId <string> (required)	# Immutable. Required. The step id. The id must be unique among all jobs within the template. The step id is used as prefix for job id, as job `goog-dataproc-workflow-step-id` label, and in prerequisiteStepIds field from other steps. The id must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), and hyphens (-). Cannot begin or end with underscore or hyphen. Must consist of between 3 and 50 characters.
spec.location <string> (required)	# Immutable. The location for the resource
spec.parameters <[]object>	# Immutable. Optional. Template parameters whose values are substituted into the template. Values for parameters must be provided when the template is instantiated.
spec.parameters[].description <string>	# Immutable. Optional. Brief description of the parameter. Must not exceed 1024 characters.
spec.parameters[].fields <[]string> (required)	# Immutable. Required. Paths to all fields that the parameter replaces. A field is allowed to appear in at most one parameter's list of field paths. A field path is similar in syntax to a google.protobuf.FieldMask. For example, a field path that references the zone field of a workflow template's cluster selector would be specified as `placement.clusterSelector.zone`. Also, field paths can reference fields using the following syntax: * Values in maps can be referenced by key: * labels['key'] * placement.clusterSelector.clusterLabels['key'] * placement.managedCluster.labels['key'] * placement.clusterSelector.clusterLabels['key'] * jobs['step-id'].labels['key'] * Jobs in the jobs list can be referenced by step-id: * jobs['step-id'].hadoopJob.mainJarFileUri * jobs['step-id'].hiveJob.queryFileUri * jobs['step-id'].pySparkJob.mainPythonFileUri * jobs['step-id'].hadoopJob.jarFileUris[0] * jobs['step-id'].hadoopJob.archiveUris[0] * jobs['step-id'].hadoopJob.fileUris[0] * jobs['step-id'].pySparkJob.pythonFileUris[0] * Items in repeated fields can be referenced by a zero-based index: * jobs['step-id'].sparkJob.args[0] * Other examples: * jobs['step-id'].hadoopJob.properties['key'] * jobs['step-id'].hadoopJob.args[0] * jobs['step-id'].hiveJob.scriptVariables['key'] * jobs['step-id'].hadoopJob.mainJarFileUri * placement.clusterSelector.zone It may not be possible to parameterize maps and repeated fields in their entirety since only individual map values and individual items in repeated fields can be referenced. For example, the following field paths are invalid: - placement.clusterSelector.clusterLabels - jobs['step-id'].sparkJob.args
spec.parameters[].name <string> (required)	# Immutable. Required. Parameter name. The parameter name is used as the key, and paired with the parameter value, which are passed to the template when the template is instantiated. The name must contain only capital letters (A-Z), numbers (0-9), and underscores (_), and must not start with a number. The maximum length is 40 characters.
spec.parameters[].validation <object>	# Immutable. Optional. Validation rules to be applied to this parameter's value.
spec.parameters[].validation.regex <object>	# Immutable. Validation based on regular expressions.
spec.parameters[].validation.regex.regexes <[]string> (required)	# Immutable. Required. RE2 regular expressions used to validate the parameter's value. The value must match the regex in its entirety (substring matches are not sufficient).
spec.parameters[].validation.values <object>	# Immutable. Validation based on a list of allowed values.
spec.parameters[].validation.values.values <[]string> (required)	# Immutable. Required. List of allowed values for the parameter.
spec.placement <object> (required)	# Immutable. Required. WorkflowTemplate scheduling information.
spec.placement.clusterSelector <object>	# Immutable. Optional. A selector that chooses target cluster for jobs based on metadata. The selector is evaluated at the time each job is submitted.
spec.placement.clusterSelector.clusterLabels <map[string]string> (required)	# Immutable. Required. The cluster labels. Cluster must have all labels to match.
spec.placement.clusterSelector.zone <string>	# Immutable. Optional. The zone where workflow process executes. This parameter does not affect the selection of the cluster. If unspecified, the zone of the first cluster matching the selector is used.
spec.placement.managedCluster <object>	# Immutable. A cluster that is managed by the workflow.
spec.placement.managedCluster.clusterName <string> (required)	# Immutable. Required. The cluster name prefix. A unique cluster name will be formed by appending a random suffix. The name must contain only lower-case letters (a-z), numbers (0-9), and hyphens (-). Must begin with a letter. Cannot begin or end with hyphen. Must consist of between 2 and 35 characters.
spec.placement.managedCluster.config <object> (required)	# Immutable. Required. The cluster configuration.
spec.placement.managedCluster.config.autoscalingConfig <object>	# Immutable. Optional. Autoscaling config for the policy associated with the cluster. Cluster does not autoscale if this field is unset.
spec.placement.managedCluster.config.autoscalingConfig.policyRef <object>	# Immutable.
spec.placement.managedCluster.config.autoscalingConfig.policyRef.external <string>	# Optional. The autoscaling policy used by the cluster. Only resource names including projectid and location (region) are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/locations/[dataproc_region]/autoscalingPolicies/[policy_id]` * `projects/[project_id]/locations/[dataproc_region]/autoscalingPolicies/[policy_id]` Note that the policy must be in the same project and Dataproc region. Allowed value: The Google Cloud resource name of a `DataprocAutoscalingPolicy` resource (format: `projects/{{project}}/locations/{{location}}/autoscalingPolicies/{{name}}`).
spec.placement.managedCluster.config.autoscalingConfig.policyRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.autoscalingConfig.policyRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.encryptionConfig <object>	# Immutable. Optional. Encryption settings for the cluster.
spec.placement.managedCluster.config.encryptionConfig.gcePdKmsKeyRef <object>	# Immutable.
spec.placement.managedCluster.config.encryptionConfig.gcePdKmsKeyRef.external <string>	# Optional. The Cloud KMS key name to use for PD disk encryption for all instances in the cluster. Allowed value: The `selfLink` field of a `KMSCryptoKey` resource.
spec.placement.managedCluster.config.encryptionConfig.gcePdKmsKeyRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.encryptionConfig.gcePdKmsKeyRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.endpointConfig <object>	# Immutable. Optional. Port/endpoint configuration for this cluster
spec.placement.managedCluster.config.endpointConfig.enableHttpPortAccess <boolean>	# Immutable. Optional. If true, enable http access to specific ports on the cluster from external sources. Defaults to false.
spec.placement.managedCluster.config.gceClusterConfig <object>	# Immutable. Optional. The shared Compute Engine config settings for all instances in a cluster.
spec.placement.managedCluster.config.gceClusterConfig.internalIPOnly <boolean>	# Immutable. Optional. If true, all instances in the cluster will only have internal IP addresses. By default, clusters are not restricted to internal IP addresses, and will have ephemeral external IP addresses assigned to each instance. This `internal_ip_only` restriction can only be enabled for subnetwork enabled networks, and all off-cluster dependencies must be configured to be accessible without external IP addresses.
spec.placement.managedCluster.config.gceClusterConfig.metadata <map[string]string>	# Immutable. The Compute Engine metadata entries to add to all instances (see [Project and instance metadata](https://cloud.google.com/compute/docs/storing-retrieving-metadata#project_and_instance_metadata)).
spec.placement.managedCluster.config.gceClusterConfig.networkRef <object>	# Immutable.
spec.placement.managedCluster.config.gceClusterConfig.networkRef.external <string>	# Optional. The Compute Engine network to be used for machine communications. Cannot be specified with subnetwork_uri. If neither `network_uri` nor `subnetwork_uri` is specified, the "default" network of the project is used, if it exists. Cannot be a "Custom Subnet Network" (see [Using Subnetworks](https://cloud.google.com/compute/docs/subnetworks) for more information). A full URL, partial URI, or short name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/regions/global/default` * `projects/[project_id]/regions/global/default` * `default` Allowed value: The `selfLink` field of a `ComputeNetwork` resource.
spec.placement.managedCluster.config.gceClusterConfig.networkRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.gceClusterConfig.networkRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.gceClusterConfig.nodeGroupAffinity <object>	# Immutable. Optional. Node Group Affinity for sole-tenant clusters.
spec.placement.managedCluster.config.gceClusterConfig.nodeGroupAffinity.nodeGroupRef <object> (required)	# Immutable.
spec.placement.managedCluster.config.gceClusterConfig.nodeGroupAffinity.nodeGroupRef.external <string>	# Required. The URI of a sole-tenant [node group resource](https://cloud.google.com/compute/docs/reference/rest/v1/nodeGroups) that the cluster will be created on. A full URL, partial URI, or node group name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/zones/us-central1-a/nodeGroups/node-group-1` * `projects/[project_id]/zones/us-central1-a/nodeGroups/node-group-1` * `node-group-1` Allowed value: The `selfLink` field of a `ComputeNodeGroup` resource.
spec.placement.managedCluster.config.gceClusterConfig.nodeGroupAffinity.nodeGroupRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.gceClusterConfig.nodeGroupAffinity.nodeGroupRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.gceClusterConfig.privateIPv6GoogleAccess <string>	# Immutable. Optional. The type of IPv6 access for a cluster. Possible values: PRIVATE_IPV6_GOOGLE_ACCESS_UNSPECIFIED, INHERIT_FROM_SUBNETWORK, OUTBOUND, BIDIRECTIONAL
spec.placement.managedCluster.config.gceClusterConfig.reservationAffinity <object>	# Immutable. Optional. Reservation Affinity for consuming Zonal reservation.
spec.placement.managedCluster.config.gceClusterConfig.reservationAffinity.consumeReservationType <string>	# Immutable. Optional. Type of reservation to consume Possible values: TYPE_UNSPECIFIED, NO_RESERVATION, ANY_RESERVATION, SPECIFIC_RESERVATION
spec.placement.managedCluster.config.gceClusterConfig.reservationAffinity.key <string>	# Immutable. Optional. Corresponds to the label key of reservation resource.
spec.placement.managedCluster.config.gceClusterConfig.reservationAffinity.values <[]string>	# Immutable. Optional. Corresponds to the label values of reservation resource.
spec.placement.managedCluster.config.gceClusterConfig.serviceAccountRef <object>	# Immutable.
spec.placement.managedCluster.config.gceClusterConfig.serviceAccountRef.external <string>	# Optional. The [Dataproc service account](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/service-accounts#service_accounts_in_dataproc) (also see [VM Data Plane identity](https://cloud.google.com/dataproc/docs/concepts/iam/dataproc-principals#vm_service_account_data_plane_identity)) used by Dataproc cluster VM instances to access Google Cloud Platform services. If not specified, the [Compute Engine default service account](https://cloud.google.com/compute/docs/access/service-accounts#default_service_account) is used. Allowed value: The `email` field of an `IAMServiceAccount` resource.
spec.placement.managedCluster.config.gceClusterConfig.serviceAccountRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.gceClusterConfig.serviceAccountRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.gceClusterConfig.serviceAccountScopes <[]string>	# Immutable. Optional. The URIs of service account scopes to be included in Compute Engine instances. The following base set of scopes is always included: * https://www.googleapis.com/auth/cloud.useraccounts.readonly * https://www.googleapis.com/auth/devstorage.read_write * https://www.googleapis.com/auth/logging.write If no scopes are specified, the following defaults are also provided: * https://www.googleapis.com/auth/bigquery * https://www.googleapis.com/auth/bigtable.admin.table * https://www.googleapis.com/auth/bigtable.data * https://www.googleapis.com/auth/devstorage.full_control
spec.placement.managedCluster.config.gceClusterConfig.shieldedInstanceConfig <object>	# Immutable. Optional. Shielded Instance Config for clusters using Compute Engine Shielded VMs.
spec.placement.managedCluster.config.gceClusterConfig.shieldedInstanceConfig.enableIntegrityMonitoring <boolean>	# Immutable. Optional. Defines whether instances have integrity monitoring enabled. Integrity monitoring compares the most recent boot measurements to the integrity policy baseline and returns a pair of pass/fail results depending on whether they match or not.
spec.placement.managedCluster.config.gceClusterConfig.shieldedInstanceConfig.enableSecureBoot <boolean>	# Immutable. Optional. Defines whether the instances have Secure Boot enabled. Secure Boot helps ensure that the system only runs authentic software by verifying the digital signature of all boot components, and halting the boot process if signature verification fails.
spec.placement.managedCluster.config.gceClusterConfig.shieldedInstanceConfig.enableVtpm <boolean>	# Immutable. Optional. Defines whether the instance have the vTPM enabled. Virtual Trusted Platform Module protects objects like keys, certificates and enables Measured Boot by performing the measurements needed to create a known good boot baseline, called the integrity policy baseline.
spec.placement.managedCluster.config.gceClusterConfig.subnetworkRef <object>	# Immutable.
spec.placement.managedCluster.config.gceClusterConfig.subnetworkRef.external <string>	# Optional. The Compute Engine subnetwork to be used for machine communications. Cannot be specified with network_uri. A full URL, partial URI, or short name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/regions/us-east1/subnetworks/sub0` * `projects/[project_id]/regions/us-east1/subnetworks/sub0` * `sub0` Allowed value: The `selfLink` field of a `ComputeSubnetwork` resource.
spec.placement.managedCluster.config.gceClusterConfig.subnetworkRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.gceClusterConfig.subnetworkRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.gceClusterConfig.tags <[]string>	# Immutable. The Compute Engine tags to add to all instances (see [Tagging instances](https://cloud.google.com/compute/docs/label-or-tag-resources#tags)).
spec.placement.managedCluster.config.gceClusterConfig.zone <string>	# Immutable. Optional. The zone where the Compute Engine cluster will be located. On a create request, it is required in the "global" region. If omitted in a non-global Dataproc region, the service will pick a zone in the corresponding Compute Engine region. On a get request, zone will always be present. A full URL, partial URI, or short name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/zones/[zone]` * `projects/[project_id]/zones/[zone]` * `us-central1-f`
spec.placement.managedCluster.config.initializationActions <[]object>	# Immutable. Optional. Commands to execute on each node after config is completed. By default, executables are run on master and all worker nodes. You can test a node's `role` metadata to run an executable on a master or worker node, as shown below using `curl` (you can also use `wget`): ROLE=$(curl -H Metadata-Flavor:Google http://metadata/computeMetadata/v1/instance/attributes/dataproc-role) if [[ "${ROLE}" == 'Master' ]]; then ... master specific actions ... else ... worker specific actions ... fi
spec.placement.managedCluster.config.initializationActions[].executableFile <string>	# Immutable. Required. Cloud Storage URI of executable file.
spec.placement.managedCluster.config.initializationActions[].executionTimeout <string>	# Immutable. Optional. Amount of time executable has to complete. Default is 10 minutes (see JSON representation of [Duration](https://developers.google.com/protocol-buffers/docs/proto3#json)). Cluster creation fails with an explanatory error message (the name of the executable that caused the error and the exceeded timeout period) if the executable is not completed at end of the timeout period.
spec.placement.managedCluster.config.lifecycleConfig <object>	# Immutable. Optional. Lifecycle setting for the cluster.
spec.placement.managedCluster.config.lifecycleConfig.autoDeleteTime <string> format=date-time	# Immutable. Optional. The time when cluster will be auto-deleted (see JSON representation of [Timestamp](https://developers.google.com/protocol-buffers/docs/proto3#json)).
spec.placement.managedCluster.config.lifecycleConfig.autoDeleteTtl <string>	# Immutable. Optional. The lifetime duration of cluster. The cluster will be auto-deleted at the end of this period. Minimum value is 10 minutes; maximum value is 14 days (see JSON representation of [Duration](https://developers.google.com/protocol-buffers/docs/proto3#json)).
spec.placement.managedCluster.config.lifecycleConfig.idleDeleteTtl <string>	# Immutable. Optional. The duration to keep the cluster alive while idling (when no jobs are running). Passing this threshold will cause the cluster to be deleted. Minimum value is 5 minutes; maximum value is 14 days (see JSON representation of [Duration](https://developers.google.com/protocol-buffers/docs/proto3#json)).
spec.placement.managedCluster.config.masterConfig <object>	# Immutable. Optional. The Compute Engine config settings for the master instance in a cluster.
spec.placement.managedCluster.config.masterConfig.accelerators <[]object>	# Immutable. Optional. The Compute Engine accelerator configuration for these instances.
spec.placement.managedCluster.config.masterConfig.accelerators[].acceleratorCount <integer> format=int64	# Immutable. The number of the accelerator cards of this type exposed to this instance.
spec.placement.managedCluster.config.masterConfig.accelerators[].acceleratorType <string>	# Immutable. Full URL, partial URI, or short name of the accelerator type resource to expose to this instance. See [Compute Engine AcceleratorTypes](https://cloud.google.com/compute/docs/reference/beta/acceleratorTypes). Examples: * `https://www.googleapis.com/compute/beta/projects/[project_id]/zones/us-east1-a/acceleratorTypes/nvidia-tesla-k80` * `projects/[project_id]/zones/us-east1-a/acceleratorTypes/nvidia-tesla-k80` * `nvidia-tesla-k80` **Auto Zone Exception**: If you are using the Dataproc [Auto Zone Placement](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/auto-zone#using_auto_zone_placement) feature, you must use the short name of the accelerator type resource, for example, `nvidia-tesla-k80`.
spec.placement.managedCluster.config.masterConfig.diskConfig <object>	# Immutable. Optional. Disk option config settings.
spec.placement.managedCluster.config.masterConfig.diskConfig.bootDiskSizeGb <integer> format=int64	# Immutable. Optional. Size in GB of the boot disk (default is 500GB).
spec.placement.managedCluster.config.masterConfig.diskConfig.bootDiskType <string>	# Immutable. Optional. Type of the boot disk (default is "pd-standard"). Valid values: "pd-balanced" (Persistent Disk Balanced Solid State Drive), "pd-ssd" (Persistent Disk Solid State Drive), or "pd-standard" (Persistent Disk Hard Disk Drive). See [Disk types](https://cloud.google.com/compute/docs/disks#disk-types).
spec.placement.managedCluster.config.masterConfig.diskConfig.numLocalSsds <integer> format=int64	# Immutable. Optional. Number of attached SSDs, from 0 to 4 (default is 0). If SSDs are not attached, the boot disk is used to store runtime logs and [HDFS](https://hadoop.apache.org/docs/r1.2.1/hdfs_user_guide.html) data. If one or more SSDs are attached, this runtime bulk data is spread across them, and the boot disk contains only basic config and installed binaries.
spec.placement.managedCluster.config.masterConfig.imageRef <object>	# Immutable.
spec.placement.managedCluster.config.masterConfig.imageRef.external <string>	# Optional. The Compute Engine image resource used for cluster instances. The URI can represent an image or image family. Image examples: * `https://www.googleapis.com/compute/beta/projects/[project_id]/global/images/[image-id]` * `projects/[project_id]/global/images/[image-id]` * `image-id` Image family examples. Dataproc will use the most recent image from the family: * `https://www.googleapis.com/compute/beta/projects/[project_id]/global/images/family/[custom-image-family-name]` * `projects/[project_id]/global/images/family/[custom-image-family-name]` If the URI is unspecified, it will be inferred from `SoftwareConfig.image_version` or the system default. Allowed value: The `selfLink` field of a `ComputeImage` resource.
spec.placement.managedCluster.config.masterConfig.imageRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.masterConfig.imageRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.masterConfig.machineType <string>	# Immutable. Optional. The Compute Engine machine type used for cluster instances. A full URL, partial URI, or short name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/zones/us-east1-a/machineTypes/n1-standard-2` * `projects/[project_id]/zones/us-east1-a/machineTypes/n1-standard-2` * `n1-standard-2` **Auto Zone Exception**: If you are using the Dataproc [Auto Zone Placement](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/auto-zone#using_auto_zone_placement) feature, you must use the short name of the machine type resource, for example, `n1-standard-2`.
spec.placement.managedCluster.config.masterConfig.minCpuPlatform <string>	# Immutable. Optional. Specifies the minimum cpu platform for the Instance Group. See [Dataproc -> Minimum CPU Platform](https://cloud.google.com/dataproc/docs/concepts/compute/dataproc-min-cpu).
spec.placement.managedCluster.config.masterConfig.numInstances <integer> format=int64	# Immutable. Optional. The number of VM instances in the instance group. For [HA cluster](/dataproc/docs/concepts/configuring-clusters/high-availability) [master_config](#FIELDS.master_config) groups, **must be set to 3**. For standard cluster [master_config](#FIELDS.master_config) groups, **must be set to 1**.
spec.placement.managedCluster.config.masterConfig.preemptibility <string>	# Immutable. Optional. Specifies the preemptibility of the instance group. The default value for master and worker groups is `NON_PREEMPTIBLE`. This default cannot be changed. The default value for secondary instances is `PREEMPTIBLE`. Possible values: PREEMPTIBILITY_UNSPECIFIED, NON_PREEMPTIBLE, PREEMPTIBLE
spec.placement.managedCluster.config.secondaryWorkerConfig <object>	# Immutable. Optional. The Compute Engine config settings for additional worker instances in a cluster.
spec.placement.managedCluster.config.secondaryWorkerConfig.accelerators <[]object>	# Immutable. Optional. The Compute Engine accelerator configuration for these instances.
spec.placement.managedCluster.config.secondaryWorkerConfig.accelerators[].acceleratorCount <integer> format=int64	# Immutable. The number of the accelerator cards of this type exposed to this instance.
spec.placement.managedCluster.config.secondaryWorkerConfig.accelerators[].acceleratorType <string>	# Immutable. Full URL, partial URI, or short name of the accelerator type resource to expose to this instance. See [Compute Engine AcceleratorTypes](https://cloud.google.com/compute/docs/reference/beta/acceleratorTypes). Examples: * `https://www.googleapis.com/compute/beta/projects/[project_id]/zones/us-east1-a/acceleratorTypes/nvidia-tesla-k80` * `projects/[project_id]/zones/us-east1-a/acceleratorTypes/nvidia-tesla-k80` * `nvidia-tesla-k80` **Auto Zone Exception**: If you are using the Dataproc [Auto Zone Placement](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/auto-zone#using_auto_zone_placement) feature, you must use the short name of the accelerator type resource, for example, `nvidia-tesla-k80`.
spec.placement.managedCluster.config.secondaryWorkerConfig.diskConfig <object>	# Immutable. Optional. Disk option config settings.
spec.placement.managedCluster.config.secondaryWorkerConfig.diskConfig.bootDiskSizeGb <integer> format=int64	# Immutable. Optional. Size in GB of the boot disk (default is 500GB).
spec.placement.managedCluster.config.secondaryWorkerConfig.diskConfig.bootDiskType <string>	# Immutable. Optional. Type of the boot disk (default is "pd-standard"). Valid values: "pd-balanced" (Persistent Disk Balanced Solid State Drive), "pd-ssd" (Persistent Disk Solid State Drive), or "pd-standard" (Persistent Disk Hard Disk Drive). See [Disk types](https://cloud.google.com/compute/docs/disks#disk-types).
spec.placement.managedCluster.config.secondaryWorkerConfig.diskConfig.numLocalSsds <integer> format=int64	# Immutable. Optional. Number of attached SSDs, from 0 to 4 (default is 0). If SSDs are not attached, the boot disk is used to store runtime logs and [HDFS](https://hadoop.apache.org/docs/r1.2.1/hdfs_user_guide.html) data. If one or more SSDs are attached, this runtime bulk data is spread across them, and the boot disk contains only basic config and installed binaries.
spec.placement.managedCluster.config.secondaryWorkerConfig.imageRef <object>	# Immutable.
spec.placement.managedCluster.config.secondaryWorkerConfig.imageRef.external <string>	# Optional. The Compute Engine image resource used for cluster instances. The URI can represent an image or image family. Image examples: * `https://www.googleapis.com/compute/beta/projects/[project_id]/global/images/[image-id]` * `projects/[project_id]/global/images/[image-id]` * `image-id` Image family examples. Dataproc will use the most recent image from the family: * `https://www.googleapis.com/compute/beta/projects/[project_id]/global/images/family/[custom-image-family-name]` * `projects/[project_id]/global/images/family/[custom-image-family-name]` If the URI is unspecified, it will be inferred from `SoftwareConfig.image_version` or the system default. Allowed value: The `selfLink` field of a `ComputeImage` resource.
spec.placement.managedCluster.config.secondaryWorkerConfig.imageRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.secondaryWorkerConfig.imageRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.secondaryWorkerConfig.machineType <string>	# Immutable. Optional. The Compute Engine machine type used for cluster instances. A full URL, partial URI, or short name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/zones/us-east1-a/machineTypes/n1-standard-2` * `projects/[project_id]/zones/us-east1-a/machineTypes/n1-standard-2` * `n1-standard-2` **Auto Zone Exception**: If you are using the Dataproc [Auto Zone Placement](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/auto-zone#using_auto_zone_placement) feature, you must use the short name of the machine type resource, for example, `n1-standard-2`.
spec.placement.managedCluster.config.secondaryWorkerConfig.minCpuPlatform <string>	# Immutable. Optional. Specifies the minimum cpu platform for the Instance Group. See [Dataproc -> Minimum CPU Platform](https://cloud.google.com/dataproc/docs/concepts/compute/dataproc-min-cpu).
spec.placement.managedCluster.config.secondaryWorkerConfig.numInstances <integer> format=int64	# Immutable. Optional. The number of VM instances in the instance group. For [HA cluster](/dataproc/docs/concepts/configuring-clusters/high-availability) [master_config](#FIELDS.master_config) groups, **must be set to 3**. For standard cluster [master_config](#FIELDS.master_config) groups, **must be set to 1**.
spec.placement.managedCluster.config.secondaryWorkerConfig.preemptibility <string>	# Immutable. Optional. Specifies the preemptibility of the instance group. The default value for master and worker groups is `NON_PREEMPTIBLE`. This default cannot be changed. The default value for secondary instances is `PREEMPTIBLE`. Possible values: PREEMPTIBILITY_UNSPECIFIED, NON_PREEMPTIBLE, PREEMPTIBLE
spec.placement.managedCluster.config.securityConfig <object>	# Immutable. Optional. Security settings for the cluster.
spec.placement.managedCluster.config.securityConfig.kerberosConfig <object>	# Immutable. Optional. Kerberos related configuration.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.crossRealmTrustAdminServer <string>	# Immutable. Optional. The admin server (IP or hostname) for the remote trusted realm in a cross realm trust relationship.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.crossRealmTrustKdc <string>	# Immutable. Optional. The KDC (IP or hostname) for the remote trusted realm in a cross realm trust relationship.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.crossRealmTrustRealm <string>	# Immutable. Optional. The remote realm the Dataproc on-cluster KDC will trust, should the user enable cross realm trust.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.crossRealmTrustSharedPassword <string>	# Immutable. Optional. The Cloud Storage URI of a KMS encrypted file containing the shared password between the on-cluster Kerberos realm and the remote trusted realm, in a cross realm trust relationship.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.enableKerberos <boolean>	# Immutable. Optional. Flag to indicate whether to Kerberize the cluster (default: false). Set this field to true to enable Kerberos on a cluster.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.kdcDbKey <string>	# Immutable. Optional. The Cloud Storage URI of a KMS encrypted file containing the master key of the KDC database.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.keyPassword <string>	# Immutable. Optional. The Cloud Storage URI of a KMS encrypted file containing the password to the user provided key. For the self-signed certificate, this password is generated by Dataproc.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.keystore <string>	# Immutable. Optional. The Cloud Storage URI of the keystore file used for SSL encryption. If not provided, Dataproc will provide a self-signed certificate.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.keystorePassword <string>	# Immutable. Optional. The Cloud Storage URI of a KMS encrypted file containing the password to the user provided keystore. For the self-signed certificate, this password is generated by Dataproc.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.kmsKeyRef <object>	# Immutable.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.kmsKeyRef.external <string>	# Optional. The uri of the KMS key used to encrypt various sensitive files. Allowed value: The `selfLink` field of a `KMSCryptoKey` resource.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.kmsKeyRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.securityConfig.kerberosConfig.kmsKeyRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.securityConfig.kerberosConfig.realm <string>	# Immutable. Optional. The name of the on-cluster Kerberos realm. If not specified, the uppercased domain of hostnames will be the realm.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.rootPrincipalPassword <string>	# Immutable. Optional. The Cloud Storage URI of a KMS encrypted file containing the root principal password.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.tgtLifetimeHours <integer> format=int64	# Immutable. Optional. The lifetime of the ticket granting ticket, in hours. If not specified, or user specifies 0, then default value 10 will be used.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.truststore <string>	# Immutable. Optional. The Cloud Storage URI of the truststore file used for SSL encryption. If not provided, Dataproc will provide a self-signed certificate.
spec.placement.managedCluster.config.securityConfig.kerberosConfig.truststorePassword <string>	# Immutable. Optional. The Cloud Storage URI of a KMS encrypted file containing the password to the user provided truststore. For the self-signed certificate, this password is generated by Dataproc.
spec.placement.managedCluster.config.softwareConfig <object>	# Immutable. Optional. The config settings for software inside the cluster.
spec.placement.managedCluster.config.softwareConfig.additionalProperties <any>
spec.placement.managedCluster.config.softwareConfig.imageVersion <string>	# Immutable. Optional. The version of software inside the cluster. It must be one of the supported [Dataproc Versions](https://cloud.google.com/dataproc/docs/concepts/versioning/dataproc-versions#supported_dataproc_versions), such as "1.2" (including a subminor version, such as "1.2.29"), or the ["preview" version](https://cloud.google.com/dataproc/docs/concepts/versioning/dataproc-versions#other_versions). If unspecified, it defaults to the latest Debian version.
spec.placement.managedCluster.config.softwareConfig.optionalComponents <[]string>	# Immutable. Optional. The set of components to activate on the cluster.
spec.placement.managedCluster.config.softwareConfig.properties <map[string]string>	# Immutable. Optional. The properties to set on daemon config files. Property keys are specified in `prefix:property` format, for example `core:hadoop.tmp.dir`. The following are supported prefixes and their mappings: * capacity-scheduler: `capacity-scheduler.xml` * core: `core-site.xml` * distcp: `distcp-default.xml` * hdfs: `hdfs-site.xml` * hive: `hive-site.xml` * mapred: `mapred-site.xml` * pig: `pig.properties` * spark: `spark-defaults.conf` * yarn: `yarn-site.xml` For more information, see [Cluster properties](https://cloud.google.com/dataproc/docs/concepts/cluster-properties).
spec.placement.managedCluster.config.stagingBucketRef <object>	# Immutable.
spec.placement.managedCluster.config.stagingBucketRef.external <string>	# Optional. A Cloud Storage bucket used to stage job dependencies, config files, and job driver console output. If you do not specify a staging bucket, Cloud Dataproc will determine a Cloud Storage location (US, ASIA, or EU) for your cluster's staging bucket according to the Compute Engine zone where your cluster is deployed, and then create and manage this project-level, per-location bucket (see [Dataproc staging bucket](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/staging-bucket)). **This field requires a Cloud Storage bucket name, not a URI to a Cloud Storage bucket.** Allowed value: The Google Cloud resource name of a `StorageBucket` resource (format: `{{name}}`).
spec.placement.managedCluster.config.stagingBucketRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.stagingBucketRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.tempBucketRef <object>	# Immutable.
spec.placement.managedCluster.config.tempBucketRef.external <string>	# Optional. A Cloud Storage bucket used to store ephemeral cluster and jobs data, such as Spark and MapReduce history files. If you do not specify a temp bucket, Dataproc will determine a Cloud Storage location (US, ASIA, or EU) for your cluster's temp bucket according to the Compute Engine zone where your cluster is deployed, and then create and manage this project-level, per-location bucket. The default bucket has a TTL of 90 days, but you can use any TTL (or none) if you specify a bucket. **This field requires a Cloud Storage bucket name, not a URI to a Cloud Storage bucket.** Allowed value: The Google Cloud resource name of a `StorageBucket` resource (format: `{{name}}`).
spec.placement.managedCluster.config.tempBucketRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.tempBucketRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.workerConfig <object>	# Immutable. Optional. The Compute Engine config settings for worker instances in a cluster.
spec.placement.managedCluster.config.workerConfig.accelerators <[]object>	# Immutable. Optional. The Compute Engine accelerator configuration for these instances.
spec.placement.managedCluster.config.workerConfig.accelerators[].acceleratorCount <integer> format=int64	# Immutable. The number of the accelerator cards of this type exposed to this instance.
spec.placement.managedCluster.config.workerConfig.accelerators[].acceleratorType <string>	# Immutable. Full URL, partial URI, or short name of the accelerator type resource to expose to this instance. See [Compute Engine AcceleratorTypes](https://cloud.google.com/compute/docs/reference/beta/acceleratorTypes). Examples: * `https://www.googleapis.com/compute/beta/projects/[project_id]/zones/us-east1-a/acceleratorTypes/nvidia-tesla-k80` * `projects/[project_id]/zones/us-east1-a/acceleratorTypes/nvidia-tesla-k80` * `nvidia-tesla-k80` **Auto Zone Exception**: If you are using the Dataproc [Auto Zone Placement](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/auto-zone#using_auto_zone_placement) feature, you must use the short name of the accelerator type resource, for example, `nvidia-tesla-k80`.
spec.placement.managedCluster.config.workerConfig.diskConfig <object>	# Immutable. Optional. Disk option config settings.
spec.placement.managedCluster.config.workerConfig.diskConfig.bootDiskSizeGb <integer> format=int64	# Immutable. Optional. Size in GB of the boot disk (default is 500GB).
spec.placement.managedCluster.config.workerConfig.diskConfig.bootDiskType <string>	# Immutable. Optional. Type of the boot disk (default is "pd-standard"). Valid values: "pd-balanced" (Persistent Disk Balanced Solid State Drive), "pd-ssd" (Persistent Disk Solid State Drive), or "pd-standard" (Persistent Disk Hard Disk Drive). See [Disk types](https://cloud.google.com/compute/docs/disks#disk-types).
spec.placement.managedCluster.config.workerConfig.diskConfig.numLocalSsds <integer> format=int64	# Immutable. Optional. Number of attached SSDs, from 0 to 4 (default is 0). If SSDs are not attached, the boot disk is used to store runtime logs and [HDFS](https://hadoop.apache.org/docs/r1.2.1/hdfs_user_guide.html) data. If one or more SSDs are attached, this runtime bulk data is spread across them, and the boot disk contains only basic config and installed binaries.
spec.placement.managedCluster.config.workerConfig.imageRef <object>	# Immutable.
spec.placement.managedCluster.config.workerConfig.imageRef.external <string>	# Optional. The Compute Engine image resource used for cluster instances. The URI can represent an image or image family. Image examples: * `https://www.googleapis.com/compute/beta/projects/[project_id]/global/images/[image-id]` * `projects/[project_id]/global/images/[image-id]` * `image-id` Image family examples. Dataproc will use the most recent image from the family: * `https://www.googleapis.com/compute/beta/projects/[project_id]/global/images/family/[custom-image-family-name]` * `projects/[project_id]/global/images/family/[custom-image-family-name]` If the URI is unspecified, it will be inferred from `SoftwareConfig.image_version` or the system default. Allowed value: The `selfLink` field of a `ComputeImage` resource.
spec.placement.managedCluster.config.workerConfig.imageRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.placement.managedCluster.config.workerConfig.imageRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.placement.managedCluster.config.workerConfig.machineType <string>	# Immutable. Optional. The Compute Engine machine type used for cluster instances. A full URL, partial URI, or short name are valid. Examples: * `https://www.googleapis.com/compute/v1/projects/[project_id]/zones/us-east1-a/machineTypes/n1-standard-2` * `projects/[project_id]/zones/us-east1-a/machineTypes/n1-standard-2` * `n1-standard-2` **Auto Zone Exception**: If you are using the Dataproc [Auto Zone Placement](https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/auto-zone#using_auto_zone_placement) feature, you must use the short name of the machine type resource, for example, `n1-standard-2`.
spec.placement.managedCluster.config.workerConfig.minCpuPlatform <string>	# Immutable. Optional. Specifies the minimum cpu platform for the Instance Group. See [Dataproc -> Minimum CPU Platform](https://cloud.google.com/dataproc/docs/concepts/compute/dataproc-min-cpu).
spec.placement.managedCluster.config.workerConfig.numInstances <integer> format=int64	# Immutable. Optional. The number of VM instances in the instance group. For [HA cluster](/dataproc/docs/concepts/configuring-clusters/high-availability) [master_config](#FIELDS.master_config) groups, **must be set to 3**. For standard cluster [master_config](#FIELDS.master_config) groups, **must be set to 1**.
spec.placement.managedCluster.config.workerConfig.preemptibility <string>	# Immutable. Optional. Specifies the preemptibility of the instance group. The default value for master and worker groups is `NON_PREEMPTIBLE`. This default cannot be changed. The default value for secondary instances is `PREEMPTIBLE`. Possible values: PREEMPTIBILITY_UNSPECIFIED, NON_PREEMPTIBLE, PREEMPTIBLE
spec.placement.managedCluster.labels <map[string]string>	# Immutable. Optional. The labels to associate with this cluster. Label keys must be between 1 and 63 characters long, and must conform to the following PCRE regular expression: p{Ll}p{Lo}{0,62} Label values must be between 1 and 63 characters long, and must conform to the following PCRE regular expression: [p{Ll}p{Lo}p{N}_-]{0,63} No more than 32 labels can be associated with a given cluster.
spec.projectRef <object>	# Immutable. The Project that this resource belongs to.
spec.projectRef.external <string>	# The project for the resource Allowed value: The Google Cloud resource name of a `Project` resource (format: `projects/{{name}}`).
spec.projectRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.projectRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.resourceID <string>	# Immutable. Optional. The name of the resource. Used for creation and acquisition. When unset, the value of `metadata.name` is used as the default.
status <object>
status.conditions <[]object>	# Conditions represent the latest available observation of the resource's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.createTime <string> format=date-time	# Output only. The time template was created.
status.observedGeneration <integer>	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.placement <object>
status.placement.managedCluster <object>
status.placement.managedCluster.config <object>
status.placement.managedCluster.config.endpointConfig <object>
status.placement.managedCluster.config.endpointConfig.httpPorts <map[string]string>	# Output only. The map of port descriptions to URLs. Will only be populated if enable_http_port_access is true.
status.placement.managedCluster.config.lifecycleConfig <object>
status.placement.managedCluster.config.lifecycleConfig.idleStartTime <string> format=date-time	# Output only. The time when cluster became idle (most recent job finished) and became eligible for deletion due to idleness (see JSON representation of [Timestamp](https://developers.google.com/protocol-buffers/docs/proto3#json)).
status.placement.managedCluster.config.masterConfig <object>
status.placement.managedCluster.config.masterConfig.instanceNames <[]string>	# Output only. The list of instance names. Dataproc derives the names from `cluster_name`, `num_instances`, and the instance group.
status.placement.managedCluster.config.masterConfig.isPreemptible <boolean>	# Output only. Specifies that this instance group contains preemptible instances.
status.placement.managedCluster.config.masterConfig.managedGroupConfig <object>	# Output only. The config for Compute Engine Instance Group Manager that manages this group. This is only used for preemptible instance groups.
status.placement.managedCluster.config.masterConfig.managedGroupConfig.instanceGroupManagerName <string>	# Output only. The name of the Instance Group Manager for this group.
status.placement.managedCluster.config.masterConfig.managedGroupConfig.instanceTemplateName <string>	# Output only. The name of the Instance Template used for the Managed Instance Group.
status.placement.managedCluster.config.secondaryWorkerConfig <object>
status.placement.managedCluster.config.secondaryWorkerConfig.instanceNames <[]string>	# Output only. The list of instance names. Dataproc derives the names from `cluster_name`, `num_instances`, and the instance group.
status.placement.managedCluster.config.secondaryWorkerConfig.isPreemptible <boolean>	# Output only. Specifies that this instance group contains preemptible instances.
status.placement.managedCluster.config.secondaryWorkerConfig.managedGroupConfig <object>	# Output only. The config for Compute Engine Instance Group Manager that manages this group. This is only used for preemptible instance groups.
status.placement.managedCluster.config.secondaryWorkerConfig.managedGroupConfig.instanceGroupManagerName <string>	# Output only. The name of the Instance Group Manager for this group.
status.placement.managedCluster.config.secondaryWorkerConfig.managedGroupConfig.instanceTemplateName <string>	# Output only. The name of the Instance Template used for the Managed Instance Group.
status.placement.managedCluster.config.workerConfig <object>
status.placement.managedCluster.config.workerConfig.instanceNames <[]string>	# Output only. The list of instance names. Dataproc derives the names from `cluster_name`, `num_instances`, and the instance group.
status.placement.managedCluster.config.workerConfig.isPreemptible <boolean>	# Output only. Specifies that this instance group contains preemptible instances.
status.placement.managedCluster.config.workerConfig.managedGroupConfig <object>	# Output only. The config for Compute Engine Instance Group Manager that manages this group. This is only used for preemptible instance groups.
status.placement.managedCluster.config.workerConfig.managedGroupConfig.instanceGroupManagerName <string>	# Output only. The name of the Instance Group Manager for this group.
status.placement.managedCluster.config.workerConfig.managedGroupConfig.instanceTemplateName <string>	# Output only. The name of the Instance Template used for the Managed Instance Group.
status.updateTime <string> format=date-time	# Output only. The time template was last updated.
status.version <integer> format=int64	# Output only. The current version of this workflow template.
