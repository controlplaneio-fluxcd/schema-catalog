# schema source: OpenShift v4.22 https://github.com/openshift/api
apiVersion <string> enum=network.openshift.io/v1
kind <string> enum=NetNamespace
metadata.name <string> (required)
metadata.namespace <string>
egressIPs <[]string>	# egressIPs is a list of reserved IPs that will be used as the source for external traffic coming from pods in this namespace. (If empty, external traffic will be masqueraded to Node IPs.)
netid <integer> (required) default=0 format=int64	# netid is the network identifier of the network namespace assigned to each overlay network packet. This can be manipulated with the "oc adm pod-network" commands.
netname <string> (required) default=""	# netname is the name of the network namespace. (This is the same as the object's name, but both fields must be set.)
