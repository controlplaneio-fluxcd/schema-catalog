# schema source: GCP Config Connector v1.155.1 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=appoptimize.cnrm.cloud.google.com/v1alpha1
kind <string> enum=AppOptimizeReport
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# AppOptimizeReportSpec defines the desired state of AppOptimizeReport
spec.dimensions <[]string> (required)	# Required. A list of dimensions to include in the report. Supported values: * `project` * `application` * `service_or_workload` * `resource` * `resource_type` * `location` * `product_display_name` * `sku` * `month` * `day` * `hour`
spec.filter <string>	# Optional. A Common Expression Language (CEL) expression used to filter the data for the report.
spec.location <string> (required) (immutable)	# Immutable. The location of this resource.
spec.metrics <[]string> (required)	# Required. A list of metrics to include in the report. Supported values: * `cost` * `cpu_mean_utilization` * `cpu_usage_core_seconds` * `cpu_allocation_core_seconds` * `cpu_p95_utilization` * `memory_mean_utilization` * `memory_usage_byte_seconds` * `memory_allocation_byte_seconds` * `memory_p95_utilization`
spec.projectRef <object> (required)	# The project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The AppOptimizeReport name. If not given, the metadata.name will be used.
spec.scopes <[]object>	# Optional. The resource containers for which to fetch data. Default is the project specified in the report's parent.
spec.scopes[].applicationRef <object>	# Optional. An App Hub Application to fetch data from.
spec.scopes[].applicationRef.external <string>	# A reference to an externally managed AppHubApplication resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/applications/{{applicationID}}".
spec.scopes[].applicationRef.name <string>	# The name of an AppHubApplication resource.
spec.scopes[].applicationRef.namespace <string>	# The namespace of an AppHubApplication resource.
spec.scopes[].projectRef <object>	# Optional. A Google Cloud Platform project to fetch data from.
spec.scopes[].projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.scopes[].projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.scopes[].projectRef.name <string>	# The `name` field of a `Project` resource.
spec.scopes[].projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
status <object>	# AppOptimizeReportStatus defines the config connector machine state of AppOptimizeReport
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the AppOptimizeReport resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.expireTime <string>	# Output only. Timestamp in UTC of when this report expires. Once the report expires, it will no longer be accessible and the report's underlying data will be deleted.
