# schema source: OPA Gatekeeper v3.23.0 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=expansion.gatekeeper.sh/v1beta1
kind <string> enum=ExpansionTemplate (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ExpansionTemplateSpec defines the desired state of ExpansionTemplate.
spec.applyTo <[]object>	# ApplyTo lists the specific groups, versions and kinds of generator resources which will be expanded.
spec.applyTo[].groups <[]string>
spec.applyTo[].kinds <[]string>
spec.applyTo[].versions <[]string>
spec.enforcementAction <string>	# EnforcementAction specifies the enforcement action to be used for resources matching the ExpansionTemplate. Specifying an empty value will use the enforcement action specified by the Constraint in violation.
spec.generatedGVK <object>	# GeneratedGVK specifies the GVK of the resources which the generator resource creates.
spec.generatedGVK.group <string>
spec.generatedGVK.kind <string>
spec.generatedGVK.version <string>
spec.templateSource <string>	# TemplateSource specifies the source field on the generator resource to use as the base for expanded resource. For Pod-creating generators, this is usually spec.template
status <object>	# ExpansionTemplateStatus defines the observed state of ExpansionTemplate.
status.byPod <[]object>
status.byPod[].errors <[]object>
status.byPod[].errors[].message <string> (required)
status.byPod[].errors[].type <string>
status.byPod[].id <string>	# Important: Run "make" to regenerate code after modifying this file
status.byPod[].observedGeneration <integer> format=int64
status.byPod[].operations <[]string>
status.byPod[].templateUID <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
