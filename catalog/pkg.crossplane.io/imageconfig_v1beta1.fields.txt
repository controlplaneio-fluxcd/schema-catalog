# schema source: Crossplane v2.3.4 https://github.com/crossplane/crossplane
apiVersion <string> enum=pkg.crossplane.io/v1beta1
kind <string> enum=ImageConfig (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ImageConfigSpec contains the configuration for matching images.
spec.matchImages <[]object> (required)	# MatchImages is a list of image matching rules. This ImageConfig will match an image if any one of these rules is satisfied. In the case where multiple ImageConfigs match an image for a given purpose the one with the most specific match will be used. If multiple rules of equal specificity match an arbitrary one will be selected.
spec.matchImages[].prefix <string> (required)	# Prefix is the prefix that should be matched. When multiple prefix rules match an image path, the longest one takes precedence.
spec.matchImages[].type <string> enum=Prefix default="Prefix"	# Type is the type of match.
spec.registry <object>	# Registry is the configuration for the registry.
spec.registry.authentication <object>	# Authentication is the authentication information for the registry.
spec.registry.authentication.pullSecretRef <object> (required)	# PullSecretRef is a reference to a secret that contains the credentials for the registry.
spec.registry.authentication.pullSecretRef.name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.rewriteImage <object>	# RewriteImage defines how a matched image's path should be rewritten.
spec.rewriteImage.prefix <string> (required)	# Prefix is the prefix that will replace the portion of the image's path matched by the prefix in the ImageMatch. If multiple prefixes matched, the longest one will be replaced.
spec.runtime <object>	# Runtime allows configuration of runtime options for the image.
spec.runtime.configRef <object>	# ConfigReference references a RuntimeConfig resource that will be used to configure the package runtime.
spec.runtime.configRef.apiVersion <string> default="pkg.crossplane.io/v1beta1"	# API version of the referent.
spec.runtime.configRef.kind <string> default="DeploymentRuntimeConfig"	# Kind of the referent.
spec.runtime.configRef.name <string> (required)	# Name of the RuntimeConfig.
spec.verification <object>	# Verification contains the configuration for verifying the image.
spec.verification.cosign <object>	# Cosign is the configuration for verifying the image using cosign.
spec.verification.cosign.authorities <[]object> (required)	# Authorities defines the rules for discovering and validating signatures.
spec.verification.cosign.authorities[].attestations <[]object>	# Attestations is a list of individual attestations for this authority, once the signature for this authority has been verified.
spec.verification.cosign.authorities[].attestations[].name <string> (required)	# Name of the attestation.
spec.verification.cosign.authorities[].attestations[].predicateType <string> (required)	# PredicateType defines which predicate type to verify. Matches cosign verify-attestation options.
spec.verification.cosign.authorities[].key <object>	# Key defines the type of key to validate the image.
spec.verification.cosign.authorities[].key.hashAlgorithm <string> (required) default="sha256"	# HashAlgorithm always defaults to sha256 if the algorithm hasn't been explicitly set
spec.verification.cosign.authorities[].key.secretRef <object> (required)	# SecretRef sets a reference to a secret with the key.
spec.verification.cosign.authorities[].key.secretRef.key <string> (required)	# The key to select.
spec.verification.cosign.authorities[].key.secretRef.name <string> (required)	# Name of the secret.
spec.verification.cosign.authorities[].keyless <object>	# Keyless sets the configuration to verify the authority against a Fulcio instance.
spec.verification.cosign.authorities[].keyless.identities <[]object> (required)	# Identities sets a list of identities.
spec.verification.cosign.authorities[].keyless.identities[].issuer <string>	# Issuer defines the issuer for this identity.
spec.verification.cosign.authorities[].keyless.identities[].issuerRegExp <string>	# IssuerRegExp specifies a regular expression to match the issuer for this identity. This has precedence over the Issuer field.
spec.verification.cosign.authorities[].keyless.identities[].subject <string>	# Subject defines the subject for this identity.
spec.verification.cosign.authorities[].keyless.identities[].subjectRegExp <string>	# SubjectRegExp specifies a regular expression to match the subject for this identity. This has precedence over the Subject field.
spec.verification.cosign.authorities[].keyless.insecureIgnoreSCT <boolean>	# InsecureIgnoreSCT omits verifying if a certificate contains an embedded SCT
spec.verification.cosign.authorities[].name <string> (required)	# Name is the name for this authority.
spec.verification.provider <string> (required) enum=Cosign	# Provider is the provider that should be used to verify the image.
