# schema source: GCP Config Connector v1.152.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=metastore.cnrm.cloud.google.com/v1alpha1
kind <string> enum=MetastoreService
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# MetastoreServiceSpec defines the desired state of MetastoreService
spec.databaseType <string> enum=MYSQL|SPANNER	# Immutable. The database type that the Metastore service stores its data.
spec.encryptionConfig <object> (immutable)	# Immutable. Information used to configure the Dataproc Metastore service to encrypt customer data at rest. Cannot be updated.
spec.encryptionConfig.kmsKeyRef <object>	# The fully qualified customer provided Cloud KMS key name to use for customer data encryption
spec.encryptionConfig.kmsKeyRef.external <string>	# A reference to an externally managed KMSCryptoKey. Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
spec.encryptionConfig.kmsKeyRef.name <string>	# The `name` of a `KMSCryptoKey` resource.
spec.encryptionConfig.kmsKeyRef.namespace <string>	# The `namespace` of a `KMSCryptoKey` resource.
spec.hiveMetastoreConfig <object>	# Configuration information specific to running Hive metastore software as the metastore service.
spec.hiveMetastoreConfig.configOverrides <map[string]string>	# A mapping of Hive metastore configuration key-value pairs to apply to the Hive metastore (configured in `hive-site.xml`). The mappings override system defaults (some keys cannot be overridden). These overrides are also applied to auxiliary versions and can be further customized in the auxiliary version's `AuxiliaryVersionConfig`.
spec.hiveMetastoreConfig.endpointProtocol <string>	# The protocol to use for the metastore service endpoint. If unspecified, defaults to `THRIFT`.
spec.hiveMetastoreConfig.kerberosConfig <object>	# Information used to configure the Hive metastore service as a service principal in a Kerberos realm. To disable Kerberos, use the `UpdateService` method and specify this field's path (`hive_metastore_config.kerberos_config`) in the request's `update_mask` while omitting this field from the request's `service`.
spec.hiveMetastoreConfig.kerberosConfig.keytab <object>	# A Kerberos keytab file that can be used to authenticate a service principal with a Kerberos Key Distribution Center (KDC).
spec.hiveMetastoreConfig.kerberosConfig.keytab.secretRef <object>	# The relative resource name of a Secret Manager secret version
spec.hiveMetastoreConfig.kerberosConfig.keytab.secretRef.external <string>	# A reference to an externally managed SecretManagerSecret resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/secrets/{{secretID}}".
spec.hiveMetastoreConfig.kerberosConfig.keytab.secretRef.name <string>	# The name of a SecretManagerSecret resource.
spec.hiveMetastoreConfig.kerberosConfig.keytab.secretRef.namespace <string>	# The namespace of a SecretManagerSecret resource.
spec.hiveMetastoreConfig.kerberosConfig.krb5ConfigGCSURI <string>	# A Cloud Storage URI that specifies the path to a krb5.conf file. It is of the form `gs://{bucket_name}/path/to/krb5.conf`, although the file does not need to be named krb5.conf explicitly.
spec.hiveMetastoreConfig.kerberosConfig.principal <string>	# A Kerberos principal that exists in the both the keytab the KDC to authenticate as. A typical principal is of the form `primary/instance@REALM`, but there is no exact format.
spec.hiveMetastoreConfig.version <string>	# Immutable. The Hive metastore schema version.
spec.labels <map[string]string>	# User-defined labels for the metastore service.
spec.location <string> (required)
spec.maintenanceWindow <object>	# The one hour maintenance window of the metastore service. This specifies when the service can be restarted for maintenance purposes in UTC time. Maintenance window is not needed for services with the SPANNER database type.
spec.maintenanceWindow.dayOfWeek <string>	# The day of week, when the window starts.
spec.maintenanceWindow.hourOfDay <object>	# The hour of day (0-23) when the window starts.
spec.maintenanceWindow.hourOfDay.value <integer> format=int32	# The int32 value.
spec.networkConfig <object>	# The configuration specifying the network settings for the Dataproc Metastore service.
spec.networkConfig.consumers <[]object>	# Immutable. The consumer-side network configuration for the Dataproc Metastore instance.
spec.networkConfig.consumers[].subnetworkRef <object>	# Immutable. The subnetwork of the customer project from which an IP address is reserved and used as the Dataproc Metastore service's endpoint. It is accessible to hosts in the subnet and to all hosts in a subnet in the same region and same network. There must be at least one IP address available in the subnet's primary range.
spec.networkConfig.consumers[].subnetworkRef.external <string>	# The ComputeSubnetwork selflink of form "projects/{{project}}/regions/{{region}}/subnetworks/{{name}}", when not managed by Config Connector.
spec.networkConfig.consumers[].subnetworkRef.name <string>	# The `name` field of a `ComputeSubnetwork` resource.
spec.networkConfig.consumers[].subnetworkRef.namespace <string>	# The `namespace` field of a `ComputeSubnetwork` resource.
spec.networkRef <object>	# Immutable. The relative resource name of the VPC network on which the instance can be accessed.
spec.networkRef.external <string>	# The value of an externally managed ComputeNetwork resource. Should be in the format "https://www.googleapis.com/compute/{{version}}/projects/{{projectId}}/global/networks/{{networkId}}" or "projects/{{projectId}}/global/networks/{{networkId}}"
spec.networkRef.name <string>	# The name of a ComputeNetwork resource.
spec.networkRef.namespace <string>	# The namespace of a ComputeNetwork resource.
spec.port <integer> format=int32	# The TCP port at which the metastore service is reached. Default: 9083.
spec.projectRef <object>	# The Project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.releaseChannel <string> enum=STABLE|CANARY	# Immutable. The release channel of the service. If unspecified, defaults to `STABLE`.
spec.resourceID <string>	# The MetastoreService name. If not given, the metadata.name will be used.
spec.scalingConfig <object>	# Scaling configuration of the metastore service.
spec.scalingConfig.instanceSize <string>	# An enum of readable instance sizes, with each instance size mapping to a float value (e.g. InstanceSize.EXTRA_SMALL = scaling_factor(0.1))
spec.scalingConfig.scalingFactor <number>	# Scaling factor, increments of 0.1 for values less than 1.0, and increments of 1.0 for values greater than 1.0.
spec.telemetryConfig <object>	# The configuration specifying telemetry settings for the Dataproc Metastore service. If unspecified defaults to `JSON`.
spec.telemetryConfig.logFormat <string>	# The output format of the Dataproc Metastore service's logs.
spec.tier <string> enum=DEVELOPER|ENTERPRISE	# The tier of the service.
status <object>	# MetastoreServiceStatus defines the config connector machine state of MetastoreService
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the MetastoreService resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.artifactGCSURI <string>	# Output only. A Cloud Storage URI (starting with `gs://`) that specifies where artifacts related to the metastore service are stored.
status.observedState.createTime <string>	# Output only. The time when the metastore service was created.
status.observedState.endpointURI <string>	# Output only. The URI of the endpoint used to access the metastore service.
status.observedState.metadataManagementActivity <object>	# Output only. The metadata management activities of the metastore service.
status.observedState.metadataManagementActivity.metadataExports <[]object>	# Output only. The latest metadata exports of the metastore service.
status.observedState.metadataManagementActivity.metadataExports[].databaseDumpType <string>	# Output only. The type of the database dump.
status.observedState.metadataManagementActivity.metadataExports[].destinationGCSURI <string>	# Output only. A Cloud Storage URI of a folder that metadata are exported to, in the form of `gs://<bucket_name>/<path_inside_bucket>/<export_folder>`, where `<export_folder>` is automatically generated.
status.observedState.metadataManagementActivity.metadataExports[].endTime <string>	# Output only. The time when the export ended.
status.observedState.metadataManagementActivity.metadataExports[].startTime <string>	# Output only. The time when the export started.
status.observedState.metadataManagementActivity.metadataExports[].state <string>	# Output only. The current state of the export.
status.observedState.metadataManagementActivity.restores <[]object>	# Output only. The latest restores of the metastore service.
status.observedState.metadataManagementActivity.restores[].backup <string>	# Output only. The relative resource name of the metastore service backup to restore from, in the following form: `projects/{project_id}/locations/{location_id}/services/{service_id}/backups/{backup_id}`.
status.observedState.metadataManagementActivity.restores[].details <string>	# Output only. The restore details containing the revision of the service to be restored to, in format of JSON.
status.observedState.metadataManagementActivity.restores[].endTime <string>	# Output only. The time when the restore ended.
status.observedState.metadataManagementActivity.restores[].startTime <string>	# Output only. The time when the restore started.
status.observedState.metadataManagementActivity.restores[].state <string>	# Output only. The current state of the restore.
status.observedState.metadataManagementActivity.restores[].type <string>	# Output only. The type of restore.
status.observedState.networkConfig <object>	# The configuration specifying the network settings for the Dataproc Metastore service.
status.observedState.networkConfig.consumers <[]object>	# Immutable. The consumer-side network configuration for the Dataproc Metastore instance.
status.observedState.networkConfig.consumers[].endpointLocation <string>	# Output only. The location of the endpoint URI. Format: `projects/{project}/locations/{location}`.
status.observedState.networkConfig.consumers[].endpointURI <string>	# Output only. The URI of the endpoint used to access the metastore service.
status.observedState.state <string>	# Output only. The current state of the metastore service.
status.observedState.stateMessage <string>	# Output only. Additional information about the current state of the metastore service, if available.
status.observedState.uid <string>	# Output only. The globally unique resource identifier of the metastore service.
status.observedState.updateTime <string>	# Output only. The time when the metastore service was last updated.
