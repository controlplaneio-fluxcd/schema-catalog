# schema source: Kubernetes v1.36.2
apiVersion <string> enum=authentication.k8s.io/v1
kind <string> enum=SelfSubjectReview (cluster-scoped)
metadata.name <string> (required)
status <object>	# status is filled in by the server with the user attributes.
status.userInfo <object>	# userInfo is a set of attributes belonging to the user making this request.
status.userInfo.extra <map[string]array>	# extra is any additional information provided by the authenticator.
status.userInfo.groups <[]string>	# groups is the names of groups this user is a part of.
status.userInfo.uid <string>	# uid is a unique value that identifies this user across time. If this user is deleted and another user by the same name is added, they will have different UIDs.
status.userInfo.username <string>	# username is the name that uniquely identifies this user among all active users.
