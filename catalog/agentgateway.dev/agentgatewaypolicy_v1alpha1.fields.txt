# schema source: Agentgateway v1.3.1 https://github.com/agentgateway/agentgateway
apiVersion <string> enum=agentgateway.dev/v1alpha1
kind <string> enum=AgentgatewayPolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Desired policy configuration.
spec.backend <object>	# Settings for how to connect to destination backends. A backend policy can target a `Gateway` (optionally, with a `sectionName` indicating the listener), `ListenerSet`, `Route` (optionally, with a `sectionName` indicating the route rule), or a `Service` or `Backend` (optionally, with a `sectionName` indicating the port for `Service`, or sub-backend for `Backend`). Note that a backend policy applies when connecting to a specific destination backend. Targeting a higher level resource, like `Gateway`, is just a way to easily apply a policy to a group of backends. When multiple policies are selected for a given request, they are merged on a field-level basis, but not a deep merge. Precedence is given to more precise policies: `Gateway` < `Listener` < `Route` < `Route Rule` < `Backend` or `Service`. For example, if a `Gateway` policy sets `tcp` and `tls`, and a `Backend` policy sets `tls`, the effective policy would be `tcp` from the `Gateway`, and `tls` from the `Backend`.
spec.backend.ai <object>	# Settings for AI workloads. This is only applicable when connecting to a `Backend` of type `ai`.
spec.backend.ai.defaults <[]object> min=1 max=64	# Defaults to merge with user input fields. If the field is already set, the field in the request is used.
spec.backend.ai.defaults[].field <string> (required) max=256	# Name of the field.
spec.backend.ai.defaults[].value <any> (required)	# Default value for the field. This can be any JSON data type.
spec.backend.ai.modelAliases <map[string]string>	# Maps friendly model names to actual provider model names. Example: `{"fast": "gpt-3.5-turbo", "smart": "gpt-4-turbo"}`. Note: This field is only applicable when using the agentgateway data plane.
spec.backend.ai.overrides <[]object> min=1 max=64	# Overrides to merge with user input fields. If the field is already set, the field is overwritten.
spec.backend.ai.overrides[].field <string> (required) max=256	# Name of the field.
spec.backend.ai.overrides[].value <any> (required)	# Default value for the field. This can be any JSON data type.
spec.backend.ai.prompt <object>	# Enriches requests sent to the LLM provider by appending and prepending system prompts. This can be configured only for LLM providers that use the `CHAT` or `CHAT_STREAMING` API route type.
spec.backend.ai.prompt.append <[]object>	# Messages to append to the prompt sent by the client.
spec.backend.ai.prompt.append[].content <string> (required)	# String content of the message.
spec.backend.ai.prompt.append[].role <string> (required)	# Role of the message. The available roles depend on the backend LLM provider model, such as `SYSTEM` or `USER` in the OpenAI API.
spec.backend.ai.prompt.prepend <[]object>	# Messages to prepend to the prompt sent by the client.
spec.backend.ai.prompt.prepend[].content <string> (required)	# String content of the message.
spec.backend.ai.prompt.prepend[].role <string> (required)	# Role of the message. The available roles depend on the backend LLM provider model, such as `SYSTEM` or `USER` in the OpenAI API.
spec.backend.ai.promptCaching <object>	# Automatic prompt caching for supported providers, currently AWS Bedrock. Reduces API costs by caching static content like system prompts and tool definitions. Only applicable for Bedrock Claude 3+ and Nova models.
spec.backend.ai.promptCaching.cacheMessageOffset <integer> default=0 min=0	# Shifts the message cache point further back in the conversation. 0 (default) places it at the second-to-last message. Higher values move it N additional messages towards the start, clamped to bounds.
spec.backend.ai.promptCaching.cacheMessages <boolean> default=true	# Enables caching for conversation messages. Caches all messages in the conversation for cost savings.
spec.backend.ai.promptCaching.cacheSystem <boolean> default=true	# Enables caching for system prompts. Inserts a cache point after all system messages.
spec.backend.ai.promptCaching.cacheTools <boolean> default=false	# Enables caching for tool definitions. Inserts a cache point after all tool specifications.
spec.backend.ai.promptCaching.minTokens <integer> default=1024 min=0	# Minimum estimated token count before caching is enabled. Uses rough heuristic (word count × 1.3) to estimate tokens. Bedrock requires at least 1,024 tokens for caching to be effective.
spec.backend.ai.promptGuard <object>	# Guardrails for LLM requests and responses.
spec.backend.ai.promptGuard.request <[]object> min=1 max=8	# Prompt guards to apply to requests sent by the client.
spec.backend.ai.promptGuard.request[].bedrockGuardrails <object>	# AWS Bedrock Guardrails settings for prompt guarding.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.identifier <string> (required) min=1 max=256	# Identifier of the Guardrail policy to use for the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies <object>	# Policies for communicating with AWS Bedrock Guardrails.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth <object>	# Settings for managing authentication to the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws <object>	# Explicit AWS authentication method for the backend. When omitted, default AWS SDK credential discovery is used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the AWS credentials. When using the default Secret resolver, the `Secret` must have keys `accessKey`, `secretKey`, and optionally `sessionToken`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure <object>	# Azure authentication method for the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.managedIdentity.clientId <string> (required)
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.managedIdentity.objectId <string> (required)
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.managedIdentity.resourceId <string> (required)
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the Azure credentials. When using the default Secret resolver, the `Secret` must have keys `clientID`, `tenantID`, and `clientSecret`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.azure.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp <object>	# Google authentication method for the backend. When omitted, default Google credential discovery is used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing ADC-compatible Google credential JSON. When using the default Secret resolver, this must be stored in the `credentials.json` key. When omitted, ambient credentials are used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.cookie <object>
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.header <object>
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.header.prefix <string> min=1 max=256
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.queryParameter <object>
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.passthrough <object (free-form)>	# Passes through an existing token that has been sent by the client and validated. Other policies, like JWT and API key authentication, will strip the original client credentials. Passthrough backend authentication causes the original token to be added back into the request. If there are no client authentication policies on the request, the original token would be unchanged, so this would have no effect.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the key to use as the authorization value. When using the default Secret resolver, this must be stored in the `Authorization` key.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.http <object>	# Settings for managing HTTP requests to the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.http.requestTimeout <string>	# Deadline for receiving a response from the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version to use when connecting to the backend. If not specified, the version is automatically determined: * `Service` types can specify it with `appProtocol` on the `Service` port. * If traffic is identified as gRPC, `HTTP2` is used. * If the incoming traffic was plaintext HTTP, the original protocol will be used. * If the incoming traffic was HTTPS, `HTTP1` will be used. This is because most clients will transparently upgrade HTTPS traffic to `HTTP2`, even if the backend doesn't support it.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tcp <object>	# Settings for managing TCP connections to the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tcp.connectTimeout <string>	# Deadline for establishing a connection to the destination.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls <object>	# Settings for managing TLS connections to the backend. If this field is set, TLS will be initiated to the backend; the system trusted CA certificates will be used to validate the server, and the SNI will automatically be set based on the destination.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate. WARNING: This is an insecure option that should only be used if the risks are understood. There are two modes: * `All` disables all TLS verification. * `Hostname` verifies the CA certificate is trusted, but ignores any mismatch of hostname or SANs. Note that this method is still insecure; prefer setting `verifySubjectAltNames` to customize the valid hostnames if possible.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend, using the specified key (`tls.key`) and cert (`tls.crt`) from the referenced credential source, defaulting to a Kubernetes `Secret`. An optional `ca.cert` field, if present, will be used to verify the server certificate. If `caCertificateRefs` is also specified, the `caCertificateRefs` field takes priority. If unspecified, no client certificate will be used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel <object>	# Settings for managing tunnel connections, with behavior like `HTTPS_PROXY`, to the backend.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.request[].bedrockGuardrails.region <string> (required) min=1 max=256	# AWS region where the guardrail is deployed, for example `us-west-2`).
spec.backend.ai.promptGuard.request[].bedrockGuardrails.version <string> (required) min=1 max=256	# Version of the Guardrail policy to use for the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor <object>	# Google Model Armor settings for prompt guarding.
spec.backend.ai.promptGuard.request[].googleModelArmor.location <string> default="us-central1" min=1 max=256	# Google Cloud location, for example `us-central1`. Defaults to `us-central1` if not specified.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies <object>	# Policies for communicating with Google Model Armor.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth <object>	# Settings for managing authentication to the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws <object>	# Explicit AWS authentication method for the backend. When omitted, default AWS SDK credential discovery is used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the AWS credentials. When using the default Secret resolver, the `Secret` must have keys `accessKey`, `secretKey`, and optionally `sessionToken`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure <object>	# Azure authentication method for the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.managedIdentity.clientId <string> (required)
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.managedIdentity.objectId <string> (required)
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.managedIdentity.resourceId <string> (required)
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the Azure credentials. When using the default Secret resolver, the `Secret` must have keys `clientID`, `tenantID`, and `clientSecret`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.azure.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp <object>	# Google authentication method for the backend. When omitted, default Google credential discovery is used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing ADC-compatible Google credential JSON. When using the default Secret resolver, this must be stored in the `credentials.json` key. When omitted, ambient credentials are used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.cookie <object>
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.header <object>
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.header.prefix <string> min=1 max=256
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.queryParameter <object>
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.passthrough <object (free-form)>	# Passes through an existing token that has been sent by the client and validated. Other policies, like JWT and API key authentication, will strip the original client credentials. Passthrough backend authentication causes the original token to be added back into the request. If there are no client authentication policies on the request, the original token would be unchanged, so this would have no effect.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the key to use as the authorization value. When using the default Secret resolver, this must be stored in the `Authorization` key.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.auth.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.http <object>	# Settings for managing HTTP requests to the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.http.requestTimeout <string>	# Deadline for receiving a response from the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version to use when connecting to the backend. If not specified, the version is automatically determined: * `Service` types can specify it with `appProtocol` on the `Service` port. * If traffic is identified as gRPC, `HTTP2` is used. * If the incoming traffic was plaintext HTTP, the original protocol will be used. * If the incoming traffic was HTTPS, `HTTP1` will be used. This is because most clients will transparently upgrade HTTPS traffic to `HTTP2`, even if the backend doesn't support it.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tcp <object>	# Settings for managing TCP connections to the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tcp.connectTimeout <string>	# Deadline for establishing a connection to the destination.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls <object>	# Settings for managing TLS connections to the backend. If this field is set, TLS will be initiated to the backend; the system trusted CA certificates will be used to validate the server, and the SNI will automatically be set based on the destination.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate. WARNING: This is an insecure option that should only be used if the risks are understood. There are two modes: * `All` disables all TLS verification. * `Hostname` verifies the CA certificate is trusted, but ignores any mismatch of hostname or SANs. Note that this method is still insecure; prefer setting `verifySubjectAltNames` to customize the valid hostnames if possible.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend, using the specified key (`tls.key`) and cert (`tls.crt`) from the referenced credential source, defaulting to a Kubernetes `Secret`. An optional `ca.cert` field, if present, will be used to verify the server certificate. If `caCertificateRefs` is also specified, the `caCertificateRefs` field takes priority. If unspecified, no client certificate will be used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef[].group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef[].kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel <object>	# Settings for managing tunnel connections, with behavior like `HTTPS_PROXY`, to the backend.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.request[].googleModelArmor.projectId <string> (required) min=1 max=256	# Google Cloud project ID.
spec.backend.ai.promptGuard.request[].googleModelArmor.templateId <string> (required) min=1 max=256	# Template ID for Google Model Armor.
spec.backend.ai.promptGuard.request[].openAIModeration <object>	# Passes prompt data through the OpenAI Moderations endpoint. See https://developers.openai.com/api/reference/resources/moderations for more information.
spec.backend.ai.promptGuard.request[].openAIModeration.model <string>	# Moderation model to use. For example, `omni-moderation`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies <object>	# Policies for communicating with OpenAI.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth <object>	# Settings for managing authentication to the backend.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws <object>	# Explicit AWS authentication method for the backend. When omitted, default AWS SDK credential discovery is used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the AWS credentials. When using the default Secret resolver, the `Secret` must have keys `accessKey`, `secretKey`, and optionally `sessionToken`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure <object>	# Azure authentication method for the backend.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.managedIdentity.clientId <string> (required)
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.managedIdentity.objectId <string> (required)
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.managedIdentity.resourceId <string> (required)
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the Azure credentials. When using the default Secret resolver, the `Secret` must have keys `clientID`, `tenantID`, and `clientSecret`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.azure.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp <object>	# Google authentication method for the backend. When omitted, default Google credential discovery is used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing ADC-compatible Google credential JSON. When using the default Secret resolver, this must be stored in the `credentials.json` key. When omitted, ambient credentials are used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.cookie <object>
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.header <object>
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.header.prefix <string> min=1 max=256
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.queryParameter <object>
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.passthrough <object (free-form)>	# Passes through an existing token that has been sent by the client and validated. Other policies, like JWT and API key authentication, will strip the original client credentials. Passthrough backend authentication causes the original token to be added back into the request. If there are no client authentication policies on the request, the original token would be unchanged, so this would have no effect.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the key to use as the authorization value. When using the default Secret resolver, this must be stored in the `Authorization` key.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.auth.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.http <object>	# Settings for managing HTTP requests to the backend.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.http.requestTimeout <string>	# Deadline for receiving a response from the backend.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version to use when connecting to the backend. If not specified, the version is automatically determined: * `Service` types can specify it with `appProtocol` on the `Service` port. * If traffic is identified as gRPC, `HTTP2` is used. * If the incoming traffic was plaintext HTTP, the original protocol will be used. * If the incoming traffic was HTTPS, `HTTP1` will be used. This is because most clients will transparently upgrade HTTPS traffic to `HTTP2`, even if the backend doesn't support it.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tcp <object>	# Settings for managing TCP connections to the backend.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tcp.connectTimeout <string>	# Deadline for establishing a connection to the destination.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls <object>	# Settings for managing TLS connections to the backend. If this field is set, TLS will be initiated to the backend; the system trusted CA certificates will be used to validate the server, and the SNI will automatically be set based on the destination.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate. WARNING: This is an insecure option that should only be used if the risks are understood. There are two modes: * `All` disables all TLS verification. * `Hostname` verifies the CA certificate is trusted, but ignores any mismatch of hostname or SANs. Note that this method is still insecure; prefer setting `verifySubjectAltNames` to customize the valid hostnames if possible.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend, using the specified key (`tls.key`) and cert (`tls.crt`) from the referenced credential source, defaulting to a Kubernetes `Secret`. An optional `ca.cert` field, if present, will be used to verify the server certificate. If `caCertificateRefs` is also specified, the `caCertificateRefs` field takes priority. If unspecified, no client certificate will be used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef[].group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef[].kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel <object>	# Settings for managing tunnel connections, with behavior like `HTTPS_PROXY`, to the backend.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.request[].regex <object>	# Regular expression (regex) matching for prompt guards and data masking.
spec.backend.ai.promptGuard.request[].regex.action <string> enum=Mask|Reject default="Mask"	# The action to take if a regex pattern is matched in a request or response. This setting applies only to request matches. `PromptguardResponse` matches are always masked by default. Defaults to `Mask`.
spec.backend.ai.promptGuard.request[].regex.builtins <[]string>	# Built-in regex patterns to match against the request or response. Matches and built-ins are additive.
spec.backend.ai.promptGuard.request[].regex.matches <[]string>	# Regex patterns to match against the request or response. Matches and built-ins are additive.
spec.backend.ai.promptGuard.request[].response <object>	# Custom response message to return to the client. If not specified, defaults to `The request was rejected due to inappropriate content`.
spec.backend.ai.promptGuard.request[].response.message <string> default="The request was rejected due to inappropriate content"	# Custom response message to return to the client. If not specified, defaults to `The request was rejected due to inappropriate content`.
spec.backend.ai.promptGuard.request[].response.statusCode <integer> default=403 format=int32 min=200 max=599	# Status code to return to the client. Defaults to 403.
spec.backend.ai.promptGuard.request[].webhook <object>	# Webhook that receives requests for prompt guarding.
spec.backend.ai.promptGuard.request[].webhook.backendRef <object> (required)	# Webhook server to reach. Supported types: Service and Backend.
spec.backend.ai.promptGuard.request[].webhook.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.request[].webhook.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.request[].webhook.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.request[].webhook.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.request[].webhook.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.request[].webhook.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the webhook guardrail is unavailable or returns an error. `FailOpen` allows the request to continue. `FailClosed` (default) rejects the request.
spec.backend.ai.promptGuard.request[].webhook.forwardHeaderMatches <[]object>	# HTTP header matches used to select the headers to forward to the webhook. Request headers are used when forwarding requests and response headers are used when forwarding responses. By default, no headers are forwarded.
spec.backend.ai.promptGuard.request[].webhook.forwardHeaderMatches[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, only the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent. When a header is repeated in an HTTP request, it is implementation-specific behavior as to how this is represented. Generally, proxies should follow the guidance from the RFC: https://www.rfc-editor.org/rfc/rfc7230.html#section-3.2.2 regarding processing a repeated header, with special handling for "Set-Cookie".
spec.backend.ai.promptGuard.request[].webhook.forwardHeaderMatches[].type <string> enum=Exact|RegularExpression default="Exact"	# Type specifies how to match against the value of the header. Support: Core (Exact) Support: Implementation-specific (RegularExpression) Since RegularExpression HeaderMatchType has implementation-specific conformance, implementations can support POSIX, PCRE or any other dialects of regular expressions. Please read the implementation's documentation to determine the supported dialect.
spec.backend.ai.promptGuard.request[].webhook.forwardHeaderMatches[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.backend.ai.promptGuard.response <[]object> min=1 max=8	# Prompt guards to apply to responses returned by the LLM provider.
spec.backend.ai.promptGuard.response[].bedrockGuardrails <object>	# AWS Bedrock Guardrails settings for prompt guarding.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.identifier <string> (required) min=1 max=256	# Identifier of the Guardrail policy to use for the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies <object>	# Policies for communicating with AWS Bedrock Guardrails.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth <object>	# Settings for managing authentication to the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws <object>	# Explicit AWS authentication method for the backend. When omitted, default AWS SDK credential discovery is used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the AWS credentials. When using the default Secret resolver, the `Secret` must have keys `accessKey`, `secretKey`, and optionally `sessionToken`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure <object>	# Azure authentication method for the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.managedIdentity.clientId <string> (required)
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.managedIdentity.objectId <string> (required)
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.managedIdentity.resourceId <string> (required)
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the Azure credentials. When using the default Secret resolver, the `Secret` must have keys `clientID`, `tenantID`, and `clientSecret`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.azure.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp <object>	# Google authentication method for the backend. When omitted, default Google credential discovery is used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing ADC-compatible Google credential JSON. When using the default Secret resolver, this must be stored in the `credentials.json` key. When omitted, ambient credentials are used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.cookie <object>
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.header <object>
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.header.prefix <string> min=1 max=256
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.queryParameter <object>
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.passthrough <object (free-form)>	# Passes through an existing token that has been sent by the client and validated. Other policies, like JWT and API key authentication, will strip the original client credentials. Passthrough backend authentication causes the original token to be added back into the request. If there are no client authentication policies on the request, the original token would be unchanged, so this would have no effect.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the key to use as the authorization value. When using the default Secret resolver, this must be stored in the `Authorization` key.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.http <object>	# Settings for managing HTTP requests to the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.http.requestTimeout <string>	# Deadline for receiving a response from the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version to use when connecting to the backend. If not specified, the version is automatically determined: * `Service` types can specify it with `appProtocol` on the `Service` port. * If traffic is identified as gRPC, `HTTP2` is used. * If the incoming traffic was plaintext HTTP, the original protocol will be used. * If the incoming traffic was HTTPS, `HTTP1` will be used. This is because most clients will transparently upgrade HTTPS traffic to `HTTP2`, even if the backend doesn't support it.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tcp <object>	# Settings for managing TCP connections to the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tcp.connectTimeout <string>	# Deadline for establishing a connection to the destination.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls <object>	# Settings for managing TLS connections to the backend. If this field is set, TLS will be initiated to the backend; the system trusted CA certificates will be used to validate the server, and the SNI will automatically be set based on the destination.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate. WARNING: This is an insecure option that should only be used if the risks are understood. There are two modes: * `All` disables all TLS verification. * `Hostname` verifies the CA certificate is trusted, but ignores any mismatch of hostname or SANs. Note that this method is still insecure; prefer setting `verifySubjectAltNames` to customize the valid hostnames if possible.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend, using the specified key (`tls.key`) and cert (`tls.crt`) from the referenced credential source, defaulting to a Kubernetes `Secret`. An optional `ca.cert` field, if present, will be used to verify the server certificate. If `caCertificateRefs` is also specified, the `caCertificateRefs` field takes priority. If unspecified, no client certificate will be used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel <object>	# Settings for managing tunnel connections, with behavior like `HTTPS_PROXY`, to the backend.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.response[].bedrockGuardrails.region <string> (required) min=1 max=256	# AWS region where the guardrail is deployed, for example `us-west-2`).
spec.backend.ai.promptGuard.response[].bedrockGuardrails.version <string> (required) min=1 max=256	# Version of the Guardrail policy to use for the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor <object>	# Google Model Armor settings for prompt guarding.
spec.backend.ai.promptGuard.response[].googleModelArmor.location <string> default="us-central1" min=1 max=256	# Google Cloud location, for example `us-central1`. Defaults to `us-central1` if not specified.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies <object>	# Policies for communicating with Google Model Armor.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth <object>	# Settings for managing authentication to the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws <object>	# Explicit AWS authentication method for the backend. When omitted, default AWS SDK credential discovery is used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the AWS credentials. When using the default Secret resolver, the `Secret` must have keys `accessKey`, `secretKey`, and optionally `sessionToken`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure <object>	# Azure authentication method for the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.managedIdentity.clientId <string> (required)
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.managedIdentity.objectId <string> (required)
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.managedIdentity.resourceId <string> (required)
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the Azure credentials. When using the default Secret resolver, the `Secret` must have keys `clientID`, `tenantID`, and `clientSecret`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.azure.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp <object>	# Google authentication method for the backend. When omitted, default Google credential discovery is used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing ADC-compatible Google credential JSON. When using the default Secret resolver, this must be stored in the `credentials.json` key. When omitted, ambient credentials are used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.cookie <object>
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.header <object>
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.header.prefix <string> min=1 max=256
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.queryParameter <object>
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.passthrough <object (free-form)>	# Passes through an existing token that has been sent by the client and validated. Other policies, like JWT and API key authentication, will strip the original client credentials. Passthrough backend authentication causes the original token to be added back into the request. If there are no client authentication policies on the request, the original token would be unchanged, so this would have no effect.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the key to use as the authorization value. When using the default Secret resolver, this must be stored in the `Authorization` key.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.auth.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.http <object>	# Settings for managing HTTP requests to the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.http.requestTimeout <string>	# Deadline for receiving a response from the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version to use when connecting to the backend. If not specified, the version is automatically determined: * `Service` types can specify it with `appProtocol` on the `Service` port. * If traffic is identified as gRPC, `HTTP2` is used. * If the incoming traffic was plaintext HTTP, the original protocol will be used. * If the incoming traffic was HTTPS, `HTTP1` will be used. This is because most clients will transparently upgrade HTTPS traffic to `HTTP2`, even if the backend doesn't support it.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tcp <object>	# Settings for managing TCP connections to the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tcp.connectTimeout <string>	# Deadline for establishing a connection to the destination.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls <object>	# Settings for managing TLS connections to the backend. If this field is set, TLS will be initiated to the backend; the system trusted CA certificates will be used to validate the server, and the SNI will automatically be set based on the destination.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate. WARNING: This is an insecure option that should only be used if the risks are understood. There are two modes: * `All` disables all TLS verification. * `Hostname` verifies the CA certificate is trusted, but ignores any mismatch of hostname or SANs. Note that this method is still insecure; prefer setting `verifySubjectAltNames` to customize the valid hostnames if possible.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend, using the specified key (`tls.key`) and cert (`tls.crt`) from the referenced credential source, defaulting to a Kubernetes `Secret`. An optional `ca.cert` field, if present, will be used to verify the server certificate. If `caCertificateRefs` is also specified, the `caCertificateRefs` field takes priority. If unspecified, no client certificate will be used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef[].group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef[].kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel <object>	# Settings for managing tunnel connections, with behavior like `HTTPS_PROXY`, to the backend.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.response[].googleModelArmor.projectId <string> (required) min=1 max=256	# Google Cloud project ID.
spec.backend.ai.promptGuard.response[].googleModelArmor.templateId <string> (required) min=1 max=256	# Template ID for Google Model Armor.
spec.backend.ai.promptGuard.response[].regex <object>	# Regular expression (regex) matching for prompt guards and data masking.
spec.backend.ai.promptGuard.response[].regex.action <string> enum=Mask|Reject default="Mask"	# The action to take if a regex pattern is matched in a request or response. This setting applies only to request matches. `PromptguardResponse` matches are always masked by default. Defaults to `Mask`.
spec.backend.ai.promptGuard.response[].regex.builtins <[]string>	# Built-in regex patterns to match against the request or response. Matches and built-ins are additive.
spec.backend.ai.promptGuard.response[].regex.matches <[]string>	# Regex patterns to match against the request or response. Matches and built-ins are additive.
spec.backend.ai.promptGuard.response[].response <object>	# Custom response message to return to the client. If not specified, defaults to `The response was rejected due to inappropriate content`.
spec.backend.ai.promptGuard.response[].response.message <string> default="The request was rejected due to inappropriate content"	# Custom response message to return to the client. If not specified, defaults to `The request was rejected due to inappropriate content`.
spec.backend.ai.promptGuard.response[].response.statusCode <integer> default=403 format=int32 min=200 max=599	# Status code to return to the client. Defaults to 403.
spec.backend.ai.promptGuard.response[].webhook <object>	# Webhook that receives responses for prompt guarding.
spec.backend.ai.promptGuard.response[].webhook.backendRef <object> (required)	# Webhook server to reach. Supported types: Service and Backend.
spec.backend.ai.promptGuard.response[].webhook.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.ai.promptGuard.response[].webhook.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.ai.promptGuard.response[].webhook.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.ai.promptGuard.response[].webhook.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.ai.promptGuard.response[].webhook.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.ai.promptGuard.response[].webhook.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the webhook guardrail is unavailable or returns an error. `FailOpen` allows the request to continue. `FailClosed` (default) rejects the request.
spec.backend.ai.promptGuard.response[].webhook.forwardHeaderMatches <[]object>	# HTTP header matches used to select the headers to forward to the webhook. Request headers are used when forwarding requests and response headers are used when forwarding responses. By default, no headers are forwarded.
spec.backend.ai.promptGuard.response[].webhook.forwardHeaderMatches[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, only the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent. When a header is repeated in an HTTP request, it is implementation-specific behavior as to how this is represented. Generally, proxies should follow the guidance from the RFC: https://www.rfc-editor.org/rfc/rfc7230.html#section-3.2.2 regarding processing a repeated header, with special handling for "Set-Cookie".
spec.backend.ai.promptGuard.response[].webhook.forwardHeaderMatches[].type <string> enum=Exact|RegularExpression default="Exact"	# Type specifies how to match against the value of the header. Support: Core (Exact) Support: Implementation-specific (RegularExpression) Since RegularExpression HeaderMatchType has implementation-specific conformance, implementations can support POSIX, PCRE or any other dialects of regular expressions. Please read the implementation's documentation to determine the supported dialect.
spec.backend.ai.promptGuard.response[].webhook.forwardHeaderMatches[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.backend.ai.promptGuard.streaming <string> enum=Enabled	# Apply prompt guards to streaming responses and realtime websocket messages. Defaults to disabled to preserve streaming throughput unless explicitly enabled.
spec.backend.ai.routes <map[string]string>	# Rules for identifying the type of traffic to handle. The keys are URL path suffixes matched using ends-with comparison, for example `"/v1/chat/completions"`. The special `*` wildcard matches any path. If not specified, all traffic defaults to `completions` type.
spec.backend.ai.transformations <[]object> min=1 max=64	# CEL transformations to compute and set fields in the request body. The expression result overwrites any existing value for that field. This has a higher priority than `overrides` if both are set for the same key.
spec.backend.ai.transformations[].expression <string> (required) min=1 max=16384	# CEL expression used to compute the field value.
spec.backend.ai.transformations[].field <string> (required) max=256	# Name of the field to set.
spec.backend.auth <object>	# Settings for managing authentication to the backend.
spec.backend.auth.aws <object>	# Explicit AWS authentication method for the backend. When omitted, default AWS SDK credential discovery is used.
spec.backend.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.backend.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.backend.auth.aws.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the AWS credentials. When using the default Secret resolver, the `Secret` must have keys `accessKey`, `secretKey`, and optionally `sessionToken`.
spec.backend.auth.aws.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.auth.aws.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.auth.aws.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.backend.auth.azure <object>	# Azure authentication method for the backend.
spec.backend.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.backend.auth.azure.managedIdentity.clientId <string> (required)
spec.backend.auth.azure.managedIdentity.objectId <string> (required)
spec.backend.auth.azure.managedIdentity.resourceId <string> (required)
spec.backend.auth.azure.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing the Azure credentials. When using the default Secret resolver, the `Secret` must have keys `clientID`, `tenantID`, and `clientSecret`.
spec.backend.auth.azure.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.auth.azure.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.auth.azure.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.auth.gcp <object>	# Google authentication method for the backend. When omitted, default Google credential discovery is used.
spec.backend.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.backend.auth.gcp.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, containing ADC-compatible Google credential JSON. When using the default Secret resolver, this must be stored in the `credentials.json` key. When omitted, ambient credentials are used.
spec.backend.auth.gcp.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.auth.gcp.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.auth.gcp.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.backend.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.backend.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`.
spec.backend.auth.location.cookie <object>
spec.backend.auth.location.cookie.name <string> (required) min=1 max=256
spec.backend.auth.location.header <object>
spec.backend.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.backend.auth.location.header.prefix <string> min=1 max=256
spec.backend.auth.location.queryParameter <object>
spec.backend.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.backend.auth.passthrough <object (free-form)>	# Passes through an existing token that has been sent by the client and validated. Other policies, like JWT and API key authentication, will strip the original client credentials. Passthrough backend authentication causes the original token to be added back into the request. If there are no client authentication policies on the request, the original token would be unchanged, so this would have no effect.
spec.backend.auth.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the key to use as the authorization value. When using the default Secret resolver, this must be stored in the `Authorization` key.
spec.backend.auth.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.auth.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.auth.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.extAuth <object>	# External authentication configuration for requests sent to this backend.
spec.backend.extAuth.backendRef <object>	# External Authorization server to reach. Supported types: `Service` and `Backend`.
spec.backend.extAuth.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.extAuth.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.extAuth.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.extAuth.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.extAuth.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.extAuth.cache <object>	# Caches gRPC authorization results. WARNING: the safety of this feature depends on the cache key accurately capturing every request property that the authorization service uses to make a decision. For example, if the service returns different results based on both path and authorization header, both must be included in `key`; otherwise, one request may incorrectly reuse another request's authorization result. If any key expression fails to evaluate or produces an unsupported value, the request is still sent to the authorization service, but its result is not read from or written to the cache.
spec.backend.extAuth.cache.key <[]string> (required) min=1 max=16	# Ordered list of CEL expressions evaluated against the request to construct the cache key.
spec.backend.extAuth.cache.maxEntries <integer> format=int32 min=1	# Maximum number of authorization results to keep in the cache. If unset, this defaults to 10000.
spec.backend.extAuth.cache.ttl <string> (required) min=1 max=16384	# Duration string, such as `5m`, or a CEL expression that returns the duration that cached authorization results may be reused, or a timestamp when the cached authorization result expires. The expression is evaluated after the authorization response has been applied to the request.
spec.backend.extAuth.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the external authorization service is unavailable or returns an error. "FailOpen" allows the request to continue. "FailClosed" (default) denies the request.
spec.backend.extAuth.forwardBody <object>	# Whether to include the HTTP body in the authorization request. If enabled, the request body will be buffered.
spec.backend.extAuth.forwardBody.maxSize <int-or-string> (required)	# Largest body, in bytes, that will be buffered and sent to the authorization server. If the body size is larger than `maxSize`, then the request will be rejected with a response.
spec.backend.extAuth.grpc <object>	# Uses the gRPC External Authorization [protocol](https://www.envoyproxy.io/docs/envoy/latest/api-v3/service/auth/v3/external_auth.proto) should be used.
spec.backend.extAuth.grpc.contextExtensions <map[string]string>	# Additional arbitrary key-value pairs to send to the authorization server in the `context_extensions` field.
spec.backend.extAuth.grpc.requestMetadata <map[string]string>	# Metadata to send to the authorization server. This maps to the `metadata_context.filter_metadata` field of the request, and allows dynamic CEL expressions. If unset, by default the `envoy.filters.http.jwt_authn` key is set if the JWT policy is used as well, for compatibility.
spec.backend.extAuth.http <object>	# Uses HTTP to connect to the authorization server. The authorization server must return a `200` status code, otherwise the request is considered an authorization failure.
spec.backend.extAuth.http.addRequestHeaders <map[string]string>	# Additional headers to add to the request to the authorization server. While `allowedRequestHeaders` just passes the original headers through, `addRequestHeaders` allows defining custom headers based on CEL expressions.
spec.backend.extAuth.http.allowedRequestHeaders <[]string> max=64	# Additional headers from the client request that will be sent to the authorization server. If unset, the following headers are sent by default: `Authorization`.
spec.backend.extAuth.http.allowedResponseHeaders <[]string> max=64	# Headers from the authorization response that will be copied into the request to the backend.
spec.backend.extAuth.http.path <string> min=1 max=16384	# Path to send to the authorization server. If unset, this defaults to the original request path. This is a CEL expression, which allows customizing the path based on the incoming request. For example, to add a prefix, use `"/prefix/" + request.path`.
spec.backend.extAuth.http.redirect <string> min=1 max=16384	# Optional expression that determines a path to redirect to on authorization failure. This is useful to redirect to a sign-in page.
spec.backend.extAuth.http.responseMetadata <map[string]string>	# Metadata fields to construct from the authorization response. These will be included under the `extauthz` variable in future CEL expressions. Setting this is useful for things like logging usernames, without needing to include them as headers to the backend, as `allowedResponseHeaders` would.
spec.backend.health <object>	# Settings for passive and active health checking.
spec.backend.health.eviction <object>	# Settings for evicting unhealthy backends.
spec.backend.health.eviction.consecutiveFailures <integer> format=int32 min=0	# Number of consecutive unhealthy responses required before the backend is evicted. For example, a value of 5 means the backend must receive 5 unhealthy responses in a row before being evicted. When both consecutiveFailures and healthThreshold are set, the backend is evicted when either condition is met. When neither is set, a single unhealthy response can trigger eviction.
spec.backend.health.eviction.duration <string> default="3s"	# Base time a backend should be evicted after being marked unhealthy. Subsequent evictions use multiplicative backoff (duration * times_evicted). If all endpoints are evicted, the load balancer falls back to returning evicted endpoints rather than failing entirely. If unset, defaults to `3s`.
spec.backend.health.eviction.healthThreshold <integer> format=int32 min=0 max=100	# EWMA health score threshold, expressed as 0 to 100. When set, a backend is only evicted if its computed health drops below this value after an unhealthy response. For example, 50 means the backend is evicted when its EWMA health falls below 50% following failures. Unlike consecutiveFailures (which counts consecutive failures), this uses a sliding-window average so a single success in a stream of failures can delay eviction. When both consecutiveFailures and healthThreshold are set, the backend is evicted when either condition is met. When neither is set, a single unhealthy response triggers eviction.
spec.backend.health.eviction.restoreHealth <integer> format=int32 min=0 max=100	# Health score from 0 to 100 assigned to a backend when it returns from eviction. For gradual recovery, set below 100; for full recovery immediately, set 100. If unset, the backend resumes with the health it had when evicted.
spec.backend.health.unhealthyCondition <string> min=1 max=16384	# CEL expression that determines whether a response indicates an unhealthy backend. When the expression evaluates to true, the backend is considered unhealthy and may be evicted. For example, to evict on 5xx responses: `response.code >= 500`. When unset, any 5xx response, or a connection failure, is treated as unhealthy. This default lowers the backend's health score but does not trigger eviction on its own.
spec.backend.http <object>	# Settings for managing HTTP requests to the backend.
spec.backend.http.requestTimeout <string>	# Deadline for receiving a response from the backend.
spec.backend.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version to use when connecting to the backend. If not specified, the version is automatically determined: * `Service` types can specify it with `appProtocol` on the `Service` port. * If traffic is identified as gRPC, `HTTP2` is used. * If the incoming traffic was plaintext HTTP, the original protocol will be used. * If the incoming traffic was HTTPS, `HTTP1` will be used. This is because most clients will transparently upgrade HTTPS traffic to `HTTP2`, even if the backend doesn't support it.
spec.backend.mcp <object>	# Settings for MCP workloads. This is only applicable when connecting to a `Backend` of type `mcp`.
spec.backend.mcp.authentication <object>	# MCP backend-specific authentication rules. This field is deprecated; prefer to use traffic policy `jwtAuthentication.mcp`, which ensures authentication runs before other policies such as transformation and rate limiting.
spec.backend.mcp.authentication.audiences <[]string> min=1 max=64	# Allowed audiences that are allowed access. This corresponds to the `aud` claim ([RFC 7519 §4.1.3](https://datatracker.ietf.org/doc/html/rfc7519#section-4.1.3)). If unset, any audience is allowed.
spec.backend.mcp.authentication.clientId <string>	# Client ID to use for short-circuiting Dynamic Client Registration. If set, the gateway will not proxy registration requests to the IDP and instead return this client ID.
spec.backend.mcp.authentication.issuer <string> min=1 max=256	# IdP that issued the JWT. This corresponds to the `iss` claim ([RFC 7519 §4.1.1](https://tools.ietf.org/html/rfc7519#section-4.1.1)).
spec.backend.mcp.authentication.jwks <object> (required)	# Remote JSON Web Key used to validate the signature of the JWT.
spec.backend.mcp.authentication.jwks.backendRef <object> (required)	# Remote JWKS server to reach. Supported types are `Service` and static `Backend`. An `AgentgatewayPolicy` containing backend TLS config can then be attached to the `Service` or `Backend` in order to set TLS options for a connection to the remote `jwks` source.
spec.backend.mcp.authentication.jwks.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.mcp.authentication.jwks.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.mcp.authentication.jwks.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.mcp.authentication.jwks.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.mcp.authentication.jwks.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.mcp.authentication.jwks.cacheDuration <string> default="5m"
spec.backend.mcp.authentication.jwks.jwksPath <string> (required) min=1 max=2000	# Path to the IdP `jwks` endpoint, relative to the root, commonly `".well-known/jwks.json"`.
spec.backend.mcp.authentication.mode <string> enum=Optional|Permissive|Strict default="Strict"	# Validation mode for JWT authentication.
spec.backend.mcp.authentication.provider <string> enum=Auth0|Keycloak|Okta	# Identity provider to use for authentication.
spec.backend.mcp.authentication.resourceMetadata <object (free-form)>	# Metadata to use for MCP resources.
spec.backend.mcp.authorization <object>	# MCP backend authorization. Unlike authorization at the HTTP level, which rejects unauthorized requests with a `403` error, this policy works at the `MCPBackend` level. List operations, such as `list_tools`, will have each item evaluated. Items that do not meet the rule will be filtered. Get or call operations, such as `call_tool`, will evaluate the specific item and reject requests that do not meet the rule.
spec.backend.mcp.authorization.action <string> enum=Allow|Deny|Require default="Allow"	# The effect of this rule when it matches. If unspecified, defaults to `Allow`. `Require` rules are cumulative: all require rules must match.
spec.backend.mcp.authorization.policy <object> (required)	# The authorization rule to evaluate. * `Allow`: any matching allow rule allows the request. * `Require`: every require rule must match for the request to be allowed. * `Deny`: any matching deny rule denies the request. A CEL expression that fails to evaluate does not match. Prefer `Require` for deny-by-default behavior. If at least one `Allow` rule is configured, requests are denied unless at least one allow rule matches.
spec.backend.mcp.authorization.policy.matchExpressions <[]string> (required) min=1 max=256	# CEL expressions that must all evaluate to true for the rule to match.
spec.backend.mcp.guardrails <object>	# `guardrails` routes selected JSON-RPC methods through a remote policy server.
spec.backend.mcp.guardrails.processors <[]object> (required) min=1 max=16	# `processors` is the ordered list of policy processors applied to matched methods. Processors run in the order listed; the first to reject a request short-circuits the chain.
spec.backend.mcp.guardrails.processors[].methods <map[string]string> (required)	# `methods` is the allowlist of JSON-RPC methods (e.g. `tools/call`, `tools/list`) routed through this processor, keyed by method name with the phase it runs in. Keys may be exact, a prefix wildcard (`tools/*`), a suffix wildcard (`*/list`), or `*` for all methods; the most specific match wins. Methods matching no key, including unknown ones, bypass this processor.
spec.backend.mcp.guardrails.processors[].remote <object>	# `remote` configures a gRPC policy server.
spec.backend.mcp.guardrails.processors[].remote.allowedRequestHeaders <[]string> max=64	# `allowedRequestHeaders` lists the incoming request headers forwarded to the policy server in `McpRequest.headers`. If empty, all headers and pseudo-headers (`:authority`, `:method`, ...) are forwarded. Matching is case-insensitive.
spec.backend.mcp.guardrails.processors[].remote.backendRef <object> (required)	# `backendRef` references the remote guardrails policy server. Supported types: `Service` and `Backend`.
spec.backend.mcp.guardrails.processors[].remote.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.mcp.guardrails.processors[].remote.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.mcp.guardrails.processors[].remote.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.mcp.guardrails.processors[].remote.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.mcp.guardrails.processors[].remote.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.backend.mcp.guardrails.processors[].remote.disallowedRequestHeaders <[]string> max=64	# `disallowedRequestHeaders` lists header names never forwarded to the policy server, even if listed in `allowedRequestHeaders`. Matching is case-insensitive.
spec.backend.mcp.guardrails.processors[].remote.failureMode <string> enum=FailClosed|FailOpen	# `failureMode` controls behavior when the policy server is unreachable or returns an error. `FailOpen` allows the request; `FailClosed` (default) denies it.
spec.backend.mcp.guardrails.processors[].remote.metadata <map[string]string>	# `metadata` is static or CEL-evaluated context surfaced to the policy server as fields of the `metadata_context` google.protobuf.Struct, keyed by config key. Values are CEL expressions.
spec.backend.tcp <object>	# Settings for managing TCP connections to the backend.
spec.backend.tcp.connectTimeout <string>	# Deadline for establishing a connection to the destination.
spec.backend.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.backend.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.backend.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.backend.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.backend.tls <object>	# Settings for managing TLS connections to the backend. If this field is set, TLS will be initiated to the backend; the system trusted CA certificates will be used to validate the server, and the SNI will automatically be set based on the destination.
spec.backend.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.backend.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.backend.tls.caCertificateRefs[].name <string> default=""	# Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.backend.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate. WARNING: This is an insecure option that should only be used if the risks are understood. There are two modes: * `All` disables all TLS verification. * `Hostname` verifies the CA certificate is trusted, but ignores any mismatch of hostname or SANs. Note that this method is still insecure; prefer setting `verifySubjectAltNames` to customize the valid hostnames if possible.
spec.backend.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.backend.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend, using the specified key (`tls.key`) and cert (`tls.crt`) from the referenced credential source, defaulting to a Kubernetes `Secret`. An optional `ca.cert` field, if present, will be used to verify the server certificate. If `caCertificateRefs` is also specified, the `caCertificateRefs` field takes priority. If unspecified, no client certificate will be used.
spec.backend.tls.mtlsCertificateRef[].group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.backend.tls.mtlsCertificateRef[].kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.backend.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.backend.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.backend.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.backend.transformation <object>	# Mutates and transforms requests and responses sent to and from the backend.
spec.backend.transformation.request <object>	# Request transformation settings.
spec.backend.transformation.request.add <[]object> min=1 max=16	# Headers to add to the request and what each value should be set to. If there is already a header with these values then append the value as an extra entry.
spec.backend.transformation.request.add[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.backend.transformation.request.add[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.backend.transformation.request.body <string> min=1 max=16384	# HTTP body transformation.
spec.backend.transformation.request.metadata <map[string]string>	# Stores CEL-evaluated values under the `metadata` CEL variable for subsequent policy evaluations. `metadata` is evaluated before header or body transformations.
spec.backend.transformation.request.remove <[]string> min=1 max=16	# Header names to remove from the request or response.
spec.backend.transformation.request.set <[]object> min=1 max=16	# Headers to set and the values to use.
spec.backend.transformation.request.set[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.backend.transformation.request.set[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.backend.transformation.response <object>	# Response transformation settings.
spec.backend.transformation.response.add <[]object> min=1 max=16	# Headers to add to the request and what each value should be set to. If there is already a header with these values then append the value as an extra entry.
spec.backend.transformation.response.add[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.backend.transformation.response.add[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.backend.transformation.response.body <string> min=1 max=16384	# HTTP body transformation.
spec.backend.transformation.response.metadata <map[string]string>	# Stores CEL-evaluated values under the `metadata` CEL variable for subsequent policy evaluations. `metadata` is evaluated before header or body transformations.
spec.backend.transformation.response.remove <[]string> min=1 max=16	# Header names to remove from the request or response.
spec.backend.transformation.response.set <[]object> min=1 max=16	# Headers to set and the values to use.
spec.backend.transformation.response.set[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.backend.transformation.response.set[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.backend.tunnel <object>	# Settings for managing tunnel connections, with behavior like `HTTPS_PROXY`, to the backend.
spec.backend.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.backend.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.backend.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.backend.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.backend.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.backend.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.frontend <object>	# Settings for how to handle incoming traffic. A frontend policy can only target a `Gateway`. `Listener` and `ListenerSet` are not valid targets. When multiple policies are selected for a given request, they are merged on a field-level basis, but not a deep merge. For example, policy A sets `tcp` and `tls`, and policy B sets `tls`; the effective policy would be `tcp` from policy A, and `tls` from policy B.
spec.frontend.accessLog <object>	# Access logging configuration.
spec.frontend.accessLog.attributes <object>	# Customizations to the key-value pairs that are logged.
spec.frontend.accessLog.attributes.add <[]object> min=1	# Additional key-value pairs to add to each entry. The value is a CEL expression. If the CEL expression fails to evaluate, the pair will be excluded.
spec.frontend.accessLog.attributes.add[].expression <string> (required) min=1 max=16384	# A Common Expression Language (CEL) expression.
spec.frontend.accessLog.attributes.add[].name <string> (required) min=1 max=256
spec.frontend.accessLog.attributes.remove <[]string> min=1 max=32	# Default fields to remove. For example, `http.method`.
spec.frontend.accessLog.filter <string> min=1 max=16384	# CEL expression used to filter logs. A log will only be emitted if the expression evaluates to `true`.
spec.frontend.accessLog.otlp <object>	# OTLP access log export to an OpenTelemetry-compatible backend.
spec.frontend.accessLog.otlp.backendRef <object> (required)	# OTLP server to send access logs to. Supported types: `Service` and `AgentgatewayBackend`.
spec.frontend.accessLog.otlp.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.frontend.accessLog.otlp.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.frontend.accessLog.otlp.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.frontend.accessLog.otlp.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.frontend.accessLog.otlp.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.frontend.accessLog.otlp.path <string> min=1 max=1024	# OTLP/HTTP path to use. This is only applicable when `protocol` is `HTTP`. If unset, this defaults to `/v1/logs`.
spec.frontend.accessLog.otlp.protocol <string> enum=GRPC|HTTP default="GRPC"	# OTLP protocol variant to use.
spec.frontend.connect <object>	# Settings for downstream HTTP CONNECT handling. If unset, CONNECT requests are rejected with Method Not Allowed.
spec.frontend.connect.mode <string> (required) enum=Deny|Route|Tunnel	# Whether downstream CONNECT requests are accepted.
spec.frontend.http <object>	# Settings for managing incoming HTTP requests.
spec.frontend.http.http1HeaderCase <string> enum=Lowercase|Preserve	# Controls HTTP/1 request header name casing when encoding responses on the same connection. This only applies to `HTTP/1`. If a request is HTTP/2 in either the incoming or outgoing request, this will be ignored. HTTP/2 requests are always lower case. Modifying the headers from other policies may result in the original case being lost.
spec.frontend.http.http1IdleTimeout <string>	# Timeout before an unused connection is closed. If unset, this defaults to 10 minutes.
spec.frontend.http.http1MaxHeaders <integer> format=int32 min=1 max=4096	# Maximum number of headers allowed in `HTTP/1.1` requests. If unset, this defaults to 100.
spec.frontend.http.http2ConnectionWindowSize <int-or-string>	# Initial window size for connection-level flow control for received data.
spec.frontend.http.http2FrameSize <int-or-string>	# Maximum frame size to use. If unset, this defaults to `16kb`.
spec.frontend.http.http2KeepaliveInterval <string>
spec.frontend.http.http2KeepaliveTimeout <string>
spec.frontend.http.http2MaxHeaderSize <int-or-string>	# Maximum aggregate size of decoded HTTP/2 request headers. If unset, this defaults to `16Ki`.
spec.frontend.http.http2WindowSize <int-or-string>	# Initial window size for stream-level flow control for received data.
spec.frontend.http.maxBufferSize <int-or-string>	# Maximum HTTP body size that will be buffered into memory. Bodies will only be buffered for policies which require buffering. If unset, this defaults to `2mb`.
spec.frontend.http.maxConnectionDuration <string>	# Maximum time a connection is allowed to remain open. After this duration, the connection is gracefully closed after the current in-flight request completes. Useful for ensuring even traffic distribution behind load balancers during scaling events.
spec.frontend.metrics <object>	# Custom Prometheus metric label configuration. CEL expressions are evaluated per-request and added as labels to all Prometheus metrics exposed by agentgateway.
spec.frontend.metrics.attributes <object> (required)	# Customizations to the labels that are added to Prometheus metrics.
spec.frontend.metrics.attributes.add <[]object> min=1 max=16	# Additional key-value pairs to add as custom labels to all Prometheus metrics. The value is a CEL expression evaluated per-request. If the CEL expression fails to evaluate, the label value is set to "unknown". WARNING: High-cardinality labels (e.g., per-user IDs) can significantly increase Prometheus storage and memory usage. Prefer low-cardinality dimensions like team or environment.
spec.frontend.metrics.attributes.add[].expression <string> (required) min=1 max=16384	# A Common Expression Language (CEL) expression.
spec.frontend.metrics.attributes.add[].name <string> (required) min=1 max=256
spec.frontend.networkAuthorization <object>	# CEL authorization on downstream network connections. This runs before protocol handling and is intended for L4 access control, for example using `source.address` with `cidr(...).containsIP(...)`.
spec.frontend.networkAuthorization.action <string> enum=Allow|Deny|Require default="Allow"	# The effect of this rule when it matches. If unspecified, defaults to `Allow`. `Require` rules are cumulative: all require rules must match.
spec.frontend.networkAuthorization.policy <object> (required)	# The authorization rule to evaluate. * `Allow`: any matching allow rule allows the request. * `Require`: every require rule must match for the request to be allowed. * `Deny`: any matching deny rule denies the request. A CEL expression that fails to evaluate does not match. Prefer `Require` for deny-by-default behavior. If at least one `Allow` rule is configured, requests are denied unless at least one allow rule matches.
spec.frontend.networkAuthorization.policy.matchExpressions <[]string> (required) min=1 max=256	# CEL expressions that must all evaluate to true for the rule to match.
spec.frontend.proxyProtocol <object>	# Settings for downstream PROXY protocol handling. If configured, incoming connections may require a PROXY header before normal protocol handling. This can also be configured to allow both PROXY and non-PROXY traffic on the same listener.
spec.frontend.proxyProtocol.mode <string> enum=Optional|Strict default="Strict"	# Whether PROXY headers are required or optional. If unset, this defaults to `Strict`.
spec.frontend.proxyProtocol.version <string> enum=All|V1|V2 default="V2"	# PROXY protocol version to accept. If unset, this defaults to `V2`.
spec.frontend.tcp <object>	# Settings for managing incoming TCP connections.
spec.frontend.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.frontend.tcp.keepalive.interval <string>	# Time between keepalive probes. If unset, this defaults to 180s.
spec.frontend.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.frontend.tcp.keepalive.time <string>	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.frontend.tls <object>	# Settings for managing incoming TLS connections.
spec.frontend.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.frontend.tls.cipherSuites <[]string>	# Cipher suites for a TLS listener. The value is a comma-separated list of cipher suites, for example `TLS13_AES_256_GCM_SHA384,TLS13_AES_128_GCM_SHA256`. Use this in the TLS options field of a TLS listener.
spec.frontend.tls.handshakeTimeout <string>	# Deadline for a TLS handshake to complete. If unset, this defaults to `15s`.
spec.frontend.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS listener. For example: `X25519_MLKEM768,X25519`.
spec.frontend.tls.maxProtocolVersion <string> enum=1.2|1.3	# Maximum TLS version to support.
spec.frontend.tls.minProtocolVersion <string> enum=1.2|1.3	# Minimum TLS version to support.
spec.frontend.tracing <object>	# OpenTelemetry tracing settings.
spec.frontend.tracing.attributes <object>	# Customizations to the key-value pairs that are included in the trace.
spec.frontend.tracing.attributes.add <[]object> min=1	# Additional key-value pairs to add to each entry. The value is a CEL expression. If the CEL expression fails to evaluate, the pair will be excluded.
spec.frontend.tracing.attributes.add[].expression <string> (required) min=1 max=16384	# A Common Expression Language (CEL) expression.
spec.frontend.tracing.attributes.add[].name <string> (required) min=1 max=256
spec.frontend.tracing.attributes.remove <[]string> min=1 max=32	# Default fields to remove. For example, `http.method`.
spec.frontend.tracing.backendRef <object> (required)	# OTLP server to reach. Supported types: `Service` and `AgentgatewayBackend`.
spec.frontend.tracing.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.frontend.tracing.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.frontend.tracing.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.frontend.tracing.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.frontend.tracing.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.frontend.tracing.clientSampling <string> min=1 max=16384	# Expression that determines the amount of client sampling. Client sampling determines whether to initiate a new trace span if the incoming request does have a trace already. This should evaluate to a float between `0.0` and `1.0`, or a boolean (`true` or `false`). If unspecified, client sampling is `100%` enabled.
spec.frontend.tracing.path <string> min=1 max=1024	# OTLP path to use. This is only applicable when `protocol` is `HTTP`. If unset, this defaults to `/v1/traces`.
spec.frontend.tracing.protocol <string> enum=GRPC|HTTP default="GRPC"	# OTLP protocol variant to use.
spec.frontend.tracing.randomSampling <string> min=1 max=16384	# Expression that determines the amount of random sampling. Random sampling will initiate a new trace span if the incoming request does not have a trace initiated already. This should evaluate to a float between `0.0` and `1.0`, or a boolean (`true` or `false`). If unspecified, random sampling is disabled.
spec.frontend.tracing.resources <[]object>	# Entity producing telemetry and resources resources to be included in the trace.
spec.frontend.tracing.resources[].expression <string> (required) min=1 max=16384	# A Common Expression Language (CEL) expression.
spec.frontend.tracing.resources[].name <string> (required) min=1 max=256
spec.strategy <object>	# Policy merge and conflict resolution strategy. Strategy settings apply to the policy object as a whole. Individual strategy fields may only be valid for specific policy kinds; for example, inheritance is only valid when this policy contains traffic settings.
spec.strategy.inheritance <string> enum=Default|Override	# Controls whether less-specific traffic policies prevent more-specific traffic policies from contributing to the effective policy. This field is only valid on traffic policies. Frontend and backend policy merging does not use inheritance. When unset or set to `Default`, traffic policy fields are merged by specificity, with more-specific attachment points such as routes and route rules able to override fields from less-specific attachment points such as gateways and listeners. In other words, this policy provides `Default`s that can be overridden. For example, you may provide a `Default` timeout policy for the entire Gateway that is overridden by specific routes. When set to `Override`, this policy blocks traffic policies at more-specific attachment points from being included in the effective policy. This is useful when a gateway-level policy must remain authoritative for all routes below it.
spec.targetRefs <[]object> min=1 max=16	# Target resources to attach the policy to.
spec.targetRefs[].group <string> (required) pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# The API group of the target resource. For Kubernetes Gateway API resources, the group is `gateway.networking.k8s.io`.
spec.targetRefs[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# The API kind of the target resource, such as `Gateway` or `HTTPRoute`.
spec.targetRefs[].name <string> (required) min=1 max=253	# The name of the target resource.
spec.targetRefs[].sectionName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The named section of the target resource.
spec.targetSelectors <[]object> min=1 max=16	# Target selectors used to select resources to attach the policy to.
spec.targetSelectors[].group <string> (required) pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# The API group of the target resource. For Kubernetes Gateway API resources, the group is `gateway.networking.k8s.io`.
spec.targetSelectors[].kind <string> (required) pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# The API kind of the target resource, such as `Gateway` or `HTTPRoute`.
spec.targetSelectors[].matchLabels <map[string]string> (required)	# Labels that must be present on each selected target resource.
spec.targetSelectors[].sectionName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# The named section of each selected target resource.
spec.traffic <object>	# Settings for how to process traffic. A traffic policy can target a `Gateway` (optionally, with a `sectionName` indicating the listener), `ListenerSet`, or `Route` (optionally, with a `sectionName` indicating the route rule). When multiple policies are selected for a given request, they are merged on a field-level basis, but not a deep merge. Precedence is given to more precise policies: `Gateway` < `Listener` < `Route` < `Route Rule`. For example, policy A sets `timeouts` and `retries`, and policy B sets `retries`; the effective policy would be `timeouts` from policy A, and `retries` from policy B.
spec.traffic.apiKeyAuthentication <object>	# Authenticates users based on a configured API key.
spec.traffic.apiKeyAuthentication.location <object>	# Where API keys are read from. If omitted, credentials are read from the `Authorization` header with the `Bearer ` prefix.
spec.traffic.apiKeyAuthentication.location.cookie <object>
spec.traffic.apiKeyAuthentication.location.cookie.name <string> (required) min=1 max=256
spec.traffic.apiKeyAuthentication.location.expression <string> min=1 max=16384	# CEL expression that extracts the credential from the request.
spec.traffic.apiKeyAuthentication.location.header <object>
spec.traffic.apiKeyAuthentication.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.traffic.apiKeyAuthentication.location.header.prefix <string> min=1 max=256
spec.traffic.apiKeyAuthentication.location.queryParameter <object>
spec.traffic.apiKeyAuthentication.location.queryParameter.name <string> (required) min=1 max=256
spec.traffic.apiKeyAuthentication.mode <string> enum=Optional|Permissive|Strict default="Strict"	# Validation mode for API key authentication.
spec.traffic.apiKeyAuthentication.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing a set of API keys. If there are many Secret-backed keys, `secretSelector` can be used instead. Each entry in the credential data represents one API key. The key is an arbitrary identifier. The value can either be: * A string representing the API key. * A JSON object with two fields, `key` and `metadata`. `key` contains the API key. `metadata` contains arbitrary JSON metadata associated with the key, which may be used by other policies. For example, you may write an authorization policy allowing `apiKey.group == 'sales'`. Example: apiVersion: v1 kind: Secret metadata: name: api-key stringData: client1: | { "key": "k-123", "metadata": { "group": "sales", "created_at": "2024-10-01T12:00:00Z" } } client2: "k-456"
spec.traffic.apiKeyAuthentication.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.traffic.apiKeyAuthentication.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.traffic.apiKeyAuthentication.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.traffic.apiKeyAuthentication.secretSelector <object>	# Selects multiple Kubernetes `Secret` resources containing API keys. It is Secret-only; use `secretRef` for other credential kinds. If the same key is defined in multiple secrets, the behavior is undefined. Each entry in the `Secret` data represents one API key. The key is an arbitrary identifier. The value can either be: * A string representing the API key. * A JSON object with two fields, `key` and `metadata`. `key` contains the API key. `metadata` contains arbitrary JSON metadata associated with the key, which may be used by other policies. For example, you may write an authorization policy allowing `apiKey.group == 'sales'`. Example: apiVersion: v1 kind: Secret metadata: name: api-key stringData: client1: | { "key": "k-123", "metadata": { "group": "sales", "created_at": "2024-10-01T12:00:00Z" } } client2: "k-456"
spec.traffic.apiKeyAuthentication.secretSelector.matchLabels <map[string]string> (required)	# Labels that must be present on each selected Secret.
spec.traffic.authorization <object>	# Access rules based on roles and permissions. If multiple authorization rules are applied across different policies, at the same or different attachment points, all rules are merged.
spec.traffic.authorization.action <string> enum=Allow|Deny|Require default="Allow"	# The effect of this rule when it matches. If unspecified, defaults to `Allow`. `Require` rules are cumulative: all require rules must match.
spec.traffic.authorization.policy <object> (required)	# The authorization rule to evaluate. * `Allow`: any matching allow rule allows the request. * `Require`: every require rule must match for the request to be allowed. * `Deny`: any matching deny rule denies the request. A CEL expression that fails to evaluate does not match. Prefer `Require` for deny-by-default behavior. If at least one `Allow` rule is configured, requests are denied unless at least one allow rule matches.
spec.traffic.authorization.policy.matchExpressions <[]string> (required) min=1 max=256	# CEL expressions that must all evaluate to true for the rule to match.
spec.traffic.basicAuthentication <object>	# Authenticates users based on the `Basic` authentication scheme (RFC 7617), where a username and password are encoded in the request.
spec.traffic.basicAuthentication.location <object>	# Where Basic credentials are read from. If omitted, credentials are read from the `Authorization` header with the `Basic ` prefix.
spec.traffic.basicAuthentication.location.cookie <object>
spec.traffic.basicAuthentication.location.cookie.name <string> (required) min=1 max=256
spec.traffic.basicAuthentication.location.expression <string> min=1 max=16384	# CEL expression that extracts the credential from the request.
spec.traffic.basicAuthentication.location.header <object>
spec.traffic.basicAuthentication.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.traffic.basicAuthentication.location.header.prefix <string> min=1 max=256
spec.traffic.basicAuthentication.location.queryParameter <object>
spec.traffic.basicAuthentication.location.queryParameter.name <string> (required) min=1 max=256
spec.traffic.basicAuthentication.mode <string> enum=Optional|Strict default="Strict"	# Validation mode for basic authentication.
spec.traffic.basicAuthentication.realm <string>	# `realm` value to return in the `WWW-Authenticate` header for failed authentication requests. If unset, `Restricted` will be used.
spec.traffic.basicAuthentication.secretRef <object>	# Credential source, defaulting to a Kubernetes `Secret`, storing the `.htaccess` file. When using the default Secret resolver, the `Secret` must have a key named `.htaccess`, and should contain the complete `.htaccess` file. Note: passwords should be the hash of the password, not the raw password. Use the `htpasswd` or similar commands to generate a hash. MD5, bcrypt, crypt, and SHA-1 are supported. Example: apiVersion: v1 kind: Secret metadata: name: basic-auth stringData: .htaccess: | alice:$apr1$3zSE0Abt$IuETi4l5yO87MuOrbSE4V. bob:$apr1$Ukb5LgRD$EPY2lIfY.A54jzLELNIId/
spec.traffic.basicAuthentication.secretRef.group <string>	# The API group of the referenced credential. Empty selects the core API group.
spec.traffic.basicAuthentication.secretRef.kind <string>	# The kind of the referenced credential. Empty defaults to `Secret`.
spec.traffic.basicAuthentication.secretRef.name <string> (required) min=1 max=253	# The name of the referenced credential.
spec.traffic.basicAuthentication.users <[]string> min=1 max=256	# Inline list of username and password pairs that will be accepted. Each entry represents one line of the `htpasswd` format: https://httpd.apache.org/docs/2.4/programs/htpasswd.html. Note: passwords should be the hash of the password, not the raw password. Use the `htpasswd` or similar commands to generate a hash. MD5, bcrypt, crypt, and SHA-1 are supported. Example: users: - "user1:$apr1$ivPt0D4C$DmRhnewfHRSrb3DQC.WHC." - "user2:$2y$05$r3J4d3VepzFkedkd/q1vI.pBYIpSqjfN0qOARV3ScUHysatnS0cL2"
spec.traffic.buffer <object>	# Buffers request and response bodies. Buffered bodies are accumulated in memory by the proxy until completion before being forwarded. This changes the proxies default behavior, which streams bodies. Warning: large bodies can lead to excessive memory usage in the proxy. Utilize with care, or with strict limits.
spec.traffic.buffer.request <object>	# Request body buffering settings.
spec.traffic.buffer.request.maxBytes <int-or-string>	# Maximum number of bytes to buffer from the request or response body. If unset, defaults to the global proxy setting, which defaults to 2Mi.
spec.traffic.buffer.response <object>	# Response body buffering settings.
spec.traffic.buffer.response.maxBytes <int-or-string>	# Maximum number of bytes to buffer from the request or response body. If unset, defaults to the global proxy setting, which defaults to 2Mi.
spec.traffic.cors <object>	# CORS configuration for the policy.
spec.traffic.cors.allowCredentials <boolean>	# AllowCredentials indicates whether the actual cross-origin request allows to include credentials. When set to true, the gateway will include the `Access-Control-Allow-Credentials` response header with value true (case-sensitive). When set to false or omitted the gateway will omit the header `Access-Control-Allow-Credentials` entirely (this is the standard CORS behavior). Support: Extended
spec.traffic.cors.allowHeaders <[]string> max=64	# AllowHeaders indicates which HTTP request headers are supported for accessing the requested resource. Header names are not case-sensitive. Multiple header names in the value of the `Access-Control-Allow-Headers` response header are separated by a comma (","). When the `allowHeaders` field is configured with one or more headers, the gateway must return the `Access-Control-Allow-Headers` response header which value is present in the `allowHeaders` field. If any header name in the `Access-Control-Request-Headers` request header is not included in the list of header names specified by the response header `Access-Control-Allow-Headers`, it will present an error on the client side. If any header name in the `Access-Control-Allow-Headers` response header does not recognize by the client, it will also occur an error on the client side. A wildcard indicates that the requests with all HTTP headers are allowed. If the configuration contains the wildcard `*` in `allowHeaders` and `allowCredentials` is set to `false`, the `Access-Control-Allow-Headers` response header may either contain the wildcard `*` or echo the value of the `Access-Control-Request-Headers` request header. If the configuration contains the wildcard `*` in `allowHeaders` and `allowCredentials` is set to `true`, the gateway must not return `*` in the `Access-Control-Allow-Headers` response header. Instead, it must return one or more header names matching the value of the `Access-Control-Request-Headers` request header. If the `Access-Control-Request-Headers` header is not present in the request, the gateway must omit the `Access-Control-Allow-Headers` response header. Support: Extended
spec.traffic.cors.allowMethods <[]string> max=9	# AllowMethods indicates which HTTP methods are supported for accessing the requested resource. Valid values are any method defined by RFC9110, along with the special value `*`, which represents all HTTP methods are allowed. Method names are case-sensitive, so these values are also case-sensitive. (See https://www.rfc-editor.org/rfc/rfc2616#section-5.1.1) Multiple method names in the value of the `Access-Control-Allow-Methods` response header are separated by a comma (","). A CORS-safelisted method is a method that is `GET`, `HEAD`, or `POST`. (See https://fetch.spec.whatwg.org/#cors-safelisted-method) The CORS-safelisted methods are always allowed, regardless of whether they are specified in the `allowMethods` field. When the `allowMethods` field is configured with one or more methods, the gateway must return the `Access-Control-Allow-Methods` response header which value is present in the `allowMethods` field. If the HTTP method of the `Access-Control-Request-Method` request header is not included in the list of methods specified by the response header `Access-Control-Allow-Methods`, it will present an error on the client side. If the configuration contains the wildcard `*` in `allowMethods` and `allowCredentials` is set to `false`, the `Access-Control-Allow-Methods` response header may either contain the wildcard `*` or echo the value of the `Access-Control-Request-Method` request header. If the configuration contains the wildcard `*` in `allowMethods` and `allowCredentials` is set to `true`, the gateway must not return `*` in the `Access-Control-Allow-Methods` response header. Instead, it must return a single HTTP method matching the value of the `Access-Control-Request-Method` request header. If the `Access-Control-Request-Method` header is not present in the request, the gateway must omit the `Access-Control-Allow-Methods` response header. Support: Extended
spec.traffic.cors.allowOrigins <[]string> max=64	# AllowOrigins indicates whether the response can be shared with requested resource from the given `Origin`. The `Origin` consists of a scheme and a host, with an optional port, and takes the form `<scheme>://<host>(:<port>)`. Valid values for scheme are: `http` and `https`. Valid values for port are any integer between 1 and 65535 (the list of available TCP/UDP ports). Note that, if not included, port `80` is assumed for `http` scheme origins, and port `443` is assumed for `https` origins. This may affect origin matching. The host part of the origin may contain the wildcard character `*`. These wildcard characters behave as follows: * `*` is a greedy match to the _left_, including any number of DNS labels to the left of its position. This also means that `*` will include any number of period `.` characters to the left of its position. * A wildcard by itself matches all hosts. An origin value that includes _only_ the `*` character indicates requests from all `Origin`s are allowed. When the `allowOrigins` field is configured with multiple origins, it means the server supports clients from multiple origins. If the request `Origin` matches the configured allowed origins, the gateway must return the given `Origin` and sets value of the header `Access-Control-Allow-Origin` same as the `Origin` header provided by the client. The status code of a successful response to a "preflight" request is always an OK status (i.e., 204 or 200). If the request `Origin` does not match the configured allowed origins, the gateway returns 204/200 response but doesn't set the relevant cross-origin response headers. Alternatively, the gateway responds with 403 status to the "preflight" request is denied, coupled with omitting the CORS headers. The cross-origin request fails on the client side. Therefore, the client doesn't attempt the actual cross-origin request. Conversely, if the request `Origin` matches one of the configured allowed origins, the gateway sets the response header `Access-Control-Allow-Origin` to the same value as the `Origin` header provided by the client. If the configuration contains the wildcard `*` in `allowOrigins` and `allowCredentials` is set to `false`, the `Access-Control-Allow-Origin` response header may either contain the wildcard `*` or echo the value of the `Origin` request header. If the configuration contains the wildcard `*` in `allowOrigins` and `allowCredentials` is set to `true`, the gateway must not return `*` in the `Access-Control-Allow-Origin` response header. Instead, it must return a single origin matching the value of the `Origin` request header. Support: Extended
spec.traffic.cors.exposeHeaders <[]string> max=64	# ExposeHeaders indicates which HTTP response headers can be exposed to client-side scripts in response to a cross-origin request. A CORS-safelisted response header is an HTTP header in a CORS response that it is considered safe to expose to the client scripts. The CORS-safelisted response headers include the following headers: `Cache-Control` `Content-Language` `Content-Length` `Content-Type` `Expires` `Last-Modified` `Pragma` (See https://fetch.spec.whatwg.org/#cors-safelisted-response-header-name) The CORS-safelisted response headers are exposed to client by default. When an HTTP header name is specified using the `exposeHeaders` field, this additional header will be exposed as part of the response to the client. Header names are not case-sensitive. Multiple header names in the value of the `Access-Control-Expose-Headers` response header are separated by a comma (","). A wildcard indicates that the responses with all HTTP headers are exposed to clients. If the configuration contains the wildcard `*` in `exposeHeaders` and `allowCredentials` is set to `false`, the `Access-Control-Expose-Headers` response header can contain the wildcard `*`. If the configuration contains the wildcard `*` in `exposeHeaders` and `allowCredentials` is set to `true`, the gateway cannot use the `*` in the `Access-Control-Expose-Headers` response header. Support: Extended
spec.traffic.cors.maxAge <integer> default=5 format=int32 min=1	# MaxAge indicates the duration (in seconds) for the client to cache the results of a "preflight" request. The information provided by the `Access-Control-Allow-Methods` and `Access-Control-Allow-Headers` response headers can be cached by the client until the time specified by `Access-Control-Max-Age` elapses. The default value of `Access-Control-Max-Age` response header is 5 (seconds). When the `MaxAge` field is unspecified, the gateway sets the response header "Access-Control-Max-Age: 5" by default.
spec.traffic.csrf <object>	# Cross-Site Request Forgery (CSRF) policy for this traffic policy. The CSRF policy has the following behavior: * Safe methods (`GET`, `HEAD`, `OPTIONS`) are automatically allowed. * Requests without `Sec-Fetch-Site` or `Origin` headers are assumed to be same-origin or non-browser requests and are allowed. * Otherwise, the `Sec-Fetch-Site` header is checked, with a fallback to comparing the `Origin` header to the `Host` header.
spec.traffic.csrf.additionalOrigins <[]string> min=1 max=16	# Additional source origins that will be allowed in addition to the destination origin. The `Origin` consists of a scheme and a host, with an optional port, and takes the form `<scheme>://<host>(:<port>)`.
spec.traffic.directResponse <object>	# Sends a direct response to the client.
spec.traffic.directResponse.body <string> min=1 max=4096	# Content to return in the HTTP response body. The maximum length of the body is restricted to prevent excessively large responses. If this field is omitted, no body is included in the response.
spec.traffic.directResponse.bodyExpression <string> min=1 max=16384	# CEL expression that produces the HTTP response body. Strings and bytes are written directly; other values are serialized as JSON. If this field is omitted, no expression body is included in the response.
spec.traffic.directResponse.conditional <[]object> min=1 max=16	# Conditional policy execution. Set this or the top-level directResponse fields. The first matching policy will be executed. A single policy may be provided without a condition set; if so, it must be the last policy and will be the fallback in case no conditions are met.
spec.traffic.directResponse.conditional[].condition <string> min=1 max=16384	# CEL expression that must evaluate to true for this policy to execute.
spec.traffic.directResponse.conditional[].policy <object> (required)	# Policy to apply when the condition matches.
spec.traffic.directResponse.conditional[].policy.body <string> min=1 max=4096	# Content to return in the HTTP response body. The maximum length of the body is restricted to prevent excessively large responses. If this field is omitted, no body is included in the response.
spec.traffic.directResponse.conditional[].policy.bodyExpression <string> min=1 max=16384	# CEL expression that produces the HTTP response body. Strings and bytes are written directly; other values are serialized as JSON. If this field is omitted, no expression body is included in the response.
spec.traffic.directResponse.conditional[].policy.headers <[]object> min=1 max=16	# Response headers to set on the direct response.
spec.traffic.directResponse.conditional[].policy.headers[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to set.
spec.traffic.directResponse.conditional[].policy.headers[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.directResponse.conditional[].policy.status <integer> format=int32 min=200 max=599	# HTTP status code to return.
spec.traffic.directResponse.headers <[]object> min=1 max=16	# Response headers to set on the direct response.
spec.traffic.directResponse.headers[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to set.
spec.traffic.directResponse.headers[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.directResponse.status <integer> format=int32 min=200 max=599	# HTTP status code to return.
spec.traffic.extAuth <object>	# External authentication configuration for the policy. This selects the external server to send requests to for authentication. An extAuth policy can be conditionally set by nesting configuration under the `conditional` field.
spec.traffic.extAuth.backendRef <object>	# External Authorization server to reach. Supported types: `Service` and `Backend`.
spec.traffic.extAuth.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.extAuth.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.extAuth.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.extAuth.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.extAuth.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.extAuth.cache <object>	# Caches gRPC authorization results. WARNING: the safety of this feature depends on the cache key accurately capturing every request property that the authorization service uses to make a decision. For example, if the service returns different results based on both path and authorization header, both must be included in `key`; otherwise, one request may incorrectly reuse another request's authorization result. If any key expression fails to evaluate or produces an unsupported value, the request is still sent to the authorization service, but its result is not read from or written to the cache.
spec.traffic.extAuth.cache.key <[]string> (required) min=1 max=16	# Ordered list of CEL expressions evaluated against the request to construct the cache key.
spec.traffic.extAuth.cache.maxEntries <integer> format=int32 min=1	# Maximum number of authorization results to keep in the cache. If unset, this defaults to 10000.
spec.traffic.extAuth.cache.ttl <string> (required) min=1 max=16384	# Duration string, such as `5m`, or a CEL expression that returns the duration that cached authorization results may be reused, or a timestamp when the cached authorization result expires. The expression is evaluated after the authorization response has been applied to the request.
spec.traffic.extAuth.conditional <[]object> min=1 max=16	# Conditional policy execution. Set this or the top-level extAuth fields. The first matching policy will be executed. A single policy may be provided without a condition set; if so, it must be the last policy and will be the fallback in case no conditions are met.
spec.traffic.extAuth.conditional[].condition <string> min=1 max=16384	# CEL expression that must evaluate to true for this policy to execute.
spec.traffic.extAuth.conditional[].policy <object> (required)	# Policy to apply when the condition matches.
spec.traffic.extAuth.conditional[].policy.backendRef <object>	# External Authorization server to reach. Supported types: `Service` and `Backend`.
spec.traffic.extAuth.conditional[].policy.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.extAuth.conditional[].policy.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.extAuth.conditional[].policy.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.extAuth.conditional[].policy.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.extAuth.conditional[].policy.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.extAuth.conditional[].policy.cache <object>	# Caches gRPC authorization results. WARNING: the safety of this feature depends on the cache key accurately capturing every request property that the authorization service uses to make a decision. For example, if the service returns different results based on both path and authorization header, both must be included in `key`; otherwise, one request may incorrectly reuse another request's authorization result. If any key expression fails to evaluate or produces an unsupported value, the request is still sent to the authorization service, but its result is not read from or written to the cache.
spec.traffic.extAuth.conditional[].policy.cache.key <[]string> (required) min=1 max=16	# Ordered list of CEL expressions evaluated against the request to construct the cache key.
spec.traffic.extAuth.conditional[].policy.cache.maxEntries <integer> format=int32 min=1	# Maximum number of authorization results to keep in the cache. If unset, this defaults to 10000.
spec.traffic.extAuth.conditional[].policy.cache.ttl <string> (required) min=1 max=16384	# Duration string, such as `5m`, or a CEL expression that returns the duration that cached authorization results may be reused, or a timestamp when the cached authorization result expires. The expression is evaluated after the authorization response has been applied to the request.
spec.traffic.extAuth.conditional[].policy.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the external authorization service is unavailable or returns an error. "FailOpen" allows the request to continue. "FailClosed" (default) denies the request.
spec.traffic.extAuth.conditional[].policy.forwardBody <object>	# Whether to include the HTTP body in the authorization request. If enabled, the request body will be buffered.
spec.traffic.extAuth.conditional[].policy.forwardBody.maxSize <int-or-string> (required)	# Largest body, in bytes, that will be buffered and sent to the authorization server. If the body size is larger than `maxSize`, then the request will be rejected with a response.
spec.traffic.extAuth.conditional[].policy.grpc <object>	# Uses the gRPC External Authorization [protocol](https://www.envoyproxy.io/docs/envoy/latest/api-v3/service/auth/v3/external_auth.proto) should be used.
spec.traffic.extAuth.conditional[].policy.grpc.contextExtensions <map[string]string>	# Additional arbitrary key-value pairs to send to the authorization server in the `context_extensions` field.
spec.traffic.extAuth.conditional[].policy.grpc.requestMetadata <map[string]string>	# Metadata to send to the authorization server. This maps to the `metadata_context.filter_metadata` field of the request, and allows dynamic CEL expressions. If unset, by default the `envoy.filters.http.jwt_authn` key is set if the JWT policy is used as well, for compatibility.
spec.traffic.extAuth.conditional[].policy.http <object>	# Uses HTTP to connect to the authorization server. The authorization server must return a `200` status code, otherwise the request is considered an authorization failure.
spec.traffic.extAuth.conditional[].policy.http.addRequestHeaders <map[string]string>	# Additional headers to add to the request to the authorization server. While `allowedRequestHeaders` just passes the original headers through, `addRequestHeaders` allows defining custom headers based on CEL expressions.
spec.traffic.extAuth.conditional[].policy.http.allowedRequestHeaders <[]string> max=64	# Additional headers from the client request that will be sent to the authorization server. If unset, the following headers are sent by default: `Authorization`.
spec.traffic.extAuth.conditional[].policy.http.allowedResponseHeaders <[]string> max=64	# Headers from the authorization response that will be copied into the request to the backend.
spec.traffic.extAuth.conditional[].policy.http.path <string> min=1 max=16384	# Path to send to the authorization server. If unset, this defaults to the original request path. This is a CEL expression, which allows customizing the path based on the incoming request. For example, to add a prefix, use `"/prefix/" + request.path`.
spec.traffic.extAuth.conditional[].policy.http.redirect <string> min=1 max=16384	# Optional expression that determines a path to redirect to on authorization failure. This is useful to redirect to a sign-in page.
spec.traffic.extAuth.conditional[].policy.http.responseMetadata <map[string]string>	# Metadata fields to construct from the authorization response. These will be included under the `extauthz` variable in future CEL expressions. Setting this is useful for things like logging usernames, without needing to include them as headers to the backend, as `allowedResponseHeaders` would.
spec.traffic.extAuth.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the external authorization service is unavailable or returns an error. "FailOpen" allows the request to continue. "FailClosed" (default) denies the request.
spec.traffic.extAuth.forwardBody <object>	# Whether to include the HTTP body in the authorization request. If enabled, the request body will be buffered.
spec.traffic.extAuth.forwardBody.maxSize <int-or-string> (required)	# Largest body, in bytes, that will be buffered and sent to the authorization server. If the body size is larger than `maxSize`, then the request will be rejected with a response.
spec.traffic.extAuth.grpc <object>	# Uses the gRPC External Authorization [protocol](https://www.envoyproxy.io/docs/envoy/latest/api-v3/service/auth/v3/external_auth.proto) should be used.
spec.traffic.extAuth.grpc.contextExtensions <map[string]string>	# Additional arbitrary key-value pairs to send to the authorization server in the `context_extensions` field.
spec.traffic.extAuth.grpc.requestMetadata <map[string]string>	# Metadata to send to the authorization server. This maps to the `metadata_context.filter_metadata` field of the request, and allows dynamic CEL expressions. If unset, by default the `envoy.filters.http.jwt_authn` key is set if the JWT policy is used as well, for compatibility.
spec.traffic.extAuth.http <object>	# Uses HTTP to connect to the authorization server. The authorization server must return a `200` status code, otherwise the request is considered an authorization failure.
spec.traffic.extAuth.http.addRequestHeaders <map[string]string>	# Additional headers to add to the request to the authorization server. While `allowedRequestHeaders` just passes the original headers through, `addRequestHeaders` allows defining custom headers based on CEL expressions.
spec.traffic.extAuth.http.allowedRequestHeaders <[]string> max=64	# Additional headers from the client request that will be sent to the authorization server. If unset, the following headers are sent by default: `Authorization`.
spec.traffic.extAuth.http.allowedResponseHeaders <[]string> max=64	# Headers from the authorization response that will be copied into the request to the backend.
spec.traffic.extAuth.http.path <string> min=1 max=16384	# Path to send to the authorization server. If unset, this defaults to the original request path. This is a CEL expression, which allows customizing the path based on the incoming request. For example, to add a prefix, use `"/prefix/" + request.path`.
spec.traffic.extAuth.http.redirect <string> min=1 max=16384	# Optional expression that determines a path to redirect to on authorization failure. This is useful to redirect to a sign-in page.
spec.traffic.extAuth.http.responseMetadata <map[string]string>	# Metadata fields to construct from the authorization response. These will be included under the `extauthz` variable in future CEL expressions. Setting this is useful for things like logging usernames, without needing to include them as headers to the backend, as `allowedResponseHeaders` would.
spec.traffic.extProc <object>	# External processing configuration for the policy.
spec.traffic.extProc.backendRef <object>	# External Processor server to reach. Supported types: `Service` and `Backend`.
spec.traffic.extProc.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.extProc.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.extProc.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.extProc.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.extProc.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.extProc.conditional <[]object> min=1 max=16	# Conditional policy execution. Set this or the top-level extProc fields. The first matching policy will be executed. A single policy may be provided without a condition set; if so, it must be the last policy and will be the fallback in case no conditions are met.
spec.traffic.extProc.conditional[].condition <string> min=1 max=16384	# CEL expression that must evaluate to true for this policy to execute.
spec.traffic.extProc.conditional[].policy <object> (required)	# Policy to apply when the condition matches.
spec.traffic.extProc.conditional[].policy.backendRef <object>	# External Processor server to reach. Supported types: `Service` and `Backend`.
spec.traffic.extProc.conditional[].policy.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.extProc.conditional[].policy.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.extProc.conditional[].policy.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.extProc.conditional[].policy.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.extProc.conditional[].policy.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.extProc.conditional[].policy.processingOptions <object>	# How request and response phases are sent to ext_proc.
spec.traffic.extProc.conditional[].policy.processingOptions.allowModeOverride <boolean> default=false	# Allows ext_proc `mode_override` values from matching header responses to update subsequent request/response processing phases for this exchange. Defaults to `false`.
spec.traffic.extProc.conditional[].policy.processingOptions.requestBodyMode <string> enum=None|Buffered|BufferedPartial|FullDuplexStreamed default="FullDuplexStreamed"	# How request bodies are sent to the external processor. `Buffered` buffers the full body and returns an error if it exceeds 8KB. `BufferedPartial` buffers up to 8KB and sends the buffered prefix if the body exceeds that limit. Defaults to `FullDuplexStreamed`.
spec.traffic.extProc.conditional[].policy.processingOptions.requestHeaderMode <string> enum=Send|Skip default="Send"	# Whether request headers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.conditional[].policy.processingOptions.requestTrailerMode <string> enum=Skip|Send default="Send"	# Whether request trailers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.conditional[].policy.processingOptions.responseBodyMode <string> enum=None|Buffered|BufferedPartial|FullDuplexStreamed default="FullDuplexStreamed"	# How response bodies are sent to the external processor. `Buffered` buffers the full body and returns an error if it exceeds 8KB. `BufferedPartial` buffers up to 8KB and sends the buffered prefix if the body exceeds that limit. Defaults to `FullDuplexStreamed`.
spec.traffic.extProc.conditional[].policy.processingOptions.responseHeaderMode <string> enum=Send|Skip default="Send"	# Whether response headers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.conditional[].policy.processingOptions.responseTrailerMode <string> enum=Skip|Send default="Send"	# Whether response trailers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.processingOptions <object>	# How request and response phases are sent to ext_proc.
spec.traffic.extProc.processingOptions.allowModeOverride <boolean> default=false	# Allows ext_proc `mode_override` values from matching header responses to update subsequent request/response processing phases for this exchange. Defaults to `false`.
spec.traffic.extProc.processingOptions.requestBodyMode <string> enum=None|Buffered|BufferedPartial|FullDuplexStreamed default="FullDuplexStreamed"	# How request bodies are sent to the external processor. `Buffered` buffers the full body and returns an error if it exceeds 8KB. `BufferedPartial` buffers up to 8KB and sends the buffered prefix if the body exceeds that limit. Defaults to `FullDuplexStreamed`.
spec.traffic.extProc.processingOptions.requestHeaderMode <string> enum=Send|Skip default="Send"	# Whether request headers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.processingOptions.requestTrailerMode <string> enum=Skip|Send default="Send"	# Whether request trailers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.processingOptions.responseBodyMode <string> enum=None|Buffered|BufferedPartial|FullDuplexStreamed default="FullDuplexStreamed"	# How response bodies are sent to the external processor. `Buffered` buffers the full body and returns an error if it exceeds 8KB. `BufferedPartial` buffers up to 8KB and sends the buffered prefix if the body exceeds that limit. Defaults to `FullDuplexStreamed`.
spec.traffic.extProc.processingOptions.responseHeaderMode <string> enum=Send|Skip default="Send"	# Whether response headers are sent to the external processor. Defaults to `Send`.
spec.traffic.extProc.processingOptions.responseTrailerMode <string> enum=Skip|Send default="Send"	# Whether response trailers are sent to the external processor. Defaults to `Send`.
spec.traffic.headerModifiers <object>	# Request and response header modification policy.
spec.traffic.headerModifiers.request <object>	# Header changes to apply before forwarding a request.
spec.traffic.headerModifiers.request.add <[]object> max=16	# Add adds the given header(s) (name, value) to the request before the action. It appends to any existing values associated with the header name. Input: GET /foo HTTP/1.1 my-header: foo Config: add: - name: "my-header" value: "bar,baz" Output: GET /foo HTTP/1.1 my-header: foo,bar,baz
spec.traffic.headerModifiers.request.add[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent.
spec.traffic.headerModifiers.request.add[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.traffic.headerModifiers.request.remove <[]string> max=16	# Remove the given header(s) from the HTTP request before the action. The value of Remove is a list of HTTP header names. Note that the header names are case-insensitive (see https://datatracker.ietf.org/doc/html/rfc2616#section-4.2). Input: GET /foo HTTP/1.1 my-header1: foo my-header2: bar my-header3: baz Config: remove: ["my-header1", "my-header3"] Output: GET /foo HTTP/1.1 my-header2: bar
spec.traffic.headerModifiers.request.set <[]object> max=16	# Set overwrites the request with the given header (name, value) before the action. Input: GET /foo HTTP/1.1 my-header: foo Config: set: - name: "my-header" value: "bar" Output: GET /foo HTTP/1.1 my-header: bar
spec.traffic.headerModifiers.request.set[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent.
spec.traffic.headerModifiers.request.set[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.traffic.headerModifiers.response <object>	# Header changes to apply before returning a response.
spec.traffic.headerModifiers.response.add <[]object> max=16	# Add adds the given header(s) (name, value) to the request before the action. It appends to any existing values associated with the header name. Input: GET /foo HTTP/1.1 my-header: foo Config: add: - name: "my-header" value: "bar,baz" Output: GET /foo HTTP/1.1 my-header: foo,bar,baz
spec.traffic.headerModifiers.response.add[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent.
spec.traffic.headerModifiers.response.add[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.traffic.headerModifiers.response.remove <[]string> max=16	# Remove the given header(s) from the HTTP request before the action. The value of Remove is a list of HTTP header names. Note that the header names are case-insensitive (see https://datatracker.ietf.org/doc/html/rfc2616#section-4.2). Input: GET /foo HTTP/1.1 my-header1: foo my-header2: bar my-header3: baz Config: remove: ["my-header1", "my-header3"] Output: GET /foo HTTP/1.1 my-header2: bar
spec.traffic.headerModifiers.response.set <[]object> max=16	# Set overwrites the request with the given header (name, value) before the action. Input: GET /foo HTTP/1.1 my-header: foo Config: set: - name: "my-header" value: "bar" Output: GET /foo HTTP/1.1 my-header: bar
spec.traffic.headerModifiers.response.set[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name is the name of the HTTP Header to be matched. Name matching MUST be case-insensitive. (See https://tools.ietf.org/html/rfc7230#section-3.2). If multiple entries specify equivalent header names, the first entry with an equivalent name MUST be considered for a match. Subsequent entries with an equivalent header name MUST be ignored. Due to the case-insensitivity of header names, "foo" and "Foo" are considered equivalent.
spec.traffic.headerModifiers.response.set[].value <string> (required) min=1 max=4096	# Value is the value of HTTP Header to be matched. <gateway:experimental:description> Must consist of printable US-ASCII characters, optionally separated by single tabs or spaces. See: https://tools.ietf.org/html/rfc7230#section-3.2 </gateway:experimental:description> <gateway:experimental:validation:Pattern=`^[!-~]+([\t ]?[!-~]+)*$`>
spec.traffic.hostRewrite <object>	# How to rewrite the `Host` header for requests. If the `HTTPRoute` `urlRewrite` filter already specifies a host rewrite, this setting is ignored.
spec.traffic.hostRewrite.mode <string> (required) enum=Auto|None	# Hostname rewrite mode. The following may be specified: * `Auto`: automatically set the `Host` header based on the destination. * `None`: do not rewrite the `Host` header. The original `Host` header will be passed through. This setting defaults to `Auto` when connecting to hostname-based `Backend` types, and `None` otherwise, for `Service` or IP-based backends.
spec.traffic.jwtAuthentication <object>	# Authenticates users based on JWT tokens.
spec.traffic.jwtAuthentication.location <object>	# Where JWT credentials are read from. If omitted, credentials are read from the `Authorization` header with the `Bearer ` prefix.
spec.traffic.jwtAuthentication.location.cookie <object>
spec.traffic.jwtAuthentication.location.cookie.name <string> (required) min=1 max=256
spec.traffic.jwtAuthentication.location.expression <string> min=1 max=16384	# CEL expression that extracts the credential from the request.
spec.traffic.jwtAuthentication.location.header <object>
spec.traffic.jwtAuthentication.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# HTTPHeaderName is the name of an HTTP header. Valid values include: * "Authorization" * "Set-Cookie" Invalid values include: - ":method" - ":" is an invalid character. This means that HTTP/2 pseudo headers are not currently supported by this type. - "/invalid" - "/ " is an invalid character
spec.traffic.jwtAuthentication.location.header.prefix <string> min=1 max=256
spec.traffic.jwtAuthentication.location.queryParameter <object>
spec.traffic.jwtAuthentication.location.queryParameter.name <string> (required) min=1 max=256
spec.traffic.jwtAuthentication.mcp <object>	# Enables MCP OAuth metadata endpoint handling and MCP-specific authentication behavior on top of standard JWT validation. When set, the gateway will serve the MCP OAuth metadata discovery endpoints.
spec.traffic.jwtAuthentication.mcp.clientId <string>	# Client ID to use for short-circuiting Dynamic Client Registration. If set, the gateway will not proxy registration requests to the IDP and instead return this client ID.
spec.traffic.jwtAuthentication.mcp.provider <string> enum=Auth0|Keycloak|Okta	# Identity provider to use for MCP authentication flows.
spec.traffic.jwtAuthentication.mcp.resourceMetadata <object (free-form)>	# Metadata to use for MCP resources, served at the MCP OAuth metadata endpoints.
spec.traffic.jwtAuthentication.mode <string> enum=Optional|Permissive|Strict default="Strict"	# Validation mode for JWT authentication.
spec.traffic.jwtAuthentication.providers <[]object> (required) min=1 max=64
spec.traffic.jwtAuthentication.providers[].audiences <[]string> min=1 max=64	# Allowed audiences that are allowed access. This corresponds to the `aud` claim ([RFC 7519 §4.1.3](https://datatracker.ietf.org/doc/html/rfc7519#section-4.1.3)). If unset, any audience is allowed.
spec.traffic.jwtAuthentication.providers[].issuer <string> (required) min=1 max=256	# IdP that issued the JWT. This corresponds to the `iss` claim ([RFC 7519 §4.1.1](https://tools.ietf.org/html/rfc7519#section-4.1.1)).
spec.traffic.jwtAuthentication.providers[].jwks <object> (required)	# JSON Web Key Set used to validate the signature of the JWT.
spec.traffic.jwtAuthentication.providers[].jwks.inline <string> min=2 max=65536	# Inline JSON Web Key Set used to validate the signature of the JWT.
spec.traffic.jwtAuthentication.providers[].jwks.remote <object>	# How to reach the JSON Web Key Set from a remote address.
spec.traffic.jwtAuthentication.providers[].jwks.remote.backendRef <object> (required)	# Remote JWKS server to reach. Supported types are `Service` and static `Backend`. An `AgentgatewayPolicy` containing backend TLS config can then be attached to the `Service` or `Backend` in order to set TLS options for a connection to the remote `jwks` source.
spec.traffic.jwtAuthentication.providers[].jwks.remote.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.jwtAuthentication.providers[].jwks.remote.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.jwtAuthentication.providers[].jwks.remote.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.jwtAuthentication.providers[].jwks.remote.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.jwtAuthentication.providers[].jwks.remote.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.jwtAuthentication.providers[].jwks.remote.cacheDuration <string> default="5m"
spec.traffic.jwtAuthentication.providers[].jwks.remote.jwksPath <string> (required) min=1 max=2000	# Path to the IdP `jwks` endpoint, relative to the root, commonly `".well-known/jwks.json"`.
spec.traffic.phase <string> enum=PostRouting|PreRouting	# The phase to apply the traffic policy to. If the phase is `PreRouting`, the `targetRef` must be a `Gateway` or a `Listener`. `PreRouting` is typically used only when a policy needs to influence the routing decision. Even when using `PostRouting` mode, the policy can target the `Gateway` or `Listener`. This is a helper for applying the policy to all routes under that `Gateway` or `Listener`, and follows the merging logic described above. Note: `PreRouting` and `PostRouting` rules do not merge together. These are independent execution phases. That is, all `PreRouting` rules will merge and execute, then all `PostRouting` rules will merge and execute. If unset, this defaults to `PostRouting`.
spec.traffic.rateLimit <object>	# Rate limiting configuration for the policy. This limits the rate at which requests are processed.
spec.traffic.rateLimit.conditional <[]object> min=1 max=16	# Conditional policy execution. Set this or the top-level rateLimit fields. The first matching policy will be executed. A single policy may be provided without a condition set; if so, it must be the last policy and will be the fallback in case no conditions are met.
spec.traffic.rateLimit.conditional[].condition <string> min=1 max=16384	# CEL expression that must evaluate to true for this policy to execute.
spec.traffic.rateLimit.conditional[].policy <object> (required)	# Policy to apply when the condition matches.
spec.traffic.rateLimit.conditional[].policy.global <object>	# Global rate limiting policy using an external service.
spec.traffic.rateLimit.conditional[].policy.global.backendRef <object> (required)	# Rate limit server to reach. Supported types: `Service` and `Backend`.
spec.traffic.rateLimit.conditional[].policy.global.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.rateLimit.conditional[].policy.global.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.rateLimit.conditional[].policy.global.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.rateLimit.conditional[].policy.global.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.rateLimit.conditional[].policy.global.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.rateLimit.conditional[].policy.global.descriptors <[]object> (required) min=1 max=16	# Dimensions for rate limiting. These values are passed to the rate limit service which applies configured limits based on them. Each descriptor represents a single rate limit rule with one or more entries.
spec.traffic.rateLimit.conditional[].policy.global.descriptors[].cost <string> min=1 max=16384	# Common Expression Language (`CEL`) expression that determines the cost of the request for this descriptor. If unset, `Requests` costs default to 1, and `Tokens` costs default to the total token count. `Tokens` cost are evaluated after the request has completed. For non-streaming requests, `request`, `llm`, and `response` fields are all available; for streaming requests, `response` is not available (however, all LLM attributes are in `llm`). For `Requests`, cost is computed during the request phase. See https://agentgateway.dev/docs/standalone/latest/reference/cel/ for more info.
spec.traffic.rateLimit.conditional[].policy.global.descriptors[].entries <[]object> (required) min=1 max=16	# Individual components that make up this descriptor.
spec.traffic.rateLimit.conditional[].policy.global.descriptors[].entries[].expression <string> (required) min=1 max=16384	# Common Expression Language (`CEL`) expression that defines the value for the descriptor. For example, to rate limit based on the Client IP: `source.address`. See https://agentgateway.dev/docs/standalone/latest/reference/cel/ for more info.
spec.traffic.rateLimit.conditional[].policy.global.descriptors[].entries[].name <string> (required) min=1 max=64	# Name of the descriptor.
spec.traffic.rateLimit.conditional[].policy.global.descriptors[].unit <string> enum=Requests|Tokens	# Cost unit. If unspecified, `Requests` is used.
spec.traffic.rateLimit.conditional[].policy.global.domain <string> (required) min=1 max=256	# Domain under which this limit should apply. This is an arbitrary string that enables a rate limit server to distinguish between different applications.
spec.traffic.rateLimit.conditional[].policy.global.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the remote rate limit service is unavailable or returns an error. `FailOpen` allows the request to continue. `FailClosed` (default) denies the request.
spec.traffic.rateLimit.conditional[].policy.local <[]object> min=1 max=16	# Local rate limiting policy.
spec.traffic.rateLimit.conditional[].policy.local[].burst <integer> format=int32	# Allowance of requests above the request-per-unit that should be allowed within a short period of time.
spec.traffic.rateLimit.conditional[].policy.local[].requests <integer> format=int32 min=1	# Number of HTTP requests per unit of time that are allowed. Requests exceeding this limit will fail with a `429` error.
spec.traffic.rateLimit.conditional[].policy.local[].tokens <integer> format=int32 min=1	# Number of LLM tokens per unit of time that are allowed. Requests exceeding this limit will fail with a `429` error. Both input and output tokens are counted. However, token counts are not known until the request completes. As a result, token-based rate limits will apply to future requests only.
spec.traffic.rateLimit.conditional[].policy.local[].unit <string> (required) enum=Hours|Minutes|Seconds	# Unit of time for the limit.
spec.traffic.rateLimit.global <object>	# Global rate limiting policy using an external service.
spec.traffic.rateLimit.global.backendRef <object> (required)	# Rate limit server to reach. Supported types: `Service` and `Backend`.
spec.traffic.rateLimit.global.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. For example, "gateway.networking.k8s.io". When unspecified or empty string, core API group is inferred.
spec.traffic.rateLimit.global.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is the Kubernetes resource kind of the referent. For example "Service". Defaults to "Service" when not specified. ExternalName services can refer to CNAME DNS records that may live outside of the cluster and as such are difficult to reason about in terms of conformance. They also may not be safe to forward to (see CVE-2021-25740 for more information). Implementations SHOULD NOT support ExternalName Services. Support: Core (Services with a type other than ExternalName) Support: Implementation-specific (Services with type ExternalName)
spec.traffic.rateLimit.global.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.traffic.rateLimit.global.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the backend. When unspecified, the local namespace is inferred. Note that when a namespace different than the local namespace is specified, a ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details. Support: Core
spec.traffic.rateLimit.global.backendRef.port <integer> format=int32 min=1 max=65535	# Port specifies the destination port number to use for this resource. Port is required when the referent is a Kubernetes Service. In this case, the port number is the service port number, not the target port. For other resources, destination port might be derived from the referent resource or this field.
spec.traffic.rateLimit.global.descriptors <[]object> (required) min=1 max=16	# Dimensions for rate limiting. These values are passed to the rate limit service which applies configured limits based on them. Each descriptor represents a single rate limit rule with one or more entries.
spec.traffic.rateLimit.global.descriptors[].cost <string> min=1 max=16384	# Common Expression Language (`CEL`) expression that determines the cost of the request for this descriptor. If unset, `Requests` costs default to 1, and `Tokens` costs default to the total token count. `Tokens` cost are evaluated after the request has completed. For non-streaming requests, `request`, `llm`, and `response` fields are all available; for streaming requests, `response` is not available (however, all LLM attributes are in `llm`). For `Requests`, cost is computed during the request phase. See https://agentgateway.dev/docs/standalone/latest/reference/cel/ for more info.
spec.traffic.rateLimit.global.descriptors[].entries <[]object> (required) min=1 max=16	# Individual components that make up this descriptor.
spec.traffic.rateLimit.global.descriptors[].entries[].expression <string> (required) min=1 max=16384	# Common Expression Language (`CEL`) expression that defines the value for the descriptor. For example, to rate limit based on the Client IP: `source.address`. See https://agentgateway.dev/docs/standalone/latest/reference/cel/ for more info.
spec.traffic.rateLimit.global.descriptors[].entries[].name <string> (required) min=1 max=64	# Name of the descriptor.
spec.traffic.rateLimit.global.descriptors[].unit <string> enum=Requests|Tokens	# Cost unit. If unspecified, `Requests` is used.
spec.traffic.rateLimit.global.domain <string> (required) min=1 max=256	# Domain under which this limit should apply. This is an arbitrary string that enables a rate limit server to distinguish between different applications.
spec.traffic.rateLimit.global.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the remote rate limit service is unavailable or returns an error. `FailOpen` allows the request to continue. `FailClosed` (default) denies the request.
spec.traffic.rateLimit.local <[]object> min=1 max=16	# Local rate limiting policy.
spec.traffic.rateLimit.local[].burst <integer> format=int32	# Allowance of requests above the request-per-unit that should be allowed within a short period of time.
spec.traffic.rateLimit.local[].requests <integer> format=int32 min=1	# Number of HTTP requests per unit of time that are allowed. Requests exceeding this limit will fail with a `429` error.
spec.traffic.rateLimit.local[].tokens <integer> format=int32 min=1	# Number of LLM tokens per unit of time that are allowed. Requests exceeding this limit will fail with a `429` error. Both input and output tokens are counted. However, token counts are not known until the request completes. As a result, token-based rate limits will apply to future requests only.
spec.traffic.rateLimit.local[].unit <string> (required) enum=Hours|Minutes|Seconds	# Unit of time for the limit.
spec.traffic.retry <object>	# Retry policy.
spec.traffic.retry.attempts <integer> min=1	# Attempts specifies the maximum number of times an individual request from the gateway to a backend should be retried. If the maximum number of retries has been attempted without a successful response from the backend, the Gateway MUST return an error. When this field is unspecified, the number of times to attempt to retry a backend request is implementation-specific. Support: Extended
spec.traffic.retry.backoff <string> pattern="^([0-9]{1,5}(h|m|s|ms)){1,4}$"	# Backoff specifies the minimum duration a Gateway should wait between retry attempts and is represented in Gateway API Duration formatting. For example, setting the `rules[].retry.backoff` field to the value `100ms` will cause a backend request to first be retried approximately 100 milliseconds after timing out or receiving a response code configured to be retriable. An implementation MAY use an exponential or alternative backoff strategy for subsequent retry attempts, MAY cap the maximum backoff duration to some amount greater than the specified minimum, and MAY add arbitrary jitter to stagger requests, as long as unsuccessful backend requests are not retried before the configured minimum duration. If a Request timeout (`rules[].timeouts.request`) is configured on the route, the entire duration of the initial request and any retry attempts MUST not exceed the Request timeout duration. If any retry attempts are still in progress when the Request timeout duration has been reached, these SHOULD be canceled if possible and the Gateway MUST immediately return a timeout error. If a BackendRequest timeout (`rules[].timeouts.backendRequest`) is configured on the route, any retry attempts which reach the configured BackendRequest timeout duration without a response SHOULD be canceled if possible and the Gateway should wait for at least the specified backoff duration before attempting to retry the backend request again. If a BackendRequest timeout is _not_ configured on the route, retry attempts MAY time out after an implementation default duration, or MAY remain pending until a configured Request timeout or implementation default duration for total request time is reached. When this field is unspecified, the time to wait between retry attempts is implementation-specific. Support: Extended
spec.traffic.retry.codes <[]integer>	# Codes defines the HTTP response status codes for which a backend request should be retried. Support: Extended
spec.traffic.retry.condition <string> min=1 max=16384	# `condition` is a CEL expression evaluated against each response to decide whether to retry. A response is retried when its status code is in `codes` or this expression evaluates to `true`.
spec.traffic.retry.precondition <string> min=1 max=16384	# `precondition` is a CEL expression evaluated against the request before any attempt is made. When it evaluates to `false`, retries are disabled and only the initial attempt is made, for example `request.method == "GET"`. Retrying requires buffering the request body in memory for replay, so this lets us skip that cost when the request is known to be non-retriable (for example streaming uploads or long-lived connections like websockets).
spec.traffic.timeouts <object>	# Request timeouts. It is applicable to `HTTPRoute` resources and ignored for other targeted kinds.
spec.traffic.timeouts.request <string>	# Timeout for an individual request from the gateway to a backend. This covers the time from when the request first starts being sent from the gateway to when the full response has been received from the backend.
spec.traffic.transformation <object>	# Mutates and transforms requests and responses before forwarding them to the destination.
spec.traffic.transformation.conditional <[]object> min=1 max=16	# Conditional policy execution. Set this or the top-level transformation fields. The first matching policy will be executed. A single policy may be provided without a condition set; if so, it must be the last policy and will be the fallback in case no conditions are met.
spec.traffic.transformation.conditional[].condition <string> min=1 max=16384	# CEL expression that must evaluate to true for this policy to execute.
spec.traffic.transformation.conditional[].policy <object> (required)	# Policy to apply when the condition matches.
spec.traffic.transformation.conditional[].policy.request <object>	# Request transformation settings.
spec.traffic.transformation.conditional[].policy.request.add <[]object> min=1 max=16	# Headers to add to the request and what each value should be set to. If there is already a header with these values then append the value as an extra entry.
spec.traffic.transformation.conditional[].policy.request.add[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.conditional[].policy.request.add[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.conditional[].policy.request.body <string> min=1 max=16384	# HTTP body transformation.
spec.traffic.transformation.conditional[].policy.request.metadata <map[string]string>	# Stores CEL-evaluated values under the `metadata` CEL variable for subsequent policy evaluations. `metadata` is evaluated before header or body transformations.
spec.traffic.transformation.conditional[].policy.request.remove <[]string> min=1 max=16	# Header names to remove from the request or response.
spec.traffic.transformation.conditional[].policy.request.set <[]object> min=1 max=16	# Headers to set and the values to use.
spec.traffic.transformation.conditional[].policy.request.set[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.conditional[].policy.request.set[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.conditional[].policy.response <object>	# Response transformation settings.
spec.traffic.transformation.conditional[].policy.response.add <[]object> min=1 max=16	# Headers to add to the request and what each value should be set to. If there is already a header with these values then append the value as an extra entry.
spec.traffic.transformation.conditional[].policy.response.add[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.conditional[].policy.response.add[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.conditional[].policy.response.body <string> min=1 max=16384	# HTTP body transformation.
spec.traffic.transformation.conditional[].policy.response.metadata <map[string]string>	# Stores CEL-evaluated values under the `metadata` CEL variable for subsequent policy evaluations. `metadata` is evaluated before header or body transformations.
spec.traffic.transformation.conditional[].policy.response.remove <[]string> min=1 max=16	# Header names to remove from the request or response.
spec.traffic.transformation.conditional[].policy.response.set <[]object> min=1 max=16	# Headers to set and the values to use.
spec.traffic.transformation.conditional[].policy.response.set[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.conditional[].policy.response.set[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.request <object>	# Request transformation settings.
spec.traffic.transformation.request.add <[]object> min=1 max=16	# Headers to add to the request and what each value should be set to. If there is already a header with these values then append the value as an extra entry.
spec.traffic.transformation.request.add[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.request.add[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.request.body <string> min=1 max=16384	# HTTP body transformation.
spec.traffic.transformation.request.metadata <map[string]string>	# Stores CEL-evaluated values under the `metadata` CEL variable for subsequent policy evaluations. `metadata` is evaluated before header or body transformations.
spec.traffic.transformation.request.remove <[]string> min=1 max=16	# Header names to remove from the request or response.
spec.traffic.transformation.request.set <[]object> min=1 max=16	# Headers to set and the values to use.
spec.traffic.transformation.request.set[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.request.set[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.response <object>	# Response transformation settings.
spec.traffic.transformation.response.add <[]object> min=1 max=16	# Headers to add to the request and what each value should be set to. If there is already a header with these values then append the value as an extra entry.
spec.traffic.transformation.response.add[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.response.add[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
spec.traffic.transformation.response.body <string> min=1 max=16384	# HTTP body transformation.
spec.traffic.transformation.response.metadata <map[string]string>	# Stores CEL-evaluated values under the `metadata` CEL variable for subsequent policy evaluations. `metadata` is evaluated before header or body transformations.
spec.traffic.transformation.response.remove <[]string> min=1 max=16	# Header names to remove from the request or response.
spec.traffic.transformation.response.set <[]object> min=1 max=16	# Headers to set and the values to use.
spec.traffic.transformation.response.set[].name <string> (required) pattern="^:?[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# The name of the header to add.
spec.traffic.transformation.response.set[].value <string> (required) min=1 max=16384	# CEL expression that generates the output value for the header.
status <object>	# Current policy status.
status.ancestors <[]object> (required) max=16	# Ancestors is a list of ancestor resources (usually Gateways) that are associated with the policy, and the status of the policy with respect to each ancestor. When this policy attaches to a parent, the controller that manages the parent and the ancestors MUST add an entry to this list when the controller first sees the policy and SHOULD update the entry as appropriate when the relevant ancestor is modified. Note that choosing the relevant ancestor is left to the Policy designers; an important part of Policy design is designing the right object level at which to namespace this status. Note also that implementations MUST ONLY populate ancestor status for the Ancestor resources they are responsible for. Implementations MUST use the ControllerName field to uniquely identify the entries in this list that they are responsible for. Note that to achieve this, the list of PolicyAncestorStatus structs MUST be treated as a map with a composite key, made up of the AncestorRef and ControllerName fields combined. A maximum of 16 ancestors will be represented in this list. An empty list means the Policy is not relevant for any ancestors. If this slice is full, implementations MUST NOT add further entries. Instead they MUST consider the policy unimplementable and signal that on any related resources such as the ancestor that would be referenced here. For example, if this list was full on BackendTLSPolicy, no additional Gateways would be able to reference the Service targeted by the BackendTLSPolicy.
status.ancestors[].ancestorRef <object> (required)	# AncestorRef corresponds with a ParentRef in the spec that this PolicyAncestorStatus struct describes the status of.
status.ancestors[].ancestorRef.group <string> default="gateway.networking.k8s.io" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. When unspecified, "gateway.networking.k8s.io" is inferred. To set the core API group (such as for a "Service" kind referent), Group must be explicitly set to "" (empty string). Support: Core
status.ancestors[].ancestorRef.kind <string> default="Gateway" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is kind of the referent. There are two kinds of parent resources with "Core" support: * Gateway (Gateway conformance profile) * Service (Mesh conformance profile, ClusterIP Services only) Support for other resources is Implementation-Specific.
status.ancestors[].ancestorRef.name <string> (required) min=1 max=253	# Name is the name of the referent. Support: Core
status.ancestors[].ancestorRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace is the namespace of the referent. When unspecified, this refers to the local namespace of the Route. Note that there are specific rules for ParentRefs which cross namespace boundaries. Cross-namespace references are only valid if they are explicitly allowed by something in the namespace they are referring to. For example: Gateway has the AllowedRoutes field, and ReferenceGrant provides a generic way to enable any other kind of cross-namespace reference. <gateway:experimental:description> ParentRefs from a Route to a Service in the same namespace are "producer" routes, which apply default routing rules to inbound connections from any namespace to the Service. ParentRefs from a Route to a Service in a different namespace are "consumer" routes, and these routing rules are only applied to outbound connections originating from the same namespace as the Route, for which the intended destination of the connections are a Service targeted as a ParentRef of the Route. </gateway:experimental:description> Support: Core
status.ancestors[].ancestorRef.port <integer> format=int32 min=1 max=65535	# Port is the network port this Route targets. It can be interpreted differently based on the type of parent resource. When the parent resource is a Gateway, this targets all listeners listening on the specified port that also support this kind of Route(and select this Route). It's not recommended to set `Port` unless the networking behaviors specified in a Route must apply to a specific port as opposed to a listener(s) whose port(s) may be changed. When both Port and SectionName are specified, the name and port of the selected listener must match both specified values. <gateway:experimental:description> When the parent resource is a Service, this targets a specific port in the Service spec. When both Port (experimental) and SectionName are specified, the name and port of the selected port must match both specified values. </gateway:experimental:description> Implementations MAY choose to support other parent resources. Implementations supporting other types of parent resources MUST clearly document how/if Port is interpreted. For the purpose of status, an attachment is considered successful as long as the parent resource accepts it partially. For example, Gateway listeners can restrict which Routes can attach to them by Route kind, namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from the referencing Route, the Route MUST be considered successfully attached. If no Gateway listeners accept attachment from this Route, the Route MUST be considered detached from the Gateway. Support: Extended
status.ancestors[].ancestorRef.sectionName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# SectionName is the name of a section within the target resource. In the following resources, SectionName is interpreted as the following: * Gateway: Listener name. When both Port (experimental) and SectionName are specified, the name and port of the selected listener must match both specified values. * Service: Port name. When both Port (experimental) and SectionName are specified, the name and port of the selected listener must match both specified values. Implementations MAY choose to support attaching Routes to other resources. If that is the case, they MUST clearly document how SectionName is interpreted. When unspecified (empty string), this will reference the entire resource. For the purpose of status, an attachment is considered successful if at least one section in the parent resource accepts it. For example, Gateway listeners can restrict which Routes can attach to them by Route kind, namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from the referencing Route, the Route MUST be considered successfully attached. If no Gateway listeners accept attachment from this Route, the Route MUST be considered detached from the Gateway. Support: Core
status.ancestors[].conditions <[]object> (required) min=1 max=8	# Conditions describes the status of the Policy with respect to the given Ancestor. <gateway:util:excludeFromCRD> Notes for implementors: Conditions are a listType `map`, which means that they function like a map with a key of the `type` field _in the k8s apiserver_. This means that implementations must obey some rules when updating this section. * Implementations MUST perform a read-modify-write cycle on this field before modifying it. That is, when modifying this field, implementations must be confident they have fetched the most recent version of this field, and ensure that changes they make are on that recent version. * Implementations MUST NOT remove or reorder Conditions that they are not directly responsible for. For example, if an implementation sees a Condition with type `special.io/SomeField`, it MUST NOT remove, change or update that Condition. * Implementations MUST always _merge_ changes into Conditions of the same Type, rather than creating more than one Condition of the same Type. * Implementations MUST always update the `observedGeneration` field of the Condition to the `metadata.generation` of the Gateway at the time of update creation. * If the `observedGeneration` of a Condition is _greater than_ the value the implementation knows about, then it MUST NOT perform the update on that Condition, but must wait for a future reconciliation and status update. (The assumption is that the implementation's copy of the object is stale and an update will be re-triggered if relevant.) </gateway:util:excludeFromCRD>
status.ancestors[].conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.ancestors[].conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.ancestors[].conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.ancestors[].conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.ancestors[].conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.ancestors[].conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.ancestors[].controllerName <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\\/[A-Za-z0-9\\/\\-._~%!$&'()*+,;=:]+$" min=1 max=253	# ControllerName is a domain/path string that indicates the name of the controller that wrote this status. This corresponds with the controllerName field on GatewayClass. Example: "example.net/gateway-controller". The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are valid Kubernetes names (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names). Controllers MUST populate this field when writing status. Controllers should ensure that entries to status populated with their ControllerName are cleaned up when they are no longer necessary.
