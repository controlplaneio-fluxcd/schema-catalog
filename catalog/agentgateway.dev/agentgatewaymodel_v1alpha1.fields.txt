# schema source: Agentgateway v1.4.1 https://github.com/agentgateway/agentgateway
apiVersion <string> enum=agentgateway.dev/v1alpha1
kind <string> enum=AgentgatewayModel
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# Desired model configuration.
spec.azure <object>	# Provider-specific settings for Azure AI.
spec.azure.apiVersion <string> min=1 max=64	# The version of the Azure OpenAI API to use. If unset, defaults to `v1`.
spec.azure.projectName <string> min=1 max=256	# The Foundry project name, required when `resourceType` is `Foundry`. Used to construct paths: /api/projects/{projectName}/openai/v1/...
spec.azure.resourceName <string> (required) min=1 max=256	# The Azure resource name used to construct the endpoint host. For OpenAI: {resourceName}.openai.azure.com For Foundry: {resourceName}.services.ai.azure.com Note: when the Azure portal "Foundry legacy" template was used, the generated resource name may end in "-resource" (e.g. "myproject-resource"); that suffix is part of the resource name as the user configured it, not part of the hostname suffix agentgateway should append.
spec.azure.resourceType <string> (required) enum=Foundry|OpenAI	# The type of Azure endpoint. Determines the host suffix.
spec.baseURL <string> format=uri min=1 max=1024	# BaseURL overrides the provider address and base path prefix. It must use the http or https scheme. Backend policies may override the default TLS configuration. Query parameters, fragments, and user info are not supported.
spec.bedrock <object>	# Provider-specific settings for Amazon Bedrock.
spec.bedrock.guardrail <object>	# Guardrail policy to use for the backend. See <https://docs.aws.amazon.com/bedrock/latest/userguide/guardrails.html>. If not specified, the AWS Guardrail policy will not be used.
spec.bedrock.guardrail.identifier <string> (required) min=1 max=256	# Identifier of the Guardrail policy to use for the backend.
spec.bedrock.guardrail.version <string> (required) min=1 max=256	# Version of the Guardrail policy to use for the backend.
spec.bedrock.region <string> default="us-east-1" pattern="^[a-z0-9-]+$" min=1 max=63	# AWS region to use for the backend. Defaults to `us-east-1` if not specified.
spec.custom <object>	# Provider-specific settings for a custom provider.
spec.custom.backendRef <object>	# Kubernetes backend that serves this provider. `backendRef` may target only a namespace-local Service or InferencePool. If unset, host and port must be set on the parent provider.
spec.custom.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# API group of the referenced resource. For example, `gateway.networking.k8s.io`. When unspecified or empty string, core API group is inferred.
spec.custom.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind of the referenced resource. For example, `Service`. Defaults to "Service" when not specified.
spec.custom.backendRef.name <string> (required) min=1 max=253	# Name of the referenced resource.
spec.custom.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number to use for this resource. Required when the referenced resource is a Kubernetes Service.
spec.custom.formats <[]object> (required) min=1 max=6	# Provider-native API formats this provider supports.
spec.custom.formats[].path <string> min=1 max=1024	# Default upstream path override for this format. If unset, agentgateway uses the default path for the format.
spec.custom.formats[].type <string> (required) enum=AnthropicTokenCount|Completions|Embeddings|Messages|Realtime|Rerank|Responses	# Provider-native API format.
spec.match <object>	# Conditions for selecting this model from client requests.
spec.match.model <string> min=1 max=1024	# Model name matched against client requests. It may be exact, a suffix wildcard such as `gpt-*`, a prefix wildcard such as `*-latest`, or `*`. When omitted, the model matches metadata.name exactly.
spec.parentRefs <[]object> (required) min=1 max=16	# Gateways and listeners to which this model attaches.
spec.parentRefs[].group <string> default="gateway.networking.k8s.io" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. When unspecified, "gateway.networking.k8s.io" is inferred. To set the core API group (such as for a "Service" kind referent), Group must be explicitly set to "" (empty string). Support: Core
spec.parentRefs[].kind <string> default="Gateway" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is kind of the referent. There are two kinds of parent resources with "Core" support: * Gateway (Gateway conformance profile) * Service (Mesh conformance profile, ClusterIP Services only) Support for other resources is Implementation-Specific.
spec.parentRefs[].name <string> (required) min=1 max=253	# Name is the name of the referent. Support: Core
spec.parentRefs[].namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the Route's local namespace. Cross-namespace references must be explicitly allowed, for example via ReferenceGrant. Support: Core
spec.parentRefs[].port <integer> format=int32 min=1 max=65535	# Port this Route targets on the parent, interpreted per parent kind (for example a Gateway listener port or Service port). Support: Extended
spec.parentRefs[].sectionName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of a section within the target resource, for example a Gateway Listener name or Service port name. Empty references the entire resource. Support: Core
spec.policies <object>	# Policies applied to this concrete model.
spec.policies.auth <object>	# Credentials used to authenticate requests to this model provider.
spec.policies.auth.aws <object>	# Explicit AWS authentication method for the model provider. When omitted, default AWS SDK credential discovery is used.
spec.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.policies.auth.aws.assumeRole.sessionName <string> pattern="^[\\w+=,.@-]{2,64}$"	# SessionName is a custom session name (RoleSessionName) for CloudTrail and Cost & Usage Report attribution. If unset, AWS generates a random name.
spec.policies.auth.aws.assumeRole.sessionNameExpression <string> min=1 max=16384	# SessionNameExpression is a CEL expression evaluated against each request to produce the session name (RoleSessionName), for example `jwt.sub` or `request.headers["x-team"]`. If the expression does not produce a valid session name at request time, the request is rejected.
spec.policies.auth.aws.assumeRole.tags <[]object> max=50	# Session tags passed to STS AssumeRole for cost attribution in the AWS Cost & Usage Report, once activated. STS allows at most 50 per role session.
spec.policies.auth.aws.assumeRole.tags[].expression <string> min=1 max=16384	# CEL expression evaluated against each request to produce the tag value, for example `jwt.sub` or `request.headers["x-app"]`. Requests with invalid tag values are rejected.
spec.policies.auth.aws.assumeRole.tags[].key <string> (required) min=1 max=128	# Key is the tag key.
spec.policies.auth.aws.assumeRole.tags[].value <string> max=256	# Value is a static tag value.
spec.policies.auth.aws.region <string> min=1 max=256	# AWS SigV4 signing region, for example `us-east-1`. Set this when the target AWS service is in a different region than the gateway. If unset, typed AWS backends may provide this automatically; otherwise the ambient AWS region is used.
spec.policies.auth.aws.secretRef <object>	# Credential source for AWS credentials, defaulting to a Kubernetes `Secret`. The default Secret resolver expects `accessKey`, `secretKey`, and optional `sessionToken` keys.
spec.policies.auth.aws.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.aws.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.policies.auth.azure <object>	# Azure authentication method for the model provider.
spec.policies.auth.azure.managedIdentity <object>	# Managed identity authentication settings.
spec.policies.auth.azure.managedIdentity.clientId <string> (required)
spec.policies.auth.azure.managedIdentity.objectId <string> (required)
spec.policies.auth.azure.managedIdentity.resourceId <string> (required)
spec.policies.auth.azure.secretRef <object>	# Credential source for Azure credentials, defaulting to a Kubernetes `Secret`. The default Secret resolver expects `clientID`, `tenantID`, and `clientSecret` keys.
spec.policies.auth.azure.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.azure.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.azure.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.azure.workloadIdentity <object (free-form)>	# Workload identity authentication settings. Uses the federated token and Azure env vars projected into the data plane pod. Recommended on AKS with Workload Identity enabled.
spec.policies.auth.credentials <[]object> min=1 max=8	# Credentials is a list of additional credentials to inject on the backend request. Each entry resolves a Secret key and writes its value to the entry's location. `credentials` is independent of the primary `key`/`secretRef`/`passthrough` mechanism and may be set on its own or alongside it.
spec.policies.auth.credentials[].location <object> (required)	# Where the credential is inserted on the backend request.
spec.policies.auth.credentials[].location.cookie <object>
spec.policies.auth.credentials[].location.cookie.name <string> (required) min=1 max=256
spec.policies.auth.credentials[].location.header <object>
spec.policies.auth.credentials[].location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.auth.credentials[].location.header.prefix <string> min=1 max=256
spec.policies.auth.credentials[].location.queryParameter <object>
spec.policies.auth.credentials[].location.queryParameter.name <string> (required) min=1 max=256
spec.policies.auth.credentials[].secretRef <object> (required)	# SecretRef references a Kubernetes Secret holding the credential value, and optionally overrides the key read from it. Defaults to `Authorization`, matching the key convention used by the top-level `secretRef`.
spec.policies.auth.credentials[].secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.credentials[].secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.auth.credentials[].secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.credentials[].secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.gcp <object>	# Google authentication method for the model provider. When omitted, default Google credential discovery is used.
spec.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.policies.auth.gcp.secretRef <object>	# Credential source for ADC-compatible Google credential JSON, defaulting to a Kubernetes `Secret`. By default, the value is read from `credentials.json`; set `secretRef.key` to override it. When omitted, ambient credentials are used.
spec.policies.auth.gcp.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.gcp.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.auth.gcp.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.policies.auth.location <object>	# Where backend credentials are inserted. If omitted, credentials are written to the `Authorization` header with the `Bearer ` prefix. This applies to `key`, `secretRef`, and `passthrough`. Entries in `credentials` carry their own location.
spec.policies.auth.location.cookie <object>
spec.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.policies.auth.location.header <object>
spec.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.auth.location.header.prefix <string> min=1 max=256
spec.policies.auth.location.queryParameter <object>
spec.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange <object>	# OAuth 2.0 token exchange (RFC 8693) / jwt-bearer (RFC 7523) authentication.
spec.policies.auth.oauthTokenExchange.actorToken <object>	# RFC 8693 delegation actor token. TokenExchange grant only.
spec.policies.auth.oauthTokenExchange.actorToken.mayAct <string> enum=Required	# may_act claim validation mode. When omitted, may_act is not enforced.
spec.policies.auth.oauthTokenExchange.actorToken.source <object> (required)	# Where to read the actor token. Actor tokens have no default source.
spec.policies.auth.oauthTokenExchange.actorToken.source.cookie <object>
spec.policies.auth.oauthTokenExchange.actorToken.source.cookie.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange.actorToken.source.expression <string> min=1 max=16384	# CEL expression that extracts the credential from the request.
spec.policies.auth.oauthTokenExchange.actorToken.source.header <object>
spec.policies.auth.oauthTokenExchange.actorToken.source.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.auth.oauthTokenExchange.actorToken.source.header.prefix <string> min=1 max=256
spec.policies.auth.oauthTokenExchange.actorToken.source.queryParameter <object>
spec.policies.auth.oauthTokenExchange.actorToken.source.queryParameter.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange.actorToken.tokenType <string>	# OAuth token type. Empty defaults to AccessToken. Custom absolute URI values are supported for actor tokens.
spec.policies.auth.oauthTokenExchange.additionalParams <map[string]string>	# Extra form params; values are CEL expressions over the incoming request.
spec.policies.auth.oauthTokenExchange.audiences <[]string> min=1 max=64	# Audiences sent to the token endpoint.
spec.policies.auth.oauthTokenExchange.backendRef <object> (required)	# RFC 8693 token endpoint backend.
spec.policies.auth.oauthTokenExchange.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.auth.oauthTokenExchange.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.auth.oauthTokenExchange.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.auth.oauthTokenExchange.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.auth.oauthTokenExchange.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.auth.oauthTokenExchange.cache <object>	# Response cache configuration.
spec.policies.auth.oauthTokenExchange.cache.inMemory <object>
spec.policies.auth.oauthTokenExchange.cache.inMemory.defaultTtl <string>	# TTL used when the token endpoint omits expires_in. Default 300s.
spec.policies.auth.oauthTokenExchange.cache.inMemory.maxEntries <integer> format=int32	# Default 8192; 0 disables the cache.
spec.policies.auth.oauthTokenExchange.clientAuth <object>	# Client authentication for the token endpoint. When unset, none is sent.
spec.policies.auth.oauthTokenExchange.clientAuth.clientId <string> (required) min=1	# Client ID sent to the token endpoint.
spec.policies.auth.oauthTokenExchange.clientAuth.method <string> enum=ClientSecretBasic|ClientSecretPost|PrivateKeyJwt	# Client authentication method. Defaults to ClientSecretBasic.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt <object>	# Client assertion settings. Required when method is PrivateKeyJwt.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.alg <string> enum=ES256|ES384|PS256|RS256|RS384|RS512	# JWS signing algorithm. Defaults to RS256.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.assertionAudience <string> (required) min=1	# Audience for the client assertion, typically the token endpoint URL.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.certificateHeader <string> enum=x5c|x5t#S256	# JWS certificate header. Required when certificateRef is set.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.certificateRef <object>	# PEM-encoded X.509 certificate chain, leaf first, for certificateHeader. The leaf public key should match signingKeyRef; a mismatch only logs a warning but the token endpoint will reject the assertions. Required when certificateHeader is set. The key defaults to `certificate`.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.certificateRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.certificateRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.certificateRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.certificateRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.kid <string>	# Optional JWS key ID header.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.signingKeyRef <object> (required)	# PEM-encoded RSA or EC private key; key defaults to `signingKey`.
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.signingKeyRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.signingKeyRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.signingKeyRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.oauthTokenExchange.clientAuth.privateKeyJwt.signingKeyRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.oauthTokenExchange.clientAuth.secretRef <object>	# Secret providing the `clientSecret` key by default; override via `secretRef.key`. When omitted, client_id is sent without a secret, which is only valid with ClientSecretPost.
spec.policies.auth.oauthTokenExchange.clientAuth.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.oauthTokenExchange.clientAuth.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.auth.oauthTokenExchange.clientAuth.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.oauthTokenExchange.clientAuth.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.auth.oauthTokenExchange.grantType <string> enum=JwtBearer|TokenExchange	# RFC followed by the request. Defaults to TokenExchange (RFC 8693).
spec.policies.auth.oauthTokenExchange.location <object>	# Where the exchanged token is written to the backend request. Defaults to Authorization: Bearer.
spec.policies.auth.oauthTokenExchange.location.cookie <object>
spec.policies.auth.oauthTokenExchange.location.cookie.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange.location.header <object>
spec.policies.auth.oauthTokenExchange.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.auth.oauthTokenExchange.location.header.prefix <string> min=1 max=256
spec.policies.auth.oauthTokenExchange.location.queryParameter <object>
spec.policies.auth.oauthTokenExchange.location.queryParameter.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange.path <string> pattern="^/"	# Token endpoint path; defaults to "/". Must start with "/".
spec.policies.auth.oauthTokenExchange.requestedTokenType <string> enum=AccessToken|Jwt|IdToken|IdJag	# RFC 8693 requested_token_type. Unlike subject/actor token types, only the built-in values may be requested; custom URIs are not supported here.
spec.policies.auth.oauthTokenExchange.resources <[]string> min=1 max=64	# Resources sent to the token endpoint.
spec.policies.auth.oauthTokenExchange.scopes <[]string> min=1 max=64	# Scopes sent to the token endpoint.
spec.policies.auth.oauthTokenExchange.subjectToken <object>	# Subject token / assertion source and type. Defaults to Authorization Bearer, AccessToken. The token type may be a built-in value or a custom absolute URI for providers that support custom token exchange profiles.
spec.policies.auth.oauthTokenExchange.subjectToken.source <object>	# Where to read the token. CEL `expression` variant is permitted.
spec.policies.auth.oauthTokenExchange.subjectToken.source.cookie <object>
spec.policies.auth.oauthTokenExchange.subjectToken.source.cookie.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange.subjectToken.source.expression <string> min=1 max=16384	# CEL expression that extracts the credential from the request.
spec.policies.auth.oauthTokenExchange.subjectToken.source.header <object>
spec.policies.auth.oauthTokenExchange.subjectToken.source.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.auth.oauthTokenExchange.subjectToken.source.header.prefix <string> min=1 max=256
spec.policies.auth.oauthTokenExchange.subjectToken.source.queryParameter <object>
spec.policies.auth.oauthTokenExchange.subjectToken.source.queryParameter.name <string> (required) min=1 max=256
spec.policies.auth.oauthTokenExchange.subjectToken.tokenType <string>	# OAuth token type. Empty defaults to AccessToken. Custom absolute URI values are supported for subject tokens.
spec.policies.auth.passthrough <object (free-form)>	# Reuses a client token already validated by another policy. Those policies may strip client credentials; passthrough adds the original token back to the backend request. Without client auth policies, this has no effect.
spec.policies.auth.secretRef <object>	# Credential source for the authorization value, defaulting to a Kubernetes `Secret`. By default, the value is read from the `Authorization` key; set `secretRef.key` to override it. A `Bearer ` prefix is stripped only from the default `Authorization` key.
spec.policies.auth.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.auth.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.auth.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.auth.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.authorization <object>	# Authorization rules that clients must satisfy to use this model.
spec.policies.authorization.action <string> enum=Allow|Deny|Require default="Allow"	# The effect of this rule when it matches. If unspecified, defaults to `Allow`. `Require` rules are cumulative: all require rules must match.
spec.policies.authorization.policy <object> (required)	# The authorization rule to evaluate. * `Allow`: any matching allow rule allows the request. * `Require`: every require rule must match for the request to be allowed. * `Deny`: any matching deny rule denies the request. `Deny` is not recommended because expression failures fail to deny; prefer `Allow` or `Require`. If used, design expressions defensively against evaluation errors. If at least one `Allow` rule is configured, requests are denied unless at least one allow rule matches.
spec.policies.authorization.policy.matchExpressions <[]string> (required) min=1 max=256	# CEL expressions that must all evaluate to true for the rule to match.
spec.policies.headers <object>	# Request and response header changes applied to provider traffic.
spec.policies.headers.request <object>	# Header changes to apply before forwarding a request.
spec.policies.headers.request.add <[]object> max=16	# Add adds the given header(s) (name, value) to the request before the action. It appends to any existing values associated with the header name. Input: GET /foo HTTP/1.1 my-header: foo Config: add: - name: "my-header" value: "bar,baz" Output: GET /foo HTTP/1.1 my-header: foo,bar,baz
spec.policies.headers.request.add[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of the HTTP header, case-insensitive
spec.policies.headers.request.add[].value <string> (required) min=1 max=4096	# Value for the HTTP header
spec.policies.headers.request.remove <[]string> max=16	# Remove the given header(s) from the HTTP request before the action. The value of Remove is a list of HTTP header names. Note that the header names are case-insensitive (see https://datatracker.ietf.org/doc/html/rfc2616#section-4.2). Input: GET /foo HTTP/1.1 my-header1: foo my-header2: bar my-header3: baz Config: remove: ["my-header1", "my-header3"] Output: GET /foo HTTP/1.1 my-header2: bar
spec.policies.headers.request.set <[]object> max=16	# Set overwrites the request with the given header (name, value) before the action. Input: GET /foo HTTP/1.1 my-header: foo Config: set: - name: "my-header" value: "bar" Output: GET /foo HTTP/1.1 my-header: bar
spec.policies.headers.request.set[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of the HTTP header, case-insensitive
spec.policies.headers.request.set[].value <string> (required) min=1 max=4096	# Value for the HTTP header
spec.policies.headers.response <object>	# Header changes to apply before returning a response.
spec.policies.headers.response.add <[]object> max=16	# Add adds the given header(s) (name, value) to the request before the action. It appends to any existing values associated with the header name. Input: GET /foo HTTP/1.1 my-header: foo Config: add: - name: "my-header" value: "bar,baz" Output: GET /foo HTTP/1.1 my-header: foo,bar,baz
spec.policies.headers.response.add[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of the HTTP header, case-insensitive
spec.policies.headers.response.add[].value <string> (required) min=1 max=4096	# Value for the HTTP header
spec.policies.headers.response.remove <[]string> max=16	# Remove the given header(s) from the HTTP request before the action. The value of Remove is a list of HTTP header names. Note that the header names are case-insensitive (see https://datatracker.ietf.org/doc/html/rfc2616#section-4.2). Input: GET /foo HTTP/1.1 my-header1: foo my-header2: bar my-header3: baz Config: remove: ["my-header1", "my-header3"] Output: GET /foo HTTP/1.1 my-header2: bar
spec.policies.headers.response.set <[]object> max=16	# Set overwrites the request with the given header (name, value) before the action. Input: GET /foo HTTP/1.1 my-header: foo Config: set: - name: "my-header" value: "bar" Output: GET /foo HTTP/1.1 my-header: bar
spec.policies.headers.response.set[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of the HTTP header, case-insensitive
spec.policies.headers.response.set[].value <string> (required) min=1 max=4096	# Value for the HTTP header
spec.policies.health <object>	# Health checking and eviction behavior for this model provider.
spec.policies.health.eviction <object>	# Settings for evicting unhealthy backends.
spec.policies.health.eviction.consecutiveFailures <integer> format=int32 min=0	# Number of consecutive unhealthy responses required before the backend is evicted. For example, a value of 5 means the backend must receive 5 unhealthy responses in a row before being evicted. When both consecutiveFailures and healthThreshold are set, the backend is evicted when either condition is met. When neither is set, a single unhealthy response can trigger eviction.
spec.policies.health.eviction.duration <string> default="3s" max=32	# Base time a backend should be evicted after being marked unhealthy. Subsequent evictions use multiplicative backoff (duration * times_evicted). If all endpoints are evicted, the load balancer falls back to returning evicted endpoints rather than failing entirely. If unset, defaults to `3s`.
spec.policies.health.eviction.healthThreshold <integer> format=int32 min=0 max=100	# EWMA health score threshold, from 0 to 100. When set, a backend is evicted only if its computed health drops below this value after an unhealthy response (e.g. 50 evicts when EWMA health falls below 50%). Unlike consecutiveFailures, this sliding-window average lets a single success delay eviction. If both are set, either condition evicts; if neither, a single unhealthy response evicts.
spec.policies.health.eviction.restoreHealth <integer> format=int32 min=0 max=100	# Health score from 0 to 100 assigned to a backend when it returns from eviction. For gradual recovery, set below 100; for full recovery immediately, set 100. If unset, the backend resumes with the health it had when evicted.
spec.policies.health.unhealthyCondition <string> min=1 max=16384	# CEL expression that determines whether a response indicates an unhealthy backend. When the expression evaluates to true, the backend is considered unhealthy and may be evicted. For example, to evict on 5xx responses: `response.code >= 500`. When unset, any 5xx response, or a connection failure, is treated as unhealthy. This default lowers the backend's health score but does not trigger eviction on its own.
spec.policies.promptGuard <object>	# Guardrails for requests and responses sent to this model provider.
spec.policies.promptGuard.request <[]object> min=1 max=8	# Prompt guards to apply to requests sent by the client.
spec.policies.promptGuard.request[].bedrockGuardrails <object>	# AWS Bedrock Guardrails settings for prompt guarding.
spec.policies.promptGuard.request[].bedrockGuardrails.identifier <string> (required) min=1 max=256	# Identifier of the Guardrail policy to use for the backend.
spec.policies.promptGuard.request[].bedrockGuardrails.policies <object>	# Policies for communicating with AWS Bedrock Guardrails.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth <object>	# Settings for authenticating to AWS Bedrock Guardrails.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws <object>	# AWS authentication method for Bedrock Guardrails. Use `aws: {}` for default AWS SDK credential discovery.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.sessionName <string> pattern="^[\\w+=,.@-]{2,64}$"	# SessionName is a custom session name (RoleSessionName) for CloudTrail and Cost & Usage Report attribution. If unset, AWS generates a random name.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.sessionNameExpression <string> min=1 max=16384	# SessionNameExpression is a CEL expression evaluated against each request to produce the session name (RoleSessionName), for example `jwt.sub` or `request.headers["x-team"]`. If the expression does not produce a valid session name at request time, the request is rejected.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.tags <[]object> max=50	# Session tags passed to STS AssumeRole for cost attribution in the AWS Cost & Usage Report, once activated. STS allows at most 50 per role session.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.tags[].expression <string> min=1 max=16384	# CEL expression evaluated against each request to produce the tag value, for example `jwt.sub` or `request.headers["x-app"]`. Requests with invalid tag values are rejected.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.tags[].key <string> (required) min=1 max=128	# Key is the tag key.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.assumeRole.tags[].value <string> max=256	# Value is a static tag value.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.region <string> min=1 max=256	# AWS SigV4 signing region, for example `us-east-1`. Set this when the target AWS service is in a different region than the gateway. If unset, typed AWS backends may provide this automatically; otherwise the ambient AWS region is used.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef <object>	# Credential source for AWS credentials, defaulting to a Kubernetes `Secret`. The default Secret resolver expects `accessKey`, `secretKey`, and optional `sessionToken` keys.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.key <string> max=2048	# Inline API key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location <object>	# Where API keys are inserted. Defaults to the `Authorization` header with the `Bearer ` prefix. Applies to `key` and `secretRef`.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.cookie <object>
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.header <object>
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.header.prefix <string> min=1 max=256
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.queryParameter <object>
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef <object>	# Credential source for the API key, defaulting to a Kubernetes `Secret`. By default, the value is read from the `Authorization` key; set `secretRef.key` to override it. A `Bearer ` prefix is stripped only from the default `Authorization` key.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].bedrockGuardrails.policies.auth.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].bedrockGuardrails.policies.http <object>	# Settings for managing HTTP requests to the backend
spec.policies.promptGuard.request[].bedrockGuardrails.policies.http.requestTimeout <string> max=32	# Deadline for receiving a response from the backend.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version for backend connections. If unset, it is inferred: `Service` appProtocol, `HTTP2` for gRPC, the original protocol for plaintext HTTP, or `HTTP1` for HTTPS because clients often upgrade HTTPS to HTTP/2 even when the backend does not support it.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tcp <object>	# Settings for managing TCP connections to the backend
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tcp.connectTimeout <string> max=32	# Deadline for establishing a connection to the destination.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive.interval <string> max=32	# Time between keepalive probes. If unset, this defaults to 180s.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tcp.keepalive.time <string> max=32	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls <object>	# Settings for managing TLS connections to the backend When set, TLS is originated to the backend using the system trusted CA certificates, and SNI is inferred from the destination.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate WARNING: insecure; only use if the risks are understood Modes: * `All` disables all TLS verification * `Hostname` trusts the CA certificate but ignores hostname/SAN mismatches. Still insecure; prefer `verifySubjectAltNames` where possible.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend using `tls.key` and `tls.crt` from the referenced credential source (defaulting to a Kubernetes `Secret`). An optional `ca.cert`, if present, verifies the server certificate, but `caCertificateRefs` takes priority. If unspecified, no client certificate is used.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel <object>	# Settings for managing tunnel connections to the backend, like `HTTPS_PROXY`
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.request[].bedrockGuardrails.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.request[].bedrockGuardrails.region <string> (required) min=1 max=256	# AWS region where the guardrail is deployed, for example `us-west-2`).
spec.policies.promptGuard.request[].bedrockGuardrails.version <string> (required) min=1 max=256	# Version of the Guardrail policy to use for the backend.
spec.policies.promptGuard.request[].googleModelArmor <object>	# Google Model Armor settings for prompt guarding.
spec.policies.promptGuard.request[].googleModelArmor.location <string> default="us-central1" min=1 max=256	# Google Cloud location, for example `us-central1`. Defaults to `us-central1` if not specified.
spec.policies.promptGuard.request[].googleModelArmor.policies <object>	# Policies for communicating with Google Model Armor.
spec.policies.promptGuard.request[].googleModelArmor.policies.auth <object>	# Settings for authenticating to Google Model Armor.
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp <object>	# Google authentication method for Model Armor. Use `gcp: {}` for default Google credential discovery.
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef <object>	# Credential source for ADC-compatible Google credential JSON, defaulting to a Kubernetes `Secret`. By default, the value is read from `credentials.json`; set `secretRef.key` to override it. When omitted, ambient credentials are used.
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].googleModelArmor.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.policies.promptGuard.request[].googleModelArmor.policies.http <object>	# Settings for managing HTTP requests to the backend
spec.policies.promptGuard.request[].googleModelArmor.policies.http.requestTimeout <string> max=32	# Deadline for receiving a response from the backend.
spec.policies.promptGuard.request[].googleModelArmor.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version for backend connections. If unset, it is inferred: `Service` appProtocol, `HTTP2` for gRPC, the original protocol for plaintext HTTP, or `HTTP1` for HTTPS because clients often upgrade HTTPS to HTTP/2 even when the backend does not support it.
spec.policies.promptGuard.request[].googleModelArmor.policies.tcp <object>	# Settings for managing TCP connections to the backend
spec.policies.promptGuard.request[].googleModelArmor.policies.tcp.connectTimeout <string> max=32	# Deadline for establishing a connection to the destination.
spec.policies.promptGuard.request[].googleModelArmor.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.policies.promptGuard.request[].googleModelArmor.policies.tcp.keepalive.interval <string> max=32	# Time between keepalive probes. If unset, this defaults to 180s.
spec.policies.promptGuard.request[].googleModelArmor.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.policies.promptGuard.request[].googleModelArmor.policies.tcp.keepalive.time <string> max=32	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls <object>	# Settings for managing TLS connections to the backend When set, TLS is originated to the backend using the system trusted CA certificates, and SNI is inferred from the destination.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate WARNING: insecure; only use if the risks are understood Modes: * `All` disables all TLS verification * `Hostname` trusts the CA certificate but ignores hostname/SAN mismatches. Still insecure; prefer `verifySubjectAltNames` where possible.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend using `tls.key` and `tls.crt` from the referenced credential source (defaulting to a Kubernetes `Secret`). An optional `ca.cert`, if present, verifies the server certificate, but `caCertificateRefs` takes priority. If unspecified, no client certificate is used.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef[].group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef[].kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.policies.promptGuard.request[].googleModelArmor.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel <object>	# Settings for managing tunnel connections to the backend, like `HTTPS_PROXY`
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.request[].googleModelArmor.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.request[].googleModelArmor.projectId <string> (required) min=1 max=256	# Google Cloud project ID.
spec.policies.promptGuard.request[].googleModelArmor.templateId <string> (required) min=1 max=256	# Template ID for Google Model Armor.
spec.policies.promptGuard.request[].openAIModeration <object>	# Passes prompt data through the OpenAI Moderations endpoint. See https://developers.openai.com/api/reference/resources/moderations for more information.
spec.policies.promptGuard.request[].openAIModeration.model <string>	# Moderation model to use. For example, `omni-moderation`.
spec.policies.promptGuard.request[].openAIModeration.policies <object>	# Policies for communicating with OpenAI.
spec.policies.promptGuard.request[].openAIModeration.policies.auth <object>	# Settings for authenticating to OpenAI.
spec.policies.promptGuard.request[].openAIModeration.policies.auth.key <string> max=2048	# Inline key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location <object>	# Where backend credentials are inserted. Defaults to the `Authorization` header with the `Bearer ` prefix. Applies to `key` and `secretRef`.
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.cookie <object>
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.header <object>
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.header.prefix <string> min=1 max=256
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.queryParameter <object>
spec.policies.promptGuard.request[].openAIModeration.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.policies.promptGuard.request[].openAIModeration.policies.auth.secretRef <object>	# Credential source for the authorization value, defaulting to a Kubernetes `Secret`. By default, the value is read from the `Authorization` key; set `secretRef.key` to override it. A `Bearer ` prefix is stripped only from the default `Authorization` key.
spec.policies.promptGuard.request[].openAIModeration.policies.auth.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].openAIModeration.policies.auth.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.promptGuard.request[].openAIModeration.policies.auth.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].openAIModeration.policies.auth.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].openAIModeration.policies.http <object>	# Settings for managing HTTP requests to the backend
spec.policies.promptGuard.request[].openAIModeration.policies.http.requestTimeout <string> max=32	# Deadline for receiving a response from the backend.
spec.policies.promptGuard.request[].openAIModeration.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version for backend connections. If unset, it is inferred: `Service` appProtocol, `HTTP2` for gRPC, the original protocol for plaintext HTTP, or `HTTP1` for HTTPS because clients often upgrade HTTPS to HTTP/2 even when the backend does not support it.
spec.policies.promptGuard.request[].openAIModeration.policies.tcp <object>	# Settings for managing TCP connections to the backend
spec.policies.promptGuard.request[].openAIModeration.policies.tcp.connectTimeout <string> max=32	# Deadline for establishing a connection to the destination.
spec.policies.promptGuard.request[].openAIModeration.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.policies.promptGuard.request[].openAIModeration.policies.tcp.keepalive.interval <string> max=32	# Time between keepalive probes. If unset, this defaults to 180s.
spec.policies.promptGuard.request[].openAIModeration.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.policies.promptGuard.request[].openAIModeration.policies.tcp.keepalive.time <string> max=32	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.policies.promptGuard.request[].openAIModeration.policies.tls <object>	# Settings for managing TLS connections to the backend When set, TLS is originated to the backend using the system trusted CA certificates, and SNI is inferred from the destination.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent
spec.policies.promptGuard.request[].openAIModeration.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate WARNING: insecure; only use if the risks are understood Modes: * `All` disables all TLS verification * `Hostname` trusts the CA certificate but ignores hostname/SAN mismatches. Still insecure; prefer `verifySubjectAltNames` where possible.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend using `tls.key` and `tls.crt` from the referenced credential source (defaulting to a Kubernetes `Secret`). An optional `ca.cert`, if present, verifies the server certificate, but `caCertificateRefs` takes priority. If unspecified, no client certificate is used.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef[].group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef[].kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.request[].openAIModeration.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.request[].openAIModeration.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.policies.promptGuard.request[].openAIModeration.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel <object>	# Settings for managing tunnel connections to the backend, like `HTTPS_PROXY`
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.request[].openAIModeration.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.request[].regex <object>	# Regular expression (regex) matching for prompt guards and data masking.
spec.policies.promptGuard.request[].regex.action <string> enum=Mask|Reject default="Mask"	# The action to take if a regex pattern is matched in a request or response. This setting applies only to request matches. `PromptguardResponse` matches are always masked by default. Defaults to `Mask`.
spec.policies.promptGuard.request[].regex.builtins <[]string>	# Built-in regex patterns to match against the request or response. Matches and built-ins are additive.
spec.policies.promptGuard.request[].regex.matches <[]string>	# Regex patterns to match against the request or response. Matches and built-ins are additive.
spec.policies.promptGuard.request[].response <object>	# Custom response message to return to the client. If not specified, defaults to `The request was rejected due to inappropriate content`.
spec.policies.promptGuard.request[].response.message <string> default="The request was rejected due to inappropriate content"	# Custom response message to return to the client. If not specified, defaults to `The request was rejected due to inappropriate content`.
spec.policies.promptGuard.request[].response.statusCode <integer> default=403 format=int32 min=200 max=599	# Status code to return to the client. Defaults to 403.
spec.policies.promptGuard.request[].webhook <object>	# Webhook that receives requests for prompt guarding.
spec.policies.promptGuard.request[].webhook.backendRef <object> (required)	# Webhook server to reach. Supported types: Service and Backend.
spec.policies.promptGuard.request[].webhook.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.request[].webhook.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.request[].webhook.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.request[].webhook.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.request[].webhook.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.request[].webhook.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the webhook guardrail is unavailable or returns an error. `FailOpen` allows the request to continue. `FailClosed` (default) rejects the request.
spec.policies.promptGuard.request[].webhook.forwardHeaderMatches <[]object>	# HTTP header matches used to select the headers to forward to the webhook. Request headers are used when forwarding requests and response headers are used when forwarding responses. By default, no headers are forwarded.
spec.policies.promptGuard.request[].webhook.forwardHeaderMatches[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of the HTTP header to match, case-insensitive. When names are equivalent, only the first matching entry is used
spec.policies.promptGuard.request[].webhook.forwardHeaderMatches[].type <string> enum=Exact|RegularExpression default="Exact"	# How to match against the header value: `Exact` (default) or `RegularExpression`. The regex dialect is implementation-specific
spec.policies.promptGuard.request[].webhook.forwardHeaderMatches[].value <string> (required) min=1 max=4096	# Value of the HTTP header to match
spec.policies.promptGuard.request[].webhook.headers <map[string]string>	# CEL-computed headers to include in webhook requests.
spec.policies.promptGuard.response <[]object> min=1 max=8	# Prompt guards to apply to responses returned by the LLM provider.
spec.policies.promptGuard.response[].bedrockGuardrails <object>	# AWS Bedrock Guardrails settings for prompt guarding.
spec.policies.promptGuard.response[].bedrockGuardrails.identifier <string> (required) min=1 max=256	# Identifier of the Guardrail policy to use for the backend.
spec.policies.promptGuard.response[].bedrockGuardrails.policies <object>	# Policies for communicating with AWS Bedrock Guardrails.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth <object>	# Settings for authenticating to AWS Bedrock Guardrails.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws <object>	# AWS authentication method for Bedrock Guardrails. Use `aws: {}` for default AWS SDK credential discovery.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole <object>	# AWS STS AssumeRole settings to use before signing backend requests. Ambient AWS credentials are used as the source credentials for STS.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.roleArn <string> (required) pattern="^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$" min=1	# AWS IAM role ARN to assume.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.sessionName <string> pattern="^[\\w+=,.@-]{2,64}$"	# SessionName is a custom session name (RoleSessionName) for CloudTrail and Cost & Usage Report attribution. If unset, AWS generates a random name.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.sessionNameExpression <string> min=1 max=16384	# SessionNameExpression is a CEL expression evaluated against each request to produce the session name (RoleSessionName), for example `jwt.sub` or `request.headers["x-team"]`. If the expression does not produce a valid session name at request time, the request is rejected.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.tags <[]object> max=50	# Session tags passed to STS AssumeRole for cost attribution in the AWS Cost & Usage Report, once activated. STS allows at most 50 per role session.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.tags[].expression <string> min=1 max=16384	# CEL expression evaluated against each request to produce the tag value, for example `jwt.sub` or `request.headers["x-app"]`. Requests with invalid tag values are rejected.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.tags[].key <string> (required) min=1 max=128	# Key is the tag key.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.assumeRole.tags[].value <string> max=256	# Value is a static tag value.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.region <string> min=1 max=256	# AWS SigV4 signing region, for example `us-east-1`. Set this when the target AWS service is in a different region than the gateway. If unset, typed AWS backends may provide this automatically; otherwise the ambient AWS region is used.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef <object>	# Credential source for AWS credentials, defaulting to a Kubernetes `Secret`. The default Secret resolver expects `accessKey`, `secretKey`, and optional `sessionToken` keys.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.aws.serviceName <string> min=1 max=256	# AWS SigV4 signing service name, for example `bedrock`, `bedrock-agentcore`, or `execute-api`). If unset, typed AWS backends may provide this automatically.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.key <string> max=2048	# Inline API key to use as the value of the `Authorization` header. This option is the least secure; usage of a `Secret` is preferred.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location <object>	# Where API keys are inserted. Defaults to the `Authorization` header with the `Bearer ` prefix. Applies to `key` and `secretRef`.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.cookie <object>
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.cookie.name <string> (required) min=1 max=256
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.header <object>
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.header.name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of an HTTP header. HTTP/2 pseudo-headers (names beginning with `:`) are not supported
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.header.prefix <string> min=1 max=256
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.queryParameter <object>
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.location.queryParameter.name <string> (required) min=1 max=256
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef <object>	# Credential source for the API key, defaulting to a Kubernetes `Secret`. By default, the value is read from the `Authorization` key; set `secretRef.key` to override it. A `Bearer ` prefix is stripped only from the default `Authorization` key.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.response[].bedrockGuardrails.policies.auth.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.response[].bedrockGuardrails.policies.http <object>	# Settings for managing HTTP requests to the backend
spec.policies.promptGuard.response[].bedrockGuardrails.policies.http.requestTimeout <string> max=32	# Deadline for receiving a response from the backend.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version for backend connections. If unset, it is inferred: `Service` appProtocol, `HTTP2` for gRPC, the original protocol for plaintext HTTP, or `HTTP1` for HTTPS because clients often upgrade HTTPS to HTTP/2 even when the backend does not support it.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tcp <object>	# Settings for managing TCP connections to the backend
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tcp.connectTimeout <string> max=32	# Deadline for establishing a connection to the destination.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive.interval <string> max=32	# Time between keepalive probes. If unset, this defaults to 180s.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tcp.keepalive.time <string> max=32	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls <object>	# Settings for managing TLS connections to the backend When set, TLS is originated to the backend using the system trusted CA certificates, and SNI is inferred from the destination.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate WARNING: insecure; only use if the risks are understood Modes: * `All` disables all TLS verification * `Hostname` trusts the CA certificate but ignores hostname/SAN mismatches. Still insecure; prefer `verifySubjectAltNames` where possible.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend using `tls.key` and `tls.crt` from the referenced credential source (defaulting to a Kubernetes `Secret`). An optional `ca.cert`, if present, verifies the server certificate, but `caCertificateRefs` takes priority. If unspecified, no client certificate is used.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel <object>	# Settings for managing tunnel connections to the backend, like `HTTPS_PROXY`
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.response[].bedrockGuardrails.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.response[].bedrockGuardrails.region <string> (required) min=1 max=256	# AWS region where the guardrail is deployed, for example `us-west-2`).
spec.policies.promptGuard.response[].bedrockGuardrails.version <string> (required) min=1 max=256	# Version of the Guardrail policy to use for the backend.
spec.policies.promptGuard.response[].googleModelArmor <object>	# Google Model Armor settings for prompt guarding.
spec.policies.promptGuard.response[].googleModelArmor.location <string> default="us-central1" min=1 max=256	# Google Cloud location, for example `us-central1`. Defaults to `us-central1` if not specified.
spec.policies.promptGuard.response[].googleModelArmor.policies <object>	# Policies for communicating with Google Model Armor.
spec.policies.promptGuard.response[].googleModelArmor.policies.auth <object>	# Settings for authenticating to Google Model Armor.
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp <object>	# Google authentication method for Model Armor. Use `gcp: {}` for default Google credential discovery.
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.audience <string> min=1 max=256	# Explicit `aud` value for the ID token. Only valid with `IdToken` type. If not set, the `aud` is automatically derived from the backend hostname.
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef <object>	# Credential source for ADC-compatible Google credential JSON, defaulting to a Kubernetes `Secret`. By default, the value is read from `credentials.json`; set `secretRef.key` to override it. When omitted, ambient credentials are used.
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.key <string> min=1 max=253	# Key in the referenced Secret. If omitted, a location-specific default is used
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.secretRef.name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.response[].googleModelArmor.policies.auth.gcp.type <string> enum=AccessToken|IdToken	# The type of token to generate. To authenticate to GCP services, generally an `AccessToken` is used. To authenticate to Cloud Run, an `IdToken` is used.
spec.policies.promptGuard.response[].googleModelArmor.policies.http <object>	# Settings for managing HTTP requests to the backend
spec.policies.promptGuard.response[].googleModelArmor.policies.http.requestTimeout <string> max=32	# Deadline for receiving a response from the backend.
spec.policies.promptGuard.response[].googleModelArmor.policies.http.version <string> enum=HTTP1|HTTP2	# HTTP protocol version for backend connections. If unset, it is inferred: `Service` appProtocol, `HTTP2` for gRPC, the original protocol for plaintext HTTP, or `HTTP1` for HTTPS because clients often upgrade HTTPS to HTTP/2 even when the backend does not support it.
spec.policies.promptGuard.response[].googleModelArmor.policies.tcp <object>	# Settings for managing TCP connections to the backend
spec.policies.promptGuard.response[].googleModelArmor.policies.tcp.connectTimeout <string> max=32	# Deadline for establishing a connection to the destination.
spec.policies.promptGuard.response[].googleModelArmor.policies.tcp.keepalive <object>	# Settings for enabling TCP keepalives on the connection.
spec.policies.promptGuard.response[].googleModelArmor.policies.tcp.keepalive.interval <string> max=32	# Time between keepalive probes. If unset, this defaults to 180s.
spec.policies.promptGuard.response[].googleModelArmor.policies.tcp.keepalive.retries <integer> format=int32 min=1 max=64	# Maximum number of keepalive probes to send before dropping the connection. If unset, this defaults to 9.
spec.policies.promptGuard.response[].googleModelArmor.policies.tcp.keepalive.time <string> max=32	# Time a connection needs to be idle before keepalive probes start being sent. If unset, this defaults to 180s.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls <object>	# Settings for managing TLS connections to the backend When set, TLS is originated to the backend using the system trusted CA certificates, and SNI is inferred from the destination.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate WARNING: insecure; only use if the risks are understood Modes: * `All` disables all TLS verification * `Hostname` trusts the CA certificate but ignores hostname/SAN mismatches. Still insecure; prefer `verifySubjectAltNames` where possible.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend using `tls.key` and `tls.crt` from the referenced credential source (defaulting to a Kubernetes `Secret`). An optional `ca.cert`, if present, verifies the server certificate, but `caCertificateRefs` takes priority. If unspecified, no client certificate is used.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef[].group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef[].kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.policies.promptGuard.response[].googleModelArmor.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel <object>	# Settings for managing tunnel connections to the backend, like `HTTPS_PROXY`
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.response[].googleModelArmor.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.response[].googleModelArmor.projectId <string> (required) min=1 max=256	# Google Cloud project ID.
spec.policies.promptGuard.response[].googleModelArmor.templateId <string> (required) min=1 max=256	# Template ID for Google Model Armor.
spec.policies.promptGuard.response[].regex <object>	# Regular expression (regex) matching for prompt guards and data masking.
spec.policies.promptGuard.response[].regex.action <string> enum=Mask|Reject default="Mask"	# The action to take if a regex pattern is matched in a request or response. This setting applies only to request matches. `PromptguardResponse` matches are always masked by default. Defaults to `Mask`.
spec.policies.promptGuard.response[].regex.builtins <[]string>	# Built-in regex patterns to match against the request or response. Matches and built-ins are additive.
spec.policies.promptGuard.response[].regex.matches <[]string>	# Regex patterns to match against the request or response. Matches and built-ins are additive.
spec.policies.promptGuard.response[].response <object>	# Custom response message to return to the client. If not specified, defaults to `The response was rejected due to inappropriate content`.
spec.policies.promptGuard.response[].response.message <string> default="The request was rejected due to inappropriate content"	# Custom response message to return to the client. If not specified, defaults to `The request was rejected due to inappropriate content`.
spec.policies.promptGuard.response[].response.statusCode <integer> default=403 format=int32 min=200 max=599	# Status code to return to the client. Defaults to 403.
spec.policies.promptGuard.response[].webhook <object>	# Webhook that receives responses for prompt guarding.
spec.policies.promptGuard.response[].webhook.backendRef <object> (required)	# Webhook server to reach. Supported types: Service and Backend.
spec.policies.promptGuard.response[].webhook.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.promptGuard.response[].webhook.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.promptGuard.response[].webhook.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.promptGuard.response[].webhook.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.promptGuard.response[].webhook.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.policies.promptGuard.response[].webhook.failureMode <string> enum=FailClosed|FailOpen	# Behavior when the webhook guardrail is unavailable or returns an error. `FailOpen` allows the request to continue. `FailClosed` (default) rejects the request.
spec.policies.promptGuard.response[].webhook.forwardHeaderMatches <[]object>	# HTTP header matches used to select the headers to forward to the webhook. Request headers are used when forwarding requests and response headers are used when forwarding responses. By default, no headers are forwarded.
spec.policies.promptGuard.response[].webhook.forwardHeaderMatches[].name <string> (required) pattern="^[A-Za-z0-9!#$%&'*+\\-.^_\\x60|~]+$" min=1 max=256	# Name of the HTTP header to match, case-insensitive. When names are equivalent, only the first matching entry is used
spec.policies.promptGuard.response[].webhook.forwardHeaderMatches[].type <string> enum=Exact|RegularExpression default="Exact"	# How to match against the header value: `Exact` (default) or `RegularExpression`. The regex dialect is implementation-specific
spec.policies.promptGuard.response[].webhook.forwardHeaderMatches[].value <string> (required) min=1 max=4096	# Value of the HTTP header to match
spec.policies.promptGuard.response[].webhook.headers <map[string]string>	# CEL-computed headers to include in webhook requests.
spec.policies.promptGuard.streaming <string> enum=Enabled	# Apply prompt guards to streaming responses and realtime websocket messages. Defaults to disabled to preserve streaming throughput unless explicitly enabled.
spec.policies.tls <object>	# TLS settings for connections to this model provider.
spec.policies.tls.alpnProtocols <[]string> min=1 max=16	# Application-Layer Protocol Negotiation (`ALPN`) value to use in the TLS handshake. If not present, defaults to `["h2", "http/1.1"]`.
spec.policies.tls.caCertificateRefs <[]object> max=1	# CA certificate `ConfigMap` to use to verify the server certificate. If unset, the system's trusted certificates are used.
spec.policies.tls.caCertificateRefs[].name <string> default=""	# Name of the referent
spec.policies.tls.insecureSkipVerify <string> enum=All|Hostname	# Originates TLS but skips verification of the backend's certificate WARNING: insecure; only use if the risks are understood Modes: * `All` disables all TLS verification * `Hostname` trusts the CA certificate but ignores hostname/SAN mismatches. Still insecure; prefer `verifySubjectAltNames` where possible.
spec.policies.tls.keyExchangeGroups <[]string>	# Ordered list of key exchange groups for a TLS connection. For example: `X25519_MLKEM768,X25519`.
spec.policies.tls.mtlsCertificateRef <[]object> max=1	# Enables mutual TLS to the backend using `tls.key` and `tls.crt` from the referenced credential source (defaulting to a Kubernetes `Secret`). An optional `ca.cert`, if present, verifies the server certificate, but `caCertificateRefs` takes priority. If unspecified, no client certificate is used.
spec.policies.tls.mtlsCertificateRef[].group <string>	# API group of the referenced credential; empty selects the core API group
spec.policies.tls.mtlsCertificateRef[].kind <string>	# Kind of the referenced credential; empty defaults to `Secret`
spec.policies.tls.mtlsCertificateRef[].name <string> (required) min=1 max=253	# Name of the referenced credential
spec.policies.tls.sni <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Server Name Indicator (`SNI`) to use in the TLS handshake. If unset, the `SNI` is automatically set based on the destination hostname.
spec.policies.tls.verifySubjectAltNames <[]string> min=1 max=16	# Subject Alternative Names (`SAN`) to verify in the server certificate. If not present, the destination hostname is automatically used.
spec.policies.transformations <[]object> min=1 max=64	# CEL transformations applied to fields in the provider request body.
spec.policies.transformations[].expression <string> (required) min=1 max=16384	# CEL expression used to compute the field value.
spec.policies.transformations[].field <string> (required) min=1 max=256	# Name of the field to set.
spec.policies.tunnel <object>	# Proxy tunnel used to reach this model provider.
spec.policies.tunnel.backendRef <object> (required)	# Proxy server to reach. Supported types: `Service` and `Backend`.
spec.policies.tunnel.backendRef.group <string> default="" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group of the referent, for example `gateway.networking.k8s.io`. Empty selects the core API group
spec.policies.tunnel.backendRef.kind <string> default="Service" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kubernetes resource kind of the referent, for example `Service`. Defaults to `Service`
spec.policies.tunnel.backendRef.name <string> (required) min=1 max=253	# Name is the name of the referent.
spec.policies.tunnel.backendRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the local namespace. A cross-namespace reference requires a ReferenceGrant in the referent namespace
spec.policies.tunnel.backendRef.port <integer> format=int32 min=1 max=65535	# Destination port number. Required when the referent is a Kubernetes `Service`
spec.provider <string> enum=Anthropic|Azure|Baseten|Bedrock|Cerebras|Cohere|Custom|Deepinfra|Deepseek|Fireworks|Gemini|Groq|Huggingface|Mistral|Ollama|OpenAI|Openrouter|TogetherAI|VertexAI|XAI	# Provider serving this concrete model. Provider-specific configuration is set by the corresponding field below when needed.
spec.vertexai <object>	# Provider-specific settings for Vertex AI.
spec.vertexai.projectId <string> (required) min=1 max=64	# The ID of the Google Cloud Project that you use for the Vertex AI.
spec.vertexai.region <string> default="global" min=1 max=64	# The location of the Google Cloud Project that you use for the Vertex AI. Special values: `global` uses the global endpoint, while `us` and `eu` use restricted multi-region endpoints. Other values are treated as regional locations. Defaults to `global` if not specified.
spec.virtualModel <object>	# Request-time routing among concrete AgentgatewayModel resources.
spec.virtualModel.conditional <object>	# Ordered condition-based model selection.
spec.virtualModel.conditional.targets <[]object> (required) min=1 max=64	# Concrete model targets evaluated in order. The first matching condition is selected. One final target may omit when to act as the fallback.
spec.virtualModel.conditional.targets[].model <string> min=1 max=1024	# Concrete model name selected through the referenced model. It is required when modelRef points to a wildcard match.model. When omitted, the referenced model's exact effective match.model is used.
spec.virtualModel.conditional.targets[].modelRef <object> (required)	# Same-namespace AgentgatewayModel resource selected by this target.
spec.virtualModel.conditional.targets[].modelRef.name <string> default=""	# Name of the referent
spec.virtualModel.conditional.targets[].when <string> min=1 max=16384	# CEL expression that must evaluate to true for this target to be selected. Omit only on the final fallback target.
spec.virtualModel.failover <object>	# Priority-based model selection with failover between priority groups.
spec.virtualModel.failover.targets <[]object> (required) min=1 max=64	# Concrete model targets grouped by priority. Lower values are preferred.
spec.virtualModel.failover.targets[].model <string> min=1 max=1024	# Concrete model name selected through the referenced model. It is required when modelRef points to a wildcard match.model. When omitted, the referenced model's exact effective match.model is used.
spec.virtualModel.failover.targets[].modelRef <object> (required)	# Same-namespace AgentgatewayModel resource selected by this target.
spec.virtualModel.failover.targets[].modelRef.name <string> default=""	# Name of the referent
spec.virtualModel.failover.targets[].priority <integer> (required) format=int32 min=0 max=1000000	# Priority of this target. Lower values are preferred. Targets at the same priority are selected using a score that considers health and latency. The next priority is used only when every target at this priority is degraded. Configure policies.health on concrete target models to customize degradation and eviction behavior.
spec.virtualModel.weighted <object>	# Weight-based model selection.
spec.virtualModel.weighted.targets <[]object> (required) min=1 max=64	# Concrete model targets and their relative weights.
spec.virtualModel.weighted.targets[].model <string> min=1 max=1024	# Concrete model name selected through the referenced model. It is required when modelRef points to a wildcard match.model. When omitted, the referenced model's exact effective match.model is used.
spec.virtualModel.weighted.targets[].modelRef <object> (required)	# Same-namespace AgentgatewayModel resource selected by this target.
spec.virtualModel.weighted.targets[].modelRef.name <string> default=""	# Name of the referent
spec.virtualModel.weighted.targets[].weight <integer> default=1 format=int32 min=1 max=1000000	# Relative traffic weight. Defaults to 1.
spec.visibility <string> enum=Internal|Public default="Public"	# Controls whether clients can request this model directly. Internal models can only be selected by virtual models. Defaults to Public.
status <object>	# Current model attachment status.
status.parents <[]object> max=16	# Status for each Gateway parent to which this model is attached.
status.parents[].conditions <[]object> (required) min=1 max=8	# Conditions describes the status of the route with respect to the Gateway. Note that the route's availability is also subject to the Gateway's own status conditions and listener status. If the Route's ParentRef specifies an existing Gateway that supports Routes of this kind AND that Gateway's controller has sufficient access, then that Gateway's controller MUST set the "Accepted" condition on the Route, to indicate whether the route has been accepted or rejected by the Gateway, and why. A Route MUST be considered "Accepted" if at least one of the Route's rules is implemented by the Gateway. There are a number of cases where the "Accepted" condition may not be set due to lack of controller visibility, that includes when: * The Route refers to a nonexistent parent. * The Route is of a type that the controller does not support. * The Route is in a namespace to which the controller does not have access. <gateway:util:excludeFromCRD> Notes for implementors: Conditions are a listType `map`, which means that they function like a map with a key of the `type` field _in the k8s apiserver_. This means that implementations must obey some rules when updating this section. * Implementations MUST perform a read-modify-write cycle on this field before modifying it. That is, when modifying this field, implementations must be confident they have fetched the most recent version of this field, and ensure that changes they make are on that recent version. * Implementations MUST NOT remove or reorder Conditions that they are not directly responsible for. For example, if an implementation sees a Condition with type `special.io/SomeField`, it MUST NOT remove, change or update that Condition. * Implementations MUST always _merge_ changes into Conditions of the same Type, rather than creating more than one Condition of the same Type. * Implementations MUST always update the `observedGeneration` field of the Condition to the `metadata.generation` of the Gateway at the time of update creation. * If the `observedGeneration` of a Condition is _greater than_ the value the implementation knows about, then it MUST NOT perform the update on that Condition, but must wait for a future reconciliation and status update. (The assumption is that the implementation's copy of the object is stale and an update will be re-triggered if relevant.) </gateway:util:excludeFromCRD>
status.parents[].conditions[].lastTransitionTime <string> (required) format=date-time	# lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable.
status.parents[].conditions[].message <string> (required) max=32768	# message is a human readable message indicating details about the transition. This may be an empty string.
status.parents[].conditions[].observedGeneration <integer> format=int64 min=0	# observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.parents[].conditions[].reason <string> (required) pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$" min=1 max=1024	# reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
status.parents[].conditions[].status <string> (required) enum=True|False|Unknown	# status of the condition, one of True, False, Unknown.
status.parents[].conditions[].type <string> (required) pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$" max=316	# type of condition in CamelCase or in foo.example.com/CamelCase.
status.parents[].controllerName <string> (required) pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\\/[A-Za-z0-9\\/\\-._~%!$&'()*+,;=:]+$" min=1 max=253	# ControllerName is a domain/path string that indicates the name of the controller that wrote this status. This corresponds with the controllerName field on GatewayClass. Example: "example.net/gateway-controller". The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are valid Kubernetes names (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names). Controllers MUST populate this field when writing status. Controllers should ensure that entries to status populated with their ControllerName are cleaned up when they are no longer necessary.
status.parents[].parentRef <object> (required)	# ParentRef corresponds with a ParentRef in the spec that this RouteParentStatus struct describes the status of.
status.parents[].parentRef.group <string> default="gateway.networking.k8s.io" pattern="^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" max=253	# Group is the group of the referent. When unspecified, "gateway.networking.k8s.io" is inferred. To set the core API group (such as for a "Service" kind referent), Group must be explicitly set to "" (empty string). Support: Core
status.parents[].parentRef.kind <string> default="Gateway" pattern="^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$" min=1 max=63	# Kind is kind of the referent. There are two kinds of parent resources with "Core" support: * Gateway (Gateway conformance profile) * Service (Mesh conformance profile, ClusterIP Services only) Support for other resources is Implementation-Specific.
status.parents[].parentRef.name <string> (required) min=1 max=253	# Name is the name of the referent. Support: Core
status.parents[].parentRef.namespace <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" min=1 max=63	# Namespace of the referent. Defaults to the Route's local namespace. Cross-namespace references must be explicitly allowed, for example via ReferenceGrant. Support: Core
status.parents[].parentRef.port <integer> format=int32 min=1 max=65535	# Port this Route targets on the parent, interpreted per parent kind (for example a Gateway listener port or Service port). Support: Extended
status.parents[].parentRef.sectionName <string> pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$" min=1 max=253	# Name of a section within the target resource, for example a Gateway Listener name or Service port name. Empty references the entire resource. Support: Core
