# schema source: Fluent Operator v3.9.0 https://github.com/fluent/fluent-operator
apiVersion <string> enum=fluentbit.fluent.io/v1alpha2
kind <string> enum=ClusterParser (cluster-scoped)
metadata.name <string> (required)
spec <object>	# ParserSpec defines the desired state of ClusterParser
spec.decoders <[]object>	# Decoders are a built-in feature available through the Parsers file, each Parser definition can optionally set one or multiple decoders. There are two type of decoders type: Decode_Field and Decode_Field_As.
spec.decoders[].decodeField <string>	# If the content can be decoded in a structured message, append that structure message (keys and values) to the original log message.
spec.decoders[].decodeFieldAs <string>	# Any content decoded (unstructured or structured) will be replaced in the same key/value, no extra keys are added.
spec.json <object>	# JSON defines json parser configuration.
spec.json.timeFormat <string>	# Time_Format, eg. %Y-%m-%dT%H:%M:%S %z
spec.json.timeKeep <boolean>	# Time_Keep
spec.json.timeKey <string>	# Time_Key
spec.logfmt <object>	# Logfmt defines logfmt parser configuration.
spec.logfmt.timeFormat <string>	# Time_Format, eg. %Y-%m-%dT%H:%M:%S %z
spec.logfmt.timeKeep <boolean>	# Time_Keep
spec.logfmt.timeKey <string>	# Time_Key
spec.ltsv <object>	# LTSV defines ltsv parser configuration.
spec.ltsv.timeFormat <string>	# Time_Format, eg. %Y-%m-%dT%H:%M:%S %z
spec.ltsv.timeKeep <boolean>	# Time_Keep
spec.ltsv.timeKey <string>	# Time_Key
spec.ltsv.types <string>
spec.regex <object>	# Regex defines regex parser configuration.
spec.regex.regex <string>
spec.regex.timeFormat <string>	# Time_Format, eg. %Y-%m-%dT%H:%M:%S %z
spec.regex.timeKeep <boolean>	# Time_Keep
spec.regex.timeKey <string>	# Time_Key
spec.regex.timeOffset <string>	# Time_Offset, eg. +0200
spec.regex.types <string>
