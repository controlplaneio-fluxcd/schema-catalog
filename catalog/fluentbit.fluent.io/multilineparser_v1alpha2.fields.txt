# schema source: Fluent Operator v3.9.0 https://github.com/fluent/fluent-operator
apiVersion <string> enum=fluentbit.fluent.io/v1alpha2
kind <string> enum=MultilineParser
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>
spec.flushTimeout <integer> default=5000	# Timeout in milliseconds to flush a non-terminated multiline buffer. Default is set to 5 seconds.
spec.keyContent <string>	# For an incoming structured message, specify the key that contains the data that should be processed by the regular expression and possibly concatenated.
spec.parser <string>	# Name of a pre-defined parser that must be applied to the incoming content before applying the regex rule. If no parser is defined, it's assumed that's a raw text and not a structured message.
spec.rules <[]object>	# Configure a rule to match a multiline pattern. The rule has a specific format described below. Multiple rules can be defined.
spec.rules[].next <string> (required)
spec.rules[].regex <string> (required)
spec.rules[].start <string> (required)
spec.type <string> enum=regex default="regex"	# Set the multiline mode, for now, we support the type regex.
