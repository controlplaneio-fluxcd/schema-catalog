# schema source: Sealed Secrets v0.38.4 https://github.com/bitnami-labs/sealed-secrets
apiVersion <string> enum=bitnami.com/v1alpha1
kind <string> enum=SealedSecret
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# SealedSecretSpec is the specification of a SealedSecret.
spec.data <string> format=byte	# Data is deprecated and will be removed eventually. Use per-value EncryptedData instead.
spec.encryptedData <map[string]string> (required)
spec.template <object>	# Template defines the structure of the Secret that will be created from this sealed secret.
spec.template.data <map[string]string>	# Keys that should be templated using decrypted data.
spec.template.immutable <boolean>	# Immutable, if set to true, ensures that data stored in the Secret cannot be updated (only object metadata can be modified). If not set to true, the field can be modified at any time. Defaulted to nil.
spec.template.metadata <object>	# Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata
spec.template.metadata.annotations <map[string]string>
spec.template.metadata.finalizers <[]string>
spec.template.metadata.labels <map[string]string>
spec.template.metadata.name <string>
spec.template.metadata.namespace <string>
spec.template.type <string>	# Used to facilitate programmatic handling of secret data.
status <object>	# SealedSecretStatus is the most recently observed status of the SealedSecret.
status.conditions <[]object>	# Represents the latest available observations of a sealed secret's current state.
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].lastUpdateTime <string> format=date-time	# The last time this condition was updated.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition for a sealed secret. Valid values for "Synced": "True", "False", or "Unknown".
status.conditions[].type <string> (required)	# Type of condition for a sealed secret. Valid value: "Synced"
status.observedGeneration <integer> format=int64	# ObservedGeneration reflects the generation most recently observed by the sealed-secrets controller.
