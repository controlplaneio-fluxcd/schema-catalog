# schema source: Upbound AWS Provider v2.6.0 https://github.com/crossplane-contrib/provider-upjet-aws
apiVersion <string> enum=autoscaling.aws.upbound.io/v1beta2
kind <string> enum=LaunchConfiguration (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)	# LaunchConfigurationSpec defines the desired state of LaunchConfiguration
spec.deletionPolicy <string> enum=Orphan|Delete default="Delete"	# DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either "Delete" or "Orphan" the external resource. This field is planned to be deprecated in favor of the ManagementPolicies field in a future release. Currently, both could be set independently and non-default values would be honored if the feature flag is enabled. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
spec.forProvider <object> (required)
spec.forProvider.associatePublicIpAddress <boolean>	# Associate a public ip address with an instance in a VPC.
spec.forProvider.ebsBlockDevice <[]object>	# Additional EBS block devices to attach to the instance. See Block Devices below for details.
spec.forProvider.ebsBlockDevice[].deleteOnTermination <boolean>	# Whether the volume should be destroyed on instance termination (Default: true).
spec.forProvider.ebsBlockDevice[].deviceName <string>	# The name of the device to mount.
spec.forProvider.ebsBlockDevice[].encrypted <boolean>	# Whether the volume should be encrypted or not. Defaults to false.
spec.forProvider.ebsBlockDevice[].iops <number>	# The amount of provisioned IOPS. This must be set with a volume_type of "io1".
spec.forProvider.ebsBlockDevice[].noDevice <boolean>	# Whether the device in the block device mapping of the AMI is suppressed.
spec.forProvider.ebsBlockDevice[].snapshotId <string>	# The Snapshot ID to mount.
spec.forProvider.ebsBlockDevice[].throughput <number>	# The throughput (MiBps) to provision for a gp3 volume.
spec.forProvider.ebsBlockDevice[].volumeSize <number>	# The size of the volume in gigabytes.
spec.forProvider.ebsBlockDevice[].volumeType <string>	# The type of volume. Can be standard, gp2, gp3, st1, sc1 or io1.
spec.forProvider.ebsOptimized <boolean>	# If true, the launched EC2 instance will be EBS-optimized.
spec.forProvider.enableMonitoring <boolean>	# Enables/disables detailed monitoring. This is enabled by default.
spec.forProvider.ephemeralBlockDevice <[]object>	# Customize Ephemeral (also known as "Instance Store") volumes on the instance. See Block Devices below for details.
spec.forProvider.ephemeralBlockDevice[].deviceName <string>	# The name of the block device to mount on the instance.
spec.forProvider.ephemeralBlockDevice[].noDevice <boolean>	# Whether the device in the block device mapping of the AMI is suppressed.
spec.forProvider.ephemeralBlockDevice[].virtualName <string>	# The Instance Store Device Name.
spec.forProvider.iamInstanceProfile <string>	# The name attribute of the IAM instance profile to associate with launched instances.
spec.forProvider.imageId <string>	# The EC2 image ID to launch.
spec.forProvider.instanceType <string>	# The size of instance to launch.
spec.forProvider.keyName <string>	# The key name that should be used for the instance.
spec.forProvider.metadataOptions <object>	# The metadata options for the instance.
spec.forProvider.metadataOptions.httpEndpoint <string>	# The state of the metadata service: enabled, disabled.
spec.forProvider.metadataOptions.httpPutResponseHopLimit <number>	# The desired HTTP PUT response hop limit for instance metadata requests.
spec.forProvider.metadataOptions.httpTokens <string>	# If session tokens are required: optional, required.
spec.forProvider.placementTenancy <string>	# The tenancy of the instance. Valid values are default or dedicated, see AWS's Create Launch Configuration for more details.
spec.forProvider.region <string> (required)	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
spec.forProvider.rootBlockDevice <object>	# Customize details about the root block device of the instance. See Block Devices below for details.
spec.forProvider.rootBlockDevice.deleteOnTermination <boolean>	# Whether the volume should be destroyed on instance termination. Defaults to true.
spec.forProvider.rootBlockDevice.encrypted <boolean>	# Whether the volume should be encrypted or not. Defaults to false.
spec.forProvider.rootBlockDevice.iops <number>	# The amount of provisioned IOPS. This must be set with a volume_type of io1.
spec.forProvider.rootBlockDevice.throughput <number>	# The throughput (MiBps) to provision for a gp3 volume.
spec.forProvider.rootBlockDevice.volumeSize <number>	# The size of the volume in gigabytes.
spec.forProvider.rootBlockDevice.volumeType <string>	# The type of volume. Can be standard, gp2, gp3, st1, sc1 or io1.
spec.forProvider.securityGroups <[]string>	# A list of associated security group IDS.
spec.forProvider.spotPrice <string>	# The maximum price to use for reserving spot instances.
spec.forProvider.userData <string>	# The user data to provide when launching the instance. Do not pass gzip-compressed data via this argument; see user_data_base64 instead.
spec.forProvider.userDataBase64 <string>	# Can be used instead of user_data to pass base64-encoded binary data directly. Use this instead of user_data whenever the value is not a valid UTF-8 string. For example, gzip-encoded user data must be base64-encoded and passed via this argument to avoid corruption.
spec.initProvider <object>	# THIS IS A BETA FIELD. It will be honored unless the Management Policies feature flag is disabled. InitProvider holds the same fields as ForProvider, with the exception of Identifier and other resource reference fields. The fields that are in InitProvider are merged into ForProvider when the resource is created. The same fields are also added to the terraform ignore_changes hook, to avoid updating them after creation. This is useful for fields that are required on creation, but we do not desire to update them after creation, for example because of an external controller is managing them, like an autoscaler.
spec.initProvider.associatePublicIpAddress <boolean>	# Associate a public ip address with an instance in a VPC.
spec.initProvider.ebsBlockDevice <[]object>	# Additional EBS block devices to attach to the instance. See Block Devices below for details.
spec.initProvider.ebsBlockDevice[].deleteOnTermination <boolean>	# Whether the volume should be destroyed on instance termination (Default: true).
spec.initProvider.ebsBlockDevice[].deviceName <string>	# The name of the device to mount.
spec.initProvider.ebsBlockDevice[].encrypted <boolean>	# Whether the volume should be encrypted or not. Defaults to false.
spec.initProvider.ebsBlockDevice[].iops <number>	# The amount of provisioned IOPS. This must be set with a volume_type of "io1".
spec.initProvider.ebsBlockDevice[].noDevice <boolean>	# Whether the device in the block device mapping of the AMI is suppressed.
spec.initProvider.ebsBlockDevice[].snapshotId <string>	# The Snapshot ID to mount.
spec.initProvider.ebsBlockDevice[].throughput <number>	# The throughput (MiBps) to provision for a gp3 volume.
spec.initProvider.ebsBlockDevice[].volumeSize <number>	# The size of the volume in gigabytes.
spec.initProvider.ebsBlockDevice[].volumeType <string>	# The type of volume. Can be standard, gp2, gp3, st1, sc1 or io1.
spec.initProvider.ebsOptimized <boolean>	# If true, the launched EC2 instance will be EBS-optimized.
spec.initProvider.enableMonitoring <boolean>	# Enables/disables detailed monitoring. This is enabled by default.
spec.initProvider.ephemeralBlockDevice <[]object>	# Customize Ephemeral (also known as "Instance Store") volumes on the instance. See Block Devices below for details.
spec.initProvider.ephemeralBlockDevice[].deviceName <string>	# The name of the block device to mount on the instance.
spec.initProvider.ephemeralBlockDevice[].noDevice <boolean>	# Whether the device in the block device mapping of the AMI is suppressed.
spec.initProvider.ephemeralBlockDevice[].virtualName <string>	# The Instance Store Device Name.
spec.initProvider.iamInstanceProfile <string>	# The name attribute of the IAM instance profile to associate with launched instances.
spec.initProvider.imageId <string>	# The EC2 image ID to launch.
spec.initProvider.instanceType <string>	# The size of instance to launch.
spec.initProvider.keyName <string>	# The key name that should be used for the instance.
spec.initProvider.metadataOptions <object>	# The metadata options for the instance.
spec.initProvider.metadataOptions.httpEndpoint <string>	# The state of the metadata service: enabled, disabled.
spec.initProvider.metadataOptions.httpPutResponseHopLimit <number>	# The desired HTTP PUT response hop limit for instance metadata requests.
spec.initProvider.metadataOptions.httpTokens <string>	# If session tokens are required: optional, required.
spec.initProvider.placementTenancy <string>	# The tenancy of the instance. Valid values are default or dedicated, see AWS's Create Launch Configuration for more details.
spec.initProvider.rootBlockDevice <object>	# Customize details about the root block device of the instance. See Block Devices below for details.
spec.initProvider.rootBlockDevice.deleteOnTermination <boolean>	# Whether the volume should be destroyed on instance termination. Defaults to true.
spec.initProvider.rootBlockDevice.encrypted <boolean>	# Whether the volume should be encrypted or not. Defaults to false.
spec.initProvider.rootBlockDevice.iops <number>	# The amount of provisioned IOPS. This must be set with a volume_type of io1.
spec.initProvider.rootBlockDevice.throughput <number>	# The throughput (MiBps) to provision for a gp3 volume.
spec.initProvider.rootBlockDevice.volumeSize <number>	# The size of the volume in gigabytes.
spec.initProvider.rootBlockDevice.volumeType <string>	# The type of volume. Can be standard, gp2, gp3, st1, sc1 or io1.
spec.initProvider.securityGroups <[]string>	# A list of associated security group IDS.
spec.initProvider.spotPrice <string>	# The maximum price to use for reserving spot instances.
spec.initProvider.userData <string>	# The user data to provide when launching the instance. Do not pass gzip-compressed data via this argument; see user_data_base64 instead.
spec.initProvider.userDataBase64 <string>	# Can be used instead of user_data to pass base64-encoded binary data directly. Use this instead of user_data whenever the value is not a valid UTF-8 string. For example, gzip-encoded user data must be base64-encoded and passed via this argument to avoid corruption.
spec.managementPolicies <[]string> default=["*"]	# THIS IS A BETA FIELD. It is on by default but can be opted out through a Crossplane feature flag. ManagementPolicies specify the array of actions Crossplane is allowed to take on the managed and external resources. This field is planned to replace the DeletionPolicy field in a future release. Currently, both could be set independently and non-default values would be honored if the feature flag is enabled. If both are custom, the DeletionPolicy field will be ignored. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223 and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
spec.providerConfigRef <object> default={"name":"default"}	# ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
spec.providerConfigRef.name <string> (required)	# Name of the referenced object.
spec.providerConfigRef.policy <object>	# Policies for referencing.
spec.providerConfigRef.policy.resolution <string> enum=Required|Optional default="Required"	# Resolution specifies whether resolution of this reference is required. The default is 'Required', which means the reconcile will fail if the reference cannot be resolved. 'Optional' means this reference will be a no-op if it cannot be resolved.
spec.providerConfigRef.policy.resolve <string> enum=Always|IfNotPresent	# Resolve specifies when this reference should be resolved. The default is 'IfNotPresent', which will attempt to resolve the reference only when the corresponding field is not present. Use 'Always' to resolve the reference on every reconcile.
spec.writeConnectionSecretToRef <object>	# WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
spec.writeConnectionSecretToRef.name <string> (required)	# Name of the secret.
spec.writeConnectionSecretToRef.namespace <string> (required)	# Namespace of the secret.
status <object>	# LaunchConfigurationStatus defines the observed state of LaunchConfiguration.
status.atProvider <object>
status.atProvider.arn <string>	# The Amazon Resource Name of the launch configuration.
status.atProvider.associatePublicIpAddress <boolean>	# Associate a public ip address with an instance in a VPC.
status.atProvider.ebsBlockDevice <[]object>	# Additional EBS block devices to attach to the instance. See Block Devices below for details.
status.atProvider.ebsBlockDevice[].deleteOnTermination <boolean>	# Whether the volume should be destroyed on instance termination (Default: true).
status.atProvider.ebsBlockDevice[].deviceName <string>	# The name of the device to mount.
status.atProvider.ebsBlockDevice[].encrypted <boolean>	# Whether the volume should be encrypted or not. Defaults to false.
status.atProvider.ebsBlockDevice[].iops <number>	# The amount of provisioned IOPS. This must be set with a volume_type of "io1".
status.atProvider.ebsBlockDevice[].noDevice <boolean>	# Whether the device in the block device mapping of the AMI is suppressed.
status.atProvider.ebsBlockDevice[].snapshotId <string>	# The Snapshot ID to mount.
status.atProvider.ebsBlockDevice[].throughput <number>	# The throughput (MiBps) to provision for a gp3 volume.
status.atProvider.ebsBlockDevice[].volumeSize <number>	# The size of the volume in gigabytes.
status.atProvider.ebsBlockDevice[].volumeType <string>	# The type of volume. Can be standard, gp2, gp3, st1, sc1 or io1.
status.atProvider.ebsOptimized <boolean>	# If true, the launched EC2 instance will be EBS-optimized.
status.atProvider.enableMonitoring <boolean>	# Enables/disables detailed monitoring. This is enabled by default.
status.atProvider.ephemeralBlockDevice <[]object>	# Customize Ephemeral (also known as "Instance Store") volumes on the instance. See Block Devices below for details.
status.atProvider.ephemeralBlockDevice[].deviceName <string>	# The name of the block device to mount on the instance.
status.atProvider.ephemeralBlockDevice[].noDevice <boolean>	# Whether the device in the block device mapping of the AMI is suppressed.
status.atProvider.ephemeralBlockDevice[].virtualName <string>	# The Instance Store Device Name.
status.atProvider.iamInstanceProfile <string>	# The name attribute of the IAM instance profile to associate with launched instances.
status.atProvider.id <string>	# The ID of the launch configuration.
status.atProvider.imageId <string>	# The EC2 image ID to launch.
status.atProvider.instanceType <string>	# The size of instance to launch.
status.atProvider.keyName <string>	# The key name that should be used for the instance.
status.atProvider.metadataOptions <object>	# The metadata options for the instance.
status.atProvider.metadataOptions.httpEndpoint <string>	# The state of the metadata service: enabled, disabled.
status.atProvider.metadataOptions.httpPutResponseHopLimit <number>	# The desired HTTP PUT response hop limit for instance metadata requests.
status.atProvider.metadataOptions.httpTokens <string>	# If session tokens are required: optional, required.
status.atProvider.placementTenancy <string>	# The tenancy of the instance. Valid values are default or dedicated, see AWS's Create Launch Configuration for more details.
status.atProvider.region <string>	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
status.atProvider.rootBlockDevice <object>	# Customize details about the root block device of the instance. See Block Devices below for details.
status.atProvider.rootBlockDevice.deleteOnTermination <boolean>	# Whether the volume should be destroyed on instance termination. Defaults to true.
status.atProvider.rootBlockDevice.encrypted <boolean>	# Whether the volume should be encrypted or not. Defaults to false.
status.atProvider.rootBlockDevice.iops <number>	# The amount of provisioned IOPS. This must be set with a volume_type of io1.
status.atProvider.rootBlockDevice.throughput <number>	# The throughput (MiBps) to provision for a gp3 volume.
status.atProvider.rootBlockDevice.volumeSize <number>	# The size of the volume in gigabytes.
status.atProvider.rootBlockDevice.volumeType <string>	# The type of volume. Can be standard, gp2, gp3, st1, sc1 or io1.
status.atProvider.securityGroups <[]string>	# A list of associated security group IDS.
status.atProvider.spotPrice <string>	# The maximum price to use for reserving spot instances.
status.atProvider.userData <string>	# The user data to provide when launching the instance. Do not pass gzip-compressed data via this argument; see user_data_base64 instead.
status.atProvider.userDataBase64 <string>	# Can be used instead of user_data to pass base64-encoded binary data directly. Use this instead of user_data whenever the value is not a valid UTF-8 string. For example, gzip-encoded user data must be base64-encoded and passed via this argument to avoid corruption.
status.conditions <[]object>	# Conditions of the resource.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time this condition transitioned from one status to another.
status.conditions[].message <string>	# A Message containing details about this condition's last transition from one status to another, if any.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# A Reason for this condition's last transition from one status to another.
status.conditions[].status <string> (required)	# Status of this condition; is it currently True, False, or Unknown?
status.conditions[].type <string> (required)	# Type of this condition. At most one of each condition type may apply to a resource at any point in time.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the latest metadata.generation which resulted in either a ready state, or stalled due to error it can not recover from without human intervention.
