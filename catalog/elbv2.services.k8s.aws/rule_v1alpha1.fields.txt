# schema source: AWS Elastic Load Balancing v2 Controller v1.5.2 https://github.com/aws-controllers-k8s/elbv2-controller
apiVersion <string> enum=elbv2.services.k8s.aws/v1alpha1
kind <string> enum=Rule
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# RuleSpec defines the desired state of Rule. Information about a rule.
spec.actions <[]object> (required)	# The actions.
spec.actions[].authenticateCognitoConfig <object>	# Request parameters to use when integrating with Amazon Cognito to authenticate users.
spec.actions[].authenticateCognitoConfig.authenticationRequestExtraParams <map[string]string>
spec.actions[].authenticateCognitoConfig.onUnauthenticatedRequest <string>
spec.actions[].authenticateCognitoConfig.scope <string>
spec.actions[].authenticateCognitoConfig.sessionCookieName <string>
spec.actions[].authenticateCognitoConfig.sessionTimeout <integer> format=int64
spec.actions[].authenticateCognitoConfig.userPoolARN <string>
spec.actions[].authenticateCognitoConfig.userPoolClientID <string>
spec.actions[].authenticateCognitoConfig.userPoolDomain <string>
spec.actions[].authenticateOIDCConfig <object>	# Request parameters when using an identity provider (IdP) that is compliant with OpenID Connect (OIDC) to authenticate users.
spec.actions[].authenticateOIDCConfig.authenticationRequestExtraParams <map[string]string>
spec.actions[].authenticateOIDCConfig.authorizationEndpoint <string>
spec.actions[].authenticateOIDCConfig.clientID <string>
spec.actions[].authenticateOIDCConfig.clientSecret <string>
spec.actions[].authenticateOIDCConfig.issuer <string>
spec.actions[].authenticateOIDCConfig.onUnauthenticatedRequest <string>
spec.actions[].authenticateOIDCConfig.scope <string>
spec.actions[].authenticateOIDCConfig.sessionCookieName <string>
spec.actions[].authenticateOIDCConfig.sessionTimeout <integer> format=int64
spec.actions[].authenticateOIDCConfig.tokenEndpoint <string>
spec.actions[].authenticateOIDCConfig.useExistingClientSecret <boolean>
spec.actions[].authenticateOIDCConfig.userInfoEndpoint <string>
spec.actions[].fixedResponseConfig <object>	# Information about an action that returns a custom HTTP response.
spec.actions[].fixedResponseConfig.contentType <string>
spec.actions[].fixedResponseConfig.messageBody <string>
spec.actions[].fixedResponseConfig.statusCode <string>
spec.actions[].forwardConfig <object>	# Information about a forward action.
spec.actions[].forwardConfig.targetGroupStickinessConfig <object>	# Information about the target group stickiness for a rule.
spec.actions[].forwardConfig.targetGroupStickinessConfig.durationSeconds <integer> format=int64
spec.actions[].forwardConfig.targetGroupStickinessConfig.enabled <boolean>
spec.actions[].forwardConfig.targetGroups <[]object>
spec.actions[].forwardConfig.targetGroups[].targetGroupARN <string>
spec.actions[].forwardConfig.targetGroups[].targetGroupRef <object>	# Reference field for TargetGroupARN
spec.actions[].forwardConfig.targetGroups[].targetGroupRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.actions[].forwardConfig.targetGroups[].targetGroupRef.from.name <string>
spec.actions[].forwardConfig.targetGroups[].targetGroupRef.from.namespace <string>
spec.actions[].forwardConfig.targetGroups[].weight <integer> format=int64
spec.actions[].order <integer> format=int64
spec.actions[].redirectConfig <object>	# Information about a redirect action. A URI consists of the following components: protocol://hostname:port/path?query. You must modify at least one of the following components to avoid a redirect loop: protocol, hostname, port, or path. Any components that you do not modify retain their original values. You can reuse URI components using the following reserved keywords: * #{protocol} * #{host} * #{port} * #{path} (the leading "/" is removed) * #{query} For example, you can change the path to "/new/#{path}", the hostname to "example.#{host}", or the query to "#{query}&value=xyz".
spec.actions[].redirectConfig.host <string>
spec.actions[].redirectConfig.path <string>
spec.actions[].redirectConfig.port <string>
spec.actions[].redirectConfig.protocol <string>
spec.actions[].redirectConfig.query <string>
spec.actions[].redirectConfig.statusCode <string>
spec.actions[].targetGroupARN <string>
spec.actions[].targetGroupRef <object>	# Reference field for TargetGroupARN
spec.actions[].targetGroupRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.actions[].targetGroupRef.from.name <string>
spec.actions[].targetGroupRef.from.namespace <string>
spec.actions[].type <string>
spec.conditions <[]object> (required)	# The conditions.
spec.conditions[].field <string>
spec.conditions[].hostHeaderConfig <object>	# Information about a host header condition.
spec.conditions[].hostHeaderConfig.values <[]string>
spec.conditions[].httpHeaderConfig <object>	# Information about an HTTP header condition. There is a set of standard HTTP header fields. You can also define custom HTTP header fields.
spec.conditions[].httpHeaderConfig.httpHeaderName <string>
spec.conditions[].httpHeaderConfig.values <[]string>
spec.conditions[].httpRequestMethodConfig <object>	# Information about an HTTP method condition. HTTP defines a set of request methods, also referred to as HTTP verbs. For more information, see the HTTP Method Registry (https://www.iana.org/assignments/http-methods/http-methods.xhtml). You can also define custom HTTP methods.
spec.conditions[].httpRequestMethodConfig.values <[]string>
spec.conditions[].pathPatternConfig <object>	# Information about a path pattern condition.
spec.conditions[].pathPatternConfig.values <[]string>
spec.conditions[].queryStringConfig <object>	# Information about a query string condition. The query string component of a URI starts after the first '?' character and is terminated by either a '#' character or the end of the URI. A typical query string contains key/value pairs separated by '&' characters. The allowed characters are specified by RFC 3986. Any character can be percentage encoded.
spec.conditions[].queryStringConfig.values <[]object>
spec.conditions[].queryStringConfig.values[].key <string>
spec.conditions[].queryStringConfig.values[].value <string>
spec.conditions[].sourceIPConfig <object>	# Information about a source IP condition. You can use this condition to route based on the IP address of the source that connects to the load balancer. If a client is behind a proxy, this is the IP address of the proxy not the IP address of the client.
spec.conditions[].sourceIPConfig.values <[]string>
spec.conditions[].values <[]string>
spec.listenerARN <string>	# The Amazon Resource Name (ARN) of the listener.
spec.listenerRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.listenerRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.listenerRef.from.name <string>
spec.listenerRef.from.namespace <string>
spec.priority <integer> (required) format=int64	# The rule priority. A listener can't have multiple rules with the same priority.
spec.tags <[]object>	# The tags to assign to the rule.
spec.tags[].key <string>
spec.tags[].value <string>
status <object>	# RuleStatus defines the observed state of Rule
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.isDefault <boolean>	# Indicates whether this is the default rule.
