# schema source: OpenFeature Operator v0.9.2 https://github.com/open-feature/open-feature-operator
apiVersion <string> enum=core.openfeature.dev/v1beta1
kind <string> enum=FeatureFlag
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# FeatureFlagSpec defines the desired state of FeatureFlag
spec.flagSpec <object>	# FlagSpec is the structured representation of the feature flag specification
spec.flagSpec.$evaluators <object (free-form)>
spec.flagSpec.flags <map[string]object> (required)
spec.flagSpec.flags.<key>.defaultVariant <string> (required)
spec.flagSpec.flags.<key>.metadata <object (free-form)>	# Metadata holds optional per-flag metadata. Additional metadata keys are preserved to support flagd metadata inheritance.
spec.flagSpec.flags.<key>.state <string> (required) enum=ENABLED|DISABLED
spec.flagSpec.flags.<key>.targeting <object (free-form)>	# Targeting is the json targeting rule
spec.flagSpec.flags.<key>.variants <object (free-form)> (required)
spec.flagSpec.metadata <object (free-form)>	# Metadata holds optional flag-set level metadata. Additional metadata keys are preserved to support flagd metadata inheritance.
status <object (free-form)>	# FeatureFlagStatus defines the observed state of FeatureFlag
