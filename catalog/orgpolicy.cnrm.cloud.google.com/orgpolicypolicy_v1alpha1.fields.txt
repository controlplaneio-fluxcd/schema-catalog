# schema source: GCP Config Connector v1.153.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=orgpolicy.cnrm.cloud.google.com/v1alpha1
kind <string> enum=OrgPolicyPolicy
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# OrgPolicyPolicySpec defines the desired state of OrgPolicyPolicy
spec.dryRunSpec <object>	# Dry-run policy. Audit-only policy, can be used to monitor how the policy would have impacted the existing and future resources if it's enforced.
spec.dryRunSpec.inheritFromParent <boolean>	# Determines the inheritance behavior for this policy. If `inherit_from_parent` is true, policy rules set higher up in the hierarchy (up to the closest root) are inherited and present in the effective policy. If it is false, then no rules are inherited, and this policy becomes the new root for evaluation. This field can be set only for policies which configure list constraints.
spec.dryRunSpec.reset <boolean>	# Ignores policies set above this resource and restores the `constraint_default` enforcement behavior of the specific constraint at this resource. This field can be set in policies for either list or boolean constraints. If set, `rules` must be empty and `inherit_from_parent` must be set to false.
spec.dryRunSpec.rules <[]object>	# In policies for boolean constraints, the following requirements apply: - There must be one and only one policy rule where condition is unset. - Boolean policy rules with conditions must set `enforced` to the opposite of the policy rule without a condition. - During policy evaluation, policy rules with conditions that are true for a target resource take precedence.
spec.dryRunSpec.rules[].allowAll <boolean>	# Setting this to true means that all values are allowed. This field can be set only in policies for list constraints.
spec.dryRunSpec.rules[].condition <object>	# A condition which determines whether this rule is used in the evaluation of the policy. When set, the `expression` field in the `Expr' must include from 1 to 10 subexpressions, joined by the "||" or "&&" operators. Each subexpression must be of the form "resource.matchTag('<ORG_ID>/tag_key_short_name, 'tag_value_short_name')". or "resource.matchTagId('tagKeys/key_id', 'tagValues/value_id')". where key_name and value_name are the resource names for Label Keys and Values. These names are available from the Tag Manager Service. An example expression is: "resource.matchTag('123456789/environment, 'prod')". or "resource.matchTagId('tagKeys/123', 'tagValues/456')".
spec.dryRunSpec.rules[].condition.description <string>	# Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.
spec.dryRunSpec.rules[].condition.expression <string>	# Textual representation of an expression in Common Expression Language syntax.
spec.dryRunSpec.rules[].condition.location <string>	# Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.
spec.dryRunSpec.rules[].condition.title <string>	# Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.
spec.dryRunSpec.rules[].denyAll <boolean>	# Setting this to true means that all values are denied. This field can be set only in policies for list constraints.
spec.dryRunSpec.rules[].enforce <boolean>	# If `true`, then the policy is enforced. If `false`, then any configuration is acceptable. This field can be set only in policies for boolean constraints.
spec.dryRunSpec.rules[].parameters <object>	# Optional. Required for managed constraints if parameters are defined. Passes parameter values when policy enforcement is enabled. Each field corresponds to a known managed-constraint parameter key; the value type matches the type defined in the constraint definition. For example, the essentialcontacts.managed.allowedContactDomains managed constraint takes: parameters: allowedDomains: - "@example.com"
spec.dryRunSpec.rules[].parameters.allowAnyGKEPath <boolean>	# When true, any Google Kubernetes Engine resource path is permitted.
spec.dryRunSpec.rules[].parameters.allowPaths <[]string>	# Resource paths that are explicitly allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedAWSAccountIDs <[]string>	# AWS account IDs that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedDataSources <[]string>	# Data sources that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedDomains <[]string>	# Contact domains that are allowed by the managed constraint (for example, email domains permitted by essentialcontacts.managed.allowedContactDomains).
spec.dryRunSpec.rules[].parameters.allowedEgressFQDNs <[]string>	# Fully qualified domain names that are allowed for egress traffic.
spec.dryRunSpec.rules[].parameters.allowedEncryptions <[]string>	# Encryption types or modes that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedMemberSubjects <[]string>	# Member subjects (for example, users or service accounts) that are allowed.
spec.dryRunSpec.rules[].parameters.allowedParents <[]string>	# Parent resources (organizations, folders, or projects) that are allowed.
spec.dryRunSpec.rules[].parameters.allowedPreviewFeatures <[]string>	# Preview features that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedPrincipalSets <[]string>	# Principal sets that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedProviders <[]string>	# Providers that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedRetentionDurationSeconds <integer> format=int64	# Retention durations (in seconds) that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedRetentionPeriods <[]string>	# Retention periods that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedSchemes <[]string>	# URI schemes that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.allowedServiceList <[]string>	# Services that are allowed by the managed constraint (service list form).
spec.dryRunSpec.rules[].parameters.allowedServices <[]string>	# Services that are allowed by the managed constraint.
spec.dryRunSpec.rules[].parameters.defaultAWSProvider <string>	# Default AWS provider used when no explicit provider is specified.
spec.dryRunSpec.rules[].parameters.defaultXMLServiceProvider <string>	# Default XML service provider used when no explicit provider is specified.
spec.dryRunSpec.rules[].parameters.deniedEditions <[]string>	# Editions that are denied by the managed constraint.
spec.dryRunSpec.rules[].parameters.enforcedProjects <[]string>	# Projects where the managed constraint is enforced.
spec.dryRunSpec.rules[].parameters.minimumDestroyScheduleDurationInDays <integer> format=int64	# Minimum destroy schedule duration, in days, required by the managed constraint.
spec.dryRunSpec.rules[].parameters.nonFIPSMachineTypes <[]string>	# Compute Engine machine types that are not FIPS-compliant but are allowed.
spec.dryRunSpec.rules[].values <object>	# List of values to be used for this policy rule. This field can be set only in policies for list constraints.
spec.dryRunSpec.rules[].values.allowedValues <[]string>	# List of values allowed at this resource.
spec.dryRunSpec.rules[].values.deniedValues <[]string>	# List of values denied at this resource.
spec.folderRef <object>	# Immutable. The Folder that this resource belongs to. One and only one of 'projectRef', 'folderRef', or 'organizationRef' must be set.
spec.folderRef.external <string>	# The 'name' field of a folder, when not managed by Config Connector. This field must be set when 'name' field is not set.
spec.folderRef.name <string>	# The 'name' field of a 'Folder' resource. This field must be set when 'external' field is not set.
spec.folderRef.namespace <string>	# The 'namespace' field of a 'Folder' resource. If unset, the namespace is defaulted to the namespace of the referencer resource.
spec.organizationRef <object>	# Immutable. The Organization that this resource belongs to. One and only one of 'projectRef', 'folderRef', or 'organizationRef' must be set.
spec.organizationRef.external <string> (required)	# The 'name' field of an organization, when not managed by Config Connector.
spec.projectRef <object>	# Immutable. The Project that this resource belongs to. One and only one of 'projectRef', 'folderRef', or 'organizationRef' must be set.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The OrgPolicyPolicy name. If not given, the metadata.name will be used.
spec.spec <object>	# Basic information about the Organization Policy.
spec.spec.inheritFromParent <boolean>	# Determines the inheritance behavior for this policy. If `inherit_from_parent` is true, policy rules set higher up in the hierarchy (up to the closest root) are inherited and present in the effective policy. If it is false, then no rules are inherited, and this policy becomes the new root for evaluation. This field can be set only for policies which configure list constraints.
spec.spec.reset <boolean>	# Ignores policies set above this resource and restores the `constraint_default` enforcement behavior of the specific constraint at this resource. This field can be set in policies for either list or boolean constraints. If set, `rules` must be empty and `inherit_from_parent` must be set to false.
spec.spec.rules <[]object>	# In policies for boolean constraints, the following requirements apply: - There must be one and only one policy rule where condition is unset. - Boolean policy rules with conditions must set `enforced` to the opposite of the policy rule without a condition. - During policy evaluation, policy rules with conditions that are true for a target resource take precedence.
spec.spec.rules[].allowAll <boolean>	# Setting this to true means that all values are allowed. This field can be set only in policies for list constraints.
spec.spec.rules[].condition <object>	# A condition which determines whether this rule is used in the evaluation of the policy. When set, the `expression` field in the `Expr' must include from 1 to 10 subexpressions, joined by the "||" or "&&" operators. Each subexpression must be of the form "resource.matchTag('<ORG_ID>/tag_key_short_name, 'tag_value_short_name')". or "resource.matchTagId('tagKeys/key_id', 'tagValues/value_id')". where key_name and value_name are the resource names for Label Keys and Values. These names are available from the Tag Manager Service. An example expression is: "resource.matchTag('123456789/environment, 'prod')". or "resource.matchTagId('tagKeys/123', 'tagValues/456')".
spec.spec.rules[].condition.description <string>	# Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.
spec.spec.rules[].condition.expression <string>	# Textual representation of an expression in Common Expression Language syntax.
spec.spec.rules[].condition.location <string>	# Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.
spec.spec.rules[].condition.title <string>	# Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.
spec.spec.rules[].denyAll <boolean>	# Setting this to true means that all values are denied. This field can be set only in policies for list constraints.
spec.spec.rules[].enforce <boolean>	# If `true`, then the policy is enforced. If `false`, then any configuration is acceptable. This field can be set only in policies for boolean constraints.
spec.spec.rules[].parameters <object>	# Optional. Required for managed constraints if parameters are defined. Passes parameter values when policy enforcement is enabled. Each field corresponds to a known managed-constraint parameter key; the value type matches the type defined in the constraint definition. For example, the essentialcontacts.managed.allowedContactDomains managed constraint takes: parameters: allowedDomains: - "@example.com"
spec.spec.rules[].parameters.allowAnyGKEPath <boolean>	# When true, any Google Kubernetes Engine resource path is permitted.
spec.spec.rules[].parameters.allowPaths <[]string>	# Resource paths that are explicitly allowed by the managed constraint.
spec.spec.rules[].parameters.allowedAWSAccountIDs <[]string>	# AWS account IDs that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedDataSources <[]string>	# Data sources that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedDomains <[]string>	# Contact domains that are allowed by the managed constraint (for example, email domains permitted by essentialcontacts.managed.allowedContactDomains).
spec.spec.rules[].parameters.allowedEgressFQDNs <[]string>	# Fully qualified domain names that are allowed for egress traffic.
spec.spec.rules[].parameters.allowedEncryptions <[]string>	# Encryption types or modes that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedMemberSubjects <[]string>	# Member subjects (for example, users or service accounts) that are allowed.
spec.spec.rules[].parameters.allowedParents <[]string>	# Parent resources (organizations, folders, or projects) that are allowed.
spec.spec.rules[].parameters.allowedPreviewFeatures <[]string>	# Preview features that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedPrincipalSets <[]string>	# Principal sets that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedProviders <[]string>	# Providers that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedRetentionDurationSeconds <integer> format=int64	# Retention durations (in seconds) that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedRetentionPeriods <[]string>	# Retention periods that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedSchemes <[]string>	# URI schemes that are allowed by the managed constraint.
spec.spec.rules[].parameters.allowedServiceList <[]string>	# Services that are allowed by the managed constraint (service list form).
spec.spec.rules[].parameters.allowedServices <[]string>	# Services that are allowed by the managed constraint.
spec.spec.rules[].parameters.defaultAWSProvider <string>	# Default AWS provider used when no explicit provider is specified.
spec.spec.rules[].parameters.defaultXMLServiceProvider <string>	# Default XML service provider used when no explicit provider is specified.
spec.spec.rules[].parameters.deniedEditions <[]string>	# Editions that are denied by the managed constraint.
spec.spec.rules[].parameters.enforcedProjects <[]string>	# Projects where the managed constraint is enforced.
spec.spec.rules[].parameters.minimumDestroyScheduleDurationInDays <integer> format=int64	# Minimum destroy schedule duration, in days, required by the managed constraint.
spec.spec.rules[].parameters.nonFIPSMachineTypes <[]string>	# Compute Engine machine types that are not FIPS-compliant but are allowed.
spec.spec.rules[].values <object>	# List of values to be used for this policy rule. This field can be set only in policies for list constraints.
spec.spec.rules[].values.allowedValues <[]string>	# List of values allowed at this resource.
spec.spec.rules[].values.deniedValues <[]string>	# List of values denied at this resource.
status <object>	# OrgPolicyPolicyStatus defines the config connector machine state of OrgPolicyPolicy
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the OrgPolicyPolicy resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.dryRunSpec <object>	# Dry-run policy. Audit-only policy, can be used to monitor how the policy would have impacted the existing and future resources if it's enforced.
status.observedState.dryRunSpec.updateTime <string>	# Output only. The time stamp this was previously updated. This represents the last time a call to `CreatePolicy` or `UpdatePolicy` was made for that policy.
status.observedState.spec <object>	# Basic information about the Organization Policy.
status.observedState.spec.updateTime <string>	# Output only. The time stamp this was previously updated. This represents the last time a call to `CreatePolicy` or `UpdatePolicy` was made for that policy.
