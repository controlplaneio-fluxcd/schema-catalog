# schema source: OPA Gatekeeper v3.22.2 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=externaldata.gatekeeper.sh/v1beta1
kind <string> enum=Provider (cluster-scoped)
metadata.name <string> (required)
spec <object>	# Spec defines the Provider specifications.
spec.caBundle <string>	# CABundle is a base64-encoded string that contains the TLS CA bundle in PEM format. It is used to verify the signature of the provider's certificate.
spec.timeout <integer>	# Timeout is the timeout when querying the provider.
spec.url <string>	# URL is the url for the provider. URL is prefixed with https://.
status <object>	# ProviderStatus defines the observed state of Provider.
status.byPod <[]object>	# ByPod is the status of the provider by pod
status.byPod[].active <boolean>
status.byPod[].errors <[]object>
status.byPod[].errors[].errorTimestamp <string> format=date-time
status.byPod[].errors[].message <string> (required)
status.byPod[].errors[].retryable <boolean>
status.byPod[].errors[].type <string>	# Type indicates a specific class of error for use by controller code. If not present, the error should be treated as not matching any known type.
status.byPod[].id <string>
status.byPod[].lastCacheUpdateTime <string> format=date-time
status.byPod[].lastTransitionTime <string> format=date-time
status.byPod[].observedGeneration <integer> format=int64
status.byPod[].operations <[]string>
status.byPod[].providerUID <string>	# Storing the provider UID allows us to detect drift, such as when a provider has been recreated after its CRD was deleted out from under it, interrupting the watch
