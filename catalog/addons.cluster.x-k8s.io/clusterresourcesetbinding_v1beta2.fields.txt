# schema source: Cluster API v1.13.4 https://github.com/kubernetes-sigs/cluster-api
apiVersion <string> enum=addons.cluster.x-k8s.io/v1beta2
kind <string> enum=ClusterResourceSetBinding
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# spec is the desired state of ClusterResourceSetBinding.
spec.bindings <[]object> max=100	# bindings is a list of ClusterResourceSets and their resources.
spec.bindings[].clusterResourceSetName <string> (required) min=1 max=253	# clusterResourceSetName is the name of the ClusterResourceSet that is applied to the owner cluster of the binding.
spec.bindings[].resources <[]object> max=100	# resources is a list of resources that the ClusterResourceSet has.
spec.bindings[].resources[].applied <boolean> (required)	# applied is to track if a resource is applied to the cluster or not.
spec.bindings[].resources[].hash <string> min=1 max=256	# hash is the hash of a resource's data. This can be used to decide if a resource is changed. For "ApplyOnce" ClusterResourceSet.spec.strategy, this is no-op as that strategy does not act on change.
spec.bindings[].resources[].kind <string> (required) enum=Secret|ConfigMap	# kind of the resource. Supported kinds are: Secrets and ConfigMaps.
spec.bindings[].resources[].lastAppliedTime <string> format=date-time	# lastAppliedTime identifies when this resource was last applied to the cluster.
spec.bindings[].resources[].name <string> (required) min=1 max=253	# name of the resource that is in the same namespace with ClusterResourceSet object.
spec.clusterName <string> (required) min=1 max=63	# clusterName is the name of the Cluster this binding applies to.
