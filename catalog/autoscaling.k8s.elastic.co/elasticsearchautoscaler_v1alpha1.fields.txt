# schema source: Elastic Cloud v3.4.1 https://github.com/elastic/cloud-on-k8s
apiVersion <string> enum=autoscaling.k8s.elastic.co/v1alpha1
kind <string> enum=ElasticsearchAutoscaler
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ElasticsearchAutoscalerSpec holds the specification of an Elasticsearch autoscaler resource.
spec.elasticsearchRef <object> (required)	# ElasticsearchRef is a reference to an Elasticsearch cluster that exists in the same namespace.
spec.elasticsearchRef.name <string> min=1	# Name is the name of the Elasticsearch resource to scale automatically.
spec.policies <[]object> (required)
spec.policies[].deciders <map[string]object>	# Deciders allow the user to override default settings for autoscaling deciders.
spec.policies[].name <string>	# Name identifies the autoscaling policy in the autoscaling specification.
spec.policies[].resources <object> (required)	# AutoscalingResources model the limits, submitted by the user, for the supported resources in an autoscaling policy. Only the node count range is mandatory. For other resources, a limit range is required only if the Elasticsearch autoscaling capacity API returns a requirement for a given resource. For example, the memory limit range is only required if the autoscaling API response contains a memory requirement. If there is no limit range for a resource, and if that resource is not mandatory, then the resources in the NodeSets managed by the autoscaling policy are left untouched.
spec.policies[].resources.cpu <object>	# QuantityRange models a resource limit range for resources which can be expressed with resource.Quantity.
spec.policies[].resources.cpu.max <int-or-string> (required)	# Max represents the upper limit for the resources managed by the autoscaler.
spec.policies[].resources.cpu.min <int-or-string> (required)	# Min represents the lower limit for the resources managed by the autoscaler.
spec.policies[].resources.cpu.requestsToLimitsRatio <int-or-string>	# RequestsToLimitsRatio allows to customize Kubernetes resource Limit based on the Request.
spec.policies[].resources.memory <object>	# QuantityRange models a resource limit range for resources which can be expressed with resource.Quantity.
spec.policies[].resources.memory.max <int-or-string> (required)	# Max represents the upper limit for the resources managed by the autoscaler.
spec.policies[].resources.memory.min <int-or-string> (required)	# Min represents the lower limit for the resources managed by the autoscaler.
spec.policies[].resources.memory.requestsToLimitsRatio <int-or-string>	# RequestsToLimitsRatio allows to customize Kubernetes resource Limit based on the Request.
spec.policies[].resources.nodeCount <object> (required)	# NodeCountRange is used to model the minimum and the maximum number of nodes over all the NodeSets managed by the same autoscaling policy.
spec.policies[].resources.nodeCount.max <integer> (required) format=int32	# Max represents the maximum number of nodes in a tier.
spec.policies[].resources.nodeCount.min <integer> (required) format=int32	# Min represents the minimum number of nodes in a tier.
spec.policies[].resources.storage <object>	# QuantityRange models a resource limit range for resources which can be expressed with resource.Quantity.
spec.policies[].resources.storage.max <int-or-string> (required)	# Max represents the upper limit for the resources managed by the autoscaler.
spec.policies[].resources.storage.min <int-or-string> (required)	# Min represents the lower limit for the resources managed by the autoscaler.
spec.policies[].resources.storage.requestsToLimitsRatio <int-or-string>	# RequestsToLimitsRatio allows to customize Kubernetes resource Limit based on the Request.
spec.policies[].roles <[]string>	# An autoscaling policy must target a unique set of roles.
spec.pollingPeriod <string>	# PollingPeriod is the period at which to synchronize with the Elasticsearch autoscaling API.
status <object>
status.conditions <[]object>	# Conditions holds the current service state of the autoscaling controller.
status.conditions[].lastTransitionTime <string> format=date-time
status.conditions[].message <string>
status.conditions[].status <string> (required)
status.conditions[].type <string> (required)	# ConditionType defines the condition of an Elasticsearch resource.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the last observed generation by the controller.
status.policies <[]object>	# AutoscalingPolicyStatuses is used to expose state messages to user or external system.
status.policies[].lastModificationTime <string> format=date-time	# LastModificationTime is the last time the resources have been updated, used by the cooldown algorithm.
status.policies[].name <string> (required)	# Name is the name of the autoscaling policy
status.policies[].nodeSets <[]object>	# NodeSetNodeCount holds the number of nodes for each nodeSet.
status.policies[].nodeSets[].name <string> (required)	# Name of the Nodeset.
status.policies[].nodeSets[].nodeCount <integer> (required) format=int32	# NodeCount is the number of nodes, as computed by the autoscaler, expected in this NodeSet.
status.policies[].resources <object>	# ResourcesSpecification holds the resource values common to all the nodeSets managed by a same autoscaling policy. Only the resources managed by the autoscaling controller are saved in the Status.
status.policies[].resources.limits <object (free-form)>	# ResourceList is a set of (resource name, quantity) pairs.
status.policies[].resources.requests <object (free-form)>	# ResourceList is a set of (resource name, quantity) pairs.
status.policies[].state <[]object>	# PolicyStates may contain various messages regarding the current state of this autoscaling policy.
status.policies[].state[].messages <[]string> (required)
status.policies[].state[].type <string> (required)
