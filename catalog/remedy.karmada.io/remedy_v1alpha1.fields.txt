# schema source: Karmada v1.18.1 https://github.com/karmada-io/karmada
apiVersion <string> enum=remedy.karmada.io/v1alpha1
kind <string> enum=Remedy (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)	# Spec represents the desired behavior of Remedy.
spec.actions <[]string>	# Actions specifies the actions that remedy system needs to perform. If empty, no action will be performed.
spec.clusterAffinity <object>	# ClusterAffinity specifies the clusters that Remedy needs to pay attention to. For clusters that meet the DecisionConditions, Actions will be preformed. If empty, all clusters will be selected.
spec.clusterAffinity.clusterNames <[]string>	# ClusterNames is the list of clusters to be selected.
spec.decisionMatches <[]object>	# DecisionMatches indicates the decision matches of triggering the remedy system to perform the actions. As long as any one DecisionMatch matches, the Actions will be preformed. If empty, the Actions will be performed immediately.
spec.decisionMatches[].clusterConditionMatch <object>	# ClusterConditionMatch describes the cluster condition requirement.
spec.decisionMatches[].clusterConditionMatch.conditionStatus <string> (required)	# ConditionStatus specifies the ClusterStatue condition status.
spec.decisionMatches[].clusterConditionMatch.conditionType <string> (required)	# ConditionType specifies the ClusterStatus condition type.
spec.decisionMatches[].clusterConditionMatch.operator <string> (required) enum=Equal|NotEqual	# Operator represents a conditionType's relationship to a conditionStatus. Valid operators are Equal, NotEqual.
