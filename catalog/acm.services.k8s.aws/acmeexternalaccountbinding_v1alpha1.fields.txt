# schema source: AWS ACM Controller v1.9.2 https://github.com/aws-controllers-k8s/acm-controller
apiVersion <string> enum=acm.services.k8s.aws/v1alpha1
kind <string> enum=AcmeExternalAccountBinding
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# AcmeExternalAccountBindingSpec defines the desired state of AcmeExternalAccountBinding. Contains detailed information about an ACME external account binding.
spec.acmeEndpointARN <string> (immutable)	# The ARN of the ACME endpoint to associate this external account binding with. The Amazon Resource Name (ARN) of the ACME endpoint. Regex Pattern: `^arn:aws[a-z-]*:acm:[a-z0-9-]+:[0-9]{12}:acme-endpoint/[a-zA-Z0-9-]+$`
spec.acmeEndpointRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.acmeEndpointRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.acmeEndpointRef.from.name <string>
spec.acmeEndpointRef.from.namespace <string>
spec.credentialsOutput <object> (immutable)	# The Kubernetes Secret where EAB credentials are written. Specify the namespace, name, and key. The sensitive macKey is written under the given key; the keyId is written under a fixed keyId key, unless key is itself keyId, in which case the macKey wins and the identifier is available in status.keyID. If namespace is omitted, the resource's namespace is used. The Secret must already exist; the controller populates it.
spec.credentialsOutput.key <string> (required)	# Key is the key within the secret
spec.credentialsOutput.name <string>	# name is unique within a namespace to reference a secret resource.
spec.credentialsOutput.namespace <string>	# namespace defines the space within which the secret name must be unique.
spec.expiration <object> (immutable)	# The expiration configuration for the external account binding.
spec.expiration.type <string>
spec.expiration.value <integer> format=int64
spec.roleARN <string> (immutable)	# The ARN of the IAM role associated with the external account binding. The role's trust policy must grant acm-acme.amazonaws.com access to sts:AssumeRole, sts:SetSourceIdentity, and sts:TagSession. The Amazon Resource Name (ARN) of the IAM role to associate with the external account binding. Regex Pattern: `^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$`
spec.roleRef <object>	# AWSResourceReferenceWrapper provides a wrapper around *AWSResourceReference type to provide more user friendly syntax for references using 'from' field Ex: APIIDRef: from: name: my-api
spec.roleRef.from <object>	# AWSResourceReference provides all the values necessary to reference another k8s resource for finding the identifier(Id/ARN/Name)
spec.roleRef.from.name <string>
spec.roleRef.from.namespace <string>
spec.tags <[]object>	# One or more tags to associate with the external account binding.
spec.tags[].key <string>
spec.tags[].value <string>
status <object>	# AcmeExternalAccountBindingStatus defines the observed state of AcmeExternalAccountBinding
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.createdAt <string> format=date-time	# The time at which the external account binding was created.
status.expiresAt <string> format=date-time	# The time at which the external account binding expires.
status.keyID <string>	# The external account binding key identifier. This value is not sensitive and pairs with the macKey stored in the credentials Secret. It is surfaced in status so ACME clients can reference it directly when registering an account.
status.lastUsedAt <string> format=date-time	# The time at which the external account binding was last used.
status.revokedAt <string> format=date-time	# The time at which the external account binding was revoked.
status.updatedAt <string> format=date-time	# The time at which the external account binding was last updated.
