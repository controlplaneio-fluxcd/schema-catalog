# schema source: AWS ACM Controller v1.8.0 https://github.com/aws-controllers-k8s/acm-controller
apiVersion <string> enum=acm.services.k8s.aws/v1alpha1
kind <string> enum=AcmeEndpoint
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# AcmeEndpointSpec defines the desired state of AcmeEndpoint. Contains detailed information about an ACME endpoint.
spec.authorizationBehavior <string> (required)	# The authorization behavior for the ACME endpoint. Valid values: PRE_APPROVED. The authorization behavior for the ACME endpoint.
spec.certificateAuthority <object> (required)	# The certificate authority to use for issuing certificates through this endpoint. Currently only PublicCertificateAuthority (Amazon Trust Services) is supported. Optionally specify AllowedKeyAlgorithms to restrict key algorithms for issued certificates. The type of certificate authority to use for issuing certificates through this ACME endpoint.
spec.certificateAuthority.publicCertificateAuthority <object>	# Configuration for a public certificate authority.
spec.certificateAuthority.publicCertificateAuthority.allowedKeyAlgorithms <[]string>
spec.certificateTags <[]object> (immutable)	# Tags to apply to certificates issued through this ACME endpoint.
spec.certificateTags[].key <string>
spec.certificateTags[].value <string>
spec.contact <string>	# Whether ACME clients must provide contact information during account registration. Valid values: REQUIRED, NOT_REQUIRED. Specifies whether ACME clients must provide contact information during account registration.
spec.tags <[]object>	# One or more tags to associate with the ACME endpoint.
spec.tags[].key <string>
spec.tags[].value <string>
status <object>	# AcmeEndpointStatus defines the observed state of AcmeEndpoint
status.ackResourceMetadata <object>	# All CRs managed by ACK have a common `Status.ACKResourceMetadata` member that is used to contain resource sync state, account ownership, constructed ARN for the resource
status.ackResourceMetadata.arn <string>	# ARN is the Amazon Resource Name for the resource. This is a globally-unique identifier and is set only by the ACK service controller once the controller has orchestrated the creation of the resource OR when it has verified that an "adopted" resource (a resource where the ARN annotation was set by the Kubernetes user on the CR) exists and matches the supplied CR's Spec field values. https://github.com/aws/aws-controllers-k8s/issues/270
status.ackResourceMetadata.ownerAccountID <string> (required)	# OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
status.ackResourceMetadata.partition <string>	# Partition is the AWS partition in which the resource exists or will exist
status.ackResourceMetadata.region <string> (required)	# Region is the AWS region in which the resource exists or will exist.
status.acmeEndpointARN <string>	# The Amazon Resource Name (ARN) of the ACME endpoint. Regex Pattern: `^arn:aws[a-z-]*:acm:[a-z0-9-]+:[0-9]{12}:acme-endpoint/[a-zA-Z0-9-]+$`
status.conditions <[]object>	# All CRs managed by ACK have a common `Status.Conditions` member that contains a collection of `ackv1alpha1.Condition` objects that describe the various terminal states of the CR and its backend AWS service API resource
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
status.createdAt <string> format=date-time	# The time at which the ACME endpoint was created.
status.endpointURL <string>	# The URL of the ACME endpoint.
status.failureReason <string>	# The reason the ACME endpoint failed, if applicable.
status.status <string>	# The status of the ACME endpoint.
status.updatedAt <string> format=date-time	# The time at which the ACME endpoint was last updated.
