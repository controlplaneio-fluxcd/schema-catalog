# schema source: Upbound AWS Provider v2.6.0 https://github.com/crossplane-contrib/provider-upjet-aws
apiVersion <string> enum=identitystore.aws.upbound.io/v1beta2
kind <string> enum=User (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)	# UserSpec defines the desired state of User
spec.deletionPolicy <string> enum=Orphan|Delete default="Delete"	# DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either "Delete" or "Orphan" the external resource. This field is planned to be deprecated in favor of the ManagementPolicies field in a future release. Currently, both could be set independently and non-default values would be honored if the feature flag is enabled. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
spec.forProvider <object> (required)
spec.forProvider.addresses <object>	# Details about the user's address. At most 1 address is allowed. Detailed below.
spec.forProvider.addresses.country <string>	# The country that this address is in.
spec.forProvider.addresses.formatted <string>	# The name that is typically displayed when the address is shown for display.
spec.forProvider.addresses.locality <string>	# The address locality.
spec.forProvider.addresses.postalCode <string>	# The postal code of the address.
spec.forProvider.addresses.primary <boolean>	# When true, this is the primary address associated with the user.
spec.forProvider.addresses.region <string>	# The region of the address.
spec.forProvider.addresses.streetAddress <string>	# The street of the address.
spec.forProvider.addresses.type <string>	# The type of address.
spec.forProvider.displayName <string>	# The name that is typically displayed when the user is referenced.
spec.forProvider.emails <object>	# Details about the user's email. At most 1 email is allowed. Detailed below.
spec.forProvider.emails.primary <boolean>	# When true, this is the primary email associated with the user.
spec.forProvider.emails.type <string>	# The type of email.
spec.forProvider.emails.value <string>	# The email address. This value must be unique across the identity store.
spec.forProvider.identityStoreId <string> (required)	# The globally unique identifier for the identity store that this user is in.
spec.forProvider.locale <string>	# The user's geographical region or location.
spec.forProvider.name <object>	# Details about the user's full name. Detailed below.
spec.forProvider.name.familyName <string>	# The family name of the user.
spec.forProvider.name.formatted <string>	# The name that is typically displayed when the name is shown for display.
spec.forProvider.name.givenName <string>	# The given name of the user.
spec.forProvider.name.honorificPrefix <string>	# The honorific prefix of the user.
spec.forProvider.name.honorificSuffix <string>	# The honorific suffix of the user.
spec.forProvider.name.middleName <string>	# The middle name of the user.
spec.forProvider.nickname <string>	# An alternate name for the user.
spec.forProvider.phoneNumbers <object>	# Details about the user's phone number. At most 1 phone number is allowed. Detailed below.
spec.forProvider.phoneNumbers.primary <boolean>	# When true, this is the primary phone number associated with the user.
spec.forProvider.phoneNumbers.type <string>	# The type of phone number.
spec.forProvider.phoneNumbers.value <string>	# The user's phone number.
spec.forProvider.preferredLanguage <string>	# The preferred language of the user.
spec.forProvider.profileUrl <string>	# An URL that may be associated with the user.
spec.forProvider.region <string> (required)	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
spec.forProvider.timezone <string>	# The user's time zone.
spec.forProvider.title <string>	# The user's title.
spec.forProvider.userName <string>	# A unique string used to identify the user. This value can consist of letters, accented characters, symbols, numbers, and punctuation. This value is specified at the time the user is created and stored as an attribute of the user object in the identity store. The limit is 128 characters.
spec.forProvider.userType <string>	# The user type.
spec.initProvider <object>	# THIS IS A BETA FIELD. It will be honored unless the Management Policies feature flag is disabled. InitProvider holds the same fields as ForProvider, with the exception of Identifier and other resource reference fields. The fields that are in InitProvider are merged into ForProvider when the resource is created. The same fields are also added to the terraform ignore_changes hook, to avoid updating them after creation. This is useful for fields that are required on creation, but we do not desire to update them after creation, for example because of an external controller is managing them, like an autoscaler.
spec.initProvider.addresses <object>	# Details about the user's address. At most 1 address is allowed. Detailed below.
spec.initProvider.addresses.country <string>	# The country that this address is in.
spec.initProvider.addresses.formatted <string>	# The name that is typically displayed when the address is shown for display.
spec.initProvider.addresses.locality <string>	# The address locality.
spec.initProvider.addresses.postalCode <string>	# The postal code of the address.
spec.initProvider.addresses.primary <boolean>	# When true, this is the primary address associated with the user.
spec.initProvider.addresses.streetAddress <string>	# The street of the address.
spec.initProvider.addresses.type <string>	# The type of address.
spec.initProvider.displayName <string>	# The name that is typically displayed when the user is referenced.
spec.initProvider.emails <object>	# Details about the user's email. At most 1 email is allowed. Detailed below.
spec.initProvider.emails.primary <boolean>	# When true, this is the primary email associated with the user.
spec.initProvider.emails.type <string>	# The type of email.
spec.initProvider.emails.value <string>	# The email address. This value must be unique across the identity store.
spec.initProvider.locale <string>	# The user's geographical region or location.
spec.initProvider.name <object>	# Details about the user's full name. Detailed below.
spec.initProvider.name.familyName <string>	# The family name of the user.
spec.initProvider.name.formatted <string>	# The name that is typically displayed when the name is shown for display.
spec.initProvider.name.givenName <string>	# The given name of the user.
spec.initProvider.name.honorificPrefix <string>	# The honorific prefix of the user.
spec.initProvider.name.honorificSuffix <string>	# The honorific suffix of the user.
spec.initProvider.name.middleName <string>	# The middle name of the user.
spec.initProvider.nickname <string>	# An alternate name for the user.
spec.initProvider.phoneNumbers <object>	# Details about the user's phone number. At most 1 phone number is allowed. Detailed below.
spec.initProvider.phoneNumbers.primary <boolean>	# When true, this is the primary phone number associated with the user.
spec.initProvider.phoneNumbers.type <string>	# The type of phone number.
spec.initProvider.phoneNumbers.value <string>	# The user's phone number.
spec.initProvider.preferredLanguage <string>	# The preferred language of the user.
spec.initProvider.profileUrl <string>	# An URL that may be associated with the user.
spec.initProvider.timezone <string>	# The user's time zone.
spec.initProvider.title <string>	# The user's title.
spec.initProvider.userName <string>	# A unique string used to identify the user. This value can consist of letters, accented characters, symbols, numbers, and punctuation. This value is specified at the time the user is created and stored as an attribute of the user object in the identity store. The limit is 128 characters.
spec.initProvider.userType <string>	# The user type.
spec.managementPolicies <[]string> default=["*"]	# THIS IS A BETA FIELD. It is on by default but can be opted out through a Crossplane feature flag. ManagementPolicies specify the array of actions Crossplane is allowed to take on the managed and external resources. This field is planned to replace the DeletionPolicy field in a future release. Currently, both could be set independently and non-default values would be honored if the feature flag is enabled. If both are custom, the DeletionPolicy field will be ignored. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223 and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
spec.providerConfigRef <object> default={"name":"default"}	# ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
spec.providerConfigRef.name <string> (required)	# Name of the referenced object.
spec.providerConfigRef.policy <object>	# Policies for referencing.
spec.providerConfigRef.policy.resolution <string> enum=Required|Optional default="Required"	# Resolution specifies whether resolution of this reference is required. The default is 'Required', which means the reconcile will fail if the reference cannot be resolved. 'Optional' means this reference will be a no-op if it cannot be resolved.
spec.providerConfigRef.policy.resolve <string> enum=Always|IfNotPresent	# Resolve specifies when this reference should be resolved. The default is 'IfNotPresent', which will attempt to resolve the reference only when the corresponding field is not present. Use 'Always' to resolve the reference on every reconcile.
spec.writeConnectionSecretToRef <object>	# WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
spec.writeConnectionSecretToRef.name <string> (required)	# Name of the secret.
spec.writeConnectionSecretToRef.namespace <string> (required)	# Namespace of the secret.
status <object>	# UserStatus defines the observed state of User.
status.atProvider <object>
status.atProvider.addresses <object>	# Details about the user's address. At most 1 address is allowed. Detailed below.
status.atProvider.addresses.country <string>	# The country that this address is in.
status.atProvider.addresses.formatted <string>	# The name that is typically displayed when the address is shown for display.
status.atProvider.addresses.locality <string>	# The address locality.
status.atProvider.addresses.postalCode <string>	# The postal code of the address.
status.atProvider.addresses.primary <boolean>	# When true, this is the primary address associated with the user.
status.atProvider.addresses.region <string>	# The region of the address.
status.atProvider.addresses.streetAddress <string>	# The street of the address.
status.atProvider.addresses.type <string>	# The type of address.
status.atProvider.displayName <string>	# The name that is typically displayed when the user is referenced.
status.atProvider.emails <object>	# Details about the user's email. At most 1 email is allowed. Detailed below.
status.atProvider.emails.primary <boolean>	# When true, this is the primary email associated with the user.
status.atProvider.emails.type <string>	# The type of email.
status.atProvider.emails.value <string>	# The email address. This value must be unique across the identity store.
status.atProvider.externalIds <[]object>	# A list of identifiers issued to this resource by an external identity provider.
status.atProvider.externalIds[].id <string>	# The identifier issued to this resource by an external identity provider.
status.atProvider.externalIds[].issuer <string>	# The issuer for an external identifier.
status.atProvider.id <string>	# The identifier issued to this resource by an external identity provider.
status.atProvider.identityStoreId <string>	# The globally unique identifier for the identity store that this user is in.
status.atProvider.locale <string>	# The user's geographical region or location.
status.atProvider.name <object>	# Details about the user's full name. Detailed below.
status.atProvider.name.familyName <string>	# The family name of the user.
status.atProvider.name.formatted <string>	# The name that is typically displayed when the name is shown for display.
status.atProvider.name.givenName <string>	# The given name of the user.
status.atProvider.name.honorificPrefix <string>	# The honorific prefix of the user.
status.atProvider.name.honorificSuffix <string>	# The honorific suffix of the user.
status.atProvider.name.middleName <string>	# The middle name of the user.
status.atProvider.nickname <string>	# An alternate name for the user.
status.atProvider.phoneNumbers <object>	# Details about the user's phone number. At most 1 phone number is allowed. Detailed below.
status.atProvider.phoneNumbers.primary <boolean>	# When true, this is the primary phone number associated with the user.
status.atProvider.phoneNumbers.type <string>	# The type of phone number.
status.atProvider.phoneNumbers.value <string>	# The user's phone number.
status.atProvider.preferredLanguage <string>	# The preferred language of the user.
status.atProvider.profileUrl <string>	# An URL that may be associated with the user.
status.atProvider.region <string>	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
status.atProvider.timezone <string>	# The user's time zone.
status.atProvider.title <string>	# The user's title.
status.atProvider.userId <string>	# The identifier for this user in the identity store.
status.atProvider.userName <string>	# A unique string used to identify the user. This value can consist of letters, accented characters, symbols, numbers, and punctuation. This value is specified at the time the user is created and stored as an attribute of the user object in the identity store. The limit is 128 characters.
status.atProvider.userType <string>	# The user type.
status.conditions <[]object>	# Conditions of the resource.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time this condition transitioned from one status to another.
status.conditions[].message <string>	# A Message containing details about this condition's last transition from one status to another, if any.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# A Reason for this condition's last transition from one status to another.
status.conditions[].status <string> (required)	# Status of this condition; is it currently True, False, or Unknown?
status.conditions[].type <string> (required)	# Type of this condition. At most one of each condition type may apply to a resource at any point in time.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the latest metadata.generation which resulted in either a ready state, or stalled due to error it can not recover from without human intervention.
