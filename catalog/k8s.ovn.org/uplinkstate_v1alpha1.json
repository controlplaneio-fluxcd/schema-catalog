{
  "description": "UplinkState contains node-local discovery and gateway state for an Uplink.",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "additionalProperties": false,
      "properties": {
        "nodeName": {
          "description": "NodeName is the node this state belongs to.",
          "maxLength": 253,
          "minLength": 1,
          "type": "string",
          "x-kubernetes-validations": [
            {
              "message": "nodeName is immutable",
              "rule": "self == oldSelf"
            }
          ]
        },
        "uplinkName": {
          "description": "UplinkName is the Uplink this state belongs to.",
          "maxLength": 253,
          "minLength": 1,
          "type": "string",
          "x-kubernetes-validations": [
            {
              "message": "uplinkName is immutable",
              "rule": "self == oldSelf"
            }
          ]
        }
      },
      "required": [
        "nodeName",
        "uplinkName"
      ],
      "type": "object"
    },
    "status": {
      "additionalProperties": false,
      "properties": {
        "conditions": {
          "description": "Conditions reports node-local discovery and gateway programming state for\nthis resolved uplink.",
          "items": {
            "additionalProperties": false,
            "description": "Condition contains details for one aspect of the current state of this API Resource.",
            "properties": {
              "lastTransitionTime": {
                "description": "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.",
                "format": "date-time",
                "type": "string"
              },
              "message": {
                "description": "message is a human readable message indicating details about the transition.\nThis may be an empty string.",
                "maxLength": 32768,
                "type": "string"
              },
              "observedGeneration": {
                "description": "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "reason": {
                "description": "reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.",
                "maxLength": 1024,
                "minLength": 1,
                "pattern": "^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
                "type": "string"
              },
              "status": {
                "description": "status of the condition, one of True, False, Unknown.",
                "enum": [
                  "True",
                  "False",
                  "Unknown"
                ],
                "type": "string"
              },
              "type": {
                "description": "type of condition in CamelCase or in foo.example.com/CamelCase.",
                "maxLength": 316,
                "pattern": "^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
                "type": "string"
              }
            },
            "required": [
              "lastTransitionTime",
              "message",
              "reason",
              "status",
              "type"
            ],
            "type": "object"
          },
          "type": "array",
          "x-kubernetes-list-map-keys": [
            "type"
          ],
          "x-kubernetes-list-type": "map"
        },
        "defaultGateways": {
          "description": "DefaultGateways are default route next-hop IPs discovered for the\nselected host interface.",
          "items": {
            "description": "IPAddress is an IP address.",
            "maxLength": 64,
            "type": "string",
            "x-kubernetes-validations": [
              {
                "message": "must be a valid IP address",
                "rule": "isIP(self)"
              }
            ]
          },
          "maxItems": 2,
          "type": "array",
          "x-kubernetes-list-type": "atomic"
        },
        "hostFunction": {
          "additionalProperties": false,
          "description": "HostFunction identifies the PCI function (a PF, or a VF on that PF)\nbacking the host interface. Only SR-IOV capable functions are\npublished: they are the ones that can have a representor on a DPU,\nwhich is what this field exists to resolve. In split DPU mode the\nDPU-host publishes it so the DPU can resolve the interface's\nrepresentor and OVS bridge directly, without scanning bridges by host\nMAC. Absent when the host interface has no such function or its\nidentity cannot be resolved.",
          "properties": {
            "pfID": {
              "description": "PFID is the index of the physical function backing the host interface.",
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "vfID": {
              "description": "VFID is the index of the virtual function on the physical function.\nAbsent when the host interface is the physical function itself.",
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "pfID"
          ],
          "type": "object"
        },
        "hostInterfaceName": {
          "description": "HostInterfaceName is the host-visible Linux interface selected by\nUplink spec. It carries the host-side gateway L3 identity in all modes.",
          "maxLength": 15,
          "minLength": 1,
          "pattern": "^[^/\\s]+$",
          "type": "string"
        },
        "ipAddresses": {
          "description": "IPAddresses are host-side shared gateway IP addresses.",
          "items": {
            "description": "IPAddressCIDR is an IP address with a network prefix.",
            "maxLength": 64,
            "type": "string",
            "x-kubernetes-validations": [
              {
                "message": "must be a valid CIDR",
                "rule": "isCIDR(self)"
              }
            ]
          },
          "maxItems": 2,
          "type": "array",
          "x-kubernetes-list-type": "atomic"
        },
        "macAddress": {
          "description": "MACAddress is the MAC address used for the OVN gateway interface.",
          "pattern": "^([0-9a-fA-F]{2}:){5}[0-9a-fA-F]{2}$",
          "type": "string"
        },
        "ovsBridge": {
          "additionalProperties": false,
          "description": "OVSBridge contains resolved OVS bridge data.",
          "properties": {
            "name": {
              "description": "Name is the resolved OVS bridge used for OVN bridge mappings and flows.",
              "maxLength": 15,
              "minLength": 1,
              "pattern": "^[^/\\s]+$",
              "type": "string"
            }
          },
          "type": "object"
        },
        "type": {
          "description": "Type is defined by the matched nodeConfig of the Uplink.",
          "enum": [
            "OVSBridge"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "required": [
    "spec"
  ],
  "type": "object",
  "x-flux-schema-type": "UplinkState"
}
