# schema source: Upbound AWS Provider v2.8.1 https://github.com/crossplane-contrib/provider-upjet-aws
apiVersion <string> enum=ses.aws.upbound.io/v1beta1
kind <string> enum=ReceiptRule (cluster-scoped)
metadata.name <string> (required)
spec <object> (required)	# ReceiptRuleSpec defines the desired state of ReceiptRule
spec.deletionPolicy <string> enum=Orphan|Delete default="Delete"	# DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either "Delete" or "Orphan" the external resource. This field is planned to be deprecated in favor of the ManagementPolicies field in a future release. Currently, both could be set independently and non-default values would be honored if the feature flag is enabled. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
spec.forProvider <object> (required)
spec.forProvider.addHeaderAction <[]object>	# Configuration block for adding a header to received emails. Detailed below.
spec.forProvider.addHeaderAction[].headerName <string>	# Name of the header to add.
spec.forProvider.addHeaderAction[].headerValue <string>	# Value of the header to add.
spec.forProvider.addHeaderAction[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.after <string>	# Name of the rule to place this rule after.
spec.forProvider.bounceAction <[]object>	# Configuration block for rejecting received emails. Detailed below.
spec.forProvider.bounceAction[].message <string>	# Message to send.
spec.forProvider.bounceAction[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.bounceAction[].sender <string>	# Email address of the sender.
spec.forProvider.bounceAction[].smtpReplyCode <string>	# RFC 5321 SMTP reply code.
spec.forProvider.bounceAction[].statusCode <string>	# RFC 3463 SMTP enhanced status code.
spec.forProvider.bounceAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.forProvider.enabled <boolean>	# If true, the rule will be enabled.
spec.forProvider.lambdaAction <[]object>	# Configuration block for calling an AWS Lambda function. Detailed below.
spec.forProvider.lambdaAction[].functionArn <string>	# ARN of the Lambda function to invoke.
spec.forProvider.lambdaAction[].invocationType <string>	# Event or RequestResponse.
spec.forProvider.lambdaAction[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.lambdaAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.forProvider.name <string>	# Name of the rule.
spec.forProvider.recipients <[]string>	# List of email addresses.
spec.forProvider.region <string> (required)	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
spec.forProvider.ruleSetName <string>	# Name of the rule set.
spec.forProvider.s3Action <[]object>	# Configuration block for storing received emails in an S3 bucket. Detailed below.
spec.forProvider.s3Action[].bucketName <string>	# Name of the S3 bucket.
spec.forProvider.s3Action[].iamRoleArn <string>	# ARN of the IAM role to be used by Amazon Simple Email Service while writing to the Amazon S3 bucket, optionally encrypting your mail via the provided customer managed key, and publishing to the Amazon SNS topic.
spec.forProvider.s3Action[].kmsKeyArn <string>	# ARN of the KMS key.
spec.forProvider.s3Action[].objectKeyPrefix <string>	# Key prefix of the S3 bucket.
spec.forProvider.s3Action[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.s3Action[].topicArn <string>	# ARN of an SNS topic to notify.
spec.forProvider.scanEnabled <boolean>	# If true, incoming emails will be scanned for spam and viruses.
spec.forProvider.snsAction <[]object>	# Configuration block for publishing to an SNS topic. Detailed below.
spec.forProvider.snsAction[].encoding <string>	# Encoding to use for the email within the Amazon SNS notification. Default value is UTF-8.
spec.forProvider.snsAction[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.snsAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.forProvider.stopAction <[]object>	# Configuration block for terminating the evaluation of the receipt rule set. Detailed below.
spec.forProvider.stopAction[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.stopAction[].scope <string>	# Scope to apply. The only acceptable value is RuleSet.
spec.forProvider.stopAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.forProvider.tlsPolicy <string>	# Require or Optional.
spec.forProvider.workmailAction <[]object>	# Configuration block for calling Amazon WorkMail. Detailed below.
spec.forProvider.workmailAction[].organizationArn <string>	# ARN of the WorkMail organization.
spec.forProvider.workmailAction[].position <number>	# Position of the action in the receipt rule.
spec.forProvider.workmailAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.initProvider <object>	# THIS IS A BETA FIELD. It will be honored unless the Management Policies feature flag is disabled. InitProvider holds the same fields as ForProvider, with the exception of Identifier and other resource reference fields. The fields that are in InitProvider are merged into ForProvider when the resource is created. The same fields are also added to the terraform ignore_changes hook, to avoid updating them after creation. This is useful for fields that are required on creation, but we do not desire to update them after creation, for example because of an external controller is managing them, like an autoscaler.
spec.initProvider.addHeaderAction <[]object>	# Configuration block for adding a header to received emails. Detailed below.
spec.initProvider.addHeaderAction[].headerName <string>	# Name of the header to add.
spec.initProvider.addHeaderAction[].headerValue <string>	# Value of the header to add.
spec.initProvider.addHeaderAction[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.after <string>	# Name of the rule to place this rule after.
spec.initProvider.bounceAction <[]object>	# Configuration block for rejecting received emails. Detailed below.
spec.initProvider.bounceAction[].message <string>	# Message to send.
spec.initProvider.bounceAction[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.bounceAction[].sender <string>	# Email address of the sender.
spec.initProvider.bounceAction[].smtpReplyCode <string>	# RFC 5321 SMTP reply code.
spec.initProvider.bounceAction[].statusCode <string>	# RFC 3463 SMTP enhanced status code.
spec.initProvider.bounceAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.initProvider.enabled <boolean>	# If true, the rule will be enabled.
spec.initProvider.lambdaAction <[]object>	# Configuration block for calling an AWS Lambda function. Detailed below.
spec.initProvider.lambdaAction[].functionArn <string>	# ARN of the Lambda function to invoke.
spec.initProvider.lambdaAction[].invocationType <string>	# Event or RequestResponse.
spec.initProvider.lambdaAction[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.lambdaAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.initProvider.name <string>	# Name of the rule.
spec.initProvider.recipients <[]string>	# List of email addresses.
spec.initProvider.ruleSetName <string>	# Name of the rule set.
spec.initProvider.s3Action <[]object>	# Configuration block for storing received emails in an S3 bucket. Detailed below.
spec.initProvider.s3Action[].bucketName <string>	# Name of the S3 bucket.
spec.initProvider.s3Action[].iamRoleArn <string>	# ARN of the IAM role to be used by Amazon Simple Email Service while writing to the Amazon S3 bucket, optionally encrypting your mail via the provided customer managed key, and publishing to the Amazon SNS topic.
spec.initProvider.s3Action[].kmsKeyArn <string>	# ARN of the KMS key.
spec.initProvider.s3Action[].objectKeyPrefix <string>	# Key prefix of the S3 bucket.
spec.initProvider.s3Action[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.s3Action[].topicArn <string>	# ARN of an SNS topic to notify.
spec.initProvider.scanEnabled <boolean>	# If true, incoming emails will be scanned for spam and viruses.
spec.initProvider.snsAction <[]object>	# Configuration block for publishing to an SNS topic. Detailed below.
spec.initProvider.snsAction[].encoding <string>	# Encoding to use for the email within the Amazon SNS notification. Default value is UTF-8.
spec.initProvider.snsAction[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.snsAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.initProvider.stopAction <[]object>	# Configuration block for terminating the evaluation of the receipt rule set. Detailed below.
spec.initProvider.stopAction[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.stopAction[].scope <string>	# Scope to apply. The only acceptable value is RuleSet.
spec.initProvider.stopAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.initProvider.tlsPolicy <string>	# Require or Optional.
spec.initProvider.workmailAction <[]object>	# Configuration block for calling Amazon WorkMail. Detailed below.
spec.initProvider.workmailAction[].organizationArn <string>	# ARN of the WorkMail organization.
spec.initProvider.workmailAction[].position <number>	# Position of the action in the receipt rule.
spec.initProvider.workmailAction[].topicArn <string>	# ARN of an SNS topic to notify.
spec.managementPolicies <[]string> default=["*"]	# THIS IS A BETA FIELD. It is on by default but can be opted out through a Crossplane feature flag. ManagementPolicies specify the array of actions Crossplane is allowed to take on the managed and external resources. This field is planned to replace the DeletionPolicy field in a future release. Currently, both could be set independently and non-default values would be honored if the feature flag is enabled. If both are custom, the DeletionPolicy field will be ignored. See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223 and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
spec.providerConfigRef <object> default={"name":"default"}	# ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
spec.providerConfigRef.name <string> (required)	# Name of the referenced object.
spec.providerConfigRef.policy <object>	# Policies for referencing.
spec.providerConfigRef.policy.resolution <string> enum=Required|Optional default="Required"	# Resolution specifies whether resolution of this reference is required. The default is 'Required', which means the reconcile will fail if the reference cannot be resolved. 'Optional' means this reference will be a no-op if it cannot be resolved.
spec.providerConfigRef.policy.resolve <string> enum=Always|IfNotPresent	# Resolve specifies when this reference should be resolved. The default is 'IfNotPresent', which will attempt to resolve the reference only when the corresponding field is not present. Use 'Always' to resolve the reference on every reconcile.
spec.writeConnectionSecretToRef <object>	# WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
spec.writeConnectionSecretToRef.name <string> (required)	# Name of the secret.
spec.writeConnectionSecretToRef.namespace <string> (required)	# Namespace of the secret.
status <object>	# ReceiptRuleStatus defines the observed state of ReceiptRule.
status.atProvider <object>
status.atProvider.addHeaderAction <[]object>	# Configuration block for adding a header to received emails. Detailed below.
status.atProvider.addHeaderAction[].headerName <string>	# Name of the header to add.
status.atProvider.addHeaderAction[].headerValue <string>	# Value of the header to add.
status.atProvider.addHeaderAction[].position <number>	# Position of the action in the receipt rule.
status.atProvider.after <string>	# Name of the rule to place this rule after.
status.atProvider.arn <string>	# SES receipt rule ARN.
status.atProvider.bounceAction <[]object>	# Configuration block for rejecting received emails. Detailed below.
status.atProvider.bounceAction[].message <string>	# Message to send.
status.atProvider.bounceAction[].position <number>	# Position of the action in the receipt rule.
status.atProvider.bounceAction[].sender <string>	# Email address of the sender.
status.atProvider.bounceAction[].smtpReplyCode <string>	# RFC 5321 SMTP reply code.
status.atProvider.bounceAction[].statusCode <string>	# RFC 3463 SMTP enhanced status code.
status.atProvider.bounceAction[].topicArn <string>	# ARN of an SNS topic to notify.
status.atProvider.enabled <boolean>	# If true, the rule will be enabled.
status.atProvider.id <string>	# SES receipt rule name.
status.atProvider.lambdaAction <[]object>	# Configuration block for calling an AWS Lambda function. Detailed below.
status.atProvider.lambdaAction[].functionArn <string>	# ARN of the Lambda function to invoke.
status.atProvider.lambdaAction[].invocationType <string>	# Event or RequestResponse.
status.atProvider.lambdaAction[].position <number>	# Position of the action in the receipt rule.
status.atProvider.lambdaAction[].topicArn <string>	# ARN of an SNS topic to notify.
status.atProvider.name <string>	# Name of the rule.
status.atProvider.recipients <[]string>	# List of email addresses.
status.atProvider.region <string>	# Region where this resource will be managed. Defaults to the Region set in the provider configuration. Region is the region you'd like your resource to be created in.
status.atProvider.ruleSetName <string>	# Name of the rule set.
status.atProvider.s3Action <[]object>	# Configuration block for storing received emails in an S3 bucket. Detailed below.
status.atProvider.s3Action[].bucketName <string>	# Name of the S3 bucket.
status.atProvider.s3Action[].iamRoleArn <string>	# ARN of the IAM role to be used by Amazon Simple Email Service while writing to the Amazon S3 bucket, optionally encrypting your mail via the provided customer managed key, and publishing to the Amazon SNS topic.
status.atProvider.s3Action[].kmsKeyArn <string>	# ARN of the KMS key.
status.atProvider.s3Action[].objectKeyPrefix <string>	# Key prefix of the S3 bucket.
status.atProvider.s3Action[].position <number>	# Position of the action in the receipt rule.
status.atProvider.s3Action[].topicArn <string>	# ARN of an SNS topic to notify.
status.atProvider.scanEnabled <boolean>	# If true, incoming emails will be scanned for spam and viruses.
status.atProvider.snsAction <[]object>	# Configuration block for publishing to an SNS topic. Detailed below.
status.atProvider.snsAction[].encoding <string>	# Encoding to use for the email within the Amazon SNS notification. Default value is UTF-8.
status.atProvider.snsAction[].position <number>	# Position of the action in the receipt rule.
status.atProvider.snsAction[].topicArn <string>	# ARN of an SNS topic to notify.
status.atProvider.stopAction <[]object>	# Configuration block for terminating the evaluation of the receipt rule set. Detailed below.
status.atProvider.stopAction[].position <number>	# Position of the action in the receipt rule.
status.atProvider.stopAction[].scope <string>	# Scope to apply. The only acceptable value is RuleSet.
status.atProvider.stopAction[].topicArn <string>	# ARN of an SNS topic to notify.
status.atProvider.tlsPolicy <string>	# Require or Optional.
status.atProvider.workmailAction <[]object>	# Configuration block for calling Amazon WorkMail. Detailed below.
status.atProvider.workmailAction[].organizationArn <string>	# ARN of the WorkMail organization.
status.atProvider.workmailAction[].position <number>	# Position of the action in the receipt rule.
status.atProvider.workmailAction[].topicArn <string>	# ARN of an SNS topic to notify.
status.conditions <[]object>	# Conditions of the resource.
status.conditions[].lastTransitionTime <string> (required) format=date-time	# LastTransitionTime is the last time this condition transitioned from one status to another.
status.conditions[].message <string>	# A Message containing details about this condition's last transition from one status to another, if any.
status.conditions[].observedGeneration <integer> format=int64	# ObservedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
status.conditions[].reason <string> (required)	# A Reason for this condition's last transition from one status to another.
status.conditions[].status <string> (required)	# Status of this condition; is it currently True, False, or Unknown?
status.conditions[].type <string> (required)	# Type of this condition. At most one of each condition type may apply to a resource at any point in time.
status.lastHandledReconcileAt <string>	# LastHandledReconcileAt holds the value of the most recent reconcile-requested-at annotation token that the controller has processed. Users can compare this to the annotation to determine whether a reconcile request has been handled.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the latest metadata.generation which resulted in either a ready state, or stalled due to error it can not recover from without human intervention.
