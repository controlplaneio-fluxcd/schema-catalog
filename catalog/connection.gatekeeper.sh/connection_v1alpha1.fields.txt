# schema source: OPA Gatekeeper v3.23.0 https://github.com/open-policy-agent/gatekeeper
apiVersion <string> enum=connection.gatekeeper.sh/v1alpha1
kind <string> enum=Connection
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# ConnectionSpec defines the desired state of Connection.
spec.config <any> (required)
spec.driver <string> (required)	# Driver is the name of one of the expected drivers i.e. dapr, disk
status <object>	# ConnectionStatus defines the observed state of Connection.
status.byPod <[]object>
status.byPod[].active <boolean>	# Indicator for alive connection with at least one successful publish
status.byPod[].connectionUID <string>	# UID is a type that holds unique ID values, including UUIDs. Because we don't ONLY use UUIDs, this is an alias to string. Being a type captures intent and helps make sure that UIDs and names do not get conflated.
status.byPod[].errors <[]object>
status.byPod[].errors[].message <string> (required)
status.byPod[].errors[].type <string> (required)
status.byPod[].id <string>	# ID is the unique identifier for the pod that wrote the status
status.byPod[].observedGeneration <integer> format=int64
status.byPod[].operations <[]string>
