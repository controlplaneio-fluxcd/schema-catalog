# schema source: NVIDIA Network Operator v26.1.0 https://github.com/Mellanox/network-operator
apiVersion <string> enum=mellanox.com/v1alpha1
kind <string> enum=HostDeviceNetwork (cluster-scoped)
metadata.name <string> (required)
spec <object>	# Defines the desired state of HostDeviceNetwork
spec.ipam <string>	# IPAM configuration to be used for this network
spec.networkNamespace <string>	# Namespace of the NetworkAttachmentDefinition custom resource
spec.resourceName <string>	# Host device resource pool name
status <object>	# Defines the observed state of HostDeviceNetwork
status.appliedStates <[]object>	# AppliedStates provide a finer view of the observed state
status.appliedStates[].message <string>	# Message is a human readable message indicating details about why the state is in this condition
status.appliedStates[].name <string> (required)	# Name of the deployed component this state refers to
status.appliedStates[].state <string> (required) enum=ready|notReady|ignore|error	# The state of the deployed component. ("ready", "notReady", "ignore", "error")
status.hostDeviceNetworkAttachmentDef <string>	# Network attachment definition generated from HostDeviceNetworkSpec
status.reason <string>	# Informative string in case the observed state is error
status.state <string> (required) enum=notReady|ready|error	# Reflects the state of the HostDeviceNetwork
