# schema source: NVIDIA Network Operator v26.1.2 https://github.com/Mellanox/network-operator
apiVersion <string> enum=mellanox.com/v1alpha1
kind <string> enum=MacvlanNetwork (cluster-scoped)
metadata.name <string> (required)
spec <object>	# Defines the desired state of MacvlanNetworkSpec
spec.ipam <string>	# IPAM configuration to be used for this network.
spec.master <string>	# Name of the host interface to enslave. Defaults to default route interface
spec.mode <string> enum=bridge|private|vepa|passthru	# Mode of interface one of "bridge", "private", "vepa", "passthru"
spec.mtu <integer> min=0	# MTU of interface to the specified value. 0 for master's MTU
spec.networkNamespace <string>	# Namespace of the NetworkAttachmentDefinition custom resource
status <object>	# Defines the observed state of MacvlanNetwork
status.macvlanNetworkAttachmentDef <string>	# Network attachment definition generated from MacvlanNetworkSpec
status.reason <string>	# Informative string in case the observed state is error
status.state <string> (required) enum=notReady|ready|error	# Reflects the state of the MacvlanNetwork
