# schema source: NVIDIA Network Operator v26.1.0 https://github.com/Mellanox/network-operator
apiVersion <string> enum=mellanox.com/v1alpha1
kind <string> enum=IPoIBNetwork (cluster-scoped)
metadata.name <string> (required)
spec <object>	# Defines the desired state of IPoIBNetwork
spec.ipam <string>	# IPAM configuration to be used for this network.
spec.master <string>	# Name of the host interface to enslave. Defaults to default route interface
spec.networkNamespace <string>	# Namespace of the NetworkAttachmentDefinition custom resource
status <object>	# Defines the observed state of IPoIBNetwork
status.ipoibNetworkAttachmentDef <string>	# Network attachment definition generated from IPoIBNetworkSpec
status.reason <string>	# Informative string in case the observed state is error
status.state <string> (required) enum=notReady|ready|error	# Reflects the state of the IPoIBNetwork
