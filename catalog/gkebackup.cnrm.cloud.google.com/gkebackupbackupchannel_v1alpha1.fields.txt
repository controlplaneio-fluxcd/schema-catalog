# schema source: GCP Config Connector v1.155.1 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=gkebackup.cnrm.cloud.google.com/v1alpha1
kind <string> enum=GKEBackupBackupChannel
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)	# GKEBackupBackupChannelSpec defines the desired state of GKEBackupBackupChannel
spec.description <string>	# Optional. User specified descriptive string for this BackupChannel.
spec.destinationProjectRef <object> (required)	# Required. Immutable. The project where Backups are allowed to be stored. The format is `projects/{projectId}` or `projects/{projectNumber}`.
spec.destinationProjectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.destinationProjectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.destinationProjectRef.name <string>	# The `name` field of a `Project` resource.
spec.destinationProjectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.labels <map[string]string>	# Optional. A set of custom labels supplied by user.
spec.location <string> (required)	# The location of this resource.
spec.projectRef <object> (required)	# The project that this resource belongs to.
spec.projectRef.external <string>	# The `projectID` field of a project, when not managed by Config Connector.
spec.projectRef.kind <string>	# The kind of the Project resource; optional but must be `Project` if provided.
spec.projectRef.name <string>	# The `name` field of a `Project` resource.
spec.projectRef.namespace <string>	# The `namespace` field of a `Project` resource.
spec.resourceID <string>	# The GKEBackupBackupChannel name. If not given, the metadata.name will be used.
status <object>	# GKEBackupBackupChannelStatus defines the config connector machine state of GKEBackupBackupChannel
status.conditions <[]object>	# Conditions represent the latest available observations of the object's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.externalRef <string>	# A unique specifier for the GKEBackupBackupChannel resource in GCP.
status.observedGeneration <integer> format=int64	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.observedState <object>	# ObservedState is the state of the resource as most recently observed in GCP.
status.observedState.createTime <string>	# Output only. The timestamp when this BackupChannel resource was created.
status.observedState.destinationProjectID <string>	# Output only. The project_id where Backups are allowed to be stored.
status.observedState.etag <string>	# Output only. `etag` is used for optimistic concurrency control as a way to help prevent simultaneous updates of a BackupChannel from overwriting each other.
status.observedState.uid <string>	# Output only. Server generated global unique identifier of [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format.
status.observedState.updateTime <string>	# Output only. The timestamp when this BackupChannel resource was last updated.
