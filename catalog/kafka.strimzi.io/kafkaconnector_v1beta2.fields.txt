# schema source: Strimzi 0.51.0 https://github.com/strimzi/strimzi-kafka-operator
apiVersion <string> enum=kafka.strimzi.io/v1beta2 (deprecated)	# Version v1beta2 of the KafkaConnector API is deprecated. Please use the v1 version instead.
kind <string> enum=KafkaConnector
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# The specification of the Kafka Connector.
spec.alterOffsets <object>	# Configuration for altering offsets.
spec.alterOffsets.fromConfigMap <object> (required)	# Reference to the ConfigMap where the new offsets are stored.
spec.alterOffsets.fromConfigMap.name <string>
spec.autoRestart <object>	# Automatic restart of connector and tasks configuration.
spec.autoRestart.enabled <boolean>	# Whether automatic restart for failed connectors and tasks should be enabled or disabled.
spec.autoRestart.maxRestarts <integer>	# The maximum number of connector restarts that the operator will try. If the connector remains in a failed state after reaching this limit, it must be restarted manually by the user. Defaults to an unlimited number of restarts.
spec.class <string>	# The Class for the Kafka Connector.
spec.config <object (free-form)>	# The Kafka Connector configuration. The following properties cannot be set: name, connector.class, tasks.max, connector.plugin.version.
spec.listOffsets <object>	# Configuration for listing offsets.
spec.listOffsets.toConfigMap <object> (required)	# Reference to the ConfigMap where the list of offsets will be written to.
spec.listOffsets.toConfigMap.name <string>
spec.pause <boolean>	# Whether the connector should be paused. Defaults to false.
spec.state <string> enum=paused|stopped|running	# The state the connector should be in. Defaults to running.
spec.tasksMax <integer> min=1	# The maximum number of tasks for the Kafka Connector.
spec.version <string>	# Desired version or version range to respect when starting the Kafka Connector. This is only supported when using Kafka Connect version 4.1.0 and higher.
status <object>	# The status of the Kafka Connector.
status.autoRestart <object>	# The auto restart status.
status.autoRestart.connectorName <string>	# The name of the connector being restarted.
status.autoRestart.count <integer>	# The number of times the connector or task is restarted.
status.autoRestart.lastRestartTimestamp <string>	# The last time the automatic restart was attempted. The required format is 'yyyy-MM-ddTHH:mm:ssZ' in the UTC time zone.
status.conditions <[]object>	# List of status conditions.
status.conditions[].lastTransitionTime <string>	# Last time the condition of a type changed from one status to another. The required format is 'yyyy-MM-ddTHH:mm:ssZ', in the UTC time zone.
status.conditions[].message <string>	# Human-readable message indicating details about the condition's last transition.
status.conditions[].reason <string>	# The reason for the condition's last transition (a single word in CamelCase).
status.conditions[].status <string>	# The status of the condition, either True, False or Unknown.
status.conditions[].type <string>	# The unique identifier of a condition, used to distinguish between other conditions in the resource.
status.connectorStatus <object (free-form)>	# The connector status, as reported by the Kafka Connect REST API.
status.observedGeneration <integer>	# The generation of the CRD that was last reconciled by the operator.
status.tasksMax <integer>	# The maximum number of tasks for the Kafka Connector.
status.topics <[]string>	# The list of topics used by the Kafka Connector.
