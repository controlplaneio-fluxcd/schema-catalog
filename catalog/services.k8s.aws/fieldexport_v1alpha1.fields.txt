# schema source: AWS ACK Runtime v0.62.0 https://github.com/aws-controllers-k8s/runtime
apiVersion <string> enum=services.k8s.aws/v1alpha1
kind <string> enum=FieldExport
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object>	# FieldExportSpec defines the desired state of the FieldExport.
spec.from <object> (required)	# ResourceFieldSelector provides the values necessary to identify an individual field on an individual K8s resource.
spec.from.path <string> (required)
spec.from.resource <object> (required)	# NamespacedResource provides all the values necessary to identify an ACK resource of a given type (within the same namespace as the custom resource containing this type).
spec.from.resource.group <string> (required)
spec.from.resource.kind <string> (required)
spec.from.resource.name <string> (required)
spec.to <object> (required)	# FieldExportTarget provides the values necessary to identify the output path for a field export.
spec.to.key <string>	# Key overrides the default value (`<namespace>.<FieldExport-resource-name>`) for the FieldExport target
spec.to.kind <string> (required) enum=configmap|secret	# FieldExportOutputType represents all types that can be produced by a field export operation
spec.to.name <string> (required)
spec.to.namespace <string>	# Namespace is marked as optional, so we cannot compose `NamespacedName`
status <object>	# FieldExportStatus defines the observed status of the FieldExport.
status.conditions <[]object> (required)	# A collection of `ackv1alpha1.Condition` objects that describe the various recoverable states of the field CR
status.conditions[].lastTransitionTime <string> format=date-time	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# A human readable message indicating details about the transition.
status.conditions[].reason <string>	# The reason for the condition's last transition.
status.conditions[].status <string> (required)	# Status of the condition, one of True, False, Unknown.
status.conditions[].type <string> (required)	# Type is the type of the Condition
