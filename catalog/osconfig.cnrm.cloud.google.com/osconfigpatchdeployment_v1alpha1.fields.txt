# schema source: GCP Config Connector v1.152.0 https://github.com/GoogleCloudPlatform/k8s-config-connector
apiVersion <string> enum=osconfig.cnrm.cloud.google.com/v1alpha1
kind <string> enum=OSConfigPatchDeployment
metadata.name <string> (required)
metadata.namespace <string> (required)
spec <object> (required)
spec.description <string>	# Immutable. Description of the patch deployment. Length of the description is limited to 1024 characters.
spec.duration <string>	# Immutable. Duration of the patch. After the duration ends, the patch times out. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
spec.instanceFilter <object> (required)	# Immutable. VM instances to patch.
spec.instanceFilter.all <boolean>	# Immutable. Target all VM instances in the project. If true, no other criteria is permitted.
spec.instanceFilter.groupLabels <[]object>	# Immutable. Targets VM instances matching ANY of these GroupLabels. This allows targeting of disparate groups of VM instances.
spec.instanceFilter.groupLabels[].labels <map[string]string> (required)	# Immutable. Compute Engine instance labels that must be present for a VM instance to be targeted by this filter.
spec.instanceFilter.instanceNamePrefixes <[]string>	# Immutable. Targets VMs whose name starts with one of these prefixes. Similar to labels, this is another way to group VMs when targeting configs, for example prefix="prod-".
spec.instanceFilter.instances <[]string>	# Immutable. Targets any of the VM instances specified. Instances are specified by their URI in the 'form zones/{{zone}}/instances/{{instance_name}}', 'projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}', or 'https://www.googleapis.com/compute/v1/projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}'.
spec.instanceFilter.zones <[]string>	# Immutable. Targets VM instances in ANY of these zones. Leave empty to target VM instances in any zone.
spec.oneTimeSchedule <object>	# Immutable. Schedule a one-time execution.
spec.oneTimeSchedule.executeTime <string> (required)	# Immutable. The desired patch job execution time. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
spec.patchConfig <object>	# Immutable. Patch configuration that is applied.
spec.patchConfig.apt <object>	# Immutable. Apt update settings. Use this setting to override the default apt patch rules.
spec.patchConfig.apt.excludes <[]string>	# Immutable. List of packages to exclude from update. These packages will be excluded.
spec.patchConfig.apt.exclusivePackages <[]string>	# Immutable. An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field cannot be specified with any other patch configuration fields.
spec.patchConfig.apt.type <string>	# Immutable. By changing the type to DIST, the patching is performed using apt-get dist-upgrade instead. Possible values: ["DIST", "UPGRADE"].
spec.patchConfig.goo <object>	# Immutable. goo update settings. Use this setting to override the default goo patch rules.
spec.patchConfig.goo.enabled <boolean> (required)	# Immutable. goo update settings. Use this setting to override the default goo patch rules.
spec.patchConfig.migInstancesAllowed <boolean>	# Immutable. Allows the patch job to run on Managed instance groups (MIGs).
spec.patchConfig.postStep <object>	# Immutable. The ExecStep to run after the patch update.
spec.patchConfig.postStep.linuxExecStepConfig <object>	# Immutable. The ExecStepConfig for all Linux VMs targeted by the PatchJob.
spec.patchConfig.postStep.linuxExecStepConfig.allowedSuccessCodes <[]integer>	# Immutable. Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
spec.patchConfig.postStep.linuxExecStepConfig.gcsObject <object>	# Immutable. A Cloud Storage object containing the executable.
spec.patchConfig.postStep.linuxExecStepConfig.gcsObject.bucket <string> (required)	# Immutable. Bucket of the Cloud Storage object.
spec.patchConfig.postStep.linuxExecStepConfig.gcsObject.generationNumber <string> (required)	# Immutable. Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
spec.patchConfig.postStep.linuxExecStepConfig.gcsObject.object <string> (required)	# Immutable. Name of the Cloud Storage object.
spec.patchConfig.postStep.linuxExecStepConfig.interpreter <string>	# Immutable. The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with shebang lines. Possible values: ["SHELL", "POWERSHELL"].
spec.patchConfig.postStep.linuxExecStepConfig.localPath <string>	# Immutable. An absolute path to the executable on the VM.
spec.patchConfig.postStep.windowsExecStepConfig <object>	# Immutable. The ExecStepConfig for all Windows VMs targeted by the PatchJob.
spec.patchConfig.postStep.windowsExecStepConfig.allowedSuccessCodes <[]integer>	# Immutable. Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
spec.patchConfig.postStep.windowsExecStepConfig.gcsObject <object>	# Immutable. A Cloud Storage object containing the executable.
spec.patchConfig.postStep.windowsExecStepConfig.gcsObject.bucket <string> (required)	# Immutable. Bucket of the Cloud Storage object.
spec.patchConfig.postStep.windowsExecStepConfig.gcsObject.generationNumber <string> (required)	# Immutable. Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
spec.patchConfig.postStep.windowsExecStepConfig.gcsObject.object <string> (required)	# Immutable. Name of the Cloud Storage object.
spec.patchConfig.postStep.windowsExecStepConfig.interpreter <string>	# Immutable. The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with shebang lines. Possible values: ["SHELL", "POWERSHELL"].
spec.patchConfig.postStep.windowsExecStepConfig.localPath <string>	# Immutable. An absolute path to the executable on the VM.
spec.patchConfig.preStep <object>	# Immutable. The ExecStep to run before the patch update.
spec.patchConfig.preStep.linuxExecStepConfig <object>	# Immutable. The ExecStepConfig for all Linux VMs targeted by the PatchJob.
spec.patchConfig.preStep.linuxExecStepConfig.allowedSuccessCodes <[]integer>	# Immutable. Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
spec.patchConfig.preStep.linuxExecStepConfig.gcsObject <object>	# Immutable. A Cloud Storage object containing the executable.
spec.patchConfig.preStep.linuxExecStepConfig.gcsObject.bucket <string> (required)	# Immutable. Bucket of the Cloud Storage object.
spec.patchConfig.preStep.linuxExecStepConfig.gcsObject.generationNumber <string> (required)	# Immutable. Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
spec.patchConfig.preStep.linuxExecStepConfig.gcsObject.object <string> (required)	# Immutable. Name of the Cloud Storage object.
spec.patchConfig.preStep.linuxExecStepConfig.interpreter <string>	# Immutable. The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with shebang lines. Possible values: ["SHELL", "POWERSHELL"].
spec.patchConfig.preStep.linuxExecStepConfig.localPath <string>	# Immutable. An absolute path to the executable on the VM.
spec.patchConfig.preStep.windowsExecStepConfig <object>	# Immutable. The ExecStepConfig for all Windows VMs targeted by the PatchJob.
spec.patchConfig.preStep.windowsExecStepConfig.allowedSuccessCodes <[]integer>	# Immutable. Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
spec.patchConfig.preStep.windowsExecStepConfig.gcsObject <object>	# Immutable. A Cloud Storage object containing the executable.
spec.patchConfig.preStep.windowsExecStepConfig.gcsObject.bucket <string> (required)	# Immutable. Bucket of the Cloud Storage object.
spec.patchConfig.preStep.windowsExecStepConfig.gcsObject.generationNumber <string> (required)	# Immutable. Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
spec.patchConfig.preStep.windowsExecStepConfig.gcsObject.object <string> (required)	# Immutable. Name of the Cloud Storage object.
spec.patchConfig.preStep.windowsExecStepConfig.interpreter <string>	# Immutable. The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with shebang lines. Possible values: ["SHELL", "POWERSHELL"].
spec.patchConfig.preStep.windowsExecStepConfig.localPath <string>	# Immutable. An absolute path to the executable on the VM.
spec.patchConfig.rebootConfig <string>	# Immutable. Post-patch reboot settings. Possible values: ["DEFAULT", "ALWAYS", "NEVER"].
spec.patchConfig.windowsUpdate <object>	# Immutable. Windows update settings. Use this setting to override the default Windows patch rules.
spec.patchConfig.windowsUpdate.classifications <[]string>	# Immutable. Only apply updates of these windows update classifications. If empty, all updates are applied. Possible values: ["CRITICAL", "SECURITY", "DEFINITION", "DRIVER", "FEATURE_PACK", "SERVICE_PACK", "TOOL", "UPDATE_ROLLUP", "UPDATE"].
spec.patchConfig.windowsUpdate.excludes <[]string>	# Immutable. List of KBs to exclude from update.
spec.patchConfig.windowsUpdate.exclusivePatches <[]string>	# Immutable. An exclusive list of kbs to be updated. These are the only patches that will be updated. This field must not be used with other patch configurations.
spec.patchConfig.yum <object>	# Immutable. Yum update settings. Use this setting to override the default yum patch rules.
spec.patchConfig.yum.excludes <[]string>	# Immutable. List of packages to exclude from update. These packages will be excluded.
spec.patchConfig.yum.exclusivePackages <[]string>	# Immutable. An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field cannot be specified with any other patch configuration fields.
spec.patchConfig.yum.minimal <boolean>	# Immutable. Will cause patch to run yum update-minimal instead.
spec.patchConfig.yum.security <boolean>	# Immutable. Adds the --security flag to yum update. Not supported on all platforms.
spec.patchConfig.zypper <object>	# Immutable. zypper update settings. Use this setting to override the default zypper patch rules.
spec.patchConfig.zypper.categories <[]string>	# Immutable. Install only patches with these categories. Common categories include security, recommended, and feature.
spec.patchConfig.zypper.excludes <[]string>	# Immutable. List of packages to exclude from update.
spec.patchConfig.zypper.exclusivePatches <[]string>	# Immutable. An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command. This field must not be used with any other patch configuration fields.
spec.patchConfig.zypper.severities <[]string>	# Immutable. Install only patches with these severities. Common severities include critical, important, moderate, and low.
spec.patchConfig.zypper.withOptional <boolean>	# Immutable. Adds the --with-optional flag to zypper patch.
spec.patchConfig.zypper.withUpdate <boolean>	# Immutable. Adds the --with-update flag, to zypper patch.
spec.patchDeploymentId <string> (required)	# Immutable. A name for the patch deployment in the project. When creating a name the following rules apply: * Must contain only lowercase letters, numbers, and hyphens. * Must start with a letter. * Must be between 1-63 characters. * Must end with a number or a letter. * Must be unique within the project.
spec.projectRef <object> (required)	# The project that this resource belongs to.
spec.projectRef.external <string>	# Allowed value: The `name` field of a `Project` resource.
spec.projectRef.name <string>	# Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
spec.projectRef.namespace <string>	# Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
spec.recurringSchedule <object>	# Immutable. Schedule recurring executions.
spec.recurringSchedule.endTime <string>	# Immutable. The end time at which a recurring patch deployment schedule is no longer active. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
spec.recurringSchedule.lastExecuteTime <string>	# The time the last patch job ran successfully. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
spec.recurringSchedule.monthly <object>	# Immutable. Schedule with monthly executions.
spec.recurringSchedule.monthly.monthDay <integer>	# Immutable. One day of the month. 1-31 indicates the 1st to the 31st day. -1 indicates the last day of the month. Months without the target day will be skipped. For example, a schedule to run "every month on the 31st" will not run in February, April, June, etc.
spec.recurringSchedule.monthly.weekDayOfMonth <object>	# Immutable. Week day in a month.
spec.recurringSchedule.monthly.weekDayOfMonth.dayOfWeek <string> (required)	# Immutable. A day of the week. Possible values: ["MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY", "SATURDAY", "SUNDAY"].
spec.recurringSchedule.monthly.weekDayOfMonth.weekOrdinal <integer> (required)	# Immutable. Week number in a month. 1-4 indicates the 1st to 4th week of the month. -1 indicates the last week of the month.
spec.recurringSchedule.nextExecuteTime <string>	# The time the next patch job is scheduled to run. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
spec.recurringSchedule.startTime <string>	# Immutable. The time that the recurring schedule becomes effective. Defaults to createTime of the patch deployment. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
spec.recurringSchedule.timeOfDay <object> (required)	# Immutable. Time of the day to run a recurring deployment.
spec.recurringSchedule.timeOfDay.hours <integer>	# Immutable. Hours of day in 24 hour format. Should be from 0 to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time.
spec.recurringSchedule.timeOfDay.minutes <integer>	# Immutable. Minutes of hour of day. Must be from 0 to 59.
spec.recurringSchedule.timeOfDay.nanos <integer>	# Immutable. Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999.
spec.recurringSchedule.timeOfDay.seconds <integer>	# Immutable. Seconds of minutes of the time. Must normally be from 0 to 59. An API may allow the value 60 if it allows leap-seconds.
spec.recurringSchedule.timeZone <object> (required)	# Immutable. Defines the time zone that timeOfDay is relative to. The rules for daylight saving time are determined by the chosen time zone.
spec.recurringSchedule.timeZone.id <string> (required)	# Immutable. IANA Time Zone Database time zone, e.g. "America/New_York".
spec.recurringSchedule.timeZone.version <string>	# Immutable. IANA Time Zone Database version number, e.g. "2019a".
spec.recurringSchedule.weekly <object>	# Immutable. Schedule with weekly executions.
spec.recurringSchedule.weekly.dayOfWeek <string> (required)	# Immutable. IANA Time Zone Database time zone, e.g. "America/New_York". Possible values: ["MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY", "SATURDAY", "SUNDAY"].
spec.resourceID <string>	# Immutable. Optional. The service-generated name of the resource. Used for acquisition only. Leave unset to create a new resource.
spec.rollout <object>	# Immutable. Rollout strategy of the patch job.
spec.rollout.disruptionBudget <object> (required)	# Immutable. The maximum number (or percentage) of VMs per zone to disrupt at any given moment. The number of VMs calculated from multiplying the percentage by the total number of VMs in a zone is rounded up. During patching, a VM is considered disrupted from the time the agent is notified to begin until patching has completed. This disruption time includes the time to complete reboot and any post-patch steps. A VM contributes to the disruption budget if its patching operation fails either when applying the patches, running pre or post patch steps, or if it fails to respond with a success notification before timing out. VMs that are not running or do not have an active agent do not count toward this disruption budget. For zone-by-zone rollouts, if the disruption budget in a zone is exceeded, the patch job stops, because continuing to the next zone requires completion of the patch process in the previous zone. For example, if the disruption budget has a fixed value of 10, and 8 VMs fail to patch in the current zone, the patch job continues to patch 2 VMs at a time until the zone is completed. When that zone is completed successfully, patching begins with 10 VMs at a time in the next zone. If 10 VMs in the next zone fail to patch, the patch job stops.
spec.rollout.disruptionBudget.fixed <integer>	# Immutable. Specifies a fixed value.
spec.rollout.disruptionBudget.percentage <integer>	# Immutable. Specifies the relative value defined as a percentage, which will be multiplied by a reference value.
spec.rollout.mode <string> (required)	# Immutable. Mode of the patch rollout. Possible values: ["ZONE_BY_ZONE", "CONCURRENT_ZONES"].
status <object>
status.conditions <[]object>	# Conditions represent the latest available observation of the resource's current state.
status.conditions[].lastTransitionTime <string>	# Last time the condition transitioned from one status to another.
status.conditions[].message <string>	# Human-readable message indicating details about last transition.
status.conditions[].reason <string>	# Unique, one-word, CamelCase reason for the condition's last transition.
status.conditions[].status <string>	# Status is the status of the condition. Can be True, False, Unknown.
status.conditions[].type <string>	# Type is the type of the condition.
status.createTime <string>	# Time the patch deployment was created. Timestamp is in RFC3339 text format. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
status.lastExecuteTime <string>	# The last time a patch job was started by this deployment. Timestamp is in RFC3339 text format. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
status.name <string>	# Unique name for the patch deployment resource in a project. The patch deployment name is in the form: projects/{project_id}/patchDeployments/{patchDeploymentId}.
status.observedGeneration <integer>	# ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
status.updateTime <string>	# Time the patch deployment was last updated. Timestamp is in RFC3339 text format. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
